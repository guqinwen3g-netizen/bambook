# Suppliers 供应商 — 模块概述

> 模块代号：`Suppliers / Supplier Management`
> 主目录：`docs/design/04-模块设计/06-资源与支撑/Suppliers-供应商/`
> 关联代码：`components/SuppliersManager.tsx`、`server/src/suppliers/factoryRoute.ts / factoryRouteV2.ts / factoryService.ts`、`server/prisma/schema.prisma` 3084–3193 行
> 文档版本：v1.0 · 最后更新：2026-08-15

---

## §1 实现状态

| 维度 | 状态 | 说明 |
| --- | --- | --- |
| FactoryProfile 模型 | ✅ 已落地 | schema.prisma 3084–3129 行，`relationId @unique` 1:1 挂 Relation，承载产能/专长/评分/银行/黑名单。 |
| FactoryEvaluation 模型 | ✅ 已落地 | schema.prisma 3132–3151 行，`kind = inspection/delivery`，append-only 评分明细真源。 |
| FactoryCertification 模型 | ✅ 已落地 | schema.prisma 3154–3172 行，含 `validUntil` 有效期，预警扫描对象。 |
| FactoryCapacity 模型 | ✅ 已落地 | schema.prisma 3176–3193 行，月计划产能 upsert，占用量实时聚合不落地。 |
| 服务层 | ✅ 已落地 | `server/src/suppliers/factoryService.ts`（核心服务）、`factoryRoute.ts / factoryRouteV2.ts`（REST 端点）。 |
| 评分重算 | ✅ 已落地 | 事务内均值重算 `qualityScore / deliveryScore` 缓存（factoryService.ts 323–332 行）。 |
| 自动评分 | ✅ 已落地 | `recordAutoEvaluation` 幂等入口（H1c），验货/交货事件自动追加评分。 |
| 产能占用聚合 | ✅ 已落地 | `aggregateOccupied`（factoryService.ts 504–523 行），按在手 PO `expectedDeliveryDate` 落月聚合。 |
| 黑名单 | ✅ 已落地 | `setBlacklist / clearBlacklist / assertNotBlacklisted`，拉黑后新建采购单禁选。 |
| 认证预警 | ✅ 已落地 | `listExpiringCertifications(days)` 按到期日扫描。 |
| 前端组件 | ✅ 已落地 | `components/SuppliersManager.tsx`（1623 行），左排名列表 + 右 360° Tabs（总览/评分/认证/产能）。 |
| 跨模块落点 | ✅ 已落地 | `primeSuppliersFactoryPreview` 跨模块跳转（Relations → Suppliers）。 |
| 测试 | ✅ 已落地 | `server/src/suppliers/__tests__/factoryRoute.test.ts`。 |

---

## §2 业务定位与目标

### 2.1 业务定位

`Suppliers` 是 Bambook 服装生产管理系统的「供应商中枢」——承载服装工厂、面料厂、辅料厂等所有生产供应商的工厂属性、评分、认证、产能、银行、黑名单等管理数据。

**身份真源在 `Relation`（category=Supplier 的组织）**，`FactoryProfile` 以 1:1 关联 Relation 承载工厂专属属性。这是与 CRM 模块一致的设计原则：身份在 Relation，业务属性在 Profile。

### 2.2 核心目标

1. **一厂一档**：每个 `FactoryProfile` 唯一 `relationId` 挂 Relation。
2. **360° 视图**：总览 / 评分记录 / 认证管理 / 产能日历四 Tab 完整呈现工厂全貌。
3. **自动评分**：验货/交货业务事件自动追加评分，事务内重算缓存分。
4. **产能可视化**：月计划产能 + 在手采购单占用实时聚合，忙闲一目了然。
5. **认证预警**：30 天内到期认证顶部横幅提醒，剩余天数色阶（success/warning/danger）。
6. **黑名单管控**：拉黑后禁止新建采购单，服务端 `assertNotBlacklisted` 强制校验。
7. **跨模块落点**：Relations → Suppliers 跨模块跳转，支持黑名单工厂落点。

### 2.3 设计原则

- **软删除**（`deletedAt BigInt?`），不物理删除。
- **事务内写入 + 评分重算**（fail closed）：评分追加同事务重算缓存，禁止客户端直改。
- **事件发布失败不阻断业务**（fire-and-forget）。
- **BDS v2.1 主题透明**：bds 组件类 + 语义 token，无 isDarkMode 样式分支，无阴影，大圆角。

---

## §3 核心概念与术语

| 术语 | 定义 |
| --- | --- |
| 工厂档案 `FactoryProfile` | 供应商工厂属性扩展（1:1 挂 Relation） |
| 评估记录 `FactoryEvaluation` | 验货/交货评分明细（append-only 真源） |
| 评分类型 `kind` | `inspection`（验货质量） / `delivery`（交期达成） |
| 评分缓存 `qualityScore / deliveryScore` | FactoryProfile 上的缓存分（0-100，事务内重算） |
| 来源类型 `sourceType` | `inspectionReport / shipment / purchaseOrder / manual` |
| 认证记录 `FactoryCertification` | BSCI/SEDEX/WRAP/ISO9001 等认证 + 有效期 |
| 产能日历 `FactoryCapacity` | 月计划产能（YYYY-MM + capacity + unit） |
| 产能占用 `occupied` | 在手 PO 按交期落月聚合的占用量（不落地，实时算） |
| 黑名单 `blacklistedAt + blacklistReason + blacklistedById` | 拉黑后禁新建采购单 |
| 价位水平 `priceLevel` | High / Mid / Low |
| 专长 `specialties` | 擅长品类数组（西装/大衣/衬衫/针织…） |
| 银行信息 `bankName / bankAccount / bankSwift` | 收款信息 |
| 360° 总览 `FactoryOverview` | profile + evaluations + certifications + capacity 聚合 |

---

## §4 数据模型概览

### 4.1 聚合根关系图

```
Relation (category=Supplier, isOrganization=true)  ← 身份真源
    │ 1:1 @unique (relationId)
    ▼
FactoryProfile  ← 工厂属性扩展
    │ 1:N (factoryId)
    ├─► FactoryEvaluation (评分明细 append-only)
    ├─► FactoryCertification (认证记录 + 有效期)
    └─► FactoryCapacity (月产能日历)

FactoryProfile
    │ 字段引用
    ├─► qualityScore / deliveryScore（缓存分，事务内重算）
    ├─► blacklistedAt / blacklistReason / blacklistedById（黑名单）
    └─► bankName / bankAccount / bankSwift（银行信息）

FactoryCapacity
    │ 占用量 occupied 实时聚合
    ▼
PurchaseOrder (supplierRelationId, expectedDeliveryDate)
    └─► PurchaseLine (quantity)  → 按落月累加 = occupied
```

### 4.2 关键约束

- `FactoryProfile.relationId` 全局 `@unique`，1:1 挂 Relation，`onDelete: Cascade`。
- `FactoryCapacity` 上 `@@unique([factoryId, month])`，保证一厂一月一行。
- 评分缓存 `qualityScore / deliveryScore` 由服务端事务内重算，禁止客户端直改。
- 产能占用 `occupied` 不落地（实时聚合），口径变更无需回填。
- 黑名单 `blacklistedAt` 非空时禁止新建采购单（`assertNotBlacklisted` 服务端强制）。

### 4.3 ID 生成

- `FactoryProfile.id`：`FACP__${shortId}`（crypto.randomBytes base64url）。
- `FactoryEvaluation.id`：`FAEV__${shortId}`。
- `FactoryCertification.id`：`FACR__${shortId}`。
- `FactoryCapacity.id`：`FACC__${shortId}`。

---

## §5 关键流程

### 5.1 工厂档案创建流程

```
1. 用户在 SuppliersManager 点击「新建工厂档案」
2. 选择供应商（category=Supplier 的 Relation 组织，前端过滤已建档案）
3. 填产能：monthlyCapacity + capacityUnit (PC/M)
4. 填工人数量、设备清单、擅长品类
5. 填价位水平、首次合作日期
6. 填银行信息：bankName / bankAccount / bankSwift
7. 填备注
8. 提交 → POST /api/factories
9. 服务端校验 Relation 是未删除的 Supplier 组织
10. $transaction 写入 FactoryProfile + AuditLog
11. 返回档案详情
```

### 5.2 评分追加与重算流程

```
触发源：
  A. 用户手动追加（EvaluationsTab → +手动评分）
  B. 业务事件自动追加（验货报告 / 交货完成 → recordAutoEvaluation）

流程：
  addEvaluation(factoryId, { kind, score, sourceType, sourceId, evaluatedAt, note })
    │
    ▼
  $transaction {
    1. 校验 factoryId 存在且未软删
    2. assertScore(score)  // 0-100
    3. 校验 evaluatedAt 格式 YYYY-MM-DD
    4. 写入 FactoryEvaluation（append-only）
    5. 同事务重算缓存：
       - 查 inspectionRows / deliveryRows
       - avg(inspectionRows) → qualityScore
       - avg(deliveryRows) → deliveryScore
    6. 更新 FactoryProfile.qualityScore / deliveryScore / updatedAt
  }
    │
    ▼
  事务提交后发布事件 FactoryEvaluationAdded（fire-and-forget）
```

### 5.3 自动评分幂等流程

```
业务事件（如验货报告提交）触发 recordAutoEvaluation
  │
  ▼
1. 通过 relationId 查 FactoryProfile（无档案静默跳过，不阻断主流程）
2. 查重：同 (factoryId, kind, sourceType, sourceId) 是否已存在
   - 存在 → 静默跳过（recorded: false, evaluationId: dup.id）
3. 不存在 → 调用 addEvaluation 追加
```

### 5.4 产能占用聚合流程

```
用户进入工厂详情 → 切到「产能日历」Tab
  │
  ▼
listCapacity(factoryId)
  │
  ▼
1. 查 FactoryCapacity[]（按 month 升序）
2. aggregateOccupied(relationId, months[])
   - 查 PurchaseOrder（supplierRelationId + status in [Sent, Confirmed, PartiallyReceived]）
   - 按 expectedDeliveryDate 落月（slice 0,7）
   - 累加 PurchaseLine.quantity 到 occupied[month]
3. 返回 rows.map(r => ({ ...r, occupied: occupied[r.month] || 0 }))
  │
  ▼
前端渲染：
  - 每行：month + capacity + occupied + 占用比%
  - 占用条进度条：>100% danger / >80% warning / 其他 success
```

### 5.5 黑名单流程

```
拉黑：
  setBlacklist(factoryId, reason, actorId)
    │
    ▼
  $transaction {
    1. 校验调用者权限（owner/admin/manager）
    2. FactoryProfile.update({ blacklistedAt: now, blacklistReason: reason, blacklistedById: actorId })
    3. AuditLog
  }
    │
    ▼
  后续新建采购单时：
    assertNotBlacklisted(factoryId)  // 服务端强制校验
    若 blacklistedAt != null → 抛错「工厂已拉黑，禁止新建采购单」

解除拉黑：
  clearBlacklist(factoryId, actorId)
    │
    ▼
  FactoryProfile.update({ blacklistedAt: null, blacklistReason: null, blacklistedById: null })
```

### 5.6 认证预警流程

```
SuppliersManager 加载时 / 定时任务
  │
  ▼
listExpiringCertifications(EXPIRING_DAYS=30)
  │
  ▼
1. 查 FactoryCertification（deletedAt=null, validUntil != null）
2. 计算 validUntil - today ≤ 30 天
3. 返回 FactoryCertification[] (含 factory.profile 关联)
  │
  ▼
前端顶部横幅展示：{N} 项工厂认证将于 30 天内到期
点击展开 → 列表显示工厂名 + 认证类型 + 剩余天数 + 色阶徽章
```

---

## §6 API 与服务契约

### 6.1 REST 端点（factoryRoute.ts / factoryRouteV2.ts）

| Method | Path | 用途 |
| --- | --- | --- |
| GET | `/api/factories` | 工厂列表（支持 search / sort / blacklisted 筛选） |
| GET | `/api/factories/:id` | 工厂详情 |
| POST | `/api/factories` | 创建工厂档案 |
| PUT | `/api/factories/:id` | 更新工厂档案 |
| DELETE | `/api/factories/:id` | 软删除工厂档案 |
| POST | `/api/factories/:id/blacklist` | 拉黑 |
| POST | `/api/factories/:id/unblacklist` | 解除拉黑 |
| GET | `/api/factories/:id/overview` | 360° 总览（profile + evaluations + certifications + capacity） |
| POST | `/api/factories/:id/evaluations` | 追加评分 |
| GET | `/api/factories/:id/evaluations` | 评分列表 |
| POST | `/api/factories/:id/certifications` | 追加认证 |
| PUT | `/api/factories/:id/certifications/:certId` | 更新认证 |
| DELETE | `/api/factories/:id/certifications/:certId` | 删除认证 |
| POST | `/api/factories/:id/capacity` | upsert 月产能 |
| DELETE | `/api/factories/:id/capacity/:month` | 删除月产能 |
| GET | `/api/factories/:id/capacity` | 产能列表（含 occupied） |
| GET | `/api/factories/certifications/expiring?days=30` | 到期预警 |

### 6.2 服务层契约（factoryService.ts）

- `createFactoryService(prisma)` 工厂函数，返回所有方法。
- 所有 mutation 走 `$transaction` + AuditLog。
- 评分重算纯函数 `deliveryScoreForDaysLate` / `inspectionScoreForResult`（factoryService.ts 92–106 行）集中口径。
- 评分缓存 `qualityScore / deliveryScore` 由 `addEvaluation` 同事务重算。
- 产能占用 `aggregateOccupied` 纯查询，无写入。

### 6.3 业务事件

- `FactoryEvaluationAdded`：评分追加后发布。
- `FactoryBlacklisted`：拉黑后发布。
- `FactoryUnblacklisted`：解除拉黑后发布。
- `FactoryCertificationExpiring`：定时扫描发布（30 天内到期）。
- `FactoryCapacityUpdated`：月产能 upsert 后发布。

### 6.4 跨模块事件订阅

- 验货报告提交 → `recordAutoEvaluation(kind='inspection')` 自动追加质量分。
- 采购单收齐 → `recordAutoEvaluation(kind='delivery')` 自动追加交期分。

---

## §7 UI / UX 设计

### 7.1 布局结构

```
┌────────────────────────────────────────────────────────────┐
│ PageHeader: 供应商管理 + 新建工厂档案按钮                  │
├────────────────────────────────────────────────────────────┤
│ 认证到期预警横幅（expiringCerts.length > 0 时显示）        │
│   {N} 项工厂认证将于 30 天内到期 [查看/收起]              │
├──────────────────┬─────────────────────────────────────────┤
│ 左列表（w-80）   │ 右详情 360°                              │
│ ┌──────────────┐ │ ┌─────────────────────────────────────┐ │
│ │ 搜索 + 刷新  │ │ │ 工厂头部卡                          │ │
│ │ 过滤段控件   │ │ │   名称 + 已拉黑徽章 + 价位徽章       │ │
│ │ 排序下拉     │ │ │   累计单数/金额/首次合作/月产能      │ │
│ ├──────────────┤ │ │   质量分 / 交期分（ScoreBadge）     │ │
│ │ 排名列表     │ │ │   编辑档案/关系档案/拉黑/删除按钮    │ │
│ │   工厂 A     │ │ ├─────────────────────────────────────┤ │
│ │   质 85 交90│ │ │ Tab 栏: 总览 / 评分记录 / 认证管理 / 产能日历 │
│ │   工厂 B     │ │ ├─────────────────────────────────────┤ │
│ │   质 70 交85│ │ │ Tab 内容（AnimatePresence 切换）     │ │
│ │   ...       │ │ │   + RelatedEntitiesPanel 跨模块关联 │ │
│ ├──────────────┤ │ └─────────────────────────────────────┘ │
│ │ 共 N 家工厂 │ │                                         │
│ └──────────────┘ │                                         │
└──────────────────┴─────────────────────────────────────────┘
```

### 7.2 排名列表

- **搜索**：按工厂名（relation.name）模糊搜索，300ms debounce。
- **过滤**：`active`（正常）/ `blacklisted`（已拉黑）/ `all`（全部）三段控件。
- **排序**：`quality`（质量分优先）/ `delivery`（交期分优先）/ `orders`（订单数优先）/ `amount`（金额优先）。
- **卡片信息**：工厂名 + 已拉黑徽章 + 质量分徽章 + 交期分徽章 + 累计单数。
- **色阶**：≥80 success / ≥60 warning / <60 danger。

### 7.3 详情头部卡

- 工厂名 + 已拉黑徽章（danger）+ 价位徽章（info）。
- 累计单数 / 累计金额 / 首次合作 / 月产能。
- 质量分 / 交期分 ScoreBadge（大字 + 标签）。
- 操作行：编辑档案 / 关系档案（跳 Relations）/ 拉黑或解除拉黑 / 删除。
- 已拉黑时显示拉黑原因 alert（danger）。

### 7.4 Tab 栏

| Tab | 图标 | 内容 |
| --- | --- | --- |
| 总览 | Building2 | 擅长品类徽章 + 档案字段网格（10 字段） + 备注 + Relation 联系信息 |
| 评分记录 | Star | 评分类型段控件（全部/验货/交期）+ 评分列表（类型徽章 + 分数徽章 + 备注 + 日期 + 来源） |
| 认证管理 | ShieldCheck | 认证列表（图标 + 类型 + 编号 + 签发/有效期 + 剩余天数徽章 + 编辑/删除） |
| 产能日历 | CalendarRange | 月产能列表（月份 + 计划产能 + 占用量 + 占用比 + 进度条 + 编辑/删除） |

### 7.5 弹窗表单

- **ProfileForm**：新建/编辑档案（供应商选择 + 产能 + 工人 + 专长 + 价位 + 银行 + 备注）。
- **EvaluationForm**：手动追加评分（类型 + 分数 0-100 + 日期 + 备注）。
- **CertificationForm**：新增/编辑认证（类型下拉 + 自定义 + 编号 + 签发/有效期 + 长期有效复选框）。
- **CapacityForm**：设置月产能（月份 + 计划产能 + 单位 + 备注）。
- **BlacklistForm**：拉黑表单（拉黑原因必填 + 警示 alert）。

### 7.6 设计系统合规

- 完全使用 BDS v2.1 组件类（bds-btn / bds-card / bds-badge / bds-listrow / bds-segment / bds-alert / bds-modal）。
- 语义 token（`--text-primary` / `--bg-panel` / `--success-tint` 等），主题透明。
- 无 isDarkMode 分支，暗色由 tokens.css `[data-theme]` 统一覆盖。
- 无阴影、无 rim、大圆角（`rounded-card`）。
- 评分色阶：`scoreSemantic` 函数（≥80 success / ≥60 warning / <60 danger）。
- 认证色阶：`certSemantic` 函数（null neutral / <0 danger / ≤30 warning / 其他 success）。

---

## §8 权限与审计

### 8.1 权限

- 工厂档案 CRUD：登录用户均可。
- **黑名单操作**：owner / admin / manager 权限（服务端 `assertPermission` 强制）。
- 评分追加：登录用户均可（手动）；系统事件自动追加无 actorId 时记 system。
- 认证 / 产能 CRUD：登录用户均可。

### 8.2 审计

- 所有 mutation 通过 `writeRouteAuditLog` 同事务写入 `AuditLog`。
- `source` 字段标记来源（`factory-service:blacklist` 等）。
- `targetType = 'FactoryProfile' / 'FactoryEvaluation' / 'FactoryCertification' / 'FactoryCapacity'`。
- 黑名单操作含 `actorId`（拉黑人）。

---

## §9 性能与扩展性

### 9.1 索引

- `FactoryProfile`：`blacklistedAt`（黑名单筛选）、`qualityScore`（排名）。
- `FactoryEvaluation`：`factoryId + kind`（评分查询）、`sourceType + sourceId`（幂等查重）。
- `FactoryCertification`：`factoryId`、`validUntil`（预警扫描）。
- `FactoryCapacity`：`factoryId + month`（unique）、`month`（按月查询）。

### 9.2 扩展性

- 评分类型 `kind` 为 String，可扩展新类型（如 `responsiveness` 响应速度）不改表。
- 认证 `type` 为 String，可扩展新认证类型。
- 产能 `unit` 为 String，可扩展新单位（如 `KG`）。
- `specialties` 为 String[]，可任意扩展品类。

### 9.3 已知性能优化点

- 360° 总览 `getOverview` 并行查 evaluations + certifications + capacity，避免串行。
- 评分列表 `take: 200` 限制返回数量。
- 产能占用聚合按 `expectedDeliveryDate` 索引（PurchaseOrder 已有）。
- 排名列表 `limit: 100` 分页。

---

## §10 错误处理与边界

### 10.1 错误场景

| 场景 | 错误信息 |
| --- | --- |
| 评分越界 | `评分必须在 0-100 之间` |
| 月份格式错 | `month 必须是 YYYY-MM` |
| 日期格式错 | `evaluatedAt 必须是 YYYY-MM-DD` |
| 产能负数 | `capacity 必须是非负数字` |
| 工厂不存在 | `工厂档案不存在` |
| 拉黑后新建 PO | `工厂已拉黑，禁止新建采购单`（服务端 assertNotBlacklisted） |
| 拉黑原因空 | 前端 `拉黑原因必填` |
| Relation 非 Supplier | `Relation 不是 Supplier 组织` |

### 10.2 边界

- `qualityScore / deliveryScore` 缓存分默认 0（无评分时）。
- 产能占用 `occupied` 无 PO 时为 0。
- 认证 `validUntil` 为 null 时长期有效，预警扫描跳过。
- 黑名单 `blacklistedAt` 为 null 时正常。

---

## §11 测试策略

### 11.1 后端测试

`server/src/suppliers/__tests__/factoryRoute.test.ts`：

- 工厂档案 CRUD 全量回归。
- 评分追加 + 重算闭环。
- 自动评分幂等（重复事件不重复追加）。
- 黑名单拉黑/解除 + 拉黑后新建 PO 抛错。
- 认证预警扫描（30 天前/内/已过期/长期有效）。
- 产能 upsert + occupied 聚合。

### 11.2 测试用例覆盖

- 评分边界（0 / 100 / -1 / 101 / 非数字）。
- 月份边界（YYYY-MM / YYYYMM / 空）。
- 黑名单权限（owner/admin/manager 可拉黑，普通用户拒绝）。
- 产能占用聚合（多 PO 落同月累加 / 跨月分离）。
- 360° 总览并行查询。

### 11.3 验证命令

```bash
cd server && npx vitest run src/suppliers
```

---

## §12 部署与运维

- 与主服务同进程，无独立部署。
- 认证到期预警可挂定时任务（每日扫描），或前端列表加载时触发。
- 黑名单状态变更实时生效（无缓存层）。

---

## §13 已知限制与技术债务

1. 评分均值算法简单（算术平均），无时间衰减或加权（近期评分权重更高）。建议引入 EWMA 指数加权移动平均。
2. 产能占用聚合按 PO 数量累加，未区分面料（M）与成衣（PC）单位混用场景。建议按 unit 分轨聚合。
3. 黑名单仅本地标记，未同步上游 ERP 或下游客户。
4. 认证预警仅 30 天阈值，未支持多级预警（60/90 天提前通知）。
5. 银行信息明文存储（无加密），建议敏感字段加密。
6. 工厂评分缓存重算仅追加评分时触发，删除评分（软删）后未自动重算（需手动触发或定期重算任务）。

---

## §14 演进路线

1. **评分时间衰减**：引入 EWMA 算法，近期评分权重更高。
2. **产能分轨聚合**：按 unit（PC/M）分轨占用聚合。
3. **黑名单同步**：与上游 ERP / 下游客户同步拉黑状态。
4. **多级预警**：60/90/30 天多级认证预警。
5. **银行信息加密**：敏感字段 AES 加密存储。
6. **评分删除重算**：软删评分后自动重算缓存分。
7. **AI 工厂推荐**：基于订单需求（品类/产能/价位）推荐合适工厂。
8. **供应商门户**：开放给供应商自助更新产能/认证。

---

## §15 交叉链接

1. [`供应商档案.md`](./供应商档案.md) — FactoryProfile 字段级规格 + 产能 + 认证 + 评分 + 银行。
2. [`自动评分公式.md`](./自动评分公式.md) — 质量分 / 交期分计算口径与重算机制。
3. [`产能日历.md`](./产能日历.md) — 月计划产能 + 在手 PO 占用实时聚合可视化。
4. [`黑名单与预警.md`](./黑名单与预警.md) — 拉黑流程 + 认证到期预警 + 色阶策略。
5. [`../../02-数据模型/产品与档案模型组.md`](../../02-数据模型/产品与档案模型组.md) — FactoryProfile / Evaluation / Certification / Capacity 全量字段表与 ER 图。
6. [`../Products-数字档案/模块概述.md`](../Products-数字档案/模块概述.md) — GarmentProfile.factoryRelationId / TrimmingProfile.supplierRelationId 反查工厂档案。
7. [`../../02-数据模型/采购与库存模型组.md`](../../02-数据模型/采购与库存模型组.md) — PurchaseOrder.supplierRelationId 关联工厂档案，拉黑后禁新建 PO。
8. [`../../03-接口契约/Suppliers-API.md`](../../03-接口契约/Suppliers-API.md) — REST 端点完整契约（如已生成）。

---

## §16 附录

### 16.1 关键文件清单

| 文件 | 行数 | 用途 |
| --- | --- | --- |
| `components/SuppliersManager.tsx` | 1623 | 前端组件主体 |
| `server/src/suppliers/factoryService.ts` | ~580 | 核心服务（含评分重算、产能聚合） |
| `server/src/suppliers/factoryRoute.ts` | - | REST 端点 v1 |
| `server/src/suppliers/factoryRouteV2.ts` | - | REST 端点 v2 |
| `server/src/suppliers/__tests__/factoryRoute.test.ts` | - | 后端测试 |
| `server/prisma/schema.prisma` 3084–3193 行 | - | FactoryProfile / Evaluation / Certification / Capacity 模型 |
| `types.ts` 3275–3387 行 | - | TypeScript 类型定义 |

### 16.2 评分类型与来源

| 评分类型 `kind` | 含义 | 来源 `sourceType` |
| --- | --- | --- |
| `inspection` | 验货质量 | `inspectionReport`（验货报告）/ `manual`（手动） |
| `delivery` | 交期达成 | `shipment`（货运）/ `purchaseOrder`（采购单）/ `manual`（手动） |

### 16.3 认证类型枚举

`CERT_TYPES`（SuppliersManager.tsx 140 行）：

`BSCI / SEDEX / WRAP / ISO9001 / ISO14001 / OEKO-TEX / GRS / OCS / 其他（自定义）`
