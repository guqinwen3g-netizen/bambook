# 业务自动化触发器设计

> 模块代号：`Settings / Automation Rules`
> 父模块：[Settings 账户与系统配置](./账户与系统配置.md)
> 关联代码：`components/AutomationRulesSection.tsx`、`server/src/config/automationConfig.ts`、`server/src/events/businessEventBus.ts`（事件发布）、`types.ts`（AutomationRule 类型）
> 文档版本：v1.0 · 最后更新：2026-08-15

---

## §1 实现状态

| 维度 | 状态 | 说明 |
| --- | --- | --- |
| 预置规则 | ✅ 已落地 | `AUTOMATION_RULES` 9 条规则：L1/L2/L3/L5/L6/L7/L8/L9/L10，覆盖订单→生产→发货→发票→核销→BOM→采购→入库→报关全链路。 |
| 配置服务 | ✅ 已落地 | `automationConfig.ts`：内存缓存 + 文件持久化（`server/data/automation-config.json`）+ 启动加载 + API 修改时同步写盘。 |
| 事件驱动 | ✅ 已落地 | 每条规则绑定 `eventType`（OrderConfirmed/ProductionCompleted/ShipmentCompleted/PaymentVoucherCreated/BOMConfirmed/MaterialReceived/QuotationAccepted/CustomsCleared）。 |
| 前端组件 | ✅ 已落地 | `AutomationRulesSection.tsx`：拉取 `/api/v1/automation/rules` + 卡片展示 + 启停开关 + 乐观更新（失败回滚）+ 加载骨架 + 错误重试 + 空态。 |
| 事件元数据 | ✅ 已落地 | `EVENT_META` 映射：4 事件类型 → 中文标签 + 图标（OrderConfirmed/ProductionCompleted/ShipmentCompleted/PaymentVoucherCreated）。 |
| BDS v2.1 主题 | ✅ 已落地 | 使用 `BAMBOOK_OS` token + flat 设计 + 大圆角 + 半透明膜色。 |
| 单租户设计 | ✅ 已落地 | 单租户桌面 ERP 场景，无需数据库迁移（文件持久化即可）。 |
| 不变量保证 | ✅ 已落地 | 配置读取永不抛错（失败返回默认值，所有规则默认 enabled）；配置写入失败仅日志，不阻断 API。 |
| 审计 | ⚠️ 部分落地 | 配置变更写日志，但未写 AuditLog（仅 logger 记录）。 |

---

## §2 业务定位与目标

### 2.1 业务定位

业务自动化触发器是 Bambook 的「**流程联动引擎**」——基于业务事件（BusinessEvent）自动触发下游操作，减少人工重复操作：

```
业务事件发生 → 自动化规则匹配 → 触发下游操作（创建草稿/自动核销/自动入库...）
```

### 2.2 核心目标

1. **事件驱动**：基于 `BusinessEventBus` 发布的事件自动触发
2. **可启停**：每条规则可独立启停，不删除规则本身
3. **草稿优先**：触发的下游操作默认为 Draft 状态，需人工审核后确认
4. **幂等**：同事件多次触发不会重复创建（通过业务唯一键去重）
5. **文件持久化**：配置存 `server/data/automation-config.json`，无需数据库迁移

---

## §3 核心概念与术语

| 术语 | 定义 |
| --- | --- |
| `AutomationRule` | 自动化规则：`{ id, name, description, eventType, enabled }` |
| `AUTOMATION_RULES` | 9 条预置规则常量数组 |
| `eventType` | 触发事件类型（BusinessEventType） |
| `enabled` | 启停状态布尔（true=启用） |
| `EVENT_META` | 事件类型 → 中文标签 + 图标映射 |
| `businessEventBus` | 业务事件总线，发布 BusinessEvent |
| `乐观更新` | 前端 toggle 即刻反映 UI，失败回滚 |
| `automation-config.json` | 配置文件持久化路径 |

---

## §4 9 条预置规则

### 4.1 AUTOMATION_RULES 清单

| ID | 名称 | eventType | 说明 |
| --- | --- | --- | --- |
| L1_init_production | 订单确认 → 初始化生产 | OrderConfirmed | 订单确认后自动初始化 10 阶段生产管线 |
| L2_create_shipment | 生产完成 → 创建发货单 | ProductionCompleted | 生产完成后自动创建发货单草稿（Draft，需人工审核） |
| L3_issue_invoice | 发货完成 → 创建发票 | ShipmentCompleted | 发货交付后自动创建应收发票草稿（Draft，需人工审核） |
| L5_auto_allocate | 收款登记 → 自动核销 | PaymentVoucherCreated | 收款凭证登记后自动核销到关联发票（仅 Receipt，需有匹配发票） |
| L6_create_bom_draft | 订单确认 → 生成 BOM 草稿 | OrderConfirmed | 订单确认后从产品 BOM 模板复制生成 Draft BOM（需有已确认/归档模板） |
| L7_create_procurement | BOM 确认 → 生成采购需求 | BOMConfirmed | BOM 确认后自动生成 Draft 采购单（含物料行，需采购员审核） |
| L8_auto_stock_in | 采购来料 → 自动入库 | MaterialReceived | 采购来料接收后自动创建入库流水（需有 active 仓库，默认入主仓） |
| L9_convert_quotation_to_order | 报价接受 → 转订单草稿 | QuotationAccepted | 报价被接受后自动创建 Draft 订单（含行明细、条款带出，需人工审核后确认） |
| L10_create_tax_refund_draft | 报关放行 → 退税申报草稿 | CustomsCleared | 报关放行后自动核算生成退税申报草稿（FOB×汇率×HS退税率加权，需人工提交审核） |

### 4.2 事件类型映射

```typescript
const EVENT_META: Record<string, EventMeta> = {
  OrderConfirmed:        { label: '订单已确认',    Icon: PackageCheck },
  ProductionCompleted:   { label: '生产已完成',    Icon: Factory },
  ShipmentCompleted:     { label: '发货已完成',    Icon: Truck },
  PaymentVoucherCreated: { label: '收款凭证已创建', Icon: Receipt },
};

const getEventMeta = (eventType: string): EventMeta =>
  EVENT_META[eventType] || { label: eventType, Icon: Zap };
```

### 4.3 触发链路

```
OrderConfirmed 事件发布
  ├─► L1_init_production 触发：初始化生产管线
  └─► L6_create_bom_draft 触发：生成 BOM 草稿

ProductionCompleted 事件发布
  └─► L2_create_shipment 触发：创建发货单草稿

ShipmentCompleted 事件发布
  └─► L3_issue_invoice 触发：创建发票草稿

PaymentVoucherCreated 事件发布
  └─► L5_auto_allocate 触发：自动核销

BOMConfirmed 事件发布
  └─► L7_create_procurement 触发：生成采购需求

MaterialReceived 事件发布
  └─► L8_auto_stock_in 触发：自动入库

QuotationAccepted 事件发布
  └─► L9_convert_quotation_to_order 触发：转订单草稿

CustomsCleared 事件发布
  └─► L10_create_tax_refund_draft 触发：退税申报草稿
```

---

## §5 配置服务

### 5.1 automationConfig.ts

```typescript
import { readFileSync, writeFileSync, mkdirSync, existsSync } from 'fs';
import { join } from 'path';
import { logger } from '../lib/logger';

export interface AutomationRule {
  id: string;
  name: string;
  description: string;
  eventType: string;
  enabled: boolean;
}

export const AUTOMATION_RULES: readonly AutomationRule[] = [...]; // 9 条

// 内存缓存
let configCache: Record<string, boolean> | null = null;
const CONFIG_FILE = join(process.cwd(), 'server/data/automation-config.json');
```

### 5.2 不变量

```typescript
// 配置读取永不抛错（失败时返回默认值，所有规则默认 enabled）
export function isRuleEnabled(ruleId: string): boolean {
  try {
    if (!configCache) loadConfig();
    return configCache?.[ruleId] ?? true;  // 默认 enabled
  } catch {
    return true;  // 失败也返回 true（不阻断业务）
  }
}

// 配置写入失败仅日志，不阻断 API 响应
export function setRuleEnabled(ruleId: string, enabled: boolean): void {
  try {
    if (!configCache) loadConfig();
    configCache![ruleId] = enabled;
    saveConfig();
  } catch (err) {
    logger.error('Failed to persist automation config', { err });
    // 不抛错，内存缓存已更新
  }
}
```

### 5.3 文件持久化

```typescript
function loadConfig(): void {
  if (!existsSync(CONFIG_FILE)) {
    configCache = {};
    return;
  }
  const raw = readFileSync(CONFIG_FILE, 'utf-8');
  configCache = JSON.parse(raw);
}

function saveConfig(): void {
  if (!existsSync(dirname(CONFIG_FILE))) {
    mkdirSync(dirname(CONFIG_FILE), { recursive: true });
  }
  writeFileSync(CONFIG_FILE, JSON.stringify(configCache, null, 2));
}
```

- 路径：`server/data/automation-config.json`
- 格式：`{ "L1_init_production": true, "L2_create_shipment": false, ... }`
- 启动时加载，API 修改时同步写盘

---

## §6 API 端点

### 6.1 路由

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | `/api/v1/automation/rules` | 列出所有规则（含 enabled 状态） |
| PATCH | `/api/v1/automation/rules/:id` | 更新规则启停（`{ enabled: boolean }`） |

### 6.2 请求/响应

```typescript
// GET /api/v1/automation/rules
{
  ok: true,
  rules: [
    { id, name, description, eventType, enabled },
    ...
  ]
}

// PATCH /api/v1/automation/rules/L1_init_production
// 请求
{ enabled: false }
// 响应
{ ok: true, rule: { id, name, description, eventType, enabled: false } }
```

---

## §7 前端组件

### 7.1 AutomationRulesSection.tsx

```typescript
interface AutomationRulesSectionProps {
  isDarkMode: boolean;
}

export function AutomationRulesSection({ isDarkMode }: AutomationRulesSectionProps) {
  const [rules, setRules] = useState<AutomationRule[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  // 拉取规则列表
  useEffect(() => {
    apiService.listAutomationRules()
      .then(setRules)
      .catch(e => setError(e?.message || '加载失败'))
      .finally(() => setLoading(false));
  }, []);

  // 启停切换（乐观更新）
  const handleToggle = async (ruleId: string, enabled: boolean) => {
    const prev = rules;
    setRules(rules.map(r => r.id === ruleId ? { ...r, enabled } : r));  // 乐观更新
    try {
      await apiService.updateAutomationRule(ruleId, { enabled });
    } catch (e) {
      setRules(prev);  // 失败回滚
      setError(e?.message || '更新失败');
    }
  };

  return (
    <div>
      {/* 加载骨架 */}
      {/* 错误重试 */}
      {/* 空态 */}
      {/* 规则卡片列表 */}
      {rules.map(rule => (
        <RuleCard
          key={rule.id}
          rule={rule}
          onToggle={(enabled) => handleToggle(rule.id, enabled)}
        />
      ))}
    </div>
  );
}
```

### 7.2 规则卡片

```tsx
<div className={`${BAMBOOK_OS.material.card} p-5`}>
  <div className="flex items-center justify-between">
    <div className="flex items-center gap-3">
      <Icon size={20} strokeWidth={1.2} />  {/* 事件类型图标 */}
      <div>
        <div className="text-sm font-light">{rule.name}</div>
        <div className="text-xs text-[var(--text-quaternary)]">{rule.description}</div>
      </div>
    </div>
    <div className="flex items-center gap-3">
      <span className="text-[10px] tracking-wide">{EVENT_META[rule.eventType]?.label}</span>
      <Toggle checked={rule.enabled} onChange={onToggle} />
    </div>
  </div>
</div>
```

### 7.3 乐观更新策略

```typescript
const handleToggle = async (ruleId, enabled) => {
  const prev = rules;
  setRules(rules.map(r => r.id === ruleId ? { ...r, enabled } : r));  // 即刻反映 UI
  try {
    await apiService.updateAutomationRule(ruleId, { enabled });
  } catch (e) {
    setRules(prev);  // 失败回滚
    setError(e?.message || '更新失败');
  }
};
```

- **乐观更新**：toggle 即刻反映 UI
- **失败回滚**：API 失败时恢复原状态
- **错误提示**：显示错误消息（不阻断其他规则操作）

---

## §8 交互流程

### 8.1 加载规则

```
用户进入 Settings → automation Tab
  └─► AutomationRulesSection 挂载
      └─► apiService.listAutomationRules()
          ├─► 200: setRules(data) + setLoading(false)
          ├─► 401: setError('未授权')
          └─► 网络错误: setError('加载失败')
```

### 8.2 启停规则

```
用户点击规则卡片的 Toggle
  └─► handleToggle(ruleId, !currentEnabled)
      ├─► setRules(乐观更新)  // UI 即刻反映
      └─► apiService.updateAutomationRule(ruleId, { enabled })
          ├─► 200: 保持新状态
          └─► 失败: setRules(prev) + setError('更新失败')
```

### 8.3 事件触发

```
业务操作（如订单确认）
  └─► businessEventBus.publish({ type: 'OrderConfirmed', payload })
      └─► automationConfig 检查 L1/L6 规则是否 enabled
          ├─► L1 enabled: 触发初始化生产管线
          └─► L6 enabled: 触发生成 BOM 草稿
              └─► 下游服务执行（创建 Draft 状态的实体）
```

---

## §9 状态机

### 9.1 规则状态

```
              ┌─────────┐  enable (PATCH enabled=true)
              │ enabled │◄────────────────────────
              └────┬────┘
                   │ disable (PATCH enabled=false)
                   ▼
              ┌─────────┐
              │ disabled│  (事件不触发该规则)
              └────┬────┘
                   │ enable
                   ▼
              ┌─────────┐
              │ enabled │
              └─────────┘
```

### 9.2 事件触发条件

```
BusinessEvent 发布
  └─► for each rule in AUTOMATION_RULES:
      if rule.eventType === event.type AND isRuleEnabled(rule.id):
          trigger(rule, event)  // 触发下游操作
```

### 9.3 下游操作状态

| 规则 | 下游操作 | 初始状态 | 审核要求 |
| --- | --- | --- | --- |
| L1 | 初始化生产管线 | 10 阶段管线 | 自动（无需审核） |
| L2 | 创建发货单 | Draft | 需人工审核 |
| L3 | 创建发票 | Draft | 需人工审核 |
| L5 | 自动核销 | Reconciled | 自动（需有匹配发票） |
| L6 | 生成 BOM 草稿 | Draft | 需人工审核 |
| L7 | 生成采购需求 | Draft | 需采购员审核 |
| L8 | 自动入库 | Inbound 完成 | 自动（需有 active 仓库） |
| L9 | 转订单草稿 | Draft | 需人工审核后确认 |
| L10 | 退税申报草稿 | Draft | 需人工提交审核 |

---

## §10 权限与访问控制

### 10.1 前端权限

- automation Tab 入口要求 `settings:read` 权限
- 启停规则要求 `settings:write` 权限

### 10.2 后端权限

- `/api/v1/automation/rules` GET：JWT + `settings:read`
- `/api/v1/automation/rules/:id` PATCH：JWT + `settings:write`
- 事件触发由后端 `businessEventBus` 自动执行，无需用户权限

### 10.3 配置文件保护

- `server/data/automation-config.json` 文件需文件系统权限保护
- 未来扩展：升级到 `SystemConfig` 表持久化 + 加密

---

## §11 审计与可观测性

### 11.1 日志

```typescript
// 配置变更
logger.info('Automation rule toggled', { ruleId, enabled, actorId });

// 事件触发
logger.info('Automation rule triggered', { ruleId, eventType, entityId });
```

### 11.2 当前限制

- ❌ 配置变更**未写 AuditLog**（仅 logger 记录）
- ❌ 事件触发**未写 AuditLog**（仅 logger 记录）
- ❌ 无**触发历史**查询界面

### 11.3 未来扩展

- 配置变更写 AuditLog（operation=`automation_rule_update`）
- 事件触发写 AuditLog（operation=`automation_triggered`）
- 触发历史查询界面（AdminPanel 新增 Tab）
- 触发成功率统计（成功/失败/跳过）

---

## §12 测试覆盖

| 测试维度 | 覆盖点 |
| --- | --- |
| 9 条规则预置 | AUTOMATION_RULES 数组完整性 |
| 配置读取 | 默认 enabled + 失败返回 true |
| 配置写入 | 内存更新 + 文件持久化 |
| 事件触发 | eventType 匹配 + enabled 检查 |
| 前端拉取 | listAutomationRules + loading/error 状态 |
| 乐观更新 | toggle 即刻反映 + 失败回滚 |
| 错误重试 | error 显示 + 可重试 |
| 空态 | 无规则时显示空态 |

---

## §13 已知限制与未来扩展

### 13.1 当前限制

- ❌ 规则**不可自定义**（仅 9 条预置规则的启停）
- ❌ 配置**文件持久化**（非数据库，跨设备不同步）
- ❌ 配置变更**未写 AuditLog**
- ❌ 无**触发条件**配置（如「仅金额 > 10000 才触发」）
- ❌ 无**触发历史**查询
- ❌ 无**触发失败重试**机制

### 13.2 未来扩展

- 自定义规则（事件 + 条件 + 动作三元组）
- 升级到 SystemConfig 持久化 + 跨设备同步
- 配置变更写 AuditLog
- 触发条件配置（金额阈值/客户分层/季节等）
- 触发历史查询界面
- 触发失败重试机制（指数退避）
- 触发成功率仪表盘

---

## §14 实现备注

### 14.1 单租户设计

```typescript
// 单租户桌面 ERP 场景，无需数据库迁移
// 配置存 server/data/automation-config.json
```

- 不接入 Prisma 数据库
- 文件持久化即可满足单租户需求
- 未来多租户时升级到 `SystemConfig` 表（scope='tenant::{tenantId}'）

### 14.2 草稿优先原则

所有触发的下游操作默认为 Draft 状态，需人工审核后确认。避免：

- 自动创建未审核的正式单据
- 数据不一致（如自动核销到错误发票）
- 业务流程跳过人工把关

### 14.3 幂等保证

- 同事件多次触发不会重复创建（通过业务唯一键去重）
- 如 L9：报价接受 → 转订单，同一报价多次接受事件只创建一个 Draft 订单
- 通过 `Order.quotationId` 唯一约束保证

---

## §15 交叉链接

1. [账户与系统配置](./账户与系统配置.md) — 父模块，automation Tab 是 10 Tab 之一
2. [Orders 订单管理 — 模块概述](../../03-订单与生产/Orders-订单管理/模块概述.md) — L1/L6/L9 规则关联订单
3. [ProductionBoard 生产看板 — 模块概述](../../03-订单与生产/ProductionBoard-生产看板/模块概述.md) — L1 规则触发生产管线
4. [Shipments 货运与出运 — 模块概述](../../04-出货与单据/Shipments-货运与出运/模块概述.md) — L2 规则触发发货单
5. [DocumentCenter 单据中心 — 模块概述](../../04-出货与单据/DocumentCenter-单据中心/模块概述.md) — L3 规则触发发票
6. [FinanceManager 总览](../../05-财务与结算/FinanceManager总览.md) — L5 规则触发核销
7. [BOM 物料清单 — 模块概述](../../03-订单与生产/BOM-物料清单/模块概述.md) — L6/L7 规则关联 BOM
8. [Procurement 采购管理 — 模块概述](../../03-订单与生产/Procurement-采购管理/模块概述.md) — L7/L8 规则关联采购
9. [Inventory 库存管理 — 模块概述](../../03-订单与生产/Inventory-库存管理/模块概述.md) — L8 规则触发入库
10. [Customs 报关与退税 — 模块概述](../../04-出货与单据/Customs-报关与退税/模块概述.md) — L10 规则触发退税申报

---

## §16 变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v1.0 | 2026-08-15 | 初始版本：9 条规则 + 事件驱动 + 乐观更新 + 文件持久化 + 草稿优先 |
