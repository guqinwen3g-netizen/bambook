# 账户与系统配置设计

> 模块代号：`Settings / Account & System`
> 父模块：Settings 设置
> 关联代码：`components/Settings.tsx`、`services/authService.ts`（getAuthState/changePassword/logout/hasPermission/updateMyProfile）、`services/storageService.ts`、`services/apiService.ts`（connectivity tests）、`server/prisma/schema.prisma`（SystemConfig 模型 4842–4875 行）
> 文档版本：v1.0 · 最后更新：2026-08-15

---

## §1 实现状态

| 维度 | 状态 | 说明 |
| --- | --- | --- |
| 双模式入口 | ✅ 已落地 | `Settings` 组件支持 `mode: 'account' \| 'system'`：account 模式默认 `account` Tab，system 模式默认 `appearance` Tab。 |
| 10 个 Tab（原）→ 11 个 Tab（含 business_rules） | ⚠️ 部分落地 | 现有 `SETTINGS_TABS` 数组：account / appearance / ai / voice / sync / storage / api / automation / security / company。**需新增 `business_rules` Tab**（放在 automation 之后，security 之前），承载 MOQ 阈值配置与后续业务规则开关。 |
| 账号管理 | ✅ 已落地 | account Tab：头像上传 + Canvas 裁切（256×256 webp 0.86）+ 个人资料编辑 + 修改密码 + 登出。 |
| 外观配置 | ✅ 已落地 | appearance Tab：主题模式（light/dark/system）+ 壁纸预设（28 张）+ 缩放（--ui-lab-app-scale）。 |
| AI 配置 | ✅ 已落地 | ai Tab：模型选择 + 温度 + RAG 连接测试（health + Bearer 验证）。 |
| 语音配置 | ✅ 已落地 | voice Tab：自动播报 + 语速调节。 |
| 同步配置 | ✅ 已落地 | sync Tab：云端 + 知识库同步设置。 |
| 存储管理 | ✅ 已落地 | storage Tab：设备存储报告（DeviceStorageReport）+ 缓存清理。 |
| API 配置 | ✅ 已落地 | api Tab：对外接口密钥管理 + 主数据 API 连通性测试。 |
| 安全配置 | ✅ 已落地 | security Tab：隐私设置 + 数据重置。 |
| 自动化配置 | ✅ 已落地 | automation Tab：业务流程联动规则（`AutomationRulesSection` 组件）。 |
| **MOQ 阈值配置（business_rules Tab）** | ❌ P1 缺口未落地 | business_rules Tab → MOQ 阈值子区块：fabricDefaultMoq / garmentDefaultMoq / capsuleMoq 3 字段 + 生效方式（默认不追溯）+ 变更理由必填 + 变更历史表格。守卫 `requireRole('SUPER_ADMIN','ADMIN')`，scope `settings:moq:write` / `settings:moq:read`。独立 MoqThresholdConfig / MoqThresholdConfigHistory 模型，不复用泛型 SystemConfig KV。 |
| 公司抬头 | ✅ 已落地 | company Tab：出口方信息 + 银行信息 + 抬头配置。 |
| 头像裁切 | ✅ 已落地 | `createCircularAvatarDataUrl`：Canvas 圆形裁切 + 旋转 + 缩放 + 偏移，输出 256×256 webp。 |
| 连通性测试 | ✅ 已落地 | 主数据 API + 知识库 API 双端测试，含延迟日志（info/success/error/latency）。 |
| SystemConfig 模型 | ✅ 已落地 | `SystemConfig` 表：scope + key + group + valueType + value + encrypted + version，支持 AES-GCM 加密。 |
| BDS v2.1 主题 | ✅ 已落地 | 全 Tab 使用 `BAMBOOK_OS` token + flat 设计（无阴影/无 rim/大圆角）。 |

---

## §2 业务定位与目标

### 2.1 业务定位

Settings 是 Bambook 的「**个性化与系统配置中枢**」——承载账号身份、外观主题、AI/语音/同步等所有偏好设置，并通过 `SystemConfig` 表持久化系统级配置。

### 2.2 双模式设计

| 模式 | 入口 | 默认 Tab | 用途 |
| --- | --- | --- | --- |
| `account` | 顶部「账号」入口 | `account` | 个人身份与登录管理 |
| `system` | 顶部「系统设置」入口 | `appearance` | 系统级配置 |

### 2.3 核心目标

1. **双模式隔离**：account 模式只展示个人相关 Tab，system 模式展示全部 Tab。
2. **配置真源分离**：用户偏好存本地（IndexedDB），系统配置存 `SystemConfig` 表。
3. **连通性可测**：主数据 API + 知识库 API 双端测试，含延迟日志。
4. **头像本地裁切**：Canvas 圆形裁切，避免服务端处理。
5. **BDS 主题一致**：全 Tab 使用 `BAMBOOK_OS` token，flat 设计统一。

---

## §3 核心概念与术语

| 术语 | 定义 |
| --- | --- |
| `mode` | Settings 模式：`account`（个人）/ `system`（系统） |
| `TabId` | 10 个 Tab 标识：account/appearance/ai/voice/sync/storage/api/automation/security/company |
| `SystemConfig` | 系统配置表，scope + key + value + encrypted + version |
| `localConfig` | 前端本地配置状态（SystemConfig 类型） |
| `AvatarCropDraft` | 头像裁切草稿：src/fileName/naturalWidth/Height/scale/rotation/offset |
| `DeviceStorageReport` | 设备存储报告（storageService 返回） |
| `testLogs` | 连通性测试日志：`{ msg, type: info/success/error/latency }` |
| `BAMBOOK_OS` | 设计 token 系统（colors/borderRadius/spacing 等） |
| `WALLPAPER_PRESETS` | 28 张壁纸预设（极简/自然/城市/动漫/纯色 5 组） |

---

## §4 Tab 清单

### 4.1 SETTINGS_TABS 数组

```typescript
export const SETTINGS_TABS: { id: TabId; label: string; hint: string; icon: typeof Layout }[] = [
  { id: 'account',    label: '账号',   hint: '身份与登录',     icon: User },
  { id: 'appearance', label: '外观',   hint: '主题与显示',     icon: Layout },
  { id: 'ai',         label: 'AI 对话', hint: '模型与温度',     icon: BrainCircuit },
  { id: 'voice',      label: '朗读',    hint: '自动播报语速',   icon: Volume2 },
  { id: 'sync',       label: '同步',    hint: '云端与知识库',   icon: Globe },
  { id: 'storage',    label: '存储',    hint: '缓存与空间',     icon: HardDrive },
  { id: 'api',        label: 'API',     hint: '对外接口密钥',   icon: Cable },
  { id: 'automation', label: '自动化',  hint: '业务流程联动规则', icon: Workflow },
  { id: 'business_rules', label: '业务规则', hint: 'MOQ阈值/审批开关/规则配置', icon: Settings2 },
  { id: 'security',   label: '安全',    hint: '隐私与重置',     icon: Shield },
  { id: 'company',    label: '公司抬头', hint: '出口方与银行信息', icon: Building2 }
];
```

### 4.2 Tab 详情

| Tab | 模式 | 数据源 | 关键功能 |
| --- | --- | --- | --- |
| account | account/system | `authService.getAuthState()` | 头像上传 + 裁切 + 个人资料 + 改密 + 登出 |
| appearance | system | `localConfig` (SystemConfig) | 主题模式 + 壁纸预设 + 缩放 |
| ai | system | `localConfig` + `knowledgeApiService` | 模型选择 + 温度 + RAG 连接测试 |
| voice | system | `localConfig` | 自动播报开关 + 语速 |
| sync | system | `localConfig` | 云端同步 + 知识库同步 |
| storage | system | `storageService.getDeviceStorageReport()` | 存储报告 + 缓存清理 |
| api | system | `localConfig` | API 密钥 + 主数据 API 测试 |
| automation | system | `/api/v1/automation/rules` | 业务规则启停（乐观更新） |
| business_rules | system | `/api/v1/moq-thresholds`（MOQ独立接口）+ 后续业务规则接口 | MOQ 阈值配置（fabricDefaultMoq/garmentDefaultMoq/capsuleMoq 三字段 + 变更历史 + 生效方式）+ 后续审批规则开关/业务规则枚举配置 |
| security | system | `localConfig` | 隐私 + 数据重置 |
| company | system | `localConfig` + 公司档案 | 出口方 + 银行 + 抬头 |

---

## §5 API 端点

### 5.1 账号相关

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | `/api/v1/auth/me` | 获取当前用户信息 |
| PATCH | `/api/v1/auth/me` | 更新个人资料（updateMyProfile） |
| POST | `/api/v1/auth/change-password` | 修改密码 |
| POST | `/api/v1/auth/logout` | 登出 |
| POST | `/api/v1/auth/avatar` | 上传头像（base64） |

### 5.2 系统配置（SystemConfig）

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | `/api/v1/system-config` | 列出配置（按 scope/group 过滤） |
| GET | `/api/v1/system-config/:id` | 获取单条配置（`${scope}::${key}`） |
| PATCH | `/api/v1/system-config/:id` | 更新配置（version +1 + 审计） |
| GET | `/api/v1/system-config/:id/history` | 配置变更历史 |

### 5.3 连通性测试

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | `/api/v1/health` | 主数据 API 健康检查 |
| GET | `/health` | 知识库 API 健康检查（Python 服务） |
| POST | `/v1/chat/debug-context` | 知识库 Bearer 验证（不调用 LLM） |

### 5.4 自动化规则

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | `/api/v1/automation/rules` | 列出自动化规则 |
| PATCH | `/api/v1/automation/rules/:id` | 启停规则（乐观更新） |

### 5.5 MOQ 阈值配置（独立 MoqThresholdConfig 模型，非泛型 SystemConfig）

> **守卫**：读接口 `requireScope('settings:moq:read')` + `requireRole('SUPER_ADMIN','ADMIN')`；写接口 `requireScope('settings:moq:write')` + `requireRole('SUPER_ADMIN','ADMIN')`。

| 方法 | 路径 | 用途 | 关键约束 |
| --- | --- | --- | ---|
| GET | `/api/v1/moq-thresholds/current` | 获取当前 isActive=true 的生效配置（单条返回：{ fabricDefaultMoq, garmentDefaultMoq, capsuleMoq, effectiveFrom, configId, changedBy, changedAt }） | 始终只返回 1 条 isActive=true；若不存在则兜底返回 seed 默认值（800/200/20）及 source='fallback_constant' 标记 |
| GET | `/api/v1/moq-thresholds/history` | 分页获取 `MoqThresholdConfigHistory` append-only 变更历史（DESC 按 changedAt），含变更前后对比、变更人、变更理由 | query 参数：page/pageSize |
| POST | `/api/v1/moq-thresholds` | 提交新 MOQ 阈值配置（事务内：旧 isActive=true → 翻 false + effectiveTo=now；写入新 isActive=true 记录，effectiveFrom 默认为 now 或可指定 future 时间；append 一条 History） | body 字段：fabricDefaultMoq（正整数）、garmentDefaultMoq（正整数）、capsuleMoq（正整数）、changeReason（≥5 字字符串必填）、effectiveFrom（DateTime，可选，支持定时生效）；并发冲突通过「上次提交配置 ID 校验」乐观锁防止双管理员打架；生效方式默认不追溯（已 Confirmed 订单按 moqSnapshot 不变，新单按新值） |

---

## §X 业务规则（business_rules Tab）面板 UI 设计（MOQ 阈值区块 · 决策点·配置可调非写死）

> business_rules Tab 定位：业务规则配置中枢，当前首期落地 MOQ 阈值子区块，后续可扩展审批规则开关、不追溯策略开关、价格审批阈值枚举、账龄桶配置等。

### X.1 MOQ 阈值子区块布局（Settings 内 Section 组件）

```
┌─ business_rules Tab ───────────────────────────────────────────────────────────────┐
│  ┌─ MOQ 阈值配置（卡片 Section）─────────────────────────────────────────────────┐ │
│  │ ℹ️ 说明：此处配置的是 MOQ 取数优先级第 5 级（系统配置档，见 MOQ最小起订量.md §2.4） │ │
│  │          可被产品档案 / 工厂合同 / 客户协议 / 行级 override 覆盖。                │ │
│  │          已 Confirmed 订单按创建时 Order.moqSnapshot 口径（不追溯原则）。          │ │
│  │                                                                                │ │
│  │ 面料档 fabricDefaultMoq：    [ X-Input number ，单位 米 ]   当前 seed 默认 800    │ │
│  │ 成衣档 garmentDefaultMoq：  [ X-Input number ，单位 件 ]   当前 seed 默认 200    │ │
│  │ Capsule 档 capsuleMoq：     [ X-Input number ，单位 件 ]   当前 seed 默认 20     │ │
│  │                                                                                │ │
│  │ 生效方式：● 立即生效（effectiveFrom=now） ○ 定时生效（[ X-DatePicker 选择时间 ]） │ │
│  │ 变更理由：[ X-Textarea，≥5字必填 ]  例：「2026Q4 工厂产能调整，面料起订量统一上调」  │ │
│  │                                                                                │ │
│  │                                     [ 预览影响订单数（可选）]   [ 提交新配置 ]    │ │
│  └────────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                    │
│  ┌─ 配置变更历史（卡片 Section）───────────────────────────────────────────────────┐ │
│  │ 表头：生效时间 | 面料 | 成衣 | Capsule | 变更人 | 变更理由 | 操作（查看 diff）     │ │
│  │ < 分页表格（默认 20 条，按 changedAt DESC 取 history）>                          │ │
│  └────────────────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────────────┘
```

### X.2 关键交互说明

| 项 | 说明 |
|---|---|
| 守卫显示 | 非 SUPER_ADMIN / ADMIN 打开 `business_rules` Tab → MOQ 配置卡片全部字段 disabled + Toast「暂无权限配置业务规则」+ 历史表格仅读 |
| 提交前校验 | fabricDefaultMoq / garmentDefaultMoq / capsuleMoq 必须为正整数；capsuleMoq 不得高于 garmentDefaultMoq（语义约束：Capsule 是降级豁免不应比成衣档更严格）；changeReason ≥5 字 |
| 乐观锁防冲突 | 提交 body 带 `lastSeenConfigId`（GET current 返回的 configId）→ 服务端校验与 DB 当前 isActive=true 的 ID 一致，不一致返回 409 Conflict「MOQ 配置已被其他管理员变更，请刷新后重试」 |
| 预览影响订单数（可选按钮） | 点击后调用 GET `/api/v1/moq-thresholds/preview-impact` → 返回按 Draft 状态订单估算：若按当前输入值生效，将有多少行订单行触发 MOQ 门禁（纯读接口，不真正修改配置） |
| 提交成功反馈 | Toast 「MOQ 新配置已提交，生效时间：{effectiveFrom}，已 Confirmed 订单不受影响（不追溯），新创建订单/报价按新值执行」+ 新配置自动 append 到历史表格首行 |
| 与 seed 默认值关系 | GET current 若 DB 无 isActive 记录 → 返回 seed 默认 800/200/20 且 payload.source='fallback_constant'，UI 在卡片右上角显示 ⚠️ Badge「当前使用兜底常量，建议首次提交一份配置以记录审计轨迹」 |

---

## §6 服务层架构

### 6.1 文件结构

```
components/
├── Settings.tsx                    # 主组件
├── AutomationRulesSection.tsx     # 自动化规则子组件
└── ui/
    ├── bambookOsTokens.ts          # BAMBOOK_OS token
    ├── PageHeader.tsx              # 页头
    ├── SidePanelContainer.tsx      # 侧边容器
    ├── ScrollEdgeFades.tsx          # 滚动边缘渐变
    ├── UserAvatar.tsx               # 用户头像
    └── osAdaptiveContrast.ts        # 自适应对比度

services/
├── authService.ts                  # 认证服务
├── storageService.ts               # 存储服务
├── apiService.ts                   # 主数据 API
└── knowledgeApiService.ts          # 知识库 API
```

### 6.2 状态管理

```typescript
// 主组件状态
const [activeTab, setActiveTab] = useState<TabId>(() => (mode === 'account' ? 'account' : 'appearance'));
const [localConfig, setLocalConfig] = useState<SystemConfig>(config);
const [accountView, setAccountView] = useState<'overview' | 'modify'>('overview');

// 连通性测试
const [isTestingMainApi, setIsTestingMainApi] = useState(false);
const [isTestingKnowledgeApi, setIsTestingKnowledgeApi] = useState(false);
const [testLogs, setTestLogs] = useState<{ msg; type: 'info'|'success'|'error'|'latency' }[]>([]);

// 密码修改
const [currentPw, setCurrentPw] = useState('');
const [newPw, setNewPw] = useState('');
const [confirmPw, setConfirmPw] = useState('');
const [pwMsg, setPwMsg] = useState<{ ok: boolean; text: string } | null>(null);

// 存储管理
const [storageReport, setStorageReport] = useState<DeviceStorageReport | null>(null);

// 头像裁切
const [avatarCrop, setAvatarCrop] = useState<AvatarCropDraft | null>(null);
```

---

## §7 头像裁切

### 7.1 裁切参数

```typescript
const AVATAR_CROP_PREVIEW_SIZE = 224;  // 预览尺寸
const AVATAR_OUTPUT_SIZE = 256;         // 输出尺寸

interface AvatarCropDraft {
  src: string;            // dataUrl
  fileName: string;
  naturalWidth: number;
  naturalHeight: number;
  scale: number;          // 缩放
  rotation: number;       // 旋转（度）
  offset: { x: number; y: number };  // 偏移
}
```

### 7.2 裁切流程

```typescript
const createCircularAvatarDataUrl = async (draft: AvatarCropDraft): Promise<string> => {
  const img = await loadAvatarImage(draft.src);
  const canvas = document.createElement('canvas');
  canvas.width = AVATAR_OUTPUT_SIZE;
  canvas.height = AVATAR_OUTPUT_SIZE;
  const ctx = canvas.getContext('2d');
  
  // 圆形裁切
  ctx.beginPath();
  ctx.arc(AVATAR_OUTPUT_SIZE/2, AVATAR_OUTPUT_SIZE/2, AVATAR_OUTPUT_SIZE/2, 0, Math.PI*2);
  ctx.clip();
  
  // 旋转 + 缩放 + 偏移
  ctx.translate(AVATAR_OUTPUT_SIZE/2 + draft.offset.x * offsetScale,
                AVATAR_OUTPUT_SIZE/2 + draft.offset.y * offsetScale);
  ctx.rotate((normalizedRotation * Math.PI) / 180);
  ctx.drawImage(img, -drawW/2, -drawH/2, drawW, drawH);
  
  return canvas.toDataURL('image/webp', 0.86);
};
```

### 7.3 输出

- 格式：webp（质量 0.86）
- 尺寸：256×256
- 形状：圆形（clip）
- 上传：base64 通过 `POST /api/v1/auth/avatar`

---

## §8 连通性测试

### 8.1 主数据 API 测试

```typescript
const testMainApi = async () => {
  setIsTestingMainApi(true);
  setTestLogs([]);
  try {
    addLog('主数据 API 健康检查中…', 'info');
    const start = Date.now();
    await apiService.health();
    const latency = Date.now() - start;
    addLog(`✓ 健康检查通过 (${latency}ms)`, 'success');
    addLog(`延迟: ${latency}ms`, 'latency');
  } catch (e) {
    addLog(`✗ 健康检查失败: ${e.message}`, 'error');
  } finally {
    setIsTestingMainApi(false);
  }
};
```

### 8.2 知识库 API 测试

```typescript
const testKnowledgeApi = async () => {
  setIsTestingKnowledgeApi(true);
  setTestLogs([]);
  try {
    addLog('知识库 API 连接测试中…', 'info');
    const result = await knowledgeApiService.testConnection(endpoint, apiKey);
    if (result.ok) {
      addLog(`✓ 连接成功 (${result.testedUrl})`, 'success');
    } else {
      addLog(`✗ 连接失败: ${result.detail}`, 'error');
    }
  } finally {
    setIsTestingKnowledgeApi(false);
  }
};
```

### 8.3 日志类型

| type | 显示 |
| --- | --- |
| `info` | 蓝色（进行中） |
| `success` | 绿色（成功） |
| `error` | 红色（失败） |
| `latency` | 灰色（延迟数据） |

---

## §9 壁纸预设

### 9.1 分组

| 组 | 数量 | 示例 |
| --- | --- | --- |
| 极简 | 4 | 蓝羽流光 / 蓝紫柔光 / 赛博光束 / 经典渐变 |
| 自然 | 5 | 湖镜列车 / 雪浪成墙 / 雪峰流云 / 碧浪卷心 / 雾海灰潮 |
| 城市 | 1 | 沪上暮光 |
| 动漫 | 2 | 星落晚窗 / 暮野孤影 |
| 纯色 | 16 | 纯黑/纯白/主题蓝/雾蓝/湖青/暮紫/石墨/暖灰/... |

### 9.2 预览样式

```typescript
const DEFAULT_WALLPAPER_PREVIEW_LIGHT_STYLE = {
  backgroundImage: [
    'radial-gradient(circle at 12% 8%, rgba(93,224,230,0.22), transparent 34%)',
    'radial-gradient(circle at 86% 92%, rgba(0,74,173,0.16), transparent 36%)',
    'radial-gradient(circle at 52% 48%, rgba(213,229,242,0.34), transparent 42%)',
    'linear-gradient(135deg, #DDE8F2 0%, #CFDEEC 48%, #BCCFE1 100%)',
  ].join(', '),
};
```

---

## §10 SystemConfig 数据模型

### 10.1 表结构

```prisma
model SystemConfig {
  id          String   @id          // `${scope}::${key}`
  scope       String   @default("global")
  key         String                  // 同 scope 唯一
  group       String                  // company/finance/tax/logistics/ui/rbac/ops/custom
  label       String                  // OPS Panel 展示用
  valueType   String                  // 'string'|'number'|'boolean'|'json'|'enum'
  value       Json?                    // 加密则存 "${iv.hex}.${cipher.hex}.${tag.hex}"
  encrypted   Boolean  @default(false) // AES-GCM
  version     Int      @default(1)
  description String?
  meta        Json?                    // { options?, min?, max?, placeholder? }
  updatedBy   String?
  auditReason String?
  createdAt   DateTime @default(now())
  updatedAt   BigInt

  @@unique([scope, key])
  @@index([group])
  @@index([scope])
  @@index([encrypted])
}
```

### 10.2 分组语义

| group | 用途 |
| --- | --- |
| `company` | 公司基本信息（名称/地址/联系方式） |
| `finance` | 财务（默认币种/汇率/利润比例/佣金率/账期阈值） |
| `tax` | 税务（VAT 率/退税率/开票限额/抬头） |
| `logistics` | 物流（Incoterm/起运港/货代） |
| `ui` | 界面（主题色/Logo/Dashboard 布局/语言） |
| `rbac` | 权限（密码长度/会话 TTL/锁定阈值） |
| `ops` | 运维（SMTP/OPS_TOKEN/备份/LLM 网关） |
| `custom` | 自定义 |

### 10.3 加密配置

- AES-GCM 加密
- 敏感配置（API_KEY / DB_URL / SMTP_PASSWORD / LLM_KEY）必须 `encrypted=true`
- 存储格式：`${iv.hex}.${cipher.hex}.${tag.hex}`

---

## §11 交互流程

### 11.1 修改密码

```
用户进入 account Tab → 点击「修改密码」
  └─► accountView = 'modify'
      └─► 输入 currentPw + newPw + confirmPw
          └─► authService.changePassword(currentPw, newPw)
              ├─► 200: setPwMsg({ ok: true, text: '密码已更新' })
              └─► 400: setPwMsg({ ok: false, text: '原密码错误' })
              └─► 422: setPwMsg({ ok: false, text: '新密码不符合复杂度要求' })
```

### 11.2 上传头像

```
用户点击头像 → 选择图片
  └─► readImageFileAsDataUrl(file) → dataUrl
      └─► setAvatarCrop({ src, fileName, naturalWidth, naturalHeight, scale:1, rotation:0, offset:{x:0,y:0} })
          └─► 用户调整裁切（拖动/缩放/旋转）
              └─► 点击「保存」
                  └─► createCircularAvatarDataUrl(avatarCrop) → webp dataUrl
                      └─► authService.updateMyProfile({ avatarUrl })
                          ├─► 200: 更新本地状态 + 显示成功
                          └─► 失败: 显示错误
```

### 11.3 切换主题

```
用户在 appearance Tab 选择「深色模式」
  └─► setLocalConfig({ ...localConfig, themeMode: 'dark' })
      └─► onUpdateConfig(localConfig)
          └─► 父组件持久化到 IndexedDB + 应用主题
```

---

## §12 权限与访问控制

### 12.1 前端权限

- account Tab：所有已登录用户可访问（个人资料）
- 其他 Tab：要求 `settings:read` 权限
- automation Tab：要求 `settings:write` 权限修改规则

### 12.2 后端权限

- `/api/v1/auth/me`：JWT 鉴权（任何角色）
- `/api/v1/system-config` GET：JWT + `settings:read`
- `/api/v1/system-config` PATCH：JWT + `settings:write` + owner/admin
- 加密配置读取：仅 owner/admin 可见明文（其他角色返回 mask）

### 12.3 SystemConfig 加密保护

- `encrypted=true` 的配置，GET 时：
  - owner/admin → 返回明文 value
  - 其他角色 → 返回 `null` + `description: 'encrypted'`
- 防止 API_KEY / SMTP_PASSWORD 泄露给普通用户

---

## §13 审计与可观测性

### 13.1 审计日志

| 操作 | operation | targetType | 说明 |
| --- | --- | --- | --- |
| 修改密码 | `auth_change_password` | `UserAccount` | 不记录密码明文 |
| 上传头像 | `auth_update_avatar` | `UserAccount` | 记录 avatarUrl |
| 更新个人资料 | `auth_update_profile` | `UserAccount` | 记录变更字段 |
| 更新系统配置 | `system_config_update` | `SystemConfig` | before/after + versionFrom/To |
| 登出 | `auth_logout` | `UserAccount` | 记录 ip + userAgent |

### 13.2 SystemConfigHistory

```prisma
model SystemConfigHistory {
  id          String   @id
  configId    String   // 关联 SystemConfig.id
  versionFrom Int
  versionTo   Int
  valueFrom   Json?
  valueTo     Json?
  actorId     String?
  reason      String?
  sensitiveMasked Boolean @default(false)  // 加密配置历史不记录明文
  createdAt   DateTime @default(now())

  @@index([configId, createdAt])
  @@index([actorId])
}
```

---

## §14 测试覆盖

| 测试维度 | 覆盖点 |
| --- | --- |
| 双模式切换 | account → system 切换默认 Tab |
| 头像裁切 | Canvas 圆形裁切 + 旋转 + 缩放 + 偏移 |
| 密码修改 | 原密码校验 + 新密码复杂度 + 确认密码一致 |
| 连通性测试 | 主数据 + 知识库双端 + 延迟日志 |
| 主题切换 | light/dark/system 三态 |
| 壁纸预设 | 28 张预设 + 5 分组 |
| 自动化规则 | 启停 + 乐观更新 + 失败回滚 |
| 存储管理 | 报告获取 + 缓存清理 |
| SystemConfig | CRUD + 加密 + 版本历史 |

---

## §15 已知限制与未来扩展

### 15.1 当前限制

- ❌ 用户偏好（localConfig）存 IndexedDB，**跨设备不同步**（仅 SystemConfig 跨设备）
- ❌ 头像裁切无**触屏手势**支持（仅鼠标拖动）
- ❌ 壁纸预设**不可自定义**（仅 28 张预设）
- ❌ 无**配置导入/导出**功能
- ❌ automation Tab 规则**不可自定义**（仅预置 9 条规则的启停）

### 15.2 未来扩展

- 用户偏好云端同步（通过 SystemConfig scope='user::{userId}'）
- 头像触屏手势（pinch-to-zoom）
- 自定义壁纸上传
- 配置导入/导出（JSON）
- 自定义自动化规则（事件 + 条件 + 动作）

---

## §16 交叉链接

1. [公司档案与配置](./公司档案与配置.md) — company Tab 的详细设计
2. [设计系统实验室 UiLab](./设计系统实验室UiLab.md) — appearance Tab 的设计系统部分
3. [管理后台 AdminPanel](./管理后台AdminPanel.md) — SystemConfig 的 OPS Panel 可视化
4. [业务自动化触发器](./业务自动化触发器.md) — automation Tab 的详细设计
5. [Knowledge 知识库 — 模块概述](../Knowledge-知识库/模块概述.md) — ai Tab 的 RAG 连接测试
6. [通知-邮件-季节-展会数据模型](../数据模型/通知-邮件-季节-展会.md) — SystemConfig 与 Notification 配置关联

---

## §17 变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v1.0 | 2026-08-15 | 初始版本：10 Tab + 双模式 + 头像裁切 + 连通性测试 + SystemConfig + 加密 |
