# 公司档案与配置设计

> 模块代号：`Settings / Company Profile`
> 父模块：[Settings 账户与系统配置](./账户与系统配置.md)
> 关联代码：`components/Settings.tsx`（company Tab 1453–1536 行）、`types.ts`（SystemConfig.exporterProfile）、`server/prisma/schema.prisma`（SystemConfig 模型 4842–4875 行，group='company'）
> 文档版本：v1.0 · 最后更新：2026-08-15

---

## §1 实现状态

| 维度 | 状态 | 说明 |
| --- | --- | --- |
| 前端组件 | ✅ 已落地 | `Settings.tsx` company Tab：出口方/公司抬头 + 银行信息两大卡片，10+ 字段表单。 |
| 数据结构 | ✅ 已落地 | `SystemConfig.exporterProfile`：nameEn / beneficiary / addressEn / bankName / swiftCode / bankAddress / usdAccountNumber 等字段。 |
| 配置持久化 | ✅ 已落地 | 通过 `localConfig` + `onUpdateConfig` 写入 SystemConfig（scope='global', group='company'）。 |
| 单据引用 | ✅ 已落地 | exporterProfile 被 Commercial Invoice / Packing List / Contract / 报价单等外贸单据引用。 |
| 占位符示例 | ✅ 已落地 | 每个字段含真实业务占位符（如「JIANGSU PANDA CLOTHING CO.,LTD.」「BKCHCNBJ95L」）。 |
| BDS v2.1 主题 | ✅ 已落地 | 使用 `BAMBOOK_OS` token + flat 设计 + 大圆角 + 半透明膜色。 |
| 实时生效 | ✅ 已落地 | 修改后立即生效（无需重启），通过 `handleUpdate('exporterProfile', ...)` 更新。 |
| 审计 | ✅ 已落地 | SystemConfig 变更写 `SystemConfigHistory`，记录 versionFrom/To + actorId + reason。 |
| 多币种账号 | ⚠️ 部分落地 | 当前仅 USD 账号字段；EUR/CNY 等多币种账号未独立字段。 |
| 公司中文名 | ❌ 未落地 | 仅 nameEn（英文），无 nameCn（中文）。 |

---

## §2 业务定位与目标

### 2.1 业务定位

公司档案是 Bambook 的「**出口方真源**」——所有外贸单据（CI/PL/Contract/Quote）的抬头信息、银行付款信息、收款账号均从此处引用，确保：

- **一致**：所有单据使用同一套抬头，避免字段不一致
- **可改**：修改后立即生效，后续生成的单据自动引用新值
- **可审**：变更历史可追溯

### 2.2 核心目标

1. **单据抬头真源**：exporterProfile 是 CI/PL/Contract 的唯一抬头来源
2. **银行信息集中**：付款条款引用的银行信息集中管理
3. **实时生效**：修改后立即写入 SystemConfig，后续单据引用新值
4. **审计可追溯**：SystemConfigHistory 记录每次变更
5. **占位符引导**：每个字段含真实业务占位符，引导用户填写

---

## §3 核心概念与术语

| 术语 | 定义 |
| --- | --- |
| `exporterProfile` | 出口方档案对象，含公司英文名/受益人/地址/银行信息 |
| `nameEn` | 公司英文名（单据抬头） |
| `beneficiary` | 受益人名（银行/保险单据） |
| `addressEn` | 公司英文地址（多行） |
| `bankName` | 银行名称 |
| `swiftCode` | SWIFT Code（银行识别码） |
| `bankAddress` | 银行地址 |
| `usdAccountNumber` | USD 账号 |
| `handleUpdate` | 前端更新函数：`handleUpdate('exporterProfile', { ...localConfig.exporterProfile, [field]: value })` |
| `SystemConfig` | 系统配置表，scope='global', group='company' |

---

## §4 数据模型

### 4.1 exporterProfile 结构

```typescript
interface ExporterProfile {
  // 公司抬头
  nameEn: string;            // 公司英文名（单据抬头）
  beneficiary: string;      // 受益人名（银行/保险单据）
  addressEn: string;        // 公司英文地址（多行）

  // 银行信息
  bankName: string;          // 银行名称
  swiftCode: string;         // SWIFT Code
  bankAddress: string;       // 银行地址
  usdAccountNumber: string;  // USD 账号
}
```

### 4.2 SystemConfig 存储

```prisma
// 存储示例
{
  id: "global::company.exporterProfile",
  scope: "global",
  key: "company.exporterProfile",
  group: "company",
  label: "出口方档案",
  valueType: "json",
  value: { nameEn: "JIANGSU PANDA CLOTHING CO.,LTD.", ... },
  encrypted: false,        // 非敏感，不加密
  version: 3,              // 修改 3 次
  updatedBy: "user_xxx",
  auditReason: "更新 USD 账号"
}
```

### 4.3 SystemConfigHistory

```prisma
// 变更历史
{
  id: "hist_xxx",
  configId: "global::company.exporterProfile",
  versionFrom: 2,
  versionTo: 3,
  valueFrom: { ...oldValue },
  valueTo: { ...newValue },
  actorId: "user_xxx",
  reason: "更新 USD 账号",
  sensitiveMasked: false,
  createdAt: 2026-08-15T...
}
```

---

## §5 字段详情

### 5.1 出口方/公司抬头卡片

| 字段 | 类型 | 占位符 | 用途 |
| --- | --- | --- | --- |
| `nameEn` | string | `JIANGSU PANDA CLOTHING CO.,LTD.` | CI/PL/Contract 抬头 |
| `beneficiary` | string | `JIANGSU PANDA CLOTHING CO.,LTD.` | 银行/保险单据受益人 |
| `addressEn` | textarea | `ROOM A1028 WUYUE PLAZA,\nZHANGJIAGANG CITY, 215600 PR\nCHINA` | 单据地址（多行） |

### 5.2 银行信息卡片

| 字段 | 类型 | 占位符 | 用途 |
| --- | --- | --- | --- |
| `bankName` | string | `BANK OF CHINA ZHANGJIAGANG SUB-BRANCH` | CI 付款条款引用 |
| `swiftCode` | string | `BKCHCNBJ95L` | 银行识别码 |
| `bankAddress` | string | `111 MIDDLE RENMIN ROAD, ZHANGJIAGANG CITY, SUZHOU, JIANGSU PROV., P.R.CHINA.` | 银行地址 |
| `usdAccountNumber` | string | `467668133096` | USD 收款账号 |

### 5.3 字段说明

- **nameEn vs beneficiary**：通常相同，但某些场景受益人可能是子公司或关联公司
- **addressEn 多行**：用换行符分隔，单据渲染时保留换行
- **swiftCode**：8 或 11 位字母数字，银行唯一识别码
- **usdAccountNumber**：美元收款账号，用于 T/T 付款

---

## §6 API 端点

### 6.1 读取

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | `/api/v1/system-config/global::company.exporterProfile` | 获取出口方档案 |

### 6.2 更新

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| PATCH | `/api/v1/system-config/global::company.exporterProfile` | 更新出口方档案（version +1 + 审计） |

### 6.3 历史

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | `/api/v1/system-config/global::company.exporterProfile/history` | 获取变更历史 |

### 6.4 请求/响应

```typescript
// PATCH 请求
{
  value: { nameEn, beneficiary, addressEn, bankName, swiftCode, bankAddress, usdAccountNumber },
  auditReason?: string  // 可选：变更原因
}

// 响应
{
  ok: true,
  config: { id, scope, key, group, label, valueType, value, version, updatedAt },
  historyId: string
}
```

---

## §7 前端组件

### 7.1 company Tab 结构

```tsx
{activeTab === 'company' && (
  <div className="space-y-6">
    {/* 卡片 1：出口方/公司抬头 */}
    <div className={`${card} p-5`}>
      <div>出口方/公司抬头</div>
      <p>用于 Commercial Invoice / Packing List / Contract / 报价单等外贸单据的抬头信息。修改后立即生效。</p>
      <div className="grid grid-cols-1 gap-4 sm:grid-cols-2">
        <label>公司英文名（单据抬头）<input value={localConfig.exporterProfile?.nameEn ?? ''} /></label>
        <label>受益人名（银行/保险单据）<input value={localConfig.exporterProfile?.beneficiary ?? ''} /></label>
        <label className="sm:col-span-2">公司英文地址（多行用换行）<textarea value={localConfig.exporterProfile?.addressEn ?? ''} /></label>
      </div>
    </div>

    {/* 卡片 2：银行信息 */}
    <div className={`${card} p-5`}>
      <div>银行信息</div>
      <p>CI 付款条款引用的银行信息。</p>
      <div className="grid grid-cols-1 gap-4 sm:grid-cols-2">
        <label>银行名称<input value={localConfig.exporterProfile?.bankName ?? ''} /></label>
        <label>SWIFT Code<input value={localConfig.exporterProfile?.swiftCode ?? ''} /></label>
        <label className="sm:col-span-2">银行地址<input value={localConfig.exporterProfile?.bankAddress ?? ''} /></label>
        <label>USD 账号<input value={localConfig.exporterProfile?.usdAccountNumber ?? ''} /></label>
      </div>
    </div>
  </div>
)}
```

### 7.2 更新逻辑

```typescript
const handleUpdate = (key: string, value: any) => {
  const next = { ...localConfig, [key]: value };
  setLocalConfig(next);
  onUpdateConfig(next);  // 通知父组件持久化
};
```

- 修改后立即调用 `onUpdateConfig`（无需点击「保存」按钮）
- 父组件负责持久化到 SystemConfig

### 7.3 布局

- **桌面**：`grid-cols-1 sm:grid-cols-2`，两列布局
- **地址/银行地址**：`sm:col-span-2`，跨两列
- **textarea**：`min-h-[60px] resize-y`，可调整高度

---

## §8 单据引用

### 8.1 引用场景

| 单据 | 引用字段 | 用途 |
| --- | --- | --- |
| Commercial Invoice | nameEn + addressEn | 卖方抬头 |
| Packing List | nameEn + addressEn | 卖方抬头 |
| Contract | nameEn + addressEn | 甲方/卖方 |
| Quotation | nameEn + addressEn | 报价方 |
| 报关单 | nameEn + addressEn | 发货单位 |
| 银行单据 | beneficiary + bankName + swiftCode + bankAddress + usdAccountNumber | 收款信息 |

### 8.2 引用方式

- 单据生成时从 `SystemConfig` 读取 `exporterProfile`
- 渲染到单据模板的 `{{exporterName}}` `{{exporterAddress}}` `{{bankName}}` 等占位符
- 修改 exporterProfile 后，新生成的单据自动引用新值

---

## §9 交互流程

### 9.1 修改公司抬头

```
用户进入 Settings → company Tab
  └─► 修改「公司英文名」字段
      └─► onChange: handleUpdate('exporterProfile', { ...localConfig.exporterProfile, nameEn: value })
          └─► setLocalConfig(next) + onUpdateConfig(next)
              └─► 父组件持久化到 SystemConfig
                  └─► PATCH /api/v1/system-config/global::company.exporterProfile
                      ├─► 200: version +1 + SystemConfigHistory 记录
                      └─► 失败: 回滚（前端显示错误）
```

### 9.2 单据引用

```
用户生成 Commercial Invoice
  └─► 单据引擎读取 SystemConfig.exporterProfile
      └─► 渲染到模板：
          ├─ 卖方：{{exporterProfile.nameEn}}
          ├─ 地址：{{exporterProfile.addressEn}}
          └─ 付款条款：T/T 30 days to {{exporterProfile.bankName}} {{exporterProfile.swiftCode}} {{exporterProfile.usdAccountNumber}}
```

---

## §10 权限与访问控制

### 10.1 前端权限

- company Tab 入口要求 `settings:read` 权限
- 修改要求 `settings:write` 权限
- 通常仅 owner/admin/finance 角色可修改

### 10.2 后端权限

- GET SystemConfig：JWT + `settings:read`
- PATCH SystemConfig：JWT + `settings:write` + owner/admin
- exporterProfile 非加密，所有有 `settings:read` 的角色可见

### 10.3 敏感字段保护

- `usdAccountNumber` 当前**非加密**（便于单据引擎读取）
- 未来扩展：敏感字段加密，单据引擎通过专用解密 API 读取

---

## §11 审计与可观测性

### 11.1 审计日志

| 操作 | operation | targetType | detail |
| --- | --- | --- | --- |
| 更新公司档案 | `system_config_update` | `SystemConfig` | `{ configId, versionFrom, versionTo, changedFields: ['nameEn', 'bankName'] }` |

### 11.2 SystemConfigHistory

- 每次修改自动记录 `SystemConfigHistory`
- `valueFrom` / `valueTo` 保留完整 JSON 快照
- `sensitiveMasked: false`（exporterProfile 非敏感）
- 可按 `configId + createdAt` 查询历史

### 11.3 不变量

- version 每次修改 +1
- 审计与业务同事务（失败回滚）
- `updatedBy` 从 JWT 解析

---

## §12 测试覆盖

| 测试维度 | 覆盖点 |
| --- | --- |
| 字段渲染 | 7 个字段正确渲染 + 占位符 |
| 修改更新 | onChange 触发 handleUpdate + onUpdateConfig |
| 持久化 | SystemConfig PATCH + version +1 |
| 历史记录 | SystemConfigHistory 写入 + valueFrom/To |
| 单据引用 | CI/PL/Contract 引用 exporterProfile |
| 多行地址 | textarea 换行保留 |
| 布局响应 | 桌面两列 / 移动单列 |
| 权限 | `settings:write` 限制 |

---

## §13 已知限制与未来扩展

### 13.1 当前限制

- ❌ 仅 USD 账号字段，**无多币种账号**（EUR/CNY/HKD 等）
- ❌ 仅英文名/地址，**无中文**（部分国内单据需中文）
- ❌ 无**公司 Logo 上传**（单据抬头 Logo）
- ❌ 无**多套抬头**（子公司/关联公司场景需多套）
- ❌ `usdAccountNumber` 非加密（敏感信息）
- ❌ 无**SWIFT Code 格式校验**（8 或 11 位字母数字）

### 13.2 未来扩展

- 多币种账号：`accounts: { USD: '...', EUR: '...', CNY: '...' }`
- 中英文双套：`nameCn` / `nameEn` / `addressCn` / `addressEn`
- 公司 Logo 上传 + 单据引用
- 多套抬头：`profiles: ExporterProfile[]` + 默认标记
- SWIFT Code 格式校验
- 敏感字段加密（AES-GCM）

---

## §14 实现备注

### 14.1 占位符来源

所有占位符均来自真实业务示例（非 Lorem Ipsum）：
- 公司名：`JIANGSU PANDA CLOTHING CO.,LTD.`
- 银行：`BANK OF CHINA ZHANGJIAGANG SUB-BRANCH`
- SWIFT：`BKCHCNBJ95L`
- 地址：`ROOM A1028 WUYUE PLAZA, ZHANGJIAGANG CITY, 215600 PR CHINA`
- 账号：`467668133096`

### 14.2 实时生效

- 修改后立即调用 `onUpdateConfig`（无「保存」按钮）
- 父组件负责防抖（debounce）+ 持久化
- 失败时前端回滚 + 显示错误

### 14.3 单据引擎集成

- 单据生成时从 SystemConfig 读取 exporterProfile
- 不缓存（每次生成时读取最新值）
- 渲染到模板占位符

---

## §15 交叉链接

1. [账户与系统配置](./账户与系统配置.md) — 父模块，company Tab 是 10 Tab 之一
2. [管理后台 AdminPanel](./管理后台AdminPanel.md) — SystemConfig 的 OPS Panel 可视化（group='company'）
3. [DocumentCenter 单据中心 — 模块概述](../../04-出货与单据/DocumentCenter-单据中心/模块概述.md) — 单据引擎引用 exporterProfile
4. [Customs 报关与退税 — 模块概述](../../04-出货与单据/Customs-报关与退税/模块概述.md) — 报关单引用出口方信息
5. [FinanceManager 总览](../../05-财务与结算/FinanceManager总览.md) — 收款凭证引用银行信息
6. [通知-邮件-季节-展会数据模型](../数据模型/通知-邮件-季节-展会.md) — SystemConfig 数据模型
7. [BusinessTools 文档工具](../BusinessTools-业务工具/文档工具.md) — 样品发票/合同/装箱单引用 exporterProfile

---

## §16 变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v1.0 | 2026-08-15 | 初始版本：exporterProfile + 银行信息 + 单据引用 + 审计 + SystemConfig 存储 |
