# 数据工具与导入向导 (Data Tools & Import Wizard)

> 模块代号：`BusinessTools / Data Tools`
> 父模块：[BusinessTools 业务工具箱](../)
> 关联代码：`components/BusinessTools.tsx`（po-import / mes-console 跳转卡）、`components/import/ImportWizard.tsx`、`components/import/StepUpload.tsx`、`components/import/StepPreview.tsx`、`components/import/StepConfirm.tsx`、`components/import/QuotationImportWizard.tsx`、`services/importService.ts`、`server/src/import/route.ts`、`server/src/import/parseOrderPdf.ts`、`server/src/import/detectCustomer.ts`、`server/src/import/registry.ts`、`server/src/import/persistOrders.ts`、`server/src/import/extractText.ts`、`server/src/import/types.ts`
> 文档版本：v1.0 · 最后更新：2026-08-15

---

## §1 实现状态

| 维度 | 状态 | 说明 |
| --- | --- | --- |
| PO 文件导入跳转卡 | ✅ 已落地 | BusinessTools.tsx 第 68-73 行：`po-import` 跳转卡 → `onNavigate(View.Orders, undefined)` → 订单页打开导入向导。 |
| MES 控制台跳转卡 | ✅ 已落地 | BusinessTools.tsx 第 74-80 行：`mes-console` 跳转卡 → `onNavigate(View.MES)` → MES 模块。 |
| 订单导入向导（PDF） | ✅ 已落地 | `ImportWizard.tsx`（3 步流程：Upload → Preview → Confirm）+ StepUpload/StepPreview/StepConfirm 子组件。 |
| 报价导入向导（Excel） | ✅ 已落地 | `QuotationImportWizard.tsx`（3 步流程：Select → Validate → Confirm）+ xlsx 前端解析 + 后端 preview/commit 双模式。 |
| PDF 文本抽取 | ✅ 已落地 | `extractText.ts`：基于 pdf-parse 抽取 PDF 文本 + 页数统计。 |
| 客户自动识别 | ✅ 已落地 | `detectCustomer.ts`：5 条规则（Peerless 法人名/QST/GST/联系人/PO 号模式）加权打分，超过阈值返回 customerId。 |
| 客户解析器注册表 | ✅ 已落地 | `registry.ts`：`parsersByCustomer` 映射，目前仅注册 `peerless: parsePeerless` 1 个解析器。 |
| 订单持久化 | ✅ 已落地 | `persistOrders.ts`：upsert by poNumber + per-field overwrite protection（fieldSources 标记 pdf/manual/imported-then-edited）+ 事务原子性 + 部分成功容忍。 |
| 文件上传服务 | ✅ 已落地 | `services/importService.ts`：`uploadPdfsForParsing` / `saveParsedOrders` / `createManualOrder` / `updateOrderFields` / `deleteOrder` 五个 API 封装。 |
| 后端导入路由 | ✅ 已落地 | `server/src/import/route.ts`：Express Router + multer 内存存储 + 单文件 10MB + 单次最多 20 文件 + API Key 鉴权（可选）。 |
| 表头别名映射 | ✅ 已落地 | QuotationImportWizard 第 43-53 行：HEADER_ALIASES 支持中英文表头别名（报价号/quotationnumber/qt no 等）。 |
| 标准模板下载 | ✅ 已落地 | QuotationImportWizard 第 55-56 行：TEMPLATE_HEADERS + TEMPLATE_EXAMPLE 模板下载（PRD 16.2）。 |
| 状态中文映射 | ✅ 已落地 | QuotationImportWizard 第 58-64 行：STATUS_ZH_TO_ENUM（草稿→Draft/已发送→Sent/已接受→Accepted/已拒绝→Rejected/已过期→Expired）。 |
| Excel 日期归一化 | ✅ 已落地 | `normalizeDateCell`：兼容 Date 对象 / Excel 序列号（1899-12-30 起算）/ 字符串三种输入。 |
| BDS v2.1 主题 | ✅ 已落地 | 全部组件使用 `var(--text-primary)` / `var(--border-c-default)` 等 token；rgba() 任意值绕开 flat-experimental 护栏保持虚线描边可见。 |
| ESC 关闭 | ✅ 已落地 | ImportWizard 第 46-53 行：键盘事件监听 Escape 关闭向导。 |
| 解析失败容忍 | ✅ 已落地 | 单文件解析失败不阻断整体流程，失败文件在 Confirm 步骤显示但不入库。 |

---

## §2 业务定位与目标

### 2.1 业务定位

数据工具与导入向导是 Bambook「**业务工具箱**」中的**数据入口工具集合**——支持用户从外部文件（PDF/Excel/CSV）批量导入订单/报价数据，减少手工录入：

```
PDF / Excel / CSV ──► 导入向导 ──► 解析 ──► 预览/修正 ──► 入库
```

### 2.2 两类导入向导分工

| 维度 | 订单导入向导（ImportWizard） | 报价导入向导（QuotationImportWizard） |
| --- | --- | --- |
| 输入格式 | PDF（客户 PO 单扫描件） | Excel/CSV（结构化表格） |
| 解析方式 | 后端 pdf-parse 抽取文本 + 客户规则识别 + 客户专属解析器 | 前端 xlsx 解析为行 JSON |
| 客户识别 | 5 条规则加权打分（peerless 法人名/税号/联系人/PO 号模式） | 表头别名映射（中英文兼容） |
| 解析器 | `parsersByCustomer.peerless`（1 个） | 通用（无需客户专属解析器） |
| 入库接口 | `POST /api/v1/orders/import` | `POST /api/v1/quotations/import`（mode=preview/commit） |
| 幂等机制 | poNumber upsert + per-field overwrite protection | 报号已存在自动跳过 |
| 步骤数 | 3 步（Upload → Preview → Confirm） | 3 步（Select → Validate → Commit） |
| 上限 | 单次最多 20 个 PDF，单文件 10MB | 单次最多 2000 行 |

### 2.3 核心目标

1. **业务员友好**：PDF 扫描件直接上传，系统自动识别客户并解析
2. **预览与修正**：解析结果可人工编辑后再入库，避免错误数据入库
3. **幂等性**：同一 PO/报号重复导入不创建副本，按业务键 upsert
4. **字段保护**：用户手填字段不会被后续 PDF 重新导入覆盖（fieldSources 标记机制）
5. **标准模板**：报价导入提供标准 Excel 模板下载，含字段说明和示例
6. **失败容忍**：单文件/单行失败不阻断整体流程

---

## §3 核心概念与术语

| 术语 | 定义 |
| --- | --- |
| `ImportWizard` | 订单导入向导（PDF）主组件 |
| `QuotationImportWizard` | 报价导入向导（Excel/CSV）主组件 |
| `StepUpload` / `StepPreview` / `StepConfirm` | 三步流程子组件 |
| `FileEntry` | 上传文件条目：`{ id, file, status, message? }` |
| `FileStatus` | 文件状态：pending / parsing / done / error |
| `ImportFileResult` | 单文件解析结果：`{ filename, pages, detection, order?, error? }` |
| `ParsedOrder` | 解析后的订单结构：header + lines + totals |
| `ParsedLine` | 解析后的订单行：itemNo/materialCode/quantity/unitPrice 等 |
| `DetectionResult` | 客户识别结果：`{ customerId, confidence, reasons }` |
| `parsersByCustomer` | 客户专属解析器注册表 |
| `parsePeerless` | Peerless 客户专属解析器（唯一已注册） |
| `PersistResult` | 持久化结果：`{ poNumber, orderId, action, linesSaved, skippedFields }` |
| `FieldSourceTag` | 字段来源标签：pdf / manual / imported-then-edited |
| `applyOverwriteProtection` | 字段级覆盖保护：manual/imported-then-edited 标签的字段不被 PDF 重导覆盖 |
| `HEADER_ALIASES` | 报价导入表头别名映射（中英文兼容） |
| `STATUS_ZH_TO_ENUM` | 中文状态 → 英文枚举映射 |
| `normalizeDateCell` | Excel 日期归一化（兼容 Date/序列号/字符串） |
| `MAX_ROWS` | 报价导入单次最大行数（2000） |
| `mode=preview` | 报价导入只校验不写库模式 |
| `mode=commit` | 报价导入合法行落库模式 |

---

## §4 跳转卡机制

### 4.1 BusinessTools 跳转卡清单

```typescript
// components/BusinessTools.tsx 第 67-80 行
const tools: Tool[] = [
  {
    id: 'po-import',
    name: 'PO 文件导入',
    description: '跳转订单管理页，使用 PO 导入向导',
    icon: Upload,
    targetView: View.Orders,
    // 注意：不指定 targetTab，由 Orders 模块自行决定打开方式
  },
  {
    id: 'mes-console',
    name: '生产执行 MES',
    description: '工位排产 · 工时 · 计件 · 外协加工（可选模块，非核心流程）',
    icon: Cog,
    targetView: View.MES,
  },
  // ...其他工具
];
```

### 4.2 跳转 → 订单页 → 打开向导流程

```
用户点击「PO 文件导入」跳转卡
  └─► handleToolClick(tool)
        └─► onNavigate(View.Orders, undefined)
              └─► 父组件 setActiveView(View.Orders)

订单页挂载
  └─► 顶部按钮「批量导入」
        └─► setImportWizardOpen(true)
              └─► <ImportWizard isOpen={true} onConfirm={handleImportConfirm} ... />

向导 onConfirm 回调
  └─► handleImportConfirm(orders: ParsedOrder[])
        └─► saveParsedOrders(orders)  // 调 /v1/orders/import
              └─► 成功后刷新订单列表
```

---

## §5 订单导入向导（ImportWizard）

### 5.1 三步流程

```
Step 1: 选择文件（StepUpload）
  ├─ 拖拽 PDF 到虚线框 / 点击选择文件
  ├─ 多选支持，仅接收 PDF 格式
  ├─ 文件列表：文件名/大小/状态（pending/parsing/done/error）
  └─ 点击「上传并解析」按钮

Step 2: 预览 / 修正（StepPreview）
  ├─ Tab 切换各文件预览
  ├─ 显示客户识别结果（customerId + confidence）
  ├─ 订单头部字段（poNumber/season/poDate/currency/...）可编辑
  ├─ 订单行（itemNo/materialCode/quantity/unitPrice/...）可编辑
  ├─ 解析失败文件显示错误信息（确认时跳过）
  └─ 点击「继续」按钮

Step 3: 确认（StepConfirm）
  ├─ 导入摘要：文件数 / 可入库订单数 / 解析失败数
  ├─ 按币种合计金额
  ├─ 逐文件状态列表
  └─ 点击「完成」按钮 → onConfirm(orders) → onClose()
```

### 5.2 数据流

```
Step 1: 用户选文件 → FileEntry[]
  └─► goNext() 调 uploadPdfsForParsing(files)
        └─► POST /api/v1/import/order (multipart/form-data)
              └─► 服务端 multer 接收 + parseOrderPdf(buffer)
                    ├─► extractPdfText(buffer) → { text, pages }
                    ├─► detectCustomer(text) → { customerId, confidence, reasons }
                    └─► parsersByCustomer[customerId](text) → ParsedOrder
                          └─► 返回 ImportFileResult { filename, pages, detection, order, error }
        └─► 前端按文件名顺序对齐 results 与 files
              └─► setFiles(updated) + setResults(ordered) + setStep(2)

Step 2: 用户编辑 → results 更新
  └─► goNext() → setStep(3)

Step 3: 用户点击「完成」
  └─► goNext() 调 onConfirm(results.filter(r => r.order && !r.error).map(r => r.order!))
        └─► 父组件调 saveParsedOrders(orders)
              └─► POST /api/v1/orders/import (JSON)
                    └─► persistOrders(prisma, orders, { mode: 'overwrite-pdf-fields-only' })
                          └─► 逐订单事务 upsert + applyOverwriteProtection
```

### 5.3 文件状态机

```
pending ──click upload──► parsing ──success──► done
                              │
                              └──error──► error
```

| 状态 | UI 表现 |
| --- | --- |
| pending | 灰色「待处理」 |
| parsing | Loader2 旋转「解析中」 |
| done | CheckCircle2「完成」 |
| error | AlertCircle「失败」+ 错误信息 |

### 5.4 步骤指示器

```
[1 选择文件] ──► [2 预览/修正] ──► [3 确认]
   当前                已完成              已完成
```

- StepDot 组件：圆形 + 数字/图标
- 当前步骤：蓝色填充 + 白字
- 已完成步骤：浅蓝填充 + 图标
- 未到达步骤：灰色 + 数字
- Connector 组件：步骤间横线，到达后变蓝

---

## §6 PDF 解析与客户识别

### 6.1 PDF 文本抽取（extractText.ts）

```typescript
// 使用 pdf-parse 库
import pdfParse from 'pdf-parse';

export async function extractPdfText(buffer: Buffer): Promise<{ text: string; pages: number }> {
  const data = await pdfParse(buffer);
  return {
    text: data.text,
    pages: data.numpages,
  };
}
```

- 输入：PDF Buffer（multer 内存存储）
- 输出：纯文本 + 页数
- 错误处理：抛出由 parseOrderPdf 捕获并转为 error 字段

### 6.2 客户识别（detectCustomer.ts）

5 条规则加权打分：

| # | 规则 | 正则 | 权重 | 命中说明 |
| --- | --- | --- | --- | --- |
| 1 | 法人名 | `/V[êe]tements Peerless Clothing Inc\./i` | 0.5 | Peerless 公司法律名称 |
| 2 | QST 税号 | `/T\.V\.Q\.\/Q\.S\.T\. No 1000873531/i` | 0.3 |魁北克省税号 |
| 3 | GST 税号 | `/T\.P\.S\.\/G\.S\.T\. No\. 104128616/i` | 0.2 | 联邦税号 |
| 4 | 联系人 | `/Sonya Catalano\/514-593-9300/i` | 0.2 | Peerless 联系人+电话 |
| 5 | PO 号模式 | `/\b4500\d{6}\b/` | 0.1 | Peerless PO 号格式 |

```typescript
export function detectCustomer(text: string): DetectionResult {
  const scores = new Map<string, { score: number; reasons: string[] }>();
  for (const rule of RULES) {
    if (rule.pattern.test(text)) {
      // 加权累加
    }
  }
  // 返回得分最高的 customerId + confidence（上限 1.0）
  return {
    customerId: bestId,
    confidence: Math.min(1, best.score),
    reasons: best.reasons,
  };
}
```

### 6.3 客户解析器注册表（registry.ts）

```typescript
export type CustomerParser = (text: string) => ParsedOrder;

export const parsersByCustomer: Record<string, CustomerParser> = {
  peerless: parsePeerless,
};
```

**当前仅注册 1 个解析器**（peerless）。新增客户时：
1. 编写 `parse<CustomerName>(text: string): ParsedOrder` 函数
2. 在 detectCustomer.ts 添加该客户的识别规则
3. 在 registry.ts 注册 `{ <customerId>: parse<CustomerName> }`

### 6.4 parseOrderPdf 顶层入口

```typescript
export async function parseOrderPdf(buffer: Buffer): Promise<ParseResult> {
  try {
    const { text, pages } = await extractPdfText(buffer);
    const detection = detectCustomer(text);
    if (!detection.customerId) {
      return { detection, rawText: text, pages };  // 识别失败，仅返回检测信息
    }
    const parser = parsersByCustomer[detection.customerId];
    if (!parser) {
      return {
        detection, rawText: text, pages,
        error: `No parser registered for ${detection.customerId}`,
      };
    }
    const order = parser(text);
    return { detection, order, rawText: text, pages };
  } catch (e: any) {
    return {
      detection: { customerId: null, confidence: 0, reasons: [] },
      rawText: '', pages: 0,
      error: String(e?.message ?? e),
    };
  }
}
```

错误捕获并转为 error 字段，不抛出，确保单文件失败不阻断整体流程。

---

## §7 订单持久化（persistOrders.ts）

### 7.1 持久化流程

```
persistOrders(prisma, parsedOrders, opts)
  └─► 遍历 parsedOrders
        └─► 校验 poNumber 必填
        └─► 查询 existing Order by poNumber
        └─► 若 skipExisting && existing → 跳过
        └─► mapHeader(parsed, ts) → incoming
        ├─► applyOverwriteProtection(incoming.update, previousSources, mode)
        │     └─► 过滤 manual/imported-then-edited 字段
        └─► 遍历 lines → mapLine(orderId, line, i)
        └─► 查询 existingLines fieldSources
        └─► prisma.$transaction(async (tx) => {
              await tx.order.upsert({ where: { poNumber }, update, create });
              await tx.orderLine.deleteMany({ where: { orderId } });
              await tx.orderLine.createMany({ data: lineRows });
            })
        └─► 记录 PersistResult
```

### 7.2 字段级覆盖保护（fieldSources）

```typescript
export type FieldSourceTag = 'pdf' | 'manual' | 'imported-then-edited';

// 持久化时检查每个字段的来源标签：
// - 'pdf'：可被新 PDF 覆盖
// - 'manual'：用户手填，不可被 PDF 覆盖
// - 'imported-then-edited'：导入后用户编辑过，不可被 PDF 覆盖

// 模式：
// - 'overwrite-pdf-fields-only'（默认）：保护 manual/imported-then-edited 字段
// - 'force-overwrite'：强制覆盖所有字段（管理后台使用）
```

### 7.3 幂等性

- 键：`poNumber`（@@unique）
- 重新导入同一 PO：upsert 更新该行，不创建副本
- per-field overwrite protection 防止用户手填字段被覆盖

### 7.4 原子性

- 每个订单独立事务（`prisma.$transaction`）
- 单订单失败不影响其他订单
- 调用方决定如何处理部分成功

---

## §8 报价导入向导（QuotationImportWizard）

### 8.1 三步流程

```
Step 1: 选择文件
  ├─ 点击下载标准模板（TEMPLATE_HEADERS + TEMPLATE_EXAMPLE）
  ├─ 拖拽 Excel/CSV 到上传区
  └─ 前端 xlsx 解析为 HistoricalQuotationImportRow[]

Step 2: 校验预览
  ├─ POST /api/v1/quotations/import mode=preview
  │     └─► 后端只校验不写库，返回错误明细（行/字段级）
  ├─ 显示错误明细表格（行号 / 字段 / 错误信息）
  └─ 用户修正后重新校验

Step 3: 确认导入
  ├─ POST /api/v1/quotations/import mode=commit
  │     └─► 合法行落库（幂等：报号已存在自动跳过）
  └─ 显示导入报告（成功数 / 跳过数 / 失败数）
```

### 8.2 表头别名映射

```typescript
const HEADER_ALIASES: Record<keyof HistoricalQuotationImportRow, string[]> = {
  quotationNumber: ['报价号', '报价编号', 'quotationnumber', 'quotation no', 'quotation number', 'qt no'],
  customerName:    ['客户', '客户名称', 'customername', 'customer', 'client'],
  amount:          ['金额', '总金额', 'amount', 'total amount', 'total'],
  currency:        ['币种', 'currency'],
  issueDate:       ['报价日期', '日期', 'issuedate', 'issue date', 'date'],
  validUntil:      ['有效期', '有效期至', 'validuntil', 'valid until', 'validity'],
  status:          ['状态', 'status'],
  salesperson:     ['业务员', 'salesperson', 'sales'],
  notes:           ['备注', 'notes', 'remark', 'remarks'],
};
```

支持中英文表头别名，归一化为小写去空格后匹配。

### 8.3 标准模板

```typescript
const TEMPLATE_HEADERS = ['报价号', '客户', '金额', '币种', '报价日期', '有效期', '状态', '业务员', '备注'];
const TEMPLATE_EXAMPLE = ['Q-2025-001', 'Client A', 12500.5, 'USD', '2025-01-15', '2025-02-15', '已接受', 'Sales A', '历史归档导入'];
```

- 9 列字段
- 含示例行
- 用户下载后填入数据即可上传

### 8.4 状态中文映射

```typescript
const STATUS_ZH_TO_ENUM: Record<string, string> = {
  草稿: 'Draft',
  已发送: 'Sent',
  已接受: 'Accepted',
  已拒绝: 'Rejected',
  已过期: 'Expired',
};
```

允许用户在 Excel 中填中文状态，自动转为系统枚举。

### 8.5 Excel 日期归一化

```typescript
function normalizeDateCell(value: unknown): string {
  if (value instanceof Date && !Number.isNaN(value.getTime())) {
    // Date 对象 → YYYY-MM-DD
    return `${y}-${m}-${d}`;
  }
  if (typeof value === 'number' && Number.isFinite(value)) {
    // Excel 序列号（1899-12-30 起算）
    const epoch = new Date(Date.UTC(1899, 11, 30));
    const date = new Date(epoch.getTime() + value * 86400000);
    return `${y}-${m}-${d}`;
  }
  return String(value).trim().replace(/\//g, '-');  // 字符串
}
```

兼容三种输入：
- JavaScript Date 对象（xlsx 库默认解析日期单元格）
- Excel 序列号（某些 CSV 场景）
- 字符串（如 '2025-01-15' 或 '2025/01/15'）

### 8.6 行数限制

```typescript
const MAX_ROWS = 2000;
```

单次最多 2000 行，超过则提示用户分批导入。

---

## §9 后端导入路由（route.ts）

### 9.1 Express Router 配置

```typescript
const upload = multer({
  storage: multer.memoryStorage(),  // 内存存储，不写盘
  limits: {
    fileSize: opts.maxFileBytes ?? 10 * 1024 * 1024,  // 单文件 10MB
    files: opts.maxFiles ?? 20,                         // 单次最多 20 文件
  },
});

router.use((req, res, next) => {
  if (!opts.requireAuth) return next();  // 开放模式
  const k = req.headers['x-bambook-api-key'] as string | undefined;
  if (!k) return res.status(401).json({ error: 'UNAUTHORIZED', message: 'X-Bambook-API-Key header required' });
  if (!opts.apiKeys.has(k)) return res.status(403).json({ error: 'FORBIDDEN', message: 'Invalid API key' });
  next();
});
```

### 9.2 POST /order 端点

```typescript
router.post('/order', upload.array('files'), async (req, res) => {
  const files = req.files as Express.Multer.File[] | undefined ?? [];
  if (files.length === 0) {
    return res.status(400).json({ error: 'NO_FILES', message: 'Attach 1+ PDFs under multipart field name "files"' });
  }
  const results = await Promise.all(
    files.map(async (f) => {
      try {
        const r = await parseOrderPdf(f.buffer);
        return { filename: f.originalname, pages: r.pages, detection: r.detection, order: r.order ?? null, error: r.error ?? null };
      } catch (e: any) {
        return { filename: f.originalname, pages: 0, detection: { customerId: null, confidence: 0, reasons: [] }, order: null, error: String(e?.message ?? e) };
      }
    }),
  );
  return res.json({ count: results.length, results });
});
```

### 9.3 鉴权策略

| 模式 | requireAuth | 行为 |
| --- | --- | --- |
| 开放模式 | false | 任何请求都通过（本地开发友好） |
| 鉴权模式 | true | 必须带 X-Bambook-API-Key header，且 key 在 apiKeys Set 中 |

由服务端环境变量 `BAMBOOK_REQUIRE_AUTH` 控制，启动时打印日志：
```
Import API: POST /api/v1/import/order (auth: required | open)
```

---

## §10 前端服务封装（importService.ts）

### 10.1 五个 API 封装

| 函数 | 端点 | 用途 |
| --- | --- | --- |
| `uploadPdfsForParsing(files, opts)` | POST /v1/import/order | 上传 PDF 解析（不入库） |
| `saveParsedOrders(orders, opts)` | POST /v1/orders/import | 持久化已解析订单（幂等） |
| `createManualOrder(order, opts)` | POST /v1/orders | 手动创建订单（字段标记 manual） |
| `updateOrderFields(id, patch, opts)` | PUT /v1/orders/:id | 更新订单字段（字段标记 manual/imported-then-edited） |
| `deleteOrder(id, opts)` | DELETE /v1/orders/:id | 删除订单 |

### 10.2 FormData 上传注意

```typescript
// FormData 上传不能设 Content-Type（浏览器自动带 multipart boundary）
const { 'Content-Type': _ct, ...headers } = apiService.getAuthHeaders();
if (opts.apiKey) headers['X-Bambook-API-Key'] = opts.apiKey;

const res = await fetch(apiService.buildApiUrl('/v1/import/order'), {
  method: 'POST',
  body: fd,
  headers,  // 不含 Content-Type
  signal: opts.signal,
});
```

### 10.3 错误处理

所有封装统一错误处理：

```typescript
if (!res.ok) {
  let detail = '';
  try {
    detail = (await res.json())?.message ?? '';
  } catch { /* ignore */ }
  throw new Error(`Import failed (HTTP ${res.status})${detail ? ': ' + detail : ''}`);
}
```

---

## §11 前端组件结构

### 11.1 ImportWizard 组件树

```
ImportWizard (容器)
  ├─ AnimatePresence
  │   └─ motion.div (overlay)
  │       └─ motion.div (dialog)
  │           ├─ Header (Upload icon + 标题 + 关闭按钮)
  │           ├─ Step Indicator (StepDot × 3 + Connector × 2)
  │           │   ├─ StepDot n=1 label="选择文件" icon=<Upload />
  │           │   ├─ Connector
  │           │   ├─ StepDot n=2 label="预览/修正" icon=<ScanLine />
  │           │   ├─ Connector
  │           │   └─ StepDot n=3 label="确认" icon=<ShieldCheck />
  │           ├─ Body (AnimatePresence)
  │           │   ├─ step === 1 → <StepUpload />
  │           │   ├─ step === 2 → <StepPreview />
  │           │   └─ step === 3 → <StepConfirm />
  │           └─ Footer
  │               ├─ 上一步按钮
  │               ├─ 关闭按钮
  │               └─ 下一步按钮（label 随 step 变化）
```

### 11.2 QuotationImportWizard 组件树

```
QuotationImportWizard (容器)
  └─ motion.div (dialog)
      ├─ Header (FileSpreadsheet icon + 标题)
      ├─ Step Indicator (3 steps)
      ├─ Body
      │   ├─ step === 1 → 上传区 + 模板下载按钮 + 文件列表
      │   ├─ step === 2 → 校验结果表格（行/字段/错误）
      │   └─ step === 3 → 导入报告
      └─ Footer (上一步 / 关闭 / 下一步)
```

### 11.3 样式约定

- 弹窗：`bg-[var(--bg-card)]` + `border-[var(--border-c-default)]` + `rounded-floating`
- 步骤指示器背景：`bg-[var(--recessed-bg)]/50`
- 拖拽区：rgba() 任意值绕开 flat-experimental 护栏（保持虚线描边可见）
- 按钮：`bg-[var(--os-vnext-brand-blue)]` 主色 + `text-white`
- 状态徽章：statusSemanticClass('danger'/'success', isDarkMode)

---

## §12 测试覆盖

| 测试维度 | 覆盖点 |
| --- | --- |
| 跳转卡 | po-import / mes-console 跳转卡正确 targetView |
| 向导开闭 | isOpen 切换 + 状态重置 |
| ESC 关闭 | 键盘事件监听 + 触发 onClose |
| 步骤流转 | Step 1→2→3 流转 + canNext 校验 |
| 文件上传 | 拖拽 + 点击选择 + PDF 过滤 + 多选 |
| 文件状态机 | pending → parsing → done/error |
| 解析结果对齐 | 按文件名顺序对齐 results 与 files |
| 预览编辑 | 订单头部/行字段编辑 + results 更新 |
| 确认入库 | onConfirm 仅传成功解析的订单 |
| 客户识别 | 5 条规则加权打分 + 阈值 |
| 解析器注册 | parsersByCustomer 映射 |
| 持久化幂等 | poNumber upsert + 重复导入不创建副本 |
| 字段保护 | manual/imported-then-edited 字段不被 PDF 覆盖 |
| 部分成功 | 单订单失败不影响其他订单 |
| 鉴权 | requireAuth=true 时无 API key 返回 401 / 错误 key 返回 403 |
| 文件大小限制 | 单文件 >10MB 拒绝 |
| 文件数量限制 | >20 文件拒绝 |
| 报价导入模板 | TEMPLATE_HEADERS + TEMPLATE_EXAMPLE 下载 |
| 表头别名 | 中英文表头归一化匹配 |
| 状态映射 | 中文状态 → 英文枚举 |
| 日期归一化 | Date 对象 / Excel 序列号 / 字符串三种输入 |
| 行数限制 | >2000 行提示分批 |
| preview/commit 模式 | mode=preview 不写库 / mode=commit 落库 |

---

## §13 已知限制与未来扩展

### 13.1 当前限制

- ❌ **客户解析器仅 1 个**（peerless），其他客户 PDF 无法结构化解析
- ❌ **客户识别规则硬编码**，无法通过界面维护
- ❌ **报价导入不支持产品行明细**（仅 header 级，无 line items）
- ❌ **导入历史无记录**（不持久化导入批次/操作人/时间）
- ❌ **无导入模板在线编辑**（用户需下载模板后离线填写）
- ❌ **PDF 解析依赖 pdf-parse**，对扫描件 OCR 支持有限
- ❌ **字段保护规则不可配置**（硬编码 pdf/manual/imported-then-edited 三态）

### 13.2 未来扩展

- 新增客户解析器（按业务扩展）
- 客户识别规则界面化维护
- 报价导入支持产品行明细（line items）
- 导入历史持久化（ImportBatch 模型 + 操作审计）
- 在线模板编辑器（字段增删/顺序调整）
- 扫描件 OCR 支持（Tesseract / 云 OCR 服务）
- 字段保护规则可配置（管理后台设置）
- 导入预检（dry-run 模式，仅校验不入库）
- 导入报告导出（Excel/PDF）

---

## §14 实现备注

### 14.1 阶段 IA-2 收编说明

PO 文件导入与 MES 均为**跳转卡**——业务工具箱仅保留入口，实际功能由各主模块承载：

| 跳转卡 | 落点模块 | 落点位置 |
| --- | --- | --- |
| PO 文件导入 | Orders | 订单页顶部「批量导入」按钮 |
| MES | MES | MES 模块主页 |

收编理由：
- PO 导入依赖 Orders 数据模型，属订单域
- MES 是独立可选模块，已有独立视图

### 14.2 字段保护机制设计

字段保护机制是订单导入的**核心防错设计**：

```typescript
// 场景：用户首次 PDF 导入 PO 123456 → fieldSources 全标 'pdf'
// 用户在订单详情页手填 salesCurrency = 'EUR' → fieldSources.salesCurrency = 'manual'
// 用户再次 PDF 导入 PO 123456 → applyOverwriteProtection 过滤掉 salesCurrency
// 结果：用户的 'EUR' 保留，不被 PDF 的 'USD' 覆盖
```

避免：
- 用户手填字段被批量导入覆盖
- 数据丢失
- 重复劳动

### 14.3 客户识别阈值

detectCustomer 未设置硬阈值，只要任何规则命中即返回 customerId。confidence 字段供前端参考展示：

| confidence | 含义 |
| --- | --- |
| 0.1 - 0.3 | 弱命中（仅 PO 号模式） |
| 0.4 - 0.6 | 中等命中（税号或联系人） |
| 0.7+ | 强命中（法人名 + 多重命中） |

未来可引入阈值（如 <0.3 不自动识别，要求用户手动选择客户）。

### 14.4 文件上传安全

| 维度 | 限制 |
| --- | --- |
| 单文件大小 | 10MB |
| 单次文件数 | 20 |
| 存储方式 | 内存（multer memoryStorage），不写盘 |
| 鉴权 | 可选 API Key（环境变量控制） |
| 文件类型 | 前端 PDF 过滤 + 后端无校验（建议后端补充 Content-Type 校验） |

### 14.5 颜色与样式豁免

StepUpload 拖拽区使用 rgba() 任意值：

```typescript
const dropzoneBase = 'border-[rgba(100,116,139,0.50)] dark:border-[rgba(255,255,255,0.22)] bg-[rgba(255,255,255,0.60)] dark:bg-[rgba(255,255,255,0.04)] text-[var(--text-tertiary)]';
```

原因：flat-experimental 护栏会强制 `border:0 !important`，导致虚线描边消失。拖拽区的虚线描边是核心视觉可供性，必须保持可见。其余组件严格使用 BDS token。

---

## §15 交叉链接

1. [BusinessTools 业务工具箱 — 父模块] — 本文档为父模块下的子文档
2. [Orders 订单管理 — 模块概述](../../03-订单与生产/Orders-订单管理/模块概述.md) — PO 导入落点模块
3. [Orders 订单管理 — 订单录入表单](../../03-订单与生产/Orders-订单管理/订单录入表单.md) — 手动创建订单路径
4. [Orders 订单管理 — 订单状态机](../../03-订单与生产/Orders-订单管理/订单状态机.md) — 导入后订单初始状态
5. [Quotations 报价 — 模块概述](../../02-客户与开拓/Quotations-报价/模块概述.md) — 报价导入落点模块
6. [Quotations 报价 — 报价编辑器](../../02-客户与开拓/Quotations-报价/报价编辑器.md) — 报价字段定义
7. [DocumentCenter 单据中心 — 制单引擎](../../04-出货与单据/DocumentCenter-单据中心/制单引擎.md) — 单据生成（导入订单后可制单）
8. [Settings 管理后台 AdminPanel](../../08-设置与后台/Settings-设置/管理后台AdminPanel.md) — force-overwrite 模式管理入口

---

## §16 变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v1.0 | 2026-08-15 | 初始版本：2 跳转卡 + 订单导入向导（PDF）+ 报价导入向导（Excel）+ 客户识别 + 字段保护 + 持久化 |
