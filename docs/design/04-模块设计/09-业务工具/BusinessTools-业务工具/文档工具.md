# 文档工具 (Document Generators)

> 模块代号：`BusinessTools / Document Tools`
> 父模块：[BusinessTools 业务工具箱](../)
> 关联代码：`components/BusinessTools.tsx`、`components/tools/FabricSampleInvoiceGenerator.tsx`、`components/tools/ShippingNoticeGenerator.tsx`、`components/tools/ContractGenerator.tsx`、`components/tools/PackingListGenerator.tsx`、`components/tools/printDocument.ts`、`components/tools/sampleInvoiceTemplate.ts`、`components/tools/exportDocs/exporterProfile.ts`、`services/apiService.ts`
> 文档版本：v1.0 · 最后更新：2026-08-15

---

## §1 实现状态

| 维度 | 状态 | 说明 |
| --- | --- | --- |
| 样品发票生成器 | ✅ 已落地 | `FabricSampleInvoiceGenerator.tsx` 1486 行：A5 横向 HTML 预览 + Logo/印章替换 + 拖拽缩放 + 直接保存 PDF（Electron IPC）+ 云端档案（BusinessProfile）+ 历史记录持久化。 |
| 发货通知生成器 | ✅ 已落地 | `ShippingNoticeGenerator.tsx`：从 props.orders 多选 PO → 填充目的港/装船/付款/货代 → 调 `apiService.generateShippingNotice` 后端生成 Excel → 触发下载。 |
| 合同生成器 | ✅ 已落地 | `ContractGenerator.tsx`：销售/采购双语合同 HTML → printHtmlDocument → 浏览器打印 PDF；含 7 类标准条款（付款/交货/品质/包装/检验/不可抗力/仲裁）+ 金额大写 numberToWords。 |
| 装箱单生成器 | ✅ 已落地 | `PackingListGenerator.tsx`：出口装箱明细单 HTML → printHtmlDocument → PDF；11 列表（PO/Code/Desc/Qty-Ctn/Ctns/Qty/Unit/N.W./G.W./Carton Dim）+ 合计行。 |
| 共享打印工具 | ✅ 已落地 | `printDocument.ts`：`window.open` 新窗口写入 HTML + BASE_PRINT_STYLES 统一样式 + 自动 `window.print()`；`formatDate`/`formatDocNumber`/`escapeHtml` 共享 helper。 |
| 出口方档案 | ✅ 已落地 | `exporterProfile.ts`：默认 EXPORTER_PROFILE + `getExporterProfile()` 从 `localStorage['panda_system_config'].exporterProfile` 合并，供合同/装箱单引用公司抬头。 |
| 产品档案联动 | ✅ 已落地 | 样品发票 Client Code/Fabric 输入触发 `queueProductLookup` 查询 `listProductAssets(mainCategory='Fabric')`，自动填充客户编码/品名/最新价格。 |
| 客户搜索 | ✅ 已落地 | `CustomerSearchInput` 组件 + relations props 提供客户搜索下拉，自动填充 Bill To Name/Address。 |
| 跳转收编卡 | ✅ 已落地 | 出运制单引擎/单据模板管理/PO 导入/MES 跳转卡：`targetView` + `targetTab` 走 `onNavigate` 深链对应模块对应 tab，不再内嵌。 |
| BDS v2.1 主题 | ✅ 已落地 | 全部使用 `var(--recessed-bg)` / `var(--text-primary)` / `var(--border-c-default)` 等 token，无硬编码 hex（除 printDocument.ts 打印输出 HTML 外，属于豁免范围）。 |
| Electron PDF IPC | ✅ 已落地 | `window.bambookInvoice.savePdf(html, filename)` 由主进程使用浏览器打印生成 PDF 保存到本地。回退到 Blob 下载 HTML。 |
| 服务端 Excel 生成 | ⚠️ 依赖后端 | 发货通知生成器依赖 `apiService.generateShippingNotice` 后端服务生成 Excel，前端仅负责选 PO 与表单填写。 |

---

## §2 业务定位与目标

### 2.1 业务定位

文档工具是 Bambook「**业务工具箱**」内的子分组——以**订单/客户/产品**为输入，按内置模板生成**外贸单据**，无需用户从零制单：

```
订单 / 客户 / 产品数据 ──► 文档生成器 ──► HTML / Excel / PDF
```

### 2.2 与出运制单引擎的分工

| 维度 | 文档工具（本页） | 出运制单引擎（Customs tab） |
| --- | --- | --- |
| 触发场景 | 业务员临时制单、未生成运单时 | 已有运单 Shipment 后批量成套制单 |
| 输入 | 手填或选订单/客户 | 已存在的 Shipment |
| 输出类型 | 单一文档（一份发票/一份合同/一份装箱单） | 成套单据（CI+PL+CO+BL+可选 FORMA/INS/BC） |
| 数据源 | props.orders/relations + 表单 | GET /v1/shipping/:id/document-set |
| 状态机 | 不入库（样品发票除外，存 BusinessProfile 历史） | DocumentRecord 入库 + 状态机 |

### 2.3 核心目标

1. **业务员友好**：从订单/客户预填，减少手输
2. **模板内置**：合同 7 条款、装箱单 11 列、发票字段均硬编码默认值，用户只需覆盖必要字段
3. **多输出格式**：HTML 预览 + PDF 打印 + Excel（发货通知由后端生成）
4. **不破坏数据真源**：文档工具只读业务数据，不写入业务表（仅 BusinessProfile 存模板/历史）
5. **可复用打印样式**：`BASE_PRINT_STYLES` 统一控制字体/边距/表格样式，确保跨文档视觉一致

---

## §3 核心概念与术语

| 术语 | 定义 |
| --- | --- |
| `FabricSampleInvoiceGenerator` | 面料样品发票生成器组件 |
| `ShippingNoticeGenerator` | 发货通知生成器组件 |
| `ContractGenerator` | 合同生成器组件 |
| `PackingListGenerator` | 装箱单生成器组件 |
| `printHtmlDocument` | 共享打印工具：新窗口写入 HTML + 调 `window.print()` |
| `BASE_PRINT_STYLES` | 打印 HTML 共用的基础 CSS（字体/边距/表格/页脚签字栏） |
| `ExporterProfile` | 出口方（我司）档案接口，含公司名/地址/银行信息 |
| `getExporterProfile()` | 从 localStorage 读取并合并默认 EXPORTER_PROFILE |
| `SampleInvoiceTemplateConfig` | 样品发票模板配置（Logo/印章/缩放/位移/银行/付款条款） |
| `BusinessProfile` | 云端档案接口，供样品发票模板与历史持久化 |
| `sample-invoice-history` | BusinessProfile kind，存 SavedFabricSampleInvoice 数组 |
| `bambookInvoice.savePdf` | Electron IPC：传 HTML 由主进程渲染并保存为 PDF |
| `targetView` + `targetTab` | 跳转卡机制：点击卡片经 `onNavigate` 深链到对应模块 tab |
| `numberToWords` | 数字转英文大写（合同金额 SAY US DOLLARS ... ONLY） |

---

## §4 文档工具体系总览

### 4.1 文档工具清单

| # | 工具 | 文件 | 输入 | 输出 | 触发方式 |
| --- | --- | --- | --- | --- | --- |
| 1 | 样品发票生成器 | FabricSampleInvoiceGenerator.tsx | 客户/PO/面料行 | A5 横向 HTML + PDF | 内嵌组件 |
| 2 | 发货通知生成器 | ShippingNoticeGenerator.tsx | 多选 PO + 目的港/付款/货代 | Excel（后端生成） | 内嵌组件 |
| 3 | 合同生成器 | ContractGenerator.tsx | 卖买双方 + 商品行 + 7 条款 | A4 HTML + PDF | 内嵌组件 |
| 4 | 装箱单生成器 | PackingListGenerator.tsx | 订单/客户 + 装箱行 | A4 HTML + PDF | 内嵌组件 |
| — | 出运制单引擎 | ShipmentDocumentGenerator.tsx | Shipment ID | CI/PL/CO/BL 成套 | 跳转 Customs/docGenerator |
| — | 单据模板管理 | DocumentTemplateManager.tsx | — | — | 跳转 Customs/docTemplates |
| — | PO 文件导入 | ImportWizard.tsx | Excel/CSV | 订单入库 | 跳转 Orders |
| — | MES 工位排产 | MES.tsx | — | — | 跳转 MES |

### 4.2 工具卡片渲染机制

```typescript
// BusinessTools.tsx 第 66-141 行
const tools: Tool[] = [
  { id: 'po-import', name: 'PO 文件导入', targetView: View.Orders, ... },
  { id: 'mes-console', name: '生产执行 MES', targetView: View.MES, ... },
  { id: 'sample-invoice', name: '样品发票生成器', component: <FabricSampleInvoiceGenerator ... /> },
  { id: 'shipping-notice', name: '发货通知生成器', component: <ShippingNoticeGenerator ... /> },
  { id: 'contract-generator', name: '合同生成器', component: <ContractGenerator ... /> },
  { id: 'packing-list', name: '装箱单生成器', component: <PackingListGenerator ... /> },
  { id: 'shipment-documents', targetView: View.Customs, targetTab: 'docGenerator' },
  { id: 'document-templates', targetView: View.Customs, targetTab: 'docTemplates' },
  { id: 'quote-calculator', targetView: View.Pricing, targetTab: 'calculator' },
  { id: 'exchange-rate', targetView: View.Pricing, targetTab: 'taxRates' },
];

const handleToolClick = (tool: Tool) => {
  if (tool.targetView) {
    onNavigate?.(tool.targetView, tool.targetTab);
    return;
  }
  setSelectedTool(tool.id);  // 内嵌组件打开
};
```

### 4.3 阶段 IA-2 收编说明

业务工具箱已执行阶段 IA-2「**收编**」——把原本散落在此页的工具，按业务域归类到对应主模块的 tab，本页保留**跳转卡**：

- 报价计算器 / 退税汇率 → **Pricing 模块**（calculator / taxRates tab）
- 出运制单 / 单据模板管理 → **Customs 模块**（docGenerator / docTemplates tab）
- PO 导入 → **Orders 模块**（导入向导）
- MES → **MES 模块**

仅 4 个**真正无主**的制单小组件（样品发票/发货通知/合同/装箱单）保留在本页内嵌。

---

## §5 样品发票生成器（FabricSampleInvoiceGenerator）

### 5.1 数据流

```
组件挂载 useEffect
  ├─► loadDataCenterProfiles()
  │     ├─► apiService.listBusinessProfiles('fabric-sample-invoice', endpoint)
  │     ├─► apiService.listBusinessProfiles('fabric-sample-invoice-history', endpoint)
  │     ├─► 首次无档案 → saveBusinessProfile(默认模板) 创建
  │     ├─► 读取 legacy localStorage 迁移
  │     └─► 设置 profiles / selectedProfileId / savedInvoices
  │
  └─► 用户编辑表单
        ├─► 更新 invoiceNumber / items / template 字段
        ├─► template 字段变更 → scheduleProfileSave(500ms 防抖)
        │     └─► apiService.saveBusinessProfile 写回云端
        ├─► Client Code/Fabric 输入 → queueProductLookup(220ms 防抖)
        │     └─► apiService.listProductAssets(mainCategory='Fabric', search)
        │     └─► applyProductSuggestion 自动填客户码/品名/最新价
        └─► 点击「生成预览」→ generateFabricSampleInvoiceHtml → iframe srcDoc
              ├─► 点击「保存」→ 加入 savedInvoices + 持久化到 history profile
              ├─► 点击「HTML」→ Blob 下载 HTML 文件
              └─► 点击「PDF」→ window.bambookInvoice.savePdf 或回退下载 HTML
```

### 5.2 模板字段（SampleInvoiceTemplateConfig）

| 字段 | 类型 | 默认值 | 用途 |
| --- | --- | --- | --- |
| `logoDataUrl` / `stampDataUrl` | string (data URL) | '' | Logo/印章图片 |
| `logoScale` / `stampScale` | number | 1 / 2 | 缩放倍数 |
| `logoOffsetX/Y` / `stampOffsetX/Y` | number (mm) | 0 | 位移偏移 |
| `companyName` | string | getExporterProfile().nameEn | 公司英文名 |
| `companyAddress` | string | getExporterProfile().addressEn | 公司英文地址 |
| `paymentTerms` | string | 'AS PER AGREEMENT' | 付款条款 |
| `bankName` / `swiftCode` / `bankAddress` | string | 来自 ExporterProfile | 银行信息 |
| `beneficiary` | string | 来自 ExporterProfile | 受益人 |
| `usdAccountNumber` | string | 来自 ExporterProfile | USD 账号 |

### 5.3 Logo/印章拖拽与缩放

```typescript
// iframe 内 .logo/.stamp 元素：
// - mousedown 进入拖拽模式，记录 startX/Y 与初始 offset
// - mousemove 更新 transform: translate(Xmm, Ymm) scale(N)
// - mouseup commitAssetAdjustment 写回 templateRef + scheduleProfileSave

// resizeHandle 右下角：
// - 拖拽计算 dragDelta，按 baseline 比例缩放
// - clamp 限制 logoScale ∈ [0.5, 2.4] / stampScale ∈ [0.6, 3.5]
```

### 5.4 BusinessProfile 持久化结构

```typescript
// Profile kind: 'fabric-sample-invoice'
type SampleInvoiceBusinessProfile = BusinessProfile<
  SampleInvoiceProfilePayload,  // 不含 logoDataUrl/stampDataUrl
  SampleInvoiceProfileAssets    // { logoDataUrl, stampDataUrl }
>;

// Profile kind: 'fabric-sample-invoice-history'
type HistoryProfile = BusinessProfile<{ invoices: SavedFabricSampleInvoice[] }>;

// 本机只记 last profile id
localStorage['bambook:last-business-profile:fabric-sample-invoice'] = profileId;
```

### 5.5 发票号生成规则

```typescript
const generateInvoiceNumber = (date: Date): string => {
  const yy = String(date.getFullYear()).slice(-2);
  const mm = String(date.getMonth() + 1).padStart(2, '0');
  const dd = String(date.getDate()).padStart(2, '0');
  const seq = String(Math.floor(Math.random() * 90) + 10);  // 10-99
  return `PDAS${yy}${mm}${dd}${seq}`;  // 如 PDAS26081542
};
```

---

## §6 发货通知生成器（ShippingNoticeGenerator）

### 6.1 数据流

```
props.orders 过滤（未删除 + 有 poNumber）
  └─► 关键字过滤（poNumber/customer/clientCode）
        └─► availablePOs 列表（去除已选）
              └─► 用户点击 + 加入 selectedPOs

填表 options: { destinationPort, shipmentDate, paymentTerms, shippingMethod, forwarder, remarks }

点击「生成发货通知」
  └─► apiService.generateShippingNotice({
        poNumbers: [...selectedPOs],
        options: { destinationPort, shipmentDate, paymentTerms, forwarder, remarks }
      })
        └─► 后端返回 { success, downloadUrl, error }
              └─► window.open(downloadUrl) 触发 Excel 下载
```

### 6.2 选项清单

| 字段 | 选项 |
| --- | --- |
| 目的港（DESTINATION_PORTS） | SURABAYA / JAKARTA / HO CHI MINH / BANGKOK / PORT KLANG / MANILA / CHITTAGONG / CHENNAI / COLOMBO |
| 付款方式（PAYMENT_TERMS_OPTIONS） | TT 30/45/60DAYS / L/C AT SIGHT / L/C 30DAYS / D/P |
| 运输方式（SHIPPING_METHODS） | 海运 / 空运 / 海空联运 |

### 6.3 全选限制

```typescript
const handleSelectAll = () => {
  const newSelected = availablePOs.slice(0, 10);  // 单次最多 10 个
  // ...
};
```

避免一次性生成过多 PO 的发货通知，超过 10 个需分批处理。

---

## §7 合同生成器（ContractGenerator）

### 7.1 数据流

```
合同类型 sales/purchase
  └─► 卖方/买方从 relationOptions 选择（relations 过滤未删除）
        └─► 关联订单（选填）：搜索 poNumber/customer → 预填 currency/paymentTerms
              └─► 商品明细行（ContractLine[]）
                    └─► 7 类条款（paymentTerms/deliveryTerms/quality/packing/inspection/forceMajeure/arbitration）

点击「生成合同 PDF」
  └─► 校验：至少卖方或买方 + 至少一行商品
        └─► 构造 htmlBody（含合同号/双方信息/明细表/合计大写/条款表/签字栏）
              └─► printHtmlDocument({ title, htmlBody }) 触发浏览器打印
```

### 7.2 合同类型差异

| 字段 | 销售合同 (sales) | 采购合同 (purchase) |
| --- | --- | --- |
| titleEn | SALES CONTRACT | PURCHASE CONTRACT |
| titleCn | 销售合同 | 采购合同 |
| 卖方标签 | Seller 卖方 | Supplier 供货方 |
| 买方标签 | Buyer 买方 | Purchaser 采购方 |
| 默认卖方 | getExporterProfile().nameEn | 用户从 relations 选择 |
| 默认买方 | 用户从 relations 选择 | 用户从 relations 选择 |

### 7.3 7 类标准条款默认值

| 条款 | 默认值 |
| --- | --- |
| Payment 付款方式 | T/T 30% deposit, 70% before shipment |
| Delivery 交货条款 | FOB Shanghai |
| Quality 品质条款 | 按买卖双方确认的样品品质为准，允许 ±5% 公差。 |
| Packing 包装条款 | 卷装出口，每卷 100 YD，外用编织袋包装，适合长途海运。 |
| Inspection 检验条款 | 买方须在货到后 15 天内提出品质/数量异议，逾期视为接受。 |
| Force Majeure 不可抗力 | 因战争、自然灾害等不可抗力导致无法履约，双方免除责任。 |
| Arbitration 仲裁 | 凡因本合同引起的争议，提交中国国际经济贸易仲裁委员会（CIETAC）按其规则仲裁。 |

### 7.4 numberToWords 英文大写

合同金额自动转换为英文大写（如 `1234.56` → `ONE THOUSAND TWO HUNDRED THIRTY-FOUR AND FIFTY-SIX CENTS`），用于 `Total Amount (in words)` 字段，符合国际贸易合同惯例。

### 7.5 单位与币种

| 维度 | 选项 |
| --- | --- |
| UNITS | YD / M / KG / PC / SET |
| currency | USD / CNY / EUR |

---

## §8 装箱单生成器（PackingListGenerator）

### 8.1 数据流

```
订单搜索（poNumber 或 customer）
  └─► 选中订单自动填充首行 poNumber + description

客户下拉（relationOptions 过滤 Customer/Supplier 类型）
  └─► 选中作为 Consignee（收货方）

填表：destinationPort / invoiceNumber / shipper / shippingMark

装箱明细行（PackingLine[]）
  └─► 11 列：poNumber/fabricCode/description/qtyPerCarton/cartons/quantity/unit/netWeight/grossWeight/cartonDimensions

点击「生成装箱单 PDF」
  └─► 校验：至少一行非空
        └─► 构造 htmlBody（表头/双方/装运信息/明细表 + 合计行/签字栏）
              └─► printHtmlDocument 触发浏览器打印
```

### 8.2 装箱单 11 列字段

| # | 字段 | 占位 | 说明 |
| --- | --- | --- | --- |
| 1 | No. | 序号 | 自增 |
| 2 | PO No. | PO 号 | 订单号 |
| 3 | Fabric Code | 面料编码 | 自由文本 |
| 4 | Description | 品名描述 | 自由文本 |
| 5 | Qty/Ctn | 每箱数量 | 数值 |
| 6 | Ctns | 箱数 | 数值 |
| 7 | Quantity | 总数量 | 数值 |
| 8 | Unit | 单位 | YD/M/KG/PC/SET |
| 9 | N.W.(kg) | 净重 | 数值 |
| 10 | G.W.(kg) | 毛重 | 数值 |
| 11 | Carton Dim | 箱规 | L×W×H 字符串 |

### 8.3 合计行

```typescript
const totals = useMemo(() => ({
  totalCartons: lines.reduce((s, l) => s + parseNum(l.cartons), 0),
  totalQuantity: lines.reduce((s, l) => s + parseNum(l.quantity), 0),
  totalGrossWeight: lines.reduce((s, l) => s + parseNum(l.grossWeight), 0),
  totalNetWeight: lines.reduce((s, l) => s + parseNum(l.netWeight), 0),
}), [lines]);
```

合计行在 tfoot 显示，与 tbody 视觉上用 2px 顶边分隔。

---

## §9 共享打印工具（printDocument.ts）

### 9.1 BASE_PRINT_STYLES 设计原则

| 区域 | CSS 关键属性 | 设计意图 |
| --- | --- | --- |
| body | font-family 系统字体 / 12px / line-height 1.6 / padding 40px | A4 标准打印边距 |
| .doc-header | flex space-between / border-bottom 2px solid #1a202c | 标题左 + 元信息右 |
| .doc-title-block h1 | 22px / font-weight 600 / letter-spacing 0.5px | 标题清晰 |
| .doc-meta | text-align right / 11px / color #4a5568 | 单号/日期/地点靠右 |
| .doc-party-grid | grid 2 cols / gap 20px | 买卖双方并列 |
| table.doc-table | width 100% / border-collapse / font-size 11px | 表格紧凑 |
| thead th | bg #f7fafc / 大写字母 / 字间距 | 表头醒目 |
| tbody tr:nth-child(even) | bg #fafafa | 斑马纹 |
| tfoot td | border-top 2px solid / font-weight 600 / text-align right | 合计行 |
| .doc-footer | grid 2 cols / gap 40px / margin-top 40px | 签字栏左右 |
| .doc-signature .sig-line | border-top 1px solid / margin-top 40px | 签字下划线 |
| @media print | body padding 20px / .no-print 隐藏 | 打印优化 |

### 9.2 printHtmlDocument 工作机制

```typescript
export function printHtmlDocument({ title, htmlBody, extraStyles = '' }): void {
  const win = window.open('', '_blank', 'width=900,height=700');
  if (!win) {
    alert('无法打开打印窗口，请检查浏览器弹窗拦截设置。');
    return;
  }
  win.document.write(`<!DOCTYPE html>...
    <title>${title}</title>
    <style>${BASE_PRINT_STYLES}${extraStyles}</style>
    <body>${htmlBody}
    <script>window.onload = function() { setTimeout(function() { window.print(); }, 300); };</script>
  `);
  win.document.close();
}
```

- 在 Electron 环境下，浏览器打印对话框可选择「保存为 PDF」
- 300ms 延迟确保 DOM 渲染完成后再触发打印

### 9.3 共享 helper

| 函数 | 签名 | 用途 |
| --- | --- | --- |
| `formatDate(date)` | `(string\|Date) → 'YYYY-MM-DD'` | 日期格式化 |
| `formatDocNumber(n, decimals)` | `(number, number) → '1,234.56'` | 千分位数字 |
| `escapeHtml(text)` | `(string) → string` | 防 HTML 注入 |

---

## §10 出口方档案（exporterProfile.ts）

### 10.1 默认值 EXPORTER_PROFILE

```typescript
export const EXPORTER_PROFILE: ExporterProfile = {
  nameEn: 'JIANGSU PANDA CLOTHING CO.,LTD.',
  addressEn: 'ROOM A1028 WUYUE PLAZA,\nZHANGJIAGANG CITY, 215600 PR\nCHINA',
  beneficiary: 'JIANGSU PANDA CLOTHING CO.,LTD.',
  bankName: 'BANK OF CHINA ZHANGJIAGANG SUB-BRANCH',
  swiftCode: 'BKCHCNBJ95L',
  bankAddress: '111 MIDDLE RENMIN ROAD, ZHANGJIAGANG CITY, SUZHOU, JIANGSU PROV., P.R.CHINA.',
  usdAccountNumber: '467668133096',
};
```

### 10.2 运行时合并

```typescript
export function getExporterProfile(): ExporterProfile {
  try {
    const raw = localStorage.getItem('panda_system_config');
    if (!raw) return EXPORTER_PROFILE;
    const parsed = JSON.parse(raw);
    const stored = parsed?.exporterProfile;
    if (!stored || typeof stored !== 'object') return EXPORTER_PROFILE;
    return { ...EXPORTER_PROFILE, ...stored };  // 浅合并，新增字段有默认回退
  } catch {
    return EXPORTER_PROFILE;
  }
}
```

- 用户在设置页编辑公司抬头后，值存入 `panda_system_config.exporterProfile`
- 此函数读取并合并，确保新增字段有默认回退
- 任何异常（解析失败/无 localStorage）都安全降级到默认值

### 10.3 引用方

| 引用方 | 使用字段 |
| --- | --- |
| `sampleInvoiceTemplate.ts` | 全部 7 字段（默认值来源） |
| `ContractGenerator.tsx` | nameEn（seller 回退）、addressEn |
| `PackingListGenerator.tsx` | nameEn（shipper 默认）、addressEn |
| `exportDocumentTemplates.ts`（出运制单引擎） | 全部 7 字段 |

---

## §11 前端组件结构

### 11.1 BusinessTools 容器

```
BusinessTools.tsx (容器)
  ├─ PageHeader (title="业务工具箱", subtitle="Business Tools")
  ├─ AnimatePresence (mode="wait")
  │   ├─ tool-grid 视图（默认）
  │   │   └─ ScrollEdgeFades + 工具卡片网格 (grid-cols-1/2/3)
  │   │       └─ CompiledInteractiveCard (spotlight + liquid spotlight)
  │   └─ tool-content 视图（选中内嵌工具）
  │       ├─ Back Button
  │       └─ ScrollEdgeFades + 工具组件
```

### 11.2 工具组件统一模式

| 组件 | 头部 | 主体 | 操作区 |
| --- | --- | --- | --- |
| FabricSampleInvoiceGenerator | 编辑/历史页切换 + 历史按钮 | 左：表单（发票/客户/模板/明细）；右：iframe 预览 | 4 按钮（生成预览/保存/HTML/PDF） |
| ShippingNoticeGenerator | 标题 + 重置 | 左：PO 选择 + 已选；右：发货信息表单 | 1 按钮（生成发货通知） |
| ContractGenerator | 标题 + 重置 | 4 卡片：基本信息/买卖双方/商品明细/条款 | 1 按钮（生成合同 PDF） |
| PackingListGenerator | 标题 + 重置 | 2 卡片：订单与客户/装箱明细 | 1 按钮（生成装箱单 PDF） |

### 11.3 样式约定

所有工具组件使用统一 token：

```typescript
const panelClass = 'bg-[var(--recessed-bg)] border border-[var(--border-c-default)]';
const fieldClass = `w-full px-3 py-2 rounded-control text-sm transition-colors
  focus:outline-none focus:border-[var(--os-vnext-brand-blue)]
  bg-[var(--recessed-bg)] border border-[var(--border-c-default)]
  text-[var(--text-primary)] placeholder:text-[var(--text-tertiary)]`;
const labelClass = `block text-xs mb-1 text-[var(--text-tertiary)]`;
const sectionTitleClass = `text-xs font-light uppercase tracking-wider mb-3 text-[var(--text-tertiary)]`;
```

- 圆角统一 `rounded-control`（输入）/ `rounded-card`（卡片）/ `rounded-inset`（嵌套）/ `rounded-full`（按钮）
- 颜色全部走 CSS 变量，无硬编码 hex（打印输出 HTML 豁免）
- 按钮 hover 走 `var(--os-vnext-brand-blue)`/90 或 `var(--active-darken)`

---

## §12 测试覆盖

| 测试维度 | 覆盖点 |
| --- | --- |
| 工具卡片渲染 | 10 工具卡片全部出现 + 跳转卡正确 targetView/targetTab |
| 内嵌工具切换 | 点击内嵌工具卡 → selectedTool 切换 → AnimatePresence 过渡 |
| 返回按钮 | 点击返回 → selectedTool = null → 回到工具网格 |
| 样品发票初始化 | 首次进入 → 自动创建默认 profile + 读取历史 |
| 样品发票保存 | 点击保存 → savedInvoices 更新 + history profile 持久化 |
| 样品发票 PDF | Electron IPC `bambookInvoice.savePdf` 调用 |
| Logo/印章拖拽 | mousedown/mousemove/mouseup 事件正确触发 + commitAssetAdjustment |
| 产品联想 | Client Code 输入 → queueProductLookup 防抖 → applyProductSuggestion |
| 发货通知生成 | selectedPOs 非空 + destinationPort 已选 → apiService 调用 |
| 全选限制 | handleSelectAll 最多 10 个 |
| 合同生成 | 至少卖买方 + 至少一行商品 → printHtmlDocument 调用 |
| 装箱单生成 | 至少一行非空 → printHtmlDocument 调用 |
| ExporterProfile 合并 | localStorage 有值时合并 / 无值或异常时回退默认 |
| escapeHtml | 特殊字符（& < > " '）转义 |

---

## §13 已知限制与未来扩展

### 13.1 当前限制

- ❌ 文档工具**不入库**：除样品发票历史外，合同/装箱单/发货通知均不持久化到 BusinessProfile 或 DocumentRecord
- ❌ **无版本管理**：同一份合同多次生成不会保留历史版本
- ❌ **模板不可自定义**：合同 7 条款、装箱单 11 列均为硬编码默认值
- ❌ **发货通知依赖后端 Excel 服务**：若后端不可用，前端无法生成
- ❌ **样品发票 Logo/印章编辑需进入预览页**：表单页不可直接调整缩放位移
- ❌ **无多语言切换**：合同/装箱单默认中英双语，无法纯英文或纯中文输出
- ❌ **打印样式硬编码 hex**：BASE_PRINT_STYLES 使用 #1a202c 等颜色（豁免：打印 HTML 在系统窗口渲染，CSS 变量在打印窗口上下文不可靠）

### 13.2 未来扩展

- 文档生成后入库 DocumentRecord（与出运制单引擎统一状态机）
- 模板可配置（合同条款/装箱单列模板化）
- 多语言切换（纯英文/纯中文/中英双语）
- 样品发票编辑器与预览合并（所见即所得）
- 发货通知 Excel 模板可下载预填
- 合同/装箱单批量生成（一次选多订单生成多份）

---

## §14 实现备注

### 14.1 阶段 IA-2 收编边界

业务工具箱原本包含**所有**业务工具，阶段 IA-2 后仅保留**真正无主**的制单小组件：

| 工具 | 收编前 | 收编后 |
| --- | --- | --- |
| 报价计算器 | 内嵌 | 跳转 Pricing/calculator |
| 退税汇率 | 内嵌 | 跳转 Pricing/taxRates |
| 出运制单 | 内嵌 | 跳转 Customs/docGenerator |
| 单据模板管理 | 内嵌 | 跳转 Customs/docTemplates |
| PO 导入 | 内嵌 | 跳转 Orders |
| MES | 内嵌 | 跳转 MES |
| **样品发票** | 内嵌 | **保留内嵌**（无主） |
| **发货通知** | 内嵌 | **保留内嵌**（无主） |
| **合同** | 内嵌 | **保留内嵌**（无主） |
| **装箱单** | 内嵌 | **保留内嵌**（无主） |

### 14.2 样品发票 IPC 回退

```typescript
if (window.bambookInvoice?.savePdf) {
  await window.bambookInvoice.savePdf(html, filename);
} else {
  downloadFile(html, `${filename}.html`, 'text/html;charset=utf-8');
}
```

- Electron 主进程注册 `bambookInvoice.savePdf` IPC handler
- Web 环境无此 IPC，回退到下载 HTML 文件
- 用户可在浏览器中打开 HTML 并使用浏览器打印功能

### 14.3 乐观更新与防抖

样品发票模板字段编辑采用 500ms 防抖：

```typescript
const scheduleProfileSave = useCallback((nextTemplate) => {
  if (!selectedProfileId) return;
  if (profileSaveTimerRef.current) {
    window.clearTimeout(profileSaveTimerRef.current);
  }
  profileSaveTimerRef.current = window.setTimeout(() => {
    persistProfileTemplate(nextTemplate);
  }, 500);
}, [persistProfileTemplate, selectedProfileId]);
```

避免用户连续输入时频繁调用 saveBusinessProfile API。

### 14.4 颜色豁免范围

| 文件 | 豁免原因 |
| --- | --- |
| `printDocument.ts` BASE_PRINT_STYLES | 打印 HTML 在 window.open 新窗口渲染，CSS 变量在跨窗口上下文不可靠 |
| `FabricSampleInvoiceGenerator.tsx` 内 confirm/resizeHandle 内联 style | iframe 内动态创建的 overlay 元素，无法应用外层 CSS 变量 |

其余工具组件主体严格使用 BDS token，无硬编码 hex。

---

## §15 交叉链接

1. [BusinessTools 业务工具箱 — 父模块] — 本文档为父模块下的子文档
2. [Settings 公司档案与配置](../../08-设置与后台/Settings-设置/公司档案与配置.md) — ExporterProfile 由设置页编辑并存入 panda_system_config
3. [Orders 订单管理 — 模块概述](../../03-订单与生产/Orders-订单管理/模块概述.md) — 发货通知/合同/装箱单均引用 orders 数据
4. [Relations 联系人](../../02-客户与开拓/Relations-联系人.md) — 合同/装箱单的买卖双方/收货方数据源
5. [Products 数字档案 — 面料档案](../../06-资源与支撑/Products-数字档案/面料档案.md) — 样品发票产品联想数据源
6. [DocumentCenter 单据中心 — 制单引擎](../../04-出货与单据/DocumentCenter-单据中心/制单引擎.md) — 出运制单引擎（跳转目标）
7. [DocumentCenter 单据中心 — 单据模板编辑器](../../04-出货与单据/DocumentCenter-单据中心/单据模板编辑器.md) — 单据模板管理（跳转目标）
8. [Pricing 定价与成本 — 模块概述](../../03-订单与生产/Pricing-定价与成本/模块概述.md) — 报价计算器/退税率（跳转目标）

---

## §16 变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v1.0 | 2026-08-15 | 初始版本：4 内嵌文档工具 + 共享打印工具 + 出口方档案 + 阶段 IA-2 收编说明 |
