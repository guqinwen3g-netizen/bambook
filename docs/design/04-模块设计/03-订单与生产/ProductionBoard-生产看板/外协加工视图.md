# 外协加工视图 (Outsourcing View)

## 一、元信息

| 项 | 值 |
|---|---|
| **定位** | 外协加工全生命周期视图 — OutsourcingOrder 模型的创建/分配/进度跟踪/质量验收；跟单员在生产跟进视图可见外协进度（加工厂/工序/数量/验收/交期） |
| **入口** | 当前嵌入订单详情页 Pipeline 外协只读区块；规划独立外协管理页（production → 外协 tab） |
| **核心角色** | 业务员（含跟单、采购执行，原 merchandiser/buyer）、QC、销售主管（部门管理职责） |
| **范式** | 范式 D — 详情子区块（当前）/ 范式 A — 列表+编辑器（规划独立页） |
| **优先级** | P1 |
| **实现状态** | ✅ OutsourcingOrder/OutsourcingLine 模型已建；✅ 订单详情页 Pipeline 内外协只读区块已落地（getProductionBoard 聚合）；✅ 外协 CRUD 服务与路由已实现；⚠️ 独立外协管理页（列表+编辑器）未成页；⚠️ 外协与生产阶段门禁联动未实现 |
| **关联 PRD 章节** | §19.7（生产跟单表格之外协栏）、§19.18（供应商管理）、§9.3（质量门禁） |
| **关联代码** | [stageService.ts getProductionPipeline](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/production/stageService.ts#L171-L193) 外协只读区块聚合 / `schema.prisma` OutsourcingOrder / OutsourcingLine 模型（L4206-L4280）/ [types.ts OutsourcingOrder](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/types.ts#L3905-L3944) 前端类型 |

---

## 二、页面骨架

### 2.1 当前形态：订单详情页 Pipeline 外协只读区块

```
┌─ 订单详情页 → 生产管线 → 外协 tab ─────────────────────┐
│  外协加工 (N 单)                                        │
│  ┌──────────────────────────────────────────────────┐  │
│  │ OSO-20260807-001  [生产中]  缝纫 Sewing          │  │
│  │ 供应商：张家港一厂  ·  1,200 PC                   │  │
│  │ 计划交期 2026-08-20  ·  实际 2026-08-22          │  │
│  │ 合格 1,180  ·  拒收 20  ·  合格率 98.3%          │  │
│  └──────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────┐  │
│  │ OSO-20260807-002  [已收货]  印花 Printing        │  │
│  │ ...                                              │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 2.2 规划形态：独立外协管理页

```
┌─────────────────────────────────────────────────────────────────────┐
│  PageHeader: 外协加工 / Outsourcing / Management         [+ 新建]   │
├─────────────────────────────────────────────────────────────────────┤
│  筛选行: [状态▼] [工序▼] [供应商▼] [搜索...]            共 N 单     │
├─────────────────────────────────────────────────────────────────────┤
│  列表表 (raisedCard)                                                 │
│  ┌──────────┬──────┬──────┬──────┬──────┬──────┬──────┬─────────┐  │
│  │外协单号  │工序  │供应商│数量  │计划  │实际  │状态  │合格率   │  │
│  │OSO-...001│缝纫  │一厂  │1200  │08-20 │08-22 │生产中│98.3%    │  │
│  └──────────┴──────┴──────┴──────┴──────┴──────┴──────┴─────────┘  │
│  点击行 → 展开明细行（OutsourcingLine 工序明细）                    │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 三、OutsourcingOrder 模型（数据真源）

### 3.1 模型字段全量

真源 [schema.prisma L4206-L4253](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/prisma/schema.prisma#L4206-L4253)：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键，格式 `OSO_${shortId}` |
| orderNumber | String @unique | 外协单号（如 OSO-20260807-001） |
| supplierId | String? | 供应商 ID（snapshot FK → Relation） |
| orderId | String? | 关联订单（snapshot FK） |
| bomId | String? | 关联 BOM（snapshot FK） |
| processType | String | Sewing \| Cutting \| Washing \| Printing \| Embroidery \| Dyeing \| Other |
| description | String? | 外协内容描述 |
| quantity | Decimal(18,4) | 数量 |
| unit | String | PC \| SET \| DZ \| KG |
| unitPrice | Decimal(18,4) | 单价 |
| currency | String | 默认 CNY |
| totalAmount | Decimal(18,4) | = quantity × unitPrice |
| orderDate | String? | YYYY-MM-DD |
| plannedDeliveryDate | String? | YYYY-MM-DD |
| actualDeliveryDate | String? | YYYY-MM-DD |
| qualityAcceptedQty | Decimal(18,4) | 合格数量 |
| qualityRejectedQty | Decimal(18,4) | 不合格数量 |
| status | String | Draft \| Sent \| Confirmed \| InProduction \| Received \| Cancelled |
| notes | String? | 备注 |
| createdAt / updatedAt | BigInt | 时间戳 |
| deletedAt | BigInt? | 软删除 |

### 3.2 OutsourcingLine 子模型

真源 [schema.prisma L4255-L4280](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/prisma/schema.prisma#L4255-L4280)：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | `OSL_${shortId}` |
| outsourcingOrderId | String | FK cascade delete |
| processType | String | Sewing \| Cutting \| Washing \| Printing \| Other |
| description | String | 工序描述 |
| materialCode | String? | 物料编码（snapshot FK） |
| quantity | Decimal(18,4) | 数量 |
| unit | String | 单位 |
| unitPrice | Decimal(18,4) | 单价 |
| amount | Decimal(18,4) | = quantity × unitPrice |
| notes | String? | 备注 |

**索引**：`@@index([supplierId])` / `@@index([orderId])` / `@@index([bomId])` / `@@index([status])` / `@@index([processType])`

---

## 四、工序类型枚举

`OutsourcingProcessType`（types.ts）：

| 枚举值 | 中文 | 说明 |
|--------|------|------|
| Sewing | 缝纫 | 成衣缝合工序 |
| Cutting | 裁剪 | 面料裁剪 |
| Washing | 水洗 | 水洗整理 |
| Printing | 印花 | 印花加工 |
| Embroidery | 绣花 | 刺绣工序 |
| Dyeing | 染色 | 面料染色 |
| Other | 其他 | 其他外协工序 |

---

## 五、状态机

### 5.1 外协单状态流转

```
Draft ──▶ Sent ──▶ Confirmed ──▶ InProduction ──▶ Received
   │         │          │             │              
   └─────────┴──────────┴─────────────┴──▶ Cancelled
```

| 状态 | 中文 | 含义 |
|------|------|------|
| Draft | 草稿 | 新建未发送 |
| Sent | 已发送 | 已发外协厂 |
| Confirmed | 已确认 | 外协厂确认接单 |
| InProduction | 生产中 | 外协厂加工中 |
| Received | 已收货 | 验收完成入库 |
| Cancelled | 已取消 | 终态 |

### 5.2 与生产阶段的关系

⚠️ **当前未联动**。外协单状态变更不自动触发生产阶段（如 manufacturing）推进。设计建议：
- 外协全部 Received → 可推进 `manufacturing` 阶段（待补门禁）
- 外协逾期 → 看板阻塞红标（待补）

---

## 六、外协供应商分配

### 6.1 供应商关联

- `supplierId` snapshot FK → Relation（category=Supplier）+ FactoryProfile 1:1
- `getProductionPipeline` 查询时批量关联 Relation 取 `name` 填充 `supplierName`
- 创建外协单时选择供应商 → 校验 FactoryProfile 未拉黑（与采购单同规则，待补）

### 6.2 供应商选择交互（规划）

| 交互 | 设计 |
|------|------|
| 选择器 | `RelationCombobox`（与订单/采购同组件） |
| 筛选 | 仅显示 category=Supplier 且 FactoryProfile.blacklistedAt=null |
| 自动填充 | 选供应商后带出 supplierName / supplierCode 快照 |
| 拉黑拦截 | 选已拉黑供应商 → danger toast "该供应商已被拉黑" |

---

## 七、进度跟踪

### 7.1 进度字段

| 字段 | 含义 | 展示 |
|------|------|------|
| orderDate | 下单日期 | YYYY-MM-DD |
| plannedDeliveryDate | 计划交期 | YYYY-MM-DD |
| actualDeliveryDate | 实际交期 | YYYY-MM-DD（未交付空） |
| status | 当前状态 | 状态徽章 |

### 7.2 交期偏差计算

```
daysLate = actualDeliveryDate - plannedDeliveryDate （天）
```

- daysLate > 0 → 逾期，warning chip "逾期 N 天"
- daysLate = 0 / null → 未交付或按期

### 7.3 与订单详情页 Pipeline 集成

[stageService.ts getProductionPipeline L171-L193](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/production/stageService.ts#L171-L193)：

```typescript
const outsourcingRows = await prisma.outsourcingOrder.findMany({
  where: { orderId, deletedAt: null },
  select: { id, orderNumber, supplierId, processType, status, quantity, unit,
            plannedDeliveryDate, actualDeliveryDate,
            qualityAcceptedQty, qualityRejectedQty },
  orderBy: { createdAt: 'desc' },
});
// 批量查 Relation 取 supplierName
```

返回时 `quantity` / `qualityAcceptedQty` / `qualityRejectedQty` 从 Decimal 转 Number（前端不处理 BigInt/Decimal）。

---

## 八、质量验收

### 8.1 验收字段

| 字段 | 类型 | 说明 |
|------|------|------|
| qualityAcceptedQty | Decimal(18,4) | 合格数量 |
| qualityRejectedQty | Decimal(18,4) | 不合格数量 |

### 8.2 合格率计算

```
合格率 = qualityAcceptedQty / (qualityAcceptedQty + qualityRejectedQty) × 100%
```

展示规则：
- 合格率 ≥ 95% → success 语义
- 90% ≤ 合格率 < 95% → warning 语义
- 合格率 < 90% → danger 语义

### 8.3 验收与入库联动

⚠️ **当前未实现**。外协验收合格数量未自动写入库存（无 InventoryTransaction 模型）。设计建议：
- 外协 Received 时 qualityAcceptedQty → 触发库存入库（待补 InventoryTransaction）

---

## 九、API 端点（规划）

| 方法 | 路径 | 说明 | 实现状态 |
|------|------|------|---------|
| GET | `/api/v1/outsourcing?orderId=&supplierId=&status=` | 外协单列表 | ⚠️ 待补独立路由 |
| GET | `/api/v1/outsourcing/:id` | 外协单详情（含 lines） | ⚠️ 待补 |
| POST | `/api/v1/outsourcing` | 创建外协单 | ⚠️ 待补独立路由 |
| PUT | `/api/v1/outsourcing/:id` | 更新外协单（仅 Draft） | ⚠️ 待补 |
| POST | `/api/v1/outsourcing/:id/transition` | 状态流转 | ⚠️ 待补 |
| DELETE | `/api/v1/outsourcing/:id` | 软删除 | ⚠️ 待补 |

**当前可读路径**：`GET /api/v1/production/pipeline/:orderId` 返回的 `outsourcing` 字段（只读区块）。

---

## 十、权限矩阵

| 角色 | 查看外协 | 创建/编辑 | 状态流转 | 验收录入 |
|------|---------|----------|---------|---------|
| owner / admin / manager | ✅ | ✅ | ✅ | ✅ |
| merchandiser | ✅ 同部门 | ✅ | ✅ | ⚠️ 仅录入合格/拒收数 |
| buyer（采购员） | ✅ | ✅ | ✅ | ❌ |
| qc | ✅ | ❌ | ⚠️ 仅 InProduction→Received | ✅ |
| guest | ❌ | — | — | — |

---

## 十一、四态规范

| 状态 | 触发 | UI 规范 |
|------|------|---------|
| 加载态 | fetch outsourcing 中 | Loader2 + "加载外协..." |
| 空态 | 无外协单 | "暂无外协加工记录"灰态斜体（不隐藏区块） |
| 错误态 | fetch 失败 | danger alert + retry |
| 无权限 | 403 | warning alert "无权查看外协" |

---

## 十二、业务规则关联

| 规则 | 接入情况 |
|------|---------|
| §9.3 质量门禁 | ⚠️ 外协验收未接生产门禁（manufacturing 阶段不校验外协完成） |
| 供应商拉黑拦截 | ⚠️ 外协创建未校验 FactoryProfile.blacklistedAt（采购单已实现，外协待补） |
| 交期评分（H1c） | ⚠️ 外协交期未触发供应商自动评分（采购单已实现） |
| 质量评分（H1c） | ⚠️ 外协验收未触发供应商自动评分（采购单已实现） |

---

## 十三、可访问性

| 项 | 规范 |
|----|------|
| 外协单行可点击 | 展开明细行（OutsourcingLine 工序明细） |
| 状态徽章 | bds-badge 语义变体 |
| 工序类型 | 中文 label 展示（BUSINESS_LINE_LABELS 同模式） |
| 数量 tabular-nums | 防数字跳动 |

---

## 十四、设计系统约束

| 约束 | 合规说明 |
|------|---------|
| 禁止硬编码 hex | 全部 CSS 变量 |
| 圆角 | 卡片 `rounded-card`、徽章 `rounded-compact` |
| 无阴影 | border + bg tint |
| 字重 | font-light（300） |
| 合格率语义色 | success/warning/danger 三态，走 statusSemanticClass |

---

## 十五、交叉链接

| 文档 | 相对路径 |
|------|---------|
| 生产看板模块概述 | `./模块概述.md` |
| 泳道看板 | `./泳道看板.md` |
| 订单详情页（外协只读区块宿主） | `../Orders-订单管理/订单详情页.md` |
| 采购到货与质检（同模式验收） | `../Procurement-采购管理/采购到货与质检.md` |
| 质量门禁规则 | `../../03-业务规则/质量门禁规则.md` |
| 实体关系总览 | `../../02-数据模型/实体关系总览.md` |

---

## 十六、变更日志

| 日期 | 变更 | 关联 |
|------|------|------|
| 2026-08-15 | 初版外协加工视图设计文档，覆盖 OutsourcingOrder 模型 / 工序枚举 / 状态机 / 供应商分配 / 进度跟踪 / 质量验收 | — |
