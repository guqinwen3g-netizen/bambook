# 订单录入表单 (Order Entry Form)

## §1 元信息

| 项 | 值 |
|---|---|
| **定位** | 手动创建订单的统一入口——字段字典驱动的分簇表单，与详情页编辑态、PDF 导入预览共享单一真源 |
| **入口** | 订单列表页「+ 新建订单」按钮 → Modal（`showAddModal`）|
| **核心角色** | 业务员（含跟单，主要录入者） |
| **范式** | 范式 E — 录入 Modal（分簇字段 + 实时金额联动 + Relation 下拉 + autoFillFrom）|
| **优先级** | P0 |
| **实现状态** | ✅ 已落地（字段字典 `fieldsForManualForm` 驱动 / 金额联动 / Relation 下拉 autoFillFrom / 类型动态字段均接入；MOQ 校验为 P1 缺口） |
| **关联 PRD 章节** | §5.6（订单编号）、§9.5（金额联动）、§9 业务规则（MOQ/审批）、§19.7（订单页设计） |
| **关联代码** | [OrderManager.tsx](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/OrderManager.tsx#L360-L438) `getDefaultNewOrder` / [orderSchema.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/lib/orderSchema.ts) `fieldsForManualForm` / `requiredKeysForManual` / `computeAutoFillPatch` / `OrderClusterBlock` |

---

## §2 表单骨架

```
┌─────────────────────────────────────────────────────────────────────┐
│  Modal Header: 新建订单                                  [×]         │
├─────────────────────────────────────────────────────────────────────┤
│  类型选择: [面料 ▾]  (切换 → 动态显示/隐藏 cluster)                  │
├─────────────────────────────────────────────────────────────────────┤
│  Scroll Body (orderEntryScrollRef)                                   │
│  ┌─ Cluster: 基础档案 (basic) ──────────────────────────────────┐  │
│  │ PO#*  PO Item  PO 日期  季节  生产批次  品/色号  客户编码      │  │
│  │ 品名/规格  数量*  单位*                                       │  │
│  └──────────────────────────────────────────────────────────────┘  │
│  ┌─ Cluster: 买卖四方 (parties) ────────────────────────────────┐  │
│  │ [客户] customer* addr contact tel                            │  │
│  │ [面料工厂] mill ...                                           │  │
│  │ [收货方] consignee ...                                        │  │
│  │ [结款方] billTo ...                                           │  │
│  │ [内部团队] sales/merchandiser/supervisor                      │  │
│  └──────────────────────────────────────────────────────────────┘  │
│  ┌─ Cluster: 交期与物流 (delivery) ─────────────────────────────┐  │
│  ┌─ Cluster: 面料规格/物料信息 (fabric|materials) ──────────────┐  │
│  ┌─ Cluster: 销售与收汇 (sales) ────────────────────────────────┐  │
│  ┌─ Cluster: 采购与供应商 (purchase) ───────────────────────────┐  │
│  ┌─ Cluster: 特别说明 (instructions) ───────────────────────────┐  │
├─────────────────────────────────────────────────────────────────────┤
│  Footer: [取消]  [保存]   (必填项未满时保存禁用)                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## §3 字段分组（Cluster）

录入表单的 cluster 顺序由 `ORDER_CLUSTERS.manualOrder` 决定（与详情页 `detailOrder` 分离）。`manualOrder === 99` 的 cluster 不在录入表单显示（shipment/sampleShipment/sampleFabric/garmentProduction/sampleGarment —— 这些是履约阶段才填的字段）。

| manualOrder | cluster | 中文名 | 适用类型 | 录入字段数 | 说明 |
|-------------|---------|--------|---------|-----------|------|
| 1 | basic | 基础档案 | all | 11 | PO#/PO Item/PO 日期/季节/批次/品色号/客户编码/品名/数量/单位 |
| 2 | parties | 买卖四方 | all | ~22 | 5 子组（客户/工厂/收货/结款/内部团队）每子组 4 字段 |
| 3 | delivery | 交期与物流 | all | 5 | 生产交期/出厂交期/收货方/实际发货日/运输方式 |
| 4 | fabric | 面料规格 | 仅 fabric | 4 | 面料编号/成份/门幅/克重 |
| 4 | materials | 物料信息 | 仅 other | 2 | 分类/规格备注 |
| 5 | sales | 销售与收汇 | all | 11 | 销售单价/合同金额/付款方式/条件/币种/预计付款日/实际付款日/实际金额 |
| 6 | purchase | 采购与供应商 | all | 6 | 采购单价/付款日/供应商发票号/日期/金额 |
| 7 | instructions | 特别说明 | all | 1+ | specialInstructions + ocDays |
| 99 | shipment | 出货与发票 | — | 不录入 | 履约阶段在详情页编辑 |
| 99 | sampleShipment | 大货船样 | — | 不录入 | 同上 |
| 99 | sampleFabric | 匹头样 | — | 不录入 | 同上 |
| 99 | garmentProduction | 成衣生产跟单 | — | 不录入 | OrderLine 级，详情页 + 行编辑 |
| 99 | sampleGarment | 成衣样衣阶段 | — | 不录入 | 同上 |

**渲染真源**：`fieldsForManualForm(typeKey)` → 过滤 `shownInManualForm === true && (!applicableTypes || applicableTypes.includes(typeKey))`，再按 `cluster.manualOrder` + 数组内顺序渲染。

---

## §4 字段分组明细

### 4.1 基础档案（basic）

| 字段 key | 中文标签 | 类型 | 必填 | 适用类型 | 说明 |
|---------|---------|------|------|---------|------|
| poNumber | 订单号 (PO#) | text | ✅ | all | placeholder PE25-1234 |
| itemNo | PO Item 编号 | text | — | all | 同一 PO 下自动 0010/0020 递增，可改修订号 |
| poDate | PO 日期 | date | — | all | YYYY-MM-DD |
| season | 季节 | text | — | all | placeholder SS26 |
| productionBatch | 生产批次 | text | — | all | placeholder B001 |
| productColorCode | 品/色号 | text | — | fabric | 从首行 millQuality 自动带出 |
| clientCode | 客户编码 | text | — | all | 从首行 materialCode 自动带出 |
| referenceBatch | 参考批次 | text | — | fabric | 仅详情显示，录入不显示 |
| product | 品名/规格 | text | — | all | placeholder Cotton Jersey |
| quantity | 订单数量 | number | ✅ | all | 金额联动依据 |
| unit | 单位 | enum | ✅ | all | Meter/Yard/KG/Pcs/Dozen/Roll/Set/Other；按类型默认 |

### 4.2 买卖四方（parties）— 5 子组

每个角色子组含：名称（Relation 下拉）+ 地址 + 联系人 + 电话，选择 Relation 后触发 `autoFillFrom` 联动填充。

| 子组 | 字段 | Relation FK | autoFillFrom 映射 |
|------|------|------------|------------------|
| 客户 customer | customer* / customerAddress / contactPerson / contactTelephone | customer | officialAddress→customerAddress / primaryContactName→contactPerson / primaryContactPhone→contactTelephone |
| 面料工厂 mill | millName / millAddress / millContact / millPhone | mill | — |
| 收货方 consignee | consigneeName / consigneeAddress / consigneeContact | consignee | — |
| 结款方 billTo | billToName / billToAddress / billToContact / billToIsAgent | billTo | — |
| 内部团队 internal | salesPerson / merchandiser / supervisor | internal | — |

**`customer` 必填**（required: true）。`onCreateRelation` 回调支持下拉未命中时即时创建新 Relation。

### 4.3 交期与物流（delivery）

| 字段 key | 中文标签 | 类型 | 说明 |
|---------|---------|------|------|
| productionDate | 生产交期 | date | YYYY-MM-DD |
| clientDate | 出厂交期 | date | Exmill，列表页"日期"列主显 |
| shippingDate | 实际发货日 | date | 履约阶段填 |
| shippingMethod | 运输方式 | text | Sea/Air/Express |

### 4.4 面料规格（fabric，仅 Fabric）/ 物料信息（materials，仅 Other）

| cluster | 字段 | 说明 |
|---------|------|------|
| fabric | fabricCode 面料编号 / fabricContent 成份 / width 门幅(CM) / gsm 克重 | 4 字段 |
| materials | materialCategory 物料分类(辅料/纱线/其他) / materialSpecNotes 规格备注 | 2 字段 |

### 4.5 销售与收汇（sales）

| 字段 key | 中文标签 | 类型 | 说明 |
|---------|---------|------|------|
| salesPrice | 销售单价 | currency(sales) | 金额联动源 |
| contractAmount | 合同金额 | currency(sales) | = salesPrice × quantity（自动） |
| salesCurrency | 销售币种 | enum | 默认 USD |
| purchaseCurrency | 采购币种 | enum | 默认 CNY |
| paymentInstrument | 付款方式 | enum | T/T, L/C, O/A, D/P, D/A |
| paymentTerms | 付款条件 | text | Net 30, At Sight |
| expectedPaymentDate | 预计付款日 | date | — |
| actualPaymentDate | 实际付款日 | date | — |
| actualPaymentAmount | 实际收款金额 | currency(sales) | — |
| ocDays | O/C 天数 | number | 默认 30 |

### 4.6 采购与供应商（purchase）

| 字段 key | 中文标签 | 类型 |
|---------|---------|------|
| purchasePrice | 采购单价 | currency(purchase) |
| purchasePaymentDate | 采购付款日 | date |
| supplierInvoiceNumber | 供应商发票号 | text |
| supplierInvoiceDate | 供应商发票日期 | date |
| supplierInvoiceAmount | 供应商发票金额 | currency(purchase) |

### 4.7 特别说明（instructions）

| 字段 key | 中文标签 | 类型 |
|---------|---------|------|
| specialInstructions | 特别说明 | longText |
| asPerson | 跟单/售后联系人 | text |

---

## §5 验证规则

### 5.1 必填校验

`requiredKeysForManual(typeKey)` 派生必填字段集（`FieldMeta.required === true` 且类型匹配）：

| 必填字段 | 适用类型 | 校验时机 |
|---------|---------|---------|
| poNumber | all | 保存前 |
| quantity | all | 保存前 |
| unit | all | 保存前 |
| customer | all | 保存前 |

**前端表现**：必填未满时「保存」按钮禁用；必填字段 label 前置 `*` 标记。

### 5.2 类型校验

| 字段 | 规则 |
|------|------|
| number | 非负有限数；空值兜底 0 |
| date | `YYYY-MM-DD` 格式（`cleanDate` 校验） |
| enum | 必须在 `enumOptions` 白名单内 |
| currency | 2 位小数（`round2`）|

### 5.3 唯一性校验

| 字段 | 规则 | 校验位置 |
|------|------|---------|
| poNumber | 全局 `@unique` | 后端创建时校验，冲突返回 409 |
| code | 全局 `@unique`（SO-YYYYMM-NNNN） | 后端 SequenceRegister 分配 |
| (orderId, itemNo) | `@@unique` | 同一 PO 下 itemNo 不重复 |

---

## §6 MOQ 校验（最小起订量）

> ⚠️ **当前未实现**（P1 缺口）—— 录入/保存时无 MOQ 校验，低于 MOQ 审批链未实现

### 6.1 设计目标（PRD §9 MOQ 规则）

| 订单类型 / 业务线 | MOQ 阈值 | 单位 |
|------------------|---------|------|
| Fabric 面料 | 800 | Meter |
| Garment 成衣大货 | 200 | Pcs |
| Garment Capsule（设计师小单） | 20 | Pcs |
| Other 其他 | （待定） | KG |

### 6.2 校验流程（待实现）

1. 保存前校验 `quantity < MOQ(type, businessLine)`
2. 低于 MOQ → 弹出审批表单（填写低于 MOQ 原因 + 主管审批）
3. 审批通过 → 允许保存并标记 `belowMoqApproved=true`（待新增字段）
4. 审批拒绝 → 阻止保存，提示用户调整数量

### 6.3 实现落点

- 前端：`handleSaveNewOrder` 保存前插入 MOQ 校验分支
- 后端：`POST /api/v1/orders` 创建时强校验（防止绕过前端）
- 字段：Order 模型待新增 `belowMoqApproved Boolean?` + `belowMoqReason String?`

---

## §7 金额联动（applyAmountLinkage）

合同金额 ↔ 销售单价 × 数量 双向联动，规则在 `OrderManager` L571+：

| 触发 patch | 联动结果 |
|-----------|---------|
| 含 salesPrice | contractAmount = salesPrice × quantity（quantity > 0 时） |
| 含 contractAmount | salesPrice = contractAmount / quantity（quantity > 0 时） |
| 含 quantity（不含单价/合同） | contractAmount = salesPrice × quantity（沿用既存单价） |
| 同时含 salesPrice + contractAmount | 以 contractAmount 口径回算单价兜底，保留用户手动值 |

所有结果 `round2`（2 位小数，避免 float 精度噪音）。详情页编辑态同样复用此联动。

---

## §8 Relation 下拉与 autoFillFrom 联动

| 项 | 说明 |
|----|------|
| 控件 | `RelationCombobox`（从 `relations` 池按 `relationFk` 类型过滤） |
| 选择后 | `computeAutoFillPatch` 按 `autoFillFrom.mapping` 自动填充地址/联系人/电话 |
| 未命中 | `onCreateRelation(typedName, fkTarget)` 即时创建新 Relation 并回填 |
| FK 字段 | 同时写入 `xxxRelationId`（snapshot FK，不强制外键约束，Relation 归档不级联） |

---

## §9 类型切换动态字段

类型选择（Fabric/Garment/Other）切换时：
- `fieldsForManualForm(typeKey)` 重新过滤 `applicableTypes`
- Fabric → 显示 `fabric` cluster（4 字段），隐藏 `materials`
- Other → 显示 `materials` cluster（2 字段），隐藏 `fabric`
- Garment → 隐藏 `fabric` 与 `materials`（成衣专属字段在 OrderLine 级，录入阶段不显示）
- `unit` 默认值按类型切换：Fabric→Meter / Garment→Pcs / Other→KG

详细字段差异矩阵见 [订单类型分层.md](./订单类型分层.md)。

---

## §10 保存动作

`handleSaveNewOrder` → `apiService.createOrder(newOrder)`：

1. 前端必填校验通过
2. （待实现）MOQ 校验
3. POST `/api/v1/orders`，后端分配 `code`（SO-YYYYMM-NNNN）+ 写入 `fieldSources`（标记 'manual'）
4. 成功 → 关闭 Modal + `resetNewOrder()` + 新订单合并入 `orders` state + toast success
5. 失败 → `bds-toast danger` 显示后端错误，**不关闭 Modal**（保留用户输入防丢稿）

**默认值**（`getDefaultNewOrder`）：
- status: 'Pending'（创建即待确认）
- paymentMethod: 'Net 30'
- ocDays: 30
- salesCurrency: 'USD' / purchaseCurrency: 'CNY'
- needShipmentSample: true / needHeaderSample: true

---

## §11 权限

| 角色 | 录入权限 | 限制 |
|------|---------|------|
| owner/admin/manager | ✅ | 无 |
| sales | ✅ | 自动设置 ownerId=me；客户逾期 >60 天应禁止（P0 缺口未实现） |
| merchandiser | ⚠️ | 通常不录入（业务员职责），可编辑既有单 |
| qc/finance/warehouse | ❌ | 不参与录入 |

---

## §12 四态规范

| 状态 | UI 规范 |
|------|---------|
| 加载态 | 保存中按钮 spinner + 禁用 |
| 空态 | 类型未选择时 cluster 区显示提示 |
| 错误态 | toast danger + 保留输入 |
| 必填未满 | 保存按钮禁用 + 必填字段红框提示 |

---

## §13 移动端适配

- Modal 改为全屏 Sheet（`height: 'full'`）
- cluster 垂直堆叠，单列布局
- Relation 下拉改为底部 Sheet 选择器
- 顶部固定取消/保存，底部留出键盘空间

---

## §14 设计系统约束

| 约束 | 合规说明 |
|------|---------|
| 字段槽 | `bds-input`（编辑态）/ 玻璃胶囊（查阅态）|
| cluster 面板 | raisedCard + `OrderSectionHeader`（kicker EN + 中文主标题 + iconKey）|
| 数字输入 | `FIELD_NO_SPINNER_CLASS` 去原生步进器 |
| 颜色 | 全部 CSS 变量，无硬编码 hex |
| 圆角 | 输入 `rounded-field`；面板 `rounded-card` |
| 必填标记 | `*` 用 `var(--warning-text)` |

---

## §15 交叉链接

1. [模块概述](./模块概述.md) — 录入表单在模块中的定位
2. [订单列表页](./订单列表页.md) — 新建按钮入口
3. [订单详情页](./订单详情页.md) §4 — 编辑态复用同一字段字典
4. [订单类型分层](./订单类型分层.md) — 类型切换动态字段依据
5. [订单状态机](./订单状态机.md) — 创建后初始态 Pending
6. [订单与生产模型组](../../02-数据模型/订单与生产模型组.md) — Order/OrderLine 字段约束
7. [字段词典真源](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/lib/orderSchema.ts) — `fieldsForManualForm` / `requiredKeysForManual`

---

## §16 相关文档索引

| 文档 | 路径 |
|------|------|
| 默认值真源 | [OrderManager.tsx getDefaultNewOrder](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/OrderManager.tsx#L360-L436) |
| 金额联动 | [OrderManager.tsx applyAmountLinkage](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/OrderManager.tsx#L571-L578) |
| 字段词典 | [lib/orderSchema.ts ORDER_FIELDS](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/lib/orderSchema.ts#L179) |
| Cluster 渲染组件 | [components/order/OrderClusterBlock.tsx](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/order/OrderClusterBlock.tsx) |
| 前端类型 | [types.ts Order](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/types.ts#L666-L821) |
