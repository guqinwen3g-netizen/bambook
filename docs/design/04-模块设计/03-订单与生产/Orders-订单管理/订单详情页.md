# 订单详情页 (Order Detail Page)

## 元信息

| 项 | 值 |
|---|---|
| **定位** | 订单详情全量视图 — 一个订单的"主面板"：数据查阅、编辑、状态推进、下游履约动作触发、关联实体穿透、全链路生命周期、生产管线、审计留痕 |
| **入口** | 订单列表页点击任意行 → 全屏覆盖打开（桌面端）/ 底部 Sheet 快速操作（移动端） |
| **核心角色** | 业务员（含跟单，日活最高）、QC、销售主管（部门管理职责）、财务（宽泛容器不细分） |
| **范式** | 范式 D — 详情页 (Detail Surface)：全屏沉浸式阅读 + 双栏（左锚点地图 / 右内容） |
| **优先级** | P0 |
| **实现状态** | ✅ 已落地（已接入生产管线 10 阶段门禁 + 订单 360° 全链路 + 审计留痕 + 履约动作 prime 导航 + 订单行级/项目级双阅读模式） |
| **关联 PRD 章节** | §5.5（订单状态机）、§5.6（订单编号 SO-YYYYMM-NNNN）、§9（业务规则 10 条）、§19.7（订单 / 生产订单页详细设计）、§24.2（分组导航） |
| **关联代码** | [OrderManager.tsx](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/OrderManager.tsx#L1271-L1738) 桌面端详情渲染主体 / [OrderContextSection.tsx](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/order/OrderContextSection.tsx) 订单 360° / [orderSchema.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/lib/orderSchema.ts) 字段词典真源 / `schema.prisma` Order/OrderLine/OrderStatusTransition 模型 |

---

## 一、页面骨架

### 1.1 桌面端布局

```
┌─────────────────────────────────────────────────────────────────────┐
│  Hero 头部 (position:absolute, z-60, top-0 inset-x-0, pt-5 pb-3)    │
│  ┌──────────────────────────────────────┐ ┌───────────────────────┐  │
│  │ KICKER: Production Detail            │ │ 编辑项目/保存修改 │ × │  │
│  │ HERO: PE25-1234  [已确认] 查阅模式  │ │ (filterbar 背景)      │  │
│  │ META: PO 日期 2026-07-01 · SS26 ·   │ └───────────────────────┘  │
│  │       Peerless · Capsule 徽章        │                            │
│  └──────────────────────────────────────┘ (top mask 渐隐 96px)      │
├─────────────────────────────────────────────────────────────────────┤
│  Scroll Layer (z-10, inset-0, overflow-y-auto)                      │
│  ┌─────── grid-cols-[240px_minmax(0,1fr)] gap-5 px-5 pt-24 pb-5 ─┐  │
│  │ L Sticky │ Right Content Column (space-y-6)                   │  │
│  │ Detail   │   ① 概览四卡（数量/金额/出厂交期/客户）             │  │
│  │ Map (6-8│   ② 履约动作区（生成采购/发起验货/创建出运）         │  │
│  │ 项锚点) │   ③ 生产进度时间线（6 态步骤条 + 变更时间线）        │  │
│  │          │   ④ 关联视图（EntityLink：客户/供应商/收货方等）     │  │
│  │          │   ⑤ 订单 360° 全链路（报价→开发→BOM→…→财务）       │  │
│  │          │   ⑥ 订单变更历史（AuditLog）                        │  │
│  │          │   ⑦ 生产管线 10 阶段门禁（PreCutChecklist 等）      │  │
│  │          │   ⑧ 行项目详情 OR 行明细表（按 line 选择策略）      │  │
│  │          │   ⑨ 分簇字段区（13 cluster，字典驱动）              │  │
│  └──────────┴────────────────────────────────────────────────────┘  │
│  Footer Action Bar (position:absolute, inset-x-0 bottom-0, z-60)    │
│  ┌──────────────────────────────────────────────────────┬────────┐ │
│  │ bottom mask 渐隐 24px backdrop-blur                  │ 归档🗑 │ │
│  └──────────────────────────────────────────────────────┴────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

### 1.2 视觉层级说明（BDS RDL 中性）

| 区块 | 材质/样式规范 | 实现路径 |
|------|--------------|----------|
| 遮罩背景 | `bg-transparent`（底层为订单列表，不增加第二层磨砂）— 点击遮罩空白处关闭 | L1277 |
| Hero 顶部区 | `backdrop-blur-md + mask-image 渐隐 72%→100%` 顶 96px 过渡，与滚动内容自然衔接 | L1289-L1293 `OVERLAY_TOP_MASK_STYLE` |
| Detail Map 左侧锚点 | `sticky top-24` 贴住标题层，超高时内部滚动；`raisedCard` 表面（含 spotlight 聚光 + edgeFadeItem） | L1337-L1369 |
| 右侧每个 Panel | `raisedCard` 表面（带 spotlight + edgeFadeItem + bambook-panel-shadow-viewport） | L1372 起始 `OVERLAY_FORM_PANEL_CLASS` |
| Footer 归档按钮 | `bds-filterbar` 表面仅含一个破坏性按钮（Trash2），与保存/编辑分置两端（安全距离） | L1723-L1735 |
| Hero 主标题 | `text-[28px] font-light tracking-[0.03em]` — 超大号编辑级字重，与大数字同级排版 | L1298 |

### 1.3 移动端布局

不使用全屏覆盖详情（移动端详情以"操作 Sheet"方式触发）：
- 长按/点击行 → `BottomSheet` 弹出 3 项操作：编辑详情 / 快速状态变更 / 归档
- 快速状态变更：`Confirmed/Production/Shipping/Delivered` 横向滚动 badge 一键推进
- 编辑详情 → 跳转到桌面同构的字段字典驱动表单（compact density）

---

## 二、Detail Map 锚点导航（左侧 Sticky）

**锚点项 = 内容面板的目录，点击平滑滚动到对应 `#id`**

| # | 锚点 id | 名称 | 说明 | 固定/动态 |
|---|---------|------|------|-----------|
| 1 | `order-detail-summary` | 概览 | 数量、金额、交期 | 固定 |
| 2 | `order-detail-timeline` | 生产进度 | 状态变更时间线 + 步骤条 | 固定 |
| 3 | `order-detail-related` | 关联视图 | 跨模块实体链接 | 固定 |
| 4 | `order-detail-context` | 全链路 | 报价→财务生命周期 | 固定 |
| 5 | `order-detail-audit` | 变更历史 | 实体审计记录 | 固定 |
| 6 | `order-detail-pipeline` | 生产管线 | 10 阶段门禁 | 固定 |
| 7 | `order-detail-line` / `order-detail-lines` | 项目 / 行明细 | **动态二选一**：已选中 line → 单项目详情；无选择但有 lines → 行明细表 | 动态 |
| 8+ | `section-{cluster.id}` | 基础档案 / 买卖四方 / 交期与物流 / … | **动态**：按字典驱动的 cluster 生成，对应 13 簇字段 | 动态（detailSections） |

**交互规范**：
- 每个锚点左侧有一个 `h-6 w-6 rounded-full border text-[10px] font-light` 序号圆（与 19.7 线框图一致的 Step 索引视觉）
- 悬停/滚动到对应区块 → 锚点 active（`bg-[var(--accent-tint)]` 高亮）
- 超高时：Detail Map 面板自身独立 `overflow-y-auto`，不随内容区滚动

---

## 三、内容面板逐块说明

### 3.1 概览四卡（Summary Hero）

id=`order-detail-summary`

**4 宫格 StatCard：**

| 位置 | 指标名 | 主数据 | 单位/币种 | 备注 |
|------|--------|--------|-----------|------|
| 左上 | **项目数量** | LineItem.quantity → order.quantity | M / Pcs（affix 后缀） | 数据排版 26px，大号超轻数字 tabular-nums |
| 中上 | **项目金额** | LineItem.amount → contractAmount → quoteAmount | USD / CNY 前缀 | 与销售侧 `salesCurrency` 联动 |
| 右上 | **出厂交期** | line.exMillDate → order.clientDate → order.dueDate | `YYYY-MM-DD` 格式 | 归一化显示，空值显示 `—` |
| 右 | **客户** | order.customer | 纯文本（可空长文本 truncate） | 紧凑型 15px（非大号），compact=true |

**Card 分隔与规范**：
- 前 3 张右侧有 `md:border-r` 发丝分隔（第 4 张无）
- 每张 3 行：副指标（`uppercase tracking-[0.22em]` 10px faint）→ 主数据（大号）→ 指标名（11px muted）
- **编辑模式下，此区不变**（概览卡仅展示实时值，不提供编辑）

**子模块：成衣业务线切换（仅 Garment 类型 + 非编辑）**

在四卡下方追加一个 border-t 分隔区：
- 左段说明：Business Line（Capsule = 设计师小单业务）
- 右段：`bds-segment` 双段开关 `成衣大货 / Capsule`
- 切换 → `handleSetBusinessLine(businessLine)` → 走后端 update；切换中显示 `opacity-50` saving 防重
- 编辑模式下隐藏，避免与表单字段冲突（编辑模式走 cluster 字段 `businessLine`，保持单点真源）

---

### 3.2 履约动作区（Fulfillment Actions）

id 无（直接 section，无 anchor 跳转项，但可由 Detail Map 第 2 项快速滚动后肉眼可见）

**3 个 prime 下游按钮**（仅查阅模式 + 传入 `onNavigate` 时显示）：

| 按钮 | 图标 | 跳转目标 | Prime 预填契约 |
|------|------|----------|---------------|
| 生成采购单 | ShoppingCart | view='procurement' 并新建采购单 mode | 复制 `order.customer, order.millName, order.lines[].quantity, order.lines[].description` + 设置 orderId FK |
| 发起验货 | ClipboardCheck | view='qc-workbench' 并选中该订单 | 预选 orderId → QC 任务创建表单默认填入 orderId + fabric/成衣类型 |
| 创建出运 | Ship | view='shipping' 并新建 shipment 模式 | 复制 `consignee, order.lines[].quantity/amount, exMillDate → 预计 ETD` + 设置 orderId FK |

**设计说明**（阶段 IA-3 履约链联动）：
- 3 个 prime 动作是"上游完成后推下游"的关键触发点；所有跳转都携带 orderId 参数，**后端创建表单时自动绑定 FK 并生成 EntityLink**
- 不直接在详情页里嵌入下游创建表单（保持详情页阅读纯净；下游表单进入后有返回导航）
- 按钮统一 `bds-btn-secondary sm` 规格，与 Hero 顶部主按钮视觉层级区分开

---

### 3.3 生产进度时间线（Production Timeline）

id=`order-detail-timeline`

两个子区域：**6 态步骤条**（状态机推进入口）+ **变更事件时间线**

#### 3.3.1 6 态步骤条

```
Pending ──▶ Confirmed ──▶ Production ──▶ Shipping ──▶ Delivered
                                                         
                     ╳ Alert ╳ （异常态：独立出现在末尾右侧）
```

**ORDER_FLOW_STEPS = ['Pending', 'Confirmed', 'Production', 'Shipping', 'Delivered']**

**ORDER_ALLOWED_TRANSITIONS（合法推进矩阵，点击可触发 → 后端契约强校验）：**

| 当前状态 | 可推进到 | 说明 |
|---------|---------|------|
| Pending（待确认） | Confirmed / Alert | PO 录入后初始态；需客户确认 PO |
| Confirmed（已确认） | Production / Alert | 确认后转生产 |
| Production（生产中） | Shipping / Alert | 厂内生产/加工；完成后转等待出运 |
| Shipping（出运中） | Delivered / Alert | 已发运；客户收货后转交付 |
| Delivered（已交付） | — | 终态，不可变更（无动作） |
| Alert（异常） | Pending / Confirmed / Production / Shipping | 异常态独立分支；恢复时可选"回到任一非终态"（业务上通常回到上一态） |

**按钮样式（4 态）**：

| 类型 | 规格 | 视觉 |
|------|------|------|
| `STEP_BTN_DONE` | 已完成（已路过的步骤） | 勾选图标 ✓ + 弱强调（accent 色 + 文字弱化） |
| `STEP_BTN_CURRENT` | 当前态 | Raised 主按钮（`bds-btn` 默认 + 大圆角），强聚焦 |
| `STEP_BTN_ACTIONABLE` | 下一可推进态 | `bds-btn-outline`，悬停强调（唯一可点击项） |
| `STEP_BTN_DISABLED` | 未达步骤 | `bds-btn-ghost` 禁用灰态 + 无 hover |
| Alert 进行中 | 特殊 danger 风格 | `!min-w-[96px]` + danger tint，AlertTriangle 图标 |

**连接器规则**：
- 步骤之间 `h-px w-4` 发丝线
- Done 段：实色 connector (`STEP_CONNECTOR_DONE_STYLE`)
- Pending 段：低饱和 ghost 色

#### 3.3.2 变更事件时间线（Timeline Events）

`OrderStatusTransition` 表事件按 `createdAt` **升序**排列（旧事件在上，新事件在下）

**每行事件结构**：

```
●─── 待确认 → 已确认    [最新]  2026-08-01
│     客户签字回传 PO（备注）
│     by zhangsan@company.com
│
●─── 已确认 → 生产中            2026-08-03
│     …
```

**视觉规则**：
- 圆点：最新事件 = accent 点亮（`TIMELINE_DOT_ACTIVE_CLASS`），其余 = 灰态（`TIMELINE_DOT_CLASS`）
- 最新事件右侧加一个 `[最新]` badge（accent tint）
- 圆点连线：`h-4 w-px` 发丝线（末位事件无连线）
- 状态中文化：`ORDER_STATUS_LABELS[fromStatus] → ORDER_STATUS_LABELS[toStatus]`
- 备注（note）+ 操作人（operator）= 10px muted（层级降一级）

**空态**：`<p text-xs font-light muted>暂无状态变更记录</p>`（不隐藏区块，保持界面结构稳定）

**Alert 异常提醒**（BDS Alert 族 danger 语义）：
```
┌─ ⚠ ───────────────────────────────┐
│  此订单已超期，请尽快处理           │
└──────────────────────────────────┘
```
当 `status === 'Alert'` 时附加在时间线底部。

---

### 3.4 关联视图（Related Entities）

id=`order-detail-related`

复用 [RelatedEntitiesPanel.tsx](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/ui/RelatedEntitiesPanel.tsx)：
- 数据源：`GET /api/v1/relations/by-target?type=order&id={orderId}`（EntityLink 图谱查询）
- 渲染出 EntityLink 中与该 Order 关联的实体（类型标签 + 名称 + 跳转按钮）
- 常见关联类型：
  - `customer`（客户 Relation）
  - `supplier`（工厂 Relation）
  - `consignee`（收货方 Relation）
  - `salesperson` / `merchandiser`（内部团队 Relation）
  - 若实体关联缺失：此面板仍渲染但显示空态，提醒用户到 Relations 模块补录

#### 3.4.1 DR-005 内部面料交易关联视图（❌ 待实现）

> **决策来源**：[DR-005 FOB 的内部面料交易与合并利润](../../../../10-评审与决策/2026-08-16-设计评审决策记录.md)
> **设计真源**：[实体关系总览.md §6.4](../../../../02-数据模型/实体关系总览.md) / [订单与生产模型组.md §2B](../../../../02-数据模型/订单与生产模型组.md)

**触发条件**：`Order.isInternalFabricTrade === true` 或 `Order.internalFabricOrderId !== null`

**服装订单（Garment）视角** — 显示"内部面料采购"区块：

| 字段 | 数据源 | 说明 |
|------|--------|------|
| 内部面料订单号 | `Order.internalFabricOrderId → Order.code` | 跳转至面料订单详情 |
| 面料部归属 | `Order(面料).departmentId → Department.name` | 面料部部门名 |
| 内部结算价 | `OrderLine.internalTransferPrice` | 经 DR-006 审批冻结的内部结算价 |
| 内部供料单 | `Order.internalSupplyOrderId → OrderInternalTransfer` | DR-033 内部供料单状态 |
| 面料到货进度 | `Shipment(面料订单).status + ShipmentLine.quantity` | 分批到货回写 |

**面料订单（Fabric, isInternalFabricTrade=true）视角** — 显示"内部面料销售"区块：

| 字段 | 数据源 | 说明 |
|------|--------|------|
| 关联服装订单号 | `Order.internalFabricOrderId → Order.code`（反向） | 跳转至服装订单详情 |
| 服装部归属 | `Order(服装).departmentId → Department.name` | 服装部部门名 |
| 内部销售收入 | `OrderLine.internalSettlementPrice × quantity` | 面料部内部收入 |
| 内部供料状态 | `OrderInternalTransfer.direction=incoming` | 接收方状态 |
| 出运回写 | `Shipment.status → 服装订单到货回写` | 出运关联内部供料分配 |

**交互规范**：
- 内部面料交易关联区块使用 `bds-surface`（raisedCard）+ 专属 tint 色（区别于外部关联）
- 区块标题："内部面料交易（DR-005）"，含信息图标 hover 显示决策摘要
- 点击内部面料订单号/服装订单号 → 携带 `from=internal_trade` 参数跳转对应详情页
- 利润分析 tab 中追加"部门利润"与"合并抵销"双视角切换（见 [订单利润表生成.md §2.3](../../05-财务与结算/订单利润表生成.md)）

> **前端实现缺口**：[OrderManager.tsx](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/OrderManager.tsx) 当前未渲染内部面料交易关联区块；[OrderContextSection.tsx](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/order/OrderContextSection.tsx) 的 360° 全链路未包含内部交易节点。

---

### 3.5 订单 360° 全链路（Order Context / Fulfillment Chain）

id=`order-detail-context`

复用 [OrderContextSection.tsx](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/order/OrderContextSection.tsx#L22-L28)

**数据源**：`GET /api/v1/orders/:id/context`（后端聚合查询：`server/src/orders/query.ts` 的 `getOrderContext`，直接查表 10 实体，不依赖 EntityLink，与关联视图互补双轨）

**10 阶段卡布局（横向标签 + 垂直 stage 列）：**

| # | Stage 标签 | Icon | 视图跳转 | 查询实体 | 卡片字段 |
|---|-----------|------|----------|---------|---------|
| 1 | 报价 | FileText | `quotation` | Quotation（where convertedOrderId=orderId） | quotationNumber + status + currency + totalAmount + issueDate |
| 2 | 开发 | FlaskConical | `development` | DevelopmentCase（where linkedOrderId=orderId） | code + name + type + stage + currentRound |
| 3 | BOM | ClipboardList | `bom` | BOM（where orderId） | bomNumber + status + version + totalCost + currency |
| 4 | 采购 | ShoppingCart | `procurement` | PurchaseOrder（where orderId） | poNumber + status + supplierName + currency + totalAmount + 期望/实际到货 |
| 5 | 生产 | Factory | 无（留在详情） | ProductionStage（where orderId） | stageKey + stageSeq + status + startedAt + doneAt |
| 6 | 生产 QC | ClipboardCheck | `qc-workbench` | InspectionReport（where orderId） | inspectionType + result + aqlLevel + 疵点数量 |
| 7 | 外协 | Scissors | `production` → 外协 tab | OutsourcingOrder（where orderId） | orderNumber + processType + status + qty + 交付 + 合格率/拒收数 |
| 8 | 出运 | Ship | `shipping` | Shipment（where orderId） | shipmentNumber + status + shippingMethod + ETD/ATD/ETA/ATA + 装卸港 |
| 9 | 发票/核销 | Receipt | `finance` → 发票 | Invoice（where orderId） | invoiceNumber + type + status + amount + currency + issueDate + dueDate + settlementDate |
| 10 | 财务凭证 | Wallet | `finance` → 凭证 | PaymentVoucher（where orderId） | voucherNumber + type + status + amount + currency + paymentDate + invoiceId |

**阅读规则**：
- 空阶段不隐藏 → 显示"未启动"斜体灰态（让用户看到完整生命周期）
- header meta 数字：`N/10 阶段已启动`（count > 0 的阶段数）
- 卡片点击：若 `onNavigate` 传入则跳转到对应 view（chevron-right 图标）；若未传入则禁用态（pointer-default + 无 chevron）
- loading：`flex items-center gap-2 muted` + Loader2 spin（fetch 中）
- error：`bds-alert danger` 提示网络错误，提供 retry（通过 `refreshKey` prop 触发重新拉取）

---

### 3.6 订单变更历史（Audit History）

id=`order-detail-audit`

复用 [AuditHistorySection.tsx](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/ui/AuditHistorySection.tsx)：
- 数据源：`AuditLog` 表（`targetType='Order' AND targetId={orderId}`）
- 展示：`action → 操作类型 + 操作人 + 时间 + 变更前后值 diff（若有 beforeValue/afterValue）`
- 模块级权限门禁：读权限校验 → 非订单权限角色显示 403 灰块（不隐藏面板，保持视觉骨架一致）

---

### 3.7 生产管线（10 阶段门禁引擎）

id=`order-detail-pipeline`

复用 `<ProductionPipeline orderId={orderId} isDarkMode />`

**10 阶段清单**（PRD §19.7 生产跟单表格 + P0 QC 门禁）：

| 阶段 Key | 名称 | 前置条件 |
|----------|------|---------|
| order_placed | 业务下单 | 创建订单即进入 |
| materials_confirmed | 面辅料确认 | BOM 中 fabric 项确认到场 + SupplierRelation 绑定 |
| production_planned | 生产计划 | 填写 completionDate + 对应 ProductionStage.startedAt |
| in_production | 货期管理 | 生产交期 > now → 进入倒计时 |
| materials_arrived | 面辅料到厂 | PurchaseOrder.status=Received（或手动确认） |
| pre_cut_checked | 裁剪前检查 | **PreCutChecklist 全部通过**（QC 门禁 1；未通过禁止 → 后续阶段灰） |
| pp_sample_approved | 产前样确认 | SampleNode.level=pp 且 status=approved |
| manufacturing | 生产过程 | 工序顺序推进（cut/wash/sew/embroidery） |
| final_review | 成品确认 | Final AQL 验货 InspectionReport.result=pass（QC 门禁 2） |
| qc_shipped | 验货发货 | Ship 单据 CI/PL 已制 → Shipment.status=Booked |

**每个阶段卡片**：状态徽章 + 开始/完成日期 + 操作人 + 跳转对应模块（QC/BOM/Sample/PO/Ship）

---

### 3.8 行项目详情 / 行明细表

**动态二选一**（基于 `selectedLineItem` 是否存在）：

#### A. 单项目详情（`selectedLineItem` 选中时）

id=`order-detail-line`

16 个字段 4×4 grid（查阅态 档案配方；编辑态 bds-input）：

| # | 英文字段名 | 中文标签 | 类型 | 备注 |
|---|-----------|---------|------|------|
| 1 | itemNo | PO Item | text | 0010/0020 递增修订号 |
| 2 | materialCode | 客供品号 | text | 从客户 PO 带出 |
| 3 | millQuality | 工厂品色号 | text | 对应产品库 fabricCode |
| 4 | description | 描述/颜色 | text | 自由描述 + 颜色说明 |
| 5 | cloth | 成分 | text | 与 order.fabricContent 联动（但 line 可覆盖） |
| 6 | width | 门幅 | text | 例 150 CM |
| 7 | weight | 克重 | text | 例 180 GSM |
| 8 | exMillDate | 出厂日期 | date | 归一化 YYYY-MM-DD |
| 9 | deliveryDate | 到港日期 | date | 交期字段 |
| 10 | quantity | 数量 | number | spinner 隐藏（FIELD_NO_SPINNER_CLASS） |
| 11 | unitPrice | 单价 | number | 2 位 |
| 12 | netValue | 小计 | number | = quantity × unitPrice（自动联动） |
| 13 | status | 状态 | text | 行独立状态（默认 Pending） |
| 14 | shippingDate | 实际发货日 | date | 手动录入 |
| 15 | invoiceNumber | 发票号 | text | 关联财务 Invoice |
| 16 | actualPaymentDate | 收款日 | date | 手动录入 |

**查阅态档案排版配方**：
- 字段外层 `FIELD_SLOT_EMPTY_CLASS` / `FIELD_SLOT_FILLED_CLASS`（玻璃胶囊 + 深浅 tint 区分）
- 值：`FIELD_READONLY_VALUE_CLASS`（12px 字重，与字段组 cluster 同风格）
- 空值显示 `—`，不显示空白（防布局抖动）

#### B. 行明细表（`!selectedLineItem` 且 `order.lines.length > 0` 时）

id=`order-detail-lines`

复用 `<OrderLinesTable lines currency />`：
- 字段：Line # / PO Item / 品色号 / 描述 / 数量 / 单价 / 小计 / 出厂 / 状态
- 风格：raisedCard + 表格头 sticky
- 行可点击 → 切回「单项目详情」视图（即设置 `selectedLineItem = line`）

---

### 3.9 分簇字段区（13 Cluster，字典驱动）

**所有 cluster 渲染逻辑共用单一真源**：[orderSchema.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/lib/orderSchema.ts#L78-L92) `ORDER_CLUSTERS` + `ORDER_FIELDS`

**cluster 在详情页的渲染顺序（detailOrder）：**

| detailOrder | cluster.id | labelZh | 适用类型 | 字段数（估算） |
|-------------|------------|---------|---------|--------------|
| 1 | basic | 基础档案 | all | 12 字段（订单号 PO# / PO Item / PO 日期 / 季节 / 生产批次 / 品色号 / 客户编码 / 参考批次 / 品名规格 / 数量 / 单位） |
| 2 | parties | 买卖四方 | all | 5 子组 × ~4 字段 = 22（客户/工厂/收货/结款/内部团队） |
| 3 | delivery | 交期与物流 | all | 5 字段（生产交期/出厂交期/客户验厂/实际发货日/运输方式） |
| 4 | fabric | 面料规格 | 仅 fabric | 4 字段（面料编号/成份/门幅 GSM/克重 GSM） |
| 4 | materials | 物料信息 | 仅 other | 2 字段（分类/规格备注） |
| 5 | sales | 销售与收汇 | all | 11 字段（早/终合同号/单价/合同金额/付款方式/条件/币种/预计付款日/实际付款日/实际金额/…） |
| 6 | shipment | 出货与发票 | all | 8 字段（发票号/日期/发货日期/方式/出货数量/出货金额…） |
| 7 | sampleShipment | 大货船样 | fabric+garment | 4 字段（寄出日/确认日/快递单号/备注） |
| 8 | sampleFabric | 匹头样 | fabric | 5 字段（寄出日/确认日/快递单号/付费样数量…） |
| 9 | purchase | 采购与供应商 | all | 6 字段（采购单价/付款日/供应商发票号/日期/金额…） |
| 10 | instructions | 特别说明 | all | 1+ 字段（specialInstructions + ocDays 等） |
| 11 | garmentProduction | 成衣生产跟单 | garment | 5 类 JSON 字段（尺码分配 sizeBreakdown / 生产工序 productionSteps / 款号 styleNo / 色号 colorName / BOM 物料 bomItems） |
| 12 | sampleGarment | 成衣样衣阶段 | garment | 1 JSON（garmentSampleStages：Proto→Photo→Size Set→PP Sample→Top 每阶段 状态+寄出+确认+备注） |

**cluster 渲染共性**：
- 每个 cluster = 一个独立的 raisedCard 面板 + 自己的 OrderSectionHeader（kicker 英文 + 中文主标题 + cluster 专属 iconKey）
- 阅读态 4-col grid / 编辑态 compact（gap-3）
- 日期键（含 Date 字样字段）查阅态统一走 `formatYmd` → `YYYY-MM-DD` 格式
- 关系 FK 字段：阅读态显示快照名；编辑态替换为 `RelationCombobox`（从 relations 池内选择；选择后自动触发 `autoFillFrom` 字段联动填充地址/电话等）
- **编辑态联动**：每次 cluster onChange → 跑 `applyAmountLinkage`（数量 × 单价 → 小计 → 合同金额），防计算错误

**买卖四方 cluster 的特殊子组结构**（cluster=parties）：
- 5 子组横向分组（但不使用彩色竖条，RDL 中性仅靠文字层级）：
  - 客户 Customer
  - 面料工厂 Mill（Supplier）
  - 收货方 Consignee（服装厂/物流目的地）
  - 结款方 Bill-to（服装厂 or 代理；附 `billToIsAgent` boolean 开关）
  - 内部团队 Internal（销售 Sales / 跟单 Merchandiser / 主管 Supervisor）

---

## 四、模式切换（查阅 ↔ 编辑）

### 4.1 触发入口

Hero 顶部右上角：
- **查阅模式** → 显示「编辑项目」（bds-btn-secondary）
- **编辑模式** → 显示「保存修改」（bds-btn-primary），且 Hero 右侧追加 meta `编辑模式` badge

### 4.2 编辑态字段范围

**以下区域进入编辑态可改**：
1. ✅ **分簇字段区 13 cluster**（所有字段，包括买卖四方的 Relation 下拉选择 + autoFillFrom 联动）
2. ✅ **行项目详情 16 字段**（如果选中了 line）
3. ⛔ **概览四卡**（仅展示，不改；因为概览数据来自订单和行的联动值，修改分簇字段后保存会回写到概览）
4. ⛔ **履约动作 3 按钮**（隐藏——避免编辑时误触发下游 prime）
5. ⛔ **状态步骤条**（禁用 `isEditing`；先保存再推进状态，避免半中间状态污染推进记录）
6. ⛔ **下游面板（关联/360/审计/管线/明细表）**（继续只读——保存成功后重新拉取）
7. ⛔ **底部归档按钮**（保持启用——编辑中途可以放弃归档）

### 4.3 保存动作

`handleSaveEdit` → `apiService.updateOrder(id, patch, linePatch)`：
- 合并 editForm（Order 级 patch）+ editLineForm（Line 级 patch）
- 成功后：`setIsEditing(false)` + 重新拉取 order + refreshKey 递增（触发 OrderContextSection 重新拉取）
- 失败：`bds-toast danger` 显示后端错误，**不退出编辑模式**（保留用户输入防止丢稿）

---

## 五、归档操作（底部销毁性动作）

### 5.1 入口

Footer 右下角单一销毁按钮 `Trash2`（Ghost Icon 款，无文字），与 Hero 保存/编辑在页面两端（物理安全距离 + 独立渐变遮挡层）

### 5.2 二次确认

弹窗（使用 BDS ConfirmModal）：
- 标题：归档此订单？
- 说明：归档后订单从默认列表移除，但可通过「显示已归档」筛选找回；已归档订单不能推进状态或编辑
- 操作：取消 / 确认归档（确认按钮 danger 语义）

### 5.3 后端动作

`DELETE /api/v1/orders/:id`（soft-delete：设置 `deletedAt = now()`）

**后端保护**（schema.prisma 或路由实现）：
- 若订单有 ProductionStage.in_progress（状态进行中）→ 409 禁止归档
- 若订单关联 Shipment.status in [Booked, Loading, Shipped] → 409 禁止归档
- 否则允许归档 + 写入 AuditLog（`action: archive_order`）

---

## 六、订单状态机（前端步骤条 + 后端契约双校验）

### 6.1 前端限制（交互级拦截）

`ORDER_ALLOWED_TRANSITIONS` → 点击合法态按钮高亮；非法态按钮视觉禁用（不可点击）

### 6.2 后端契约（强校验）

后端路由 `POST /api/v1/orders/:id/transition`：
1. 查询 Order 当前态（DB 值为真源）
2. 若 `ORDER_ALLOWED_TRANSITIONS[currentStatus]` 不含 targetStatus → 409 `状态变更非法：${current} → ${target}`
3. 若 targetStatus === currentStatus → 200 空响应（幂等，不重写 transition 记录）
4. 否则写入 `OrderStatusTransition`（id / orderId / fromStatus / toStatus / note / operator / createdAt）
5. 同步更新 Order.status + Order.updatedAt
6. 写入 AuditLog（`action: transition_order_status`，记录 before/after 值）

### 6.3 10 阶段门禁联动（业务规则 §9.3）

状态推进到 `Production` 前：
- 后端校验：查询 PreCutChecklist 对应 orderId → **必须所有条目已通过 = true**；否则 409 `裁剪前检查未全部通过，禁止进入生产阶段`

状态推进到 `Shipping` 前：
- 后端校验：对应订单最近一条 Final Inspection（type = 'Final'）必须 result = 'pass'
- 否则 409 `最终验货未通过或未执行，禁止进入出运阶段`

（注：前端步骤条视觉上仍高亮"下一步可操作"，但真实后端契约强校验——前端体验上点击推进 → 出现 bds-toast danger 提示具体缺口 → 用户到对应模块补全后可重试）

---

## 七、数据模型与字段全量清单

### 7.1 Prisma Order 模型（80+ 字段分组说明）

**核心主键**：
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 内部 UUID（系统真源） |
| code | String? @unique | 业务编号（SO-YYYYMM-NNNN），PRD 5.6 序列规则，与 id 独立（可在 UI 展示给用户） |

**基本通用字段**：customer, product, type (Fabric/Garment/Other), quantity, status (Pending/Confirmed/.../Alert), dueDate, quoteAmount, source, importedAt, fieldSources JSON, createdAt, updatedAt, deletedAt

**ROLE 快照 + 6 FK**（customer/mill/consignee/billTo/salesPerson/merchandiser/supervisor + 每角色 × 4 个字段（name/address/contact/phone/R FK id），合计 6×4=24 字段）

**Phase 2 合同**：salesContractNumber, finalContractNumber

**基础档案**：productionBatch, productColorCode, clientCode, referenceBatch, productionDate, clientDate, fabricCode, fabricContent, width, gsm, asPerson

**销售/收汇**：salesPrice, contractAmount, paymentInstrument(T/T L/C O/A…), paymentTerms, salesCurrency(USD/CNY/EUR/HKD), expectedPaymentDate, actualPaymentDate, actualPaymentAmount

**出货与发票**：invoiceNumber, invoiceDate, shipmentDate, shipmentMethod, shipmentQuantity, shipmentAmount

**样品**：sampleSent/Confirmed/TrackingNumber + ShipmentSampleComments + fabricSampleSent/Confirmed/TrackingNumber + paidSampleQuantity

**客户实地验厂**：factoryVisitDate（到期前 7 天主动通知 E1）

**采购/供应商侧**：purchasePrice, purchasePaymentDate, supplierInvoiceNumber/Date/Amount

**Misc**：specialInstructions, ocDays, businessLine（fabric/garment/capsule）

**生产规则死线**：productionPlanDeadline（createdAt + 7d）, delayNoticeDeadline（dueDate - 15d）

**行级权限（§9.6 矩阵基础字段）**：ownerId (userId → Sales 仅看自己的单) + departmentId（同部门互相可见）

### 7.2 Prisma OrderLine 模型（行级字段 + 成衣 JSON 扩展）

**基础行字段**：id, orderId(FK cascade delete), lineNumber, itemNo, materialCode, millQuality, description, width, exMillDate, deliveryDate, quantity, unit, unitPrice, netValue, via, cloth, weight, category, notes, status, productionBatch, shippingDate/Method, invoiceNumber/Date, shipmentQuantity/Amount, actualPaymentDate/Amount, specialInstructions, fieldSources JSON

**Garment 成衣专属扩展字段（Phase 2 + garmentProduction / sampleGarment cluster 用）**：
```
sizeBreakdown         Json?  // { S: 100, M: 200, L: 200, XL: 100, XXL: 50 }
productionSteps       Json?  // [ { step: "cutting", status: "done", date: "..." }, ... ]
styleNo               String? // 款号 → 关联 ProductAsset/GarmentProfile
colorName             String? // 色号名称
bomItems              Json?   // [ { type: "fabric", name: "...", qty: 1.5, unit: "meter" }, ... ]
garmentSampleStages   Json?   // [ { stage: "proto", status: "confirmed", sentDate, confirmedDate, comments }, ... ]
```

**约束**：`@@unique([orderId, itemNo])`（同一 PO 下 itemNo 不重复）

### 7.3 Prisma OrderStatusTransition 模型（时间线事件）

```
id / orderId FK / fromStatus / toStatus / note / operator / lineId / createdAt
```

**查询排序**：`orderBy: { createdAt: 'asc' }`（升序，旧→新）

---

## 八、API 端点清单

| 方法 | 路径 | 说明 | Auth |
|------|------|------|------|
| GET | `/api/v1/orders?type=all/fabric/garment/other&keyword=&status=&capsule=&page=&size=` | 订单列表（带筛选、分页、模糊搜索） | JWT（requireJwtForRead=true 或本地 fallback 读） |
| GET | `/api/v1/orders/:id` | 单订单详情（含 lines） | 同上 |
| POST | `/api/v1/orders` | 创建订单（手动录入 or PDF import 预览确认 → 落库） | JWT 写 |
| PUT | `/api/v1/orders/:id` | 更新订单（编辑模式保存；支持 Order + OrderLine 同时 patch） | JWT 写 + 行级权限检查（owner/department） |
| POST | `/api/v1/orders/:id/transition` | 状态推进（双校验：合法矩阵 + QC 门禁 2 条） | JWT 写 |
| DELETE | `/api/v1/orders/:id` | 归档（soft-delete deletedAt） | JWT 写 + 生产/出运中禁止 |
| GET | `/api/v1/orders/:id/context` | 后端聚合 10 实体全链路（OrderContextSection 数据源） | JWT 读 |
| GET | `/api/v1/orders/:id/status-history` | OrderStatusTransition 列表（时间线数据源） | JWT 读 |
| POST | `/api/v1/orders/:id/set-business-line` | 设置 Garment 订单的 businessLine（大货 / Capsule） | JWT 写 |
| GET | `/api/v1/orders/export?type=&keyword=&status=` | 订单 Excel 批量导出（§17.3，端点待补） | JWT 读 + 导出权限门禁 |
| POST | `/api/v1/orders/import` | 订单 Excel 批量导入（§16，ImportJob 模型待建） | JWT 写 |

---

## 九、权限矩阵（对应 PRD §9.6 行级规则）

> ⚠️ 注意：前端已按矩阵实现「字段隐藏 + 按钮禁用」；**后端 prisma 查询层的行级过滤仍待实现**（P0 缺口事项）

### 9.1 角色

owner / admin / manager（主管）/ sales（业务员）/ merchandiser（跟单员）/ qc（质检）/ finance（财务）/ warehouse（仓库）/ guest

### 9.2 页面可见性

| 角色 | 进入订单模块 | 查看所有订单 | 仅看自己的单 | 编辑 | 状态推进 | 归档 |
|------|-------------|------------|------------|-----|---------|-----|
| owner / admin / manager | ✅ | ✅ | — | ✅ | ✅ | ✅（强制归档任意单） |
| sales | ✅ | ❌ | ✅（where ownerId=me） | ✅（仅自己的单 + Pending/Confirmed 阶段） | ✅（自己的单） | ❌ |
| merchandiser | ✅ | ❌ | ✅（where merchandiserRelationId=me 或同 departmentId） | ✅（仅 Confirmed→Production→Shipping 范围内字段） | ✅（日活最高） | ❌ |
| qc | ✅ | ❌ | ✅（同 departmentId） | ❌（仅可读取+生产管线 10 阶段 QC 条目写入） | ⚠️ 仅限 Production→Shipping（QC 门禁通过后才解锁） | ❌ |
| finance | ✅ | ✅（仅可看金额+发票相关字段） | ❌ | ⚠️ 仅可编辑出货与发票、销售与收汇（sales/shipment cluster） | ❌（不参与状态推进） | ❌ |
| warehouse | ✅ | ❌ | ✅（同 departmentId） | ⚠️ 仅可编辑出货与采购实际数量 | ❌ | ❌ |
| guest | ❌（入口隐藏） | — | — | — | — | — |

### 9.3 字段级敏感权限

- **佣金字段 Commission 相关**（订单未来接入）：**仅 owner / admin / manager / finance 可见**；sales / merchandiser / qc / warehouse 一律隐藏（前端隐藏 + 后端字段过滤）。当前前端隐藏已实现，但后端返回了全部字段（安全缺口 P0）。

---

## 十、四态规范（空态 / 加载 / 错误 / 无权限）

| 状态 | 触发 | UI 规范 |
|------|------|---------|
| **加载态（Loading）** | 首次打开或 refreshKey 变化 → fetch `/orders/:id` 和 `/orders/:id/context` 并行中 | Skeleton（Hero + 概览卡占位 + 面板玻璃 card 骨架 + shimmer 动画） |
| **空态（Empty Data）** | 订单无行 / 无关联实体 / 全链路阶段 0/10 | "未启动"灰态斜体；不隐藏结构（防抖动）；不使用大插画（遵循 RDL 中性契约） |
| **错误态（Error）** | fetch 返回 4xx / 5xx | `bds-alert danger` + 错误文案（中文：`订单加载失败，请重试`）+ Retry 按钮；错误栈不上屏（仅 console.error） |
| **无权限（No Permission）** | 后端返回 403 / 401 | `bds-alert warning` + 文案：`您无权查看此订单，请联系管理员申请权限`；不显示面板详情（避免数据泄露）；但 Detail Map 骨架保留（结构一致） |

---

## 十一、移动端 BottomSheet 操作规范

| 操作项 | 触发 | 反馈 |
|--------|------|------|
| 编辑详情 | 点击第一项 → 进入 compact density 的字段表单（与桌面详情字段同真源） | 底部 Sheet 全高（height: 'full'），顶栏有取消/保存 |
| 快速状态变更 | 横向滚动 badge（Confirmed / Production / Shipping / Delivered）4 个常用推进按钮 | 成功 → toast success；失败 → toast danger（后端返回的错误 message） |
| 归档订单 | danger tint 销毁项 | 二次确认 → 执行归档 → 关闭 Sheet + toast |

---

## 十二、业务规则 §9 关联项（已接 / 待接）

| # | 规则 ID | 接入订单详情页情况 |
|---|---------|-------------------|
| 1 | **9.2 价格审批**（双轨偏差 >15% 审批 → warn / >30% 未审批 block） | ⚠️ 详情页本身不直接显示报价的双轨偏差（在报价模块触发）；订单从报价转换时后端读取 `Quotation.priceDeviationLevel` 和 `Quotation.priceApprovalId`。详情页目前无"报价审批状态"badge（设计缺口 P0，建议在概览区右侧追加） |
| 2 | **9.3 质量门禁 3 条**（开裁/厂前样/出货） | ✅ 生产管线 10 阶段中的 pre_cut_checked（裁剪前检查）+ final_review（Final AQL）已接强校验；厂前样双签在 Sample 模块 |
| 3 | **9.4 信用控制 60 天逾期冻结**（客户逾期 >60 天禁止新下单 + 新状态推进） | ⚠️ 仅逾期看板；创建订单 + 状态推进未做客户逾期校验（P0 缺口） |
| 4 | **9.5 金额联动**（数量 × 单价 → 小计 → 合同金额） | ✅ `applyAmountLinkage`（编辑态实时联动，概览卡自动更新） |
| 5 | **9.7 订单变更规则 5 类**（数量/交期/价格/客户/取消 → 分级审批 + 留痕） | ⚠️ 订单直通无分级审批；变更前后值 diff UI 缺失；仅审计留痕有 before/after（但前端无 diff 对比视图） |
| 6 | **交期锁死规则**（出运后交期字段只读） | ⚠️ 目前 Shipping/Delivered 状态下 delivery/production 字段仍可编辑（无前端只读 + 后端契约） |
| 7 | **生产 7 天计划死线**（createdAt + 7d 必须有 ProductionStage.production_planned.startedAt） | ✅ productionPlanDeadline 字段已建；调度器 watchdog 待实现（E 批次已实现生产超期，该死线计划超期警告待补） |
| 8 | **延期通知 15 天**（dueDate - 15d 前如果未 Shipping → 必须通知客户 + 改单） | ✅ delayNoticeDeadline 字段已建；调度通知 E3 批次已实现发货延迟扫描 |
| 9 | **工厂验厂到期提醒**（factoryVisitDate - 7d → 主动通知） | ✅ E1 批次逾期通知调度已接入该字段 |
| 10 | **MOQ 最小起订量**（面料 800M / 成衣 200Pcs / Capsule 20Pcs） | ⚠️ 订单详情/录入未加 MOQ 校验；低于 MOQ 审批链未实现（P1 缺口） |
| 11 | **DR-005 内部面料交易**（FOB 服装→面料部采购，双向关联+独立核算+合并抵销） | ❌ 详情页未渲染内部面料交易关联区块（§3.4.1）；Order.isInternalFabricTrade 字段未实现；利润分析 tab 无部门利润/合并抵销双视角（P1 缺口） |

---

## 十三、可访问性 & 交互细节

| 项 | 规范 |
|----|------|
| **键盘 ESC 关闭** | 详情页任意时刻按 ESC → 关闭详情（等同于 × 按钮） |
| **Cmd/Ctrl+S 保存** | 编辑模式下按 ⌘S → 立即触发保存修改（全局 keydown 监听 + 冲突防护） |
| **Tab 顺序** | Hero 按钮（编辑/保存/×）→ Detail Map 锚点从上到下 → 右侧面板 Tab 内可聚焦字段 → Footer 归档按钮 |
| **aria-label** | 所有图标按钮含 aria-label（"关闭详情" / "编辑项目" / "归档订单" / "下一步 Pending→Confirmed"） |
| **滚动恢复** | 再次打开同一个订单 → 恢复上次滚动位置（以 orderId 作 key，存在 `orderDetailScrollPosRef` Map 里；目前未实现，建议补） |
| **双击关闭** | 点击遮罩背景空白处（非面板区域）→ 关闭详情；面板内部点击 stopPropagation 防穿透 |
| **内容变化平滑衔接** | 概览卡 + Hero 标题使用 animate presence（mount 时 opacity 0→1，切换订单时 crossfade） |
| **日期字段归一化** | 所有展示态的日期字段统一 `formatYmd(value)` → YYYY-MM-DD；UTC / 本地时差在 format 层处理（不存时间粒度，订单日期为"天"粒度） |
| **金额字段归一化** | 查阅态使用 `Number(n).toLocaleString('zh-CN', { maximumFractionDigits: 2 })`，空值显示 `—`（不显示 0 或空白） |

---

## 十四、设计系统约束（BDS RDL 中性 + 三层治理）

| 约束 | 合规说明 |
|------|---------|
| **禁止硬编码 hex 颜色** | 详情页内所有颜色通过 CSS 变量（`var(--bg-sunken) / var(--accent-tint) / var(--danger-tint)` 等），check:tokens 通过 |
| **圆角规则** | 所有 panel `rounded-card`（≈22px）；字段胶囊 `rounded-inset`；按钮 `rounded-control`；徽章 `rounded-compact` |
| **不使用阴影**（flat 设计） | 所有 panel 使用 `backdrop-blur + hairline`（发丝 border），不是 box-shadow；边框 `var(--border-c-subtle)` |
| **子组彩色竖条** | 买卖四方 cluster 已废除彩色竖条（RDL 中性契约），仅靠文字层级 + kicker 分组 |
| **玻璃材质（雾化 tint）** | raisedCard 表面默认 `background: rgba(255,255,255,0.72) + saturate + blur`（`var(--panel-tint)`） |
| **Hero 大数字排版** | 概览卡 26px font-extralight + tabular-nums（tabular 对齐，防止数字跳动） |
| **Kicker 英文小标题** | 统一 `uppercase tracking-[0.22em] 10px font-light muted`（与 BDS v2.1 Luna 字体字重 ≤300 一致） |
| **toast/tooltip 非转色纪律** | 该页无自定义 toast/tooltip 颜色（全部走 bds-toast / sl-tooltip 默认），不会被 check:tokens 拦截 |
| **空态插画禁止** | 全线遵循 RDL 中性契约，不使用插画/吉祥物/大图标（仅文字 + line-height 小 Lucide 图标） |

---

## 十五、待补的设计缺口（建议后续跟进）

| # | 缺口 | 优先级 | 建议在本 md 的哪个部分补充细化设计 |
|---|------|-------|--------------------------------|
| 1 | **报价审批状态 badge**（在概览卡右侧追加「报价审批：已通过 / 待审批 / 已驳回」） | P0 | §3.1 概览四卡的"第五卡"设计（与 DeviationBadge 同构） |
| 2 | **订单变更前后 diff UI**（AuditHistorySection 展开每条 action 显示 beforeValue→afterValue 对比，当前仅文本展示） | P0 | §3.6 审计面板的"diff 展开视图" |
| 3 | **滚动位置恢复**（按 orderId 记忆上次滚动锚点） | P1 | §13 可访问性新增条 |
| 4 | **交期锁死只读**（Shipping/Delivered 阶段 delivery/production/cluster 字段视觉锁定 + 后端契约） | P0 | §4.2 编辑态字段范围 + §十二业务规则 |
| 5 | **客户逾期 60 天新单冻结门禁**（创建订单 + Pending→Confirmed 推进时检查 `AccountReceivable.dueAge>60`） | P0 | §6.2 后端状态推进契约 + §十二 |
| 6 | **MOQ 校验 + 低于 MOQ 审批触发**（创建/保存时校验 quantity < MOQ → 弹审批表单） | P1 | §4.3 保存动作 + §十二 |
| 7 | **订单行级详情（成衣 garmentProduction 的 JSON 字段）独立 UI 组件**（目前是裸 JSON Editor 视觉上不太友好） | P1 | §3.9 cluster=garmentProduction 的可视化（尺码分布柱状图、生产工序泳道图、BOM 树表、样衣阶段卡片） |
| 8 | **样品节点（面料船样/匹头样 FabricShipmentSample 模型）** 独立 UI 卡片（目前 sample* 字段在字段簇裸字段） | P1 | 新增 §3.10 样品卡区块 + 后端模型补齐 |

---

## 十六、相关文档索引

| 文档 | 路径 |
|------|------|
| 订单列表页设计（本目录） | `./订单列表页.md`（待创建） |
| 订单录入表单设计 | `./订单录入表单.md`（待创建） |
| 订单类型分层方案 | `./订单类型分层.md`（待创建，当前在 `.trae/documents/order-type-field-layering-plan.md`） |
| 生产管线设计 | `./生产管线.md`（待创建） |
| 订单 360° 全链路设计 | `./订单360全链路.md`（待创建） |
| 字段词典真源（当前代码真源） | [orderSchema.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/lib/orderSchema.ts) |
| 订单状态机定义 | [OrderManager.tsx ORDER_ALLOWED_TRANSITIONS](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/OrderManager.tsx#L249-L256) |
| 订单聚合后端查询 | [orders/query.ts getOrderContext](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/orders/query.ts#L75-L168) |
