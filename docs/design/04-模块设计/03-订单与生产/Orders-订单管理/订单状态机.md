# 订单状态机 (Order State Machine)

## §1 元信息

| 项 | 值 |
|---|---|
| **定位** | 订单生命周期的 6 态有限状态机——合法推进矩阵 + QC 门禁强校验 + 前后端双轨校验 + 时间线留痕 |
| **入口** | 详情页生产进度时间线 6 态步骤条（点击可推进态按钮）/ 移动端快速状态变更 badge |
| **核心角色** | 业务员（含跟单，主推进者）、QC（门禁解锁，不细分）、销售主管（部门管理职责） |
| **范式** | 状态机文档（合法矩阵 + 门禁规则 + 双校验契约） |
| **优先级** | P0 |
| **实现状态** | ✅ 已落地（6 态枚举 + 合法矩阵 + 前后端双校验 + OrderStatusTransition 时间线 + 业务事件发布均已接入；客户逾期冻结门禁为 P0 缺口） |
| **关联 PRD 章节** | §5.5（订单状态机）、§9.3（质量门禁 3 条）、§9.4（信用控制 60 天逾期冻结） |
| **关联代码** | 后端真源 [orderLifecycleService.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/orders/orderLifecycleService.ts) `VALID_ORDER_STATUSES` / `ORDER_TRANSITIONS` / `transitionOrderStatus` / 前端镜像 [OrderManager.tsx](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/OrderManager.tsx#L239-L255) `ORDER_FLOW_STEPS` / `ORDER_ALLOWED_TRANSITIONS` |

---

## §2 6 态定义

| 状态 | 中文 | 语义 | 是否终态 |
|------|------|------|---------|
| `Pending` | 待确认 | PO 录入后初始态；需客户确认 PO | 否 |
| `Confirmed` | 已确认 | 客户确认 PO；可转生产 | 否 |
| `Production` | 生产中 | 厂内生产/加工进行中 | 否 |
| `Shipping` | 出运中 | 已发运；等待客户收货 | 否 |
| `Delivered` | 已交付 | 客户收货完成 | **是**（终态，不可变更） |
| `Alert` | 异常 | 独立异常分支；可恢复到任一非终态 | 否（可恢复） |

**枚举真源**：`VALID_ORDER_STATUSES = ['Pending', 'Confirmed', 'Production', 'Shipping', 'Delivered', 'Alert']`（6 态，不新增 Cancelled）。

**状态徽章语义**（`STATUS_BADGE_VARIANT`）：Pending=neutral / Confirmed·Production·Shipping=info / Delivered=success / Alert=warning。

---

## §3 合法推进矩阵

**后端真源** `ORDER_TRANSITIONS`（`Record<string, Set<string>>`）：

| 当前状态 (from) | 可推进到 (to) | 说明 |
|----------------|--------------|------|
| Pending | Confirmed / Alert | PO 录入后初始态；需客户确认 PO |
| Confirmed | Production / Alert | 确认后转生产（受 PreCutChecklist 门禁） |
| Production | Shipping / Alert | 厂内生产完成转等待出运（受 Final QC 门禁） |
| Shipping | Delivered / Alert | 已发运；客户收货后转交付 |
| Delivered | — | **终态**，无任何推进（空 Set） |
| Alert | Pending / Confirmed / Production / Shipping | 异常恢复；可选回到任一非终态（业务上通常回到上一态） |

**前端镜像** `ORDER_ALLOWED_TRANSITIONS`：与后端矩阵完全一致，仅做可行动性提示（按钮高亮/禁用），**唯一状态真源仍是后端**。

**步骤条主轴**（`ORDER_FLOW_STEPS`）：`['Pending', 'Confirmed', 'Production', 'Shipping', 'Delivered']` 5 步线性主轴；Alert 作为独立异常分支出现在末尾右侧。

```
Pending ──▶ Confirmed ──▶ Production ──▶ Shipping ──▶ Delivered
   │            │              │             │
   └────────────┴──────────────┴─────────────┘
                       │
                       ▼
                    ╳ Alert ╳  ──▶ 可恢复到 Pending/Confirmed/Production/Shipping
```

---

## §4 门禁规则（QC 强校验）

> 门禁在后端 `transitionOrderStatus` 内强校验；前端步骤条视觉高亮"下一步可操作"，但点击推进若门禁未过 → toast danger 提示具体缺口。

### 4.1 门禁 1：Confirmed → Production（裁剪前检查）

| 项 | 说明 |
|----|------|
| 校验对象 | `PreCutChecklist` where orderId |
| 通过条件 | 4 项 boolean 全为 true：gradingConfirmed(推码) + consumptionConfirmed(耗料) + patternConfirmed(样板) + preProductionMeeting(产前会议) |
| 未通过 | 409 `裁剪前检查未全部通过，禁止进入生产阶段` |
| 模型 | `PreCutChecklist` `orderId @unique`（一单一表） |

### 4.2 门禁 2：Production → Shipping（最终验货）

| 项 | 说明 |
|----|------|
| 校验对象 | `InspectionReport` where orderId AND inspectionType='final' |
| 通过条件 | `result = 'pass'` |
| 未通过 | 409 `最终验货未通过或未执行，禁止进入出运阶段` |
| 模型 | `InspectionReport` `@@unique([orderId, inspectionType])`（每单 final + midline 各一份） |

### 4.3 门禁 3：产前样双签（在 Sample 模块，非订单状态机内）

| 项 | 说明 |
|----|------|
| 校验对象 | `SampleNode` where developmentCase.linkedOrderId = orderId AND level='pp' |
| 通过条件 | status='approved' + ProductionStage.pp_sample_approved 双签（signedByProduction + signedByBusiness） |
| 位置 | 生产管线 10 阶段的 `pp_sample_approved` 阶段，非订单状态机直接校验 |

### 4.4 待实现门禁（P0 缺口）

| 门禁 | 规则 | 状态 |
|------|------|------|
| 客户逾期 60 天冻结 | `AccountReceivable.dueAge > 60` → 禁止新下单 + 禁止 Pending→Confirmed 推进 | ⚠️ 未实现 |
| 交期锁死 | Shipping/Delivered 阶段 delivery/production 字段只读 | ⚠️ 未实现 |

---

## §5 前端步骤条（交互级拦截）

### 5.1 按钮四态

| 类型 | 规格 | 视觉 | 触发条件 |
|------|------|------|---------|
| `STEP_BTN_DONE` | `bds-btn bds-btn-outline` | 勾选 ✓ + 弱强调 | 已路过的步骤 |
| `STEP_BTN_CURRENT` | `bds-btn bds-btn-primary` | Raised 主按钮，强聚焦 | 当前态 |
| `STEP_BTN_ACTIONABLE` | `bds-btn bds-btn-outline` | 悬停强调（唯一可点击） | 下一可推进态 |
| `STEP_BTN_DISABLED` | `bds-btn bds-btn-ghost` | 禁用灰态 + 无 hover | 未达步骤 |
| Alert 进行中 | `!min-w-[96px]` + warning tint | AlertTriangle 图标 | status === 'Alert' |

### 5.2 连接器规则

- 步骤间 `h-px w-4` 发丝线
- Done 段：实色 connector（`STEP_CONNECTOR_DONE_STYLE`，accent 45%）
- Pending 段：低饱和 ghost 色（`STEP_CONNECTOR_PENDING_CLASS`）

### 5.3 点击推进

`handleStatusTransition(toStatus, note?)` → `apiService.transitionOrderStatus(orderId, toStatus, operator, note)`：
- 成功 → 更新 `orders` state + `onSelectOrder(updated)` + 刷新时间线
- 失败 → `window.alert` 显示后端错误 message（"状态推进失败：…"）

---

## §6 后端契约（强校验真源）

`POST /api/v1/orders/:id/transition` → `transitionOrderStatus(params)`：

```
1. 枚举校验: toStatus ∈ VALID_ORDER_STATUSES
   └─ 否 → { code: 'INVALID_STATUS', 400 }

2. 查询 Order（含 deletedAt 校验）
   └─ 不存在/已删 → { code: 'ORDER_NOT_FOUND', 404 }

3. 幂等校验: existing.status === toStatus
   └─ 是 → { code: 'NO_CHANGE', 400 }（前端视为成功，不重写 transition）

4. 合法矩阵校验: ORDER_TRANSITIONS[fromStatus].has(toStatus)
   └─ 否 → { code: 'INVALID_TRANSITION', 400 }

5. （事务内）
   a. Order.update status + updatedAt
   b. OrderStatusTransition.create（id/fromStatus/toStatus/note/operator/lineId/createdAt）
   c. syncOrderEntityReferences（更新 orderedBy/suppliedBy active link）
   d. writeRouteAuditLog（operation: transition_order_status，before/after status）

6. 事务提交后（fire-and-forget，永不阻断业务）
   a. publishBusinessEvent OrderStatusChanged（所有变更都发）
   b. 若 Pending→Confirmed → 额外发 OrderConfirmed（触发生产单创建联动）
```

**错误码**：`ORDER_NOT_FOUND` / `ORDER_ALREADY_DELETED` / `INVALID_STATUS` / `INVALID_TRANSITION` / `NO_CHANGE` / `TRANSITION_FAILED`。

---

## §7 双校验契约

| 层 | 职责 | 失败行为 |
|----|------|---------|
| **前端** `ORDER_ALLOWED_TRANSITIONS` | 交互级拦截——非法态按钮视觉禁用（不可点击） | 用户无法点击非法推进 |
| **后端** `ORDER_TRANSITIONS` | 强校验真源——事务内合法性 + 门禁 + 审计留痕 | 返回错误码 + 不写入任何数据 |

**设计原则**：前端镜像仅做可行动性提示，**唯一状态真源是后端**。即使前端被绕过（直接调 API），后端事务校验仍拦截。

---

## §8 OrderStatusTransition 时间线

每次合法推进写入一条 `OrderStatusTransition` 记录：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | `OST_${ts}_${random}` |
| orderId | String | FK → Order（Cascade） |
| fromStatus | String | 推进前状态 |
| toStatus | String | 推进后状态 |
| note | String? | 推进备注（如"客户签字回传 PO"） |
| operator | String? | 操作人（'desktop-user' / actorId / 'api'） |
| lineId | String? | 行级推进标记（可选） |
| createdAt | BigInt | epoch ms |

**查询**：`GET /api/v1/orders/:id/status-history` → `orderBy: { createdAt: 'asc' }`（升序，旧→新）。

**详情页时间线渲染**：圆点（最新=accent 点亮，其余=灰）+ `[最新]` badge + 中文化状态名 + 备注 + 操作人。

---

## §9 业务事件发布

事务提交后 fire-and-forget 发布（失败不影响业务）：

| 事件 | 触发 | payload | 用途 |
|------|------|---------|------|
| `OrderStatusChanged` | 所有状态变更 | poNumber / fromStatus / toStatus / transitionId | 通用状态变更通知 |
| `OrderConfirmed` | Pending→Confirmed（且 fromStatus !== Confirmed） | poNumber / fromStatus / customer / transitionId | 触发生产单创建联动（Phase 1 Sprint 3） |

---

## §10 状态推进权限

| 角色 | 推进权限 | 限制 |
|------|---------|------|
| owner/admin/manager | ✅ 全部 | — |
| sales | ✅ 仅自己的单 | ownerId=me |
| merchandiser | ✅ 全部可推进（日活最高） | 同部门 |
| qc | ⚠️ 仅 Production→Shipping | QC 门禁通过后才解锁 |
| finance | ❌ | 不参与状态推进 |
| warehouse | ❌ | — |

---

## §11 Alert 异常态处理

| 项 | 说明 |
|----|------|
| 进入 | 任意非终态 → Alert（标记异常） |
| 恢复 | Alert → Pending/Confirmed/Production/Shipping（业务上通常回到上一态） |
| 视觉 | 步骤条末尾右侧 AlertTriangle 图标 + warning tint；时间线底部追加 Alert 提醒卡 |
| 典型场景 | 超期、质量异常、客户纠纷、生产中断 |
| 详情页 Alert 卡 | `bds-alert danger` "此订单已超期，请尽快处理"（status === 'Alert' 时附加在时间线底部） |

---

## §12 软删归档（deleteOrder）

`DELETE /api/v1/orders/:id` → `deleteOrder(params)`：

```
1. 查询 Order
   └─ 不存在 → ORDER_NOT_FOUND 404
   └─ 已删 → ORDER_ALREADY_DELETED 409

2. 关联保护校验（任一存在则禁止）
   ├─ Invoice.count(orderId, deletedAt=null) > 0
   ├─ Shipment.count(orderId, deletedAt=null) > 0
   └─ PaymentVoucher.count(orderId, deletedAt=null) > 0
   └─ 有 → HAS_DEPENDENTS 400

3. （事务内）
   a. Order.update deletedAt = now + updatedAt
   b. deactivateEntityLinks('order', orderId)（order 发出的所有 active link 置 inactive）
   c. writeRouteAuditLog（operation: delete_order）
```

**注**：归档后订单从默认列表移除（`deletedAt: null` 过滤），可通过"显示已归档"筛选找回；已归档订单不能推进状态或编辑。

---

## §13 待补缺口

| # | 缺口 | 优先级 | 落点 |
|---|------|-------|------|
| 1 | 客户逾期 60 天冻结门禁（Pending→Confirmed） | P0 | §4.4 |
| 2 | 交期锁死只读（Shipping/Delivered 阶段） | P0 | §4.4 |
| 3 | 门禁失败的前端 toast 细化（当前用 window.alert） | P1 | §5.3 |
| 4 | 状态推进的分级审批（PRD §9.7 变更规则 5 类） | P1 | §6 |

---

## §14 订单状态机 × DR-008（多轮样品双门禁）/DR-009（批准与放行分离）/DR-010（变更取消控制）验收场景

> **决策来源**：[DR-008](../../../../10-评审与决策/2026-08-16-设计评审决策记录.md#dr-008-高端服装多轮样品与双门禁) / [DR-009](../../../../10-评审与决策/2026-08-16-设计评审决策记录.md#dr-009-商业订单批准与生产放行分离) / [DR-010](../../../../10-评审与决策/2026-08-16-设计评审决策记录.md#dr-010-已批准订单的变更与取消控制)

### 14.1 商业订单批准 × 生产放行 × 实际开裁 三段分离（DR-009）与 6 态推进验收

| 验收编号 | 场景描述 | 前置条件 | 执行步骤 | 期望结果（6 态推进正确，fail-closed） | 失败路径 |
|---------|---------|---------|---------|-------------------------------------|---------|
| OSM-009-A1 | Pending→Confirmed（商业订单批准）：客户合同/PO+交期+销售主管或总领导审批，定金**不作为**统一采购门禁 | 1. Order.status=Pending；2. 已上传客户确认依据（PO 文件或销售合同）+ 交期确认；3. 销售主管已批准（或 DR-007 兜底）；4. 定金未到账 | 1. 审批通过后推进 Confirmed；2. 同时检查采购模块是否可启动 | 1. Order.status=Confirmed；2. OrderStatusTransition 记录（from=Pending→to=Confirmed + 审批人 + 确认依据快照 id + 交期快照）；3. 采购模块可启动面料采购（非紧急例外，是标准流程，DR-009 采购先行）；4. 定金字段（如有）仅显示「到账日期=未到」，不阻断 Confirmed 推进 | 缺失客户确认依据但有定金 → 仍被 fail-closed 拦截；定金未到账被错误阻塞 → 违反 DR-009 |
| OSM-009-A2 | Confirmed→Production（生产放行）：内部 QC 通过 + 业务员登记客户确认 + 产前样封存（三条件）+ 销售主管/总领导手动放行；**不以面料实际到位为前提** | 1. 订单 Confirmed，面料采购已发起但尚未全部到货；2. 最近一轮产前样：QC 内部评审 Pass（InspectionReport type=Sample，status=Pass）；3. 业务员登记客户 PP 确认（结果=Approved + 日期 + 渠道 + 证据）；4. 产前样封存（SealedSample.sealedAt/sealedBy）；5. 销售主管点「生产放行」 | 1. 后端校验 3 门禁；2. 销售主管放行确认 | 1. Order.status=Production；2. 3 门禁校验结果写入 OrderStatusTransition.payload（qcId、customerConfirmedBy、sealedId）；3. 封存产前样不可修改（writeOnce 守卫，SealedSample.version 不得被后续覆盖） | 任一缺失 → 不可推进 Production；误把「面料已全部入库」作为前置 → 违反 DR-009 生产放行与实际开裁分离原则 |
| OSM-009-A3 | Production 状态后实际开裁的触发（生产阶段推进到「开裁」）：面料实际可用 + 排产完成（区别于订单级 Production 放行） | 1. Order.status=Production，生产阶段当前 pre_cut_pending；2. 面料实际入库已满足（按 BOM 行级入库≥要裁剪数）；3. 工厂排产表已确认 | 1. 推进 ProductionStage 到 pre_cut_checked → cutting；2. 触发实际开裁开始 | 1. 生产阶段推进成功；2. Order.status 仍保持 Production（不直接跳到 Shipping）；3. 面料锁定（InventoryLock 或对应字段）防止挪用；4. 区分「生产放行确认人」与「实际开裁执行责任人」两条独立留痕 | Production（订单级放行）阶段后自动开工 → 违反 DR-009（放行与实际开工必须分离）；面料未实际到却能开裁 → 数据真实度错误 |
| OSM-009-A4 | Production→Shipping（最终出运）：大货 QC 通过 + 订单所属销售主管/总领导手动批准出运（样品 QC 与出运 QC 是两道独立门禁，不可替代） | 1. Order.status=Production；2. 产前样 QC 通过（早于生产）但大货终期 InspectionReport（type=Final，status=Fail，critical=2）；3. 业务员尝试推进 Shipping | 1. 推进 Shipping 请求 | 1. 后端 fail-closed：FINAL_QC_FAILED；2. 显示终期报告链接（疵点详情）；3. 提供「DR-013 例外申请」入口 | 仅以产前样 QC 通过就跳到 Shipping → 违反 DR-009（样品与大货出运是两道独立不可替代门禁） |
| OSM-009-A5 | 终期 QC 通过后仍需销售主管/总领导单独出运批准（非 QC 自动推进） | 1. 终期 InspectionReport status=Pass；2. 业务准备提单/发票；3. 销售主管未批出运 | 1. QC 提交 Pass 报告 | 1. QC Pass 事件发布后，订单状态仍保持 Production（或 ReadyToShip 中间态取决于阶段）；2. 出运按钮 disabled，显示「待销售主管批准出运」；3. 批准出运后才可推进 Shipping + 创建 Shipment 实际出运 | QC Pass 后自动进 Shipping → 违反 DR-009（批准动作必须是订单团队主管/总领导的独立人工操作） |
| OSM-009-A6 | 履约终点判定：记录实际贸易术语与指定地点/版本，禁止 FOB 统一默认（FOB 装运港装船凭证履约 vs 其他术语） | 1. 3 张订单：订单 A=FCA Shanghai（指定上海工厂货交承运人）；订单 B=FOB Ningbo（宁波港装船）；订单 C=DDP LA（洛杉矶完税后交货）；2. 三张 Shipment 已实际出运 ATD | 1. 业务员录入每张订单的 tradeTerm + 指定地点 + Incoterms 版本（2020/2010）；2. 对应地触发「合同履约」节点；3. 客户收货确认作为后续跟踪证据（回款/客诉） | 1. 三张订单的 tradeTerm/location/incotermsVersion 三字段完整，不可空；2. FOB 履约判定依据=装船提单日期（BL on board date）；FCA=承运人收货日期；DDP=目的地完税送达日期；3. 客户签收事件可追溯，但「合同履约」与「客户签收」「财务结清」「实际出运」分别 4 条独立记录，互不覆盖或复用同一字段 | 全部 3 张订单被系统默认 FOB 且不记录指定地点 → 严重违反 DR-009 履约终点（禁止 FOB 统一默认）；财务结清=履约判定失败 → 合同履约与财务职责混淆 |

### 14.2 DR-008 高端服装多轮样品 + 每轮先内部 QC 通过后寄客户 + 封样 writeOnce 验收

| 验收编号 | 场景描述 | 前置条件 | 执行步骤 | 期望结果（样品循环链 + 双门禁） | 失败路径 |
|---------|---------|---------|---------|-------------------------------|---------|
| OSM-008-B1 | 样品链第 1 轮产前样（PP Round 1）：先 QC 内部评审 Pass 才允许业务员登记寄客户 | 1. Garment 订单，Confirmed，处于样品阶段；2. 工厂提交 PP 样；3. QC 已分配任务 | 1. 工厂样品进入 QC 评审；2. QC 录入评审结论=Fail（major 疵点 3 处）+ 文字意见；3. 业务员尝试「登记寄客户」 | 1. 系统 fail-closed 禁止业务员登记寄送：SAMPLE_QC_NOT_PASSED；2. 此轮样品状态=QC Rejected，等待工厂重做；3. 客户意见字段=空（不允许先寄后补 QC） | 业务员跳过 QC 直接登记寄送客户 → 违反 DR-008（内部门禁：QC 通过才允许提交客户） |
| OSM-008-B2 | 第 1 轮 QC Fail → 工厂出第 2 轮样 → 第 2 轮 QC Pass → 业务员可寄客户并登记客户确认 | 1. 第 1 轮 PP QC Fail（B1）；2. 工厂重做第 2 轮样品；3. 第 2 轮 QC Pass（无 critical，major=0，minor 可接受） | 1. 业务员登记寄送（日期、快递单号、收件方）；2. 3 天后业务员登记客户确认（Approved + 日期=2026-08-20 + 渠道=WeChat + 意见=颜色 OK + 证据=上传聊天截图） | 1. SampleNode round=2 记录 QC id、qcConclusion、customerApprovedAt/By/Channel/Opinion/Evidence 七条；2. 业务员（非主管）即可登记客户确认+封存，不需要主管额外审批（DR-008）；3. 每轮目的/版本/材料配置/QC结论/客户意见/修改项/证据 全链路 7 字段模型完整；4. 轮数不设上限（可 Round N） | QC Pass 后主管审批封样才放行 → 违反 DR-008 不需要主管审批；强制限制轮数=3 自动阻断 → 样品链循环被破坏 |
| OSM-008-B3 | 封样 writeOnce：任何后续改动必须产生新样品版本（Round 3）+ 重新 QC + 重新客户确认 + 重新封存（旧封样不可变） | 1. Round 2 客户 Approved，已封存 SealedSample；2. 客户临时要求把袖口改为罗纹；3. 工厂调整后出 Round 3 样 | 1. 业务员尝试直接编辑 Round 2 的封样工艺配置=罗纹袖口；2. 后创建 Round 3 样 → QC → 客户确认 → 再次封存 | 1. 步骤 1：后端拦截 WRITE_ONCE_SEALED_SAMPLE（封样不可变）；2. 步骤 2：Round 3 SampleNode 新建；3. 新 SealedSample version=3 写入；4. 旧封样 version=2 仍保留并关联（回退或对比需要） | 直接覆盖旧封样 → 严重违反 DR-008 封样不可变；任何改动不产生新版本而修改同一 Round → 审计断裂 |
| OSM-008-B4 | 服装样品链与面料样品链独立（DR-029 强边界）：服装 PP 封样≠面料 S/S；面料 S/S 的 QC 结论不可覆盖服装产前样 QC | 1. 同客户 Garment 订单 + 其关联面料订单；2. 面料 S/S QC Pass；3. Garment PP 样尚未提交 QC | 1. 查服装订单的样品 QC 状态；2. 尝试用面料 S/S 的 QC id 作为服装生产放行的门禁依据 | 1. Garment PP QC 状态=Pending；2. 生产放行门禁拦截（缺服装 PP 的 QC Pass）；3. 两条链独立建模（SampleNode.type=GarmentPP vs FabricSS）；4. 数据模型 / 状态机 / 任务路由 / 权限校验 4 处都独立，不得用一条链复用字段兼容另一方 | 面料 S/S QC Pass 就当服装 PP QC 通过 → 违反 DR-029 强制边界（禁统一样品流程硬兼容） |

### 14.3 DR-010 已批准订单的变更/取消/暂停控制与审批验收（fail-closed）

| 验收编号 | 场景描述 | 前置条件 | 执行步骤 | 期望结果 | 失败路径 |
|---------|---------|---------|---------|---------|---------|
| OSM-010-C1 | 业务员尝试直接改已批准订单 Confirmed 的 quantity、price、deliveryDate → fail-closed 拦截（必须走 DR-010 变更审批） | 1. Order.status=Confirmed（已批准）；2. 登录=业务员（DR-041 容器）；3. 直接在编辑页面改数量 200→180、单价 ¥50→¥52、交期 2026-10-05→2026-10-15 | 1. 点保存 | 1. 后端拒绝 3 项修改，错误码=ORDER_ALREADY_CONFIRMED_DIRECT_EDIT_DENIED；2. 提供「申请变更」入口（创建 OrderChangeRequest，类型=quantity/price/deliveryDate 组合）；3. 业务员不能直接提交 Confirmed 订单以上 5 类字段修改（取消/暂停/改价/改数/改交期） | 保存成功 → 违反 DR-010 业务员直接改已批准订单；前端灰显不足，必须服务端 fail-closed |
| OSM-010-C2 | 改数量/改价/改交期变更申请经销售主管或总领导审批，记录 before/after + 原因 + 影响 | 1. 业务员提交变更申请（改数量 200→180、交期延后 10 天）；2. 变更原因=客户延迟确认 PP；3. 影响=BOM 成本变化、回款延迟风险 | 1. 销售主管审批页面：查看 before/after 对比、原因、影响摘要；2. 点 Approve | 1. OrderChangeRequest.status=Approved；2. Order.quantity=180，Order.deliveryDate=2026-10-15（只改审批通过的项）；3. 写入 OrderStatusTransition 或 OrderChangeLog（operator=主管ID、时间、原因快照、影响快照）；4. MOQ 校验重新执行（180 < garmentDefaultMoq 200 → 若此前无 Capsule 豁免则新增豁免缺口 Badge 触发审批链）；5. 改数量导致出运 Shipment 聚合自动重算（若 Shipment 已存在部分分配） | 主管审批后数据无 before/after 审计 → 变更不可追溯；改数量 < MOQ 不重新校验 → 叠加 MOQ 门禁漏洞 |
| OSM-010-C3 | 已批准订单取消申请 → 汇总 6 方面影响（合同/采购/样品/生产/付款/库存）；无不可逆承诺 → 批准直接结案；已有承诺 → 结案处理中 | 1. 订单 Confirmed，面料采购合同已发出且工厂已盖章返回（有法律承诺）；2. 业务员提交取消申请（原因=客户取消项目）；3. 销售主管批准页面显示 6 类影响汇总 | 1. 主管查看影响：采购合同（已盖章=不可逆）、样品（PP 封存=可逆）、生产（未开裁=可逆）、付款（定金未收=无）、库存（未锁料）、合同（客户PO 已签字）；2. 点批准取消 | 1. Order 未直接软删，status=CancellationInProgress（或 Alert 子态，取决于 6 态实现）；2. 关联采购订单进入「Cancel Pending」，需供应商确认撤销+违约金/责任；3. 处置完成（供应商合同撤销+财务损益确认）后，才可真正 Closed（软删或关闭态）；4. 采购、QC、财务只确认各自影响的事实，不做业务最终审批人（审批人=销售主管或总领导） | 点批准后订单直接关闭且未处理采购合同撤销 → 供应商法律风险与财务损失漏处置；审批人变成采购主管或财务 → 违反 DR-010 审批链角色边界 |
| OSM-010-C4 | 已批准订单暂停：记录原因+责任人+预计恢复日期，到期未恢复自动提醒（禁无限期挂起） | 1. 订单 Production；2. 客户要求暂停生产 30 天（原因=等待新的颜色打样）；3. 销售主管批准暂停；4. 预计恢复日期=+30 天 | 1. 暂停申请 Approved；2. Order.status=Alert（暂停态或 Alert 子态）；3. 系统到预计恢复日期 T+0 早上 9 点自动推送；T+2 再推；T+7 未恢复升级 | 1. 暂停原因/责任人（客户）/预计恢复日期 3 字段必填；2. 到期前 1 天提醒业务员；到期当天推送主管；3. T+7 未恢复仍未关闭或继续申请延长 → 自动推送总领导（系统管理员容器）+ 显示「无限期挂起风险」报表；4. 恢复后生产放行门禁按 DR-009 再次校验（封样/面料/QC 是否仍有效） | 暂停无预计恢复日期却可批准 → 可被滥用无限挂起；到期无提醒 → 订单失踪无跟踪 |
| OSM-010-C5 | 业务员可自由取消 Pending/待审批订单（草稿阶段），但 DR-010 「已批准」门槛前必须拦截 | 1. 订单 1 status=Pending（草稿未提交审批）；2. 订单 2 status=Pending 但已提交审批（ApprovalRequest.status=Pending）；3. 业务员登录 | 1. 直接取消订单 1；2. 尝试取消订单 2 | 1. 订单 1：取消成功（软删或 CanceledDraft 状态，按实际实现），不触发影响汇总（无承诺）；2. 订单 2：取消必须先撤回审批单或走审批链取消，不得直接一键删除 | 把 Pending 草稿也纳入 DR-010 强审批 → 业务员日常体验过重但合规尚可（边界案例），但把已批准订单当 Pending 删除 → 严重风险 |

---

## §15 DR-013 受控例外 × 状态机门禁验收（订单批准无依据 / 生产放行 QC Fail / 取消不可逆承诺）

> **决策来源**：[DR-013](../../../../10-评审与决策/2026-08-16-设计评审决策记录.md#dr-013-全局业务门禁的受控例外推进)
>
> **适用场景（仅当正常路径 fail-closed 被 409 拦截后，提供 EXC 入口，不得在正常路径隐藏开关）**：订单批准门禁无确认依据（OSM-009-A1 的确认依据缺失）、生产放行 3 门禁中 QC/客户/封样某条 Fail（OSM-009-A2/OSM-009-A4）、已批准订单取消含 6 方面不可逆承诺（OSM-010-C3）的三类场景。

### 15.1 状态机 × DR-013 例外 3 类场景

| 验收编号 | 场景（对应 §14 哪个门禁被 EXC 绕过） | 前置条件 | 执行步骤 | 期望结果（EXC 双模型留痕 + 原门禁仍 Fail，只在 transition 接口临时放开 + 指定动作时点） | 失败路径 |
|---------|-----------------------------------|---------|---------|----------------------------------------------------------------------------|---------|
| OSM-EXC-1 | 订单批准无确认依据（OSM-009-A1 缺口：客户PO邮件+签章+定金三项全部缺失，但客户紧急电话要求先投产，业务员有录音+客户微信截图承诺 24 小时内补 PO）→ EXC 批准后允许 Confirmed（但需 48 小时内补确认依据，否则报警回滚状态） | 1. Order.status=Pending，客户确认依据 3 项全缺失；2. 正常推进 Confirmed 被 ORDER_CONFIRMATION_DOC_MISSING 拒绝（OSM-009-A1 门禁）；3. 前端提供「DR-013 受控例外」入口；4. 业务员上传电话录音+微信截图附件 | 1. 点 EXC 入口；exceptionCategory=order_approval；subCategory=without_confirmation_doc；exceptionReason=「客户为 ZARA 战略供应商，大货交期仅 40 天窗口客户承诺 24h 内补签 PO 盖章扫描件，逾期承担所有损失（≥30字）」；customerCommitment=录音+微信截图（附件）；补救=「跟单小王 24h 跟进客户签署，未回传则自动预警+暂停后续所有采购投产动作」；责任人=小王；2. 提交 EXC；3. 张主管 Approve；4. transitionOrderStatus(Pending→Confirmed) 绑定 EXC；5. 定时任务 48h 后检查 Order 是否补确认依据快照 | 1. EXC 创建 + ApprovalRequest 创建（DR-007 路由 reviewerId=张主管）；2. 批准后 Order.status=Confirmed，但 OrderStatusTransition.payload 写入 gateExceptionExcId=EXC-XXX，不写入确认依据快照 3 字段（保持 null）；3. 页面徽标「批准依据未齐-受控例外 EXC-XXX（48h 内补）」黄色警告；4. 48h 后未补：① 生成 CRITICAL AuditLog ORDER_CONF_EXC_DOC_NOT_SUPPLIED；② 通知 Z 总领导 + 张主管；③ 暂停 Order 的任何后续 Production 推进（直到补依据或再次升级 BOSS 兜底）；5. 原 ORDER_CONFIRMATION_DOC_MISSING 门禁状态仍在系统校验器内返回 true（**不修改校验器结果**；仅在 transition 内部根据 EXC 临时放行一次） | EXC 批准后，系统静默把「电话录音」作为正式确认依据写入 Order.confirmationDocs 快照（伪造依据）→ 审计无法发现真实的批准依据缺失（违反 DR-013"例外不改变原规则和原门禁状态"） |
| OSM-EXC-2 | 生产放行 3 门禁中「大货终期 QC=Fail」（OSM-009-A4），客户亲自来厂验货后书面同意放行（签字盖章：接受小疵点 2%，不索赔；附《客户现场验货接受函》扫描件）→ EXC 绕过 QC Fail 放行 Production → Shipping | 1. Order.status=Production，大货终期 InspectionReport type=Final status=Fail，critical=0 major=0 minor=2.3% 超标（阈值 2%）；2. 客户现场验货同意并签《瑕疵接受函》；3. 正常推进 Shipping（或 Production→ReadyToShip→Shipping 中间态）被 FINAL_QC_FAILED 拦截；4. 提供 EXC 入口 | 1. 业务员提交 EXC：exceptionCategory=shipment_release 或 production_release；subCategory=qc_defect_customer_accept；bypassedApprovalIds=[对应 QC Fail 的原出运放行审批被拒 ID（如果有）]；exceptionReason=「客户已现场验货签接受函附件，承诺瑕疵接受不索赔，考虑交付期 15 天内必须出运避免空运费（≥30 字）」；attachments=《客户瑕疵接受函》+客户签字扫描件；riskMitigationPlan=「财务暂扣 5% 尾款作为风险金，客户收货确认无批量质量问题后 30 天释放」；责任人=张主管；2. 销售主管或总领导批准；3. Shipment.release + 状态推进 Shipping 绑定 EXC | 1. EXC 批准后，InspectionReport.status 仍=Fail，defectRate=2.3 不变；2. Shipment.releaseReady=true + 徽标「QC 瑕疵特批放行（EXC-XXX）」+ 链接 EXC；3. OrderStatusTransition 写入 gate="FINAL_QC", bypassExcId=EXC-XXX（快照）；4. 财务应收/尾款模块显示暂扣 5% 风险金（若 service 层已联动）；5. 其他订单的同一 Fabric/Garment 仍按 QC 阈值 2% 严格校验（不自动放宽），InspectionReport 的 QC 阈值常量不被修改 | EXC 批准后，InspectionReport 的 finalVerdict 被静默改写成 Pass（覆盖 QC 原结果）→ QC 责任链被彻底破坏；或 QC defectRate 阈值 2% 被临时改成 3% 全局 → 所有订单 QC Fail 变 Pass（严重违反 fail-closed：EXC 绝对不得修改 QC 数据与全局阈值） |
| OSM-EXC-3 | 已批准订单取消：含 6 方面不可逆承诺（OSM-010-C3 中采购合同已盖章+面料已全部入库不可逆库存占用+客户已盖章PO违约赔偿风险），但销售副总（总领导）决定必须取消并内部承担全部损失，不向客户索赔（例：战略客户关系维护，主动取消以维持关系） → 启动 EXC 批准取消（6 方面不可逆承诺的取消） | 1. Order.status=Confirmed，采购合同=已盖章返回；面料=100% 入库（占用 80W RMB 库存资金）；客户 PO=已盖章返回；2. 正常取消申请 → 取消审批链被张主管 Rejected（理由"6 项不可逆，供应商+客户+库存损失过大，不建议取消"）；3. 业务员申请 EXC，副总 Z（系统管理员总领导容器）或 BOSS 最终兜底 | 1. 业务员 EXC：exceptionCategory=order_cancel；subCategory=cancel_with_irreversible_commitments；bypassedApprovalIds=[取消审批被拒的 ID]；exceptionReason=「战略客户 ZARA 年度 500W 合作框架下主动取消本单 PO-2026-088 以维护客户关系，库存面料 ZARA 承诺下 3 单消化 100%，损失由公司内部承担（≥30 字）」；customerCommitment=ZARA 下 3 单消化库存承诺书；riskMitigationPlan=「面料 80W 占用库存，下 3 单优先匹配，3 个月未消化完则计入该客户利润中心的专项扣减成本」；责任人=Z 副总；2. Z Rejected（取消审批）的情况下，BOSS 最终兜底 Approve；3. 取消 OrderCancelRequest.approvalRequestId=BOSS_APPROVAL；status 推进 CancellationInProgress → 6 方面不可逆承诺处理中；4. 所有处理完成（供应商合同撤销/违约金记账/库存重分配/客户违约赔偿豁免记账）→ Order.status=Closed/Cancelled | 1. 审批链：取消审批被拒→EXC→BOSS 兜底 BOSS_FINAL_BYPASS 路径正确；2. Dr013ExceptionRequest.bossFinalBypassBy=BOSS.id + reason≥30 字（服务端 Zod fail-closed）；3. 每张被拒审批（原取消审批的 REJECTED）bypassedApprovalId=EXC 的 ApprovalRequestId；4. 6 方面不可逆承诺的处理均写入对应实体的变更记录（PurchaseOrder.cancel_status=CANCELLED_VIA_EXC_XXX；FabricInventory.allocation_status=REALLOCATED_EXC_XXX；Customer.paymentTerms 豁免记录=WRITTEN_OFF_EXC_XXX）；5. EXC 指定动作=订单 Order.id 取消；**不影响其他 Order 的取消审批链与 6 方面判断逻辑**（其他 Order 仍严格拦截取消，除非走自己 EXC） | EXC 批准后，取消流程跳过 6 方面不可逆承诺的处理步骤，直接把 Order 软删 Closed → 采购/库存/财务的实体状态未处理，导致后续资金/库存/法律悬空（必须 fail-closed：6 方面处理每一步都与 EXC 绑定审计，未完成前不得进入 Closed 状态） |

---

## §16 设计系统约束

| 约束 | 合规说明 |
|------|---------|
| 步骤按钮 | bds-btn 变体（primary/outline/ghost），无自定义颜色 |
| Alert 按钮态 | warning tint token（非硬编码） |
| 时间线圆点 | accent / border-c-strong token |
| 连接器 | accent 45% color-mix / border-c-strong |
| 状态徽章 | bds-badge 语义变体（neutral/info/success/warning） |

---

## §15 交叉链接

1. [模块概述](./模块概述.md) §5 — 状态机在业务流程图中的位置
2. [订单详情页](./订单详情页.md) §3.3 — 步骤条 + 变更时间线 UI
3. [订单列表页](./订单列表页.md) §4.1 — 状态筛选选项
4. [订单 360° 全链路](./订单360°全链路.md) — 状态机贯穿全链路生命周期
5. [订单与生产模型组](../../02-数据模型/订单与生产模型组.md) — OrderStatusTransition / PreCutChecklist / InspectionReport 模型
6. [后端状态机真源](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/orders/orderLifecycleService.ts) — `transitionOrderStatus`
7. [前端步骤条真源](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/OrderManager.tsx#L239-L304) — `ORDER_ALLOWED_TRANSITIONS` / `handleStatusTransition`

---

## §16 相关文档索引

| 文档 | 路径 |
|------|------|
| 后端状态机服务 | [orderLifecycleService.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/orders/orderLifecycleService.ts) |
| 业务事件总线 | [server/src/events/businessEventBus.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/events/businessEventBus.ts) |
| EntityLink 同步 | [server/src/entities/sync.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/entities/sync.ts) |
| 审计日志 | [server/src/audit/routeAudit.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/audit/routeAudit.ts) |
| PreCutChecklist 模型 | [schema.prisma L2457-L2472](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/prisma/schema.prisma#L2457-L2472) |
| InspectionReport 模型 | [schema.prisma L2481-L2513](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/prisma/schema.prisma#L2481-L2513) |
