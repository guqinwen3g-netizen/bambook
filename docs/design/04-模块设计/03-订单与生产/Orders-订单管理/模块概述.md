# 订单管理模块概述 (Orders Module Overview)

## §1 元信息

| 项 | 值 |
|---|---|
| **模块定位** | 订单与生产域的"主轴"——串联报价→开发→BOM→采购→生产→出运→财务全链路；订单是下游所有履约单据的源头 FK |
| **模块边界** | 订单录入 / 列表 / 详情 / 状态机 / 类型分层 / 360° 全链路聚合视图；不含生产管线 10 阶段独立引擎、不含报价/采购/出运/财务各自的字段编辑（仅以 snapshot FK + EntityLink 双轨关联） |
| **核心角色** | 业务员（含跟单，日活最高：状态推进、字段维护、录入、自己的单）、QC（门禁写入，不细分）、超级管理员（老板，绝密级可见：全局视图）、财务（含金额 + 发票字段，宽泛容器不细分） |
| **范式** | 范式 A（列表）+ 范式 D（详情全屏覆盖）+ 范式 E（录入 Modal）三态切换 |
| **优先级** | P0（生产核心） |
| **实现状态** | ✅ 已落地（列表/详情/录入/状态机双校验/360° 聚合/类型分层/Capsule 子视图均已接入生产；后端行级权限过滤与 MOQ 校验为已知缺口） |
| **关联 PRD 章节** | §5.5（订单状态机）、§5.6（订单编号 SO-YYYYMM-NNNN）、§9（业务规则 10 条）、§19.7（订单/生产订单页详细设计）、§24.2（分组导航） |
| **关联代码** | [OrderManager.tsx](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/OrderManager.tsx) 桌面端主组件 / [orderSchema.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/lib/orderSchema.ts) 字段词典真源 / `server/src/orders/` 后端服务（query/orderLifecycleService/orderLineMutationService/orderServiceV2） / `server/prisma/schema.prisma` Order/OrderLine/OrderStatusTransition 模型 |

---

## §2 子文档索引

本模块共 7 份子文档（含本概述），按"由外到内、由静到动"组织：

| # | 文档 | 路径 | 关注点 |
|---|------|------|--------|
| 0 | 模块概述（本文档） | `./模块概述.md` | 全局定位 / 范围 / 业务流程图 / 类型分层概要 |
| 1 | 订单详情页 | `./订单详情页.md` | 全屏详情主面板（已落地，9 大区块 + 13 cluster） |
| 2 | 订单列表页 | `./订单列表页.md` | 筛选 / 列定义 / 批量操作 / Capsule 子视图 |
| 3 | 订单录入表单 | `./订单录入表单.md` | 字段分组 / 验证规则 / MOQ 校验 / 金额联动 |
| 4 | 订单类型分层 | `./订单类型分层.md` | 4 类型 × 字段差异矩阵 / 类型切换动态显示 |
| 5 | 订单状态机 | `./订单状态机.md` | 6 态矩阵 + 门禁 + 前后端双校验 |
| 6 | 订单 360° 全链路 | `./订单360°全链路.md` | 10 阶段聚合视图 + OrderContextSection 规格 |

数据模型真源另见：[`../../02-数据模型/订单与生产模型组.md`](../../02-数据模型/订单与生产模型组.md)（覆盖 Order/OrderLine/OrderStatusTransition/DevelopmentCase/SampleNode/ProductionStage/PreCutChecklist/InspectionReport/OutsourcingOrder 9 模型）。

---

## §3 模块范围与核心能力

### 3.1 范围内（In Scope）

| 能力 | 说明 | 代码入口 |
|------|------|---------|
| 订单录入 | 手动表单 + PDF 导入预览确认双路径落库 | `OrderManager` 的 `showAddModal` / `ImportWizard` |
| 订单列表 | 类型 Tab（全部/面料/成衣/其他）+ Capsule 子视图透镜 + 状态筛选 + 模糊搜索 | `filteredOrders` + `lineItems` useMemo |
| 订单详情 | 全屏覆盖 9 大区块 + 13 cluster 字段字典驱动 | `OrderManager` 详情分支 L1271+ |
| 状态机推进 | 6 态合法矩阵 + QC 门禁 + 前后端双校验 + 时间线留痕 | `handleStatusTransition` + `orderLifecycleService.transitionOrderStatus` |
| 360° 全链路 | 直接查表 10 实体聚合（报价→财务）只读纵向生命周期 | `getOrderContext` + `OrderContextSection` |
| 类型分层 | Fabric/Garment/Other + Capsule（成衣业务线子标记）字段差异 | `orderSchema.applicableTypes` + `businessLine` |
| 下游 prime 联动 | 详情页 3 按钮（生成采购/发起验货/创建出运）携带 orderId 跳转 | `handlePrimeProcurement/Qc/Shipment` |
| 软删归档 | `deletedAt` 软删 + 关联发票/运单/凭证存在时禁止 | `orderLifecycleService.deleteOrder` |

### 3.2 范围外（Out of Scope）

- ❌ 生产管线 10 阶段门禁引擎的独立 UI（由 `ProductionPipeline` 组件承载，本模块仅嵌入）
- ❌ 报价 / 采购 / 出运 / 财务各自模块的字段编辑表单（本模块仅通过 snapshot FK + EntityLink 关联）
- ❌ 客户信用逾期 60 天冻结门禁（PRD §9.4，已知 P0 缺口，尚未接入订单创建/推进）
- ❌ 订单变更分级审批（PRD §9.7，仅审计留痕，未做 diff 视图与审批流）

---

## §4 订单类型分层概要

订单类型采用"主类型 + 业务线"二级分层：

```
Order.type ∈ { Fabric, Garment, Other }（3值主分类，Prisma String）
├── Fabric   面料订单（默认 unit=Meter，MOQ 800M）
├── Garment  成衣订单（默认 unit=Pcs，  MOQ 200Pcs）
│   └── Order.businessLine = 'capsule'  Capsule 子视图（设计师小单，MOQ 20Pcs；仅对 type=Garment 生效的子类型透镜）
└── Other    其他订单（DR-028 其他贸易，默认 unit=KG，businessLine=other）
```

**分层规则**：
- 主类型 `Order.type ∈ { Fabric, Garment, Other }（3值主分类）` 决定**字段簇可见性**（`fabric` cluster 仅 Fabric；`materials` cluster 仅 Other；`garmentProduction`/`sampleGarment` cluster 仅 Garment）
- 业务线 `businessLine ∈ { garment, capsule, fabric, other }（4值业务线标签）` 仅对 Garment 生效时 capsule 作为子类型透镜，作为列表 Capsule 子视图透镜与 MOQ 阈值切换依据；未标记的成衣归"大货"
- 详细字段差异矩阵见 [`./订单类型分层.md`](./订单类型分层.md)

---

## §5 核心业务流程图

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  报价     │───▶│  订单     │───▶│  开发/   │───▶│  BOM     │───▶│  采购     │───▶│  生产     │
│ Quotation │    │  Order   │    │  样衣     │    │          │    │  PO      │    │  Stage   │
│ (conv orderId)│ │ (FK 源)  │    │ DevCase  │    │ (orderId)│    │ (orderId)│    │ (orderId)│
└──────────┘    └────┬─────┘    │ SampleNode│    └──────────┘    └──────────┘    └────┬─────┘
                     │          └──────────┘                                      │
                     │                                                            │
                     │   ┌──────────────────────────────────────────────────────┘
                     │   ▼
                     │  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
                     │  │ QC 验货  │───▶│  出运     │───▶│  发票     │───▶│ 财务凭证 │
                     │  │ Inspect  │    │ Shipment │    │ Invoice  │    │ Voucher  │
                     │  │ (orderId)│    │ (orderId)│    │ (orderId)│    │ (orderId)│
                     │  └──────────┘    └──────────┘    └──────────┘    └──────────┘
                     │
                     │  ┌──────────────────────┐
                     └─▶│  外协 OutsourcingOrder│ (orderId + bomId snapshot FK)
                        └──────────────────────┘

订单状态机（贯穿上述链路的生命周期标记）：
  Pending ──▶ Confirmed ──▶ Production ──▶ Shipping ──▶ Delivered
                                                         
                     ╳ Alert ╳ （异常态：可恢复到任一非终态）

门禁强校验（后端 orderLifecycleService + 前端步骤条双轨）：
  • Confirmed → Production 前：PreCutChecklist 4 项全通过
  • Production → Shipping 前：InspectionReport(final).result = pass
```

**链路关联双轨制**：
1. **EntityLink 图谱**（横向发现）：`/api/v1/relations/by-target?type=order&id={orderId}` → 关联视图面板
2. **直接查表聚合**（纵向生命周期）：`/api/v1/orders/:id/context` → 360° 全链路面板

两轨互补：图谱服务"跨模块横向发现任意实体关联"；聚合查询服务"按业务阶段纵向还原完整生命周期"。

---

## §6 数据模型概要

订单管理模块的持久化真源由 3 个核心模型 + 6 个履约关联模型组成（全量字段表见数据模型组文档）：

| 模型 | 角色 | 关键约束 |
|------|------|---------|
| `Order` | 订单主表（80+ 字段，含 7 关系快照 + 业务档案） | `id` PK / `code` `poNumber` 双唯一 / `deletedAt` 软删 / 9 索引 |
| `OrderLine` | 订单行（含成衣 JSON 扩展） | `@@unique([orderId, itemNo])` / `onDelete: Cascade` |
| `OrderStatusTransition` | 状态变更时间线事件 | `orderId` FK Cascade / `createdAt` 升序查询 |
| `DevelopmentCase` | 开发案（linkedOrderId snapshot FK） | `code` unique / 软删 |
| `SampleNode` | 三级样衣节点（确认/产前/大货） | `@@unique([developmentCaseId, level])` |
| `ProductionStage` | 生产管线 10 阶段 | `@@unique([orderId, stageKey])` / 双签字段 |
| `PreCutChecklist` | 裁剪前检查（QC 门禁 1） | `orderId @unique` / 4 boolean |
| `InspectionReport` | QC 验货报告（QC 门禁 2） | `@@unique([orderId, inspectionType])` |
| `OutsourcingOrder` | 外协单（orderId/bomId snapshot FK） | `orderNumber @unique` / 软删 |

---

## §7 字段词典真源约定

所有订单字段的"存在性 / 标签 / 分组 / 类型 / 可见入口 / 适用订单类型"均在 [orderSchema.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/lib/orderSchema.ts) 的 `ORDER_FIELDS` 数组中登记一次，三处 UI（详情卡 / 录入弹窗 / PDF 导入预览）共享单一真源：

- `shownInDetail` / `shownInImportPreview` / `shownInManualForm` 三个布尔决定各入口渲染哪些字段
- `cluster` 决定分组（13 cluster），`ORDER_CLUSTERS` 决定组内顺序（detailOrder / manualOrder 分离）
- `applicableTypes` 决定类型分层（undefined = 通用；显式列表 = 仅列出的类型可见）
- `level`（'order' / 'line'）+ `lineScope`（'first' / 'each' / 'aggregate'）决定 OrderLine 级字段渲染策略
- `relationFk` + `autoFillFrom` 决定 Relation 下拉选择后的字段联动填充

新增字段的强制流程：先在 `ORDER_FIELDS` 登记 → 再在 `types.ts` 的 `Order` / `OrderLineLite` 接口加列 → 编译器用 `keyof Order` 约束保证不跑偏。

---

## §8 API 端点概要

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/orders` | 列表（筛选/分页/模糊搜索，支持 type/status/customer/dueDate） |
| GET | `/api/v1/orders/:id` | 单订单详情（含 lines） |
| POST | `/api/v1/orders` | 创建（手动 or PDF import 落库） |
| PUT | `/api/v1/orders/:id` | 更新（Order + OrderLine 同 patch） |
| POST | `/api/v1/orders/:id/transition` | 状态推进（合法矩阵 + QC 门禁双校验） |
| DELETE | `/api/v1/orders/:id` | 归档（soft-delete，关联单据存在时 400） |
| GET | `/api/v1/orders/:id/context` | 360° 全链路聚合（10 实体） |
| GET | `/api/v1/orders/:id/status-history` | 状态变更时间线 |
| POST | `/api/v1/orders/:id/set-business-line` | 设置成衣业务线（大货/Capsule） |

---

## §9 权限矩阵概要

行级权限基于 `ownerId`（归属人）+ `departmentId`（归属部门）双字段；角色可见性详见各子文档 §9。当前**前端已按矩阵实现字段隐藏 + 按钮禁用，后端 prisma 查询层行级过滤为已知 P0 缺口**。

| 角色 | 列表可见 | 编辑 | 状态推进 | 归档 |
|------|---------|------|---------|------|
| owner/admin/manager | 全部 | ✅ | ✅ | ✅ |
| sales | 仅 ownerId=me | ✅（自己的单） | ✅（自己的单） | ❌ |
| merchandiser | 同部门 | ✅（限定阶段字段） | ✅（日活最高） | ❌ |
| qc | 同部门 | 仅生产管线 QC 条目 | ⚠️ 仅 Production→Shipping | ❌ |
| finance | 全部（金额+发票字段） | 仅 sales/shipment cluster | ❌ | ❌ |

---

## §10 设计系统约束

本模块遵循 BDS RDL 中性契约（详见各子文档 §14）：
- 禁止硬编码 hex 颜色 / `rounded-[Npx]` / `box-shadow`，全部走 CSS 变量 + Tailwind 语义类
- 子组彩色竖条已废除（买卖四方仅靠文字层级 + kicker 分组）
- 空态不使用插画/吉祥物，仅文字 + line-height 小 Lucide 图标
- 状态徽章通过 `STATUS_BADGE_VARIANT` 映射 bds-badge 语义变体（neutral/info/success/warning）

---

## §11 业务规则 §9 接入状态

| 规则 | 接入状态 | 备注 |
|------|---------|------|
| 9.2 价格审批双轨偏差 | ⚠️ 报价模块触发，详情页无 badge（P0 缺口） | — |
| 9.3 质量门禁 3 条 | ✅ 裁剪前 + Final AQL 已接强校验 | 厂前样双签在 Sample 模块 |
| 9.4 信用控制 60 天逾期冻结 | ⚠️ 仅看板，创建/推进未校验（P0 缺口） | — |
| 9.5 金额联动 | ✅ `applyAmountLinkage` 实时联动 | — |
| 9.7 订单变更分级审批 | ⚠️ 仅审计留痕，无 diff 视图（P0 缺口） | — |
| 交期锁死（出运后只读） | ⚠️ 未实现（P0 缺口） | — |
| 生产 7 天计划死线 | ✅ 字段已建，调度器待补 | — |
| 延期通知 15 天 | ✅ E3 批次已实现 | — |
| 工厂验厂到期提醒 | ✅ E1 批次已实现 | — |
| MOQ 最小起订量 | ⚠️ 未校验（P1 缺口） | — |

---

## §12 移动端适配

移动端不使用全屏覆盖详情，改用 `BottomSheet` 操作模式（详见 [订单详情页.md §1.3](./订单详情页.md)）：
- 长按/点击行 → BottomSheet 弹出 3 项操作（编辑详情 / 快速状态变更 / 归档）
- 快速状态变更：横向滚动 badge（Confirmed/Production/Shipping/Delivered）一键推进
- 编辑详情：跳转 compact density 字段表单（与桌面同真源 `orderSchema`）

---

## §13 待补的设计缺口

| # | 缺口 | 优先级 | 落点子文档 |
|---|------|-------|-----------|
| 1 | 后端行级权限过滤（prisma where 层） | P0 | 订单列表页 §9 |
| 2 | MOQ 校验 + 低于 MOQ 审批链 | P1 | 订单录入表单 §6 |
| 3 | 客户逾期 60 天新单冻结门禁 | P0 | 订单状态机 §门禁 |
| 4 | 交期锁死只读（Shipping/Delivered 阶段） | P0 | 订单详情页 §4.2 |
| 5 | 订单变更前后 diff UI | P0 | 订单详情页 §3.6 |
| 6 | 报价审批状态 badge | P0 | 订单详情页 §3.1 |

---

## §14 实现状态总览

| 子能力 | 状态 | 真源 |
|--------|------|------|
| 列表 + 筛选 + Capsule 子视图 | ✅ | `OrderManager` L447-L474 |
| 详情全屏 9 区块 | ✅ | `OrderManager` L1271+ |
| 字段字典 13 cluster | ✅ | `orderSchema.ORDER_FIELDS` |
| 状态机前后端双校验 | ✅ | `orderLifecycleService.transitionOrderStatus` |
| 360° 全链路聚合 | ✅ | `query.getOrderContext` |
| 类型分层 applicableTypes | ✅ | `orderSchema` FieldMeta |
| 软删 + 关联保护 | ✅ | `orderLifecycleService.deleteOrder` |
| 业务事件发布（OrderConfirmed） | ✅ | `publishBusinessEvent` |
| 后端行级权限过滤 | ⚠️ 缺口 | prisma where 层 |
| MOQ 校验 | ⚠️ 缺口 | 录入/保存 |

---

## §15 交叉链接

1. [订单详情页](./订单详情页.md) — 全屏详情主面板（已落地）
2. [订单列表页](./订单列表页.md) — 筛选/列定义/批量操作
3. [订单录入表单](./订单录入表单.md) — 字段分组/验证/MOQ
4. [订单类型分层](./订单类型分层.md) — 4 类型字段差异矩阵
5. [订单状态机](./订单状态机.md) — 6 态矩阵 + 门禁
6. [订单 360° 全链路](./订单360°全链路.md) — 10 阶段聚合视图
7. [订单与生产模型组](../../02-数据模型/订单与生产模型组.md) — 9 模型全量字段表 + ER 图
8. [字段词典真源](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/lib/orderSchema.ts) — `ORDER_FIELDS` / `ORDER_CLUSTERS`

---

## §16 相关文档索引

| 文档 | 路径 |
|------|------|
| 字段词典代码真源 | [lib/orderSchema.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/lib/orderSchema.ts) |
| 状态机定义（前端） | [OrderManager.tsx ORDER_ALLOWED_TRANSITIONS](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/OrderManager.tsx#L248-L255) |
| 状态机定义（后端真源） | [orderLifecycleService.ts ORDER_TRANSITIONS](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/orders/orderLifecycleService.ts#L18-L25) |
| 全链路聚合查询 | [orders/query.ts getOrderContext](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/orders/query.ts#L75-L265) |
| Prisma 模型真源 | [server/prisma/schema.prisma](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/prisma/schema.prisma) Order/OrderLine/OrderStatusTransition |
| 订单类型分层方案（历史） | `.trae/documents/order-type-field-layering-plan.md` |
| 前端类型定义 | [types.ts Order](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/types.ts#L666-L821) |
