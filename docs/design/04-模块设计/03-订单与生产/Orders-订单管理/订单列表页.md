# 订单列表页 (Order List Page)

## §1 元信息

| 项 | 值 |
|---|---|
| **定位** | 订单模块的入口与索引——按类型 Tab + 状态筛选 + 模糊搜索定位订单，行点击进入详情 |
| **入口** | 主导航 Orders 视图（`View.Orders`），桌面端全屏表格 / 移动端紧凑卡片列表 |
| **核心角色** | 业务员（含跟单，日活最高）、销售主管（部门管理职责） |
| **范式** | 范式 A — 列表表格（CompiledTableShell）+ 顶部筛选条 + 类型 Tab |
| **优先级** | P0 |
| **实现状态** | ✅ 已落地（类型 Tab / Capsule 子视图 / 状态筛选 / 模糊搜索 / 行级展开均接入生产；批量导出端点待补） |
| **关联 PRD 章节** | §5.5（状态机）、§19.7（订单页详细设计）、§24.2（分组导航） |
| **关联代码** | [OrderManager.tsx](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/OrderManager.tsx#L447-L474) `filteredOrders` / `lineItems` useMemo / `ORDER_TABLE_HEADERS` L132-L139 / 后端 [query.ts queryOrders](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/orders/query.ts#L5-L47) |

---

## §2 页面骨架

```
┌─────────────────────────────────────────────────────────────────────┐
│  PageHeader (KICKER: Orders / HERO: 订单管理 / 操作区)               │
├─────────────────────────────────────────────────────────────────────┤
│  Type Tab: [全部] [面料] [成衣] [其他]          (Capsule 子开关)    │
├─────────────────────────────────────────────────────────────────────┤
│  Filter Bar: [状态 ▾] [搜索框 PO#/客户/品号] [导入] [+ 新建]        │
├─────────────────────────────────────────────────────────────────────┤
│  CompiledTableShell (raisedCard)                                     │
│  ┌──────┬──────┬──────────┬──────────┬────────┬──────────┐         │
│  │订单/客户│品号 │描述/色号  │数量/金额  │日期    │状态/动作  │  header │
│  ├──────┼──────┼──────────┼──────────┼────────┼──────────┤         │
│  │ row  │ row  │ row      │ row      │ row    │ row      │  22/15/27/13/13/10 │
│  └──────┴──────┴──────────┴──────────┴────────┴──────────┘         │
│  分页 / 空态 / 加载骨架                                              │
└─────────────────────────────────────────────────────────────────────┘
```

**栅格比例**（`ORDER_TABLE_GRID_CLASS`）：`grid-cols-[22%_15%_27%_13%_13%_10%]`，6 列固定百分比，行最小高度 `min-h-[72px]`。

---

## §3 类型 Tab（一级视图切换）

| Tab | UI key | DB 值 | 说明 |
|-----|--------|-------|------|
| 全部 | `all` | — | 不按类型过滤，显示所有未删除订单 |
| 面料 | `fabric` | `Fabric` | 默认 unit=Meter，含 fabric cluster 字段 |
| 成衣 | `garment` | `Garment` | 默认 unit=Pcs，含 garmentProduction/sampleGarment cluster |
| 其他 | `other` | `Other` | 辅料/纱线，含 materials cluster |

**映射真源**：`ORDER_TYPE_TO_DB`（UI key → PascalCase）/ `dbValueToTypeKey`（反向）。Tab 切换回调 `onOrderTypeChange`，组件内 `isAllType` / `currentDbType` 派生。

**Capsule 子视图透镜**（仅成衣 Tab）：
- 成衣 Tab 下追加 `capsuleOnly` 开关（`bds-segment` 双段：成衣大货 / Capsule）
- `capsuleActive = orderType === 'garment' && capsuleOnly`
- 过滤逻辑：`(!capsuleActive || o.businessLine === 'capsule')` —— 仅显示 `businessLine='capsule'` 的成衣；未标记的成衣归"大货"
- Capsule 是设计师小单业务线，MOQ 阈值 20Pcs（详见 [订单类型分层.md](./订单类型分层.md)）

---

## §4 筛选条件

### 4.1 状态筛选（前端级）

| 项 | 说明 |
|----|------|
| 控件 | `orderFilterStatus` state，下拉单选 |
| 选项 | `all`（默认）/ Pending / Confirmed / Production / Shipping / Delivered / Alert |
| 作用域 | 前端 `lineItems` useMemo 内 `item.order?.status === orderFilterStatus` 过滤 |
| 与后端关系 | 当前为前端过滤；后端 `queryOrders` 支持 `filters.statuses` 数组（后端能力已就绪，前端未完全对接分页） |

### 4.2 模糊搜索（前端级）

| 项 | 说明 |
|----|------|
| 控件 | `orderSearchTerm` state，文本输入框 |
| 命中字段 | `poNumber` / `customer` / `materialCode`（客供品号）/ `description`（描述色号）/ `clientCode`（客户编码）|
| 匹配方式 | `toLowerCase().includes(q)`（大小写不敏感）|
| 作用域 | `lineItems` useMemo 内过滤 |

### 4.3 后端筛选能力（queryOrders 已就绪）

后端 `buildOrderQueryWhere` 支持的筛选维度（前端列表页尚未全部对接，留作分页/服务端筛选扩展）：

| 筛选维度 | 入参字段 | 后端实现 |
|---------|---------|---------|
| 状态 | `filters.statuses` / `filters.status` | `OR` 多状态匹配 |
| 客户 | `filters.customer` | customer / billToName / customerCode 三字段 OR |
| 供应商 | `filters.supplier` / `filters.millName` | millName / millRelationId / lines.millQuality OR |
| PO 号 | `filters.poNumber` / `filters.po` | poNumber contains |
| 交期范围 | `filters.dueDateFrom` / `dueDateTo` | dueDate gte/lte |
| 创建时间范围 | `filters.createdAtFrom` / `createdAtTo` | createdAt BigInt gte/lte |
| 缺字段 | `filters.missingFields` | OR(null, '') 标识字段缺失的订单 |
| 字段级过滤 | `filters.fieldFilters` | path/operator/value，白名单 `SAFE_ORDER_FIELDS` 校验 |
| 通用查询 | `query` | 13 字段 OR 模糊（id/poNumber/customer/customerCode/product/millName/billToName/consigneeName/invoiceNumber/supplierInvoiceNumber/fabricCode/fabricContent + lines 三字段）|

### 4.4 排序

| 项 | 说明 |
|----|------|
| 前端 | 列表当前按后端返回顺序（`updatedAt desc` 默认） |
| 后端 | `orderQueryOrderBy`，白名单 `ORDER_SORT_FIELDS`：updatedAt / importedAt / dueDate / clientDate / customer / status / poNumber；默认 updatedAt desc |

---

## §5 列定义

6 列固定栅格，header 定义在 `ORDER_TABLE_HEADERS`：

| # | 列名 | 宽度 | 内容 | 对齐 |
|---|------|------|------|------|
| 1 | 订单 / 客户 | 22% | PO# + 客户名 + 类型徽章（Fabric/Garment/Other 彩色圆点）| 左 |
| 2 | 品号 | 15% | clientCode（客供品号）+ productColorCode（工厂品色号）| 左 |
| 3 | 描述 / 色号 | 27% | product + description + 面料成份/规格摘要 | 左 |
| 4 | 数量 / 金额 | 13% | quantity + unit + 金额（contractAmount → quoteAmount 兜底）+ 币种前缀 | 右 |
| 5 | 日期 | 13% | clientDate（出厂交期）→ dueDate 兜底，YYYY-MM-DD | 左 |
| 6 | 状态 / 动作 | 10% | 状态徽章（STATUS_BADGE_VARIANT）+ 行点击/更多操作 | 左 |

**列视觉规范**：
- 状态徽章语义：Pending=neutral / Confirmed·Production·Shipping=info / Delivered=success / Alert=warning
- 类型圆点允许彩色（`ORDER_TYPE_DOT_STYLE`）：Fabric=accent / Garment=wisteria / Other=warning
- 金额归一化：`Number(n).toLocaleString('zh-CN', { maximumFractionDigits: 2 })`，空值显示 `—`
- 日期归一化：`formatYmd` → YYYY-MM-DD

---

## §6 行级展开（按行渲染）

订单列表采用**行级展开**策略：一个 PO 若有多个 `OrderLine`，每个 line 在列表里独立成一行（共享 order 头信息）。

**实现**：`flattenOrderLines(orders)` → `OrderLineItem[]`，每条含 `order` 引用 + line 字段 + `displayItemNo` / `displayId` / `amount`。

**过滤链**（`lineItems` useMemo）：
1. `flattenOrderLines(orders)` 展平
2. 类型过滤：`isAllType || item.order?.type === currentDbType`
3. Capsule 过滤：`!capsuleActive || item.order?.businessLine === 'capsule'`
4. 状态过滤：`orderFilterStatus === 'all' || item.order?.status === orderFilterStatus`
5. 搜索过滤：命中 5 字段之一

**行点击**：`handleOrderClick(order)` → `onSelectOrder(order)` + 设置 `editForm` + 从 `order.lines` 映射 `poItems` → 进入详情全屏覆盖。

---

## §7 批量操作

| 操作 | 入口 | 实现 | 状态 |
|------|------|------|------|
| 批量导出 Excel | Filter Bar 导出按钮 | `GET /api/v1/orders/export?type=&keyword=&status=` | ⚠️ 端点待补 |
| 批量导入 | Filter Bar 导入按钮 | `ImportWizard` → `POST /api/v1/orders/import` → `saveParsedOrders` | ✅ 已落地 |
| 批量状态变更 | — | 未实现（当前仅单行详情内推进） | ❌ 缺口 |
| 批量归档 | — | 未实现 | ❌ 缺口 |

**导入流程**（`ImportWizard`）：
1. 选择 PDF/Excel 文件 → 后端解析为 `ParsedOrder`
2. 预览确认（`shownInImportPreview` 字段集）→ 用户可编辑
3. `saveParsedOrders` 落库 → `SavedOrderRow` → `savedRowToOrder` 合并入 `orders` state
4. 字段级 provenance：`fieldSources` 标记 'pdf'，后续 PDF 重导入跳过用户已手动编辑的字段

---

## §8 顶部操作区

| 按钮 | 图标 | 动作 | 权限 |
|------|------|------|------|
| 新建订单 | `Plus` | 打开录入 Modal（`showAddModal`）| sales+ |
| 导入 | `Upload` | 打开 `ImportWizard` | sales+ |
| 视图切换 | `Globe` / `List` | globe（3D 地图）/ list（表格）切换，`allowGlobeView` 控制 | all |
| 类型 Tab | — | 切换 `orderType` | all |
| Capsule 开关 | — | 成衣 Tab 下 `capsuleOnly` 切换 | all |

---

## §9 权限矩阵（列表页维度）

> ⚠️ 前端已按矩阵过滤；**后端 prisma where 层行级过滤仍待实现**（P0 缺口）

| 角色 | 列表可见范围 | 前端过滤依据 |
|------|------------|------------|
| owner/admin/manager | 全部未删除订单 | 无过滤 |
| sales | 仅 `ownerId=me` | ⚠️ 前端未过滤（依赖后端返回，后端缺口） |
| merchandiser | 同 `departmentId` | ⚠️ 同上 |
| qc | 同 `departmentId` | ⚠️ 同上 |
| finance | 全部（仅金额+发票字段） | ⚠️ 字段级过滤后端缺口 |
| warehouse | 同 `departmentId` | ⚠️ 同上 |
| guest | 入口隐藏 | — |

**佣金字段**：仅 owner/admin/manager/finance 可见；前端隐藏已实现，后端返回全部字段（安全缺口 P0）。

---

## §10 四态规范

| 状态 | 触发 | UI 规范 |
|------|------|---------|
| 加载态 | 首次进入 / refreshKey 变化 | 表格骨架（CompiledTableShell shimmer）+ 行占位 |
| 空态 | `filteredOrders.length === 0` 或 `lineItems.length === 0` | "暂无订单"灰态斜体；移动端 `filteredOrders.length === 0` 显示空态卡 |
| 错误态 | fetch 返回 4xx/5xx | `bds-alert danger` + 中文文案 + Retry |
| 无权限 | 后端 403/401 | `bds-alert warning` + "您无权查看订单，请联系管理员" |

---

## §11 移动端适配

- 表格改为紧凑卡片列表（每行卡片化，垂直堆叠关键字段）
- 类型 Tab 横向滚动（避免换行）
- 筛选改为底部 Sheet 展开
- 行点击 → `BottomSheet` 3 项操作（编辑详情 / 快速状态变更 / 归档），而非全屏详情
- Capsule 开关在成衣 Tab 下作为分段控件显示

---

## §12 分页

| 项 | 说明 |
|----|------|
| 前端 | 当前列表一次性加载全部 `orders`（前端过滤），无前端分页 |
| 后端 | `queryOrders` 支持 `limit`（默认 20，max 200）/ `offset`（max 1,000,000）/ `hasMore` |
| 扩展点 | 大数据量时改服务端分页：Tab/筛选/搜索参数透传 `queryOrders`，前端只渲染当前页 |

---

## §13 交互细节

| 项 | 规范 |
|----|------|
| 行 hover | `hover:bg-[var(--hover-darken)]` + cursor-pointer |
| 行点击 | 进入详情全屏覆盖（`onSelectOrder`）|
| 类型徽章 | 圆点 + 文字（面料/成衣/其他）|
| 状态徽章 | bds-badge 语义变体 |
| 搜索防抖 | 输入即时过滤（数据量小，无需 debounce）|
| 滚动容器 | `orderListScrollRef` 独立 overflow-y-auto |

---

## §14 设计系统约束

| 约束 | 合规说明 |
|------|---------|
| 表格容器 | `CompiledTableShell`（raisedCard 表面，玻璃材质 + hairline border，无阴影）|
| 表头 | sticky + `TXT_MUTED` + `KICKER_CLASS` 字号 |
| 列宽 | 固定百分比栅格（`ORDER_TABLE_COLUMN_WIDTH_CLASSES`），不使用固定 px |
| 颜色 | 全部 CSS 变量（`var(--accent-tint)` 等），无硬编码 hex |
| 圆角 | 表格容器 `rounded-card`；徽章 `rounded-compact` |
| 类型圆点 | 小元素允许彩色 token（accent/wisteria/warning），非大面积色块 |

---

## §15 交叉链接

1. [模块概述](./模块概述.md) — 订单管理全局定位与业务流程图
2. [订单详情页](./订单详情页.md) — 行点击进入的全屏详情
3. [订单录入表单](./订单录入表单.md) — 新建订单 Modal 的字段定义
4. [订单类型分层](./订单类型分层.md) — Tab 与 Capsule 子视图的类型分层依据
5. [订单状态机](./订单状态机.md) — 状态筛选选项的语义来源
6. [订单与生产模型组](../../02-数据模型/订单与生产模型组.md) — Order/OrderLine 模型字段
7. [字段词典真源](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/lib/orderSchema.ts) — 列内容字段映射

---

## §16 相关文档索引

| 文档 | 路径 |
|------|------|
| 前端列表渲染真源 | [OrderManager.tsx L447-L474](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/OrderManager.tsx#L447-L474) |
| 表头/栅格定义 | [OrderManager.tsx L121-L145](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/OrderManager.tsx#L121-L145) |
| 后端查询服务 | [orders/query.ts queryOrders](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/orders/query.ts#L5-L47) |
| 行级展开工具 | [lib/orderLineItems.ts flattenOrderLines](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/lib/orderLineItems.ts) |
| 导入向导 | [components/import/ImportWizard.tsx](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/import/ImportWizard.tsx) |
| 前端类型 | [types.ts OrderLineItem](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/types.ts#L890-L901) |
