# QC 质检中心模块概述 (QC Workbench Module Overview)

## §1 元信息

| 项 | 值 |
|---|---|
| **模块定位** | QC 质检中心是质量管理的执行枢纽 — 中期验货（生产过程中抽检）、终期验货（出货前 AQL 全检门禁）、疵点管理（三级疵点计数 + 趋势分析）、驻地管理（常驻验货点配置）、业务线管理（MOQ/生产周期基准），是生产管线 10 阶段门禁的关键判据来源 |
| **入口** | 主导航 QC Workbench（ClipboardCheck 图标）→ 全屏 Manager 页面（桌面端三 Tab：验货任务 / 驻地管理 / 业务线） |
| **核心角色** | QC（含质检员、QC 主管，不细分：执行验货、分配任务/审核报告，日活最高）、业务员（含终期验货业务批准）、销售主管（部门管理职责：疵点趋势/供应商评分） |
| **范式** | 范式 D — 看板+表单混合（验货任务三列看板 + 驻地/业务线 CRUD 表单） |
| **优先级** | P0（生产管线门禁判据来源） |
| **实现状态** | ✅ 核心已落地（验货任务看板 Assigned→InProgress→Completed 状态机 + 驻地 CRUD + 业务线 CRUD + InspectionReport 中期/终期 AQL 报告 + PreCutChecklist 裁剪前检查门禁 + qc_shipped 三门禁校验 + 验货结论自动供应商评分 + 终期 fail 广播通知）；⚠️ 疵点趋势分析看板（按供应商/产品/时间维度统计）为规划增强 |
| **关联 PRD 章节** | §4.2（QC 工作台）、§6.2（QC 驻地与验货任务）、§7.1（终期验货 fail 状态迁移）、§9.3（质量门禁 3 条） |
| **关联代码** | [QcWorkbenchManager.tsx](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/QcWorkbenchManager.tsx) 前端三 Tab / [qcService.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/qc/qcService.ts) 任务+驻地服务 / [qcRoute.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/qc/qcRoute.ts) QC 路由 / [stageService.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/production/stageService.ts) 验货报告+门禁引擎 / `schema.prisma` InspectionReport / PreCutChecklist / QCLocation / QCAssignment / BusinessLine 模型 |

---

## §2 模块边界与职责

QC 质检中心模块是生产管线质量门禁的判据来源，其核心职责矩阵：

| 职责域 | 已实现 | 规划增强 | 上下游依赖 |
|--------|--------|----------|-----------|
| **验货任务管理** | ✅ QCAssignment 状态机（Assigned→InProgress→Completed/Cancelled）+ 看板三列 + 同订单同类型唯一进行中 | ⚠️ 验货日历视图 / 任务优先级 | 上游 Order；下游 InspectionReport |
| **中期验货** | ✅ InspectionReport inspectionType=midline（AQL 抽样 + 三级疵点 + 结论 pass/conditional/fail） | ⚠️ 疵点明细逐条记录 | 上游 QCAssignment；下游 Order |
| **终期验货** | ✅ InspectionReport inspectionType=final（AQL 门禁：0 致命 + passRate≥90% + 业务批准）+ qc_shipped 门禁强校验 | ⚠️ 多份终期报告历史版本 | 上游 QCAssignment；下游 ProductionStage qc_shipped |
| **疵点管理** | ✅ 三级疵点计数（critical/major/minor）+ defectSummary 文本描述 | ⚠️ 疵点逐条录入 + 照片 + 疵点趋势分析看板 | 上游 InspectionReport；下游 FactoryProfile 供应商评分 |
| **驻地管理** | ✅ QCLocation CRUD（code @unique + focus 业务线 + 软删引用校验） | — | 无上游；下游 QCAssignment |
| **业务线管理** | ✅ BusinessLine CRUD（MOQ/生产周期/付款条件 + 启停开关） | — | 无上游；下游 Order MOQ 校验 |
| **裁剪前检查** | ✅ PreCutChecklist 四项确认（推码/耗料/样板/产前会议）+ pre_cut_checked 门禁 | — | 上游 Order；下游 ProductionStage pre_cut_checked |
| **供应商质量评分** | ✅ 验货结论自动评分（inspectionScoreForResult → FactoryProfile.recordAutoEvaluation） | ⚠️ 评分趋势看板 | 上游 InspectionReport；下游 FactoryProfile |

---

## §3 模块架构图

```
┌─────────────────────────────────────────────────────────────┐
│                 前端 QcWorkbenchManager.tsx                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │Tab:验货任务  │  │Tab:驻地管理  │  │Tab:业务线    │       │
│  │(三列看板)    │  │(卡片 CRUD)   │  │(表格 CRUD)   │       │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘       │
│         └─────────────────┼─────────────────┘               │
│                           ▼                                   │
│                   apiService (HTTP)                          │
└───────────────────────────┬─────────────────────────────────┘
                            │ /api/v1/qc/*  +  /api/v1/production/*
┌───────────────────────────▼─────────────────────────────────┐
│              后端 qcRoute.ts (Express)                       │
│  ┌─────────────────────────────────────────────────┐         │
│  │         createQcService(prisma)                 │         │
│  │  ┌──────────┐ ┌───────────┐ ┌───────────────┐  │         │
│  │  │驻地 CRUD  │ │任务状态机  │ │工作台聚合查询  │  │         │
│  │  └──────────┘ └───────────┘ └───────────────┘  │         │
│  └─────────────────────────────────────────────────┘         │
│  ┌─────────────────────────────────────────────────┐         │
│  │      stageService.ts (production 路由)           │         │
│  │  ┌─────────────────┐ ┌────────────────────────┐ │         │
│  │  │saveInspectionRep │ │savePreCutChecklist     │ │         │
│  │  │(AQL 报告 + 评分) │ │(裁剪前检查四项)        │ │         │
│  │  └─────────────────┘ └────────────────────────┘ │         │
│  │  ┌─────────────────────────────────────────────┐  │         │
│  │  │advanceStage (qc_shipped 三门禁校验)          │  │         │
│  │  └─────────────────────────────────────────────┘  │         │
│  └─────────────────────────────────────────────────┘         │
└───────────────────────────┬─────────────────────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    PostgreSQL (Prisma)                       │
│  ┌──────────────┐ ┌──────────────┐ ┌─────────────────┐     │
│  │ QCLocation   │ │ QCAssignment │ │ InspectionReport │     │
│  │ (软删)       │ │ (状态机)     │ │ (AQL+三级疵点)   │     │
│  └──────────────┘ └──────────────┘ └─────────────────┘     │
│  ┌──────────────────┐ ┌──────────────┐ ┌────────────────┐  │
│  │ PreCutChecklist  │ │ BusinessLine │ │  FactoryProfile │  │
│  │ (四项确认)       │ │ (MOQ 基准)   │ │ (供应商评分)    │  │
│  └──────────────────┘ └──────────────┘ └────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## §4 核心数据模型速览

| 模型 | 主键格式 | 职责 | 软删 | 关键约束 |
|------|---------|------|------|---------|
| **QCLocation** | `QCL__${shortId}` | 驻地主数据（编码/名称/业务线焦点） | ✅ deletedAt | code @unique；引用校验 |
| **QCAssignment** | `QCA__${shortId}` | 验货任务（订单×类型×QC 人员×状态机） | ✅ deletedAt | 同订单同类型唯一进行中 |
| **InspectionReport** | `INR__${orderId}`(final) / `INR__${orderId}__midline` | AQL 验货报告（三级疵点+结论） | ❌ upsert 覆盖 | @@unique([orderId, inspectionType]) |
| **PreCutChecklist** | `PCL__${orderId}` | 裁剪前检查四项 | ❌ upsert 覆盖 | orderId @unique |
| **BusinessLine** | `BL__${shortId}` | 业务线规则（MOQ/周期/付款） | ✅ deletedAt | code @unique |

---

## §5 验货类型与门禁

### 5.1 两种验货类型

| 类型 | inspectionType | 时机 | 报告 ID 格式 | 门禁关联 |
|------|---------------|------|-------------|---------|
| 中期验货 | `midline` | 生产过程中抽检 | `INR__${orderId}__midline` | 无直接门禁（质量监控） |
| 终期验货 | `final` | 出货前 AQL 全检 | `INR__${orderId}` | qc_shipped 门禁判据 |

### 5.2 生产管线 10 阶段门禁

| 阶段 # | stageKey | 名称 | QC 相关门禁 | 校验逻辑 |
|--------|---------|------|------------|---------|
| 6 | pre_cut_checked | 裁剪前检查 | PreCutChecklist 四项全 true | gradingConfirmed + consumptionConfirmed + patternConfirmed + preProductionMeeting |
| 7 | pp_sample_approved | 产前样确认 | 双签（生产部+业务部） | signedByProduction + signedByBusiness |
| 10 | qc_shipped | 验货发货 | Final AQL 三门禁 | ① criticalDefects=0 ② passRate≥90% ③ defectRate≤3% ④ approvedByBusiness=true |

### 5.3 qc_shipped 门禁校验逻辑

```
advanceStage(orderId, 'qc_shipped') 事务内：
1. 查 InspectionReport (orderId, inspectionType='final')
   └ 不存在 → throw INSPECTION_NOT_QUALIFIED「终期验货报告未建立」
2. result === 'fail' → throw「终期验货结论为不合格，需整改复验」
3. criticalDefects > 0 → throw「存在 N 个致命疵点（AQL 0 零容忍）」
4. passRate = passedUnits / totalUnits < 0.90 → throw「合格率 X% 低于阈值 90%」
5. defectRate = (total - passed) / total > 0.03 → throw「不合格率 X% 超过上限 3%」
6. approvedByBusiness !== true → throw「需业务部批准发货」
── 全部通过 → 推进 qc_shipped 阶段 + 发布 ProductionCompleted 事件 ──
```

**门禁常量**：MIN_PASS_RATE = 0.90 / MAX_DEFECT_RATE = 0.03

---

## §6 验货任务状态机

```
Assigned ──(start)──▶ InProgress ──(complete)──▶ Completed
   │                      │
   └──(cancel)────────────┘──(cancel)──▶ Cancelled
                                    (Completed 不可取消)
```

| 状态 | bds-badge 变体 | 标签 | 可执行操作 |
|------|---------------|------|-----------|
| Assigned | info | 已分配 | 开始 / 完成 / 取消 / 删除 |
| InProgress | warning | 进行中 | 完成 / 取消 / 删除 |
| Completed | success | 已完成 | 删除 |
| Cancelled | neutral | 已取消 | 删除 |

**状态流转校验**：
- start：仅 Assigned → InProgress
- complete：Assigned 或 InProgress → Completed（可关联 reportId）
- cancel：Assigned 或 InProgress → Cancelled（Completed 不可取消）
- 已完结任务（Completed/Cancelled）不可修改字段

**同订单同类型唯一性**：同订单同 inspectionType 仅允许一个非 Cancelled 状态的任务。

---

## §7 前端页面 Tab 结构

QcWorkbenchManager.tsx 采用三 Tab 分区（`bds-tabs` 组件族）：

| Tab | id | 图标 | 功能 | 数据源 API |
|-----|----|------|------|-----------|
| 验货任务 | `assignments` | ClipboardCheck | 三列看板（Assigned/InProgress/Completed）+ QC 人员筛选 + 新建任务 + 开始/完成/取消 | GET /qc/workbench |
| 驻地管理 | `locations` | MapPin | 驻地卡片网格 + 新建/编辑/删除（code 不可改 + 引用校验） | GET /qc/locations |
| 业务线 | `businessLines` | Layers | 业务线规则表格 + MOQ/周期/付款 CRUD + 启停开关 | GET /qc/business-lines |

---

## §8 订单详情页 prime 联动

订单详情页「发起验货」按钮通过 sessionStorage prime 直达 QC 新建任务表单：

```typescript
// OrderManager.tsx → primeQcAssignmentFromOrder(orderId)
// → sessionStorage['bambook_qc_assignment_prime'] = { orderId }
// → 跳转 view='qc-workbench'
// → QcWorkbenchManager 挂载时 readQcAssignmentPrime()
// → 自动打开新建任务表单 + 预选 orderId
```

---

## §9 验货结论与供应商评分联动

```typescript
// saveInspectionReport 内（result 非 null 时）
if (result.result && order.millRelationId) {
  const score = inspectionScoreForResult(result.result, critical);
  await factoryService.recordAutoEvaluation({
    relationId: order.millRelationId,
    kind: 'inspection',
    score,
    sourceType: 'inspectionReport',
    sourceId: result.id,
    note: `验货报告 ${result.id}（订单 ${poNumber}）结论 ${result.result}...`,
  });
}
```

| 验货结论 | critical 疵点 | 评分 score | 说明 |
|---------|--------------|-----------|------|
| pass | 0 | 高分 | 合格 + 无致命疵点 |
| pass | > 0 | 中分 | 合格但有致命疵点（异常） |
| conditional | 0 | 中分 | 有条件合格 |
| conditional | > 0 | 低分 | 有条件合格 + 致命疵点 |
| fail | 0 | 低分 | 不合格 |
| fail | > 0 | 最低分 | 不合格 + 致命疵点 |

---

## §10 终期 fail 广播通知

终期验货结论从非 fail 迁移为 fail 时，广播 critical 级通知（天然幂等）：

| 通知字段 | 值 |
|---------|---|
| type | `inspection_fail` |
| level | `critical` |
| title | `订单 ${poNumber} 终期验货不通过（critical=N, major=M）` |
| body | 疵点明细 + 出运门禁锁定说明 + 整改复验提示 |
| link | `/production?orderId=${orderId}` |
| metadata | entityType=InspectionReport, entityId, orderId, criticalDefects, majorDefects, minorDefects |
| 推送对象 | QC + 业务员 + 管理层 |

> 状态迁移触发天然幂等：重复保存相同 fail 结论不再通知；整改后再次 fail 属新事件，重新通知。

---

## §11 权限矩阵

| 角色 | 查看任务 | 派单/开始/完成 | 取消/删除 | 驻地管理 | 业务线管理 | 验货报告 | 业务批准 |
|------|---------|--------------|----------|---------|----------|---------|---------|
| owner / admin | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| qc | ✅(自己的) | ✅(自己的) | ⚠️(自己的) | ❌ | ❌ | ✅ | ❌ |
| merchandiser | ✅(同部门) | ❌ | ❌ | ❌ | ❌ | ✅(只读) | ❌ |
| sales | ✅(自己的单) | ❌ | ❌ | ❌ | ❌ | ✅(只读) | ✅(自己的单) |
| guest | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |

---

## §12 四态规范

| 状态 | 触发 | UI 规范 |
|------|------|---------|
| **加载态** | loadWorkbench / loadLocations / loadLines 进行中 | Loader2 spin 居中 |
| **空态** | 无任务/无驻地/无业务线 | `bds-empty`：glyph + title 文案 |
| **错误态** | 操作失败 | alert 弹窗显示后端错误消息 |
| **无权限** | listUserAccounts 403 | 降级为手工录入 qcUserId + 提示文案 |

---

## §13 设计系统约束

| 约束 | 合规说明 |
|------|---------|
| 主题透明 | 无 isDarkMode 分支（isDarkMode 可选 prop 但未使用样式分支） |
| bds 组件族 | bds-tabs / bds-card / bds-badge / bds-input / bds-select / bds-modal / bds-empty / bds-table / bds-listrows |
| 状态徽章映射 | QC_STATUS_BADGE_VARIANT → bds-badge 变体 |
| 圆角 | 卡片 rounded-card；按钮 rounded-control |
| 无阴影 flat | panel 使用 backdrop-blur + hairline border |
| 颜色变量 | 全部 CSS 变量 |
| 动画 | framer-motion Tab 切换 y 偏移 + AnimatePresence |

---

## §14 关键业务规则

| # | 规则 | 实现状态 |
|---|------|---------|
| 1 | 同订单同验货类型仅允许一个进行中任务（Cancelled 不占位） | ✅ |
| 2 | 已完结任务（Completed/Cancelled）不可修改字段 | ✅ |
| 3 | 驻地 code 创建后不可修改 | ✅ |
| 4 | 驻地有任务引用时不可删除 | ✅ |
| 5 | 业务线 code 创建后不可修改 | ✅ |
| 6 | PreCutChecklist 四项全 true 才能推进 pre_cut_checked | ✅ |
| 7 | 终期验货 0 致命疵点 + passRate≥90% + defectRate≤3% + 业务批准 → qc_shipped | ✅ |
| 8 | 验货结论自动供应商评分（inspectionScoreForResult） | ✅ |
| 9 | 终期 fail 广播 critical 通知（天然幂等） | ✅ |
| 10 | QC 人员选择器降级（无权限时手工录入 qcUserId） | ✅ |
| 11 | 疵点趋势分析看板（供应商/产品/时间维度） | ⚠️ 规划中 |
| 12 | 疵点逐条录入（当前仅计数 + defectSummary 文本） | ⚠️ 规划中 |

---

## §15 相关文档索引

| 文档 | 相对路径 |
|------|---------|
| 中期验货表单（AQL 抽样 + 三级疵点） | `./中期验货表单.md` |
| 终期验货表单（门禁 3 绑定 + qc_shipped 联动） | `./终期验货表单.md` |
| 疵点趋势分析（供应商/产品/时间维度统计） | `./疵点趋势分析.md` |
| 库存管理模块概述（Lock/Unlock 联动） | `../Inventory-库存管理/模块概述.md` |
| 订单详情页设计（发起验货 prime 联动） | `../Orders-订单管理/订单详情页.md` |
| 质量门禁规则 | `../../03-业务规则/质量门禁规则.md` |
| 类型真源（QCAssignment/InspectionReport） | [types.ts L4361-L4480](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/types.ts) |

---

## §16 DR-029 服装与面料 QC 不同责任闭环 + DR-008 样品内部门禁 + DR-014 并行验收场景

> **决策来源**：[DR-029](../Orders-订单管理/../../../../10-评审与决策/2026-08-16-设计评审决策记录.md#dr-029-服装与面料样品-qc-的不同责任闭环) / [DR-008](../Orders-订单管理/../../../../10-评审与决策/2026-08-16-设计评审决策记录.md#dr-008-高端服装多轮样品与双门禁) / [DR-014](../Orders-订单管理/../../../../10-评审与决策/2026-08-16-设计评审决策记录.md#dr-014-面料大货-qc-与客户样品确认并行)

### 16.1 DR-029 服装样品链 QC 责任闭环（工厂→QC→业务员→客户；每轮重新入 QC）

| 验收编号 | 场景描述 | 前置条件 | 执行步骤 | 期望结果（服装链闭环） | 失败路径 |
|---------|---------|---------|---------|-----------------------|---------|
| QC-29-A1 | 工厂每一轮产前样 → 先交服装 QC 评审（必经节点）；QC 录入内部评审意见（**非压缩通过/不通过二值**，支持文字/疵点分级/照片） | 1. Garment 订单；2. 工厂提交 Round 1 PP；3. QC 已分配任务 | 1. QC 在 QC 工作台打开新任务；2. 录入：critical=0 / major=1 / minor=3 / 文本意见="左右袖长度差 0.5cm，需修正" / 上传 3 张细节照片；3. 保存 | 1. InspectionReport（type=Sample）创建，含非二值评审字段（QC 文本、三级疵点计数、照片 evidence 引用）；2. 任务状态=QC Reviewed；3. 业务员可见 QC 的完整评审意见（不只有 Pass/Fail 红色/绿色徽章）；4. 下一步默认动作=业务员决定是否寄客户 | 将 QC 评审结论只存 status=Pass/Fail（布尔）→ 违反 DR-029 不应压缩为机械二值 |
| QC-29-A2 | QC 通过后业务员决定是否寄客户（QC→业务员转交；非 QC 直接寄），业务员登记寄送与客户确认 | 1. Round 1 QC 意见 status=Pass（无 critical，major=0）；2. 业务员打开样品详情 | 1. 业务员操作「登记寄客户」（填日期/快递单号/收件方）；2. 2 天后录入客户意见=Rejected + 日期 + 渠道=Email + 证据=客户邮件附件截图 + 意见="袖克夫工艺不对，需要对齐" | 1. SampleNode：qcId + 寄送记录 + 客户意见记录 6 字段完整；2. 下一轮必须工厂重做；3. 新样品（Round 2）必须再次先进入 QC（见 A3），不得直接再寄客户 | QC 直接跳过业务员给客户寄 → 违反 DR-029（角色分工：工厂→QC→业务员→客户） |
| QC-29-A3 | 客户不通过 → 工厂重做下一轮 → 新轮必须重新交 QC（强制再入），才可寄客户 | 1. Round 1 客户 Rejected（A2）；2. 工厂出 Round 2 样；3. 业务员希望直接寄客户以加速 | 1. 业务员尝试登记 Round 2 「寄客户」但未先录入 QC 评审 | 1. 系统拦截 RE_INSPECTION_REQUIRED：新一轮样品必须先交 QC 评审；2. Round 2 QC 任务进入 QC 待办；3. QC 评审 Pass 后才允许业务员登记寄送 | 不重新入 QC 直接寄 Round 2 → 违反 DR-029 服装不通过闭环（新样必须再 QC） |
| QC-29-A4 | QC 直接打回工厂重做（明显无法通过）：标记「直接打回」后不得寄客户，通知业务员+生成工厂重做要求+进度可见 | 1. 工厂 Round 3 样明显错误（领型完全错 + 面料用错），QC 判定即使寄客户也会被拒绝；2. QC 评审时选择「直接打回工厂重做」开关（明确评审结论，非隐藏状态修改） | 1. QC 提交评审：critical=2 / major=3 / minor=5 + 文本="返工重做；领型与面料全错，建议重新对封样"；2. 勾选「直接打回工厂重做」（DIRECT_REJECT）；3. 保存 | 1. InspectionReport.status=Rejected / disposition=DIRECT_REJECT；2. 系统给业务员推送通知 + 在样品详情显示「QC 直接打回，原因见评审」；3. 向工厂责任方（工厂业务/跟单）生成重做任务（重做要求关联 QC 评审 ID 与意见快照）；4. 业务员可见打回原因 + 工厂后续进度（重做中/已提交新样）；5. 该批样品不得寄客户（登记寄送按钮 disabled，且后端 SAMPLE_DIRECTLY_REJECTED 错误码 fail-closed） | 只给业务员看文字但允许寄客户 → DR-029 直接打回失败；生成隐藏任务而业务员不可见进度 → 双方责任断裂 |
| QC-29-A5 | 工厂提交新批（DIRECT_REJECT 后重做）→ 强制重新进入 QC 评审队列 | 1. A4 DIRECT_REJECT 批次；2. 工厂按要求重做并提交新样 | 1. 新批次样品 ID 关联原 QC id（round 链）；2. QC 工作台任务列表出现新评审任务 | 1. 新轮 QC 评审任务出现，关联前 DIRECT_REJECT；2. 只有新轮 QC Pass 后才允许再次寄客户；3. 新旧评审可对比（前后差异） | 工厂新样绕过 QC 直接给业务员 → 失败（fail-closed 必须阻断） |

### 16.2 DR-029 面料样品链 QC 责任闭环（业务员登记为主，客户不通过→业务员交给 QC→工厂→新样→业务员）

| 验收编号 | 场景描述 | 前置条件 | 执行步骤 | 期望结果（面料链闭环，区别于服装链） | 失败路径 |
|---------|---------|---------|---------|-------------------------------------|---------|
| QC-29-B1 | 面料样品链（手织样/先锋样/普通小样）全程业务员登记寄送与客户确认；样品 QC 不参与订单前样品（DR-027 与 DR-029 协同） | 1. Fabric 订单前开发样（非大货）；2. 开发样已做；3. 面料 QC 容器登录查看任务列表 | 1. 业务员登记剪样寄送；2. 登记客户确认（结果、日期、渠道、意见、证据） | 1. 面料 QC 工作台默认不出现该样品任务（订单前样品不进 QC，DR-027）；2. 所有寄送/确认动作都由业务员容器完成；3. 服装链的「先 QC 后寄」**不适用于**面料订单前样品链（链不同，DR-029 强边界） | 强制面料样品也走先 QC 后寄 → 两套链被强制统一兼容（违反 DR-029 禁止用统一样品流程） |
| QC-29-B2 | 面料客户不通过 → 业务员把客户意见交给面料 QC → QC 向工厂提技术调整（染整/后整理/修布）→ 工厂新样 → 业务员再次寄客户并登记 | 1. 面料订单客户对面色不通过（意见=偏蓝 0.5 级，需加黄调整）；2. 业务员登录；3. QC 任务列表 | 1. 业务员在样品详情点「转交 QC 跟进」；2. 面料 QC 登录 → 看到任务 + 客户原始意见快照；3. QC 录入对工厂的技术调整要求（染整调整参数 + 工厂责任方）；4. 系统给工厂发送调整任务；5. 工厂出样后业务员收到通知 → 登记寄送 → 客户确认 | 1. 转交事件可追溯：who=业务员、to=某 QC 容器用户、客户意见快照；2. QC 录入的工厂调整要求 6 字段（要求、参数、责任工厂、时间、证据、跟进人）；3. 新样到后业务员收到推送；4. 客户不通过 → QC → 工厂 → 新样 → 业务员 → 客户 5 环节闭环 3 方（业务/Q C/工厂）可见进度；5. 服装链 QC 直接评审样品的动作对该面料链不生效（任务类型不同、路由不同） | 客户反馈不交给 QC，而是业务员直接自己回复客户且无 QC 参与 → 违反 DR-029 面料链分工；服装 QC 被分配到面料任务 → 状态机/权限/路由未做到独立 |
| QC-29-B3 | 服装与面料样品链：数据模型 / 状态机 / 任务路由 / 权限校验 4 处独立（DR-029 强制边界） | 1. 7 容器账号；2. 服装链任务 + 面料链任务各 10 条；3. 4 处代码/配置 | 1. 每个角色各登录；2. 分别访问服装与面料 QC 任务列表；3. 尝试访问属于另一链的节点 API；4. 修改另一链专用字段 | 1. 角色只能看到分配链的任务（服装 QC 看服装；面料 QC 看面料；可同时分配，但内部任务分链存储）；2. 跨链访问 API → 403 CHAIN_BOUNDARY；3. 写入对方链的字段（如 GarmentPP.inspectionResult 用 FabricSS 接口提交）→ fail-closed 400 INVALID_CHAIN_SCOPE；4. 状态机定义独立：服装 PP Round→QC→业务→客户→下轮 与 面料 Dev→客户→QC→工厂→新样→业务→客户 两状态机分开版本 | 4 条边界任一条共享模型/字段复用无隔离 → 违反 DR-029 强制边界 |

### 16.3 DR-008 样品内部门禁 + DR-014 出运 3 条件独立并行验收（QC 与客户确认互不阻断）

| 验收编号 | 场景描述 | 前置条件 | 执行步骤 | 期望结果 | 失败路径 |
|---------|---------|---------|---------|---------|---------|
| QC-008-C1 | 服装样品链：每轮 QC 内部评审通过才允许寄客户（内部门禁 DR-008）；样品「QC 结论」=fail-closed 硬门禁 | 1. GarmentPP Round N QC.status=Fail（major 超标）；2. 业务员尝试登记寄 | 1. 业务员点「登记寄客户」 | 1. 后端拦截 SAMPLE_QC_GATE_NOT_PASSED（DR-008 内部门禁）；2. 前端告知"该轮 QC 未通过，请先让 QC 调整后再寄" | 允许未过 QC 寄客户 → 严重违反 DR-008 内部门禁（fail-closed 必须） |
| QC-014-C2 | 面料大货出运 3 条件独立并行：大货 QC、S/S 客户确认、匹头样（如启用）互不依赖；任一条链未完成 → 正常路径出运拦截；但 QC 与客户确认本身不互相阻塞（DR-014） | 1. 面料订单接近 Exmill Date；2. 当前状态：大货 QC 未完成（InspectionReport.status=Draft）；S/S 已寄送但客户未确认；RC 未启用；3. 业务员先完成 QC、再完成 S/S 确认（或反向顺序，两种各跑一遍） | 1. 场景 ①：先把 QC 改为 Final Pass → 检查 S/S 链条是否可登记客户确认（不被阻塞）；2. 场景 ②：先登记 S/S 客户确认 Approved → 检查 QC 任务列表是否仍可独立提交 Final Report；3. 两条都完成后：尝试申请出运（+订舱放行） | 1. 3 条链状态彼此独立；完成顺序无影响；无"先完成 A 才能编辑 B"的伪依赖；2. 任一条缺失 → 出运 fail-closed（DR-012 门禁）；3. 链条齐全 + 订舱批准 + 放行批准 → Shipment ReadyForRelease 成功；4. 任一链条失败时，前端分别列出 3 条链的状态与责任人，不展示"完成度 %"（避免用户以为达到 67% 即可出运） | 错误设计：S/S 确认后才能打开 QC 录入，或 QC Pass 才能编辑 S/S 客户确认 → 违反 DR-014 并行推进；只显示"1/3 通过，2 待完成"而不分别列 → 用户体验不清，但仍合规（体验问题，非门禁问题） |
| QC-014-C3 | 服装出运：样品 QC（PP Round N）≠ 大货出运 QC（Final）；两道独立不可替代（与 DR-009 §出运门禁呼应） | 1. Garment 订单 PP QC 通过 + 封存；2. 大货生产完成；3. 终期 Final InspectionReport 未提交；4. 业务员尝试提交出运放行 | 1. 申请出运门禁检查 | 1. 后端 fail-closed：FINAL_QC_MISSING；2. 提示「产前样 QC 不可替代大货终期 QC」；3. 允许用户直接跳转到创建终期 Final 验货任务；4. 两条任务各自独立状态与记录 | PP QC 通过直接当 Final QC → 严重违反 DR-009/014 独立门禁原则 |

---

## §17 DR-013 受控例外 × QC 门禁验收（QC Fail / 样品内部门禁 / 客户不接受）

> **决策来源**：[DR-013](../../../../10-评审与决策/2026-08-16-设计评审决策记录.md#dr-013-全局业务门禁的受控例外推进)
>
> **核心边界（DR-029 服装链 × 面料链 × QC 职责）**：QC 容器角色不得自己给自己 EXC 批准绕过 QC Fail（审批人仍走 DR-007=业务主管或总领导或 BOSS；QC 只提供专业意见，无 EXC 审批权）。

### 17.1 QC × EXC 3 类场景验收

| 验收编号 | 场景（服装链 / 面料链 / 样品内部门禁 3 条链的 QC Fail 绕过） | 前置条件 | 执行步骤 | 期望结果（审批权不在 QC；原 InspectionReport 数据不被修改；客户承诺 / 风险金 必须留痕） | 失败路径 |
|---------|------------------------------------------------------|---------|---------|----------------------------------------------------------------------------------|---------|
| QC-EXC-1 | 服装终期 QC Fail（major=1，缺陷=线头未剪/轻微色差不影响穿着，客户紧急要求出货并签字接受；但业务员想绕过 QC Fail 放行出运） | 1. InspectionReport（type=Final Garment，status=Fail）；2. 客户签《瑕疵接受函》；3. 正常 Shipment.release 接口 409：QC_FAILED；4. 前端显示「DR-013 例外申请」入口；5. QC 容器用户登录尝试申请 EXC 审批 Approve | 1. 场景①：QC 容器 Q（服装 QC）构造 EXC 的 Approve 请求（想自己批自己的 Fail 绕过）；2. 场景②：业务员提交 EXC，exceptionCategory=qc_fault；subCategory=defect_rate_over_limit；QC 专业意见字段（必填）=qcProfessionalOpinion=「线头+轻微色差不影响穿着，返工成本过高且交期临近，建议放行」（QC 容器填写）；3. exceptionReason=「客户承诺接受+交期仅 3 天空运窗口，损失超过疵点损失（≥30字）」；4. 张主管 Approve（DR-007 路由） | 1. ① QC Q Approve EXC → 403 SCOPE_DENIED（QC 容器不持 approvals:dr013_exception_approve scope，无审批权，只能填专业意见）；2. ② 业务员提交成功；3. 张主管批准后，InspectionReport.status **仍=Fail**，qcProfessionalOpinion 写入 EXC 的 payload 快照；4. Shipment.releaseReady=true 显示「QC 瑕疵特批（EXC-XXX）- QC专业意见：线头色差...」+ 链接；5. 财务暂扣 3% 尾款（按 riskMitigationPlan 配置）直到客户签收无索赔；6. 下一批同一客户/产品的 QC Final 仍严格按 2% 阈值（不被本 EXC 全局放宽） | QC 容器能自己批 EXC 把自己的 Fail 变 Pass → DR-029 QC 与业务审批的职责分离被破坏（fail-closed 必须：审批权在业务主管；QC 仅填专业意见且无最终审批权） |
| QC-EXC-2 | 面料大货 QC Fail（色差 0.8 级，阈值 0.5；客户是老客户，业务员登记客户电话确认接受色差，附电话录音+客户《色号接受函》EXCEL 盖章；但面料链的 QC 本应不做第一门禁（DR-029 面料链业务登记为主），**仍需 EXC 正式绑定出运放行**，不得仅口头接受就放行 | 1. 面料大货 InspectionReport（Fabric Final）status=Fail，色差 0.8>0.5；2. 客户电话确认接受，业务员登记（录音+附件）；3. Shipment.release 出运 3 条件 QC 条件=Fail；4. S/S 已确认，RC 未启用 | 1. 正常路径放行 → 拦截 QC_FAILED；2. 提交 EXC：exceptionCategory=qc_fault；subCategory=fabric_color_dev_customer_accept；qcProfessionalOpinion=「色差 0.8 偏黄，成衣后不明显」；exceptionReason=「10 年合作老客户承诺全批次接受色差，且赔偿 2 元/米补偿费，附盖章接受函（≥30字）」；3. 面料部 head=王主管审批；4. 通过后绑定 Shipment | 1. 王主管批准前必须看到 QC 专业意见 + 客户接受函附件 + 补偿协议 3 项齐全；缺任一项→EXC 审批按钮 disabled（fail-closed 校验）；2. 批准后面料 QC 的 InspectionReport 色差 0.8 记录仍在 QC 工作台可统计该供应商色差频率（影响供应商评分），不被覆盖；3. 出运 Shipment 级关联 EXC，后续该客户年度 EXC 次数≥阈值（例如 5 次/年）触发供应商管理预警 | 因为面料链业务登记为主，系统允许无 EXC 直接放行（跳过 QC Fail）→ DR-013 正常路径 fail-closed 原则被破坏；EXC 才允许绕过；业务登记为主的含义是"样品 QC 不做第一门禁"≠"大货 QC Fail 直接放行" |
| QC-EXC-3 | 服装样品内部门禁 DR-008（QC-008-C1）：Round N PP QC Fail（major=3超标），客户总部催样品说"先寄来看，不通过再改"，业务员想跳过 QC 内部门禁先寄；QC 本身也同意（为了加速） → 但 **样品内部门禁（QC 通过才寄）是硬规则（DR-008）**，即使 QC 和业务员双方同意也不得直接放行，必须 EXC | 1. SampleNode（Garment PP Round N）QC InspectionReport.status=Fail（major=3超标）；2. 正常路径登记寄客户=SAMPLE_QC_GATE_NOT_PASSED（QC-008-C1 拦截）；3. QC 同意先寄；4. 提供 EXC 入口；5. 寄样申请的 EXC 审批 | 1. 业务员提交 EXC：exceptionCategory=sample_gate；subCategory=garment_pp_qc_fail_customer_urge；exceptionReason=「客户总部催样要求先看版型，明确邮件（附）写了不通过免费重打，不追究交期延误；提前寄样可压缩周期 7 天（≥30字）」；qcProfessionalOpinion=「major 超标为 3 处但客户接受先看版型确认，后续修改工艺可解决」；风险=「版型无修改则节省 7 天；有修改则该轮样品成本损失 500 元」；责任人=「业务员孙跟进客户 3 天内反馈」；2. 张主管 Approve 或 Reject；3. 批准后登记寄送（绑定 EXC） | 1. 无 EXC → 即使 QC 容器在样品界面勾选"同意先寄"也无效，后端依然 SAMPLE_QC_GATE_NOT_PASSED fail-closed（同意先寄的 QC 勾选不生效，必须 EXC + 业务主管审批）；2. EXC 批准后才允许登记寄送；3. 该样品的下一轮 Round N+1 仍必须先 QC 通过才寄（不得默认后续轮也跳过）；4. 样品链 7 字段模型（round 数/QC id/客户意见等）里写入"gateBypassedExcId=EXC-XXX"审计；5. QC 的 major=3 记录仍在，不被 QC 修改 | QC 容器加个"同意先寄"勾选框就绕过样品内部门禁（无 EXC，无业务主管审批）→ 严重违反 DR-008 样品内部门禁（fail-closed 必须：QC 与业务员都无权自己解除门禁，只能提交 EXC 由业务主管解除） |

---

## §18 待补的设计缺口

| # | 缺口 | 优先级 | 建议跟进 |
|---|------|-------|---------|
| 1 | **疵点趋势分析看板**（按供应商/产品/时间维度统计疵点率 + 重复问题预警 + 供应商评分影响） | P1 | 详见 `./疵点趋势分析.md` |
| 2 | **疵点逐条录入**（当前仅 critical/major/minor 计数 + defectSummary 文本，需逐条记录疵点类型/位置/照片） | P2 | 新增 DefectRecord 模型 |
| 3 | **验货日历视图**（按 dueDate 日历展示任务分布） | P2 | 看板 Tab 增加日历视图切换 |
| 4 | **验货报告 PDF 生成**（InspectionReport 导出 PDF） | P2 | 复用 RenderedDoc 模型 |
| 5 | **整改复验流程**（fail 后整改 → 复验 → 覆盖更新报告，当前 upsert 已支持覆盖但无独立整改流程） | P2 | 新增 RectificationTask 模型 |
| 6 | **中期验货与终期验货关联**（中期疵点整改后终期是否复现的追踪） | P3 | InspectionReport 增加 midlineReportId 关联字段 |
| 7 | **QC 人员工作量统计**（按 QC 人员统计任务数/完成率/平均验货时长） | P3 | 报表中心新增 QC 工作量看板 |
| 8 | **验货报告审批流**（当前业务批准为单字段布尔值，规划为多级审批流） | P3 | 复用 ApprovalRequest 模型 |
