# 库存预警 (Inventory Alerts)

## §1 元信息

| 项 | 值 |
|---|---|
| **页面定位** | 库存预警是 InventoryManager「预警」Tab 的主视图 — 低值预警（quantity ≤ minStock）实时看板 + 业务事件总线（StockLowAlarm/StockOverstockAlarm）fire-and-forget 通知触发；呆滞预警与过期预警为规划增强 |
| **入口** | 主导航 Inventory → Tab「预警」(activeTab='alerts')；或库存物料 Tab 中低库存物料行 badge 跳转 |
| **核心角色** | 后勤（含仓储，原仓库管理员，主消费者）、业务员（含采购执行，低库存触发采购）、超级管理员（老板，绝密级可见：超储决策） |
| **范式** | 范式 A — 列表页（预警卡片列表，Tab 标题带 badge 计数） |
| **优先级** | P1 |
| **实现状态** | ✅ 低库存预警已落地（quantity ≤ minStock 实时查询 + Tab badge 计数 + 变动后事件发布 StockLowAlarm/StockOverstockAlarm）；⚠️ 呆滞预警（lastInDate/lastOutDate 滞库天数）与过期预警（批次有效期）为规划增强 |
| **关联 PRD 章节** | §9.10（库存预警规则）、§19.8（库存页面详细设计） |
| **关联代码** | [InventoryManager.tsx L578-L618](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/InventoryManager.tsx) 预警 Tab / [inventoryService.ts getLowStockItems](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/inventory/inventoryService.ts) 后端查询 / [inventoryService.ts L688-L732](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/inventory/inventoryService.ts) 事件发布 |

---

## §2 预警类型矩阵

| 预警类型 | 触发条件 | 当前状态 | 数据源 | 通知方式 |
|---------|---------|---------|--------|---------|
| **低值预警** | `quantity ≤ minStock`（minStock 非 null） | ✅ 已实现 | InventoryItem（minStock 字段） | Tab badge + 事件 StockLowAlarm |
| **超储预警** | `quantity ≥ maxStock`（maxStock 非 null，仅 Inbound 后触发） | ✅ 已实现 | InventoryItem（maxStock 字段） | 事件 StockOverstockAlarm |
| **呆滞预警** | lastInDate/lastOutDate 距今 > 滞库天数阈值（如 90 天） | ⚠️ 规划中 | InventoryItem（lastInDate/lastOutDate） | Tab badge + 定时扫描 |
| **过期预警** | 批次 expiryDate 距今 < 预警天数（如 30 天） | ⚠️ 规划中 | InventoryItem（需增加 expiryDate 字段） | Tab badge + 定时扫描 |

---

## §3 低值预警详解

### 3.1 预警判定逻辑

```
低库存 = (minStock != null) && (Number(quantity) <= Number(minStock))
```

- minStock 为 InventoryItem 字段（Decimal(18,4)? 可空）
- 仅设置了 minStock 的物料参与预警
- `<=` 包含等号（恰好等于 minStock 也预警）

### 3.2 后端查询

```typescript
// inventoryService.ts getLowStockItems()
async function getLowStockItems(): Promise<InventoryItemDetail[]> {
  const items = await prisma.inventoryItem.findMany({
    where: { deletedAt: null, minStock: { not: null } },
    include: { warehouse: true },
  });
  // 跨字段比较 Prisma 不支持，内存过滤兜底
  return items.filter((item: any) => Number(item.quantity) <= Number(item.minStock));
}
```

> ⚠️ Prisma 无法直接做跨字段比较（quantity ≤ minStock），采用先查询再内存过滤的方式。大数据量时需优化为 raw SQL。

### 3.3 前端展示

预警 Tab（activeTab='alerts'）渲染低库存物料卡片列表：

**Tab 标题 badge**：

```tsx
<button onClick={() => setActiveTab('alerts')}>
  <AlertTriangle size={12} />预警
  {lowStockItems.length > 0 && (
    <span className="bds-badge sm danger ml-1">{lowStockItems.length}</span>
  )}
</button>
```

**预警卡片**（`bds-card`）：

```
┌─ ⚠ ─ materialCode ─ description ─────────── 400 / 500 YD ─┐
│                                              仓库：主仓     当前 / 最低      │
└──────────────────────────────────────────────────────────────┘
```

| 区域 | 内容 | 样式 |
|------|------|------|
| 左图标 | AlertTriangle | danger-text 16px |
| 主区 | materialCode（bds-mono）+ description（text-secondary） | — |
| 副区 | warehouse.name | text-tertiary 12px |
| 右数值 | `quantity / minStock unit` | danger-text bds-tnum + 「当前 / 最低」10px 标签 |

### 3.4 数据拉取时机

```typescript
// 挂载时 + activeTab 切换到 alerts 时
useEffect(() => {
  if (activeTab === 'alerts') fetchLowStock();
}, [activeTab, fetchLowStock]);

// 库存变动后刷新预警（handleCreateMovement 成功后）
await fetchLowStock();
```

---

## §4 超储预警详解

### 4.1 预警判定逻辑

```
超储 = (maxStock != null) && (Number(quantity) >= Number(maxStock)) && (type === 'Inbound')
```

- maxStock 为 InventoryItem 字段（Decimal(18,4)? 可空）
- 仅 Inbound（入库）变动后触发（出库不会触发超储预警）
- `>=` 包含等号

### 4.2 事件发布

```typescript
// inventoryService.ts createStockMovement 事务提交后
if (item.maxStock != null && newQty >= Number(item.maxStock) && input.type === 'Inbound') {
  businessEventBus.publish({
    id: `bev_stock_over_${now}_${random}`,
    type: 'StockOverstockAlarm',
    sourceEntityType: 'InventoryItem',
    sourceEntityId: input.itemId,
    payload: {
      itemId, description, materialCode,
      warehouseCode, warehouseName,
      currentQty: newQty, maxStock: Number(item.maxStock), unit,
    },
    occurredAt: now,
    actorId: actorId || 'system',
  });
}
```

### 4.3 超储预警前端展示

> ⚠️ 当前超储预警仅发布事件，前端预警 Tab 未展示超储物料。需增加超储物料查询端点。

**规划设计**：

- 新增 `GET /api/v1/inventory/alerts/overstock` 端点
- 预警 Tab 增加子分区（低库存 / 超储 切换或合并展示）
- 超储卡片使用 warning 语义徽章（区别于低库存 danger）

---

## §5 预警阈值配置

### 5.1 当前阈值配置方式

阈值通过 InventoryItem 的 minStock / maxStock 字段配置：

| 字段 | 类型 | 配置入口 | 说明 |
|------|------|---------|------|
| minStock | Decimal(18,4)? | 新建物料表单 / 编辑物料（PUT /items/:id） | 最低库存预警线 |
| maxStock | Decimal(18,4)? | 新建物料表单 / 编辑物料 | 最高库存预警线 |

### 5.2 新建物料表单阈值字段

```
┌ 新增库存物料 ─────────────────────────────────────┐
│ ... 其他字段 ...                                   │
│ ┌最低库存────────┐ ┌最高库存────────┐              │
│ │ [    500    ] │ │ [   5000   ] │              │
│ └───────────────┘ └───────────────┘              │
│ (留空则不参与预警)                                  │
└──────────────────────────────────────────────────────┘
```

- 最低/最高库存留空 → 该物料不参与预警
- 填写数值 → 变动后自动校验并触发事件

### 5.3 阈值配置规则（规划增强）

> ⚠️ 当前阈值为物料级（每个 InventoryItem 独立设置）。规划为多级配置：

| 配置层级 | 粒度 | 优先级 | 说明 |
|---------|------|--------|------|
| 物料级（当前） | InventoryItem.minStock/maxStock | 最高 | 单个物料批次独立阈值 |
| 品类级（规划） | category × minStock/maxStock | 中 | 按品类批量设阈值 |
| 仓库级（规划） | warehouse × minStock/maxStock | 低 | 按仓库容量设阈值 |
| 全局级（规划） | SystemConfig 键值对 | 兜底 | 全局默认阈值 |

---

## §6 通知触发机制

### 6.1 事件总线发布（fire-and-forget）

```
库存变动事务提交
  ↓
inventoryService.createStockMovement 内：
  if (newQty <= minStock) → publish StockLowAlarm
  if (newQty >= maxStock && type=Inbound) → publish StockOverstockAlarm
  ↓ (try/catch 包裹，失败不阻断业务)
businessEventBus.publish()
  ↓
NotificationService 消费 → broadcastNotification（推送通知）
```

### 6.2 StockLowAlarm 事件 payload

```json
{
  "id": "bev_stock_low_1234567890_abc123",
  "type": "StockLowAlarm",
  "sourceEntityType": "InventoryItem",
  "sourceEntityId": "INV_xxx",
  "payload": {
    "itemId": "INV_xxx",
    "description": "面料 A",
    "materialCode": "FAB-001",
    "warehouseCode": "WH-001",
    "warehouseName": "主仓",
    "currentQty": 400,
    "minStock": 500,
    "unit": "YD",
    "movementType": "Outbound"
  },
  "occurredAt": 1234567890,
  "actorId": "user_xxx"
}
```

### 6.3 StockOverstockAlarm 事件 payload

```json
{
  "id": "bev_stock_over_1234567890_abc123",
  "type": "StockOverstockAlarm",
  "sourceEntityType": "InventoryItem",
  "sourceEntityId": "INV_xxx",
  "payload": {
    "itemId": "INV_xxx",
    "description": "面料 A",
    "materialCode": "FAB-001",
    "warehouseCode": "WH-001",
    "warehouseName": "主仓",
    "currentQty": 5500,
    "maxStock": 5000,
    "unit": "YD"
  },
  "occurredAt": 1234567890,
  "actorId": "user_xxx"
}
```

### 6.4 事件消费（NotificationService）

> ⚠️ 事件发布已实现，NotificationService 消费端通知推送为规划集成。

| 事件 | 通知类型 | 推送对象 | 推送方式 |
|------|---------|---------|---------|
| StockLowAlarm | inventory_low_stock | 仓库管理员 + 采购员 | 站内通知 + 邮件 |
| StockOverstockAlarm | inventory_overstock | 仓库管理员 + 管理层 | 站内通知 |

---

## §7 呆滞预警（规划增强）

> ⚠️ 呆滞预警尚未实现，以下为规划设计。

### 7.1 呆滞判定逻辑

```
呆滞 = (lastInDate != null || lastOutDate != null) && (距今天数 > 呆滞阈值)
呆滞天数 = min(today - lastInDate, today - lastOutDate)  // 取最近一次变动距今
```

| 呆滞等级 | 天数范围 | 语义 |
|---------|---------|------|
| 轻度呆滞 | 60-90 天 | warning |
| 中度呆滞 | 90-180 天 | warning（加粗） |
| 重度呆滞 | > 180 天 | danger |

### 7.2 呆滞预警查询（规划）

```typescript
// 规划端点：GET /api/v1/inventory/alerts/stagnant?days=90
async function getStagnantItems(days: number = 90): Promise<InventoryItemDetail[]> {
  const threshold = new Date(Date.now() - days * 86400000).toISOString().slice(0, 10);
  const items = await prisma.inventoryItem.findMany({
    where: {
      deletedAt: null,
      quantity: { gt: 0 },
      OR: [
        { lastInDate: { lt: threshold } },
        { lastOutDate: { lt: threshold } },
      ],
    },
    include: { warehouse: true },
  });
  return items;
}
```

### 7.3 呆滞阈值配置（规划）

| 配置项 | 默认值 | 配置方式 |
|--------|--------|---------|
| 呆滞预警天数 | 90 天 | SystemConfig `inventory.stagnant.days` |
| 重度呆滞天数 | 180 天 | SystemConfig `inventory.stagnant.critical.days` |

---

## §8 过期预警（规划增强）

> ⚠️ 过期预警尚未实现，需 InventoryItem 增加 expiryDate 字段。

### 8.1 过期判定逻辑

```
即将过期 = (expiryDate != null) && (expiryDate - today < 预警天数)
已过期 = (expiryDate != null) && (expiryDate < today)
```

### 8.2 需新增字段

```prisma
model InventoryItem {
  // ... 现有字段 ...
  expiryDate String? // 批次有效期 YYYY-MM-DD（需新增）
}
```

### 8.3 过期预警查询（规划）

```typescript
// 规划端点：GET /api/v1/inventory/alerts/expiring?days=30
async function getExpiringItems(days: number = 30): Promise<InventoryItemDetail[]> {
  const today = new Date().toISOString().slice(0, 10);
  const threshold = new Date(Date.now() + days * 86400000).toISOString().slice(0, 10);
  return prisma.inventoryItem.findMany({
    where: {
      deletedAt: null,
      quantity: { gt: 0 },
      expiryDate: { not: null, gte: today, lte: threshold },
    },
    include: { warehouse: true },
  });
}
```

---

## §9 API 端点清单

| 方法 | 路径 | 说明 | 状态 |
|------|------|------|------|
| GET | `/api/v1/inventory/alerts/low-stock` | 低库存预警列表 | ✅ |
| GET | `/api/v1/inventory/alerts/overstock` | 超储预警列表 | ⚠️ 规划 |
| GET | `/api/v1/inventory/alerts/stagnant?days=90` | 呆滞预警列表 | ⚠️ 规划 |
| GET | `/api/v1/inventory/alerts/expiring?days=30` | 过期预警列表 | ⚠️ 规划 |

---

## §10 权限矩阵

| 角色 | 查看预警 | 配置阈值 | 接收通知 | 处理预警（补货/清库） |
|------|---------|---------|---------|---------------------|
| owner / admin | ✅ | ✅ | ✅ | ✅ |
| warehouse | ✅ | ✅ | ✅ | ✅ |
| merchandiser | ✅ | ❌ | ✅ | ⚠️ 仅上报 |
| qc | ✅ | ❌ | ✅ | ❌ |
| finance | ✅ | ❌ | ✅ | ❌ |
| sales | ✅ | ❌ | ⚠️ 仅关联物料 | ❌ |

---

## §11 四态规范

| 状态 | 触发 | UI 规范 |
|------|------|---------|
| **加载态** | fetchLowStock 进行中 | 无独立加载态（静默刷新，Tab 标题无 spin） |
| **空态** | lowStockItems.length === 0 | `bds-empty`：glyph(AlertTriangle) + title「暂无库存预警」 |
| **错误态** | fetch 失败 | 静默（catch 内无错误展示） |
| **无权限** | 后端 403 | ⚠️ 静默处理 |

---

## §12 移动端适配

| 区域 | 桌面端 | 移动端 |
|------|--------|--------|
| 预警卡片 | flex 横排 | flex-wrap（数值区换行） |
| Tab badge | 固定在 Tab 标题右侧 | 同桌面 |

---

## §13 业务规则关联

| # | 规则 | 实现状态 |
|---|------|---------|
| 1 | 低库存判定 quantity ≤ minStock（minStock 非 null） | ✅ |
| 2 | 超储判定 quantity ≥ maxStock（maxStock 非 null，仅 Inbound 触发） | ✅ |
| 3 | 事件发布 fire-and-forget（发布失败不阻断业务） | ✅ |
| 4 | 库存变动后自动刷新预警列表 | ✅ |
| 5 | Tab 标题 badge 计数（lowStockItems.length） | ✅ |
| 6 | 低库存物料行内 badge 标记（库存物料 Tab） | ✅ |
| 7 | 呆滞预警（lastInDate/lastOutDate 滞库天数） | ⚠️ 规划中 |
| 8 | 过期预警（批次 expiryDate） | ⚠️ 规划中（需新增字段） |
| 9 | 阈值多级配置（物料级/品类级/仓库级/全局级） | ⚠️ 当前仅物料级 |
| 10 | NotificationService 消费事件推送通知 | ⚠️ 事件已发布，消费端待集成 |

---

## §14 可访问性 & 交互细节

| 项 | 规范 |
|----|------|
| **Tab badge** | lowStockItems.length > 0 时显示 bds-badge sm danger 计数 |
| **刷新按钮** | 预警 Tab 右上角 bds-btn ghost icon（RefreshCw，loading 时 spin） |
| **预警卡片动画** | `transition={{ delay: index * 0.03 }}`（stagger 入场） |
| **数值格式化** | `formatQty(quantity) / formatQty(minStock) unit`（千分位 + 单位） |
| **数值颜色** | danger-text（红色强调低库存严重性） |
| **空态文案** | 「暂无库存预警」（正向文案，表示库存健康） |

---

## §15 相关文档索引

| 文档 | 相对路径 |
|------|---------|
| 库存管理模块概述 | `./模块概述.md` |
| 库存总览（物料列表低库存 badge） | `./库存总览.md` |
| 出入库单据（变动后事件触发） | `./出入库单据.md` |
| 盘点与调拨（盘点后预警刷新） | `./盘点与调拨.md` |
| 订单详情页设计（库存联动 prime） | `../Orders-订单管理/订单详情页.md` |
| QC 质检中心模块概述 | `../QcWorkbench-QC质检中心/模块概述.md` |
| 事件总线真源 | [businessEventBus.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/events/businessEventBus.ts) |

---

## §16 待补的设计缺口

| # | 缺口 | 优先级 | 建议补充 |
|---|------|-------|---------|
| 1 | **呆滞预警**（lastInDate/lastOutDate 滞库天数超阈值，按 60/90/180 天分级） | P2 | 新增 GET /alerts/stagnant + 预警 Tab 子分区 |
| 2 | **过期预警**（InventoryItem 新增 expiryDate 字段 + 即将过期/已过期查询） | P2 | schema 迁移 + 新增 GET /alerts/expiring |
| 3 | **超储预警前端展示**（当前仅发布事件，预警 Tab 未展示超储物料） | P1 | 新增 GET /alerts/overstock + 预警 Tab 展示 |
| 4 | **阈值多级配置**（品类级/仓库级/全局级配置，当前仅物料级） | P2 | SystemConfig + 品类/仓库阈值表 |
| 5 | **NotificationService 消费集成**（事件已发布，通知推送消费端待接入） | P1 | NotificationService 订阅 StockLowAlarm/StockOverstockAlarm |
| 6 | **定时扫描预警**（当前仅变动后触发，需定时全量扫描补漏） | P2 | 定时任务每日扫描 + 发布预警事件 |
| 7 | **预警处理闭环**（预警 → 创建采购单/调拨单/报废单 → 自动消除预警） | P2 | 预警卡片增加「处理」按钮 → 跳转对应模块 |
| 8 | **低库存查询性能优化**（当前内存过滤，大数据量需 raw SQL） | P1 | `WHERE quantity <= minStock` raw SQL 替代内存过滤 |
