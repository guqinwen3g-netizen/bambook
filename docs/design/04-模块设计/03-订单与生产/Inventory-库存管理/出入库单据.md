# 出入库单据 (Stock Movement Documents)

## §1 元信息

| 项 | 值 |
|---|---|
| **页面定位** | 出入库单据是库存变动的执行载体 — 入库（采购到货/生产入库/退货入库）、出库（生产领料/销售出库/调拨出库）、锁定/解锁（验货预留）的统一流水入口，以 StockMovement 模型承载全量变动审计 |
| **入口** | 库存总览 → 物料卡片展开 → 「库存变动」按钮 → 内嵌变动表单 |
| **核心角色** | 后勤（含仓储，原仓库管理员，全型操作）、业务员（含采购执行、跟单：入库、出库/调拨）、QC（锁定/解锁，不细分） |
| **范式** | 范式 C — 内嵌表单（非弹窗，AnimatePresence height auto 展开） |
| **优先级** | P1 |
| **实现状态** | ✅ 核心已落地（StockMovement 6 型 + 事务原子性 + balanceBefore/After 审计 + AuditLog 双写 + Transfer 双仓联动 + fail-closed 库存不足校验）；⚠️ PreReceiptNotice（预到货通知）/GoodsReceiptNote（正式入库单）为规划增强，当前以裸 StockMovement Inbound 承载入库 |
| **关联 PRD 章节** | §6.3（库存管理出入库）、§9.10（库存预警规则） |
| **关联代码** | [InventoryManager.tsx L419-L452](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/InventoryManager.tsx) 变动表单 / [inventoryService.ts createStockMovement](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/inventory/inventoryService.ts) 后端事务 |

---

## §2 StockMovement 模型全量字段

| 字段 | 类型 | 说明 | 校验/约束 |
|------|------|------|-----------|
| id | String @id | 格式 `SM_${shortId}` | 系统生成 |
| movementNumber | String | 流水单号（如 `SM-20260806-A1B2`） | 系统生成（日期+随机） |
| type | String | Inbound/Outbound/Transfer/Adjustment/Lock/Unlock | `validateMovementType` 白名单校验 |
| itemId | String | 关联 InventoryItem | 必须存在且未软删 |
| warehouseId | String | 操作仓库（源仓库） | 必须存在且未软删 |
| targetWarehouseId | String? | 调拨目标仓库（仅 Transfer） | Transfer 时必填 + 目标仓活跃 |
| quantity | Decimal(18,4) | 变动数量 | Inbound/Outbound/Transfer/Lock/Unlock > 0；Adjustment 可为 0 |
| unit | String | 单位（继承自 InventoryItem 或传入） | YD/M/KG/PC/SET |
| unitCost | Decimal(18,4)? | 变动时单位成本 | 可选 |
| balanceBefore | Decimal(18,4) | 变动前余额 | 系统计算 = currentQty |
| balanceAfter | Decimal(18,4) | 变动后余额 | 系统计算（按 type 推导） |
| reason | String? | 变动原因 | 可选（建议填写） |
| referenceType | String? | 来源单据类型 | PurchaseOrder/Order/Shipment/ProductionStage/Adjustment/Manual |
| referenceId | String? | 来源单据 ID | 与 referenceType 配对 |
| operator | String? | 操作人 | 从 actorId 传入 |
| movementDate | String | 变动日期 YYYY-MM-DD | 默认今日 |
| notes | String? | 备注 | 可选 |
| createdAt | BigInt | 创建时间戳 | 系统生成（不可变） |

**索引**：itemId / type / warehouseId / movementDate / (referenceType, referenceId)

> ⚠️ StockMovement 不软删 — 流水一旦创建即永久留痕，修改变动 = 新增一条反向流水（Adjustment）。

---

## §3 六型变动详解

### 3.1 入库 Inbound

| 项 | 说明 |
|----|------|
| 余额影响 | `quantity += moveQty` |
| 日期更新 | `lastInDate = movementDate` |
| lockedQuantity | 不变 |
| 校验 | 无前置校验（正数即可入库） |
| 业务场景 | 采购到货入库（referenceType=PurchaseOrder）/ 生产入库（referenceType=ProductionStage）/ 退货入库（referenceType=Order, reason=退货）/ 初始入库（createInventoryItem 时 quantity > 0 自动生成） |
| 前端徽章 | success 绿 + ArrowDownToLine 图标 |

### 3.2 出库 Outbound

| 项 | 说明 |
|----|------|
| 余额影响 | `quantity -= moveQty` |
| 日期更新 | `lastOutDate = movementDate` |
| lockedQuantity | 不变 |
| 校验 | **fail-closed**：`moveQty > currentQty` → throw「库存不足：当前 X unit，尝试出库 Y unit」 |
| 业务场景 | 生产领料（referenceType=Order）/ 销售出库（referenceType=Shipment）/ 调拨出库（见 Transfer）/ 报废出库（reason=报废） |
| 前端徽章 | warning 橙 + ArrowUpFromLine 图标 |

### 3.3 调拨 Transfer

| 项 | 说明 |
|----|------|
| 余额影响 | 源仓 `quantity -= moveQty` / 目标仓 `quantity += moveQty`（同事务） |
| 日期更新 | 源仓 lastOutDate / 目标仓 lastInDate |
| lockedQuantity | 两仓均不变 |
| 校验 | 必须指定 targetWarehouseId + 源仓库存充足 + 目标仓活跃 |
| 双仓联动 | ① 源仓写 Outbound 流水（balanceBefore/After）② 目标仓：已有同物料批次项则累加，无则新建 InventoryItem ③ 目标仓写 Inbound 流水（referenceType=Transfer, referenceId=源流水 ID） |
| 业务场景 | 跨仓调拨（主仓→辅仓 / 工厂仓→临时仓） |
| 前端徽章 | info 蓝 + ArrowLeftRight 图标 |

**Transfer 事务执行流程**：

```
事务开始
├─ 1. 创建源仓 StockMovement（type=Transfer, balanceBefore/After）
├─ 2. 更新源仓 InventoryItem（quantity -= moveQty, lastOutDate）
├─ 3. 查找目标仓同物料批次 InventoryItem
│   ├─ 找到 → update（quantity += moveQty, lastInDate）
│   └─ 未找到 → create（新建 InventoryItem，notes="从仓库 {源仓code} 调入"）
├─ 4. 创建目标仓 StockMovement（type=Inbound, referenceType=Transfer, referenceId=源流水id）
├─ 5. 写 AuditLog（operationType=decrement, beforeValue/afterValue）
事务提交
├─ 6. [fire-and-forget] StockLowAlarm / StockOverstockAlarm 事件发布
```

### 3.4 盘点 Adjustment

| 项 | 说明 |
|----|------|
| 余额影响 | `quantity = moveQty`（绝对值覆盖，非增量） |
| 日期更新 | 不更新 lastInDate/lastOutDate |
| lockedQuantity | 不变 |
| 校验 | 无前置校验（可为 0，表示清零） |
| 业务场景 | 实地盘点后差异核销（reason=盘点, notes=盘点备注） |
| 前端徽章 | neutral 灰 + ClipboardCheck 图标 |

> ⚠️ 当前盘点以 Adjustment 承载，无独立 StockCountTask 任务化流程。详见 `./盘点与调拨.md`。

### 3.5 锁定 Lock

| 项 | 说明 |
|----|------|
| 余额影响 | `quantity` 不变 |
| lockedQuantity | `lockedQuantity += moveQty` |
| 校验 | **fail-closed**：`lockedQty + moveQty > currentQty` → throw「可锁定库存不足」 |
| 业务场景 | 验货预留（referenceType=InspectionReport）/ 订单预留（referenceType=Order） |
| 前端徽章 | warning 橙 + Lock 图标 |

### 3.6 解锁 Unlock

| 项 | 说明 |
|----|------|
| 余额影响 | `quantity` 不变 |
| lockedQuantity | `lockedQuantity -= moveQty` |
| 校验 | **fail-closed**：`moveQty > lockedQty` → throw「解锁数量超过已锁定」 |
| 业务场景 | 验货取消预留 / 订单取消后释放 |
| 前端徽章 | success 绿 + Unlock 图标 |

---

## §4 变动表单 UI

内嵌展开表单（`grid-cols-2 xl:grid-cols-4 gap-2 mb-2`）：

| 位置 | 字段 | 控件 | 必填 | 条件显示 |
|------|------|------|------|---------|
| 1 | 类型 | bds-select sm | ✅ | 始终（6 型可选） |
| 2 | 数量 | bds-input number sm | ✅ | 始终 |
| 3 | 日期 | bds-input date sm | ✅ | 始终（默认今日 YYYY-MM-DD） |
| 4 | 原因 | bds-input text sm | ❌ | 始终 |
| 5 | 目标仓库 | bds-select sm（xl:col-span-2） | ✅(Transfer) | **仅 type=Transfer** |
| 6 | 盘点备注 | bds-input text sm（xl:col-span-2） | ❌ | **仅 type=Adjustment** |

**操作按钮**：

| 按钮 | 样式 | 行为 |
|------|------|------|
| 取消 | bds-btn ghost sm | 收起表单 + 清除 movementError |
| 执行变动 | bds-btn primary sm | 提交 → actionLoading 期间禁用 + Loader2 spin |

---

## §5 流水查询

**GET /api/v1/inventory/movements** 查询参数：

| 参数 | 类型 | 说明 |
|------|------|------|
| itemId | string? | 按物料过滤 |
| warehouseId | string? | 按仓库过滤 |
| type | string? | 按变动类型过滤 |
| dateFrom | string? | movementDate >= YYYY-MM-DD |
| dateTo | string? | movementDate <= YYYY-MM-DD |
| limit | number? | 默认 100，上限 500 |
| offset | number? | 分页偏移 |

**排序**：`orderBy: { createdAt: 'desc' }`（最新在上）

**前端展示**：物料展开详情中展示最近 20 条（`listStockMovements({ itemId, limit: 20 })`），卡片内仅渲染前 8 条。

---

## §6 自动联动场景（规划增强）

> ⚠️ 以下联动场景当前需手动录入变动，规划为系统自动联动。

| 上游触发 | 自动变动 | 联动方式 | 状态 |
|---------|---------|---------|------|
| 采购单到货确认（PurchaseOrder.status=Received） | 自动 Inbound（referenceType=PurchaseOrder） | 采购模块 → 事件总线 → 库存模块 | ⚠️ 未联动 |
| 生产阶段 materials_arrived 完成 | 自动 Inbound（referenceType=ProductionStage） | 生产管线 → 事件总线 → 库存模块 | ⚠️ 未联动 |
| 订单出运创建（Shipment.status=Booked） | 自动 Outbound（referenceType=Shipment） | 出运模块 → 事件总线 → 库存模块 | ⚠️ 未联动 |
| QC 验货任务开始 | 自动 Lock（referenceType=InspectionReport） | QC 模块 → 事件总线 → 库存模块 | ⚠️ 未联动 |
| QC 验货任务取消 | 自动 Unlock（referenceType=InspectionReport） | QC 模块 → 事件总线 → 库存模块 | ⚠️ 未联动 |
| 退货入库 | 自动 Inbound（referenceType=Order, reason=退货） | 订单退货流程 → 库存模块 | ⚠️ 未联动 |

---

## §7 事务原子性与 fail-closed

**核心原则**：所有变动在 `prisma.$transaction` 内完成，保证：
1. **流水与余额原子性** — StockMovement 创建 + InventoryItem 更新同事务，不会出现有流水无余额更新
2. **Transfer 双仓原子性** — 源仓出库 + 目标仓入库同事务，不会出现调拨半途丢失
3. **审计留痕原子性** — AuditLog 与变动同事务，不会出现有变动无审计
4. **fail-closed** — 库存不足时 throw → 整个事务回滚，不留半途数据

**数值精度**：`toDecimal(n) = Math.round(n * 10000) / 10000`（4 位小数）

---

## §8 API 端点清单

| 方法 | 路径 | 说明 | Auth |
|------|------|------|------|
| GET | `/api/v1/inventory/movements` | 流水查询（多维筛选+分页） | JWT 读 |
| POST | `/api/v1/inventory/movements` | 库存变动（6 型，事务+审计+事件） | JWT 写 |

**POST /movements 请求体**：

```json
{
  "itemId": "INV_xxx",
  "type": "Inbound",
  "quantity": 1000,
  "unit": "YD",
  "unitCost": 12.5,
  "targetWarehouseId": null,
  "reason": "采购到货",
  "referenceType": "PurchaseOrder",
  "referenceId": "PO_xxx",
  "movementDate": "2026-08-15",
  "notes": "批号 BN-001"
}
```

---

## §9 权限矩阵

| 角色 | Inbound 入库 | Outbound 出库 | Transfer 调拨 | Adjustment 盘点 | Lock/Unlock | 流水查看 |
|------|------------|-------------|-------------|----------------|-------------|---------|
| owner / admin | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| warehouse | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| merchandiser | ❌ | ✅ | ✅ | ❌ | ❌ | ✅(同部门) |
| qc | ❌ | ❌ | ❌ | ❌ | ✅ | ✅(同部门) |
| finance | ❌ | ❌ | ❌ | ❌ | ❌ | ✅(成本只读) |

---

## §10 四态规范

| 状态 | 触发 | UI 规范 |
|------|------|---------|
| **加载态** | 执行变动中（actionLoading） | 按钮 Loader2 spin + 禁用 |
| **空态** | 流水为空 | 展开详情中不渲染流水区域（仅显示物料信息+变动表单） |
| **错误态** | 变动失败（库存不足/类型非法） | `text-xs danger-text` 内嵌文案（不收起表单，保留输入） |
| **无权限** | 后端 403 | ⚠️ 当前与错误态合并 |

---

## §11 移动端适配

| 区域 | 桌面端 | 移动端 |
|------|--------|--------|
| 变动表单 | grid-cols-2 xl:grid-cols-4 | grid-cols-2 |
| 流水列表 | 每行 flex 横排 | 每行 flex-wrap（余额箭头换行） |
| 执行按钮 | 右侧 justify-end | 全宽 |

---

## §12 业务规则关联

| # | 规则 | 实现状态 |
|---|------|---------|
| 1 | 余额不可为负（Outbound/Transfer/Lock 不足校验，fail-closed） | ✅ |
| 2 | 所有变动写流水 + 审计日志（StockMovement + AuditLog 同事务双写） | ✅ |
| 3 | Transfer 双仓联动（源仓出库 + 目标仓建项/累加 + 双流水，同事务） | ✅ |
| 4 | 初始入库自动流水（createInventoryItem quantity > 0 时自动生成 Inbound 流水） | ✅ |
| 5 | 变动类型白名单校验（validateMovementType，非法类型 throw） | ✅ |
| 6 | 数值精度 4 位小数 | ✅ |
| 7 | 事件发布 fire-and-forget（StockLowAlarm/StockOverstockAlarm 不阻断业务） | ✅ |
| 8 | 采购到货自动入库（PurchaseOrder → StockMovement Inbound 联动） | ⚠️ 未联动 |
| 9 | 出运创建自动出库（Shipment → StockMovement Outbound 联动） | ⚠️ 未联动 |

---

## §13 可访问性 & 交互细节

| 项 | 规范 |
|----|------|
| **类型切换条件字段** | type=Transfer 时目标仓库下拉淡入；type=Adjustment 时盘点备注输入淡入 |
| **目标仓库过滤** | Transfer 目标仓库下拉仅显示非当前仓库（`warehouses.filter(w => w.id !== item.warehouseId)`） |
| **表单重置** | 提交成功后 movementForm 重置为默认值（type=Inbound, quantity=0, movementDate=今日） |
| **错误不丢稿** | 变动失败时 movementError 显示文案，movementForm 保留用户输入 |
| **变动按钮文字切换** | movementTargetId === item.id 时按钮文字变为「收起」 |
| **流水余额箭头** | `balanceBefore → balanceAfter` 使用 bds-tnum 对齐 |

---

## §14 设计系统约束

| 约束 | 合规说明 |
|------|---------|
| 主题透明 | 无 isDarkMode 分支 |
| bds 组件族 | bds-select / bds-input / bds-btn / bds-badge |
| 语义徽章 | MOVEMENT_TYPES.semantic → bds-badge 变体 |
| 圆角 | 表单容器 rounded-inset；按钮 rounded-control |
| 无阴影 flat | 内嵌表单 background var(--bg-panel) + 无 box-shadow |
| 颜色变量 | 全部 CSS 变量（var(--danger-text) / var(--text-tertiary) 等） |
| 动画 | AnimatePresence height auto 展开（duration 0.2s） |

---

## §15 相关文档索引

| 文档 | 相对路径 |
|------|---------|
| 库存管理模块概述 | `./模块概述.md` |
| 库存总览（物料列表+展开详情） | `./库存总览.md` |
| 盘点与调拨（Adjustment/Transfer 深度设计） | `./盘点与调拨.md` |
| 库存预警（变动后事件触发） | `./库存预警.md` |
| 订单详情页设计（履约链联动 prime） | `../Orders-订单管理/订单详情页.md` |
| QC 质检中心模块概述（Lock/Unlock 联动） | `../QcWorkbench-QC质检中心/模块概述.md` |
| 数据模型真源 | [schema.prisma StockMovement](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/prisma/schema.prisma) |

---

## §16 待补的设计缺口

| # | 缺口 | 优先级 | 建议补充 |
|---|------|-------|---------|
| 1 | **PreReceiptNotice 预到货通知模型**（供应商发货通知 → 到货对照清单 → 入库单） | P1 | 新增模型：通知单号/供应商/预计到货日/物料清单/状态(Notified/Received/Cancelled) |
| 2 | **GoodsReceiptNote 正式入库单**（替代裸 Inbound，带状态机 Draft→QC放行→Posted + 关联 PreReceiptNotice） | P1 | 新增模型：GRN号/供应商/仓库/物料行/状态/QC放行标志/关联StockMovement |
| 3 | **采购到货自动联动**（PurchaseOrder.status=Received → 自动 Inbound + referenceType=PurchaseOrder） | P1 | 事件总线订阅 + 库存模块消费 |
| 4 | **出运创建自动出库**（Shipment.status=Booked → 自动 Outbound + referenceType=Shipment） | P1 | 事件总线订阅 |
| 5 | **批量入库/出库**（一次操作多物料行，当前仅单物料变动） | P2 | 新增 POST /movements/batch 端点 |
| 6 | **变动审批流**（大额出库/调拨需审批，当前无审批直接执行） | P2 | 复用 ApprovalRequest 模型 |
| 7 | **流水导出**（Excel 导出变动流水） | P2 | GET /movements/export |
| 8 | **退货入库独立流程**（当前需手动录入 Inbound + reason=退货） | P2 | 退货模块联动 |
