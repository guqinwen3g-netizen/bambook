# 库存管理模块概述 (Inventory Module Overview)

## §1 元信息

| 项 | 值 |
|---|---|
| **模块定位** | 库存管理是供应链执行的底层枢纽 — 多仓库实时库存台账、出入库流水审计、盘点差异核销、跨仓调拨、低值/呆滞/过期预警，向上承接采购到货/生产领料/销售出库/退货，向下为财务成本核算与 QC 验货提供真源 |
| **入口** | 主导航 Inventory（仓库图标）→ 全屏 Manager 页面（桌面端三 Tab：库存物料 / 仓库 / 预警） |
| **核心角色** | 后勤（含仓储，原仓库管理员，主操作人）、业务员（含跟单、采购执行：查库存可用量、到货入库）、QC（验货锁库/解锁，不细分）、财务（成本查阅，宽泛容器不细分）、超级管理员（老板，绝密级可见：预警看板） |
| **范式** | 范式 B — 列表+详情混合页（三 Tab 分区：物料列表展开式详情 / 仓库卡片网格 / 预警列表） |
| **优先级** | P1（Phase 2 B2 缺失模块补齐） |
| **实现状态** | ✅ 核心已落地（多仓 CRUD + 物料 CRUD + 6 类变动流水 + 低库存预警 + 事件总线 StockLowAlarm/StockOverstockAlarm）；⚠️ 盘点任务化（StockCountTask 独立模型）、预到货通知（PreReceiptNotice）、正式入库单（GoodsReceiptNote）、调拨单（StockTransfer 独立模型）为规划中增强，当前以 StockMovement 统一承载 |
| **关联 PRD 章节** | §6.3（库存管理）、§9.10（库存预警规则）、§19.8（库存页面详细设计） |
| **关联代码** | [InventoryManager.tsx](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/InventoryManager.tsx) 前端三 Tab / [inventoryService.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/inventory/inventoryService.ts) 后端服务工厂 / [inventoryRoute.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/inventory/inventoryRoute.ts) API 路由 / `schema.prisma` Warehouse / InventoryItem / StockMovement 模型 |

---

## §2 模块边界与职责

库存管理模块是 Bambook ERP 供应链执行层的数据枢纽，其核心职责矩阵：

| 职责域 | 已实现 | 规划增强 | 上下游依赖 |
|--------|--------|----------|-----------|
| **多仓库管理** | ✅ Warehouse CRUD（Main/Auxiliary/Temporary/Virtual 四型） | ⚠️ WarehouseLocation（库位精细化管理 A-01-03） | 无上游；下游 InventoryItem |
| **库存台账** | ✅ InventoryItem（仓库×物料×批次维度，quantity/lockedQuantity 双值） | ⚠️ 批次追踪看板（batchNumber 聚合视图） | 上游 ProductAsset/FabricProfile；下游 StockMovement |
| **出入库流水** | ✅ StockMovement 6 型（Inbound/Outbound/Transfer/Adjustment/Lock/Unlock）+ balanceBefore/After 审计 | ⚠️ PreReceiptNotice（预到货）/GoodsReceiptNote（正式入库单） | 上游 PurchaseOrder/Order/Shipment；下游 AuditLog |
| **盘点** | ✅ Adjustment 类型（目标绝对值覆盖式调整 + 流水留痕） | ⚠️ StockCountTask/StockCountItem（任务化盘点 + 差异审批） | 无上游；下游 StockMovement |
| **调拨** | ✅ Transfer 类型（事务内双仓联动：源仓出库 + 目标仓入库/建项） | ⚠️ StockTransfer 独立单据（多步状态机：申请→审批→在途→到货） | 无上游；下游 StockMovement ×2 |
| **库存预警** | ✅ 低库存（quantity ≤ minStock）+ 超储（quantity ≥ maxStock）+ 事件总线 | ⚠️ 呆滞预警（lastInDate/lastOutDate 滞库天数）+ 过期预警（批次有效期） | 上游 InventoryItem min/maxStock；下游 Notification |

---

## §3 模块架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    前端 InventoryManager.tsx                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │ Tab: 物料列表 │  │ Tab: 仓库网格 │  │ Tab: 预警列表 │        │
│  │ (展开式详情)  │  │ (卡片 CRUD)  │  │ (低库存 badge)│        │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘        │
│         │                 │                 │                │
│         └─────────────────┼─────────────────┘                │
│                           ▼                                  │
│                   apiService (HTTP)                          │
└───────────────────────────┬─────────────────────────────────┘
                            │ /api/v1/inventory/*
┌───────────────────────────▼─────────────────────────────────┐
│              后端 inventoryRoute.ts (Express)                │
│  ┌─────────────────────────────────────────────────┐        │
│  │         createInventoryService(prisma)           │        │
│  │  ┌─────────┐ ┌───────────┐ ┌──────────────────┐ │        │
│  │  │仓库 CRUD│ │物料 CRUD  │ │变动流水(事务+审计)│ │        │
│  │  └─────────┘ └───────────┘ └──────────────────┘ │        │
│  │  ┌───────────────┐ ┌──────────────────────────┐ │        │
│  │  │预警查询        │ │事件发布(fire-and-forget) │ │        │
│  │  │getLowStockItems│ │StockLowAlarm/Overstock   │ │        │
│  │  └───────────────┘ └──────────────────────────┘ │        │
│  └─────────────────────────────────────────────────┘        │
└───────────────────────────┬─────────────────────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    PostgreSQL (Prisma)                       │
│  ┌──────────┐  ┌──────────────┐  ┌─────────────────┐      │
│  │ Warehouse │  │InventoryItem │  │ StockMovement   │      │
│  │ (软删)    │  │ (软删)       │  │ (流水不可变)     │      │
│  └──────────┘  └──────────────┘  └─────────────────┘      │
│  ┌──────────┐                                                │
│  │ AuditLog │  (每次变动写审计：action/before/after)        │
│  └──────────┘                                                │
└─────────────────────────────────────────────────────────────┘
```

---

## §4 核心数据模型速览

| 模型 | 主键格式 | 职责 | 软删 | 关键约束 |
|------|---------|------|------|---------|
| **Warehouse** | `WH_${shortId}` | 仓库主数据（编码/名称/类型/管理员） | ✅ deletedAt | code @unique |
| **InventoryItem** | `INV_${shortId}` | 库存台账（仓库×物料×批次维度余额） | ✅ deletedAt | 删除前 quantity 必须为 0 |
| **StockMovement** | `SM_${shortId}` | 出入库流水（不可变审计记录） | ❌ 不软删（流水永久留痕） | balanceBefore/After + type 严格校验 |

> ⚠️ 规划模型（尚未建表）：StockCountTask / StockCountItem（任务化盘点）、PreReceiptNotice（预到货通知）、GoodsReceiptNote（正式入库单）、StockTransfer（独立调拨单）。当前盘点走 StockMovement type=Adjustment，调拨走 type=Transfer。

---

## §5 StockMovement 6 型变动矩阵

| 类型 | 标签 | 图标 | 语义徽章 | 余额影响 | lockedQuantity 影响 | 前端校验 |
|------|------|------|---------|---------|---------------------|---------|
| **Inbound** | 入库 | ArrowDownToLine | success | quantity += qty | 不变 | 无（正数即可） |
| **Outbound** | 出库 | ArrowUpFromLine | warning | quantity -= qty | 不变 | qty ≤ quantity（不足报错） |
| **Transfer** | 调拨 | ArrowLeftRight | info | 源仓 -= qty / 目标仓 += qty | 不变 | 必须指定 targetWarehouseId + qty ≤ 源仓 quantity |
| **Adjustment** | 盘点 | ClipboardCheck | neutral | quantity = qty（绝对值覆盖） | 不变 | 无（可为 0） |
| **Lock** | 锁定 | Lock | warning | 不变 | lockedQuantity += qty | lockedQty + qty ≤ quantity |
| **Unlock** | 解锁 | Unlock | success | 不变 | lockedQuantity -= qty | qty ≤ lockedQuantity |

---

## §6 业务事件总线集成

库存变动事务提交后，以 fire-and-forget 方式发布业务事件（发布失败不阻断业务）：

| 事件类型 | 触发条件 | payload 关键字段 | 下游消费者 |
|---------|---------|-----------------|-----------|
| **StockLowAlarm** | 变动后 newQty ≤ minStock | itemId / materialCode / warehouseCode / currentQty / minStock / unit / movementType | NotificationService → 推送预警通知 |
| **StockOverstockAlarm** | Inbound 后 newQty ≥ maxStock | itemId / materialCode / warehouseCode / currentQty / maxStock / unit | NotificationService → 推送超储通知 |

---

## §7 与上下游模块的联动契约

| 上游模块 | 联动场景 | 联动方式 | 当前状态 |
|---------|---------|---------|---------|
| **采购管理** | 采购到货入库 | StockMovement type=Inbound + referenceType=PurchaseOrder | ⚠️ 手动录入，未自动联动 |
| **订单管理** | 生产领料出库 | StockMovement type=Outbound + referenceType=Order | ⚠️ 手动录入 |
| **出运管理** | 销售出库 | StockMovement type=Outbound + referenceType=Shipment | ⚠️ 手动录入 |
| **生产管理** | 面辅料到厂确认 | ProductionStage materials_arrived ↔ StockMovement Inbound | ⚠️ 未联动 |
| **QC 质检** | 验货锁库/解锁 | StockMovement type=Lock/Unlock + referenceType=InspectionReport | ⚠️ 未联动 |

| 下游模块 | 联动场景 | 联动方式 | 当前状态 |
|---------|---------|---------|---------|
| **财务核算** | 库存成本查询 | InventoryItem.unitCost × quantity | ✅ 只读查询 |
| **报表中心** | 库存周转率/呆滞分析 | StockMovement 聚合统计 | ⚠️ 报表未实现 |
| **Agent** | 库存查询工具 | apiService → inventoryRoute | ✅ 可用 |

---

## §8 前端页面 Tab 结构

InventoryManager.tsx 采用三 Tab 分区（`bds-segment` 组件族）：

| Tab | id | 图标 | 功能 | 数据源 API |
|-----|----|------|------|-----------|
| 库存物料 | `items` | Boxes | 物料列表（搜索/仓库筛选/品类筛选/低库存过滤）+ 展开式详情（变动流水 + 变动表单） | GET /items + GET /movements |
| 仓库 | `warehouses` | Warehouse | 仓库卡片网格 + 新建仓库表单 | GET /warehouses |
| 预警 | `alerts` | AlertTriangle | 低库存物料列表（Tab 标题带 badge 计数） | GET /alerts/low-stock |

---

## §9 事务原子性保障

所有库存变动在 `prisma.$transaction` 内完成，保证原子性：

```
事务内执行顺序：
1. 创建 StockMovement 流水记录（balanceBefore/After 已预算）
2. 更新 InventoryItem.quantity / lockedQuantity / lastInDate / lastOutDate
3. [仅 Transfer] 目标仓库：创建或更新 InventoryItem + 写入库流水（type=Inbound, referenceType=Transfer）
4. 写 AuditLog（action=stock_movement_{type}, beforeValue/afterValue）
── 事务提交 ──
5. [事务后 fire-and-forget] 发布 StockLowAlarm / StockOverstockAlarm 事件
```

**fail-closed 原则**：Outbound / Transfer / Lock 时若库存不足，事务内 throw → 整个事务回滚，不留半途数据。

---

## §10 权限矩阵

| 角色 | 查看库存 | 创建物料 | 库存变动 | 仓库管理 | 预警查看 |
|------|---------|---------|---------|---------|---------|
| owner / admin | ✅ | ✅ | ✅ | ✅ | ✅ |
| warehouse | ✅ | ✅ | ✅ | ✅ | ✅ |
| merchandiser | ✅（同部门） | ❌ | ⚠️ 仅出库/调拨 | ❌ | ✅ |
| qc | ✅（同部门） | ❌ | ⚠️ 仅 Lock/Unlock | ❌ | ✅ |
| finance | ✅（成本只读） | ❌ | ❌ | ❌ | ✅ |
| sales | ✅（仅自己的单关联物料） | ❌ | ❌ | ❌ | ✅ |
| guest | ❌ | ❌ | ❌ | ❌ | ❌ |

> ⚠️ 后端 prisma 查询层的行级过滤仍待实现（P0 缺口）；前端 Tab 可见性已按角色控制。

---

## §11 四态规范

| 状态 | 触发 | UI 规范 |
|------|------|---------|
| **加载态** | 首次加载 / refreshKey 变化 | Loader2 spin 居中 |
| **空态** | 无物料 / 无仓库 / 无预警 | `bds-empty` 空态卡（glyph 图标 + title 文案） |
| **错误态** | fetch 4xx/5xx | `bds-alert danger` + 错误文案 + 关闭按钮 |
| **无权限** | 后端 403/401 | ⚠️ 未实现独立无权限态（当前与错误态合并） |

---

## §12 设计系统约束

| 约束 | 合规说明 |
|------|---------|
| 主题透明 | InventoryManager 无 isDarkMode 样式分支，暗色由 tokens.css `[data-theme]` 统一覆盖 |
| bds 组件族 | 全线使用 bds-segment / bds-card / bds-badge / bds-input / bds-select / bds-modal / bds-empty / bds-alert |
| 语义徽章 | MOVEMENT_TYPES 的 semantic 字段直接映射 bds-badge 变体（neutral/info/success/danger/warning） |
| 圆角规则 | 卡片 rounded-card；字段胶囊 rounded-inset；按钮 rounded-control |
| 无阴影 flat | panel 使用 backdrop-blur + hairline border，非 box-shadow |
| 数值排版 | bds-tnum（tabular-nums）对齐防跳动 |
| 动画 | framer-motion（列表项 stagger + 展开 AnimatePresence height auto） |

---

## §13 可访问性 & 交互细节

| 项 | 规范 |
|----|------|
| **Tab 切换** | 三 Tab 切换不保留各自滚动位置（每次切回重新拉取） |
| **展开/收起** | 物料列表项点击头部 → ChevronRight→ChevronDown 旋转 + AnimatePresence height auto 展开 |
| **变动表单内嵌** | 展开详情后点击「库存变动」按钮 → 表单内嵌展开（非弹窗），支持 6 型切换 |
| **Transfer 条件字段** | type=Transfer 时追加目标仓库下拉（仅非当前仓库）；type=Adjustment 时追加盘点备注输入 |
| **低库存 badge** | Tab 标题「预警」右侧 bds-badge danger 计数（lowStockItems.length > 0 时显示） |
| **物料行低库存标记** | quantity ≤ minStock 时行内显示 bds-badge danger「低库存」 |
| **锁定标记** | lockedQuantity > 0 时行内显示 bds-badge warning「锁定 N」 |
| **可用量计算** | availableQuantity = quantity - lockedQuantity（前端计算，后端不存储） |

---

## §14 关键业务规则

| # | 规则 | 实现状态 |
|---|------|---------|
| 1 | 余额不可为负（Outbound/Transfer/Lock 时校验 sufficient stock，fail-closed） | ✅ inventoryService 事务内 throw |
| 2 | 所有变动写流水 + 审计日志（StockMovement + AuditLog 双写） | ✅ 事务内完成 |
| 3 | 软删除物料前 quantity 必须为 0（防误删有库存项） | ✅ deleteInventoryItem 校验 |
| 4 | 软删除仓库前无非零库存项 | ✅ deleteWarehouse count 校验 |
| 5 | 初始数量 > 0 的物料自动生成入库流水 | ✅ createInventoryItem 事务内 |
| 6 | Transfer 双仓联动（源仓出库 + 目标仓建项/累加 + 双流水） | ✅ 同一事务内 |
| 7 | 低库存/超储事件发布（fire-and-forget，不阻断业务） | ✅ 事务后 publish |
| 8 | 数值精度 4 位小数（toDecimal: Math.round(n × 10000) / 10000） | ✅ |

---

## §15 相关文档索引

| 文档 | 相对路径 |
|------|---------|
| 库存总览（多仓看板/搜索/批次追踪） | `./库存总览.md` |
| 出入库单据（入库/出库/调拨流水） | `./出入库单据.md` |
| 盘点与调拨（盘点差异/多仓调拨） | `./盘点与调拨.md` |
| 库存预警（低值/过期/呆滞预警） | `./库存预警.md` |
| QC 质检中心模块概述 | `../QcWorkbench-QC质检中心/模块概述.md` |
| 订单详情页设计（履约链联动） | `../Orders-订单管理/订单详情页.md` |
| 字段词典与类型真源 | [types.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/types.ts) Warehouse/InventoryItem/StockMovement |

---

## §16 待补的设计缺口

| # | 缺口 | 优先级 | 建议跟进 |
|---|------|-------|---------|
| 1 | **StockCountTask 独立模型**（任务化盘点：创建任务→分配盘点人→录入实盘→差异审批→Adjustment 自动生成） | P1 | 详见 `./盘点与调拨.md` §设计缺口 |
| 2 | **PreReceiptNotice 预到货通知**（供应商发货通知 → 到货对照 → 入库单） | P1 | 详见 `./出入库单据.md` §设计缺口 |
| 3 | **GoodsReceiptNote 正式入库单**（替代裸 StockMovement Inbound，带状态机 + QC 放行） | P1 | 详见 `./出入库单据.md` §设计缺口 |
| 4 | **StockTransfer 独立调拨单**（多步状态机：申请→审批→在途→到货确认） | P2 | 详见 `./盘点与调拨.md` §设计缺口 |
| 5 | **呆滞预警**（lastInDate/lastOutDate 滞库天数超阈值预警） | P2 | 详见 `./库存预警.md` §设计缺口 |
| 6 | **批次有效期管理**（批次过期预警，需 InventoryItem 增加 expiryDate 字段） | P2 | 详见 `./库存预警.md` §设计缺口 |
| 7 | **后端行级权限过滤**（prisma 查询层按 ownerId/departmentId 过滤） | P0 | 与订单模块行级权限统一实现 |
| 8 | **库存快照报表**（期末结存快照，供财务成本核算） | P1 | 新增 InventorySnapshot 模型 + 定时任务 |
