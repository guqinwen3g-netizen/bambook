# 库存总览 (Inventory Overview)

## §1 元信息

| 项 | 值 |
|---|---|
| **页面定位** | 库存总览是 InventoryManager「库存物料」Tab 的主视图 — 多仓库存看板、库存搜索、批次追踪、展开式物料详情（含变动流水） |
| **入口** | 主导航 Inventory → 默认 Tab「库存物料」(activeTab='items') |
| **核心角色** | 后勤（含仓储，原仓库管理员，日活最高）、业务员（含跟单，查可用量）、财务（成本查阅，宽泛容器不细分） |
| **范式** | 范式 B — 列表+展开式详情（卡片头部摘要 + AnimatePresence 展开详情） |
| **优先级** | P1 |
| **实现状态** | ✅ 已落地（多仓筛选 / 品类筛选 / 搜索 / 低库存过滤 / 展开式详情含变动流水 + 库存变动内嵌表单 / 可用量计算）；⚠️ 批次追踪聚合看板（按 batchNumber 跨仓聚合视图）待补 |
| **关联 PRD 章节** | §19.8（库存页面详细设计） |
| **关联代码** | [InventoryManager.tsx L292-L504](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/InventoryManager.tsx) Tab=items 渲染主体 / [inventoryService.ts listInventoryItems](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/inventory/inventoryService.ts) 列表查询 |

---

## §2 页面骨架

```
┌──────────────────────────────────────────────────────────────────┐
│  PageHeader: "库存管理" / "Inventory"                              │
├──────────────────────────────────────────────────────────────────┤
│  bds-segment 三 Tab: [库存物料(active)] [仓库] [预警(N)]          │
├──────────────────────────────────────────────────────────────────┤
│  ┌─ 工具栏 ──────────────────────────────────────────────────┐  │
│  │ [新增物料] [全部仓库▾] [全部品类▾] [☑仅低库存] [🔍搜索物料] [↻]│  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌─ 物料卡片列表 (space-y-2) ────────────────────────────────┐  │
│  │ ┌ 卡片 1 ──────────────────────────────────────────────┐  │  │
│  │ │ [▶] materialCode · description · warehouse · 批次     │  │  │
│  │ │                          数量 1,200 YD | 最低 500      │  │  │
│  │ └──────────────────────────────────────────────────────┘  │  │
│  │ ┌ 卡片 2 (展开) ────────────────────────────────────────┐  │  │
│  │ │ [▼] materialCode · [低库存] [锁定 300]                  │  │  │
│  │ │     description · warehouse · 批次 · 库位              │  │  │
│  │ │                          数量 400 YD | 最低 500         │  │  │
│  │ │  ┌ 展开详情 ───────────────────────────────────────┐  │  │  │
│  │ │  │ 品类 / 规格 / 单位成本 / 最后入库 / 最后出库       │  │  │  │
│  │ │  │ 当前 400 | 锁定 300 | 可用 100                    │  │  │  │
│  │ │  │ ─ 最近变动 (8条) ─                                │  │  │  │
│  │ │  │ [入库] 200 YD  2026-08-10  1000→1200 · 采购到货   │  │  │  │
│  │ │  │ [出库] 800 YD  2026-08-08  2000→1200 · 生产领料   │  │  │  │
│  │ │  │ ─ 库存变动表单 (内嵌) ─                            │  │  │  │
│  │ │  │ [类型▾] [数量] [日期] [原因] [目标仓库▾(仅Transfer)]│  │  │  │
│  │ │  │                              [取消] [执行变动]     │  │  │  │
│  │ │  └──────────────────────────────────────────────────┘  │  │  │
│  │ └──────────────────────────────────────────────────────┘  │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

---

## §3 工具栏详解

| 控件 | 类型 | 功能 | 数据源 |
|------|------|------|--------|
| 新增物料 | bds-btn primary sm | 打开物料创建弹窗（ModalShell） | — |
| 仓库筛选 | bds-select sm | 按仓库过滤物料列表 | warehouses（GET /warehouses） |
| 品类筛选 | bds-select sm | 按品类过滤（Fabric/Trimmings/Accessories/Garment/Other） | ITEM_CATEGORIES 常量 |
| 仅低库存 | bds-check | 切换 lowStockOnly 过滤（quantity ≤ minStock） | — |
| 搜索物料 | bds-input sm + Search 图标 | 模糊搜索 description / materialCode / batchNumber | — |
| 刷新 | bds-btn ghost icon | 重新拉取物料列表（loading 时图标 spin） | — |

**筛选组合查询参数**（传入 `listInventoryItems`）：

```typescript
apiService.listInventoryItems({
  warehouseId: warehouseFilter || undefined,
  category: categoryFilter || undefined,
  search: searchQuery || undefined,
  lowStockOnly,
  limit: 200,
});
```

---

## §4 物料卡片（收起态）

每张卡片对应一个 `InventoryItem`，点击头部展开/收起。

**卡片头部布局**（`flex items-center gap-3 p-4 cursor-pointer`）：

| 区域 | 内容 | 样式 |
|------|------|------|
| 展开图标 | ChevronRight（收起）/ ChevronDown（展开） | text-quaternary |
| 左主区 | materialCode（bds-mono）+ 低库存/锁定 badge + description · warehouse · 批次 · 库位 | text-primary / text-tertiary |
| 右数值区 | quantity + unit（bds-tnum）+ 最低库存（text-quaternary 10px） | 低库存时 danger-text |

**徽章规则**：

| 条件 | 徽章 | 变体 |
|------|------|------|
| `quantity ≤ minStock`（minStock != null） | `低库存` | bds-badge danger |
| `lockedQuantity > 0` | `锁定 {qty}` | bds-badge warning |

---

## §5 展开式详情

点击卡片头部 → `handleExpand(itemId)` → 拉取该物料最近 20 条变动流水 + AnimatePresence height auto 展开。

### 5.1 物料信息网格

`grid-cols-2 xl:grid-cols-4 gap-3 text-xs`，展示以下字段（仅非空显示）：

| 字段 | 标签 | 格式化 |
|------|------|--------|
| category | 品类 | 原值 |
| specification | 规格 | 原值 |
| unitCost + currency | 单位成本 | `toFixed(2) {currency}` |
| lastInDate | 最后入库 | formatDate（空值 `—`） |
| lastOutDate | 最后出库 | formatDate |
| maxStock | 最高库存 | formatQty |

### 5.2 可用库存摘要

`flex items-center gap-4 text-xs` 三值并列：

| 指标 | 计算方式 | 颜色 |
|------|---------|------|
| 当前 | `quantity` | text-primary |
| 锁定 | `lockedQuantity` | warning-text |
| 可用 | `availableQuantity = quantity - lockedQuantity` | success-text |

> availableQuantity 在前端拉取后计算：`Number(it.quantity) - Number(it.lockedQuantity)`，后端不存储此字段。

### 5.3 最近变动流水

`movements.slice(0, 8)` 展示最近 8 条 StockMovement：

每行结构（`p-2 rounded-inset flex items-center gap-3 text-xs`）：

```
[类型徽章(图标+标签)] [数量 unit] [日期] [余额Before→After] · [原因]
```

- 类型徽章：从 MOVEMENT_TYPES 查找 icon + semantic（success/warning/info/neutral）
- 余额箭头：`formatQty(balanceBefore) → formatQty(balanceAfter)`（bds-tnum 对齐）
- 原因：可选，text-quaternary

### 5.4 库存变动表单（内嵌展开）

点击「库存变动」按钮 → 表单内嵌展开（非弹窗），`grid-cols-2 xl:grid-cols-4 gap-2`：

| 字段 | 控件 | 条件显示 |
|------|------|---------|
| 类型 | bds-select（6 型可选） | 始终 |
| 数量 | bds-input number | 始终 |
| 日期 | bds-input date（默认今日） | 始终 |
| 原因 | bds-input text | 始终 |
| 目标仓库 | bds-select（仅非当前仓库） | **仅 type=Transfer** |
| 盘点备注 | bds-input text | **仅 type=Adjustment** |

提交 → `handleCreateMovement` → `apiService.createStockMovement` → 成功后刷新物料列表 + 预警列表。

---

## §6 批次追踪（规划增强）

> ⚠️ 当前批次追踪仅以 `InventoryItem.batchNumber` 字段存储，无独立聚合看板。

**规划设计**（待实现）：

| 功能 | 查询方式 | UI 布局 |
|------|---------|---------|
| 批次聚合视图 | 按 batchNumber GROUP BY 聚合跨仓同批次物料 | 表格：批次号 / 总量 / 在仓数 / 涉及仓库列表 |
| 批次流水穿透 | 按 batchNumber 查 StockMovement（需 StockMovement 增 batchNumber 关联） | 时间线视图（入库→锁定→出库全链路） |
| 批次有效期 | InventoryItem 增加 expiryDate 字段 + 过期预警 | 批次列表右侧倒计时 badge |

---

## §7 多仓库存看板（规划增强）

> ⚠️ 当前仓库维度总览仅在「仓库」Tab 以卡片展示仓库主数据，无库存聚合看板。

**规划设计**（待实现）：

| 看板指标 | 数据源 | 展示形式 |
|---------|--------|---------|
| 各仓库物料数 | `InventoryItem.count({ warehouseId })` | 柱状图 / StatCard |
| 各仓库总值 | `Σ(quantity × unitCost) GROUP BY warehouseId` | 金额看板（币种区分） |
| 各仓库低库存数 | `getLowStockItems GROUP BY warehouseId` | 预警分布图 |
| 仓库利用率 | 物料数 / 容量上限（需 Warehouse 增加 capacity 字段） | 进度条 |

---

## §8 API 端点清单

| 方法 | 路径 | 说明 | 入参 |
|------|------|------|------|
| GET | `/api/v1/inventory/items` | 物料列表（带筛选/搜索/分页） | warehouseId?, category?, search?, lowStockOnly?, limit?, offset? |
| GET | `/api/v1/inventory/items/:id` | 单物料详情（含最近 50 条流水） | id |
| POST | `/api/v1/inventory/items` | 新建物料（初始数量 > 0 自动生成入库流水） | InventoryItemInput |
| PUT | `/api/v1/inventory/items/:id` | 更新物料档案（不含 quantity，quantity 走变动） | Partial<InventoryItemInput> |
| DELETE | `/api/v1/inventory/items/:id` | 软删（quantity 必须为 0） | id |
| GET | `/api/v1/inventory/movements` | 变动流水查询 | itemId?, warehouseId?, type?, dateFrom?, dateTo?, limit?, offset? |
| POST | `/api/v1/inventory/movements` | 库存变动（6 型，事务+审计+事件） | StockMovementInput |
| GET | `/api/v1/inventory/warehouses` | 仓库列表 | includeInactive? |
| GET | `/api/v1/inventory/alerts/low-stock` | 低库存预警列表 | — |

---

## §9 权限矩阵

| 角色 | 查看物料列表 | 展开详情/流水 | 新建物料 | 库存变动 | 批次追踪 |
|------|------------|-------------|---------|---------|---------|
| owner / admin | ✅ 全部 | ✅ | ✅ | ✅ | ✅ |
| warehouse | ✅ 全部 | ✅ | ✅ | ✅ | ✅ |
| merchandiser | ✅ 同部门 | ✅ | ❌ | ⚠️ 出库/调拨 | ✅ |
| qc | ✅ 同部门 | ✅ | ❌ | ⚠️ Lock/Unlock | ✅ |
| finance | ✅ 成本只读 | ✅ | ❌ | ❌ | ✅ |
| sales | ⚠️ 仅关联物料 | ⚠️ | ❌ | ❌ | ❌ |

---

## §10 四态规范

| 状态 | 触发 | UI 规范 |
|------|------|---------|
| **加载态** | fetchItems 进行中 | `Loader2 size=24 animate-spin` 居中 py-12 |
| **空态** | items.length === 0 | `bds-empty`：glyph(Boxes) + title「暂无库存物料」 |
| **错误态** | fetch 失败 | `bds-alert danger`：AlertCircle + 错误文案 + 关闭按钮 |
| **无权限** | 后端 403 | ⚠️ 当前与错误态合并（未独立处理） |

---

## §11 移动端适配

当前 InventoryManager 桌面端优先，移动端适配遵循 BDS 响应式约定：

| 区域 | 桌面端 | 移动端 |
|------|--------|--------|
| 工具栏 | flex-wrap 单行 | 折叠为垂直堆叠（筛选/搜索各占一行） |
| 物料信息网格 | `grid-cols-2 xl:grid-cols-4` | `grid-cols-2`（2 列） |
| 变动表单 | `grid-cols-2 xl:grid-cols-4` | `grid-cols-2`（2 列） |
| 卡片列表 | space-y-2 | 同桌面（卡片自适应宽度） |

---

## §12 业务规则关联

| # | 规则 | 实现状态 |
|---|------|---------|
| 1 | 可用量 = quantity - lockedQuantity（前端计算，实时反映锁定影响） | ✅ |
| 2 | 低库存判定：quantity ≤ minStock（minStock 非 null 时） | ✅ 前端+后端双计算 |
| 3 | 搜索覆盖 description / materialCode / batchNumber（OR 模糊） | ✅ |
| 4 | lowStockOnly 跨字段比较（Prisma 不支持，内存过滤兜底） | ✅ |
| 5 | 物料列表默认按 updatedAt desc 排序（最近变动优先） | ✅ |
| 6 | 变动流水默认按 createdAt desc 排序（最新在上） | ✅ |

---

## §13 可访问性 & 交互细节

| 项 | 规范 |
|----|------|
| **展开动画** | AnimatePresence + motion.div height 0→auto + opacity 0→1（duration 0.2s） |
| **列表 stagger** | 每张卡片 `transition={{ delay: index * 0.02 }}`（错峰入场） |
| **数量格式化** | `Number(n).toLocaleString('en-US', { maximumFractionDigits: 2 })`（千分位） |
| **日期格式化** | 空值显示 `—`，非空显示原值（YYYY-MM-DD 字符串） |
| **展开互斥** | 同一时刻仅展开一项（expandedId 单值，点击新项自动收起旧项） |
| **变动表单重置** | 提交成功后重置 movementForm 为默认值（type=Inbound, quantity=0） |
| **错误不丢稿** | 变动失败时 movementError 显示文案，不收起表单（保留用户输入） |

---

## §14 设计系统约束

| 约束 | 合规说明 |
|------|---------|
| 主题透明 | 无 isDarkMode 分支，暗色由 tokens.css 统一覆盖 |
| bds 组件族 | bds-segment / bds-card / bds-badge / bds-input / bds-select / bds-modal / bds-empty / bds-alert / bds-btn |
| 语义徽章映射 | MOVEMENT_TYPES.semantic → bds-badge 变体（neutral/info/success/danger/warning） |
| 圆角 | 卡片 rounded-card；字段胶囊 rounded-inset；按钮 rounded-control |
| 无阴影 flat | panel 使用 backdrop-blur + hairline border |
| 数值排版 | bds-tnum（tabular-nums）对齐防跳动 |
| 颜色变量 | 全部走 CSS 变量（var(--text-primary) / var(--danger-text) 等），无硬编码 hex |
| hover 效果 | 卡片头部 hover:bg-[var(--hover-darken)] |

---

## §15 相关文档索引

| 文档 | 相对路径 |
|------|---------|
| 库存管理模块概述 | `./模块概述.md` |
| 出入库单据（变动流水 6 型详解） | `./出入库单据.md` |
| 库存预警（低库存/超储预警机制） | `./库存预警.md` |
| 盘点与调拨（Adjustment/Transfer 详解） | `./盘点与调拨.md` |
| QC 质检中心模块概述 | `../QcWorkbench-QC质检中心/模块概述.md` |
| 订单详情页设计（履约链联动） | `../Orders-订单管理/订单详情页.md` |
| 类型真源（InventoryItem/StockMovement） | [types.ts L2131-L2210](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/types.ts) |

---

## §16 待补的设计缺口

| # | 缺口 | 优先级 | 建议补充 |
|---|------|-------|---------|
| 1 | **批次追踪聚合看板**（按 batchNumber 跨仓聚合 + 流水穿透时间线） | P2 | 新增 Tab「批次追踪」或独立页面 |
| 2 | **多仓库存看板**（各仓库物料数/总值/低库存数聚合视图） | P2 | 仓库 Tab 增强：每张仓库卡片追加库存摘要 |
| 3 | **物料列表分页**（当前 limit=200 一次拉取，大数据量时需分页） | P1 | 后端已支持 offset/limit，前端需补分页控件 |
| 4 | **物料详情弹窗**（当前仅展开式，复杂详情如批次/库位/成本趋势建议弹窗） | P2 | 复用 ModalShell 设计详情弹窗 |
| 5 | **导出库存报表**（Excel 导出当前筛选结果） | P2 | 新增 GET /items/export 端点 |
| 6 | **库存快照对比**（选择两个日期对比库存差异） | P3 | 需 InventorySnapshot 模型 |
| 7 | **库位精细化管理**（WarehouseLocation 模型，A-01-03 三级库位） | P3 | InventoryItem.locationCode 已存文本，需结构化 |
