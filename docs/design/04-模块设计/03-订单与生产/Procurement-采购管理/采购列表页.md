# 采购列表页 (Purchase Order List Page)

## 一、元信息

| 项 | 值 |
|---|---|
| **定位** | 采购单全量列表入口 — 按状态/供应商/日期筛选 + 关键字搜索 + 行展开明细 + 批量操作；采购员日活最高频的采购视图 |
| **入口** | 主导航"采购管理"→ 列表视图（ProcurementManager 组件默认态） |
| **核心角色** | 业务员（含采购执行，原 buyer）、财务（宽泛容器不细分）、销售主管（部门管理职责） |
| **范式** | 范式 A — 列表 (List)：状态 tab + 筛选 + 表格 + 行展开 |
| **优先级** | P0 |
| **实现状态** | ✅ 状态 tab 筛选（8 态）已落地；✅ 搜索（poNumber/supplierName/buyer）已实现；✅ 行展开（行明细 + 收料记录）已实现；⚠️ 供应商筛选下拉未实现（后端支持 supplierRelationId，前端未接入下拉）；⚠️ 日期范围筛选未实现（后端支持 dateFrom/dateTo，前端未接入）；⚠️ 批量操作未实现 |
| **关联 PRD 章节** | §5.6（采购单号）、§19（采购管理页详细设计） |
| **关联代码** | [ProcurementManager.tsx](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/ProcurementManager.tsx) 前端列表渲染 / [procurementService.ts listPurchaseOrders](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementService.ts#L353-L392) 后端查询 / [procurementRoute.ts GET /](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementRoute.ts#L47-L65) API |

---

## 二、页面骨架

```
┌─────────────────────────────────────────────────────────────────────┐
│  PageHeader: 采购管理 / Procurement / List                [+ 新建]   │
├─────────────────────────────────────────────────────────────────────┤
│  状态 Tab 行 (8 tab 横向滚动)                                        │
│  [全部] [草稿] [已发送] [已确认] [部分到货] [全部到货] [已关闭] [已取消]│
├─────────────────────────────────────────────────────────────────────┤
│  筛选行: [🔍 搜索 PO号/供应商/采购员] [供应商▼] [日期范围]  共 N 单  │
├─────────────────────────────────────────────────────────────────────┤
│  列表表 (raisedCard)                                                 │
│  ┌──────┬──────────┬──────┬──────┬──────┬──────┬──────┬─────────┐  │
│  │ PO号 │ 供应商   │状态  │总金额│下单日│预计  │采购员│ 操作     │  │
│  │PO-...│张家港一厂│[已确认]│$12k │08-01│08-20 │张三  │[展开 ▼] │  │
│  └──────┴──────────┴──────┴──────┴──────┴──────┴──────┴─────────┘  │
│  展开行: 行明细 (PurchaseLine) + 收料记录 (MaterialReceipt)         │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 三、状态 Tab 筛选

### 3.1 Tab 定义

真源 [ProcurementManager.tsx STATUS_TABS](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/ProcurementManager.tsx#L95-L104)：

| Tab ID | 中文标签 | 筛选条件 |
|--------|---------|---------|
| all | 全部 | 无状态过滤 |
| Draft | 草稿 | status=Draft |
| Sent | 已发送 | status=Sent |
| Confirmed | 已确认 | status=Confirmed |
| PartiallyReceived | 部分到货 | status=PartiallyReceived |
| Received | 全部到货 | status=Received |
| Closed | 已关闭 | status=Closed |
| Cancelled | 已取消 | status=Cancelled |

### 3.2 Tab 视觉

- 横向排列，超宽时横向滚动
- active tab：brand-blue 底 + 白字
- 非 active：recessed-bg 底 + tertiary 字
- 每个 tab 不带计数（避免每次切 tab 都 count 查询）

---

## 四、列定义

### 4.1 列表列

| # | 列名 | 数据字段 | 说明 |
|---|------|---------|------|
| 1 | PO 号 | `poNumber` | 业务单号（PO-YYYY-NNNN），主链接色可点击进详情 |
| 2 | 供应商 | `supplierName` | 冗余快照（无则显示 `—`） |
| 3 | 状态 | `status` | bds-badge 徽章（见 §五） |
| 4 | 总金额 | `totalAmount` + `currency` | `toLocaleString` + 币种前缀 |
| 5 | 下单日期 | `orderDate` | YYYY-MM-DD |
| 6 | 预计到货 | `expectedDeliveryDate` | YYYY-MM-DD（空显 `—`） |
| 7 | 采购员 | `buyer` | 可空 |
| 8 | 操作 | — | 展开按钮 + 状态流转快捷操作 |

### 4.2 行展开内容

点击展开按钮（ChevronDown/ChevronRight 切换）→ 展开两区块：

**A. 行明细（PurchaseLine）子表**：

| 列 | 字段 | 说明 |
|----|------|------|
| 行号 | `lineNumber` | 1,2,3... |
| 物料编码 | `materialCode` | 可空 |
| 品名描述 | `description` | 必填 |
| 分类 | `category` | Fabric/Trimmings/Accessories/Other |
| 数量 | `quantity` + `unit` | tabular-nums |
| 单价 | `unitPrice` | 4 位小数 |
| 行金额 | `amount` | = quantity × unitPrice |
| 已收 | `receivedQuantity` | 累计收料 |
| 不合格 | `rejectedQuantity` | 累计拒收 |

**B. 收料记录（MaterialReceipt）列表**：

| 列 | 字段 | 说明 |
|----|------|------|
| 收料单号 | `receiptNumber` | |
| 状态 | `status` | Accepted/Rejected/PartiallyAccepted 徽章 |
| 收货日 | `receivedDate` | |
| 总收货 | `totalReceived` | |
| 合格 | `totalAccepted` | |
| 拒收 | `totalRejected` | |
| 仓库 | `warehouseName` | |

---

## 五、状态徽章颜色编码

真源 [ProcurementManager.tsx STATUS_BADGE_VARIANT](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/ProcurementManager.tsx#L117-L125)（BDS v2.1 bds-badge 语义变体）：

| 状态 | badge 变体 | 语义 |
|------|-----------|------|
| Draft | neutral | 中性灰（草稿未提交） |
| Sent | info | 信息蓝（已发出待确认） |
| Confirmed | neutral | 中性（已确认待到货） |
| PartiallyReceived | warning | 警告橙（部分到货需跟进） |
| Received | success | 成功绿（全部到货） |
| Closed | neutral | 中性（终态已关闭） |
| Cancelled | danger | 危险红（已取消） |

**收料状态徽章**（RECEIPT_STATUS_BADGE_VARIANT）：

| 状态 | 变体 |
|------|------|
| Pending（待检） | neutral |
| Inspected（已检验） | neutral |
| Accepted（合格入库） | success |
| Rejected（不合格） | danger |
| PartiallyAccepted（部分合格） | warning |

---

## 六、筛选交互

### 6.1 已实现筛选

| 筛选器 | 类型 | 后端参数 | 前端实现 |
|--------|------|---------|---------|
| 状态 Tab | 单选 | `status` | ✅ |
| 搜索框 | 文本模糊 | `search` | ✅ 匹配 poNumber/supplierName/buyer |

### 6.2 后端已支持、前端待接入

| 筛选器 | 后端参数 | 设计 |
|--------|---------|------|
| 供应商下拉 | `supplierRelationId` | `RelationCombobox`（category=Supplier） |
| 日期范围 | `dateFrom` / `dateTo` | 双 datepicker，作用于 `orderDate` |

### 6.3 后端查询契约

```typescript
// procurementService.ts listPurchaseOrders
where: { deletedAt: null }
  + status? + supplierRelationId?
  + orderDate: { gte: dateFrom, lte: dateTo }
  + OR: [poNumber/supplierName/buyer contains search]
orderBy: { createdAt: 'desc' }
take: min(limit ?? 50, 200)  // 上限 200
skip: offset ?? 0
```

返回 `{ items: PurchaseOrder[], total: number }`。

---

## 七、批量操作（待实现）

⚠️ **未实现**。设计方案：

| 操作 | 适用状态 | 后端 | 说明 |
|------|---------|------|------|
| 批量发送 | Draft | 循环 POST /:id/send | 多选 Draft 单一键发送 |
| 批量取消 | Draft/Sent/Confirmed | 循环 POST /:id/cancel | 需填取消原因 |
| 批量关闭 | PartiallyReceived/Received | 循环 POST /:id/close | 终态归档 |
| 批量导出 | 任意 | GET /export?ids= | Excel 导出选中行 |

**交互**：列首 checkbox 多选 → 底部浮现操作栏（与订单列表同模式）。

---

## 八、新建采购单入口

工具行右侧 `+ 新建` 按钮 → `setShowCreateForm(true)` → 弹出创建表单（详见 `./采购单编辑器.md`）。

**Prime 预填**（从订单详情页触发）：
- 订单详情页"生成采购单"按钮 → `primeProcurementCreateFromOrder()` 写 sessionStorage
- 进入采购列表页 → 读取 prime → 自动打开创建表单 + 预填 orderId/customer/物料行

---

## 九、数据模型

### 9.1 PurchaseOrder 列表字段

详见 `./模块概述.md` §五。列表页用到的核心字段：`id / poNumber / status / supplierName / totalAmount / currency / orderDate / expectedDeliveryDate / buyer`。

### 9.2 PurchaseLine 展开字段

`lineNumber / materialCode / description / category / quantity / unit / unitPrice / amount / receivedQuantity / rejectedQuantity`。

### 9.3 MaterialReceipt 展开字段

`receiptNumber / status / receivedDate / totalReceived / totalAccepted / totalRejected / warehouseName`。

---

## 十、API 端点

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/procurement?status=&supplierRelationId=&dateFrom=&dateTo=&search=&limit=&offset=` | 列表查询 |
| GET | `/api/v1/procurement/:id` | 详情（含 lines + receipts，行展开数据源） |

---

## 十一、权限矩阵

| 角色 | 查看列表 | 查看行明细 | 查看金额 | 新建 |
|------|---------|----------|---------|------|
| owner / admin / manager | ✅ 全部 | ✅ | ✅ | ✅ |
| buyer | ✅ 自己建的 | ✅ | ✅ | ✅ |
| finance | ✅ | ⚠️ 仅金额/发票相关 | ✅ | ❌ |
| qc / warehouse | ✅ 同部门 | ⚠️ 仅收料相关 | ❌ | ❌ |
| guest | ❌ | — | — | — |

---

## 十二、四态规范

| 状态 | 触发 | UI 规范 |
|------|------|---------|
| 加载态 | fetch list | Loader2 + "加载采购单..." |
| 空态 | items.length === 0 | "暂无采购单" + 引导新建按钮 |
| 筛选空态 | 有 items 但当前 tab 无数据 | "当前状态下无采购单" |
| 错误态 | fetch 失败 | danger alert + retry |

---

## 十三、业务规则关联

| 规则 | 接入情况 |
|------|---------|
| §5.6 采购单号 PO-YYYY-NNNN | ✅ 列表展示自动生成单号 |
| 供应商拉黑拦截 | ✅ 新建时校验（列表页不拦截查看） |
| 金额分级审批 | ⚠️ 列表页未显示审批状态 badge |
| 交期预警 | ⚠️ expectedDeliveryDate 临近未标注 |

---

## 十四、设计系统约束

| 约束 | 合规说明 |
|------|---------|
| 禁止硬编码 hex | 全部 CSS 变量 |
| 圆角 | 表格 `rounded-card`、badge `rounded-compact`、按钮 `rounded-control` |
| 无阴影 | border + bg tint |
| 状态徽章 | bds-badge 语义变体，不自定义颜色 |
| tabular-nums | 金额/数量列 |
| 行展开动画 | AnimatePresence + height auto |

---

## 十五、交叉链接

| 文档 | 相对路径 |
|------|---------|
| 采购管理模块概述 | `./模块概述.md` |
| 采购单编辑器（新建入口） | `./采购单编辑器.md` |
| 采购到货与质检（收料记录） | `./采购到货与质检.md` |
| 供应商对账 | `./供应商对账.md` |
| 订单详情页（prime 来源） | `../Orders-订单管理/订单详情页.md` |
| 设计系统规范 | `../../01-产品总览/4. 设计系统规范.md` |
| 角色与权限矩阵 | `../../01-产品总览/6. 角色与权限矩阵.md` |

---

## 十六、变更日志

| 日期 | 变更 | 关联 |
|------|------|------|
| 2026-08-15 | 初版采购列表页设计，覆盖状态 tab / 列定义 / 行展开 / 筛选 / 批量操作方案 | — |
