# 采购到货与质检 (Purchase Receipt & QC)

## 一、元信息

| 项 | 值 |
|---|---|
| **定位** | 采购到货登记 + 来料质检 + 入库联动 — PO 确认后收料全流程；MaterialReceipt 记录每次到货的合格/拒收数量，驱动 PO 状态自动流转（PartiallyReceived/Received） |
| **入口** | 采购列表行展开 → 收料记录区 [+ 登记到货]；PO 详情页收料 tab |
| **核心角色** | 后勤（含仓储，原 warehouse，收货登记）、QC（含来料质检，不细分）、业务员（含采购执行，原 buyer，跟进） |
| **范式** | 范式 C — 表单编辑器（收料登记表单）+ 范式 A — 列表（收料记录列表） |
| **优先级** | P0 |
| **实现状态** | ✅ MaterialReceipt 模型 + createMaterialReceipt 服务已落地；✅ PO 状态自动流转（PartiallyReceived/Received）已实现；✅ 收料状态自动判定（Accepted/Rejected/PartiallyAccepted）已实现；✅ MaterialReceived 事件发布 + H1c 交期评分已接入；⚠️ InventoryTransaction 自动入库联动未实现（本文档给出设计）；⚠️ 行级收货数量回写 PurchaseLine.receivedQuantity 未实现 |
| **关联 PRD 章节** | §19（采购到货管理）、§9.3（质量门禁）、§13（供应商管理 H1c 评分） |
| **关联代码** | [procurementService.ts createMaterialReceipt](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementService.ts#L624-L762) 收料核心服务 / [ProcurementManager.tsx](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/ProcurementManager.tsx#L131-L145) 前端收料状态映射 / `schema.prisma` MaterialReceipt 模型（L3046-L3077） |

---

## 二、到货登记流程

### 2.1 整体流程

```
PO Confirmed ──▶ 仓库收货登记 ──▶ QC 质检 ──▶ 入库 ──▶ PO 状态流转
                     │                │          │
                     ▼                ▼          ▼
               MaterialReceipt    合格/拒收   InventoryTransaction
               (Pending)          (Accepted/  (待补)
                                 Rejected/
                                 PartiallyAccepted)
```

### 2.2 收料前置校验

真源 [procurementService.ts createMaterialReceipt L629-L633](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementService.ts#L629-L633)：

```typescript
if (existing.status !== 'Confirmed'
    && existing.status !== 'PartiallyReceived'
    && existing.status !== 'Received') {
  throw new Error(`仅 Confirmed/PartiallyReceived/Received 状态可收料`);
}
```

| PO 状态 | 可收料 | 说明 |
|---------|--------|------|
| Draft / Sent | ❌ | 未确认的 PO 不收料 |
| Confirmed | ✅ | 首次收料 |
| PartiallyReceived | ✅ | 继续收料 |
| Received | ✅ | 超收（已全部到货后补收） |
| Closed / Cancelled | ❌ | 终态不可收料 |

---

## 三、MaterialReceipt 模型（数据真源）

真源 [schema.prisma L3046-L3077](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/prisma/schema.prisma#L3046-L3077)：

| 字段组 | 字段 | 类型 | 说明 |
|--------|------|------|------|
| 主键 | id (`MR_${shortId}`) / receiptNumber / purchaseOrderId | String | 收料单号 + FK |
| 状态 | status | String | Pending/Inspected/Accepted/Rejected/PartiallyAccepted |
| 收货 | receivedDate | String | 收货日期 YYYY-MM-DD |
| 收货人 | receivedBy | String? | 收货人 |
| 检验 | inspectedBy / inspectionDate | String? | 检验员 + 检验日期 |
| 仓库 | warehouseId / warehouseName | String? | 收货仓库 |
| 检验结果 | totalReceived / totalAccepted / totalRejected | Decimal(18,4) | 总收货/合格/拒收 |
| 原因 | rejectionReason / qualityNotes / notes | String? | 不合格原因/质量备注 |

> **注意**：任务描述中的 `POLineReceipt` 实际模型名为 `MaterialReceipt`。本文档统一使用真实模型名。

---

## 四、收料登记表单

### 4.1 表单字段

| # | 字段 | 中文 | 类型 | 必填 | 说明 |
|---|------|------|------|------|------|
| 1 | receiptNumber | 收料单号 | text | ✅ | 手动或自动生成 |
| 2 | receivedDate | 收货日期 | date | ✅ | 默认今天 |
| 3 | receivedBy | 收货人 | text | ❌ | 默认当前用户 |
| 4 | warehouseId | 收货仓库 | selector | ❌ | 仓库选择器 |
| 5 | warehouseName | 仓库名称 | text | ❌ | 选仓库自动带出 |
| 6 | totalReceived | 总收货数量 | number | ✅ | 本次到货总数 |
| 7 | totalAccepted | 合格数量 | number | ✅ | 质检合格数 |
| 8 | totalRejected | 拒收数量 | number | ✅ | 质检不合格数 |
| 9 | rejectionReason | 拒收原因 | text | ❌ | 拒收时必填 |
| 10 | qualityNotes | 质量备注 | textarea | ❌ | 质检说明 |
| 11 | notes | 备注 | textarea | ❌ | |

**校验**：`totalReceived = totalAccepted + totalRejected`（前端应校验，后端当前未强校验）。

### 4.2 收料状态自动判定

真源 [procurementService.ts L657](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementService.ts#L657)：

```typescript
status: input.totalRejected === 0
  ? 'Accepted'
  : input.totalAccepted === 0
    ? 'Rejected'
    : 'PartiallyAccepted'
```

| 条件 | 自动状态 |
|------|---------|
| totalRejected === 0 | Accepted（全部合格） |
| totalAccepted === 0 | Rejected（全部拒收） |
| 两者都 > 0 | PartiallyAccepted（部分合格） |

---

## 五、PO 状态自动流转

真源 [procurementService.ts L638-L648](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementService.ts#L638-L648)：

```typescript
const totalOrdered = existing.lines.reduce((sum, l) => sum + Number(l.quantity), 0);
const totalPreviouslyReceived = existing.lines.reduce((sum, l) => sum + Number(l.receivedQuantity), 0);
const totalNowReceived = totalPreviouslyReceived + input.totalAccepted;

let newStatus;
if (totalNowReceived >= totalOrdered) {
  newStatus = 'Received';        // 全部收齐
} else {
  newStatus = 'PartiallyReceived'; // 部分到货
}
```

| 场景 | 累计合格数 vs 订单数 | 新 PO 状态 |
|------|---------------------|-----------|
| 首次部分收料 | 累计 < 订单数 | PartiallyReceived |
| 首次全部收齐 | 累计 ≥ 订单数 | Received |
| 后续补收 | 累计 < 订单数 | PartiallyReceived |
| 后续收齐 | 累计 ≥ 订单数 | Received |
| 超收 | 累计 > 订单数 | Received |

---

## 六、质检记录

### 6.1 质检流程

```
收货登记(Pending) ──▶ QC 检验(Inspected) ──▶ 判定(Accepted/Rejected/PartiallyAccepted)
```

当前实现为收货时一次性录入质检结果（totalAccepted/totalRejected），无独立"待检→已检"中间态流转。设计建议：

| 阶段 | 状态 | 操作人 | 说明 |
|------|------|--------|------|
| 收货登记 | Pending | warehouse | 仅录 totalReceived，质检字段空 |
| 质检进行 | Inspected | qc | 录入 totalAccepted/totalRejected |
| 判定 | Accepted/Rejected/PartiallyAccepted | 系统 | 自动判定 |

### 6.2 质检字段

| 字段 | 录入人 | 说明 |
|------|--------|------|
| inspectedBy | QC | 检验员 |
| inspectionDate | QC | 检验日期 |
| totalAccepted | QC | 合格数量 |
| totalRejected | QC | 拒收数量 |
| rejectionReason | QC | 拒收原因（拒收时必填） |
| qualityNotes | QC | 质量备注（疵点描述等） |

---

## 七、入库联动（待实现）

⚠️ **当前未实现**。MaterialReceipt 仅记录收料，未自动写入库存账本。以下为设计方案。

### 7.1 InventoryTransaction 模型（待建）

建议新增 `InventoryTransaction` 模型：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键 `IT_${shortId}` |
| transactionType | String | IN（入库）/ OUT（出库）/ ADJUST（调整） |
| materialCode | String? | 物料编码 |
| purchaseOrderId | String? | 关联采购单（入库来源） |
| materialReceiptId | String? | 关联收料记录 |
| warehouseId | String? | 仓库 |
| quantity | Decimal(18,4) | 数量（入库=正，出库=负） |
| unit | String | 单位 |
| balanceAfter | Decimal(18,4) | 库存余额（事务内重算） |
| createdAt | BigInt | 时间戳 |

### 7.2 自动入库联动设计

MaterialReceipt 创建后（status=Accepted/PartiallyAccepted）→ 自动创建 InventoryTransaction：

```
触发点：createMaterialReceipt 事务提交后
条件：totalAccepted > 0
动作：
  INSERT InventoryTransaction {
    transactionType: 'IN',
    materialCode: receipt.materialCode (待补行级关联),
    purchaseOrderId,
    materialReceiptId: receipt.id,
    warehouseId,
    quantity: totalAccepted,  // 仅合格数量入库
    unit,
    balanceAfter: 事务内重算,
  }
```

### 7.3 行级收货数量回写（待补）

当前 `createMaterialReceipt` 操作 PO 级汇总（totalOrdered/totalReceived），未回写 `PurchaseLine.receivedQuantity`。设计建议：

- 收料时按行录入（MaterialReceiptLine 子表，待建）
- 每行收料 → 累加 `PurchaseLine.receivedQuantity` / `rejectedQuantity`
- 行级全部收齐 → 该行标记完成

---

## 八、业务事件

真源 [procurementService.ts L701-L726](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementService.ts#L701-L726)：

**MaterialReceived 事件**（创建收料记录后发布，fire-and-forget）：

```typescript
{
  type: 'MaterialReceived',
  sourceEntityType: 'PurchaseOrder',
  sourceEntityId: purchaseOrderId,
  payload: {
    purchaseOrderId, poNumber, receiptId, receiptNumber,
    supplierName, supplierRelationId,
    totalReceived, totalAccepted, totalRejected,
    warehouseName, purchaseOrderStatus: newStatus,
  }
}
```

### H1c 交期自动评分

全部收齐（newStatus === 'Received'）且有关联供应商 → 自动追加交期评分（非阻塞）：

真源 [procurementService.ts L732-L759](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementService.ts#L732-L759)：

```typescript
if (newStatus === 'Received' && existing.supplierRelationId) {
  const daysLate = (actualMs - expectedMs) / DAY;
  await factoryService.recordAutoEvaluation({
    relationId: existing.supplierRelationId,
    kind: 'delivery',
    score: deliveryScoreForDaysLate(daysLate),
    sourceType: 'purchaseOrder',
    sourceId: purchaseOrderId,
    note: `采购单 ${poNumber} 全部收齐，交期偏差 ${daysLate} 天`,
  });
}
```

- 幂等：同采购单只评一次
- 无档案供应商静默跳过
- daysLate = 实际收货日 - 预计交货日

---

## 九、数据模型

### 9.1 MaterialReceipt（已实现）

详见 §三。

### 9.2 InventoryTransaction（待建）

详见 §七。

### 9.3 PurchaseLine 收货跟踪字段

| 字段 | 类型 | 说明 |
|------|------|------|
| receivedQuantity | Decimal(18,4) | 已收数量（待补回写） |
| rejectedQuantity | Decimal(18,4) | 不合格数量（待补回写） |

---

## 十、API 端点

| 方法 | 路径 | 说明 | 实现状态 |
|------|------|------|---------|
| POST | `/api/v1/procurement/:id/receipts` | 创建收料记录 | ✅ |
| GET | `/api/v1/procurement/:id/receipts` | 查询收料记录列表 | ✅ |
| POST | `/api/v1/inventory/transactions` | 创建库存流水（入库） | ⚠️ 待补 |

---

## 十一、权限矩阵

| 角色 | 登记到货 | 质检录入 | 查看收料记录 | 入库 |
|------|---------|---------|------------|------|
| owner / admin / manager | ✅ | ✅ | ✅ | ✅ |
| warehouse | ✅ | ⚠️ 仅 totalReceived | ✅ | ✅ |
| qc | ⚠️ 仅质检字段 | ✅ | ✅ | ❌ |
| buyer | ❌ | ❌ | ✅ 自己的单 | ❌ |
| finance | ❌ | ❌ | ✅ 金额相关 | ❌ |
| guest | ❌ | — | — | — |

---

## 十二、四态规范

| 状态 | 触发 | UI 规范 |
|------|------|---------|
| 加载态 | 提交收料中 | 按钮 Loader2 + 禁用 |
| 校验错误 | received > accepted + rejected | danger 文案 "总收货 ≠ 合格 + 拒收" |
| 成功 | 收料创建成功 | toast success + 刷新收料列表 + PO 状态更新 |
| 失败 | 后端报错 | toast danger 显示 error.message |
| 空收料列表 | 无收料记录 | "暂无到货记录"灰态 |

---

## 十三、收料状态徽章

真源 [ProcurementManager.tsx RECEIPT_STATUS_BADGE_VARIANT](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/ProcurementManager.tsx#L139-L145)：

| 状态 | 中文 | badge 变体 |
|------|------|-----------|
| Pending | 待检 | neutral |
| Inspected | 已检验 | neutral |
| Accepted | 合格入库 | success |
| Rejected | 不合格 | danger |
| PartiallyAccepted | 部分合格 | warning |

---

## 十四、设计系统约束

| 约束 | 合规说明 |
|------|---------|
| 禁止硬编码 hex | 全部 CSS 变量 |
| 圆角 | 表单 `rounded-card-lg`、badge `rounded-compact` |
| 无阴影 | border + bg tint |
| 数量 tabular-nums | 防数字跳动 |
| 状态徽章 | bds-badge 语义变体 |

---

## 十五、交叉链接

| 文档 | 相对路径 |
|------|---------|
| 采购管理模块概述 | `./模块概述.md` |
| 采购列表页（收料入口） | `./采购列表页.md` |
| 采购单编辑器（PO 头/行） | `./采购单编辑器.md` |
| 供应商对账（到货→发票→付款） | `./供应商对账.md` |
| 质量门禁规则 | `../../03-业务规则/质量门禁规则.md` |
| 实体关系总览 | `../../02-数据模型/实体关系总览.md` |
| 角色与权限矩阵 | `../../01-产品总览/6. 角色与权限矩阵.md` |

---

## 十六、变更日志

| 日期 | 变更 | 关联 |
|------|------|------|
| 2026-08-15 | 初版采购到货与质检设计，覆盖 MaterialReceipt 模型/收料登记/状态自动流转/质检流程/入库联动方案（待实现）/H1c 评分 | — |
