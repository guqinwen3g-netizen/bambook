# 采购管理模块概述 (Procurement Module Overview)

## 一、元信息

| 项 | 值 |
|---|---|
| **模块定位** | 采购单全生命周期管理 — PO 创建 → 审批 → 发送 → 确认 → 到货登记 → 质检 → 入库 → 对账闭环；面料/辅料采购的真源数据载体 |
| **模块边界** | 采购单 CRUD + 状态流转 + 来料检验（MaterialReceipt）+ 供应商对账；不含供应商档案管理（Relations 模块）、不含库存账本（InventoryTransaction 待补） |
| **核心角色** | 业务员（含采购执行，原 buyer，日活最高）、财务（含对账/发票，宽泛容器不细分）、QC（含来料检验，不细分）、后勤（含仓储，原 warehouse，收货入库）、销售主管（审批，部门管理职责） |
| **范式** | 范式 A — 列表 + 编辑器（List + Editor）：列表页筛选/批量操作 → 编辑器头信息+行明细 |
| **优先级** | P0 |
| **实现状态** | ✅ 采购单 CRUD + 状态流转（Draft→Sent→Confirmed→PartiallyReceived/Received→Closed/Cancelled）已落地；✅ 来料检验 MaterialReceipt 已实现；✅ 供应商拉黑拦截 + 业务事件发布 + H1c 自动评分已接入；✅ 采购单号自动生成 PO-YYYY-NNNN；⚠️ 审批流（≤5万双签/>5万总监/首单+1级）未实现；⚠️ InventoryTransaction 自动入库联动未实现；⚠️ 供应商对账独立页未成页 |
| **关联 PRD 章节** | §5.6（采购单号 PO-YYYY-NNNN）、§13（供应商管理）、§9.2（价格审批）、§9（业务规则 10 条） |
| **关联代码** | [procurementService.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementService.ts) 服务层 / [procurementRoute.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementRoute.ts) API 路由 / [ProcurementManager.tsx](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/ProcurementManager.tsx) 前端组件 / `schema.prisma` PurchaseOrder / PurchaseLine / MaterialReceipt 模型 |

---

## 二、模块边界与职责

### 2.1 模块做什么

采购管理覆盖"从需求到付款"的采购全链路：

1. **采购单创建**：手动录入 或 从订单/BOM 触发（prime 预填）
2. **审批流转**（待补）：金额分级审批 + 首单供应商加签
3. **发送确认**：Draft → Sent → Confirmed
4. **到货登记**：MaterialReceipt 收料记录
5. **质检入库**：合格/不合格数量 + 仓库归属（入库联动待补）
6. **对账闭环**：PO → 到货 → 发票 → 付款 全链路核对（待补独立页）

### 2.2 模块不做什么

| 不做的事 | 由谁负责 |
|---------|---------|
| 供应商档案 CRUD | Relations 模块（category=Supplier）+ FactoryProfile |
| BOM 物料清单 | BOM 模块（采购单可关联 bomId） |
| 库存账本 | InventoryTransaction 模块（待建，当前仅 MaterialReceipt 记录收料） |
| 应付发票 | 财务模块 Invoice（采购到货可触发 prime 跳转生成应付发票） |
| 供应商评分 | FactoryEvaluation（H1c 自动评分已接入，明细在供应商模块） |

---

## 三、采购单状态机（核心业务模型）

真源 [procurementService.ts TRANSITIONS](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementService.ts#L92-L100)：

```
Draft ──▶ Sent ──▶ Confirmed ──▶ PartiallyReceived ──▶ Received ──▶ Closed
   │         │          │               │                  │
   │         │          └──▶ Received ──┘                  │
   │         │                (一次性全收)                   │
   │         │                                               │
   └─────────┴──▶ Cancelled ◀──────────────────────────────┘
```

| 状态 | 中文 | 可流转到 | 可编辑/删除 |
|------|------|---------|------------|
| Draft | 草稿 | Sent / Cancelled | ✅ 可编辑可软删 |
| Sent | 已发送 | Confirmed / Cancelled | ❌ |
| Confirmed | 已确认 | PartiallyReceived / Received / Cancelled | ❌ |
| PartiallyReceived | 部分到货 | Received / Closed | ❌ |
| Received | 全部到货 | Closed | ❌ |
| Closed | 已关闭 | — （终态） | ❌ |
| Cancelled | 已取消 | — （终态） | ❌ |

**状态转换校验**：`validateStatusTransition(from, to)` 查 TRANSITIONS 矩阵，非法转换抛 `非法状态转换：${from} → ${to}`。

---

## 四、子文档索引

本模块包含以下子文档（同目录下）：

| # | 子文档 | 路径 | 说明 |
|---|--------|------|------|
| 1 | 模块概述（本文档） | `./模块概述.md` | 模块边界、状态机、子文档索引、数据模型概览 |
| 2 | 采购列表页 | `./采购列表页.md` | 筛选（供应商/状态/日期）、列定义、批量操作 |
| 3 | 采购单编辑器 | `./采购单编辑器.md` | PO 头信息 + PO 行（面料/辅料）+ 交期/数量/单价 + 审批流设计 |
| 4 | 采购到货与质检 | `./采购到货与质检.md` | 到货登记 + 质检记录 + 入库联动设计 |
| 5 | 供应商对账 | `./供应商对账.md` | PO→到货→发票→付款 全链路核对 + 差异处理 |

---

## 五、数据模型概览

### 5.1 PurchaseOrder 模型（采购单头）

真源 [schema.prisma L2965-L3013](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/prisma/schema.prisma#L2965-L3013)：

| 字段组 | 字段 | 说明 |
|--------|------|------|
| 主键 | id (`PO_${shortId}`) / poNumber @unique (`PO-YYYY-NNNN`) | 系统ID + 业务单号 |
| 状态 | status | Draft/Sent/Confirmed/PartiallyReceived/Received/Closed/Cancelled |
| 供应商 | supplierRelationId / supplierName / supplierCode | snapshot FK + 冗余快照 |
| 金额 | currency / totalAmount(Decimal 18,4) / exchangeRate / baseCurrency | 多币种支持 |
| 日期 | orderDate / expectedDeliveryDate / actualDeliveryDate | YYYY-MM-DD |
| 条款 | deliveryTerms / paymentTerms / shipToAddress | 交货/付款/收货 |
| 关联 | orderId / quotationId / bomId | snapshot FK |
| 其他 | buyer / notes / attachments(Json) | 采购员/备注/附件 |
| 时间 | createdAt / updatedAt / deletedAt | 软删除 |

### 5.2 PurchaseLine 模型（采购行明细）

真源 [schema.prisma L3015-L3044](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/prisma/schema.prisma#L3015-L3044)：

| 字段组 | 字段 | 说明 |
|--------|------|------|
| 主键 | id / purchaseOrderId / lineNumber | FK cascade delete |
| 物料 | materialCode / description / category / specification | Fabric/Trimmings/Accessories/Other |
| 数量价格 | quantity / unit / unitPrice / amount | Decimal(18,4)，amount=quantity×unitPrice |
| 收货跟踪 | receivedQuantity / rejectedQuantity | 默认 0，收料时累加 |
| 其他 | notes / createdAt | 备注 |

### 5.3 MaterialReceipt 模型（来料检验记录）

真源 [schema.prisma L3046-L3077](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/prisma/schema.prisma#L3046-L3077)：

| 字段组 | 字段 | 说明 |
|--------|------|------|
| 主键 | id (`MR_${shortId}`) / receiptNumber / purchaseOrderId | 收料单号 |
| 状态 | status | Pending/Inspected/Accepted/Rejected/PartiallyAccepted |
| 收货 | receivedDate / receivedBy / inspectedBy / inspectionDate | 收货人与检验员 |
| 仓库 | warehouseId / warehouseName | 收货仓库 |
| 检验结果 | totalReceived / totalAccepted / totalRejected | Decimal(18,4) |
| 原因 | rejectionReason / qualityNotes / notes | 不合格原因 |

> **注意**：任务描述中的 `POLine` / `POLineReceipt` 实际模型名为 `PurchaseLine` / `MaterialReceipt`。本模块文档统一使用真实模型名。

---

## 六、API 端点清单

真源 [procurementRoute.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementRoute.ts)：

| 方法 | 路径 | 说明 | 实现状态 |
|------|------|------|---------|
| GET | `/api/v1/procurement` | 采购单列表（status/supplier/date/search 过滤） | ✅ |
| GET | `/api/v1/procurement/:id` | 采购单详情（含 lines + receipts） | ✅ |
| POST | `/api/v1/procurement` | 创建采购单（Draft） | ✅ |
| PUT | `/api/v1/procurement/:id` | 更新采购单（仅 Draft） | ✅ |
| DELETE | `/api/v1/procurement/:id` | 软删除（仅 Draft） | ✅ |
| POST | `/api/v1/procurement/:id/send` | 发送（Draft → Sent） | ✅ |
| POST | `/api/v1/procurement/:id/confirm` | 确认（Sent → Confirmed） | ✅ |
| POST | `/api/v1/procurement/:id/cancel` | 取消 | ✅ |
| POST | `/api/v1/procurement/:id/close` | 关闭 | ✅ |
| POST | `/api/v1/procurement/:id/receipts` | 创建来料检验记录 | ✅ |
| GET | `/api/v1/procurement/:id/receipts` | 查询来料记录列表 | ✅ |

---

## 七、业务事件

[procurementService.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementService.ts) 在状态流转时发布事件（fire-and-forget，失败不阻断业务）：

| 事件类型 | 触发时机 | payload 要点 |
|---------|---------|-------------|
| `PurchaseOrderSent` | Draft → Sent | poNumber / supplierName / totalAmount / lineCount |
| `PurchaseOrderConfirmed` | Sent → Confirmed | poNumber / lines 物料明细 |
| `MaterialReceived` | 创建来料记录 | receiptNumber / totalAccepted / totalRejected / newStatus |

**H1c 自动评分**（非阻塞）：
- 全部收齐（Received）→ 供应商交期评分（deliveryScoreForDaysLate）
- 验货结论 → 供应商质量评分（inspectionScoreForResult）

---

## 八、供应商拉黑拦截

```typescript
// procurementService.ts createPurchaseOrder
if (input.supplierRelationId) {
  const factory = await prisma.factoryProfile.findUnique({
    where: { relationId: input.supplierRelationId }
  });
  if (factory && factory.deletedAt === null && factory.blacklistedAt !== null) {
    throw new Error(`该供应商已被拉黑（原因：${factory.blacklistReason}），禁止新建采购单`);
  }
}
```

身份真源：Relation（category=Supplier）→ FactoryProfile 1:1。拉黑供应商禁止新建采购单（PRD 13.1）。

---

## 九、EntityLink 图谱同步

采购单创建/更新时调用 `syncPurchaseOrderReferences` 同步 EntityLink 关联：

| 关联类型 | 来源字段 | 说明 |
|---------|---------|------|
| purchasedFrom | supplierRelationId | 采购自供应商 |
| forOrder | orderId | 为订单采购 |
| fromBom | bomId | 源自 BOM |
| fromQuotation | quotationId | 源自报价 |

软删除时 `deactivateEntityLinks` 失效发出的关联。

---

## 十、权限矩阵

| 角色 | 查看采购单 | 创建/编辑 | 发送/确认 | 收料/质检 | 关闭/取消 | 对账 |
|------|-----------|----------|----------|----------|---------|------|
| owner / admin / manager | ✅ 全部 | ✅ | ✅ | ✅ | ✅ | ✅ |
| buyer（采购员） | ✅ 自己建的 | ✅ | ✅ | ⚠️ 仅录入 | ✅ | ⚠️ 仅查看 |
| finance | ✅ 金额/发票字段 | ❌ | ❌ | ❌ | ❌ | ✅ |
| qc | ✅ 同部门 | ❌ | ❌ | ✅ 来料质检 | ❌ | ❌ |
| warehouse | ✅ 同部门 | ❌ | ❌ | ✅ 收货入库 | ❌ | ❌ |
| guest | ❌ | — | — | — | — | — |

---

## 十一、四态规范

| 状态 | 触发 | UI 规范 |
|------|------|---------|
| 加载态 | 列表 fetch | Loader2 + "加载采购单..." |
| 空态 | items.length === 0 | "暂无采购单"灰态 |
| 错误态 | fetch 失败 | danger alert + retry |
| 无权限 | 403 | warning alert "无权查看采购单" |

---

## 十二、业务规则关联

| 规则 | 接入情况 |
|------|---------|
| §5.6 采购单号 PO-YYYY-NNNN | ✅ nextBusinessNumber 自动生成 |
| §9.2 价格审批（双轨偏差） | ⚠️ 采购单未接价格审批 |
| §13.1 供应商拉黑拦截 | ✅ 已实现 |
| §9 金额分级审批（≤5万/>5万/首单） | ⚠️ 未实现（设计见采购单编辑器文档） |
| H1c 交期/质量自动评分 | ✅ 已实现 |
| EntityLink 图谱 | ✅ 已同步 |

---

## 十三、设计系统约束

| 约束 | 合规说明 |
|------|---------|
| 禁止硬编码 hex | 全部 CSS 变量 |
| 圆角 | 列表行 `rounded-card`、编辑器面板 `rounded-card-lg`、badge `rounded-compact` |
| 无阴影 | border + bg tint |
| 状态徽章 | bds-badge 语义变体（Draft=neutral/Sent=info/Received=success/Cancelled=danger 等） |
| 字重 | font-light（300） |

---

## 十四、待补缺口

| # | 缺口 | 优先级 | 对应子文档 |
|---|------|-------|-----------|
| 1 | 审批流（≤5万 SM+FinMan 双签 / >5万 总监 / 首单供应商 +1 级） | P0 | `./采购单编辑器.md` §审批流 |
| 2 | InventoryTransaction 自动入库联动 | P0 | `./采购到货与质检.md` §入库联动 |
| 3 | 供应商对账独立页（PO→到货→发票→付款） | P1 | `./供应商对账.md` |
| 4 | 差异处理（量差/质差/价差） | P1 | `./供应商对账.md` §差异处理 |
| 5 | 价格审批双轨偏差校验 | P1 | `./采购单编辑器.md` |

---

## 十五、交叉链接

| 文档 | 相对路径 |
|------|---------|
| 采购列表页 | `./采购列表页.md` |
| 采购单编辑器 | `./采购单编辑器.md` |
| 采购到货与质检 | `./采购到货与质检.md` |
| 供应商对账 | `./供应商对账.md` |
| 订单详情页（采购 prime 来源） | `../Orders-订单管理/订单详情页.md` |
| 价格审批规则 | `../../03-业务规则/价格审批规则.md` |
| 实体关系总览 | `../../02-数据模型/实体关系总览.md` |
| 角色与权限矩阵 | `../../01-产品总览/6. 角色与权限矩阵.md` |

---

## 十六、变更日志

| 日期 | 变更 | 关联 |
|------|------|------|
| 2026-08-15 | 初版采购管理模块概述，覆盖状态机 / 子文档索引 / 数据模型 / API / 业务事件 / 权限 / 缺口 | — |
