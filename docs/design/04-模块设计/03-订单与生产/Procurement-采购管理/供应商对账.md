# 供应商对账 (Supplier Reconciliation)

## 一、元信息

| 项 | 值 |
|---|---|
| **定位** | 供应商对账闭环 — PO → 到货 → 发票 → 付款 全链路核对；识别量差/质差/价差三类差异并触发处理流程；财务期末对账的核心工具 |
| **入口** | 采购管理 → 供应商对账 tab（规划）；财务模块 → 应付对账（规划） |
| **核心角色** | 财务（含对账主导，宽泛容器不细分）、业务员（含采购执行，原 buyer，差异解释）、销售主管（审批差异处理，部门管理职责） |
| **范式** | 范式 A — 列表（对账单列表）+ 范式 D — 详情（对账明细四链路核对） |
| **优先级** | P1 |
| **实现状态** | ⚠️ 供应商对账独立页未实现（本文档为设计）；✅ 数据真源已具备（PurchaseOrder + MaterialReceipt + Invoice + PaymentVoucher 模型均已建）；✅ PO→到货链路数据可查（createMaterialReceipt 已记录合格/拒收）；⚠️ 发票/付款与 PO 的关联绑定待补；⚠️ 差异自动识别与处理流程未实现 |
| **关联 PRD 章节** | §19（采购管理对账）、§9（业务规则之信用控制）、财务应付章节 |
| **关联代码** | [procurementService.ts](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementService.ts) PO + 收料数据源 / `schema.prisma` PurchaseOrder / PurchaseLine / MaterialReceipt / Invoice / PaymentVoucher 模型 / [ProcurementManager.tsx](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/ProcurementManager.tsx#L44) `primeFinanceInvoiceCreate` 采购→发票 prime |

---

## 二、对账全链路

### 2.1 四链路核对模型

```
┌───────────┐     ┌───────────┐     ┌───────────┐     ┌───────────┐
│  采购单   │ ──▶ │   到货    │ ──▶ │   发票    │ ──▶ │   付款    │
│ PurchaseOrder│ │MaterialReceipt│ │  Invoice  │ │PaymentVoucher│
├───────────┤     ├───────────┤     ├───────────┤     ├───────────┤
│ 订单数量  │     │ 收货数量  │     │ 发票数量  │     │ 付款金额  │
│ 订单金额  │     │ 合格数量  │     │ 发票金额  │     │ 付款日期  │
│ 单价      │     │ 拒收数量  │     │ 税额      │     │ 核销状态  │
└───────────┘     └───────────┘     └───────────┘     └───────────┘
      │                  │                 │                 │
      ▼                  ▼                 ▼                 ▼
   量差识别           质差识别          价差识别          付款差异
```

### 2.2 对账维度

| 维度 | 比对项 | 差异类型 | 容差 |
|------|--------|---------|------|
| 数量 | PO 订单数 vs 到货合格数 | 量差 | ±2%（行业惯例） |
| 质量 | 到货总数 vs 合格数 | 质差 | 拒收率 ≤5% |
| 价格 | PO 金额 vs 发票金额 | 价差 | ±0.5%（四舍五入容差） |
| 付款 | 发票金额 vs 付款金额 | 付差 | 0（应全额） |

---

## 三、数据模型与关联

### 3.1 PO → 到货（已实现）

| PO 字段 | 到货字段 | 关联 |
|---------|---------|------|
| PurchaseOrder.id | MaterialReceipt.purchaseOrderId | FK cascade |
| PurchaseLine.quantity | MaterialReceipt.totalAccepted | 行级（待补回写） |
| PurchaseOrder.totalAmount | SUM(MaterialReceipt.totalAccepted) | 汇总 |

### 3.2 PO → 发票（待补绑定）

当前 Invoice 模型可通过 `orderId` 关联销售订单，但与 PurchaseOrder 的绑定关系待补。设计建议：

| Invoice 字段 | 关联 PO | 说明 |
|-------------|---------|------|
| purchaseOrderId（待补） | PurchaseOrder.id | 应付发票关联采购单 |
| supplierRelationId | PurchaseOrder.supplierRelationId | 供应商一致 |
| amount | PurchaseOrder.totalAmount | 比对价差 |

**Prime 路径**：采购收料后 `primeFinanceInvoiceCreate()`（[ProcurementManager.tsx L44](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/ProcurementManager.tsx#L44)）→ 财务发票模块创建应付发票，预填采购单信息。

### 3.3 发票 → 付款（财务模块）

| Invoice 字段 | 付款字段 | 关联 |
|-------------|---------|------|
| Invoice.id | PaymentVoucher.invoiceId | FK |
| Invoice.amount | PaymentVoucher.amount | 比对付差 |
| Invoice.dueDate | PaymentVoucher.paymentDate | 付款及时性 |

---

## 四、差异类型与识别

### 4.1 量差（Quantity Variance）

**定义**：采购订单数量 vs 实际到货合格数量差异。

```
量差 = PurchaseLine.quantity - SUM(MaterialReceipt.totalAccepted)
量差率 = 量差 / PurchaseLine.quantity × 100%
```

| 量差率 | 等级 | 处理 |
|--------|------|------|
| |率| ≤ 2% | 正常 | 自动通过，记录备注 |
| 2% < |率| ≤ 5% | 轻微 | 采购员解释原因，财务确认 |
| |率| > 5% | 异常 | 供应商扣款/补货，管理层审批 |

**常见原因**：损耗超预期 / 短装 / 运输破损 / 计量误差。

### 4.2 质差（Quality Variance）

**定义**：到货总数 vs 合格数量差异（拒收部分）。

```
拒收率 = MaterialReceipt.totalRejected / MaterialReceipt.totalReceived × 100%
```

| 拒收率 | 等级 | 处理 |
|--------|------|------|
| 0% | 合格 | 无差异 |
| 0% < 率 ≤ 5% | 轻微 | 退货/折让，扣款 |
| 5% < 率 ≤ 10% | 严重 | 退货 + 供应商整改 |
| 率 > 10% | 批次拒收 | 整批退货 + 供应商评级降级 |

**关联**：MaterialReceipt.rejectionReason / qualityNotes 记录质差原因。

### 4.3 价差（Price Variance）

**定义**：采购单金额 vs 供应商发票金额差异。

```
价差 = Invoice.amount - PurchaseOrder.totalAmount
价差率 = 价差 / PurchaseOrder.totalAmount × 100%
```

| 价差率 | 等级 | 处理 |
|--------|------|------|
| |率| ≤ 0.5% | 正常 | 四舍五入容差，自动通过 |
| 0.5% < |率| ≤ 3% | 轻微 | 采购员核实单价，财务确认 |
| |率| > 3% | 异常 | 供应商对账，补开/红冲发票 |

**常见原因**：汇率波动 / 单价变更未更新 PO / 税率错误 / 运费分摊。

### 4.4 付差（Payment Variance）

**定义**：发票金额 vs 实际付款金额差异。

```
付差 = Invoice.amount - PaymentVoucher.amount
```

| 付差 | 处理 |
|------|------|
| = 0 | 全额付款，核销 |
| > 0 | 部分付款，剩余挂账 |
| < 0 | 超付，需追回或抵扣下期 |

---

## 五、对账单结构

### 5.1 对账单列表

| 列 | 字段 | 说明 |
|----|------|------|
| 供应商 | supplierName | 分组维度 |
| 对账周期 | 月份 | YYYY-MM |
| PO 数 | count | 该供应商本期 PO 数 |
| 订单总额 | SUM(totalAmount) | 本期采购额 |
| 到货合格 | SUM(totalAccepted) | 到货合格金额 |
| 发票总额 | SUM(invoice.amount) | 开票金额 |
| 付款总额 | SUM(payment.amount) | 付款金额 |
| 差异标识 | badge | 量差/质差/价差/付差 标记 |
| 状态 | badge | 待对账/对账中/已核销/有差异 |

### 5.2 对账明细（单 PO 维度）

展开对账单 → 显示该供应商本期每张 PO 的四链路明细：

```
┌─ PO-20260806-001  张家港一厂  [有差异] ─────────────────────┐
│                                                              │
│  ① 采购单    订单数 1200 YD    金额 6,600.00 USD            │
│      ↓                                                       │
│  ② 到货      收货 1200        合格 1180    拒收 20           │
│      ↓  质差: 拒收率 1.7%（轻微）                            │
│  ③ 发票      数量 1180        金额 6,490.00 USD             │
│      ↓  价差: -1.7%（质差导致金额减少，正常）                │
│  ④ 付款      金额 6,490.00    日期 2026-08-25  [已核销]      │
│                                                              │
│  差异汇总: 质差 20 YD（1.7%）→ 折让扣款 110.00 USD          │
│  处理状态: 采购员已确认 → 财务已核销                         │
└──────────────────────────────────────────────────────────────┘
```

---

## 六、差异处理流程

### 6.1 差异识别（自动）

```
对账单生成时自动跑四链路比对：
  for each PO in 周期:
    量差 = compareQuantity(PO.lines, receipts)
    质差 = compareQuality(receipts)
    价差 = comparePrice(PO.totalAmount, invoice.amount)
    付差 = comparePayment(invoice.amount, payment.amount)
  生成差异记录 → 按等级路由处理
```

### 6.2 处理路由

| 差异等级 | 处理人 | 动作 |
|---------|--------|------|
| 正常（容差内） | 系统 | 自动标记"正常差异"，对账单可直接核销 |
| 轻微 | 采购员 | 填写差异原因 → 财务确认 → 核销 |
| 异常 | 管理层 | 审批处理方案（扣款/补货/红冲）→ 执行 → 核销 |

### 6.3 差异处理动作

| 动作 | 适用差异 | 后端 | 说明 |
|------|---------|------|------|
| 折让扣款 | 量差/质差 | 创建 CreditNote（贷项通知单） | 下期 PO 抵扣或退款 |
| 补货 | 量差 | 新建 PO（补货数量） | 供应商补发短缺 |
| 退货 | 质差 | 创建退货单 | 拒收部分退回供应商 |
| 发票红冲 | 价差 | Invoice 红冲 + 重开 | 金额错误重开发票 |
| 供应商整改 | 质差严重 | FactoryEvaluation 降级 | 触发供应商评级调整 |

### 6.4 差异数据模型（待建）

建议新增 `ReconciliationVariance` 模型：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键 |
| purchaseOrderId | String | FK |
| varianceType | String | QUANTITY / QUALITY / PRICE / PAYMENT |
| expectedValue | Decimal | 期望值（PO/发票） |
| actualValue | Decimal | 实际值（到货/付款） |
| varianceAmount | Decimal | 差异额 |
| varianceRate | Decimal | 差异率 |
| severity | String | NORMAL / MINOR / MAJOR |
| reason | String? | 差异原因 |
| resolution | String? | 处理方案 |
| status | String | PENDING / CONFIRMED / RESOLVED |
| resolvedBy | String? | 处理人 |
| resolvedAt | BigInt? | 处理时间 |

---

## 七、对账单生成与周期

### 7.1 对账周期

| 周期 | 触发 | 说明 |
|------|------|------|
| 月度对账 | 每月 1 号定时任务 | 上月所有 Received+ PO 自动生成对账单 |
| 按需对账 | 手动选择供应商 + 日期范围 | 临时对账 |
| PO 级对账 | PO 关闭时 | 单 PO 对账核销 |

### 7.2 对账单状态机

```
Draft（草稿）──▶ Pending（待对账）──▶ Reconciling（对账中）──▶ Reconciled（已核销）
                        │                     │
                        └──▶ HasVariance（有差异）──▶ Resolved（已处理）──▶ Reconciled
```

---

## 八、API 端点（规划）

| 方法 | 路径 | 说明 | 实现状态 |
|------|------|------|---------|
| GET | `/api/v1/procurement/reconciliation?supplierId=&period=` | 对账单列表 | ⚠️ 待补 |
| GET | `/api/v1/procurement/reconciliation/:id` | 对账明细（四链路） | ⚠️ 待补 |
| POST | `/api/v1/procurement/reconciliation/generate` | 生成对账单（手动） | ⚠️ 待补 |
| POST | `/api/v1/procurement/reconciliation/:id/variance` | 登记差异处理 | ⚠️ 待补 |
| POST | `/api/v1/procurement/reconciliation/:id/reconcile` | 核销对账单 | ⚠️ 待补 |

---

## 九、权限矩阵

| 角色 | 查看对账单 | 差异登记 | 差异处理审批 | 核销 |
|------|-----------|---------|------------|------|
| owner / admin / manager | ✅ | ✅ | ✅ 异常级 | ✅ |
| finance | ✅ 全部 | ✅ | ⚠️ 轻微级 | ✅ |
| buyer | ✅ 自己的供应商 | ✅ 解释原因 | ❌ | ❌ |
| guest | ❌ | — | — | — |

---

## 十、四态规范

| 状态 | 触发 | UI 规范 |
|------|------|---------|
| 加载态 | 生成对账单中 | Loader2 + "生成对账单..." |
| 空态 | 周期内无 PO | "本期无采购记录" |
| 有差异 | 差异 badge 高亮 | 量差=warning / 质差=warning / 价差=danger / 付差=danger |
| 已核销 | 全部链路匹配 | success badge "已核销" |

---

## 十一、业务规则关联

| 规则 | 接入情况 |
|------|---------|
| §9 信用控制 | ⚠️ 供应商应付逾期未接对账冻结 |
| 量差容差 ±2% | ⚠️ 待实现 |
| 质差拒收率 ≤5% | ✅ MaterialReceipt 已记录拒收数，对账识别待补 |
| 价差容差 ±0.5% | ⚠️ 待实现 |
| H1c 供应商评分 | ✅ 交期/质量评分已实现，对账差异未纳入评分 |

---

## 十二、可访问性

| 项 | 规范 |
|----|------|
| 对账单行可展开 | 显示四链路明细 |
| 差异 badge 颜色编码 | warning/danger 语义 |
| 差异处理弹窗 | 原因 textarea + 处理方案 select |
| 金额 tabular-nums | 防数字跳动 |
| 周期选择 | 月份选择器 |

---

## 十三、报表导出（规划）

| 报表 | 格式 | 内容 |
|------|------|------|
| 供应商对账单 | PDF | 供应商信头 + 四链路明细 + 差异汇总 + 签字栏 |
| 差异明细表 | Excel | 全部差异 PO 明细 + 处理状态 |
| 付款明细 | Excel | 按供应商/周期的付款记录 |

---

## 十四、设计系统约束

| 约束 | 合规说明 |
|------|---------|
| 禁止硬编码 hex | 全部 CSS 变量 |
| 圆角 | 对账单 `rounded-card-lg`、明细 `rounded-card`、badge `rounded-compact` |
| 无阴影 | border + bg tint |
| 差异 badge | bds-badge 语义变体（warning/danger） |
| 字重 | font-light（300） |
| 金额 tabular-nums | 全部金额列 |

---

## 十五、交叉链接

| 文档 | 相对路径 |
|------|---------|
| 采购管理模块概述 | `./模块概述.md` |
| 采购列表页 | `./采购列表页.md` |
| 采购单编辑器 | `./采购单编辑器.md` |
| 采购到货与质检（到货数据源） | `./采购到货与质检.md` |
| 信用控制规则 | `../../03-业务规则/信用控制规则.md` |
| 业务规则总览 | `../../03-业务规则/业务规则总览.md` |
| 实体关系总览 | `../../02-数据模型/实体关系总览.md` |
| 角色与权限矩阵 | `../../01-产品总览/6. 角色与权限矩阵.md` |

---

## 十六、变更日志

| 日期 | 变更 | 关联 |
|------|------|------|
| 2026-08-15 | 初版供应商对账设计，覆盖四链路核对模型/三类差异识别（量差/质差/价差）/差异处理流程/对账单结构/ReconciliationVariance 模型方案 | — |
