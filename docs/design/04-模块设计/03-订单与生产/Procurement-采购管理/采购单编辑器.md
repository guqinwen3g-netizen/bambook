# 采购单编辑器 (Purchase Order Editor)

## 一、元信息

| 项 | 值 |
|---|---|
| **定位** | 采购单创建/编辑表单 — PO 头信息 + PO 行明细（面料/辅料）+ 交期/数量/单价 + 金额分级审批流；采购数据录入的核心界面 |
| **入口** | 采购列表页 `+ 新建` 按钮 / 订单详情页"生成采购单"prime 预填 / 列表行编辑（仅 Draft） |
| **核心角色** | 业务员（含采购执行，原 buyer，录入）、销售主管（部门管理职责，审批）、财务（宽泛容器不细分，审批）、超级管理员（老板，绝密级审批） |
| **范式** | 范式 C — 表单编辑器 (Form Editor)：头信息区 + 行明细表 + 条款区 + 审批区 |
| **优先级** | P0 |
| **实现状态** | ✅ PO 头信息 + 行明细 CRUD 已落地（Draft 可编辑）；✅ 行金额自动计算 + 总额汇总已实现；✅ 供应商选择 + 拉黑拦截 + PO 号自动生成已实现；⚠️ 审批流（≤5万双签/>5万总监/首单+1级）未实现（本文档给出设计）；⚠️ 仅 Draft 可编辑的限制已实现但无审批锁定 |
| **关联 PRD 章节** | §5.6（采购单号）、§9.2（价格审批）、§9（业务规则） |
| **关联代码** | [ProcurementManager.tsx](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/ProcurementManager.tsx#L180-L200) 前端表单状态 / [procurementService.ts createPurchaseOrder](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementService.ts#L139-L226) 后端创建 / [procurementService.ts updatePurchaseOrder](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementService.ts#L229-L316) 后端更新 |

---

## 二、编辑器骨架

```
┌─────────────────────────────────────────────────────────────────────┐
│  Header: 新建采购单 / 编辑采购单  [取消] [保存草稿] [发送]          │
├─────────────────────────────────────────────────────────────────────┤
│  ── PO 头信息区 (raisedCard) ──                                      │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐         │
│  │ PO 号*      │ 币种*       │ 下单日期*   │ 预计到货    │         │
│  │ [自动生成]  │ [USD ▼]     │ [2026-08-15]│ [2026-08-30]│         │
│  ├─────────────┴─────────────┴─────────────┴─────────────┤         │
│  │ 供应商*  [RelationCombobox ▼]  供应商编码 [自动带出]   │         │
│  │ 采购员   [输入]            关联订单  [订单选择器]      │         │
│  └────────────────────────────────────────────────────────┘         │
├─────────────────────────────────────────────────────────────────────┤
│  ── 行明细区 (raisedCard) ──  [+ 添加行]                            │
│  ┌──────┬──────────┬──────┬──────┬──────┬──────┬──────┬─────┬───┐  │
│  │ 行号 │ 物料编码 │品名* │分类  │规格  │数量* │单价* │金额 │删 │  │
│  │  1   │ F001     │面料A│Fabric│150CM │1200  │ 5.5  │6600 │ 🗑 │  │
│  │  2   │ T001     │拉链 │Trimm │3#    │2000  │ 0.8  │1600 │ 🗑 │  │
│  └──────┴──────────┴──────┴──────┴──────┴──────┴──────┴─────┴───┘  │
│  行金额自动计算：amount = quantity × unitPrice                       │
│  总金额：8,200.00 USD                                                │
├─────────────────────────────────────────────────────────────────────┤
│  ── 条款区 (raisedCard) ──                                           │
│  交货条款 [输入]  付款条款 [输入]  收货地址 [输入]                   │
│  汇率 [1 USD=7.2 CNY]  本位币 [CNY]  备注 [textarea]                │
├─────────────────────────────────────────────────────────────────────┤
│  ── 审批区（待实现） ──                                              │
│  当前审批级别：[无需审批 / SM+FinMan 双签 / 总监审批]                │
│  审批原因：首单供应商 / 金额超标 / ...                              │
│  审批人：[业务员签字] [财务签字] [总监签字]                          │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 三、PO 头信息字段

### 3.1 字段清单

真源 [ProcurementManager.tsx form 状态](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/ProcurementManager.tsx#L194-L210) + [CreatePurchaseOrderInput](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementService.ts#L40-L60)：

| # | 字段 | 中文 | 类型 | 必填 | 默认值 | 说明 |
|---|------|------|------|------|--------|------|
| 1 | poNumber | PO 号 | text | ✅ | 自动生成 PO-YYYY-NNNN | nextBusinessNumber；可手填，校验唯一 |
| 2 | currency | 币种 | select | ✅ | USD | USD / CNY / EUR |
| 3 | orderDate | 下单日期 | date | ✅ | 今天 | YYYY-MM-DD |
| 4 | expectedDeliveryDate | 预计到货 | date | ❌ | — | YYYY-MM-DD |
| 5 | supplierRelationId | 供应商 | combobox | ❌ | — | RelationCombobox（category=Supplier） |
| 6 | supplierName | 供应商名称 | text | ❌ | 选供应商自动带出 | 冗余快照 |
| 7 | supplierCode | 供应商编码 | text | ❌ | 自动带出 | |
| 8 | buyer | 采购员 | text | ❌ | 当前用户 | |
| 9 | orderId | 关联订单 | selector | ❌ | prime 预填 | 从订单触发采购时带入 |
| 10 | quotationId | 关联报价 | text | ❌ | — | |
| 11 | bomId | 关联 BOM | text | ❌ | — | |
| 12 | deliveryTerms | 交货条款 | text | ❌ | — | FOB / CIF / EXW 等 |
| 13 | paymentTerms | 付款条款 | text | ❌ | — | T/T 30% 预付等 |
| 14 | shipToAddress | 收货地址 | text | ❌ | — | |
| 15 | exchangeRate | 汇率 | number | ❌ | — | 多币种换算 |
| 16 | baseCurrency | 本位币 | text | ❌ | CNY | |
| 17 | notes | 备注 | textarea | ❌ | — | |

### 3.2 供应商选择交互

| 交互 | 设计 |
|------|------|
| 选择器 | `RelationCombobox`（与订单详情页同组件） |
| 筛选范围 | category=Supplier 且 FactoryProfile.blacklistedAt=null |
| 自动填充 | 选供应商 → 带出 supplierName / supplierCode 快照 |
| 拉黑拦截 | 选已拉黑供应商 → danger toast + 阻止选择（[procurementService.ts L141-L146](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementService.ts#L141-L146)） |

---

## 四、PO 行明细（面料/辅料）

### 4.1 行字段

真源 [ProcurementManager.tsx DraftLine](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/ProcurementManager.tsx#L156-L166) + [PurchaseLineInput](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementService.ts#L27-L36)：

| # | 字段 | 中文 | 类型 | 必填 | 默认值 | 说明 |
|---|------|------|------|------|--------|------|
| 1 | lineNumber | 行号 | auto | ✅ | 1,2,3... 递增 | 自动编号 |
| 2 | materialCode | 物料编码 | text | ❌ | — | 面料编码/辅料编码 |
| 3 | description | 品名描述 | text | ✅ | — | 必填，后端校验 |
| 4 | category | 分类 | select | ❌ | Fabric | Fabric/Trimmings/Accessories/Other |
| 5 | specification | 规格 | text | ❌ | — | 例 150CM / 180GSM |
| 6 | quantity | 数量 | number | ✅ | — | 必填，4 位小数 |
| 7 | unit | 单位 | select | ✅ | YD | YD/M/KG/PC/SET |
| 8 | unitPrice | 单价 | number | ✅ | — | 必填，4 位小数 |
| 9 | amount | 行金额 | auto | ✅ | = qty × price | 自动计算，只读 |
| 10 | notes | 备注 | text | ❌ | — | |

### 4.2 行分类枚举

`LINE_CATEGORIES = ['Fabric', 'Trimmings', 'Accessories', 'Other']`：

| 分类 | 中文 | 适用 |
|------|------|------|
| Fabric | 面料 | 主面料采购 |
| Trimmings | 辅料 | 拉链/纽扣/花边等 |
| Accessories | 配件 | 吊牌/包装等 |
| Other | 其他 | 其他物料 |

### 4.3 单位枚举

`UNITS = ['YD', 'M', 'KG', 'PC', 'SET']`：

| 单位 | 中文 | 适用 |
|------|------|------|
| YD | 码 | 面料（默认） |
| M | 米 | 面料/辅料 |
| KG | 千克 | 印花/染整 |
| PC | 件 | 成品/配件 |
| SET | 套 | 配件 |

### 4.4 行金额自动计算

真源 [procurementService.ts calcLineAmount](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementService.ts#L118-L120)：

```typescript
function calcLineAmount(quantity, unitPrice): number {
  return Math.round(quantity * unitPrice * 10000) / 10000;  // 4 位小数精度
}
function calcTotalAmount(lines): number {
  return lines.reduce((sum, l) => sum + calcLineAmount(l.quantity, l.unitPrice), 0);
}
```

前端编辑时实时联动：数量/单价变化 → 行金额重算 → 总金额重算。

---

## 五、保存与发送

### 5.1 保存草稿（Draft）

`POST /api/v1/procurement`（创建）或 `PUT /api/v1/procurement/:id`（更新，仅 Draft）。

**后端校验**（[procurementRoute.ts L89-L99](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementRoute.ts#L89-L99)）：
- poNumber / currency / orderDate 必填
- lines 非空
- 每行 description / unit / quantity / unitPrice 必填
- poNumber 唯一性（已存在 → 409）
- 供应商未拉黑

### 5.2 发送（Draft → Sent）

`POST /api/v1/procurement/:id/send` → 状态流转 + 发布 `PurchaseOrderSent` 事件。

发送后采购单锁定不可编辑（仅 Draft 可编辑）。

### 5.3 编辑限制

真源 [procurementService.ts updatePurchaseOrder L235-L237](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/src/procurement/procurementService.ts#L235-L237)：

```typescript
if (existing.status !== 'Draft') {
  throw new Error(`采购单 ${id} 状态为 ${existing.status}，仅 Draft 状态可编辑`);
}
```

更新行明细时先 `deleteMany` 旧行再 `createMany` 新行（全量替换）。

---

## 六、审批流设计（待实现）

⚠️ **当前未实现**。采购单创建后直接 Draft → Sent，无审批环节。以下为 PRD §9.2 设计方案。

### 6.1 审批分级规则

| 条件 | 审批级别 | 审批人 | 说明 |
|------|---------|--------|------|
| totalAmount ≤ 50,000（本位币 CNY 换算后） | Level 1 | 业务员（SM）+ 财务（FinMan）双签 | 常规金额双签 |
| totalAmount > 50,000 | Level 2 | 总监审批 | 大额单总监批 |
| 首单供应商（该 supplierRelationId 无历史 Confirmed+ PO） | +1 级 | 在原级别基础上加签 | 首次合作加严 |

**金额换算**：`totalAmount × exchangeRate` → 本位币 CNY，与 50,000 比较。

### 6.2 审批状态机扩展

当前状态机 `Draft → Sent` 之间插入审批环节：

```
Draft ──▶ PendingApproval ──▶ Sent ──▶ Confirmed ──▶ ...
              │
              ├─ Level1: SM签 → FinMan签 → Sent
              ├─ Level2: 总监签 → Sent
              └─ 首单+1级: 在原级别 + 附加审批人 → Sent
```

新增状态 `PendingApproval`（待审批）/ `Approved`（已审批）。

### 6.3 审批数据模型（待建）

建议新增 `PurchaseApproval` 模型：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键 |
| purchaseOrderId | String | FK |
| level | Int | 1 / 2 / 3（首单加签） |
| requiredRole | String | SM / FinMan / Director |
| approverId | String? | 审批人 |
| approvedAt | BigInt? | 审批时间 |
| status | String | Pending / Approved / Rejected |
| comment | String? | 审批意见 |

### 6.4 审批交互

| 交互 | 设计 |
|------|------|
| 触发审批 | 保存草稿后点"提交审批"→ 计算 level → 创建 N 条 PurchaseApproval（按级别） |
| 审批人视图 | 审批人的采购列表 tab 新增"待我审批" |
| 审批动作 | 通过/驳回 + 意见；全部通过 → 状态 Sent |
| 驳回 | 任一驳回 → 状态回退 Draft + 通知采购员 |
| 首单判定 | 查 PurchaseOrder where supplierRelationId + status in [Confirmed, PartiallyReceived, Received, Closed] count === 0 |

### 6.5 审批锁定

- PendingApproval 状态：采购单只读，禁止编辑
- 审批中行明细锁定，禁止修改数量/单价（防审批后篡改）

---

## 七、Prime 预填（从订单触发）

真源 [ProcurementManager.tsx L51-L91](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/components/ProcurementManager.tsx#L51-L91)：

订单详情页"生成采购单"按钮 → `primeProcurementCreateFromOrder()` 写 sessionStorage：

```typescript
interface ProcurementCreatePrime {
  orderId: string;
  poNumber?: string;
  materialCode?: string;
  description?: string;
  quantity?: number;
  unit?: string;
}
```

进入采购列表页 → 读取 prime → 自动打开创建表单 + 预填 orderId + 首行物料信息 → 用后 `clearProcurementCreatePrime()` 清除。

---

## 八、数据模型

详见 `./模块概述.md` §五。编辑器涉及：
- **PurchaseOrder**：头信息 17 字段
- **PurchaseLine**：行明细 10 字段
- **PurchaseApproval**（待建）：审批记录

---

## 九、API 端点

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/procurement` | 创建（Draft） |
| PUT | `/api/v1/procurement/:id` | 更新（仅 Draft） |
| POST | `/api/v1/procurement/:id/send` | 发送（Draft → Sent） |
| POST | `/api/v1/procurement/:id/submit-approval` | 提交审批（待补） |
| POST | `/api/v1/procurement/:id/approve` | 审批通过（待补） |
| POST | `/api/v1/procurement/:id/reject` | 审批驳回（待补） |

---

## 十、权限矩阵

| 角色 | 创建 | 编辑（Draft） | 提交审批 | 审批 | 发送 |
|------|------|-------------|---------|------|------|
| owner / admin / manager | ✅ | ✅ | ✅ | ✅（任意级） | ✅ |
| buyer | ✅ 自己建的 | ✅ 自己的 | ✅ | ❌ | ✅（审批后） |
| sales（SM） | ❌ | ❌ | ❌ | ✅ Level1 | ❌ |
| finance（FinMan） | ❌ | ❌ | ❌ | ✅ Level1 | ❌ |
| director（总监） | ❌ | ❌ | ❌ | ✅ Level2 | ❌ |
| guest | ❌ | — | — | — | — |

---

## 十一、四态规范

| 状态 | 触发 | UI 规范 |
|------|------|---------|
| 加载态 | 保存/发送中 | 按钮 Loader2 spin + 禁用 |
| 校验错误 | 必填缺失 | 字段下方 danger 文案 |
| 保存成功 | 创建/更新成功 | toast success + 关闭表单 |
| 保存失败 | 后端报错 | toast danger 显示 error.message，不关闭表单（保留输入） |

---

## 十二、业务规则关联

| 规则 | 接入情况 |
|------|---------|
| §5.6 采购单号 PO-YYYY-NNNN | ✅ 自动生成 |
| §9.2 价格审批 | ⚠️ 未实现（本文档 §六设计） |
| 供应商拉黑拦截 | ✅ 已实现 |
| 金额联动（qty×price→amount→total） | ✅ 前端实时 + 后端 calcLineAmount |
| 仅 Draft 可编辑 | ✅ 后端强校验 |
| EntityLink 图谱同步 | ✅ syncPurchaseOrderReferences |

---

## 十三、可访问性

| 项 | 规范 |
|----|------|
| 必填字段标记 | `*` 红色星号 |
| Tab 顺序 | 头信息 → 行明细（逐行逐字段）→ 条款 → 操作按钮 |
| 行删除确认 | 二次确认弹窗（防误删） |
| 金额 tabular-nums | 防数字跳动 |
| 表单滚动 | 超长表单内部滚动，Header/Footer 固定 |

---

## 十四、设计系统约束

| 约束 | 合规说明 |
|------|---------|
| 禁止硬编码 hex | 全部 CSS 变量 |
| 圆角 | 面板 `rounded-card-lg`、输入框 `rounded-control`、按钮 `rounded-control` |
| 无阴影 | border + bg tint |
| 输入框 | bds-input 规格，focus 时 brand-blue ring |
| 字重 | font-light（300），label font-normal |

---

## 十五、交叉链接

| 文档 | 相对路径 |
|------|---------|
| 采购管理模块概述 | `./模块概述.md` |
| 采购列表页（编辑器入口） | `./采购列表页.md` |
| 采购到货与质检（发送后流转） | `./采购到货与质检.md` |
| 供应商对账 | `./供应商对账.md` |
| 订单详情页（prime 来源） | `../Orders-订单管理/订单详情页.md` |
| 价格审批规则 | `../../03-业务规则/价格审批规则.md` |
| 业务规则总览 | `../../03-业务规则/业务规则总览.md` |
| 角色与权限矩阵 | `../../01-产品总览/6. 角色与权限矩阵.md` |

---

## 十六、变更日志

| 日期 | 变更 | 关联 |
|------|------|------|
| 2026-08-15 | 初版采购单编辑器设计，覆盖头信息/行明细/金额计算/审批流方案（待实现）/prime 预填 | — |
