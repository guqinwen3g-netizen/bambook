# Bambook 多会话协同推进纪律（2026-08-17）

> **定位**：本文档是多会话并行开工的唯一协调真源。依据两份评审输入制定：
> 1. 全栈综合审计（四轨，HEAD=`0d2775c`，综合 ≈84/100）
> 2. 前端设计地毯式评审报告（2026-08-17，6.4/10，批 A-J + FR-004）
>
> **生效条件**：产品负责人确认本文档后，各波次会话方可开工（解除 FR-005 冻结）。
> **铁律**：任何会话不得跨分工边界改文件；任何批次开工前先写防回退断言；任何提交前三绿。

---

## 一、综合工作台账（两份报告合并去重）

| 工作面 | 内容 | 来源 | 规模 |
|---|---|---|---|
| **W-FE 前端视觉收编** | 批 A（raw 语义色→token ~75）/ 批 B（遮罩统一 8）/ 批 C（嵌套实色块→bds-inset ~8）/ 批 D（裸 rounded ~10）/ 批 E（手写主按钮 ~14）/ 批 F（font-black 1）/ 批 G（`dark:` 收编 256，分批）/ 批 H（BDS 雾化分类色板 + TraceabilityPanel）/ 批 I（死代码 GarmentOrders）/ 批 J（UserAvatar 阴影、ErrorBanner 统一）/ alert→BDS dialog 收敛（161 处/19 文件） | 前端评审 + 全栈审计轨B | ~550 处 |
| **W-BE 后端安全契约** | 信用 `shiftUsedAmount` 原子化 / JWT_SECRET 生产断言 / DR-013 例外门禁接线（shipping/samples 消费端）/ MOQ 重算事件订阅 / commit 收口 toolId 绑定（L-1）/ orders/route 裸错误泄露 / quotationRoute 鉴权格式统一 | 全栈审计轨A/C/D | 7 项 |
| **W-FN 功能建模补强** | DR-016 合票建模（多对多分配 + 同客户同业务线校验 + 跨票累计上限 + 四方主体）/ DR-041 QC/后勤角色容器内置 + SYSTEM_ROLE_META 残留清理 / 价格偏差规则 ①②④ 接入（设计文档已自标 P0/P1）/ Agent 工具缺口（采购单只读、BOM、退税、报关单列表、核销明细列表） | 全栈审计轨A/C | 4 项 |
| **W-QA 测试文档补强** | project_rules.md 漂移修正（HEAD/测试数/审计版本）/ MOQ 双触发测试 / 交期锁死显式错误码测试 / marketing、shared 零测试域补底 | 全栈审计轨D | 4 项 |
| **W-ARCH 架构专项（冻结）** | FR-004 Compiled 方案 C：废弃运行时双路径、模板单向回收、约束转测试断言（§5.4 四阶段） | 前端评审 FR-004 | 独立专项 |

**依赖关系**：W-FE 批 G 与 W-ARCH 同触 4 个 compiled 页面区域，W-ARCH 必须等 W-FE 批 A-J 全部终审后单独立项（FR-004/FR-005 已确认）；W-FN 的 DR-016 与 W-BE 的 DR-013 同触 shipping 域，**合并进同一会话执行**（见分工）。

---

## 二、会话分工矩阵

| 会话 | 角色 | 工作面 | 拥有文件（排他写） | 禁止触碰 |
|---|---|---|---|---|
| **S-FE** | 前端视觉收编 | W-FE 全部批次 | `components/**`、`src/**`、`styles/**`、`tailwind.config.js`、`scripts/check-design-tokens.sh`、`scripts/*baseline*.json`、`docs/design-system/**`、`styles/bds/showcase.html` | `server/**`、`App.tsx`（锁定）、`docs/design/**`（只读引用）、`.trae/**` |
| **S-BE** | 后端安全 + 功能建模 | W-BE 全部 + W-FN 全部 | `server/src/**`（生产代码 + 既有测试改写）、`server/prisma/**`、`server/ops-panel/**` | `components/**`、`src/**`、`styles/**`、`scripts/check-design-tokens.sh`、`.trae/**` |
| **S-QA** | 测试与文档补强 | W-QA 全部 | 仅**新增**测试文件（命名 `*.regression.test.ts`，落 `server/src/**/__tests__/`）、`.trae/rules/project_rules.md`、`.trae/documents/**` | 一切既有文件的修改（生产代码/既有测试/前端全部） |
| **S-ARCH** | Compiled 架构专项 | W-ARCH（波次 5 才启动） | 启动时独占 `App.tsx`、`components/ui/osCompiler/**`、`components/ui/compiled*`、4 页 Manager | 未启动前冻结 |
| **总控**（主会话） | 集成与仲裁 | 波次编排、合并、终审 | 本文档、`docs/design/10-评审与决策/**` | 不直接改业务代码 |

**单写者规则**：每个文件在任意时刻只有一个会话可写。共享热点文件的归属：
- `App.tsx` → 锁定至波次 5，归 S-ARCH 独占；此前任何会话不得修改（含 import 增删）
- `server/prisma/schema.prisma` → S-BE 独占（DR-016 迁移一并由其执行）
- `scripts/check-design-tokens.sh` 及基线 json → S-FE 独占
- `package.json` / lockfile（根 + server + ops-panel）→ 全员冻结；新增依赖须向总控申报，由总控单点执行

---

## 三、工作区与 Git 纪律

1. **worktree 隔离**：S-FE / S-BE / S-QA 各在独立 git worktree 开工（历史教训：多 IDE 实例同文件并发回写曾造成修改丢失；worktree 从物理上杜绝）：
   - `git worktree add ../bambook-fe -b wave/fe-visual`
   - `git worktree add ../bambook-be -b wave/be-security`
   - `git worktree add ../bambook-qa -b wave/qa-docs`
2. **主 worktree（本目录）保持只读集成**：dev server 继续跑主目录；各分支由总控按波次合并，合并前在被合并 worktree 内完成三绿。
3. **提交粒度**：一个批次一个 commit（批 A/B/C…各自独立），commit message 前缀 `wave(fe|be|qa): 批X — …`；禁止跨批次混合提交。
4. **三绿门禁**（每次提交前在被修改的 worktree 内执行，缺一不可）：
   - `npx tsc --noEmit --skipLibCheck`（前端会话）/ `cd server && npx tsc --noEmit --skipLibCheck`（后端会话）
   - `npm run check:tokens`（S-FE 必跑；S-BE 不涉及样式可免）
   - 受影响测试套件：S-FE `npm test`；S-BE `cd server && npx vitest run`；S-QA 运行新增测试文件
5. **合并窗口**：总控在每个波次末统一 rebase + 合并 + 全量三绿 + 后端全量 vitest；波次中各会话不得自行合 main。

---

## 四、防回退断言先行（开工前置）

依据前端评审「后续动作 1」：**任何修复批次动手前，先把该批次的 grep 断言写进守卫体系**（基线计数模式：记录当前数，只允许单调下降，禁止新增）：

| 断言 | 当前基线 | 目标 |
|---|---|---|
| `dark:` 变体计数 | 256 | ≤10 |
| raw 语义色 `text-(red\|emerald\|blue\|rose\|amber\|orange\|yellow)-[0-9]` | ~75 | 0 |
| `bg-black/\d` 自造遮罩 | 8 | 0 |
| 裸 `rounded`（非 bds 刻度） | ~10 | 0 |
| 手写主按钮 `bg-\[var\(--os-vnext-brand-blue\)\]` | ~14 | 0 |
| `font-black` | 1 | 0 |

断言归 S-FE 写入 `scripts/check-design-tokens.sh` 与基线 json（其排他拥有），波次 1 第一批 commit 必须是「断言基线入库」而非修复。

---

## 五、波次计划

| 波次 | S-FE | S-BE | S-QA | 出口标准 |
|---|---|---|---|---|
| **W0 准备** | 断言基线入库 | — | project_rules.md 漂移修正（HEAD=`0d2775c`、测试数后端 3728/前端 2343、审计 v31.1） | 断言可跑、文档归真 |
| **W1 并行** | 批 A-F 机械替换（一次三绿一个 commit/批） | 3 Medium（信用原子化 / JWT 断言 / DR-013 接线）+ L-1 toolId 绑定 + orders 裸错误 + quotationRoute 鉴权统一 | MOQ 双触发 + 交期锁死回归测试（新增独立文件） | 三绿；信用并发测试新增；DR-013 门禁点有真实消费 |
| **W2 并行** | 批 H（雾化分类色板 → tokens.css + showcase + 应用规范 + TraceabilityPanel 接入） | DR-041 QC/后勤容器 + SYSTEM_ROLE_META 清理；价格偏差 ①②④ 接入 | marketing / shared 零测试域补底 | 三绿；showcase 同步；双主题截图 |
| **W3 并行** | 批 G `dark:` 收编（按文件分批，重灾区 KnowledgeBase 80 → ProductsManager 46 → AdminPanel…，每批双主题截图复核） | DR-016 合票建模（schema + service + 校验 + 迁移） | 旁路回归：每批收编后抽测对应页测试 | `dark:` 计数单调降至 ≤10 |
| **W4 并行** | 批 I/J + alert→BDS dialog 收敛（161 处，先建 bds-dialog/bds-toast 组件再逐文件替换） | Agent 工具缺口 5 类（采购单只读/BOM/退税/报关单列表/核销明细列表） | 全量回归 + 视觉 quarantine 复核 | 前端评审 10 条验收标准全过 |
| **W5 独占** | — | — | — | **S-ARCH 启动 FR-004 方案 C（§5.4 四阶段），App.tsx 解锁，单会话执行** |
| **终审** | 总控按评审第四节 10 条逐项复核 + 30 页深浅双主题截图走查 + 全量三绿 | | | 出具终审结论 |

**波次间硬约束**：
- W1-W4 任意时刻三会话并行上限 = 3；S-ARCH 不得与任何波次并行
- W-FN 的 DR-016（schema 变更）必须在 W-BE 会话内串行，不与 DR-013 同 commit
- 批 G 每批收编文件数 ≤ 5 个，批间跑 `npm test` 防视觉断言断裂

---

## 六、冲突仲裁与升级路径

1. **文件争用**：发现分工表未覆盖的文件需修改时，停止动手 → 报总控登记归属 → 更新本文档分工矩阵后再改。
2. **跨域依赖**：S-FE 需要后端改接口 / S-BE 需要前端配合时，在本文档「跨会话请求」节登记，由总控排入下一波次，禁止私自从对方目录下手。
3. **合并冲突**：总控合并时若两分支触同文件（说明分工表失效），以分工矩阵为裁决依据，越界方 rebase 重做。
4. **评审结论分歧**：设计语义争议以 `docs/design/`（设计真源）+ BDS 真源（tokens.css/components.css）为准；工程取舍争议由总控依据系统思维原则裁决并记录决策。
5. **热修复例外**：生产环境 P0 可不按波次，但仍须遵守文件归属 + 三绿，事后补登记。

### 6.1 跨会话请求登记（QA-SEC 批，2026-08-17 S-QA 上报 / 总控逐条代码核实通过）

| 编号 | 严重级 | 域 | 现象与证据 | 修复方向 | 归属 | 状态 |
|---|---|---|---|---|---|---|
| QA-SEC-1 | **High**（行级权限旁路） | marketing | `listCampaigns` 中 `buildScopeWhere` 产出的 `{ OR: [...] }`（dept 规则）被 search 分支 `where.OR = [...]` 直接覆盖，dept 用户带 search 参数可越权见全公司 campaign（`marketingService.ts:79-89`） | `AND: [scopeWhere, { OR: searchParts }]` 组合，禁止顶层 OR 直接赋值 | S-BE | ⬜ 排队（W1 后） |
| QA-SEC-2 | **High**（横向越权改删） | marketing | `updateLead`/`deleteLead` 仅校验 `kind==='self'`，dept 规则下无校验可改删他组 lead；`updateLead:183` 的 `scopeWhere` 是死代码（`marketingService.ts:179-206`） | 改用 `findFirst({ id, deletedAt: null, ...scopeWhere })` 模式（与 listLeads/createLead 一致）替代手工 resolver 判断 | S-BE | ⬜ 排队（W1 后） |
| QA-SEC-3 | Medium（编号契约静默破坏） | marketing | `createCampaign` 编号生成失败被 try/catch 吞掉，`code=null` 静默落库（`marketingService.ts:106-109`），违反 PRD 5.6 服务端强制编号契约 | 编号生成失败必须 throw，禁止 code=null 落库 | S-BE | ⬜ 排队（W1 后） |
| QA-SEC-4 | Medium（并发可用性 + 格式契约） | shared | A：`businessNumberService` docstring 声称 FOR UPDATE 实为 upsert，首建行并发 P2002 → 500；B：`db.businessSequence` 缺失时 fallback 产出时间戳+随机后缀编号，破坏 NNNN 格式且非单调，仅 warn（`businessNumberService.ts:89-120`） | A：P2002 捕获重试或事务内原生 `SELECT FOR UPDATE`；B：生产环境 throw（降级仅限显式测试注入），docstring 如实修正 | S-BE | ⬜ 排队（W1 后） |

**测试联动注意**：S-QA W2 已交付 `marketing.regression.test.ts` / `businessNumber.regression.test.ts`。若 QA-SEC 修复导致 W2 测试锁定旧（越权/静默）行为而失败，S-BE 不得自行改测试文件，须报总控协调（总控授权后由 S-BE 更新测试期望值并在 commit message 注明，或重开 S-QA 会话处理）。

**S-BE 工作队列（更新）**：W1（进行中：信用原子化 + DR-013 接线 + 3 契约）→ **QA-SEC 批（4 项，权限类 High 优先）** → W3 DR-016 合票建模。

---

## 七、开工前检查单（每个会话首次开工必过）

- [ ] 已读本文档分工矩阵，确认自己拥有/禁止的文件清单
- [ ] 已在专属 worktree 分支上（`git branch --show-current` 非 main）
- [ ] 已读对应评审报告原文（S-FE 读前端评审批 A-J 与豁免清单；S-BE 读全栈审计问题清单）
- [ ] 首个 commit 是断言基线（S-FE）或纯测试新增（S-QA），不是修复
- [ ] 移动端冻结区（`pwa/`、`md:` 类、`isMobile` 分支）不碰——移动端项目整体搁置中
- [ ] 豁免清单不动：3D 地球 WebGL 色值 / macOS 三色灯 / 品牌 SVG / 邮件与发票模板 / PandaLab 开发工具

---

## 八、决策记录

| 编号 | 决策 | 依据 |
|---|---|---|
| CD-001 | 五工作面合并去重，四会话 + 总控分工 | 两份评审报告合并 |
| CD-002 | worktree 物理隔离 > 同目录文件锁 | 历史多 IDE 实例回写事故 |
| CD-003 | 断言基线先于修复入库 | 前端评审后续动作 1 |
| CD-004 | W-ARCH（FR-004）冻结至 W4 终审后独占执行 | FR-004/FR-005 |
| CD-005 | DR-016 并入 S-BE（与 DR-013 同 shipping 域避免跨会话争文件） | 单写者规则 |
| CD-006 | 移动端冻结区全会话禁碰 | 项目硬约束（2026-08-10） |
| CD-007 | 前端收编从"违规点驱动"升级为"页面规格化驱动"（W-PG 工作面，见第十节）：先定页面骨架规格 → 30 页台账 → 逐页全维主刀销号；批 G（dark: 收编）并入逐页流程，不再按文件类型单独推进 | 2026-08-17 产品负责人反馈：按钮尺寸/filterbar 布局类问题 grep 范式抓不到，同页反复开刀无终态责任人 |

---

## 九、总控工作台（回报格式 / 验收清单 / 下一波次预布置）

### 9.1 会话完工回报格式（执行会话完成一个波次后必须按此格式回报）

```
【会话】S-FE / S-BE / S-QA
【波次】W1
【完成批次】批A / 批B / …（逐项列出）
【commit 清单】hash + message（按批次顺序）
【基线变化】check:tokens 收编行新旧值对照（如 raw_semantic 131→96）；S-BE/S-QA 填 N/A
【三绿证据】tsc exit 0 截图或尾行 / check:tokens 总结行 / 测试 Summary 行（Tests N passed）
【越界声明确认】未触碰分工矩阵外任何文件（git diff --name-only 全清单附后）
【遗留与风险】未完成项 / 需要总控裁决的问题 / 发现的范围外问题（只登记不动手）
```

### 9.2 总控合并验收清单（每波次合并前逐项核对）

- [ ] 回报格式完整，三绿证据齐全
- [ ] `git diff main...<branch> --name-only` 全清单逐文件对照分工矩阵，零越界
- [ ] 基线变化单调下降且断言基线值已在脚本中同步更新（S-FE 批次）
- [ ] worktree 内 `git rebase main` 无冲突（有冲突按第六节仲裁）
- [ ] 合并后主目录全量三绿：前端 tsc + `cd server && npx tsc --noEmit --skipLibCheck` + `npm run check:tokens` + `npm test` + `cd server && npx vitest run`
- [ ] 合并 commit 推送前更新本文档波次状态

### 9.3 W2 预布置（W1 合并验收通过后即发出口令）

| 会话 | W2 任务 | 关键约束 |
|---|---|---|
| S-FE | 批 H：BDS tokens.css 增补 14 色雾化分类色板（饱和度 25-35%，与雾琥珀 #A98A4F/雾砖红 #B05F57 同源）→ 同步 `styles/bds/showcase.html` + `docs/design-system/component-application-spec.md` → TraceabilityPanel.tsx:29-43 彩虹色板接入新 token；完成后启动批 G `dark:` 收编第一批（KnowledgeBase.tsx 80 处） | 色板是设计系统升级，须先出 showcase 预览再接入组件；批 G 每批 ≤5 文件，批间 npm test |
| S-BE | ① DR-041：QC/后勤角色容器内置 `_shared/rolePermissionMatrix.ts`（同步根 `lib/` 副本）+ SYSTEM_ROLE_META 清除"≤5万/>5万"残留描述；② 价格偏差规则 ①②④ 接入（折扣>10%/新客首单/低于成本价，hitConditions 数组合并单条 ApprovalRequest，参照 moqValidationService.ts:170-174 范式） | 权限矩阵双文件逐字同步是硬约束；价格规则政策 key 走审批策略配置 |
| S-QA | ① marketing 域补底测试（marketingService 核心路径）；② shared/businessNumberService 编号规则测试 | 仅新增 `*.regression.test.ts`，不改既有文件 |

### 9.4 波次状态登记

| 波次 | 状态 | 合并 commit | 备注 |
|---|---|---|---|
| W0 准备 | ✅ 完成 | `b009b12` | 断言基线 + 纪律落盘 + 漂移修正 |
| W1 并行 | 🔵 执行中 | `38c622c`（S-QA）/ `d3e4ded`（S-FE） | S-QA ✅（MOQ 双触发 + 交期锁死回归，3759 全绿）；S-FE ✅ 批A-F（raw语义色131→33 / 遮罩17→4 / 裸rounded 43→5 / 手写按钮35→22 / font-black清零，三绿）；S-BE 进行中 |
| W2 并行 | ✅ 完成 | `15f224a`（S-QA） | S-QA ✅ W2 完成（marketing + shared 补底，3837 全绿） |
| W3 并行 | 🔵 执行中 | `6478423`（S-QA） | S-QA ✅ W3 一阶段（104 断言守卫，2447 全绿，已合 main）→ 二阶段并入 W-PG 逐页旁路（待批次清单）；S-BE-W3-1 DR-016 任务书待命（W1 合并后发令）；~~S-FE-W3-2 批G 分批~~ CD-007 作废 |
| W4 并行 | ⬜ 待启动 | — | 任务书已预设计：S-FE-W4-1 批J+alert 收敛（组件先行，迁移并入 W-PG 逐页流程）；S-BE-W4 Agent 工具缺口任务书待 W3 合并后补发 |
| W5 独占 | ⬜ 冻结 | — | FR-004 方案 C，S-ARCH 单会话 |
| 终审 | ⬜ | — | 10 条高分验收标准逐项复核 |

---

## 十、W-PG 页面规格化收编（CD-007，2026-08-17 增补）

> **根因**：违规点驱动（grep→替换）无法捕捉布局组合问题（按钮尺寸不一、filterbar 撑高 header 等），且同一页面被批 A/G/J 反复开膛却无终态责任人。
> **新范式**：规格 → 台账 → 逐页销号。台账 30 页全量驱动，结构性零遗漏。

### 10.1 页面骨架六区强制规格（S-FE 落 `docs/design-system/page-skeleton-spec.md`，总控审定后生效）

| 区 | 强制标准 |
|---|---|
| 标题区 | 统一 `PageHeader`（bds-pagehead）：`px-7 pt-5 pb-4`、中英双语标题、面包屑右置、标题不换行省略截断、通知按钮 Dashboard 大按钮规格 |
| 筛选区 | 统一 `bds-filterbar` 范式：input 用 `bds-input sm`（34px）、按钮 `bds-btn-ghost`（40px）、6px 高度差为官方设计；单行不换行；**禁止任何元素撑高筛选行导致页面上边距异常**；**选型规则**：纯单搜索页用简单搜索框（禁为用而用套组合 bar），仅当搜索+≥1 筛选共行时才用组合嵌套 bar；搜索 icon 必须在输入框内部（leading icon），禁止外挂框外；参考基准实现 = 开发管理 / 货运管理 / 财务管理的功能 bar（一搜索+两筛选，长度与尺寸比例） |
| 操作区 | 主操作一律 `bds-btn` 默认 40px；**`bds-btn-sm` 仅表格行内操作白名单**，其余禁用；主按钮 `bds-btn bds-btn-primary` |
| 内容区 | `bds-surface` 家族 + 统一间距 token；嵌套层 `bds-inset`（--recessed-bg 冷墨洗） |
| 弹层区 | `bds-modal` / `bds-toast`（W4 alert 收敛后全站唯一入口） |
| 全局 | 字重 ≤300；rounded 仅 BDS 刻度；无阴影无 rim（豁免清单除外） |

### 10.2 台账格式（`docs/design-system/page-consistency-ledger.md`，30 行）

`| 页面 | 渲染文件 | 标题区 | 筛选区 | 操作区 | 内容区 | dark:残留 | raw色残留 | 弹窗 | 问题记录 | 验收状态 |`

- 首批录入产品负责人点名问题（2026-08-17）：① 单据中心（DocumentCenter）操作区 small buttons 尺寸不统一；② 订单管理（OrderManager）筛选 bar 高度异常撑大页面上边距；③ 报价管理（QuotationManager）纯单搜索误用组合嵌套 bar（为用而用）；④ CRM（Relations）功能 bar 整体重设计——搜索 icon 外挂框外突兀、整体设计不合格，须对齐开发/货运/财务管理基准范式
- P2 顺序：批 H（tokens 依赖）→ DocumentCenter + OrderManager + QuotationManager + Relations 四页优先销号 → 其余按台账

### 10.3 验收双轨

- **机器**（S-FE 扩展 check:tokens）：每个 *Manager.tsx 必须含 `PageHeader` 或 `bds-pagehead`（清单断言）；`bds-btn-sm` 计数基线只减不增（白名单注释豁免）；filterbar 容器禁 `h-` 非 34/40 值
- **人工**：每页改前/改后深浅双主题截图，对照六区规格表走查；台账"验收状态"列总控签字销号

### 10.4 与既有批次的归并

- 批 G（dark: 收编）：**并入逐页主刀**——每页收编时该页 dark: 一并清零，批 G 不再独立推进；W3 任务书 §S-FE-W3-2 的批次划分作废，文件清单转为台账输入
- 批 J + alert 收敛（S-FE-W4-1）：组件建设（BdsDialog/bdsToast）仍先行独立 commit；161 处迁移并入逐页主刀（每页弹窗一并替换）
- 批 H（14 色色板）：保持独立，是 P2 的 tokens 依赖，最先完成
