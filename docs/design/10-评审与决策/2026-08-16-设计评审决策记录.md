# Bambook 设计评审决策记录

> **定位**：记录产品负责人已确认、但尚待回写到各设计真源的决策。本文件是本轮评审的变更控制台账，不替代模块、数据模型和业务规则文档；在完成真源回写与验证前，禁止将本文件中的决策误报为代码已实现。
>
> **评审方法**：产品负责人确认业务事实；技术评审将其转为数据、状态机、权限、审计和验收要求。未确认的建议不得当作正式需求。

## 状态定义

| 状态 | 含义 |
|---|---|
| 已确认 | 产品负责人已明确确认，必须回写设计真源并纳入开发验收 |
| 待细化 | 方向已确认，但仍缺少触发条件、权限或异常处理细节 |
| 已撤回 | 讨论中提出过，但已被后续业务事实否定，不得实施 |

## 已确认决策

### DR-001 单公司专属交付

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 结论 | Bambook 首次上线服务单一公司、单一业务主体；不建设 SaaS 多租户、客户隔离或跨公司切换。未来新客户应独立设计和部署。 |
| 影响 | 公司档案必须可配置并在单据生成时快照保存，用于历史正确性，不代表多公司能力。 |
| 回写目标 | 产品定位与愿景、系统边界声明、公司档案与配置、单据体系总览。 |

### DR-002 业务域与轻量业务

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 结论 | 产品主轴为服装和面料两大业务域；其他贸易仅为非主业务的轻量归集，不展开为独立业务体系。 |
| 其他贸易 | 保留采购、销售、结算、附件与审计能力；默认仅强制负责人、业务方向、交易对方、记录日期，其余字段按需填写。 |
| 边界 | MTM 自然归入“其他”，当前不单独建模或单独设计。 |
| 回写目标 | 产品定位与愿景、用户画像与业务线详情、核心业务流程、信息架构与导航。 |

### DR-003 Capsule 是服装订单的 MOQ 豁免

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 结论 | Capsule 与服装大货共用同一订单模型、字段、状态机、生产链和单据链；不得创建独立模块或独立流程。 |
| MOQ | 普通服装订单按管理员维护的 MOQ 规则校验；Capsule 为业务员可直接勾选的 MOQ 豁免标记，不要求理由或额外审批。 |
| 规则治理 | MOQ 可按服装类型维护；规则变更需记录版本、修改人和生效时间；订单保存适用规则快照。Capsule 勾选须记录操作者与时间。 |
| 回写目标 | MOQ 最小起订量、订单类型分层、订单录入表单、订单状态机。 |

### DR-004 服装订单与面料订单独立建模、统一阅读

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 结论 | 服装订单与面料订单字段、状态机、工序不同，必须是独立实体；禁止以一张泛化订单表强行兼容。 |
| 总览 | 订单管理和经营总览提供统一查询投影，可按时间、金额、客户、负责人、状态排序筛选，并跳转各自详情。 |
| 回写目标 | 实体关系总览、订单与生产模型组、面料相关模型与模块、订单列表页、经营驾驶舱。 |

### DR-005 FOB 的内部面料交易与合并利润

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 结论 | 服装部和面料部相对独立。FOB 服装订单可向面料部采购：服装订单与内部面料订单双向关联、各自独立核算。 |
| 部门利润 | 服装部利润扣除内部面料采购价；面料部保留内部面料利润。 |
| 公司合并 | 合并报表必须抵销内部采购与内部销售，仅计客户外部收入、真实面料成本、服装生产成本与公司利润。 |
| 回写目标 | 实体关系总览、订单与生产模型组、报价与成本模型组、订单利润表生成、订单详情页。 |

### DR-006 价格只可人工确认

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 结论 | Track A、Track B 和任何定价模型均为参考/试算，不具有自动定价权。 |
| 草稿 | 业务员可自由试算与改价，不触发审批。 |
| 正式承诺 | 报价对外发送、创建订单、生成合同、成为内部面料结算价时，必须冻结该报价版本并提交领导审批；未批准版本不得对外发送或被正式业务单据引用。 |
| 变更 | 已批准的正式价格变更必须创建新版本并重新审批；草稿可直接修改。 |
| 回写目标 | 价格审批规则、报价编辑器、报价转订单流程、双轨成本面板、偏差校验与审批链。 |

### DR-007 报价审批权限按组织归属解析

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 结论 | 保持“销售主管”通用角色，不新增写死的“服装主管”或“面料主管”。业务员归属的团队/部门决定其主管负责的业务线与客户。 |
| 审批 | 服装价格、面料价格和内部面料结算价均由负责团队的销售主管或总领导审批；每次只需一位有效审批人；总领导拥有跨团队兜底权。 |
| 技术边界 | 总领导的业务审批权不得依赖技术超级管理员身份；应通过业务审批 scope 和组织关系授权。 |
| 回写目标 | 角色与权限矩阵、价格审批规则、审批与 human-in-the-loop、组织/团队相关数据模型。 |

### DR-008 高端服装多轮样品与双门禁

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 结论 | 样品开发是可循环链，不限制轮数。每轮必须记录目的、版本、材料/工艺配置、QC 结论、客户意见、修改项与证据。 |
| 内部门禁 | 每轮样品须先经内部 QC 通过，才允许提交客户。 |
| 客户确认 | 客户不登录 Bambook；业务员登记客户确认结果、日期、渠道与证据。业务员可在 QC 通过后直接登记确认并封存产前样，不增加主管审批。 |
| 封样 | 封存产前样为不可变生产基准。任何后续改动须产生新样品版本，并重新 QC、客户确认、封存。 |
| 回写目标 | 开发模块概述、开发详情页、开发转订单流程、QC 模块、订单状态机、订单详情页。 |

### DR-009 商业订单批准与生产放行分离

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 商业订单批准 | 客户合同或等效订单依据齐备、交期确认、销售主管或总领导批准后，大货订单成立。定金仅记录和跟踪，不作为统一采购门禁。 |
| 采购先行 | 订单获批后即可启动面料采购及相关开发；这是标准流程，不是紧急例外。 |
| 生产放行 | 内部 QC 通过、业务员已登记客户确认、产前样已封存后，即可完成管理上的生产放行；封样不以大货面料到位为前提。 |
| 放行确认人 | 系统仅在条件满足时显示“具备放行条件”；订单所属团队的销售主管或总领导必须手动确认生产放行。业务员准备资料，QC 提供样品评审结论。 |
| 实际开工 | 生产放行与实际开裁/开工必须分离。面料采购持续并行；只有面料实际可用并完成排产，才可进入实际裁剪、缝制等大货生产。 |
| 出运 QC 门禁 | 大货生产完成后必须经 QC 评审通过，才具备出运条件。样品 QC 与大货出运前 QC 是两道独立、不可替代的质量门禁。 |
| 出运批准 | QC 通过后仍须由订单所属团队的销售主管或总领导手动批准“允许出运”；批准后业务员/跟单才可安排出运执行。 |
| 履约终点 | 出运、合同履约、客户收货、财务结清必须分开记录。每张订单必须记录实际贸易术语、指定地点和适用版本；系统据此判定合同履约节点，禁止把 FOB 作为统一默认。FOB 以约定装运港的装船凭证履约；其他术语按其约定地点与凭证判定。客户收货确认按术语作为后续服务、客诉与回款跟踪或必要的履约证据。 |
| 回写目标 | 核心业务流程、订单状态机、开发转订单流程、采购模块、生产看板、QC 模块。 |

### DR-010 已批准订单的变更与取消控制

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 已确认部分 | 草稿/待审批订单可由业务员撤回、取消或暂停。领导批准订单后，订单进入正式承诺状态：取消、暂停、改价、改数量、改交期均不得由业务员直接修改，必须走正式申请与审批。 |
| 已确认审批 | 改价、改数量、改交期均须由销售主管或总领导审批，并记录前后值、原因与影响。 |
| 取消 | 已批准订单取消先进入“取消申请中”，系统汇总合同、面料采购、样品、生产、付款与库存影响。销售主管或总领导为最终业务审批人；采购、QC、财务仅确认各自影响。无不可逆承诺时批准后直接结案；已有采购或生产承诺时，批准后进入“结案处理中”，完成供应商、物料、客户责任和财务损益处置后才可关闭。 |
| 暂停 | 已批准订单暂停先进入“暂停申请中”，必须记录原因、责任人和预计恢复日期，经销售主管或总领导批准后生效；到期未恢复自动提醒，禁止无限期挂起。 |
| 回写目标 | 订单变更规则、订单状态机、订单详情页、采购模块、生产看板、财务与结算相关设计。 |

### DR-011 面料船样与匹头样规则

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 船样（S/S） | 面料订单必须管理船样。其是面料投产后的客户确认节点：系统须记录寄送、客户确认、确认人、日期、渠道、意见与证据；不得误建成服装产前样或生产前置门禁。 |
| 匹头样（RC） | 匹头样为按订单条件启用的节点，不是每张面料订单的统一必填项。是否启用由业务人员依客户要求、合同约定与本单实际情况决定并留痕；Separates（西服上衣与裤子分开销售）和常年翻单仅为常见参考场景，绝不作为系统自动判定或硬性必填规则。 |
| 时间约束 | 以面料订单的 Exmill Date 驱动倒计时：船样在出厂日前两周寄送并确认；启用 RC 的订单在出厂日前一周完成确认。若客户/合同另有时限，以该订单的明确要求覆盖，并保留原因与审批/确认记录。 |
| 模型边界 | S/S 与 RC 只属于面料订单的样品/客户确认链，不与服装的开发样、产前样封样、生产放行合并。 |
| 回写目标 | 核心业务流程、用户画像与业务线详情、订单与生产模型组、订单类型分层、订单详情页、面料订单状态机与提醒规则。 |

### DR-012 面料船样客户确认是标准发货门禁

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 标准门禁 | 面料船样寄送后，业务员必须在系统登记客户确认结果、日期、渠道、意见与证据；客户确认后，订单才具备推进面料大货发货的条件。 |
| 责任边界 | 客户不登录 Bambook；业务员负责登记客户确认，系统保留操作者与时间。该确认链属于面料订单，不复用或混入服装产前样封样链。 |
| 待细化 | 尚未确认客户未回复、明确拒绝或紧急业务例外时的处理与审批路径；在该规则补齐前，系统不得把“未确认”视作可正常发货。 |
| 回写目标 | 核心业务流程、面料订单状态机、订单与生产模型组、订单详情页、提醒与异常处理规则。 |

### DR-013 全局业务门禁的受控例外推进

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 原则 | 所有业务状态、质量、客户确认、审批等门禁均默认不可直接绕过；系统不得以隐藏开关、直接改状态或临时脚本规避规则。 |
| 例外申请 | 确需在未满足门禁时继续推进，业务员须提交例外推进申请，至少说明拟越过的门禁、原因、风险、受影响对象和补救/跟进责任人。 |
| 批准 | 仅具备更高业务审批权限的领导可批准例外；负责人团队的销售主管或总领导按组织授权决定。批准后系统只对指定订单、指定动作和指定时点开放推进权限。 |
| 审计与恢复 | 必须记录申请人、批准人、时间、理由、被越过的规则、原始状态与后续结果；例外不改变原规则，也不自动复制到其他订单。未获批准时保持原门禁。 |
| 回写目标 | 审批与 human-in-the-loop、订单状态机、面料订单状态机、QC、采购、出运、订单详情页、审计日志与权限矩阵。 |

### DR-014 面料大货 QC 与客户样品确认并行

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 并行关系 | 面料大货最终 QC 与船样（S/S）、以及按需启用的匹头样（RC）寄送/客户确认并行推进；QC 通过不是 S/S 或 RC 寄送的前置条件，样品确认也不是 QC 的前置条件。 |
| 出运资格 | 面料大货 QC 通过、S/S 客户确认完成、以及已启用 RC 的客户确认完成，均是具备申请出运资格的独立条件。任何一项未完成，除非按 DR-013 获得受控例外批准，否则不得正常推进出运。 |
| 链路边界 | 面料最终 QC 与服装 QC 为独立记录和结论；面料 QC、S/S、RC 各自保留独立状态、责任人、时间、意见与证据，禁止相互覆盖或合并为单一“已确认”字段。 |
| 回写目标 | 核心业务流程、面料订单状态机、QC 模块、订单与生产模型组、订单详情页。 |

### DR-015 出运计划、订舱、放行与实际出运分离

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 独立对象 | 面料和服装订单均须关联独立的出运计划/订舱记录，至少记录计划 ETD、承运/货代、运输方式、订舱信息、关键截点、计划/实际时间与关联单证。计划记录不等于实际出运。 |
| 订舱时机与批准 | 商业订单获批后，业务员可提前创建并提交订舱申请；订单所属团队的销售主管或总领导批准后，才可向货代确认订舱并锁定计划 ETD。订舱不等待 QC、S/S、RC 或生产放行完成。 |
| 放行边界 | QC、样品客户确认和领导“允许出运”控制的是是否允许将货物交运/实际出运，不反向阻断已获批准的订舱计划。关键门禁临近 ETD 未完成时必须预警；硬性推进走 DR-013 的受控例外。 |
| 放行批准 | 订舱批准与“允许出运”必须为两个独立审批动作；已锁定 ETD 不自动产生出运权限。所有适用的质量、样品/客户确认、单证等门禁完成后，仍须由订单所属团队的销售主管或总领导单独批准允许出运。 |
| ETD 变更 | 订舱确认后的任意 ETD 调整均须由订单所属团队的销售主管或总领导审批，不因“看似不影响合同交期”而免除审批。系统记录原值、新值、原因、影响及审批证据，并向相关样品、QC、单证、客户交期责任人同步。 |
| 实际出运 | 仅在出运门禁完成或受控例外获批后，可把计划转为实际出运；实际 ETD/离港或装运凭证必须独立记录，不能覆盖原计划。 |
| 回写目标 | 出运与报关模块、订单状态机、面料订单状态机、订单详情页、交期与生产规则、订单变更规则、提醒与审批规则。 |

### DR-016 订单与出运支持拆分与合并

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 关系 | 必须支持多对多：一张订单/订单行可拆分到多票出运；一票出运可合并多个订单/订单行。禁止假定“一单一票”。 |
| 分配记录 | 每个订单参与某票出运时，须有独立的出运分配记录，保存来源订单/订单行、计划与实际数量、单位、批次/箱号（如适用）、状态与异常；累计实际出运量不得超过该订单可出运量。 |
| 交易方边界 | 订单保存签约/服务客户；每票出运另行保存收货方/目的工厂、单证收件方、商业发票开票对象与实际付款方。它们可与签约客户不同，也不可被系统强制相等。多个订单合票仍仅在同一业务线、同一签约客户时允许；一张订单也可拆为多票，各票分别形成其实际装箱、商业发票、出运与收款记录。 |
| 通用场景 | 任一面料订单可由一个签约客户下达，并拆成任意数量的出运，分别发往不同的收货工厂/交易方；每票可有独立装箱单、商业发票和实际回款。收货、开票、付款交易方均须是正式关联方记录，并可追溯回原始订单与签约客户。 |
| 建档权限 | 业务员可直接新建收货工厂/关联交易方档案，不需要事前审批；建立时须维护与签约客户及本次业务角色的关系，后续出运、发票和回款必须引用档案而非自由文本。 |
| 门禁与审批 | 订舱、ETD、放行和实际运单凭证按“每票出运”管理；每票均独立经过订舱批准与允许出运批准。QC、S/S、已启用 RC、客户确认与订单可用数量按每个来源订单分配逐项核验。合票不得以某一订单已放行掩盖另一订单未满足门禁。 |
| 状态回写 | 订单只在全部应出数量完成相应履约后才进入完整出运/履约完成；部分出运必须可见剩余数量、后续出运计划和分批履约状态。 |
| 业务线隔离 | 出运合并只允许发生在同一业务线内。面料订单与服装订单不得合并为同一票出运，也不得共用订舱、出运放行、QC/样品门禁或单证主档。 |
| 回写目标 | 出运与报关模型组、实体关系总览、Shipment/ShipmentLine 模型、出运编辑器、装箱明细、订单详情页、订单状态机与履约报表。 |

### DR-017 关联方角色的推荐带入与可覆盖选择

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 常规关系 | 通常签约客户同时也是收货、开票与付款对象；客户关联档案须维护这些角色的推荐对象。 |
| 交互 | 业务员创建订单、出运、商业发票或回款记录时，系统展示签约客户档案中的推荐关系，支持一键带入对应角色；不得在用户未确认的情况下静默强制填值。 |
| 覆盖 | 收货方、开票对象、付款方相互独立，可分别改选任一正式关联方档案，以支持客户指定工厂收货、第三方开票或第三方付款等真实场景。所选对象与原始签约客户均保留在业务记录中。 |
| 回写目标 | 关系档案、订单录入表单、出运编辑器、商业发票、应收与回款、订单详情页。 |

### DR-018 出运票与核心商业单证一一对应

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 默认单证粒度 | 每一票实际出运默认对应独立的一套装箱单与商业发票。订单拆分为多票后，各票必须各自保留数量、金额、收货/开票/付款对象、装箱与出运凭证。 |
| 核算追溯 | 开票、应收与回款必须可追溯至具体出运票及其订单分配，禁止仅挂在订单总头而丢失分票事实。 |
| 回写目标 | Shipment/ShipmentLine/ShipmentEvent、装箱明细、商业发票、应收与回款、单据生成器、订单详情页。 |

### DR-019 商业发票与回款支持多对多核销

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 关系 | 一张商业发票可由多笔回款分次核销；一笔回款也可按金额分配核销多张商业发票。 |
| 核销记录 | 每次分配须独立保存回款、商业发票、已核销金额、币种/汇率（如适用）、日期、操作人、备注及差异处理；发票与回款均实时保留未核销余额。 |
| 手工匹配与差异 | 业务员可对每笔回款手动勾选并确认其对应的一张或多张商业发票/出运票。实际回款金额无需与发票金额完全相等；允许少收、多收或因样品费、手续费等形成差额。系统必须保留已分配金额、未分配金额和发票剩余金额，并将差额作为独立可说明、可继续处理的余额，而非静默抹平或拒绝登记。 |
| 追溯 | 商业发票仍可追溯其对应的具体出运票与订单分配；回款不因合并或拆分而失去原始付款方、付款凭证和核销去向。禁止以单一“已收/未收”字段替代分配账。 |
| 回写目标 | 应收与回款模型、商业发票、收款核销规则、财务模块、订单/出运详情页、经营报表。 |

### DR-020 回款事实由财务录入，业务员完成核销

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 到账录入 | 公司账户出现新的银行入账后，由财务录入该笔到账事实及相关银行信息/凭证；业务员不得自行虚构或替代财务登记实际到账。 |
| 通知与核销 | 财务录入后，系统通知相关业务员。业务员据客户、付款方、金额、备注和业务背景，手动选择对应的商业发票/出运票并完成多对多核销与差额说明。 |
| 职责边界 | 财务确认“钱已实际进入公司账户”；业务员确认“这笔钱对应哪一笔/哪些业务”。二者均保留操作人与时间，形成可审计的到账—匹配链。 |
| 回写目标 | 应收与回款模型、财务收款登记、业务员待办/通知、商业发票详情、核销规则与审计日志。 |

### DR-021 回款按客户/品牌/区域归属路由

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 自动路由 | 关联方档案维护客户、品牌、区域及业务员归属。财务录入到账时填写可识别的付款方、客户/品牌/区域或备注线索；系统仅在可唯一解析归属时，自动通知对应业务员进行核销。 |
| 歧义处理 | 无法识别或命中多个归属的到账进入“待分配回款”队列，仅向财务和相关销售主管可见；销售主管负责指派正确业务员。禁止所有业务员自由认领。 |
| 档案纠正 | 分配完成后，应由有权限者补全或修正关联方与客户/品牌/区域的归属关系，以提升后续自动路由准确性。 |
| 回写目标 | 关系档案、组织与数据权限、财务收款登记、通知中心、业务员待办、销售主管工作台、应收与回款规则。 |

### DR-022 回款核销差额采用可配置 allowance 与主管确认

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| Allowance | 回款与发票/出运票的差额允许存在；系统必须同时按可配置的金额上限与比例上限判断，任一超出即不可由业务员直接完成核销。阈值可按币种、客户或业务线配置，并须保留版本与生效时间。 |
| 超额差异 | 差额超过适用 allowance 时，业务员不得自行关闭或抹平余额；须提交差额说明，由相应主管确认后才可完成该笔差异处理。 |
| 可追溯 | 系统保留原始金额、已核销金额、余额、差额类型/说明、适用 allowance、确认人和时间；任何确认不覆盖原始到账事实。 |
| 回写目标 | 应收与回款规则、财务收款登记、业务员核销界面、主管审批、规则配置与审计日志。 |

### DR-023 预付款可先入账、后核销

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 预付款登记 | 定金、预付款或其他提前到账可在商业发票生成、实际出运前由财务登记；不得因缺少发票而拒绝记录真实银行到账。 |
| 关联方式 | 业务员可先把到账关联到订单，或保留为该交易方的待分配余额；之后在商业发票产生时按 DR-019 分次、多对多核销。 |
| 核算边界 | 预付款、已分配核销额和待分配余额必须分别可见；后续发票/出运只能消耗可用余额，保留完整到账来源与分配时间线。 |
| 回写目标 | 应收与回款模型、财务收款登记、订单详情页、商业发票、核销工作台、经营报表。 |

### DR-024 客户账户余额与实际付款方分离

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 账户归属 | 每个签约客户拥有独立 account balance，统一反映该客户的应收欠额、预付款、已核销金额与余额。即使实际付款由关联工厂或其他付款方完成，账务归属仍记入对应签约客户账户。 |
| 付款方留痕 | 每笔到账须同时保留实际付款方；付款方是资金来源和对账线索，不自动成为该笔订单/发票的客户账户归属。 |
| 余额处理 | 客户账户出现可用余额时，可在后续订单/商业发票继续核销；也可按实际业务性质挂为样品费等其他收入/待处理款。系统提供通用机制，不预设某一种用法必然发生。 |
| 可追溯 | 任一账户余额均须可追溯至原始订单、商业发票、出运票、实际付款方、到账凭证和分配记录；不得以汇总余额掩盖明细来源。 |
| 回写目标 | 客户/关联方账户模型、应收与回款模型、财务收款登记、商业发票、客户详情页、经营报表。 |

### DR-025 款项性质由业务员标定

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 财务职责 | 财务只登记可核验的银行到账事实和凭证，不负责判断其对应发票、订单、样品或其他业务性质。 |
| 业务员职责 | 业务员在收到到账通知、认领/被分配并完成匹配时，为每笔款项标定业务性质，例如发票回款、订单预付款、样品费或其他待处理款；该标定与核销记录共同留痕。 |
| 未识别款 | 未分配或未标定性质的到账保持待处理状态，不应被财务猜测分类，也不得被系统自动计入某一发票、订单或客户余额用途。 |
| 回写目标 | 财务收款登记、业务员核销工作台、通知中心、应收与回款规则、审计日志。 |

### DR-026 面料订单前的开发样客户确认

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 前置开发链 | 面料在客户确认订单前，先进行开发样链。可包含手织样、先锋样、普通小样及其他按实际业务命名的样品；系统以可配置样品类型管理，不把某个行业叫法硬编码为固定流程。 |
| 客户确认 | 开发样用于客户确认。业务员登记客户的确认结果、日期、渠道、意见、证据与必要修改；通常客户在该确认后才下达面料订单。客户不直接登录系统。客户也可能先下单、后补首次开发样/先锋样确认；系统须允许该顺序，并明确显示待确认事项，不得把通常流程误做成绝对拦截。 |
| 订单衔接 | 已确认的面料开发样作为后续报价/订单的重要依据和版本证据。面料订单获得商业批准后即进入投产；不能因仍待确认的开发样阻断投产。投产后才进入大货早期样品、S/S、按需 RC、面料 QC 与出运的后段链路。 |
| 模型边界 | 订单前开发样、订单后 S/S、可选 RC、面料最终 QC 分别建模并可追溯关联，禁止混为单一“样品已确认”字段，也不得与服装产前样封样链混用。 |
| 回写目标 | 面料开发模块、开发转订单流程、核心业务流程、面料订单状态机、订单与生产模型组、QC 模块、订单详情页。 |

### DR-027 面料 QC 聚焦最终大货

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 开发样职责 | 面料订单前的手织样、先锋样、普通小样等开发样不进入 QC 门禁；由业务员自行确认、寄送和登记客户反馈。 |
| 大货 QC | 面料 QC 主要负责最终大货质量评审。其结果与 S/S、可选 RC 的客户确认并行推进，并共同构成正常出运资格。 |
| 回写目标 | 面料开发模块、QC 模块、核心业务流程、面料订单状态机、角色与权限矩阵。 |

### DR-028 面料投产后的早期生产样与客户反馈

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 投产规则 | 面料客户订单经商业批准后即投产。真实的大货质量无法仅凭手织样确认，系统不得把“开发样待确认”设计为面料投产门禁。 |
| 早期生产样 | 投产后，最早可获得、能够代表实际生产质量的面料样品应尽快寄送客户确认。系统以通用“投产后早期样品”管理，暂不把不稳定的行业叫法写死。 |
| 反馈闭环 | 客户对早期生产样不确认或提出问题时，业务员登记意见与证据并反馈 QC；QC 向工厂提出染整、后整理、成品检验、修布等调整要求；工厂提供新样品；业务员寄送客户并登记确认结果。客户通过则该轮闭环，不通过则业务员将新反馈再次交给 QC，循环继续。系统必须支持不限轮次的该闭环，以尽早使后续大货符合客户需求。 |
| 正常责任流 | 面料样品链中的系统记录由业务员全程登记，包括样品、寄送、客户反馈与每轮结果；QC 负责专业判断并向工厂反馈调整要求；工厂负责按要求调整并提供新样品。正常技术调整不设“调整幅度”硬阈值，也不增加不必要的主管审批门槛。 |
| 生产连续性 | 正常的样品反馈与 QC 调整不自动暂停面料大货生产；订单保持投产/生产中状态。只有重大事故才进入已批准订单的紧急停产/终止等专门受控流程，不应把正常迭代误做成异常停产。 |
| 关系边界 | 投产后早期样品不等同于出运前 S/S，也不替代可选 RC、最终面料 QC 或出运放行。 |
| 回写目标 | 面料订单状态机、面料开发模块、生产跟进、QC 模块、客户反馈与变更规则、订单详情页。 |

### DR-029 服装与面料样品 QC 的不同责任闭环

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 面料样品链 | 业务员全程登记。客户不通过的意见由业务员交给面料 QC；面料 QC 向工厂提出技术调整；工厂提供新样；业务员寄客户并记录下一轮结果。 |
| 服装样品链 | 工厂每一轮产前样直接交给服装 QC；服装 QC 评审并直接在系统填写内部评审意见。多数情况下，QC 将样品与评审意见交给业务员，由业务员决定是否寄客户并登记客户意见；内部评审意见不应被压缩成机械的“通过/不通过”二值。 |
| 服装不通过闭环 | 客户不通过时，业务员直接向工厂业务/跟单反馈修改意见；工厂出下一轮样品后，必须再次先交服装 QC 评审并录入系统，之后才可再次寄客户。客户通过则该轮完成。 |
| QC 直接打回 | 对明显无法通过、寄客户也无意义的批次，服装 QC 在提交评审意见时可选择“直接打回工厂重做”。该批不得寄客户，系统向工厂责任方生成重做要求并记录 QC 意见；同时必须通知业务员，并使其可见打回原因与后续进度。工厂提交新批后，必须重新进入 QC 评审。该能力是 QC 的明确评审结论，不是绕过流程的隐藏状态修改。 |
| 强制边界 | 两条链均可多轮循环，但参与角色、数据录入人和工厂沟通路径不同。系统必须为服装与面料使用不同的状态机、任务路由和权限校验，禁止用统一样品流程硬兼容。 |
| 回写目标 | 开发模块、服装订单状态机、面料订单状态机、QC 模块、样品数据模型、订单详情页、角色与权限矩阵。 |

### DR-030 单据体系以 PI 为常规接单起点

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 接单依据 | PI（Proforma Invoice）是常规接单阶段的商业单据，应基于已批准的报价版本生成并作为客户确认依据。 |
| 单据链 | 已确认订单后，系统须能生成并关联销售合同、采购合同以及按实际出运票生成的装箱单、商业发票等出运单据；提单、信用证等外部单据也须可导入并关联相应订单/出运票。 |
| 回写目标 | 报价转订单流程、合同模型与生成器、DocumentCenter、出运与报关模型组、商业发票/装箱单、信用证管理、订单详情页。 |

### DR-031 每张订单须有正式确认订单依据

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 确认方式 | PI 后每张订单必须留存一份正式确认订单的依据，但不强制单一形式：客户可提供 PO/订单文件、客户采购合同；也可由我方根据已确认订单生成销售合同并对外发出。 |
| 系统生成 | 需要我方出具销售合同时，系统从已批准订单、已批准价格版本和关联方快照生成合同草稿，供业务员核对、发出和留存。不得手工重抄核心订单金额、数量、交期和贸易条款。 |
| 外部导入 | 客户提供的 PO、采购合同或其他确认文件必须作为外部原件上传/导入，关联订单、版本、客户与确认日期；外部文件不能被系统生成内容覆盖。 |
| 订单门禁 | 缺少任何一种有效确认订单依据时，订单不得进入“商业订单已批准”并启动正式采购/投产；受控例外仅按 DR-013 办理。 |
| 回写目标 | 报价转订单、合同模型/合同生成器、附件与版本管理、订单审批、采购启动门禁、订单详情页、DocumentCenter。 |

### DR-032 采购按供应商生成独立采购单据

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 拆分原则 | 一张销售订单可形成多个采购项并对应多家供应商；采购合同/采购订单必须按供应商独立生成与管理，禁止把多家供应商写进一份不可分辨责任的采购单据。 |
| 生成依据 | 采购项获批后，系统根据供应商、采购物料/服务、数量、单价、金额、交期、质量/样品要求与来源销售订单生成采购订单或采购合同草稿。 |
| 追溯 | 每份采购单据必须反向关联来源销售订单、相关订单行及采购分配；一张销售订单的数量、成本、交期与供应商履约可按采购项追溯。 |
| 回写目标 | 采购模型、采购模块、采购合同/采购订单生成器、供应商档案、订单成本与利润、订单详情页、DocumentCenter。 |

### DR-033 内部供料使用内部单据，不使用合同

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 单据性质 | 服装部向面料部采购属于内部交易，不得命名或伪装为外部采购合同；系统须提供正式、可追溯的内部采购/供料单据。 |
| 发起方 | 由服装部基于服装订单发起向面料部的内部采购申请；面料部接收、处理并确认该内部需求。 |
| 生效门槛 | 面料部确认数量、交期和已批准的内部结算价后，内部供料单才生效。生效价进入服装订单的面料成本，并成为面料部内部收入、部门利润与公司合并抵销的依据。 |
| 实际交付 | 内部供料的实际交付不另造平行出库流程，而是进入面料订单原有出运状态机：面料部可将货物发往关联服装工厂；每笔实际出运关联内部供料分配后，回写服装订单的面料到货数量、日期与状态。支持分批出运、分批到货与差异追溯。 |
| 交付单据 | 内部供料实际交付只生成内部交付单与装箱明细，不生成对外商业发票；服装部成本来自已确认内部结算价，实际到货来自关联面料出运。 |
| 核心内容 | 内部单据记录申请部门、供料部门、关联服装订单与面料订单、物料、数量、内部结算价、交期、状态及双方处理记录。 |
| 核算作用 | 该单据是服装部内部面料成本、面料部内部销售收入、部门利润与公司合并抵销的共同依据，并与外部采购合同严格区分。 |
| 回写目标 | 内部交易模型、面料/服装订单关联、采购模块、部门利润与合并报表、订单详情页、DocumentCenter。 |

### DR-034 单据按来源分为系统生成、外部原件与混合回传

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 系统生成 | 由 Bambook 根据已批准业务数据生成的 PI、销售合同、采购合同/订单、内部供料单、装箱单、商业发票等，保留所用数据与模板版本。 |
| 外部原件 | 客户 PO/采购合同、信用证、银行凭证等由外部主体形成的文件，必须上传/导入原件并关联业务对象；系统不伪造其为内部签发文件。 |
| 混合回传 | 系统可先生成业务草稿或补料资料，外部机构回传正式版本后再导入归档。例如提单：系统生成补料草稿，货代/船公司正式提单回传后关联同一出运票。 |
| 回写目标 | DocumentCenter、单据状态机、制单引擎、附件/版本模型、出运与报关模型组、合同与信用证管理。 |

### DR-035 系统生成单据的冻结与版本纪律

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 草稿 | 系统生成单据在草稿阶段允许编辑、补充与重新生成。 |
| 正式版本 | 领导批准并对外发出的 PI、销售合同、采购合同/订单、商业发票、装箱单等单据必须冻结为正式版本，并保存当时使用的业务数据与模板快照。 |
| 变更 | 已发正式单据不得原地覆盖；任何内容调整均须创建新版本，保留原版、变更人、时间、原因与前后差异。适用审批规则的变更须先完成审批。 |
| 回写目标 | DocumentCenter、单据状态机、制单引擎、合同/PI/采购单据、商业发票/装箱单、审计日志。 |

### DR-036 订单驱动的单据清单与缺失门禁

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 清单生成 | 每张订单自动生成单据清单，按业务线、贸易术语、付款方式、客户要求与出运方式判定单据为必需、可选或外部导入。 |
| 执行目的 | 清单显示每份单据的责任人、来源、状态、版本与缺失原因；业务人员不应依赖记忆查找单据。 |
| 门禁 | 与订单批准、订舱、出运、交单、收款等节点相关的必需单据，须在对应节点前被核验；不满足时正常路径阻断，例外按 DR-013 办理。 |
| 回写目标 | 单据规则配置、DocumentCenter、订单详情页、出运编辑器、订舱/出运放行、信用证管理、提醒与审批规则。 |

### DR-037 单据规则由管理维护，订单可增补特殊要求

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 规则维护 | 系统管理员（总领导，宽泛容器）/ 销售主管（部门管理职责）维护基础单据清单规则及其适用条件。 |
| 订单补充 | 业务员可在建单或执行过程中，按客户/订单实际要求增加特殊单据项与说明。 |
| 不可削弱 | 业务员不得删除或静默降级系统判定的必需单据；确需越过时只能走 DR-013 的受控例外。 |
| 回写目标 | 单据规则配置后台、订单录入、订单详情页、DocumentCenter、权限与审计规则。 |

### DR-038 单据设计交付约束与责任时限

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 设计授权 | 最终 Coding Agent 应按本评审已确认的业务模式，完成专业外贸单据的字段、模板、数据装配、校验和界面设计；不得自行改变 PI→确认订单依据→合同/采购→出运→回款的业务边界。 |
| 责任与时限 | 每张订单单据清单须记录责任人、目标完成时间、当前状态与缺失原因；临近或超过时限时自动提醒相关责任人和主管。 |
| 最终版校验 | 单据模板中的必填数据在生成最终对外版本前必须完整；缺失时阻止正常签发，并明确缺失字段、责任人和补充入口。确需强行推进时仅能按 DR-013 留痕批准。 |
| 实施标准 | 单据设计必须兼容系统生成、外部原件和混合回传三种来源；采用可配置的模板与规则，而非为某个客户、国家或实例硬编码。每种单据需在设计中明确：生成/导入时点、来源数据、责任人、审批/签发条件、版本冻结、关联订单/出运票/回款与验收场景。 |
| 回写目标 | DocumentCenter、制单引擎、单据状态机、单据规则配置、订单/出运详情、提醒与审批、验收测试说明。 |

### DR-039 样品寄送也必须具备完整单据链

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 样品寄送记录 | 面料与服装的每次样品寄送均须有独立记录，关联样品轮次/订单或开发案、收件方、寄送日期、快递服务商、快递单号与实际寄送状态。 |
| 随附单据 | 样品发票、快递/运费发票或凭证及其他该次寄样相关文件须上传或由系统生成，并关联到该次样品寄送；不得仅作为散落附件。 |
| 关系与核算 | 样品费用可进一步关联 DR-025 的款项性质与 DR-019 的回款核销；样品寄送与客户确认、QC 轮次、供应商/工厂责任均可反向追溯。 |
| 回写目标 | 服装/面料样品模型、开发模块、样品寄送界面、DocumentCenter、快递/费用凭证、应收与回款、订单详情页。 |

### DR-040 关联方档案维护客户、工厂与供应商关系

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 档案网络 | 客户、供应商、工厂及其他交易方均以正式关联方档案维护；客户档案中可登记其对应/合作的工厂、供应商及其他业务关系。 |
| 关系结构 | 客户与工厂采用多对多关系：工厂是独立档案，可关联多个客户/品牌；同一客户也可关联多家工厂。 |
| 业务引用 | 订单、采购、样品、出运、单据和回款从关联方档案及其关系中选择对象；不得依赖每张业务单据重复输入、彼此无法关联的自由文本。 |
| 可扩展性 | 同一关联方可按真实业务承担多个角色；客户与工厂/供应商的对应关系应可维护、可追溯、可用于 DR-017 的推荐带入和 DR-021 的回款路由。 |
| 回写目标 | 客户/供应商/工厂档案、关联关系模型、订单录入、出运编辑器、样品寄送、回款路由、权限归属。 |

### DR-041 业务权限采用 7 宽泛容器角色，技术与业务边界清晰（决策点 1-A 方案）

| 项 | 决策 |
|---|---|
| 状态 | 已确认（升级为 7 容器） |
| 内置角色 | 正式内置 7 宽泛容器角色：业务员、销售主管、财务、QC、后勤、系统管理员（总领导）、超级管理员（老板）；财务主管/总领导/跟单员/单证员/仓库管理员等细分岗位统一归入对应容器，首次出现时括号语义标注归属。 |
| 跟单归属 | 不新增“跟单员”内置角色。跟单工作归入业务员（宽泛容器含跟单），业务员可按团队分工承担基础数据查阅、录入与跟进；销售主管统领业务员团队。 |
| QC | QC 是独立业务角色（容器不细分），拥有分配范围内样品/大货评审与意见登记权限；其服装、面料流程由业务线/任务类型路由，不以新增硬编码“服装 QC/面料 QC”角色实现。 |
| 总领导 | 系统管理员（总领导）容器内含业务兜底审批权，拥有跨团队的价格、订单、订舱、ETD、出运、例外推进等已确认业务兜底审批权。 |
| 技术边界 | 系统管理员（总领导）容器含用户、角色、字典和系统配置权；超级管理员（老板）容器含最高技术运维与权限治理权。两者获得业务兜底审批权源于容器身份本身（总领导 = 系统管理员容器），非身份叠加。 |
| 审批修正 | 审批路由以负责团队销售主管或系统管理员（总领导）为准，替换现有固定金额阈值、不将财务（容器）强制纳入业务审批链（财务仅处理其已确认的到账、核销、财务差额等职责）；超级管理员（老板）作为绝密级最终兜底审批。 |
| 回写目标 | 角色与权限矩阵（7 容器版）、审批策略、订单/价格/订舱/出运/例外流程、QC 模块、数据范围规则、运行时权限守卫与验收测试。 |

### DR-042 其他贸易采用轻量业务记录

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 模型 | 其他贸易为轻量业务记录，不建立第三套重型订单模型；可分别支持只销售、只采购、采购加销售。 |
| 复用能力 | 复用客户/关联方、采购、销售、结算、附件、审批、出运与单据能力；按实际业务需要关联。 |
| 默认强度 | 不默认强制样品、QC、订舱、生产等大货门禁；如具体业务需要，可从统一能力中启用并按单留痕。 |
| 最低记录 | 业务负责人、业务方向、交易对方、记录日期为最低必填；其余字段按实际需要录入。 |
| 回写目标 | 其他贸易模型与页面、订单/采购/销售入口、单据清单、结算与报表、权限规则。 |

### DR-043 管理报表以业务真源驱动五类经营视图

| 项 | 决策 |
|---|---|
| 状态 | 已确认 |
| 报表类别 | 超级管理员（老板，绝密级可见：全公司）/ 销售主管（部门范围）默认查看五类视图：订单履约、样品与 QC、出运与单据、回款与风险、部门利润与公司合并利润。 |
| 履约与质量 | 展示服装/面料订单当前阶段、待确认样品、QC 结论、生产/交期/ETD 风险及待办责任人。 |
| 单据与资金 | 展示订舱/出运进度、必需单据缺失、发票、应收账龄、到账待分配、核销差额和客户账户余额。 |
| 利润口径 | 同时提供服装部、面料部独立利润与公司合并利润；公司视图按 DR-005 抵销内部面料采购/销售，不能将内部交易重复计入收入或成本。 |
| 数据纪律 | 所有指标从订单、采购、样品、QC、出运、单据、发票和回款等业务真源计算；报表只读投影，不允许手工覆盖经营数字。 |
| 回写目标 | 经营驾驶舱、报表中心、部门利润/合并利润模型、风险中心、订单/出运/回款详情页、权限与敏感字段控制。 |

### DR-044 应收/应付余额统一净额口径

| 项 | 决策 |
|---|---|
| 状态 | 已确认（2026-08-20，批次 0 口径决策，修复 P1-005） |
| 统一公式 | 未结清余额（open amount）= 发票金额 − Σ InvoiceAllocation.appliedAmount。PartiallyPaid 发票在一切统计中按净额计入，不再按发票全额。 |
| 适用范围 | 应收/应付账龄分析（五桶分档）、财务页 KPI 卡（应收发票/应付发票/待收凭证/待付凭证）、客户/供应商对账单期末余额、未来资金日历与现金流预测（REQ2-02）、催款函账龄明细（REQ2-08）。同一公式跨模块复用，禁止某模块自造口径。 |
| 凭证侧镜像 | 凭证"未核销余额" = 凭证金额 − Σ InvoiceAllocation.appliedAmount（同一分配表）；partially_reconciled 凭证的"待收/待付"统计按该净额计，不按凭证全额。 |
| 状态过滤关系 | 发票 status（Issued/PartiallyPaid/Paid）与凭证 status（unreconciled/partially_reconciled/reconciled）均由同一分配表汇总推导（recalcInvoiceStatus / recalcVoucherStatus）；状态与净额同源，不允许出现"状态已 Paid 但净额>0"的分裂。 |
| 回写目标 | reportService（账龄/对账单）、finance 路由发票列表派生字段、FinanceManager KPI、资金日历与催款套件（REQ2-02/08）、验收 §4.4 跨模块一致性测试。 |

### DR-045 核销真源 = InvoiceAllocation 分配表，凭证快照字段降级为派生投影

| 项 | 决策 |
|---|---|
| 状态 | 已确认（2026-08-20，批次 0 口径决策，修复 P1-004/P1-006） |
| 唯一真源 | InvoiceAllocation（发票×凭证 1:N 明细）是核销事实的唯一真源；发票 status、凭证 status、凭证 invoiceId/appliedAmount 快照全部由该表汇总派生。 |
| 派生快照 | PaymentVoucher.appliedAmount/invoiceId 降级为"主核销"派生快照（向后兼容展示用），只能由 applyAllocation 事务闭环内重算写入；任何路径（seed、批量导入、Agent 工具、运维脚本、直接 SQL）禁止绕过分配表直写快照或直写凭证 status。 |
| 写入通道统一 | route POST /allocations 与 Agent commitPaymentReceiveAndReconcile 均已收敛到 applyAllocation 单一事务（validate → upsert allocation → recalc 双侧 status → EntityReference/link 同步 → 审计）；新增核销写入能力必须复用该通道，不得另起炉灶。 |
| seed 纪律 | 演示/测试数据造核销事实时必须同时写 InvoiceAllocation 明细并让双侧 status 与明细一致；只写快照不写明细视同脏数据（P1-004/006 根因）。 |
| 回写目标 | seed-demo-data-v2、allocationService/allocationMutationService、finance 路由、Agent reconcileFlow、数据迁移工具（REQ2-07 导入若有核销列）、验收 AC-606。 |

### DR-046 汇率取值 = 单据时点快照，汇率表仅作录入默认

| 项 | 决策 |
|---|---|
| 状态 | 已确认（2026-08-20，批次 0 口径决策） |
| 损益计算真源 | 汇兑损益基于双边单据时点快照计算：Invoice.exchangeRate（开票日）× PaymentVoucher.exchangeRate（实际收付款日）；核销额 × 双边汇率差即损益（Receivable 收益 = 收款汇率−开票汇率，Payable 反向）。 |
| 汇率表定位 | ExchangeRate 集中表仅作为开票/收付款录入时的默认值来源与行情参考，不得回溯改写已存在单据的快照，也不参与已发生核销的损益重算。 |
| 快照缺失处理 | 双边汇率任一缺失的核销明细不参与损益计算，报表透明披露"汇率缺失未计入"明细而非静默丢弃（与合并利润 unconverted 披露同一纪律）；本币发票（currency = baseCurrency）无汇兑，直接排除。 |
| 回写目标 | reportService 汇率损益、FX 页汇率历史/锁定展示、fxSettlementService、资金日历外汇敞口（REQ2-02）、验收 C5。 |

### DR-048 REQ2-11 双抬头分账：双主体事实确认，暂缓至 v1.1；五项待决点拍板

| 项 | 决策 |
|---|---|
| 状态 | 已确认（2026-08-27，W-C 管理层仲裁） |
| D-1 立项与时点 | **双主体经营事实成立，但暂缓立项**——登记为 v1.1 首批，不阻断 v1.0 投产（▲条件性需求，立项评审方案 B 三期路径维持有效：P0 主体档案+六类单据打标 → P1 报表分账 → P2 权限/序列细化）。 |
| D-2 信用额度 | 拍板（立项时生效）：**共享 Relation 级单额度**（P0 零改动）；若分主体报表启用后出现主体间额度误伤，再在 P2 评估分主体额度。 |
| D-3 编号序列 | 拍板（立项时生效）：**两主体共用序列**，主体靠单据标注区分（评审 P0 口径）；外部对号需求以单据 `legalEntityName` 快照满足。 |
| D-4 存量数据 | 拍板：**存量 3 年历史数据全部归属默认主体（isDefault）**，迁移模板零改动，主体 B 报表自启用日起算（用户确认历史 Excel 无需按主体回溯拆分）。 |
| D-5 VAT 主体校验 | 拍板（立项时生效）：**Output 方向 seller 强校验 LegalEntity**（Input 方向 buyer 强校验），随 P0 主体档案一并落地；存量自由文本行启用日前豁免、启用日后录入强制匹配。 |
| 边界 | v1.0 期间全仓维持隐式单法人架构；任何会话不得提前动工 REQ2-11 实现代码（含 LegalEntity 模型/单据主体字段）。 |
| 回写目标 | 立项评审文档（`2026-08-20-REQ2-11-双抬头分账立项评审.md` §6 五格标记拍板结论）；v1.1 启动时以本 DR 为开工依据。 |

### DR-047 Shipment↔Batch 双状态机挂钩：Shipment→Shipped 自动推进挂接批次（W-B 契约表断层④）

| 项 | 决策 |
|---|---|
| 状态 | 已确认（2026-08-27，W-B 收尾拍板，实现 `5e46d93`） |
| 联动方向 | 单向：Shipment→Shipped 自动推进 `OrderShipmentBatch` planned→shipped；反向不联动（批次不发运运单）。物理事实优先——运单既已发运，批次状态必须同步，否则催款分级/四单对账读批次永远滞后（货发了款没催）。 |
| 归属判定 | 仅推进显式挂接（`batch.shipmentId = shipment.id`）且 status='planned' 的批次；未排船批次（shipmentId=null）保持 planned 走人工 mark-shipped——一票多批次归属歧义不猜。shipped/cancelled 终态幂等跳过、不复活。 |
| 尾款门禁语义 | 末批收款门禁（FINAL_PAYMENT_GATE_BLOCKED）的保护点在「发运前」：人工 mark-shipped 与出运放行门禁（DR-012/014）。物理发运既成事实后不再阻断状态同步，缺口以 skipGate+autoLinkage 双标记写入审计，由催款 watchdog（listOverdueFinalBatches）兜底追款。 |
| 挂接方式 | 服务层内联 4 挂接点，不挂事件订阅——Agent 直改路径（toolRuntime create/update）不发 ShipmentStatusChanged 事件，事件订阅必然漏触发：① createShipment 直建 Shipped ② updateShipment 流转 Shipped（同状态幂等 patch 不重复触发）③ Agent handleShippingCreateShipment ④ Agent handleShippingUpdateTrackingStatus。best-effort：单批失败记 warn 继续，永不阻断运单主业务。 |
| 边界 | settleStatus 财务维度不受影响（正交纪律：status/settleStatus 互不覆盖）；InvoiceOrderAllocation.batchId 无写入入口维持断层①整单口径拍板不变。 |
| 回写目标 | orderShipmentBatchService.autoAdvanceOnShipmentShipped、shipmentMutationService create/update、toolRuntime 两 handler、回归测试 14 例。 |

## 已撤回或禁止实施的方向

| 编号 | 方向 | 处置 |
|---|---|---|
| R-001 | 将 Capsule 设计成独立业务线、模块或状态机 | 已撤回；只能是服装订单的 MOQ 豁免标记。 |
| R-002 | 按“面料主管/服装主管”新增硬编码系统角色 | 已撤回；使用通用销售主管 + 组织归属。 |
| R-003 | Track A/B 或后台价目表自动生成并生效正式价格 | 已撤回；所有正式价格必须人工审批。 |
| R-004 | 未审批报价可直接对外发送 | 已撤回；所有对外发送必须以已批准版本为前提。 |
| R-005 | 因产前样未封存而阻断面料采购 | 已撤回；采购在订单获批后标准启动，生产才受封样门禁。 |

## 下一步评审队列

1. 面料业务主链已完成讨论；后续只将 DR-011、DR-012、DR-014、DR-015、DR-016、DR-026、DR-027、DR-028 回写真源并补验收场景，不重复开放业务讨论。
2. 逐段审核服装订单状态机、样品版本与 QC 数据模型是否支持 DR-008、DR-009、DR-010、DR-029。
3. 审核服装与面料内部采购、部门利润和公司合并抵销的关系。
4. 将所有已确认决策逐项回写对应设计真源，并为每条补充验收场景。
