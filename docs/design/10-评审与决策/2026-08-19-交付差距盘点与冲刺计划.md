# 交付差距盘点与冲刺计划（2026-08-19）

## §0 元信息

| 项 | 值 |
|---|---|
| 文档性质 | 甲方交付验收差距盘点 · 冲刺作战图（进度追踪真源） |
| 盘点日期 | 2026-08-19 |
| 盘点方法 | 直接读代码验证（grep/read 定点核实）+ 双子代理并行扫描（前端设计进度 / AI 能力现状），全部结论附文件证据；8-10 投产验收基线逐项复核 |
| 关联真源 | [审计报告 §192/193 投产验收专项](../../../.trae/documents/bambook-comprehensive-project-scan.md) · [2026-08-17 前端设计地毯式评审报告](./2026-08-17-前端设计地毯式评审报告.md) · [2026-08-18 前端完善工程级执行方案](./2026-08-18-前端完善工程级执行方案.md) |
| 背景约束 | 项目已延期一月；token 配额紧张；桌面 Electron 为唯一交付形态（移动端搁置） |

---

## §1 总评

| 维度 | 评级 | 结论 |
|---|---|---|
| 核心业务链 | **A** | 面料+服装双链全链路 11/11 环节通过；门禁/MOQ/审批/出运资格/合票/例外放行闭环；4002 测试全绿 |
| 投产硬门槛 | **B+** | 8-10 基线 2 个 P0（调试错误条/公司抬头）已修；requestJson 超时等 P1 残留 |
| 前端设计 | **B-** | token 层负面守卫全部收敛达标；但 30 页销号 0/30、M4/L3/L6 布局余量未清零、M5 终审未启动 |
| AI 原生能力 | **B** | Agent 主链路（62 工具+checkpoint+SSE+会话内审批闭环）真实可用；"持久化+降级"两主题存在结构性缺口 |
| 部署运维 | **A-** | OPS push 通道稳定；生产库已纯净（2026-08-19 全量清理，备份在位）待真实数据 |

---

## §2 P0 — 投产阻断项（均为"最后一公里接线"，性价比极高）

| # | 差距 | 现状证据 | 修复量 | 状态 |
|---|---|---|---|---|
| P0-1 | **requestJson 无超时**：弱网/掉线请求无限挂起卡死界面（外贸团队海外场景常态） | `services/apiService.ts:304-321` 原生 fetch 无 AbortController；仅 testConnection 有超时 | 单文件统一入口加 AbortSignal.timeout + 网络错误语义化 | ⬜ |
| P0-2 | **审批中心决议不唤醒 Agent**：同一张 ApprovalRequest 两条决议路径行为不一致（会话内审批卡 approve → Agent 循环唤醒；审批中心 `/decide` approve → Agent 挂起至 15 分钟超时失败） | 全库 approvalEventBus emit 唯一点在 `agent/route.ts:426`；`approvals/approvalRoute.ts:78-124` decide 不 emit | approvalRoute（及 approvalKernelRoute）决议后补 emit | ⬜ |
| P0-3 | **Agent 记忆/后台任务纯内存占位**：进程重启全丢，跨会话记忆（用户偏好/业务惯例/历史决策）缺失，动摇"AI 原生 ERP"定位根基 | `agent/memory.ts:19-50` 内存数组；`agent/jobs.ts:10-36` 仅 enqueue/stats 无消费者；**schema 已预留 AgentMemory（schema.prisma:1579）/AgentJob（:1854）模型未接线** | 2 文件 Prisma 接线（模型已就绪） | ⬜ |
| P0-4 | **LLM 单 provider 无降级**：火山方舟故障/限流 = 对话/规划/vision 全部 AI 能力同时停摆；前端模型选择为伪选择（MODELS 全部语义别名指向同一 DEFAULT_ARK_MODEL） | `ai/runner.ts:449-457` 仅同源 key 兜底；`types.ts:1708-1735` | 网关加备用 provider 配置 + 健康探测切换 | ⬜ |

## §3 P1 — 交付质量硬伤

| # | 差距 | 现状证据 | 状态 |
|---|---|---|---|
| P1-1 | **前端 30 页销号 0/30 + M5 终审未启动**：四页优先（DocumentCenter/OrderManager/QuotationManager/Relations）两轮工程主刀完成但产品负责人验收未落档；P2-W4 回写后规格化推进停顿（转入 DR-042 业务功能）；高分验收第 10 条（全量截图走查）无法判定 | `docs/design-system/page-consistency-ledger.md` §1 全 ⬜、§5；台账渲染文件列失准（仍指向 Phase 0 已删 compiled 路径） | ⬜ |
| P1-2 | **M4 原生控件 77**（原生 select 57 + type=date 20）：集中在 AdminPanel(13)/SeasonsManager/FinanceManager/CrmManager 表单密集页 | `check-design-tokens.sh:101` 基线 264→77 | ⬜ |
| P1-3 | **原生 alert/confirm 存量 163 处/17 文件**（2026-08-10 基线 202 处）：QcWorkbench(31)/Pricing 域(35)/Seasons(23)/Risks(20)/Suppliers(19)/Crm(15)/Marketing(9)；BdsDialog/bds-toast 组件已建，迁移并入逐页主刀 | grep `(^|[^.\w])(alert|confirm)\(` components/ 实测 | ⬜ |
| P1-4 | **RAG 语义检索静默降级**：未配 BAMBOOK_RAG_API_KEY 时无告警降为 Prisma 子串匹配，同义/模糊提问无法命中且用户不可感知；火山方舟知识库写入侧已接、检索侧死代码（searchVolcKnowledge 全库无调用） | `agent/toolRuntime.ts:4020-4059`；`ai/volcKnowledge.ts:54` | ⬜ |
| P1-5 | **审批 bus 进程内易失**：EventEmitter 单进程内存对象，服务重启/多实例时挂起 Promise 丢失（仅 15 分钟超时兜底），挂起状态无 DB 持久化 | `agent/events.ts:451` | ⬜ |
| P1-6 | **TTS 单 provider 硬编码**：provider 非 melo 直接 throw；BAMBOOK_MELO_URL 必配指向独立 Python 服务，Melo 不可用 = 全语音播报不可用 | `ai/tts.ts:88-93,219-225` | ⬜ |

## §4 P2 — 体验完善

| # | 差距 | 证据 |
|---|---|---|
| P2-1 | L3 硬编码尺寸 59（206→59）/ L6 icon size 72·strokeWidth 14（663→72）/ L2 间距 6 / L5 行高 1，ProductsManager/Cockpit/Dashboard 为重灾区 | P2-W4 回写 commit 7d8b010 |
| P2-2 | isDarkMode 三元 219 处（2439→219，独立守卫线）；主题透明深层收编未立项（dark: 已达标 7≤10） | `check-design-tokens.sh:58` |
| P2-3 | 手写主按钮 8 + text-white 11（批 E 余量）；批 I 死代码 GarmentOrders.tsx（含唯一真实暖色残留 bg-amber-500，CD-012 已裁决破例全删未执行）；批 J UserAvatar.tsx:40-43 静态阴影原位残留 | check:tokens 基线；`components/ui/UserAvatar.tsx` |
| P2-4 | token 明文 localStorage（XSS 面）；agentLoop 预算保守（maxSteps=8/90s，复合任务天花板）；台账真源失准 + compiledMaterialLibraryTemplates.tsx 残留尾巴 | authService.ts；agent/defaults.ts:10-27 |

---

## §5 已达标项（放心清单）

- **核心业务链（A）**：面料三条件门禁（大货QC+S/S确认+RC确认）+ 服装 Final QC 单条件 + 合票逐单校验 + DR-013 例外放行；出运门禁错误分流（404/409）；MOQ 豁免 Confirmed 阻断自动发起 + DR-007 防重幂等；订单变更全量审批、交期锁死、信用额度 CAS；双链生产回归实测（2026-08-19，commit c58299b+1e75ba2）
- **投产基线（8-10 §192 复核）**：① 调试错误条 ✅ 已删；② 公司抬头 ✅ SystemConfig `global::company.exporterProfile` + AdminPanel 公司档案 Tab（2026-08-18 §1A 裁决）；⑤ 登录专业化 ✅
- **设计 token 负面守卫**：hex/遮罩/字重/裸圆角/raw 语义色（主域）/dark:（7≤10）全部收敛至豁免域基线；批 A-H 完成且防回退测试三重锁定
- **安全**：JWT+RBAC+写操作 requireJwtForWrite；401/403 fail-closed 覆盖；API-key 写操作 401
- **组织协作**：DR-041 八角色容器（双文件同步守卫）；DR-042 小组数据共享（读写 scope 分离+双重门禁+跟进档位）；设置域三层分离（个人偏好/本机连接/平台配置）
- **AI 主链路真实可用**：Agent Loop（plan→tool→observe→reflect，62 工具白名单）+ Prisma checkpoint 断点续传 + SSE 流式 + 会话内审批挂起/决议/modified 三态闭环 + 客户端本地 STT（sherpa-onnx）+ Melo TTS + Assistant 前端真模型接线（审批卡真落库/表单提交/实体搜索/vision 附件）+ 知识摄取管线（checksum 去重/chunk 切分/审计落库）
- **部署运维**：OPS push 通道（ops-upload-package）；生产健康监控；生产库纯净初始态（2026-08-19 全量清理，备份 `panda1:~/backup-before-wipe-20260819-092644.sql.gz`）

---

## §6 冲刺计划（按性价比排序）

> 约束：token 配额紧张 → 严格按 ROI 排序，每批次独立可交付可验证。

### 批次 1「投产阻断清零」（4 个小改动，预计单批完成）

| 任务 | 内容 | 验收 |
|---|---|---|
| 1a | requestJson 统一 AbortSignal.timeout（默认 30s，上传类豁免）+ 超时错误语义化（TIMEOUT 区分网络错误） | 弱网模拟请求 30s 中断 + Toast 提示；前端测试 |
| 1b | approvalRoute/approvalKernelRoute decide 后 emit approvalEventBus（与 agent/route.ts 同事件契约） | 集成测试：审批中心 approve → 挂起 Agent 循环被唤醒 |
| 1c | AgentMemory/AgentJob Prisma 接线（memory.ts 换 Prisma 实现；jobs.ts 补消费者执行器） | 重启进程后记忆/任务存活；agent 测试全绿 |
| 1d | RAG 降级显式化：searchKnowledge 降级时返回 `degraded: true` 标记 + 响应文案提示；volcKnowledge 死代码处置（接检索或删除） | 未配 key 时用户可见"降级为关键词匹配"提示 |

### 批次 2「AI 可靠性」

| 任务 | 内容 |
|---|---|
| 2a | LLM 网关备用 provider 配置化（provider 链 + 健康探测切换 + 前端 MODELS 真实差异化或诚实收敛为单模型） |
| 2b | 审批挂起状态入库（挂起审批持久化，配合 checkpoint 重启恢复，替代纯内存 Promise） |
| 2c | TTS 降级路径（melo 不可用时明确报错/静音开关，而非全语音功能 crash） |

### 批次 3「前端收尾」（并入逐页主刀流程）

| 任务 | 内容 |
|---|---|
| 3a | M4 原生控件清零（select→bds-select 57 处、date→CapsuleDateInput 20 处，AdminPanel/Seasons/Finance/Crm 优先） |
| 3b | alert/confirm 逐页收敛（163→0，QcWorkbench/Pricing/Seasons/Risks/Suppliers/Crm 顺序，并入逐页主刀） |
| 3c | 批 I 死代码删除（GarmentOrders + App.tsx 死 import + pwa 引用，CD-012 已裁决）+ 批 J UserAvatar 阴影收编 |
| 3d | 30 页销号走查（台账校准渲染文件列 → 逐页深浅双主题截图 → 产品负责人签字）+ M5 机器终审（高分验收 10 条复核） |

### 批次 4「P2 长尾」（可选，视交付窗口）

L3/L6/L2/L5 布局余量清零 → isDarkMode 三元收编立项 → 批 E 余量 → token 存储加固 → agentLoop 预算调参

---

## §7 进度追踪

| 批次 | 任务 | 状态 | commit | 备注 |
|---|---|---|---|---|
| 1 | 1a requestJson 超时 | ✅ | 5382712 | 统一 AbortSignal.timeout(30s) + TIMEOUT 错误语义化 |
| 1 | 1b 审批 decide 补 emit | ✅ | 37eeb6f | approvalRoute/approvalKernelRoute decide 后 emit，跨链路唤醒挂起 Agent |
| 1 | 1c Agent 记忆/任务持久化 | ✅ | 814b571 | memory.ts/jobs.ts Prisma 接线 + 消费者执行器；生产 manifest 已含 memory 工具 |
| 1 | 1d RAG 降级显式化 | ✅ | df1623e | ragDegraded/notice 显式返回 + volcKnowledge 死代码删除 + 上传非致命化 |
| 2 | 2a LLM 备用 provider | ⬜ | | |
| 2 | 2b 审批挂起入库 | ⬜ | | |
| 2 | 2c TTS 降级 | ⬜ | | |
| 3 | 3a M4 清零 | ⬜ | | |
| 3 | 3b alert 收敛 | ⬜ | | |
| 3 | 3c 批 I/J 收尾 | ⬜ | | |
| 3 | 3d 30 页销号 + M5 终审 | ⬜ | | |

---

## §8 验证方法说明（防幻觉纪律）

- 8-10 基线复核：grep 定点核实（INJECTED GLOBAL ERROR 零命中=已修；EXPORTER_PROFILE 读 SystemConfig=已修；requestJson L304 无 AbortController=未修）
- 原生弹窗精确计数：`(^|[^.\w])(alert|confirm)\(` components/ 全量 = 163 处/17 文件（排除 .alert() 方法调用误匹配）
- 前端设计进度/AI 能力：双子代理并行扫描，结论均附文件路径行号；子代理报告与主会话 grep 交叉复核（alert 计数以主会话精确 grep 为准）
- 守卫基线数字以 `scripts/check-design-tokens.sh` 入库值 + 最新回写 commit 为准
