# 第二轮需求池收口报告（批次 0 + 批次一 + 批次二）

> **范围**：第二轮 PRD 需求池（[2026-08-20-第二轮PRD需求池.md](./2026-08-20-第二轮PRD需求池.md)）批次 0（前置缺陷修复）+ 批次一（S 级 3 项）+ 批次二（A 级 5 项）共 **8 项需求 + 12 项缺陷**全部落地收口。批次三（B 级 7 项）/批次四（C 级 7 项）未启动，列入展望。
> **收口时间**：2026-08-20（单日完成，多会话协同推进）
> **基线**：5a66b90（需求池入库）→ HEAD 42ef0b9，共 11 个功能/修复 commit
> **验证纪律**：每项需求独立走 API 级实机验收脚本（`scripts/acceptance-req2-*.mjs`）+ 单测 + 三绿门禁（tsc 0 错 / vitest 全绿 / check:tokens / 前端 npm test / electron-vite build）

---

## §1 收口结论

| 维度 | 结果 |
|---|---|
| 需求交付 | **8/8**（REQ2-01 ~ REQ2-08 全部 MVP 落地，含全栈：schema + 服务 + 路由 + 前端 UI + 验收脚本 + 设计文档） |
| 缺陷清零 | **12 项全部修复**（P0×3 / P1×6 / P2×2 / P3×1，均登记根因与回归测试） |
| 能力域提升 | **9 个 L0/L1/L2 缺口域 → L3（实测）**：IND-01/03/04/06/08、C2/C4/C5、SEC-04 |
| 上线阻断 | **⚑ SEC-04 解除**（3 年历史数据迁移通道就绪，千行级实测 3.0s） |
| 实测发现并根因修复 | 4 类新缺陷（XLSX/CSV 通道、软删唯一键占用×2、InvoiceAllocation 字段口径）——全部当场根因修复非补丁 |

---

## §2 交付明细

### 批次 0：前置缺陷修复（地基）

| Commit | 内容 | 缺陷编号 |
|---|---|---|
| `2097187` | P0 三项：通知 401（身份解析与鉴权开关解耦）/ 供应商 overview 500（PurchaseLine 无 deletedAt）/ DR-010 变更审批链中段断裂（审批结果同步回 OrderChangeRequest） | P0-001/002/003 |
| `f82f9d3` | 第二批 P1：口径决策 **DR-044（应收净额）/ DR-045（核销真源=InvoiceAllocation）/ DR-046（汇率=单据时点快照）** + 核销真源统一 + Decimal 序列化共享工具（12 路由替换） | P1-003/004/005/006 |
| `5a5f433` | 批次 0 收官：CRM 可见范围过滤（bizScope=mine）/ 工厂档案补建（7 家 FACP__DEMO- 幂等 upsert）/ 凭证状态中文化 | P1-001/002/P3-003 |
| `532ba5f` `b17a711` | 验收复测补齐：演示客户归属三键 / C5 汇兑损益可演示数据 / P2/P3 清零（驾驶舱区间、订单返回入口、AI 页标题、报价文案、演示脏数据） | P2-001/002/004 |

### 批次一：S 级（痛度 34+，每日业务命脉）

| REQ | Commit | 能力域 | 验收 | 关键交付 |
|---|---|---|---|---|
| REQ2-01 色差管理体系（痛度 42 全池第一） | `bc82172` | IND-01 L0→**L3** | API 8/8 | SampleColorBatch 两态模型（lab_dip 挂开发案/bulk 挂订单）：缸号/4-5 级评级/疵点原因结构化枚举；批色通过即封样基准；疵点评级→分数幂等入供应商质量分；取证聚合一键打印（3min SLA） |
| REQ2-02 资金日历与 30 天现金流预测 | `d873212` | C4 L0→**L3** | 回归 6 用例 + 跨模块一致实测 | 今日动作清单 + 30/60/90 天窗口预测 + 外汇敞口 + 预收款泳道；净额口径 DR-044 全链同源（Peerless $34,000 账龄/对账单/发票/日历四处一致 §4.4 铁律） |
| REQ2-03 溢短装与多单位计价 | `4260d62` | IND-08 L0→**L3** | 15 用例 | OrderLine.tolerancePercent ±N% 条款 + toleranceService 单一真源（限额内结算/超限预警/条款上下限参考）+ 单位换算库（M↔YD 精确/公斤计价克重×门幅×长度）；订单详情溢短装校验视图（per 行偏差%/verdict 徽章） |

### 批次二：A 级（痛度 30-33，合规与上线刚需）

| REQ | Commit | 能力域 | 验收 | 关键交付 |
|---|---|---|---|---|
| REQ2-04 第三方测试管理 | `2840035` | IND-03 L1→**L3** | 单测 24 用例 + 三绿 | TestRequest 三模型（委托/报告 PDF 归档/整改）挂订单；fail 门禁：failItems 必填 + ≥1 条 open 整改强制（失败项 100% 跟踪闭环）；质量域三轨分工清晰（qcChain/InspectionReport/TestRequest 无双轨） |
| REQ2-05 面料工序级委外链 | `d081377` | IND-06 L2→**L3** | API 19/19 | OrderProcessNode 工序链（坯布→染整→后整理/涂层 + 承接工厂快照）；三决策：计划成本层与 MES 执行层不耦合/加工费入 BOM 利润表不生成应付/按产出量计费；三道链累计损耗 2.857% 精确实测；**实测发现并根因修复软删行占用 @@unique 缺陷** |
| REQ2-06 GRS TC 交易证书链 | `31d369f` | IND-04 L2→**L3** | API 11/11 | TcCertificate 三段链（原料→工厂→我方 TC）；一键校验四检查项（链完整/段间吨位/订单用量勾稽/有效期）；订单详情三段泳道 + 认证 Tab 供应商维度整链追溯；缺链/吨位倒挂/过期全 fail-closed |
| REQ2-07 历史数据批量迁移 ⚑上线阻断 | `2e18c87` | SEC-04 L0→**L3** | API 15/15 | 四类模板（客户/供应商/订单/发票）+ 两段式导入（validate 逐行校验零落库 → commit 批次留痕）+ 整批软删回滚；千行级 validate+commit 3.0s（≤1 工作日锚点超额 4 个数量级）；导入库内合计 == Excel 原账；**实测发现并根因修复 XLSX/CSV 通道与软删孪生复活两类缺陷** |
| REQ2-08 催款函套件 | `42ef0b9` | C2 缺口闭合 + KPI-05 支撑 | API 15/15 | DR-050 三决策：函即时组装（账龄明细注入非模板变量）/ DunningRecord 快照留痕/一键发起挂账龄行；中英双语函（逐发票逾期天数/分段/五桶汇总）+ printHtmlDocument 中英合版打印 + 渠道/结果登记 + 历史时间线；全链 38ms；**实测发现并根因修复 InvoiceAllocation.appliedAmount 字段口径缺陷** |

---

## §3 指标提升汇总

### 能力域 L 级变化（9 域 L3，均已按二级验证纪律实测）

| 能力域 | 前 | 后 | 实测锚点 |
|---|---|---|---|
| IND-01 色差管理 | L0 | **L3** | 打色登记→批色判定→疵点入质量分→取证打印全链 8/8 |
| IND-03 测试管理 | L1 | **L3** | 委托→报告归档→fail 门禁→整改闭环，单测 24 用例 |
| IND-04 认证管理（TC） | L2 | **L3** | 三段链校验 11/11：complete/缺链/吨位倒挂/过期 |
| IND-06 委外加工链 | L2 | **L3** | 工序链 19/19：三道链损耗精确/完工锁定/超投拒绝 |
| IND-08 单位与计价 | L0 | **L3** | ±5% 发货 5.2% 预警 + 上限结算额，15 用例 |
| C2 对账/催款 | 部分实测 | **L3（催款链）** | 账龄行→中英函→登记→历史全链 15/15，38ms |
| C4 资金日历 | L0 | **L3** | 30 天预测 + 四处数字一致（净额口径铁律） |
| C5 汇兑损益 | 存在缺陷 | **L3** | P1-006 修复后 Norden −432 CNY 实测（双边快照口径） |
| SEC-04 历史数据迁移 | L0 ⚑阻断 | **L3** | 千行 3.0s + 错误行 100% 定位 + 对账一致 + 回滚 |

### 缺陷清零（12/12）

P0-001 通知 401 · P0-002 供应商 overview 500 · P0-003 审批链断裂 · P1-001 CRM 可见范围 · P1-002 工厂档案空 · P1-003 Decimal 序列化 · P1-004 凭证状态脱节 · P1-005 账龄口径矛盾 · P1-006 核销双轨 · P2-001 演示脏数据 · P2-004 订单返回入口 · P3-003 状态中文化——全部登记根因 + 回归测试锚点（详见 [acceptance-defects.md](../../../.trae/documents/acceptance-defects.md)）。

### 第一轮综合成熟度影响

第一轮 30 域综合 ≈47%（L3×4 / L2×7 / L1×7 / 缺陷×3 / L0×9）。本轮后：L3 +9 域（4→13）、缺陷域 -3、L0 -5（9→4）。综合成熟度预计由 ≈47% 提升至 ≈70%+（待全量复评确认，能力地图已逐域更新）。

---

## §4 增强项与遗留（不阻塞验收，列入后续批次）

| 来源 | 增强项（MVP 未含） |
|---|---|
| REQ2-01 | 大货匹差抽检记录（4% 抽检率联动） |
| REQ2-02 | 预收款/保证金泳道已含；无 |
| REQ2-03 | 结算金额按实际发货量自动重算（已给条款上下限参考，自动重算列增强） |
| REQ2-04 | 客户测试要求模板化（下单自动生成待测清单）；测试费用入订单成本 |
| REQ2-05 | 工序级在制看板 |
| REQ2-06 | TC PDF 附件上传（attachmentPath 已预留）；认证↔产品 SKU 关联验证 |
| REQ2-07 | 报价历史导入（价格历史规则①数据源）；导入批次报告导出 |
| REQ2-08 | 对账单批量生成（全客户循环，KPI-05 月度 ≤10min 完整闭环）；催款提醒待办化 |
| 横向 | KPI-05 单客户链路已支撑（催款全链 38ms），批量生成落地后可完整评级 |

**技术债观察**：本轮 4 类实测缺陷中 2 类同为"软删行占用唯一键"模式（REQ2-05 @@unique / REQ2-07 poNumber·invoiceNumber）——已分别在各自服务修复；若后续再出现第三例，建议抽象统一的软删唯一键预检工具（当前两例修复各自内联，避免过早抽象）。

---

## §5 批次三/四展望（未启动）

- **批次三（B 级 7 项，痛度 24-28）**：REQ2-09 Pantone 色号库（S，与 REQ2-01 同域建议先行）→ REQ2-10 工厂延迟链路 → REQ2-12 报价单图片（S，L3 唯一缺口）→ REQ2-14 海运费利润重估（S）→ REQ2-15 客户破产货权处置；REQ2-11 双抬头分账（L）与 REQ2-13 离职一键交接（资产安全类）**建议管理层单独评审后立项**。
- **批次四（C 级 7 项，痛度 <24）**：远期池，按使用深度调整优先级。
- **并行建议**：SEC-05 双轨并行运行（≥1 个月）依赖 REQ2-07 已就绪，可随生产数据导入启动计时。

---

## §6 验收凭证索引

| 脚本 | 覆盖 | 结果 |
|---|---|---|
| `scripts/acceptance-req2-batch1.mjs` | REQ2-01/02/03 | 全过 |
| `scripts/acceptance-req2-05.mjs` | REQ2-05 工序链 | 19/19 |
| `scripts/acceptance-req2-06.mjs` | REQ2-06 TC 链 | 11/11 |
| `scripts/acceptance-req2-07.mjs` | REQ2-07 迁移 | 15/15 |
| `scripts/acceptance-req2-08.mjs` | REQ2-08 催款 | 15/15 |
| `scripts/acceptance-deep.mjs` / `acceptance-parallel.mjs` / `acceptance-req2-ui.mjs` | 深链/并行/验收复测 | 全过 |
| server vitest | 全量 | **293 文件 4188 用例全绿** |
| 前端 npm test | 全量 | **104 文件 2462 通过 + 6 视觉基线隔离** |
| 三绿门禁 | 每 commit | tsc 0 错 / check:tokens / electron-vite build 全过 |
| **UI 实机走查** | REQ2-01~08 前端渲染 | **8/8 通过**（见 §7） |

---

## §7 UI 实机走查（2026-08-20 收口后追加）

> 环境：web desktop 模式（`npm run dev:desktop`，localhost:3000）+ gm 管理员账号，数据态由 API 注入走查数据后浏览器实测，走查完毕已清理（done 锁定节点按软删口径 SQL 清理）。

| 需求 | 入口 | 走查结论 |
|---|---|---|
| REQ2-01 打色批次 | 开发管理→开发单详情 | COLOR BATCHES 区块：登记打色 / 导出证据链 / 空态引导文案 ✓ |
| REQ2-02 资金日历 | 财务管理→资金日历 tab | 今日动作清单（逾期天数红标）+ 30 天预测窗口（多币种不折算声明）+ 外汇敞口 + 预收款泳道 ✓ |
| REQ2-03 溢短装 | 订单详情→溢短装校验 | per 行表格：合同量/已发量/偏差/条款 ±5%/结算额/状态徽章（未发货灰态）✓ |
| REQ2-04 第三方测试 | 订单详情→TEST REQUESTS | 数据态：TR 统计头（通过/不合格/待报告/未闭环整改）+ fail 项 ✕ 标记 + 上传报告按钮；空态引导 ✓ |
| REQ2-05 工序委外链 | 订单详情→PROCESS CHAIN | 数据态：汇总头（N 道·完成 x/N·累计损耗·加工费合计）+ 工序卡（投入/产出/损耗%/单价/预估标注）+ 开工/完工登记/删除按状态机显隐；待开工金额"（预估）"口径 ✓ |
| REQ2-06 GRS TC 链 | 订单详情→TC CHAIN | 数据态：三段泳道（原料→工厂→我方）吨位汇总 + TC 编号/效期；**一键校验→「链条完整 可清关」结果面板** ✓ |
| REQ2-07 数据迁移 | 管理后台→数据迁移 tab | 四类模板 chips + 下载/上传校验 + 流程说明 + 14 批次历史（已导入/已回滚状态 + 回滚按钮）✓ |
| REQ2-08 催款函 | 财务报表→账龄分析 | 账龄行「催款」按钮（仅应收逾期行显示）→ DunningSheet：中英函 tab 切换（主题/正文即时切换）+ 渠道/结果 chips + 登记 → **历史时间线即时刷新**（渠道·结果·金额·跟进人·备注全要素）✓ |

**走查发现与处置**：
1. 演示库验收残留数据（工序链 seq 901-906「验收链」标注 + TCA/TCB/TCC-1787 前缀 TC 证书）污染 DEMO-PO-2601001 数据态视图——**已软删清理**；残留根因是 REQ2-04 验收脚本清理不彻底（留下 TR-20260820-001 fail 委托，保留作整改闭环演示，不阻断）。
2. 累计损耗显示 -2% 疑似异常——复核为**跨链脏数据所致非代码缺陷**（首道 seq1 投入 10000 → 末道 seq906 产出 10200，公式正确；清理后消除）。
3. 自动化点击对个别按钮（Sheet 关闭/一键校验）事件派发不完整，原生 click() 正常——工具性问题，真实用户不受影响，非产品缺陷。
