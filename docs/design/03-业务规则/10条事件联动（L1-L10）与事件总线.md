# 10 条事件联动（L1-L10）与事件总线

> **PRD 关联**：§10 业务十条 L1-L10 联动 + 跨模块事件驱动架构
> **落地状态**：✅ 事件总线 + 9 条联动处理器已实现 / ⚠️ approvalPolicy 全 manual（所有联动仅创建 Draft，需人工审核推进）

---

## §1 元信息与实现状态

| 维度 | 内容 |
|------|------|
| 定位 | 跨模块事件驱动架构：业务事件发布 → 持久化 → 订阅者执行 → 联动处理器自动创建下游 Draft 实体 |
| PRD 关联 | §10 业务十条 L1-L10 联动 |
| 代码关联 | `server/src/events/businessEventBus.ts`（事件总线单例）/ `server/src/events/linkages/L1-L10`（9 条联动处理器）/ `server/src/notifications/eventBindings.ts`（事件 → 通知桥接）/ `server/src/workflow/workflowEventTriggers.ts`（事件 → 工作流触发）/ `server/src/scheduler/tasks/retryLinkages.ts`（崩溃恢复 + 清理）/ `server/src/config/automationConfig.ts`（联动开关配置） |
| 角色 | 系统管理员（联动开关配置）/ 全部角色（事件通知接收者）/ agent:auto（联动执行者） |
| 范式 | 规则型：触发事件 × 幂等 key × 执行动作 矩阵 |
| 优先级 | P1（联动失败不阻断业务，但影响自动化效率） |
| **整体落地状态** | **✅ 事件总线 + 9 条联动已实现 / ⚠️ 所有联动仅创建 Draft 需人工审核（approvalPolicy 全 manual）/ L4 由通知系统覆盖无独立联动** |

---

## §2 事件总线架构

> **代码真源**：`server/src/events/businessEventBus.ts`

### 2.1 架构组成

```
业务事务提交
    │
    ▼
publishBusinessEvent(params)
    │
    ├─ 1. 持久化到 AgentJob 表（jobType=`bev:${type}`, status='queued'）
    │     └─ best-effort，永不抛错（P2002 唯一约束 = 幂等重发，静默接受）
    │
    └─ 2. in-process emit（EventEmitter）
          ├─ 逐个订阅者 try/catch（失败隔离，单个失败不影响其他）
          ├─ 异步订阅者 catch rejected promise（fire-and-forget）
          └─ 同时 emit 到 '*' 通配订阅（监控/调试/审计）
```

### 2.2 核心不变量

| 不变量 | 实现方式 |
|--------|---------|
| publish 必须在事务提交后调用 | 文档契约 + 调用方责任（绝不在事务内调用，避免脏事件） |
| publish 永不抛错 | 持久化失败仅日志；订阅者失败仅日志 |
| 订阅者失败隔离 | 手动遍历 listeners，每个 try/catch；异步 catch rejected promise |
| 事件 payload JSON 可序列化 | 持久化到 AgentJob.payload（Json 字段） |
| 幂等去重（LinkageHandler） | in-process Set（processedKeys）+ 业务层唯一约束 |
| 失败可重试 | execute 返回 not-ok 或抛错 → 从 processedKeys 移除 → 调度器崩溃恢复重放 |

### 2.3 关键 API

| API | 说明 |
|-----|------|
| `businessEventBus.publish(event)` | 发布事件（持久化 + in-process emit） |
| `businessEventBus.subscribe(eventType, handler)` | 订阅特定事件（或 '*' 通配），返回取消订阅函数 |
| `businessEventBus.registerLinkage(handler)` | 注册联动处理器（自动幂等去重 + 失败移除 key） |
| `publishBusinessEvent(params)` | 便捷发布函数（自动生成 eventId + occurredAt） |
| `generateEventId(type)` | 生成稳定唯一事件 ID：`bev_<type>_<timestamp>_<random>` |

---

## §3 BusinessEventType 全量清单（46 种）

> **代码真源**：`server/src/events/businessEventBus.ts` line 48-94（BusinessEventType 联合类型）

### 3.1 订单域（7 种）

| 事件类型 | 触发时机 | 发布位置 |
|---------|---------|---------|
| OrderCreated | 订单创建 | orders 路由 |
| OrderConfirmed | 订单确认（状态迁移至 Confirmed） | orderLifecycleService |
| OrderStatusChanged | 订单状态变更 | orderLifecycleService |
| QuotationIssued | 报价发出 | quotationService |
| QuotationAccepted | 报价被接受 | quotationService |
| DevelopmentConverted | 开发转报价/订单 | developmentService |
| FollowUpOverdue | 跟进逾期 | crmFollowUpWatchdog |

### 3.2 生产域（4 种）

| 事件类型 | 触发时机 | 发布位置 |
|---------|---------|---------|
| ProductionStageAdvanced | 生产阶段推进（seq=1-10 每次） | stageService.advanceStage |
| ProductionCompleted | 生产完成（qc_shipped 阶段 done） | stageService.advanceStage |
| BOMConfirmed | BOM 确认 | bomService |
| BOMCostCalculated | BOM 成本核算完成 | bomService |

### 3.3 采购/物料域（3 种）

| 事件类型 | 触发时机 | 发布位置 |
|---------|---------|---------|
| PurchaseOrderSent | 采购单发出 | procurementService |
| PurchaseOrderConfirmed | 采购单确认 | procurementService |
| MaterialReceived | 采购来料接收 | procurementService |

### 3.4 出运域（3 种）

| 事件类型 | 触发时机 | 发布位置 |
|---------|---------|---------|
| ShipmentCreated | 运单创建 | shipmentMutationService |
| ShipmentCompleted | 运单交付完成 | shipmentMutationService |
| ShipmentStatusChanged | 运单状态变更 | shipmentMutationService |

### 3.5 财务域（12 种）

| 事件类型 | 触发时机 | 发布位置 |
|---------|---------|---------|
| InvoiceIssued | 发票开具 | invoiceMutationService |
| InvoiceCancelled | 发票作废 | invoiceMutationService |
| PaymentVoucherCreated | 收付款凭证登记 | paymentVoucherService |
| PaymentReceived | 收款确认 | paymentVoucherService |
| AllocationReconciled | 核销完成 | allocationMutationService |
| CustomsDeclarationCreated | 报关单创建 | customsService |
| CustomsDeclarationStatusChanged | 报关单状态变更 | customsService |
| CustomsCleared | 报关放行 | customsService |
| TaxRefundCompleted | 退税完成 | customsService |
| FxSettlementCreated | 外汇结算创建 | fxService |
| OutwardRemittanceCreated | 汇出汇款创建 | fxService |
| LcStatusChanged | 信用证状态变更 | customsService |

### 3.6 客户/供应商域（9 种）

| 事件类型 | 触发时机 | 发布位置 |
|---------|---------|---------|
| RelationOnboarded | 客户/供应商建档 | relationService |
| CreditLimitExceeded | 信用额度超限 | crmService |
| CustomerTierAssigned | 客户分层评定 | crmService |
| OpportunityStageChanged | 商机阶段流转 | crmService |
| OpportunityClosedWon | 商机成交 | crmService |
| OpportunityClosedLost | 商机流失 | crmService |
| SupplierBlacklisted | 供应商拉黑 | supplierService |
| FactoryEvaluationAdded | 工厂评估新增 | factoryService |
| FactoryCertificationExpiring | 工厂认证到期 | factoryCertificationWatchdog |

### 3.7 库存/发票/HR 域（8 种）

| 事件类型 | 触发时机 | 发布位置 |
|---------|---------|---------|
| StockLowAlarm | 库存不足预警 | inventoryWatchdog |
| StockOverstockAlarm | 库存积压预警 | inventoryWatchdog |
| VatInvoiceCreated | 增值税发票创建 | vatInvoiceService |
| VatInvoiceStatusChanged | 增值税发票状态变更 | vatInvoiceService |
| ProformaInvoiceGenerated | 形式发票生成 | proformaInvoiceService |
| ProformaInvoiceConverted | 形式发票转换 | proformaInvoiceService |
| EmployeeStatusChanged | 员工状态变更 | hrService |
| LeaveRequestDecided | 请假审批结果 | hrService |

---

## §4 L1-L10 联动处理器矩阵

> **代码真源**：`server/src/events/linkages/index.ts`（注册入口）+ `server/src/events/linkages/L1-L10*.ts`

| 编号 | 联动 ID | 触发事件 | 幂等 key | 执行动作 | 落地状态 | 代码文件 |
|------|---------|---------|---------|---------|---------|---------|
| L1 | L1_init_production | OrderConfirmed | `auto:L1:${orderId}` | 初始化 10 阶段生产管线（upsert 幂等） | ✅ | `L1InitProduction.ts` |
| L2 | L2_create_shipment | ProductionCompleted | `auto:L2:${orderId}` | 创建 Draft 发货单（type=Export, method=Sea） | ✅ | `L2CreateShipment.ts` |
| L3 | L3_issue_invoice | ShipmentCompleted | `auto:L3:${shipmentId}` | 创建 Draft 应收发票（type=Receivable） | ✅ | `L3IssueInvoice.ts` |
| L4 | —（无独立联动） | InvoiceIssued | — | 由通知系统覆盖（InvoiceIssued → 付款待办通知） | ⚠️ 通知有/无业务联动 | `eventBindings.ts` |
| L5 | L5_auto_allocate | PaymentVoucherCreated | `auto:L5:${voucherId}` | 自动核销到关联发票（仅 Receipt 类型） | ✅ | `L5AutoAllocate.ts` |
| L6 | L6_create_bom_draft | OrderConfirmed | `auto:L6:${orderId}` | 从模板 BOM 复制 Draft BOM（无模板则占位） | ✅ | `L6CreateBOMDraft.ts` |
| L7 | L7_create_procurement | BOMConfirmed | `auto:L7:${bomId}` | 从 BOM 行生成 Draft 采购单 | ✅ | `L7CreateProcurement.ts` |
| L8 | L8_auto_stock_in | MaterialReceived | `auto:L8:${receiptId}` | 采购来料自动入库（默认主仓） | ✅ | `L8AutoStockIn.ts` |
| L9 | L9_convert_quotation_to_order | QuotationAccepted | `auto:L9:${quotationId}` | 报价转 Draft 订单（复用 convertToOrder） | ✅ | `L9ConvertQuotationToOrder.ts` |
| L10 | L10_create_tax_refund_draft | CustomsCleared | `auto:L10:${declarationId}` | 报关放行 → 核算生成 Draft 退税申报 | ✅ | `L10CreateTaxRefundDraft.ts` |

**落地统计**：✅ 9 条联动已实现 / ⚠️ L4 由通知系统覆盖无独立业务联动

> **重要说明**：
> - L2 触发于 `ProductionCompleted`（非 OrderConfirmed/ShipmentCreated）
> - L5 触发于 `PaymentVoucherCreated`（非 MaterialReceived）
> - L8 触发于 `MaterialReceived`（非 PurchaseOrderConfirmed）
> - 所有联动创建的均为 **Draft 状态**，需人工审核后推进（approvalPolicy 全 manual）
> - 所有联动受 `automationConfig.ts` 开关控制，默认全部 enabled=true

---

## §5 事件发布流程

```
1. 业务事务执行（prisma.$transaction）
   ├─ 业务写操作（创建/更新/状态迁移）
   └─ 审计日志写入
        │
        ▼ 事务提交
        │
2. publishBusinessEvent(params)        ← 必须在事务提交后调用
   ├─ generateEventId(type)            → bev_<type>_<ts>_<random>
   ├─ 构造 BusinessEvent 对象
   └─ businessEventBus.publish(event)
        │
        ▼
3. 持久化到 AgentJob 表
   ├─ jobType = `bev:${event.type}`
   ├─ status = 'queued'
   ├─ payload = event（JSON）
   └─ P2002 唯一约束 = 幂等重发，静默接受
        │
        ▼
4. in-process emit（EventEmitter）
   ├─ emit(event.type) → 逐个订阅者 try/catch
   ├─ emit('*') → 通配订阅（监控/调试）
   ├─ LinkageHandler 执行（registerLinkage 注册的）
   │   ├─ idempotencyKey 去重（processedKeys Set）
   │   ├─ execute(prisma, event) → 调用下游 service
   │   └─ 失败 → 移除 key → 允许调度器重试
   └─ NotificationService 执行（eventBindings 订阅的）
       └─ notifyFromEvent(event) → 渲染 + 投递通知
```

**调用时序**（`server/src/index.ts` 启动时）：
1. `initializeNotificationBindings(prisma)` — 注入 prisma + 通知订阅
2. `registerAllLinkages()` — 注册 9 条联动执行器
3. `registerWorkflowEventTriggers(prisma)` — 注册工作流事件触发器
4. `startScheduler(prisma)` — 启动调度器（注册 23 个定时任务）

---

## §6 事件 → 通知绑定

> **代码真源**：`server/src/notifications/eventBindings.ts`

`initializeNotificationBindings(prisma)` 在启动时订阅 31 种业务事件到 `notificationService.notifyFromEvent(event)`：

| 事件域 | 订阅事件类型 | 通知投递方式 |
|--------|------------|------------|
| 订单 | OrderCreated / OrderConfirmed / OrderStatusChanged | broadcastNotification |
| 生产 | ProductionStageAdvanced / ProductionCompleted | broadcastNotification |
| 出运 | ShipmentCreated / ShipmentCompleted / ShipmentStatusChanged | broadcastNotification |
| 财务 | InvoiceIssued / InvoiceCancelled / PaymentVoucherCreated / PaymentReceived / AllocationReconciled | broadcastNotification |
| 客户 | DevelopmentConverted / RelationOnboarded / CreditLimitExceeded / OpportunityStageChanged / OpportunityClosedWon / OpportunityClosedLost / CustomerTierAssigned | broadcastNotification |
| 关务 | CustomsDeclarationCreated / CustomsDeclarationStatusChanged / CustomsCleared / TaxRefundCompleted / FxSettlementCreated / OutwardRemittanceCreated / VatInvoiceCreated / VatInvoiceStatusChanged / LcStatusChanged | broadcastNotification |
| HR | EmployeeStatusChanged / LeaveRequestDecided | broadcastNotification |

**通配订阅**：`businessEventBus.subscribe('*', logger.info)` — 全部事件记录日志（监控/调试/审计扩展用）。

**解耦设计**：业务 service 只负责 `publishBusinessEvent`（不知道通知系统存在）；通知系统订阅事件并决定渲染与投递。

---

## §7 工作流触发器

> **代码真源**：`server/src/workflow/workflowEventTriggers.ts`

### 7.1 触发机制

启动时查询所有 `isActive=true && triggerEvent != null` 的 WorkflowDefinition，订阅对应事件。事件触发时自动创建工作流实例。

### 7.2 映射规则

```
event.sourceEntityType → instance.entityType
event.sourceEntityId   → instance.entityId
event.actorId          → instance.initiatedById
```

### 7.3 幂等性

`WorkflowEngine.createInstance()` 内部去重：同实体 + 同定义 + running 状态 → 返回已有实例（不重复创建）。

### 7.4 当前状态

| 维度 | 状态 |
|------|------|
| 触发器框架 | ✅ 已实现 |
| 默认工作流定义 | ⚠️ 取决于 seedDefaultWorkflowDefinitions 种子数据 |
| 自动启动审批流程 | ⚠️ 仅当 WorkflowDefinition 配置了 triggerEvent 时触发；当前 approvalPolicy 仍为 manual always |

---

## §8 调度器重试机制

> **代码真源**：`server/src/scheduler/tasks/retryLinkages.ts`

### 8.1 崩溃恢复任务（createCrashRecoveryTask）

| 维度 | 内容 |
|------|------|
| 任务 ID | crash_recovery |
| 执行时机 | 启动时执行一次（crashRecoveryDone 标记） |
| 扫描范围 | AgentJob where status='queued' AND jobType startsWith 'bev:'，批量 50 条 |
| 动作 | 逐条 `businessEventBus.publish(event.payload)` 重放 → 标记 status='completed' |
| 失败处理 | 标记 status='failed' + error 字段，避免反复重试坏数据 |
| 幂等保证 | publish 内部 P2002 去重 + LinkageHandler idempotencyKey 去重 + 业务层唯一约束 |

### 8.2 清理任务（createCleanupTask）

| 维度 | 内容 |
|------|------|
| 任务 ID | cleanup_queued_jobs |
| 执行时机 | 每小时 |
| 扫描范围 | AgentJob where status='queued' AND jobType startsWith 'bev:' AND scheduledAt < (now - 5min) |
| 动作 | 批量标记 status='completed'（正常流程中已由 in-process handler 处理完毕但未更新状态的记录） |

### 8.3 重试限制

| 维度 | 内容 |
|------|------|
| 最大重试次数 | 无显式上限（崩溃恢复仅启动时执行一次；in-process 失败后 key 移除，下次相同事件重新触发） |
| 失败隔离 | LinkageHandler execute 返回 not-ok 或抛错 → processedKeys.delete(key) → 下次同事件可重试 |
| 坏数据保护 | 崩溃恢复时 payload 无效（无 type 字段）→ 直接标记 completed 跳过 |

> **注**：任务描述中的 `retryLinkages.ts` 实际包含 `createCrashRecoveryTask` + `createCleanupTask` 两个任务。无独立的"最大重试次数"配置——崩溃恢复是启动时一次性重放，非持续重试。

---

## §9 API 端点

> **代码真源**：`server/src/config/automationRoute.ts` + `server/src/index.ts`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/v1/automation/rules` | 列出 L1-L10 联动规则及启用状态 | settings:system:read |
| PATCH | `/api/v1/automation/rules/:id` | 更新规则启用状态（body: `{ enabled: boolean }`） | settings:system:write |
| GET | `/api/v1/events` | 实时事件 SSE 流（text/event-stream） | sdkAuth |

> **注**：
> - 任务描述中的 `/api/v1/events/recent` REST 端点不存在；`/api/v1/events` 是 SSE 流（Server-Sent Events），用于实时推送，非历史查询。
> - 任务描述中的 `/api/v1/scheduler/status` 无独立 REST 端点；调度器状态仅通过日志暴露。
> - 历史事件可通过查询 AgentJob 表（jobType startsWith 'bev:'）获取，但无专用 REST 端点。
> - 自动化规则配置持久化到 `server/data/automation-config.json`（内存缓存 + 文件持久化）。

---

## §10 权限

| 权限 | 说明 | 适用角色 |
|------|------|---------|
| audit:read | 查看事件日志（AgentJob 表 bev:* 记录） | manager / admin / owner |
| settings:system:read | 查看自动化规则配置 / 调度器状态 | admin / owner |
| settings:system:write | 修改联动规则启用状态 | admin / owner |
| sdkAuth | 订阅 SSE 事件流 | 持有 API Key 的客户端 |

---

## §11 联动处理器详解

### 11.1 L1 OrderConfirmed → 初始化生产管线

| 维度 | 内容 |
|------|------|
| 触发事件 | OrderConfirmed |
| 幂等 key | `auto:L1:${orderId}` |
| 执行动作 | `initProductionStages(prisma, orderId)` — upsert 10 行 ProductionStage（seq=1 自动 done） |
| 业务层幂等 | upsert（orderId+stageKey 唯一约束） |
| 失败处理 | 返回 not-ok，key 移除，可重试 |

### 11.2 L2 ProductionCompleted → 创建发货单草稿

| 维度 | 内容 |
|------|------|
| 触发事件 | ProductionCompleted |
| 幂等 key | `auto:L2:${orderId}` |
| 执行动作 | 创建 Draft 发货单（type=Export, shippingMethod=Sea, shipmentNumber=SHP-{po}-{ts}） |
| 业务层幂等 | 查询该订单是否已有发货单，有则跳过 |
| 输出 | Draft 发货单（需人工补充装箱/物流信息后订舱） |

### 11.3 L3 ShipmentCompleted → 创建发票草稿

| 维度 | 内容 |
|------|------|
| 触发事件 | ShipmentCompleted |
| 幂等 key | `auto:L3:${shipmentId}` |
| 执行动作 | 创建 Draft 应收发票（type=Receivable, amount=totalNet‖quoteAmount, currency=order.currency‖USD） |
| 业务层幂等 | 查询该订单是否已有 Receivable 发票，有则跳过 |
| 输出 | Draft 发票（需人工核对金额/税率/到期日后开具） |

### 11.4 L5 PaymentVoucherCreated → 自动核销

| 维度 | 内容 |
|------|------|
| 触发事件 | PaymentVoucherCreated |
| 幂等 key | `auto:L5:${voucherId}` |
| 执行动作 | 自动核销到关联发票（核销金额 = min(凭证剩余, 发票剩余)） |
| 目标发票解析 | 优先 voucher.invoiceId → 按 orderId 查 Receivable → 按 customerRelationId 查 |
| 业务层幂等 | InvoiceAllocation @@unique([invoiceId, voucherId])，重复核销 P2002 跳过 |
| 限制 | 仅 Receipt 类型凭证；仅 Issued/PartiallyPaid 状态发票 |

### 11.5 L6 OrderConfirmed → 创建 BOM 草稿

| 维度 | 内容 |
|------|------|
| 触发事件 | OrderConfirmed |
| 幂等 key | `auto:L6:${orderId}` |
| 执行动作 | 查找匹配模板 BOM（Confirmed/Archived, description 含产品名）→ 复制物料行+成本估算；无模板则创建占位 BOM |
| 业务层幂等 | 查询该订单是否已有 BOM，有则跳过 |
| 输出 | Draft BOM（需人工补充/确认） |

### 11.6 L7 BOMConfirmed → 创建采购需求草稿

| 维度 | 内容 |
|------|------|
| 触发事件 | BOMConfirmed |
| 幂等 key | `auto:L7:${bomId}` |
| 执行动作 | BOM 物料行 → PurchaseLine（materialCode/description/quantity/unit/unitPrice=unitCost），创建 Draft 采购单 |
| 业务层幂等 | 查询该 bomId 是否已有采购单，有则跳过 |
| 输出 | Draft 采购单（供应商为空，需采购员指定） |

### 11.7 L8 MaterialReceived → 自动入库

| 维度 | 内容 |
|------|------|
| 触发事件 | MaterialReceived |
| 幂等 key | `auto:L8:${receiptId}` |
| 执行动作 | 查询采购单行 → 为有 materialCode+receivedQty 的行创建/更新库存项 + 入库流水（Inbound） |
| 默认仓库 | 优先 Main 类型 active 仓库 → 任意 active 仓库 → 无则跳过告警 |
| 业务层幂等 | 查询 StockMovement where referenceId=receiptId，有则跳过 |
| 输出 | 库存项 + 入库流水（referenceType=PurchaseOrder, referenceId=receiptId） |

### 11.8 L9 QuotationAccepted → 报价转订单草稿

| 维度 | 内容 |
|------|------|
| 触发事件 | QuotationAccepted |
| 幂等 key | `auto:L9:${quotationId}` |
| 执行动作 | 复用 `quotationService.convertToOrder()`（统一建单逻辑） |
| 业务层幂等 | convertToOrder 校验 convertedOrderId，已转化抛错被捕获 → 视为幂等成功 |
| 输出 | Draft 订单（需人工审核后确认） |

### 11.9 L10 CustomsCleared → 创建退税申报草稿

| 维度 | 内容 |
|------|------|
| 触发事件 | CustomsCleared |
| 幂等 key | `auto:L10:${declarationId}` |
| 执行动作 | 复用 `customsService.createTaxRefundFromDeclaration()`（统一核算逻辑） |
| 核算数据链 | 报关单行明细 + HS 退税率 + 发票/订单汇率快照 + 运单实际离港日 |
| 业务层幂等 | createTaxRefundFromDeclaration 校验 declarationId 唯一，重复触发抛错被捕获 → 幂等成功 |
| 输出 | Draft 退税申报（需人工提交审核） |

---

## §12 事件流闭环示例

### 12.1 订单 → 生产 → 出运 → 开票 闭环

```
OrderConfirmed
  ├─ L1 → initProductionStages（10 阶段骨架）
  └─ L6 → createBOMDraft（BOM 草稿）
        │
        ▼ BOM 确认
BOMConfirmed
  └─ L7 → createProcurement（采购单草稿）
        │
        ▼ 采购来料
MaterialReceived
  └─ L8 → autoStockIn（自动入库）
        │
        ▼ 生产推进（10 阶段门禁）
ProductionStageAdvanced × 9（seq=1-9）
        │
        ▼ qc_shipped 完成
ProductionCompleted
  └─ L2 → createShipment（发货单草稿）
        │
        ▼ 发货交付
ShipmentCompleted
  └─ L3 → createInvoice（发票草稿）
        │
        ▼ 收款登记
PaymentVoucherCreated
  └─ L5 → autoAllocate（自动核销）
```

### 12.2 报价 → 订单 闭环

```
QuotationAccepted
  └─ L9 → convertToOrder（订单草稿）
        │
        ▼ 订单确认
OrderConfirmed
  └─ L1 + L6 → 生产管线 + BOM 草稿
```

### 12.3 报关 → 退税 闭环

```
CustomsCleared
  └─ L10 → createTaxRefundDraft（退税申报草稿）
```

---

## §13 自动化配置

> **代码真源**：`server/src/config/automationConfig.ts`

| 维度 | 内容 |
|------|------|
| 配置存储 | 内存缓存 + 文件持久化（`server/data/automation-config.json`） |
| 默认状态 | 所有 9 条联动 enabled=true |
| 读取容错 | loadConfig 永不抛错，失败返回默认值；isLinkageEnabled 失败返回 true（默认启用，避免意外阻断） |
| 未知规则 | 未知 linkageId 默认 enabled=true |
| 配置项 | L1_init_production / L2_create_shipment / L3_issue_invoice / L5_auto_allocate / L6_create_bom_draft / L7_create_procurement / L8_auto_stock_in / L9_convert_quotation_to_order / L10_create_tax_refund_draft |
| 修改方式 | PATCH `/api/v1/automation/rules/:id`（写审计日志） |

---

## §14 待补缺口

| 缺口编号 | 缺口描述 | 影响范围 | 修复建议 |
|---------|---------|---------|---------|
| GAP-EVT-1 | **approvalPolicy 全 manual always**：所有联动仅创建 Draft 实体，无自动确认/自动审批路径 | 自动化停留在"草稿生成"阶段，每一步需人工审核推进，效率低 | 按联动类型配置 approvalPolicy（auto/manual/conditional）；低风险联动（如 L1 初始化阶段）可 auto；高风险（如 L3 开票）保持 manual |
| GAP-EVT-2 | **L3 开票联动按订单幂等可能漏开**：L3 按 orderId 查询已有 Receivable 发票即跳过，若一订单多发货单则只开一张发票 | 一订单多次分批发货时，后续发货单不触发开票 | 幂等 key 改为按 shipmentId（当前已是 shipmentId），但业务层检查改为"该发货单是否已关联发票"而非"该订单是否已有发票" |
| GAP-EVT-4 | **L4 QC→shipped 联动未实现**：终期验货通过后不自动推进 qc_shipped 阶段，需手动调用 advanceStage | 验货通过后还需手动推进阶段，增加操作步骤 | 增加 InspectionReport result=pass + approvedByBusiness → 自动 advanceStage(qc_shipped) 联动 |
| GAP-EVT-7 | **L7 收款→额度释放未实现**：PaymentReceived 事件不触发信用额度释放，额度占用仅占用路径闭环，释放路径未自动化 | 客户付款后信用额度未自动释放，可能导致新单被误拦 | 增加 PaymentReceived → crmService.releaseCreditLimit 联动（或复用 L5 核销完成后触发） |
| GAP-EVT-8 | **历史事件无 REST 查询端点**：AgentJob 表存储了全部 bev:* 事件，但无 `/api/v1/events/recent` REST 端点 | 无法从 API 查询历史事件日志，仅能查数据库 | 增加 GET `/api/v1/events/recent` 端点（分页查询 AgentJob where jobType startsWith 'bev:'） |
| GAP-EVT-9 | **调度器状态无 REST 端点**：调度器运行状态（任务列表/lastRunAt/running）仅日志可见 | 无法从 API 监控调度器健康状态 | 增加 GET `/api/v1/scheduler/status` 端点（暴露 tasks + lastRunAt + running 状态） |

---

## §X ApprovalPolicy 规则驱动升级方案（DR-007 落地 · 决策点 3-A + 6）

> 本章节定义 approvalPolicy 从「legacy always manual（前端手工选审批人）」升级为「服务端规则驱动 resolveReviewerByDepartment」的完整迁移路径。

### X.1 现状：ApprovalRequest.reviewerId 仍可由前端传入（legacy always manual）

| 维度 | 现状描述 |
|------|---------|
| reviewerId 来源 | 前端表单手工选择审批人 → 传入 `POST /api/v1/approvals` body.reviewerId |
| approvalPolicy 全局值 | `manual always`（所有审批类型均无规则解析，完全依赖前端传入） |
| 服务端兜底 | **无**：前端不传 reviewerId → DB 写入 null（schema.prisma:1814 字段为 nullable） |
| 风险 | 前端选错审批人、绕过审批层级、无组织归属校验、跨团队越权审批 |

### X.2 目标：全部审批单在服务端通过 resolveReviewerByDepartment(userId) 规则函数解析 reviewerId

| 项 | 规则函数定义 |
|----|-------------|
| 函数名 | `resolveReviewerByDepartment(requesterId: string): string` |
| 默认解析逻辑 | `UserAccount.departmentId → Department.managerId`（= 付款申请人所属部门的主管） |
| fallback 兜底条件 | ① requesterId 无 `departmentId`（无部门） ② `Department.managerId` 为 null（部门无主管） ③ requesterId 所属部门为「财务」→ 返回系统管理员 ADMIN（总领导兜底） |
| 单次审批约束 | 返回值为**单个 userId**，一次性只需一位有效审批人（不生成多人会签/或签） |
| 覆盖范围 | **全部 ApprovalRequest 类型**：付款凭证（pay_lt5/pay_gt5 兼容名）/ MOQ 豁免 / 价格审批（①-⑤）/ 订单变更 5 类 / 信用解冻 / 取消暂停 等 |

### X.3 迁移步骤（四步走 · 平滑兼容）

| 步骤 | 动作 | 实现位置 | 风险控制 |
|------|------|---------|---------|
| ① | 在 `approvalService.createRequest` 中增加 **reviewerId 非空服务端兜底**：前端传了 reviewerId 就**覆盖**（兼容 legacy）；前端没传或传 null → 用 `resolveReviewerByDepartment(requesterId)` 计算后写入 | `server/src/approvals/approvalService.ts` createRequest 函数 | 兜底不破坏现有前端手工选审批人的流程；兼容期可并行 |
| ② | **逐步移除前端 reviewerId 选择框**：所有审批发起页面（付款审批发起/MOQ 豁免发起/价格审批发起/订单变更审批发起）移除审批人下拉；UI 改为只读展示「审批人：{解析结果}」，由前端调用 `GET /api/v1/approvals/preview-reviewer?requesterId=xx` 查询解析结果 | `components/*/ApprovalRequestModal.tsx` 各审批发起弹窗 | ① 先在后端到位后再移除前端选择框，避免空值；② 移除后保留 GET preview 接口供 UI 展示 |
| ③ | **ApprovalRequest.reviewerId 列级 NOT NULL 约束加 migration**：在确认 ①② 全量上线、服务端 reviewerId 永不落空之后，加 prisma migration：`ALTER TABLE "ApprovalRequest" ALTER COLUMN "reviewerId" SET NOT NULL;` | `server/prisma/migrations/` 新增 migration + schema.prisma 字段标记为 non-null | 加约束前先跑 dry-run SQL 检查是否有历史 null 行；有则批量回填（null 的历史单按 requesterId 重算 reviewerId 后补上）|
| ④ | **6 个 scope 名标记为兼容保留（文档注明）**：`pay_lt5 / pay_gt5 / lt10_ / gt5_` 等 scope 前缀与阈值/比例相关的命名，全部标记为「兼容保留名」——实质审批路由逻辑与金额/比例彻底无关，全部走 DR-007 统一 resolveReviewerByDepartment | 本文件 + 各规则 md 交叉注明 | 不删旧 scope 常量（避免后端权限 ACL/前端 scope 绑定代码报错），仅文档声明其语义变化 |

---

## §15 相关文档索引（交叉链接）

| 文档 | 链接 | 关联要点 |
|------|------|---------|
| 00-索引 | [../00-索引.md](../00-索引.md) | §03 业务规则 8 份文档清单 |
| 业务规则总览 | [业务规则总览.md](./业务规则总览.md) | L1-L10 联动落地状态矩阵 / 22 条规则索引 |
| 交期与生产规则 | [交期与生产规则.md](./交期与生产规则.md) | L1 联动 → initProductionStages / ProductionStageAdvanced + ProductionCompleted 事件发布点 |
| 订单状态机 | [../04-模块设计/03-订单与生产/Orders-订单管理/订单状态机.md](../04-模块设计/03-订单与生产/Orders-订单管理/订单状态机.md) | OrderConfirmed / OrderStatusChanged 事件触发点（状态迁移后发布） |
| 价格审批规则 | [价格审批规则.md](./价格审批规则.md) | L9 报价转订单联动 / QuotationAccepted 事件 / 双轨偏差审批门禁 |
| 信用控制规则 | [信用控制规则.md](./信用控制规则.md) | GAP-EVT-7 收款→额度释放缺口 / CreditLimitExceeded 事件 / L5 核销闭环 |
| 系统架构 | [../01-产品总览/2. 系统架构.md](../01-产品总览/2.%20系统架构.md) | 事件总线在整体架构中的位置 / 调度器 / AgentJob 持久化层 |
