# 质量门禁规则

> **PRD 关联**：§9.3 质量门禁 3 条（①裁剪前 PreCut / ②厂前样双签 / ③终期 AQL 致命疵点零容忍）
> **落地状态**：✅ ①裁剪前 PreCut 4 项 / ③终期 Final AQL 已强校验；⚠️ ②厂前样双签后端字段 + 校验已存在但前端 UI 缺失签字入口

---

## §1 元信息与实现状态

| 维度 | 内容 |
|------|------|
| 定位 | 生产 10 阶段流水线 3 个关键节点的质量门禁，fail-closed 锁死阶段推进；不合格不允许进入下一工序 |
| 触发入口 | advanceStage（pre_cut_checked seq=6 / pp_sample_approved seq=7 / qc_shipped seq=10） |
| 角色 | 生产部（产前样签字）/ 业务部（产前样签字 + 终期批准发货）/ QC（验货报告录入）/ 跟单（PreCut 4 项勾选） |
| 范式 | 规则型：§6 触发矩阵 |
| 优先级 | P0（3 门禁均属出货前关键防错，一旦漏检即产生客诉/空运/扣款等重大损失） |
| **落地状态** | ① ✅ / ② ⚠️（后端校验到位，前端 UI 缺签字按钮）/ ③ ✅ |
| ① 已实现代码 | `stageService.ts:270-283`（advanceStage stageKey=pre_cut_checked 分支，4 项全 true 校验，缺任一抛 PRECUT_CHECKLIST_INCOMPLETE）/ schema.prisma:2457-2472（PreCutChecklist 模型 4 布尔字段） |
| ② 部分实现代码 | `stageService.ts:285-292`（advanceStage stageKey=pp_sample_approved 分支，signedByProduction && signedByBusiness 校验，缺则 PP_SAMPLE_NOT_SIGNED）/ schema.prisma:2444-2447（ProductionStage.signedByProduction/signedByBusiness + 时间戳）/ `stageService.ts:582-620`（signStage API 后端函数已实现）；**缺口**：生产看板/订单详情页 Production pipeline 区块无「生产部签字 / 业务部签字」按钮 |
| ③ 已实现代码 | `stageService.ts:294-319`（qc_shipped 分支：result≠fail / criticalDefects=0 / passRate≥90% / defectRate≤3% / approvedByBusiness 5 道门禁全通过才放行）/ schema.prisma:2481-2513（InspectionReport 模型 + criticalDefects/majorDefects/minorDefects 三级疵点）/ `qcService.ts`（验货任务 QCAssignment 状态机，但验货报告写入由 stageService.saveInspectionReport 承担）|
| PRD 关联 | §9.3 ①-③ / §4.2 QC 工作台 / §7.1 终期验货 fail 广播 |

---

## §2 三道门禁详述

### 2.1 门禁 ①：裁剪前 PreCutChecklist 4 项（seq=6，✅ 已落地）

**PreCutChecklist 模型字段**（schema.prisma:2457-2472）：

| 字段 | 含义 | 类型 | 默认 | 谁来勾选 |
|------|------|------|------|---------|
| gradingConfirmed | 推码确认（尺码跳码表正确，含放量/缩率） | Boolean | false | 跟单/技术 |
| consumptionConfirmed | 耗料确认（单耗/排版图/面料利用率核算正确，避免超领） | Boolean | false | 跟单/物控 |
| patternConfirmed | 样板确认（最后一轮 fit/pp 样板客户签字确认，无遗留修改点） | Boolean | false | 业务/开发 |
| preProductionMeeting | 产前会议（与工厂/面料/辅料开产前会，交期/质量/特殊工艺达成一致） | Boolean | false | 生产部主持，跟单+QC 列席 |
| meetingNote | 产前会议纪要 | String? | null | — |
| confirmedBy / confirmedAt | 确认人 / 确认时间 | — | — | — |

**推进逻辑**：`stageService.ts:270-283` advanceStage 进入 pre_cut_checked 时：
1. 查 PreCutChecklist（orderId 唯一），不存在直接报错 PRECUT_CHECKLIST_INCOMPLETE
2. 4 布尔字段 AND，缺哪个 → 中文拼接缺项名（推码确认、耗料确认、样板确认、产前会议）
3. 全部通过 → stage.status=done

### 2.2 门禁 ②：厂前样（PP Sample）双签（seq=7，⚠️ 部分落地）

**双签定义**：产前样 pp_sample_approved 阶段完成必须两方签字：
- `ProductionStage.signedByProduction`（schema.prisma:2444）：工厂/我方生产部签字（确认工艺可执行、设备/人员排产到位）
- `ProductionStage.signedByBusiness`（schema.prisma:2445）：业务部签字（确认客户已批 pp、颜色/手感/尺寸与客户确认件一致）

**后端校验**（stageService.ts:285-292 ✅ 已实现）：advanceStage 时两者同时非 null 才通过，缺哪个中文提示。

**签字 API**（stageService.ts:582-620 ✅ 已实现 signStage 函数）：
```
signStage({ orderId, stageKey: 'pp_sample_approved', signType: 'production' | 'business', signerId })
```

**前端缺口（⚠️ 未实现）**：
- Production pipeline 泳道 pp_sample_approved 阶段卡片无签字按钮（应区分生产/业务两个按钮，根据当前用户角色显示有权限的按钮）
- 签字状态 UI：灰色「待签字」→ 蓝色「生产部已签」→ 绿色「双签完成」三态
- 签字人信息 tooltip：悬停显示签字人 + 时间

#### 2.2.1 双签按钮组 UI 规格说明（§9.3-② 产前样）

**双签按钮组**：在 pp_sample_approved 阶段卡片右下角渲染两个独立签字按钮（水平排列，gap-2）：

| 按钮 | 未签字态（默认） | 已签字态 | 权限控制 |
|------|------------------|---------|---------|
| **QC签字**（原生产部签字） | 灰色 `bds-btn-secondary-disabled` 文案「QC签字 待签字」，tooltip=「点击以 QC 身份签字确认产前样工艺可执行」 | 绿色 `bds-btn-success-solid` 文案「QC已签：张三 · 2026-08-15 14:30」，圆点 indicator +，悬停显示签字详情 | 仅 QC 角色可见可点（`qc:write` scope）；其他人按钮 disabled 灰色不响应点击 |
| **业务确认签字**（原业务部签字） | 灰色 `bds-btn-secondary-disabled` 文案「业务确认 待签字」，tooltip=「点击以业务员身份签字确认客户已批 PP 样」 | 绿色 `bds-btn-success-solid` 文案「业务已签：李四 · 2026-08-15 15:12」，圆点 indicator +，悬停显示签字详情 | 仅 `Order.ownerId`（业务员本人）或其部门 `Department.managerId`（销售主管）可见可点；其他人按钮 disabled |

**两个按钮都点击变绿（均已签字）后**，门禁解锁条件通过，`advanceStage(pp_sample_approved)` 不再抛 `PP_SAMPLE_NOT_SIGNED` 错误。

**签字逻辑流程**：
```
用户点击对应签字按钮
  │
  ▼
弹窗（二选一，UI Lab 统一签名组件）：
  · 方案 A（推荐，简单快速）：确认对话框
    标题「确认以「张三（QC角色）」身份签字？」
    文案：「此操作将永久记录签字人与时间戳，不可撤销」
    按钮：[取消] [确认签字]
  · 方案 B（合规留痕）：canvas 签名板
    提供手写签名画布 + 清除 + 保存按钮，签名 base64 存入 signatures.signatureImage
  │
  ▼ 确认签字后
  调用 signStage({ orderId, stageKey: 'pp_sample_approved', signType: 'qc' | 'business', signerId })
  后端写入 InspectionReport.signatures JSON（兼容 ProductionStage 字段）：
  {
    qcSignedAt: "2026-08-15T14:30:00.000Z",
    qcSignerId: "user_abc123",
    qcSignerName: "张三",        // 快照，防改名
    qcSignatureImage: "<base64>", // 方案 B 时存在
    businessSignedAt: null,
    businessSignerId: null,
    businessSignerName: null,
    businessSignatureImage: null
  }
  （同时向下兼容 ProductionStage.signedByProduction/signedAtProduction 等字段写入）
```

**后端校验**：
`StageService.checkPreCutReady()`（对应 pp_sample_approved 门禁）同时校验 signatures JSON 中 `qcSignedAt` 与 `businessSignedAt` **两个字段都非空**才算通过；任一为空则抛 `PP_SAMPLE_NOT_SIGNED`，中文提示缺哪个（例：「产前样需 QC 签字 + 业务确认双签，当前缺 QC 签字」）。

**UI 视觉纪律**：
- 未签字：按钮 `bds-btn-secondary` disabled，背景 `--surface-muted`，文案 `--text-tertiary`，左侧带空心圆点 `○`
- 已签字：按钮 `bds-btn-success-solid`，背景 `--badge-approved-bg`，文案 `--badge-approved-text`，左侧带实心圆点 `●`（6px）
- 签字人姓名与日期：已签字态按钮内紧凑显示，12px 字，`H-28px` 按钮高度；点击已签字按钮不弹窗，只展开 tooltip 详情
- 双签完成后：pp_sample_approved 阶段卡片顶部边框变绿（`--border-success`，3px 顶边），badge 显示绿色「双签完成」

### 2.3 门禁 ③：终期 AQL 0 致命疵点（seq=10，✅ 已落地）

**终期验货 InspectionReport.final 5 道门禁**（stageService.ts:294-319，顺序判断任一失败即 INSPECTION_NOT_QUALIFIED / BUSINESS_APPROVAL_REQUIRED）：

| 道次 | 判断条件 | 阈值 / 说明 | 错误消息示例 |
|------|---------|------------|------------|
| 1 | 终期报告必须存在 | orderId + inspectionType='final' 查不到 → 报错 | 终期验货报告（final）未建立 |
| 2 | result ≠ 'fail' | result ∈ {pass, conditional} 可放行；fail → 需整改复验 | 终期验货结论为不合格（fail），需整改复验 |
| 3 | criticalDefects = 0 | AQL 0 零容忍（致命疵点：断针/毒性/窒息风险/客户品牌标准严重违规）；>0 即整批拒收 | 存在 N 个致命疵点（AQL 0 零容忍） |
| 4a | passRate ≥ 90%（MIN_PASS_RATE = 0.90） | passRate = passedUnits / totalUnits | 合格率 ××% 低于阈值 90% |
| 4b | defectRate ≤ 3%（MAX_DEFECT_RATE = 0.03） | defectRate = (totalUnits - passedUnits) / totalUnits | 不合格率 ××% 超过上限 3% |
| 5 | approvedByBusiness = true | 业务部最终批准发货（防止 QC pass 但客户/业务侧仍有异议） | 需业务部批准发货 |

**终期验货 fail 广播**（stageService.ts:549-567 ✅ 已实现）：
结论从非 fail 迁移到 fail 瞬间（previous.result≠'fail' && result==='fail'）→ notificationService.broadcastNotification：type=`inspection_fail`，level=`critical`，通知对象 = QC + 业务员 + 管理层，链接 `/production?orderId=xxx`。

**自动供应商评分**（stageService.ts:524-545 ✅ 已实现 H1c）：
验货报告保存成功 → 通过 Order.millRelationId → 工厂档案 FactoryProfile → recordAutoEvaluation，评分算法 `inspectionScoreForResult(result, criticalDefects)`。

---

## §6 触发矩阵（3 门禁 × 动作 × 门禁 × 异常分支）

| # | 触发条件 | 动作 | 门禁（Gate） | 异常分支（Fail-Closed） |
|---|---------|------|-------------|----------------------|
| 1 | advanceStage(pre_cut_checked) 被调用 | ✅ 查 PreCutChecklist → 4 项全 true 校验 ✅ 阶段 status=done + 审计 ✅ 发布 ProductionStageAdvanced 事件 | ✅ 缺任一项 → PRECUT_CHECKLIST_INCOMPLETE，阶段保持 pending，UI Toast 缺项清单 | Checklist 行不存在（历史订单未补录）→ 报错提示「请先完成裁剪前检查」而非静默失败 |
| 2 | savePreCutChecklist 勾选 4 项 | ✅ upsert PCL__orderId → 写 confirmedBy/confirmedAt + 审计 | 无（只记录，推进仍需 advanceStage 显式调用） | 并发勾选（两人同时勾不同项）→ upsert 原子按字段合并，不会互相覆盖 |
| 3 | advanceStage(pp_sample_approved) 被调用 | ⚠️ 后端：signedByProduction && signedByBusiness 校验 ✅ | ⚠️ 后端校验缺签 → PP_SAMPLE_NOT_SIGNED 已实现；前端无签字入口导致 100% 卡在此阶段（当前实际落地缺口） | signStage 签字人无对应角色权限 → 403；未实现，当前仅后端缺签校验 |
| 4 | signStage('production' | 'business') 签字请求 | ✅ 后端：写 signedByProduction/signedAtProduction 或 business 对应字段 | 无（签名接口无额外门槛，建议补角色权限） | 重复签字 → 覆盖旧签字 + 审计 log 应记录「原签字人 → 新签字人」变更 |
| 5 | saveInspectionReport('final') 保存终期报告 | ✅ upsert 报告 + ✅ criticalDefects/major/minor + ✅ result 迁移 fail 瞬间广播 critical + ✅ 供应商自动评分 | 无（保存即写入，不自动推进 qc_shipped 阶段；需用户显式 advanceStage 触发门禁） | orderId 不存在/软删 → ORDER_NOT_FOUND；非法 result（非 pass/conditional/fail）→ 抛错 |
| 6 | advanceStage(qc_shipped) 被调用 | ✅ 5 道门（报告存在/result≠fail/critical=0/pass≥90%+defect≤3%/approvedByBusiness）全通过才放行 ✅ 通过后发布 ProductionCompleted 事件（触发后续运单/单据联动） | ✅ 任一门失败 → INSPECTION_NOT_QUALIFIED / BUSINESS_APPROVAL_REQUIRED；**禁止 shipment 创建** | 报告存在但字段不全（totalUnits=0 导致 passRate=0）→ 报合格率 0%<90%，提示「请先补全验货数量」 |
| 7 | InspectionReport.result 从 pass → fail（整改后复验又 fail = 新事件） | ✅ stageService.ts:549 previous 判断幂等：只有迁移瞬间才广播（重复保存相同 fail 不再通知） | 无 | 两次 fail 间隔短（客户与我方 QC 结论不一致）→ 需两条独立 critical 通知，并在 metadata 带时间戳区分 |
| 8 | approvedByBusiness=true（业务批货） | ✅ saveInspectionReport 参数 approvedByBusiness → 写 approvedAt 时间戳 + businessApprover | 建议补：无对应业务角色（非业务部/管理层/老板）→ 403；当前未实现，任何人可设 true | 业务批准后，qc_shipped 阶段完成前，有人手工改 InspectionReport 字段 → 批准失效需重新批（建议 approvedAt 时间戳早于 updatedAt → 自动 reset approvedByBusiness） |

---

## §11 业务规则关联（22 条交集）

| 规则 | 关联点 |
|------|--------|
| §9.7 订单变更-交期 | 终期验货 fail 需整改复验 → 实际交期延后 → 触发交期变更审批 + 客户延期通知（交期与生产规则 15 天延期） |
| §10 L10 事件 | ProductionCompleted（qc_shipped 通过后发布）→ L5 Shipment 建运单 → L6 贸易单据草稿（tradeDocumentLifecycleService.generateTradeDocumentsFromShipment 已实现） |
| §9.4 信用控制 | 终期验货 fail → 延期/返工导致订单逾期 → 影响客户账期（但不属于客户逾期，需区分工厂责任 vs 客户付款责任） |

---

## §14 待补设计缺口（⚠️ ② UI + 增强建议）

| P | 缺口 | 细化位置 |
|---|------|---------|
| P0 | pp_sample_approved 前端签字按钮 + 三态（待签/生产已签/双签完成）+ 角色控制 | components/MesManager / ProductionBoard Pipeline 区块 + 订单详情页生产管线 |
| P1 | signStage 签字人角色权限校验（生产部只能签 production；业务部/管理层只能签 business） | stageService.ts signStage 函数内新增 Role 判断 |
| P1 | approvedByBusiness 业务批准角色限制 + approvedAt < updatedAt 自动重置（防止改数据后批准仍有效） | stageService.ts saveInspectionReport 业务批准字段写入前置判断 |
| P1 | PreCutChecklist 每项填写人 + 时间独立记录（当前仅整体 confirmedBy/confirmedAt，无法追溯 4 项分别是谁填的） | PreCutChecklist 模型扩展 4 组 × confirmedBy/confirmedAt 字段 |
| P2 | 中期验货 midline 不达标（result=fail）→ 自动禁止中期→生产过程阶段推进（当前只 block qc_shipped，中期 fail 建议加一个 manufacturing 入口门禁） | stageService.ts manufacturing(seq=8) 进入前加 midline inspection pass 判断 |

---

## §13 DR-013 受控例外 × 3 道质量门禁验收

> **决策来源**：[DR-013](../10-评审与决策/2026-08-16-设计评审决策记录.md#dr-013-全局业务门禁的受控例外推进) × [DR-029 QC 责任闭环](./QC-WorkBench.md)
> **Fail-Closed 铁律（DR-029）**：QC 容器角色只有录入 InspectionReport + 填写 qcProfessionalOpinion（EXC 专业意见）的权限，**绝对不持有 EXC 最终审批权**。批准权永远在业务主管（DR-007 路由）；EXC 批准后 InspectionReport 原数据 **一字不改**。

| 验收编号 | 场景 | 前置条件 | 执行步骤 | 期望结果（QC 数据零修改 + DR-029 责权分离） | 失败路径 |
|---------|------|---------|---------|-------------------------------------------|---------|
| QCG-EXC-1 | PreCut 门禁（① seq=6）4 项缺样板确认（样板客户还在走内部审批，但 ETD=7 天不得不先开裁，客户总部已通过邮件确认样板，纸质签字 2 天后补） | 1. PreCutChecklist：gradingConfirmed=T / consumptionConfirmed=T / patternConfirmed=F（缺样板确认）/ preProductionMeeting=T；2. advanceStage(pre_cut_checked) 被 PRECUT_CHECKLIST_INCOMPLETE 拦截，缺项=样板确认；3. 客户总部邮件《Pattern Approval for Style X》（邮件附件 + 转发链）确认样板，客户承诺 2 天内补盖章签字版；4. 业务员在拦截弹窗看到 EXC 入口 | 1. 业务员提交 EXC：exceptionCategory=production_gate / subCategory=precut_missing_pattern_approval；exceptionReason=「客户邮件已确认样板（附件），纸质签字版 2 天内补寄；ETD=7 天，不先开裁将延误 14 天导致空运费 8W 美元（≥30字）」；attachments=[客户邮件截图 + 转发链 PDF]；riskMitigationPlan=「跟单 2 天内盯纸质签字版，收到后立即 patternConfirmed=T 补签，超期自动 RiskAlert」；2. QC 容器不需要填专业意见（PreCut 是跟单/业务的 Checklist，不是 QC 检验）；3. 张主管 Approve | 1. advanceStage(pre_cut_checked) 绑定 EXC-500 → 成功通过（写入 OrderStatusTransition.gate=PRECUT_PATTERN, bypassExcId=EXC-500）；2. PreCutChecklist.patternConfirmed **仍=F**（不被 EXC 改成 T）；前端在 pp_sample_approved 阶段卡片顶部显示黄徽标「PreCut 样板确认后补（EXC-500）+ 链接」；3. 系统 watchdog 2 天后检查 patternConfirmed 是否仍=F → 自动生成 RiskAlert（severity=high）「EXC-500 承诺的样板签字版未补，请跟单跟进」；4. 其他订单的 PreCut 门禁仍严格校验 4 项全 T（EXC 不全局放宽 PreCut） | EXC 批准后自动把 PreCutChecklist.patternConfirmed=T → 补签承诺没人跟进了（跟单以为系统自动完成了），纸质签字版永远收不到（EXC 只放行动作，不补数据，数据必须人工跟进）；Watchdog 不查补签 → EXC 承诺的补料事项无闭环 |
| QCG-EXC-2 | 产前样双签门禁（② seq=7）缺业务部签字（ownerId 业务员出差 3 天，无法登录签字；但客户已批 PP 样 + 生产部已签工艺可执行，差业务签字）→ EXC 绕过业务签字缺口，不允许 QC 容器代理签字 | 1. ProductionStage.pp_sample_approved：signedByProduction=张（生产部/QC 已签）/ signedByBusiness=null（缺业务签字）；2. advanceStage(pp_sample_approved) 被 PP_SAMPLE_NOT_SIGNED（缺业务签字）拦截；3. 业务员孙出差（已邮件委托同事代办，但代办人无 Order.ownerId 权限，§2.2.1 业务签字权限=ownerId 或部门主管）；4. 孙本人手机能看到审批但无电脑客户端可签字（或无法访问 VPN） | 1. 场景①：QC 容器 Q（已签生产部）想帮孙「代签业务签字」（勾选"业务签字同意"复选框直接调 signStage business）；2. 场景②：张主管（部门主管）走 EXC：exceptionCategory=production_gate / subCategory=pp_sample_business_signer_unavailable；exceptionReason=「业务孙出差 3 天无法签字，已微信委托（截图附），客户 PP 样已批 + 生产已签，不推进延误 3 天（≥30字）」；attachments=[孙微信委托截图 + 客户 PP Approval 邮件]；3. 张主管 Approve EXC；4. advanceStage 绑定 EXC | 1. ① QC Q 调 signStage(business) → 403 SCOPE_DENIED + PP_SIGN_BUSINESS_ROLE_REQUIRED（只有 Order.ownerId 或 Department.headId 可签业务，QC 容器即使勾选"同意"复选框也被后端拦截，复选框 disabled 灰显=前端防呆）；2. ② EXC 批准后 advanceStage 成功，ProductionStage 写 ppBypassedExcId=EXC-501（审计快照）；3. signedByBusiness 仍=null（不被 EXC 填孙的 ID 伪造签字）；前端卡片显示「业务签字由 EXC-501 特批（孙出差代办）+ 链接」；4. 孙回来后 3 天内必须补签（系统 watchdog 提醒）；若孙回来后明确拒绝认可 → 立即暂停后续阶段，回退 pp_sample_approved | QC 容器能代签业务签字 → 双签的职责分离（生产/业务双方独立确认）完全崩溃（QC 一人就能签两方）；EXC 把 signedByBusiness 填孙 ID → 伪造业务签字，孙回来如果不同意，没证据证明"他本人没签过"（审计必须明确是 EXC 特批，不是本人签字） |
| QCG-EXC-3 | 终期 AQL 门禁（③ seq=10）终期验货 critical=0 / minor=2.8%（阈值=3%，卡在临界线边差 0.2%，但客户亲自来厂看过并口头同意，且客户 SGS 第三方验货 2 天后到，客户说先出货 SGS 报告后补附，业务员不想等 SGS 延误 14 天空运）→ QC Fill 有专业意见权，无审批权 | 1. InspectionReport（type=Final）result=Fail，defectRate=2.8%，minor=14，threshold=3%；2. advanceStage(qc_shipped) → INSPECTION_NOT_QUALIFIED；3. 客户现场确认 + 客户 SGS 排期 2 天后到 + SGS 保函《若验货不合格客户承担返空费》；4. QC Q 填 EXC 专业意见 | 1. QC Q 登录：填写 qcProfessionalOpinion=「终期验货 minor 14 件（2.8%）为线头/折痕类，无 major/critical，距离阈值 3% 仅差 0.2%；客户现场验过同批次 50 件抽箱均接受；建议放行，后续 SGS 不合格可追溯返工」（仅 QC 能写此字段，业务角色写该字段会被拒绝）；2. 业务员孙提交 EXC：exceptionCategory=qc_fault；subCategory=qc_final_minor_below_threshold_customer_accept；exceptionReason=「终期 minor=2.8% 临界线，客户现场接受 + SGS 保函（附），ETD=4 天，等 SGS 延误 14 天空运费 12W 美元（≥30字）」；qcProfessionalOpinion=QC Q 已填内容（从 EXC.payload 快照，**不是把 QC 意见写回 InspectionReport**）；3. 张主管 Approve；4. Shipment.release 绑定 EXC | 1. QC Q Approve EXC（尝试审批）→ 403 SCOPE_DENIED DR013_QC_CANNOT_APPROVE；2. EXC 批准后：① InspectionReport.result=Fail，defectRate=2.8%（**不被改**，qcProfessionalOpinion 只存在 EXC.payload）；② 全局 QC defectRate 阈值仍=3%（不临时改 3.5%）；③ Shipment.releaseReady=true，徽标「终期 QC 临界瑕疵特批放行（EXC-502）- QC 专业意见：线头折痕 minor=2.8%」+ 链接 EXC；④ 后续该客户下一批订单 QC Final 仍严格按 3%（本次 EXC 不放宽该客户全局 QC 标准）；⑤ 财务暂扣 3% 尾款 45 天（客户收货后 SGS 合格释放） | QC 可以直接 Approve EXC → DR-029 彻底崩塌（QC 自己验 Fail，自己 EXC Approve Pass，完全无人监督）；InspectionReport.result 被静默改 Pass → QC 责任链彻底失真；阈值临时改成 3.5% 全局 → 所有其他订单突然全 Pass（严重 fail-closed：EXC 绝对不得修改 QC 数据和全局阈值） |

---

## §15 相关文档索引（交叉链接）

| 文档 | 链接 | 关联要点 |
|------|------|---------|
| 业务规则总览 | [业务规则总览.md](./业务规则总览.md) | §6 条款 §9.3 ①-③ 三条门禁状态映射 |
| 交期与生产规则 | [交期与生产规则.md](./交期与生产规则.md) | §10 业务十条 / 生产管线 10 阶段 / 交期延期通知 / 验厂 7 天提醒 |
| 10 条事件联动与事件总线 | [10条事件联动（L1-L10）与事件总线.md](./10条事件联动（L1-L10）与事件总线.md) | ProductionStageAdvanced / ProductionCompleted 事件（qc_shipped 通过后发布）|
| 信用控制规则 | [信用控制规则.md](./信用控制规则.md) | 验货 fail 导致返工 vs 客户付款账期区分 |
| 订单变更规则 | [订单变更规则.md](./订单变更规则.md) | 终期 fail→复验→交期变更→触发变更审批链 |
| 数据模型总览 | [02-数据模型/实体关系总览.md](../02-数据模型/实体关系总览.md) | PreCutChecklist / ProductionStage / InspectionReport / QCLocation / QCAssignment 模型组 |
| 订单详情页 | [04-模块设计/订单管理/订单详情页.md](../04-模块设计/订单管理/订单详情页.md) | 生产管线泳道 UI（签字按钮 / 阶段 badge / PreCutChecklist 四宫格 / 终期 AQL 结果卡片）|
| 模块设计-QC 质检中心 | [04-模块设计/03-订单与生产/QcWorkbench-QC质检中心/*](../04-模块设计/03-订单与生产/QcWorkbench-QC质检中心/) | 中期/终期验货表单 / 疵点趋势 / QC 工作台与驻地任务 |
