# 交期与生产规则

> **PRD 关联**：§10 业务十条 L1-L10（交期锁死 / 7 天计划死线 / 15 天延期通知 / 验厂 7 天提醒 / 生产 10 阶段门禁）
> **落地状态**：⚠️ 字段与调度 watchdog 已到位；交期锁死门禁未接、7 天计划死线仅预警无硬门禁
> **DR-011 关联**：生产放行是独立步骤，不因商业批准自动完成，须 QC 通过 + 客户确认 + 产前样封存 + 主管手动确认
> **DR-012 关联**：面料开发样阶段独立 S/S 阶段；S/S 后默认追加面料最终 QC，可与 S/S/RC 并行但不可省略
> **DR-013 关联**：打样收费允许不收款但不得未记录；收款可后补；样品收款为收入，运杂费为费用，独立记账
> **DR-014 关联**：产前样/销售样/开发样支持替代物料；不得强制先行采购或占用大货采购单

---

## §1 元信息与实现状态

| 维度 | 内容 |
|------|------|
| 定位 | 订单交期全生命周期门禁：下单→生产计划→生产执行→验货发货→交期锁死，覆盖 PRD §10 业务十条 L1-L10。DR-011：生产放行独立步骤（QC + 客户确认 + 产前样封存 + 主管手动确认）；DR-012：面料开发样独立 S/S 阶段 + 面料最终 QC；DR-013：打样收费独立记账；DR-014：产前样独立物料来源 |
| PRD 关联 | §10 业务十条 L1-L10 |
| 代码关联 | `server/src/production/stageService.ts`（10 阶段定义 + 3 道硬门禁）/ `server/src/scheduler/schedulerService.ts`（调度框架）/ `server/src/scheduler/tasks/productionDeadlineWatchdog.ts`（计划超期 + 延期通知窗口）/ `server/src/scheduler/tasks/factoryVisitWatchdog.ts`（验厂提醒）/ `server/prisma/schema.prisma`（Order 交期字段 + ProductionStage + PreCutChecklist + InspectionReport 模型） |
| 角色 | 跟单（交期录入/PreCut 勾选）/ 生产部（产前样签字/排产）/ 业务部（产前样签字/终期批准发货）/ QC（验货报告）/ 管理层（交期延期监督） |
| 范式 | 规则型：触发条件 × 动作 × 门禁 × 异常分支 矩阵 |
| 优先级 | P0（交期违约直接触发客诉/空运/扣款/索赔） |
| **整体落地状态** | **⚠️ 字段全有 / 调度 watchdog 全有 / 3 道生产硬门禁已接 / 交期锁死未接 / 7 天计划死线仅预警无硬门禁** |

---

## §2 规则总览：L1-L10 十条规则 × 落地状态矩阵

| 条款 | 规则名称 | 规则内容摘要 | 落地状态 | 实现位置 / 缺口 |
|------|---------|-------------|----------|----------------|
| L1 | 交期锁死 | Order 进入 Shipping / Delivered 状态后，dueDate / clientDate / deliveryDate 等交期字段只读，禁止编辑 | ❌ 未接 | 缺口：`orderLifecycleService.ts` transitionOrderStatus 未对字段修改设置状态拦截；Order update 路由未按状态区分可编辑字段 |
| L2 | 7 天计划死线 | 订单确认后 7 天内必须下达生产计划（productionPlanDeadline = createdAt + 7d），超时触发预警 | ⚠️ 字段有 + watchdog 预警有 / 硬门禁未接 | `schema.prisma:195`（productionPlanDeadline 字段）/ `productionDeadlineWatchdog.ts`（逾期 1-3 天 warning / >3 天 critical）；**缺口**：无硬门禁阻断超时订单的生产推进 |
| L3 | 15 天延期通知窗口 | 交期前 15 天（delayNoticeDeadline = dueDate - 15d）开启延期通知窗口，到期未确认可按期交付则须向客户发延期通知 | ✅ 已接 | `schema.prisma:196`（delayNoticeDeadline 字段）/ `productionDeadlineWatchdog.ts`（窗口到期 → critical 广播通知） |
| L4 | 验厂 7 天提醒 | 客户实地验厂日（factoryVisitDate）前 7 天进入提醒窗口，2-7 天 warning / 0-1 天 critical | ✅ 已接 | `schema.prisma:178`（factoryVisitDate 字段）/ `factoryVisitWatchdog.ts`（每日 11:30 扫描 + riskService.raiseAlert 分级） |
| L5 | 面辅料到厂门禁 | materials_arrived 阶段（seq=5）：面辅料到厂确认，前置面辅料确认阶段完成 | ✅ 已接 | `stageService.ts` PRODUCTION_STAGES[4] + advanceStage 顺序校验（STAGE_NOT_SEQUENTIAL） |
| L6 | 裁剪前 PreCut 4 项 | pre_cut_checked 阶段（seq=6）：PreCutChecklist 4 项全 true（推码/耗料/样板/产前会议），缺任一锁死 | ✅ 已接 | `stageService.ts:270-283`（PRECUT_CHECKLIST_INCOMPLETE 门禁）/ `schema.prisma` PreCutChecklist 模型 |
| L7 | 产前样双签 | pp_sample_approved 阶段（seq=7）：生产部签字 + 业务部签字（双签），缺签锁死 | ⚠️ 后端校验有 / 前端 UI 缺签字按钮 | `stageService.ts:285-292`（PP_SAMPLE_NOT_SIGNED 门禁）+ `signStage()` 函数已实现；**缺口**：生产看板/订单详情页无签字入口 |
| L8 | 生产过程门禁 | manufacturing 阶段（seq=8）：生产过程执行，前置产前样双签完成 | ✅ 已接 | `stageService.ts` PRODUCTION_STAGES[7] + advanceStage 顺序校验 |
| L9 | 成品确认门禁 | final_review 阶段（seq=9）：成品确认，前置生产过程完成 | ✅ 已接 | `stageService.ts` PRODUCTION_STAGES[8] + advanceStage 顺序校验 |
| L10 | 终期验货发货 AQL | qc_shipped 阶段（seq=10）：终期验货 result≠fail + criticalDefects=0 + passRate≥90% + defectRate≤3% + approvedByBusiness，5 道全通过才放行 | ✅ 已接 | `stageService.ts:294-319`（INSPECTION_NOT_QUALIFIED + BUSINESS_APPROVAL_REQUIRED 门禁） |

**落地统计**：✅ 6 条 / ⚠️ 3 条 / ❌ 1 条

---

## §3 逐条规则详解

### 3.1 L1 交期锁死（❌ 未接）

**触发条件**：Order.status 迁移至 `Shipping` 或 `Delivered`

**期望动作**：dueDate / clientDate / exMillDate / OrderLine.deliveryDate / OrderLine.shippingDate 等交期相关字段变为只读，任何修改请求被拒绝

**门禁**：应在 `orderLifecycleService.ts` transitionOrderStatus 完成状态迁移后标记字段锁定；Order update 路由按 status 区分可编辑字段白名单

**异常分支**：交期变更需走订单变更审批链（参见 [订单变更规则.md](./订单变更规则.md) §9.7-③），审批通过后由系统解锁一次性写入

**当前缺口**：
- `transitionOrderStatus` 未对字段修改设置状态拦截
- Order update 路由（`server/src/orders/` 路由层）未按 status 区分可编辑字段，Shipping/Delivered 状态下交期字段仍可直通修改
- 无字段锁定标记位（如 `deliveryLockedAt`）

### 3.2 L2 7 天计划死线（⚠️ 字段有 + 预警有 / 硬门禁未接）

**触发条件**：订单确认后，`productionPlanDeadline`（= createdAt + 7d）已过且生产计划阶段（production_planned, seq=3）未完成

**字段定义**（`schema.prisma:195`）：
```
productionPlanDeadline String? // 下单后7天内需出生产计划（createdAt + 7d），超时触发预警
```

**动作**（`productionDeadlineWatchdog.ts`，每小时扫描）：
- 逾期 1-3 天 → `warning` 级通知（broadcastNotification）
- 逾期 >3 天 → `critical` 级通知
- 去重：dedupKey = `prod:plan:${orderId}:${tier}:${today}`，同级当天只发一次

**门禁（未接）**：期望超时后阻断该订单后续生产阶段推进（如禁止推进至 in_production），当前仅通知无硬拦截

**异常分支**：跟单员可在订单表单手动修改 productionPlanDeadline（无门禁校验）

### 3.3 L3 15 天延期通知窗口（✅ 已接）

**触发条件**：`delayNoticeDeadline`（= dueDate - 15d）已到且订单未交付

**字段定义**（`schema.prisma:196`）：
```
delayNoticeDeadline String? // 交货期前15天需发延期通知（dueDate - 15d），超时升级
```

**动作**（`productionDeadlineWatchdog.ts`）：
- delayNoticeDeadline ≤ today → `critical` 级广播通知
- 通知内容：「延期通知窗口已开启，若预计无法按期交付，须立即向客户发出延期通知」
- 去重：dedupKey = `prod:notice:${orderId}:${today}`

**门禁**：无硬门禁（不阻断生产），仅提醒业务员评估并决定是否发延期通知

**异常分支**：若可按期交付，业务员推进生产即可，无需发延期通知

> **注**：本规则是「交期前 15 天开启延期通知窗口」，非「交期延期超 15 天」。delayNoticeDeadline 在订单创建时由 dueDate 倒推计算。

### 3.4 L4 验厂 7 天提醒（✅ 已接）

**触发条件**：`factoryVisitDate`（客户计划实地验厂日）非空且距今天数 ∈ [0, 7]

**字段定义**（`schema.prisma:178`）：
```
factoryVisitDate String? // YYYY-MM-DD 客户计划实地验厂日
```

**动作**（`factoryVisitWatchdog.ts`，每日 11:30 后执行一次）：
- 距验厂日 2-7 天 → `warning`（确认接待安排）
- 距验厂日 0-1 天 → `critical`（临近升级）
- 验厂日已过 → 不再提醒
- 排除已完结/已出运/已开票收款状态（EXCLUDED_STATUSES）

**门禁**：无硬门禁，通过 `riskService.raiseAlert` 产生风险预警（非 notificationService）

**去重**：dedupKey = `factory_visit:${orderId}:${visitDate}:${tier}`，每个 tier 只发一次

### 3.5 L5-L10 生产 10 阶段门禁（✅ / ⚠️ 详见 §4）

10 阶段由 `stageService.ts` 的 `PRODUCTION_STAGES` 常量定义，`advanceStage()` 函数统一推进。核心门禁逻辑：

- **顺序校验**（全阶段）：前置阶段未 done → `STAGE_NOT_SEQUENTIAL`
- **L6 PreCut 4 项**（seq=6）：4 布尔字段 AND 校验 → `PRECUT_CHECKLIST_INCOMPLETE`
- **L7 产前样双签**（seq=7）：signedByProduction && signedByBusiness → `PP_SAMPLE_NOT_SIGNED`
- **L10 终期 AQL**（seq=10）：5 道门禁（result≠fail / criticalDefects=0 / passRate≥90% / defectRate≤3% / approvedByBusiness）→ `INSPECTION_NOT_QUALIFIED` / `BUSINESS_APPROVAL_REQUIRED`

详细门禁规则参见 [质量门禁规则.md](./质量门禁规则.md)。

---

## §4 10 个生产阶段定义表

> **代码真源**：`server/src/production/stageService.ts` PRODUCTION_STAGES 常量（line 16-27）

| seq | stageKey | 阶段名 | 前置条件 | 门禁规则 | 输出物 | 代码字段 |
|-----|----------|--------|---------|----------|--------|---------|
| 1 | order_placed | 业务下单 | 订单创建 | 无（初始化时自动 done） | 订单建档 | ProductionStage.status='done', doneAt=now |
| 2 | materials_confirmed | 面辅料确认 | seq=1 done | 顺序校验 | 面辅料清单确认 | ProductionStage.status |
| 3 | production_planned | 生产计划 | seq=2 done | 顺序校验 + L2 7天死线预警 | 生产计划下达 | ProductionStage.status + Order.productionPlanDeadline |
| 4 | in_production | 货期管理 | seq=3 done | 顺序校验 | 排产确认 | ProductionStage.status |
| 5 | materials_arrived | 面辅料到厂 | seq=4 done | 顺序校验 | 到料验收 | ProductionStage.status |
| 6 | pre_cut_checked | 裁剪前检查 | seq=5 done | **PreCutChecklist 4 项全 true**（推码/耗料/样板/产前会议） | 裁剪许可 | PreCutChecklist.gradingConfirmed/consumptionConfirmed/patternConfirmed/preProductionMeeting |
| 7 | pp_sample_approved | 产前样确认 | seq=6 done | **双签**（signedByProduction && signedByBusiness） | 产前样签字确认 | ProductionStage.signedByProduction/signedByBusiness |
| 8 | manufacturing | 生产过程 | seq=7 done | 顺序校验 | 大货生产完成 | ProductionStage.status |
| 9 | final_review | 成品确认 | seq=8 done | 顺序校验 | 成品入库确认 | ProductionStage.status |
| 10 | qc_shipped | 验货发货 | seq=9 done | **终期 AQL 5 道**（result≠fail / criticalDefects=0 / passRate≥90% / defectRate≤3% / approvedByBusiness） | 验货放行 + 发货 | InspectionReport(final) + approvedByBusiness |

**初始化逻辑**（`initProductionStages`）：订单确认后由 L1 联动（`OrderConfirmed` 事件）自动创建 10 行 ProductionStage，seq=1 自动 done，其余 pending。使用 upsert 幂等。

**推进逻辑**（`advanceStage`）：事务内校验前置顺序 + 门禁规则 → 更新 status='done' + doneAt → 写审计日志 → 事务提交后发布 `ProductionStageAdvanced` 事件；seq=10 完成时额外发布 `ProductionCompleted` 事件。

---

## §5 交期计算公式

```
Order.productionPlanDeadline = Order.createdAt + 7d          （L2 七天计划死线）
Order.delayNoticeDeadline    = Order.dueDate - 15d           （L3 延期通知窗口）
Order.factoryVisitDate       = 手动录入（YYYY-MM-DD）          （L4 验厂提醒基准）
```

**字段层级关系**：

| 字段 | 所属模型 | 含义 | 计算方式 |
|------|---------|------|---------|
| dueDate | Order | 订单交期（业务约定交付日） | 手动录入 |
| clientDate | Order | 出厂交期 Exmill | 手动录入 |
| exMillDate | OrderLine | 行级出厂交期 | 手动录入 |
| deliveryDate | OrderLine | 行级交货日 | 手动录入 |
| shippingDate | OrderLine | 行级发运日 | 手动录入 |
| productionPlanDeadline | Order | 生产计划死线 | createdAt + 7d（下单时计算） |
| delayNoticeDeadline | Order | 延期通知窗口开启日 | dueDate - 15d（下单时计算） |
| factoryVisitDate | Order | 客户实地验厂日 | 手动录入 |

> **注**：dueDate 在 Order 模型，deliveryDate/shippingDate 在 OrderLine 模型（行级）。L1 交期锁死需同时覆盖 Order 与 OrderLine 两层字段。

---

## §6 schedulerService 定时检查规则

> **代码真源**：`server/src/scheduler/schedulerService.ts`（调度框架）+ `server/src/scheduler/tasks/`（21 个任务）+ `server/src/scheduler/index.ts`（注册入口，共 23 个任务）

### 6.1 调度框架

| 维度 | 内容 |
|------|------|
| 主循环间隔 | 30 秒（tickIntervalMs=30000） |
| 任务并发 | 单次 tick 内同一任务不并发（task.running 标记） |
| 失败隔离 | 单任务失败仅日志，不影响其他任务 |
| 启动行为 | 启动后立即执行一次 tick（用于崩溃恢复等启动时任务） |

### 6.2 交期相关定时任务

| 任务 ID | 检查频率 | 触发条件 | 动作 | 通知模板 | 代码文件 |
|---------|---------|----------|------|---------|---------|
| production_deadline_watchdog | 每小时 | productionPlanDeadline < today | 逾期 1-3 天 warning / >3 天 critical | 「订单 {poNumber} 生产计划超期 {N} 天」 | `tasks/productionDeadlineWatchdog.ts` |
| production_deadline_watchdog | 每小时 | delayNoticeDeadline ≤ today | critical（延期通知窗口开启） | 「订单 {poNumber} 延期通知窗口已开启」 | 同上 |
| factory_visit_watchdog | 每日 11:30 | factoryVisitDate 距今 0-7 天 | 2-7 天 warning / 0-1 天 critical | 「客户计划于 {date} 实地验厂（订单 {poNumber}）」 | `tasks/factoryVisitWatchdog.ts` |
| shipment_delay_detector | 每小时 | ETD 已过未回填 ATD / ETA 已过未回填 ATA | 1-3 天 warning / >3 天 critical | 「运单 {number} 离港/到港延误 {N} 天」 | `tasks/shipmentDelayDetector.ts` |

### 6.3 去重规则

所有 watchdog 采用「dedupKey 含 tier + 日期」去重策略：
- 同级别当天只发一次通知
- 级别升级（warning → critical）会重新通知（新 dedupKey）
- 示例：`prod:plan:${orderId}:${tier}:${todayStr}`

---

## §7 API 端点

> **代码真源**：`server/src/production/route.ts`（生产管线路由）+ `server/src/index.ts`（路由注册 line 617-619）

| 方法 | 路径 | 说明 | 门禁 |
|------|------|------|------|
| GET | `/api/v1/production/board` | 在手订单 × 10 阶段泳道看板聚合（PRD 19.8，最多 500 单） | 只读 |
| GET | `/api/v1/production/:orderId` | 获取完整生产管线（stages + checklist + inspection + outsourcing） | 只读 |
| POST | `/api/v1/production/:orderId/advance/:stageKey` | 推进指定阶段（含门禁校验） | RBAC: production_manager/factory/manager/admin/owner |
| PUT | `/api/v1/production/:orderId/checklist` | 保存 PreCutChecklist | 同上 |
| PUT | `/api/v1/production/:orderId/inspection` | 保存 InspectionReport（含 fail 广播） | 同上 |
| GET | `/api/v1/automation/rules` | 列出 L1-L10 联动规则启用状态 | settings:system:read |

> **注**：任务描述中的 `/api/v1/orders/:id/stages` 实际不存在；生产管线端点挂在 `/api/v1/production/:orderId` 下。`/api/v1/scheduler/status` 无独立 REST 端点，调度器在服务启动时由 `startScheduler(prisma)` 内部初始化，状态仅通过日志暴露。

---

## §8 权限

| 权限 | 说明 | 适用角色 |
|------|------|---------|
| production:read | 查看生产管线 / 看板 / 验货报告 | 全部角色（只读） |
| production:write | 推进阶段 / 保存 PreCutChecklist / 保存验货报告 | production_manager / factory / manager / admin / owner |
| orders:approve:change_delivery | 交期变更审批（L1 解锁后一次性写入） | manager / admin / owner（当前未接，参见 [订单变更规则.md](./订单变更规则.md)） |

**RBAC 实现**（`production/route.ts:39-52`）：
- 写操作角色检查：`PRODUCTION_WRITE_ROLES = ['production_manager', 'factory', 'manager', 'admin', 'owner']`
- 无角色默认拒绝（fail-closed）：`roles.length === 0 → 403 FORBIDDEN`
- 开发模式（requireAuth=false）跳过角色检查

---

## §9 触发条件 × 动作 × 门禁 × 异常分支矩阵

### L1 交期锁死

| 维度 | 内容 |
|------|------|
| 触发条件 | Order.status → Shipping / Delivered |
| 期望动作 | dueDate / clientDate / deliveryDate / shippingDate 只读 |
| 门禁 | ❌ 未接（无状态拦截） |
| 异常分支 | 交期变更需走订单变更审批链（L3 老板审批） |

### L2 7 天计划死线

| 维度 | 内容 |
|------|------|
| 触发条件 | productionPlanDeadline（createdAt + 7d）已过 |
| 动作 | watchdog 每小时扫描 → 分级通知（warning / critical） |
| 门禁 | ⚠️ 仅预警无硬门禁（不阻断生产推进） |
| 异常分支 | 跟单员可手动修改 productionPlanDeadline |

### L3 15 天延期通知窗口

| 维度 | 内容 |
|------|------|
| 触发条件 | delayNoticeDeadline（dueDate - 15d）≤ today |
| 动作 | watchdog → critical 广播通知 |
| 门禁 | 无（仅提醒） |
| 异常分支 | 可按期交付则无需发延期通知 |

### L4 验厂 7 天提醒

| 维度 | 内容 |
|------|------|
| 触发条件 | factoryVisitDate 距今 0-7 天 |
| 动作 | watchdog 每日 11:30 → riskService.raiseAlert 分级 |
| 门禁 | 无（仅提醒） |
| 异常分支 | 验厂日已过不再提醒 |

### L6 裁剪前 PreCut 4 项

| 维度 | 内容 |
|------|------|
| 触发条件 | advanceStage(stageKey='pre_cut_checked') |
| 动作 | 校验 PreCutChecklist 4 项全 true |
| 门禁 | ✅ fail-closed：缺任一 → PRECUT_CHECKLIST_INCOMPLETE |
| 异常分支 | 补全缺失项后重新推进 |

### L7 产前样双签

| 维度 | 内容 |
|------|------|
| 触发条件 | advanceStage(stageKey='pp_sample_approved') |
| 动作 | 校验 signedByProduction && signedByBusiness |
| 门禁 | ✅ fail-closed：缺签 → PP_SAMPLE_NOT_SIGNED |
| 异常分支 | 通过 signStage() 补签后重新推进 |

### L10 终期 AQL

| 维度 | 内容 |
|------|------|
| 触发条件 | advanceStage(stageKey='qc_shipped') |
| 动作 | 校验终期验货报告 5 道门禁 |
| 门禁 | ✅ fail-closed：result=fail / criticalDefects>0 / passRate<90% / defectRate>3% / 未批准 → INSPECTION_NOT_QUALIFIED / BUSINESS_APPROVAL_REQUIRED |
| 异常分支 | 整改复验合格 + 业务部批准后重新推进 |

---

## §10 数据模型关联

| 模型 | schema 位置 | 关键字段 | 关联 |
|------|------------|---------|------|
| Order | schema.prisma:137+ | dueDate / clientDate / productionPlanDeadline / delayNoticeDeadline / factoryVisitDate | 1:N ProductionStage |
| ProductionStage | schema.prisma:2430+ | orderId / stageKey / stageSeq / status / signedByProduction / signedByBusiness / doneAt | N:1 Order |
| PreCutChecklist | schema.prisma:2457+ | orderId / gradingConfirmed / consumptionConfirmed / patternConfirmed / preProductionMeeting | 1:1 Order |
| InspectionReport | schema.prisma:2481+ | orderId / inspectionType / result / criticalDefects / majorDefects / minorDefects / approvedByBusiness | 1:1 Order（per type） |
| OutsourcingOrder | schema.prisma | orderId / supplierId / processType / status / qualityAcceptedQty | N:1 Order（外协只读区块） |

---

## §11 事件发布

`advanceStage()` 在事务提交后发布以下事件（fire-and-forget，失败不影响业务）：

| 事件类型 | 触发时机 | payload 关键字段 |
|---------|---------|-----------------|
| ProductionStageAdvanced | 每次阶段完成（seq=1-10） | stageKey / stageLabel / operator / auditId |
| ProductionCompleted | seq=10 qc_shipped 完成 | stageKey / completedAt / auditId |

> 事件发布通过 `publishBusinessEvent()` 便捷函数，持久化到 AgentJob 表（jobType=`bev:${type}`）。详见 [10条事件联动（L1-L10）与事件总线.md](./10条事件联动（L1-L10）与事件总线.md)。

---

## §12 交期变更联动

交期字段变更触发的联动链路（当前部分未接）：

```
Order.dueDate 变更
  ├─ → delayNoticeDeadline 重算（dueDate - 15d）        ⚠️ 未自动重算
  ├─ → 订单变更审批链（§9.7-③ 延期天数分级审批）          ❌ 未接
  ├─ → 通知客户（延期通知）                                ⚠️ 仅 watchdog 提醒，无自动通知客户
  └─ → 生产计划调整评估                                    ❌ 未接
```

详见 [订单变更规则.md](./订单变更规则.md) §9.7-③。

---

## §13 外协生产阶段

`getProductionPipeline()` 返回外协只读区块（`outsourcing` 字段），数据来自 OutsourcingOrder 模型：

| 字段 | 含义 |
|------|------|
| orderNumber | 外协单号 |
| supplierId / supplierName | 加工厂 |
| processType | 工序类型 |
| status | 外协状态 |
| quantity / unit | 数量/单位 |
| plannedDeliveryDate / actualDeliveryDate | 计划/实际交期 |
| qualityAcceptedQty / qualityRejectedQty | 验收合格/不合格数量 |

> **注**：外协区块当前为只读展示，未接入生产阶段推进门禁。外协进度不阻断主生产线阶段推进。

---

## §14A DR-011 生产放行独立审批

> **核心决策**（DR-011）：生产放行是独立步骤，不因商业批准自动完成。四条件同时满足 + 主管手动确认才可开工。

### 14A.1 生产放行四条件

| 条件 | 说明 | 当前状态 |
|------|------|---------|
| 内部 QC 通过 | 内部 QC 对生产准备工作评审通过 | ❌ 未建模 |
| 业务员已登记客户确认 | 业务员登记客户已确认产前样 | ⚠️ 部分实现（PreCutChecklist 门禁 7 产前样双签） |
| 产前样已封存归档 | 产前样实物已封存，归档记录完整 | ❌ 未建模 |
| 主管手动确认 | 销售主管或总领导手动批准生产放行 | ❌ 未建模 |

### 14A.2 生产放行与其他门禁的关系

| 门禁 | 关系 |
|------|------|
| 商业订单批准 | 生产放行前置（DR-009 七步分离链） |
| 产前样双签（PreCutChecklist 门禁 7） | 生产放行的子集条件（QC + 客户确认），但不等于生产放行（缺封存归档 + 主管确认） |
| 面料实际到位 | 不阻断生产放行（封样不以面料到位为前提） |

---

## §14B DR-012 面料开发样与 S/S 分离

> **核心决策**（DR-012）：面料的开发样阶段独立于 S/S 阶段。S/S 之后默认追加一次面料最终 QC；面料 QC 可与 S/S/RC 并行，但不可省略。

### 14B.1 面料样品链

```
开发样 → 早期生产样 → S/S 船样 → 面料最终 QC → 可选 RC 匹头样
```

| 阶段 | 说明 | QC 要求 |
|------|------|---------|
| 开发样 | 面料开发阶段样品 | 独立阶段，不属于 S/S |
| 早期生产样 | 大货生产早期样品 | 独立阶段 |
| S/S 船样 | 船样确认 | S/S 后默认追加面料最终 QC |
| 面料最终 QC | S/S 后必检 | 可与 S/S/RC 并行，但不可省略 |
| RC 匹头样 | 长期翻单 + Separates 类可选 | 独立阶段 |

### 14B.2 当前状态

| 项 | 状态 |
|---|------|
| 面料开发样独立阶段 | ❌ 未建模（当前 DevelopmentCase.type=fabric 无独立面料样品链） |
| S/S 后默认追加面料最终 QC | ❌ 未建模（当前 FabricShipmentSample 无最终 QC 字段） |
| 面料 QC 与 S/S/RC 并行 | ❌ 未建模 |

---

## §14C DR-013 打样收费独立记账

> **核心决策**（DR-013）：打样收费允许不收款但不得未记录。客户明确以打样费换取未来订单时，必须建立收费义务；收款可后补。

### 14C.1 记账规则

| 项 | 规则 |
|---|------|
| 样品收款 | 作为收入 |
| 样品运杂费 | 作为费用 |
| 独立记账 | 样品收款与运杂费独立记账，不得并入订单收入混算 |
| 收费义务 | 客户明确以打样费换取未来订单时，必须建立收费义务记录 |
| 收款后补 | 允许先发货后收款，但收费义务必须先行记录 |

### 14C.2 当前状态

| 项 | 状态 |
|---|------|
| 打样收费记录 | ❌ 未建模（当前无打样收费实体） |
| 收费义务 | ❌ 未建模 |
| 独立记账（收入 vs 费用） | ❌ 未建模 |

---

## §14D DR-014 产前样独立物料来源

> **核心决策**（DR-014）：产前样、销售样、开发样均支持替代物料。不得强制要求这些样品先行采购或占用大货采购单。

### 14D.1 物料来源规则

| 样品类型 | 物料来源 | 规则 |
|---------|---------|------|
| 产前样 | 替代物料（可用库存面料/替代面料） | 不得强制先行采购或占用大货采购单 |
| 销售样 | 替代物料 | 同上 |
| 开发样 | 替代物料 | 同上 |

### 14D.2 关键原则

| 原则 | 说明 |
|------|------|
| 不强制采购 | 样品可使用替代物料，不强制先行采购 |
| 不占大货采购单 | 样品物料不得占用大货采购单数量 |
| 独立记录 | 样品物料来源须独立记录（替代物料/库存/新采购） |

### 14D.3 当前状态

| 项 | 状态 |
|---|------|
| 样品物料来源字段 | ❌ 未建模 |
| 替代物料支持 | ❌ 未建模 |
| 不占大货采购单校验 | ❌ 未建模 |

---

## §14 待补缺口

| 缺口编号 | 缺口描述 | 影响范围 | 修复建议 |
|---------|---------|---------|---------|
| GAP-DLV-1 | **L1 交期锁死未接**：Shipping/Delivered 状态后 dueDate/clientDate/deliveryDate/shippingDate 仍可编辑 | 交期变更无门禁，已出运订单交期可被随意修改，破坏履约数据完整性 | 在 Order update 路由按 status 设置可编辑字段白名单；transitionOrderStatus 迁移至 Shipping/Delivered 时写入 deliveryLockedAt 标记 |
| GAP-DLV-2 | **L2 7 天计划死线硬门禁未接**：productionPlanDeadline 超时仅预警，不阻断生产推进 | 超期订单仍可推进至 in_production，死线形同虚设 | advanceStage 校验 production_planned 阶段 doneAt ≤ productionPlanDeadline + 宽限期；或超时后要求管理层解锁 |
| GAP-DLV-3 | **delayNoticeDeadline 未自动重算**：dueDate 变更后 delayNoticeDeadline 不自动更新 | 交期变更后延期通知窗口基准失效 | Order update 时若 dueDate 变更则自动重算 delayNoticeDeadline = dueDate - 15d |
| GAP-DLV-4 | **外协 OutsourcingOrder 阶段未接入 UI 门禁**：外协进度仅只读展示，不阻断主生产线 | 外协未完成时不影响主生产线推进，可能导致成品缺件 | 在 manufacturing / final_review 阶段增加外协完成率校验 |
| GAP-DLV-5 | **产前样双签前端 UI 缺失**：signStage() 后端函数已实现，但生产看板/订单详情页无签字按钮 | 双签门禁后端有但用户无法从前端触发签字 | 在生产管线泳道 UI 增加「生产部签字 / 业务部签字」按钮，调用 signStage API |
| GAP-DLV-6 | **DR-011 生产放行独立审批未实现**：无生产放行实体/字段/审批流，产前样双签 ≠ 生产放行 | 商业批准后可直接推进生产，缺少独立生产放行门禁 | 新增 ProductionRelease 实体（orderId / qcPassed / clientConfirmed / sampleSealed / approvedBy / approvedAt）+ 审批流 |
| GAP-DLV-7 | **DR-012 面料开发样独立阶段未建模**：DevelopmentCase.type=fabric 无独立面料样品链（开发样→早期生产样→S/S→最终QC→RC） | 面料样品流程与成衣混用，无法支持面料独立 QC 链 | DevelopmentCase 新增 fabricSampleStage 字段或独立 FabricSampleChain 实体 |
| GAP-DLV-8 | **DR-012 S/S 后默认追加面料最终 QC 未建模**：FabricShipmentSample 无最终 QC 字段 | S/S 确认后无法强制追加面料最终 QC | FabricShipmentSample 新增 finalQcRequired / finalQcResult / finalQcDate 字段 |
| GAP-DLV-9 | **DR-013 打样收费记录未建模**：无打样收费实体/收费义务/独立记账 | 打样收费无法追踪，收入/费用混算 | 新增 SampleCharge 实体（developmentCaseId / chargeType / amount / currency / paidAt / paymentVoucherId） |
| GAP-DLV-10 | **DR-014 样品物料来源未建模**：无样品物料来源字段/替代物料支持/不占大货采购单校验 | 样品物料与大货采购混用，无法支持替代物料 | SampleNode / FabricShipmentSample 新增 materialSource（stock/alternative/new_purchase）字段 + 校验逻辑 |

---

## §15 相关文档索引（交叉链接）

| 文档 | 链接 | 关联要点 |
|------|------|---------|
| 评审决策记录 | [10-评审与决策/2026-08-16-设计评审决策记录.md](../10-评审与决策/2026-08-16-设计评审决策记录.md) | DR-011：生产放行独立；DR-012：面料开发样与 S/S 分离；DR-013：打样收费独立记账；DR-014：产前样独立物料来源 |
| 业务规则总览 | [业务规则总览.md](./业务规则总览.md) | §6 条款 §10-A / §10-B / L1-L10 状态映射 |
| 10 条事件联动与事件总线 | [10条事件联动（L1-L10）与事件总线.md](./10条事件联动（L1-L10）与事件总线.md) | L1 联动 OrderConfirmed → initProductionStages / ProductionStageAdvanced / ProductionCompleted 事件 |
| 订单状态机 | [../04-模块设计/订单管理/订单详情页.md](../04-模块设计/订单管理/订单详情页.md) | Order 状态流转 Shipping/Delivered → 交期锁死触发点 |
| 质量门禁规则 | [质量门禁规则.md](./质量门禁规则.md) | L6 PreCut 4 项 / L7 产前样双签 / L10 终期 AQL 三道门禁详解 |
| 订单变更规则 | [订单变更规则.md](./订单变更规则.md) | §9.7-③ 交期变更 → 延期天数分级审批 + 通知客户联动 |
| 数据模型总览 | [../02-数据模型/实体关系总览.md](../02-数据模型/实体关系总览.md) | Order / ProductionStage / PreCutChecklist / InspectionReport / OutsourcingOrder 模型组 |
| 00-索引 | [../00-索引.md](../00-索引.md) | §03 业务规则 8 份文档清单 |
