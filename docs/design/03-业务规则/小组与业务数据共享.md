# 小组与业务数据共享（DR-042）

> **决策提议编号**：DR-042（自定义小组 + 业务数据受控共享）
> **关联决策**：DR-041（7+1 宽泛容器角色）/ PL-2B（同部门互相可见，跨部门隔离）/ DR-007（审批人按部门解析）
> **落地状态**：🟡 后端 Phase 1+2 核心已落地（TeamDataGrant 模型 + teamShareService 授权引擎 + relationServiceV2 读/写 scope 分离 + relations/crm 路由接线 + 27 项单测）；AdminPanel 小组管理 Tab 已实现（v2.1 裁决迁入 HRManager，待迁移）；v2.2 三层视野（档案图书馆化 / 业务按跟进客户锚定 / 团队协作互见）设计定稿，待 Phase 2.6 实施
> **核心意图**（项目决策登记原文）：「角色权限通过 departmentId 实现分组隔离，**支持按品牌客户、市场或业务类型自定义小组**」
> **版本**：v2.2（2026-08-19 三层视野评审，修订记录见 §0）

---

## §0 修订记录

### v2.2 修订（2026-08-19 三层视野评审，用户确认业务模型）

| # | v2.1 状态 | v2.2 修订 | 动因 |
|---|----------|----------|------|
| 1 | 档案读视野 = 本人维 ∪ 小组维 | **档案图书馆化（L1）**：Relation 档案（normal）全公司可查——档案本意是 library/字典（查谁是 H&M、找谁对接）；新增 `sensitivity` 字段，confidential 档案仅本人维 + 管理角色可见；敏感**字段**走既有 stripSensitive 遮罩 | 用户业务模型确认：档案是全公司共享的查阅载体，不是按账户隔离的私有数据。v2.1 的「提成归属敏感性」担忧改由敏感标记精确解决，而非一刀切隔离（一刀切还会引发重复建档——同事不知道客户已存在） |
| 2 | 订单等子实体按**订单创建者**锚定（orderServiceV2 `ownerId=me`）；客户转让后历史订单视野错位；前端读路径仍走 V1 无行级过滤 | **业务层换锚（L2）**：订单/报价/跟进等业务记录可见性锚 = **宿主客户的跟进人**（`relation.ownerId ∨ salesRepIds`）∪ 团队共享 ∪ 管理角色——「我跟进哪些客户，我的订单履约体系就针对哪些客户」 | 用户业务模型确认：真正要隔离的是业务操作过程，锚点是跟进的客户而非记录创建者；同时补齐 §5.3 派生可见（v2.1 只设计未实现：组员看得到客户看不到订单的畸形态） |
| 3 | TeamDataGrant 语义 = 档案共享 | **团队语义升级（L3）**：档案全公司可见后，组共享价值升格为**业务子树互见 + 协作跟进**（组员互相看见彼此客户的订单/报价/跟进 + 组员可写跟进）；confidential 档案禁止组共享（防绕过敏感标记） | 用户业务模型确认：共享团队 = 相互看见所有信息 + 可以协作跟进 |
| 4 | DataScopeRule 不分读写（relations 开 all 则读写全开） | **读写分离扩展**：`{ kind: 'all', write?: 'self' }`——sales 的 relations 条目改为「读 all + 写 self」 | 图书馆化必须读开写收，否则业务员可改全公司任何档案 |

**三层视野模型**（v2.2 起为全系统数据可见性总纲）：

| 层 | 内容 | 读视野 | 写权限 |
|----|------|--------|--------|
| L1 档案层（图书馆） | 客户/供应商档案、知识库、数字档案 | 全公司可查（normal）；confidential 仅本人维 + 管理角色；敏感字段按 SensitiveFieldScope 遮罩 | 负责人 + 管理角色（写 scope 恒为本人维，不随读放开） |
| L2 业务层（工作台） | 订单、报价、跟进、履约链路 | 按「我跟进的客户」锚定 ∪ 团队客户 ∪ 管理角色全量 | 各业务域自身规则（订单：创建者 ∪ 宿主客户跟进人） |
| L3 团队层（协作组） | 团队客户名下整套业务子树 | 组内互相全见 | 协作跟进可写（read+followup 档），档案本体写仍归负责人 |

### v2.1 修订（2026-08-19 组织权责归位评审，用户确认业务模型）

| # | v2 状态 | v2.1 修订 | 动因 |
|---|---------|----------|------|
| 1 | 读视野 = PL-2B 部门维 ∪ 小组维 | **组为主**：读视野 = 本人维 ∪ 小组维；**部门退出数据权限计算**（保留目录展示 + DR-007 审批链参考） | 用户业务模型确认：部门仅目录（组织条理），数据视野由「角色功能面 + 所加入小组并集」驱动；销售主管的管理半径也是组驱动（所加入的所有组），非部门子树；外贸客户档案有提成归属敏感性，同部门互见是过度授权 |
| 2 | 小组管理 UI 在 AdminPanel | **入口归位 HRManager（人事管理）**：部门/小组/团队/项目/任务等组织人事概念统一归人事域；AdminPanel 收敛为软件层治理（账号/角色/工具权限/平台规则/公司主体/知识库 ACL） | 权责判断标准：AdminPanel 管"平台怎么运转"（换家公司要重配），HRManager 管"本公司怎么组织"（组织形态描述）；后端 API 本就挂 `/api/hr/teams*`（hr 域），UI 迁移即前后端语义对齐，后端零改动 |
| 3 | HR 编制团队与 DR-042 业务组双概念共用 Team 模型 | **单概念**：Team = 业务协作组（唯一概念）；项目/任务挂靠小组；「编制团队」概念消除 | 双概念串扰：Admin 建的跨部门组出现在 HR 编制树孤儿区、HR 建的编制团队混入业务组列表可被授权共享 |
| 4 | Dashboard 团队切换器未定位 | **视角聚焦器**（§8.6）：权限层永远融合（多组并集，无切换概念），展示层可选聚焦（默认"全部"融合概况，点选某组看该组经营切片） | 用户确认"融合为主，不强切换"；切换器本意是经营视角巡检（老板逐组巡检/主管看各组健康度），非权限切换 |

**P0 数据一致性修复**（随 v2.1 实施）：HR 侧 `DELETE /teams/:id` 改走 dissolve 事务逻辑（回收全部授权 + 审计 + 软删），消除「从 HR 界面删组留下僵尸授权行」的分叉（读路径 fail-closed 无越权，但审计链断裂）。

### v2 修订（2026-08-19 代码级评审）

v1 设计经代码级评审（对照 `relationServiceV2.ts` 行级权限实现）发现 4 个设计缺陷，本版修复：

| # | v1 缺陷 | 后果 | v2 修复 |
|---|---------|------|---------|
| 1 | 团队归属缓存进 JWT claims（7 天 cookie） | 退组/解散后最长 7 天仍可见共享数据，违反即时失效（T-03） | **每请求解析**：按 `TeamMember.userId` 索引查询（毫秒级），可选 60s 进程内缓存（§5.2） |
| 2 | `TeamDataGrant.team` 外键 `onDelete: Cascade` | 物理删组会连带物理删授权行，审计链断裂 | 解散走**事务化软删**（先批量 revoke 再软删组）；AuditLog 为审计最终真源（§9.2） |
| 3 | entityType 起步三值（relation/order/quotation） | 订单可见性当前**无行级过滤**（Order 模型无 departmentId），直接共享订单语义悬空；且「看得到订单看不到客户」是畸形可见性 | 起步**只共享 Relation（客户/供应商档案）**，订单/报价/跟进作为子实体**派生可见**（§5.3） |
| 4 | 授权资格写成「本部门归属的数据」 | 归属判定口径不唯一（departmentId 可能缺） | 收敛为不变量：**对实体有行级写权限者才可共享该实体**（复用既有写权限校验，§6.1） |

---

## §1 元信息与实现状态

| 维度 | 内容 |
|------|------|
| 定位 | 业务分组单元：按品牌客户 / 市场 / 业务类型自由组建的跨部门小组，实现组内业务数据受控共享 |
| 与部门的关系 | **正交，不是一回事**（§2）；部门管目录展示与审批链（v2.1 起退出数据视野计算），小组是数据视野的唯一协作维度 |
| 触发入口 | HRManager 人事管理·小组管理（建组/拉人/授权/编辑/解散，v2.1 从 AdminPanel 归位迁入）/ Relation 详情页（就地共享） |
| 角色 | 销售主管/管理员（建组）/ 组长（组管理+授权）/ 业务员（组内只读+跟进可写） |
| 范式 | 规则型：§5 可见性合成 + §6 权限矩阵 |
| 优先级 | P1（部门审批链已闭环后的小组共享能力；不阻塞当前交付主线） |
| 现有模型真源 | schema.prisma:1198-1231（Team / TeamMember）/ 底座域模型组.md §12 §13 |
| 现有接口 | server/src/hr/route.ts:192-338（teams CRUD + 成员进出，已存在未接线） |
| 行级权限消费点 | `relationServiceV2.ts:112-132`（buildScopeWhere：OR 组合 ownerId/salesRepIds/departmentId）——小组维在此扩展 |
| ❌ 缺口 1 | 业务实体与 Team 零关联，组建了也没有数据能挂上去 |
| ❌ 缺口 2 | `DataScopeRule` 预留 `{ kind: 'team' }`（rolePermissionMatrix.ts:307）无消费方 |
| ❌ 缺口 3 | 无小组管理 UI（AdminPanel 无 Tab） |

---

## §2 概念定位：部门 vs 小组

| 维度 | 部门 Department | 小组 Team |
|------|---------------|-----------|
| 本质 | 行政组织架构（**目录**） | 业务分组单元（**数据视野驱动**） |
| 组建逻辑 | 稳定汇报线（业务部/质量部/财务部/后勤部/总经办） | 按品牌客户/市场/业务类型自由组建（如「波兰成衣组」「日本面料组」） |
| 成员规则 | 单一归属（`UserAccount.primaryDeptId` 一对一） | 多对多（一人可同时进 N 个组，TeamMember） |
| 跨部门 | 否，树形封闭 | 是，`Team.departmentId` 可空 = 天然跨部门（业务员+主管+老板+后勤混编同组） |
| 消费场景 | 组织架构树目录展示 / DR-007 审批人解析 / 汇报线（**v2.1 起退出数据视野计算**） | **数据视野唯一协作维度（L3）**：组内互见共享客户的业务子树（订单/报价/跟进）+ 协作跟进 + 项目任务挂靠（v2.2：档案本体已图书馆化全公司可查，组的价值聚焦业务协作） |
| 生命周期 | 长期稳定（低频变更） | 高频（项目启动建组、结束解散） |
| 维护者 | 管理员（人事管理） | 销售主管/管理员（随业务调整，人事管理入口） |

**铁律**：小组**不参与**审批人解析（那是 DR-007 部门 `headId` 的职责）；小组**只影响数据可见性**。两条链路正交解耦，避免「一条数据到底跟部门走还是跟组走」的语义混乱。

**多组叠加的心理模型**：用户进 N 个组 ≠ 界面分成 N 块。类比微信：进了 10 个群，消息列表仍是同一条信息流，靠头像区分来源。UI 呈现见 §8。

---

## §3 建模取舍

### 3.1 方案 A：业务实体直接归属（挂 teamId）

在 Relation 上直接加 `teamId String?`。

| 优点 | 缺点 |
|------|------|
| 模型简单，查询快（单字段过滤） | **单值归属**：H&M 无法同时属于「欧洲市场组」+「H&M 品牌组」 |
| 无新增表 | 组解散时数据要迁移（脏活） |
| | 与部门归属并存，组织归属出现两个真源 |

### 3.2 方案 B：共享授权表（TeamDataGrant）✅ 采用

数据本体不动（归属仍是本人维链路），新增授权表表达「这个实体的业务子树共享给哪个组协作」——**云文档分享模型**：文件不动、还在原主人手里，只把查看权限发给若干群（v2.2：档案本体已全公司可查，授权承载的是业务子树互见 + 协作跟进权）。

| 优点 | 缺点 |
|------|------|
| 多对多：一个客户可同时授权多个组 | 多一张表，可见性查询 OR 子条件 |
| 数据本体不动：DR-007 审批链零污染 | 授权操作比勾字段多一步 |
| 组解散 = 撤销授权，业务视野无感回归本人维范围 | |
| 授权自带审计链（谁/何时/撤销/理由） | |

### 3.3 方案 C：起步收敛——只共享 Relation，子实体派生（v2 新增，与 B 合并采用）

v1 允许分别共享 relation/order/quotation。评审否决：

1. **业务心智**：用户说「把 H&M 共享给欧洲组」，不会说「共享 H&M 档案但不共享其订单」——客户的业务上下文天然一体，拆开共享是畸形可见性（v2.2 注：档案本体已全公司可查，「共享」的心智对象就是业务子树）；
2. **现状依赖**：Order 模型无行级 scope 字段，订单维度授权无消费点；
3. **模型经济**：一张授权行解锁整棵子树，授权管理成本低一个量级。

**最终形态**：`entityType` 字段保留（为未来独立共享单张报价单等场景预留），但 Phase 2 仅消费 `'relation'`。子实体可见性由 §5.3 派生规则承接。

**明确不做**（防过度设计）：
- 不做组的自动规则归集（rule-based grant，如「欧洲客户自动进欧洲组」）——Phase 4 演进预留，首期手动授权；
- 不做组内层级（组长→副组长）；只保留 `Team.leaderId` 单一组长 + `TeamMember.role`（member/leader）既有字段；
- 不做跨租户/对外共享（供应商门户等）——纯内部协作场景。

---

## §4 数据模型设计

### 4.1 新增：TeamDataGrant（Prisma）

```prisma
/// DR-042 小组数据共享授权：业务实体 × 小组 多对多受控共享。
/// 铁律：数据本体组织归属不变（部门链路），本表仅表达「共享视图」。
model TeamDataGrant {
  id           String    @id @default(cuid())
  teamId       String
  entityType   String    // 起步仅 'relation'（§3.3）；字段保留扩展位
  entityId     String    // 被共享实体 id（快照引用，不强 FK——与 Order.customerRelationId 同范式）
  permission   String    @default("read+followup") // 'read'（纯只读）| 'read+followup'（可加跟进）
  grantedBy    String    // 授权人 UserAccount.id
  grantedAt    DateTime  @default(now())
  revokedAt    DateTime? // 软撤销（null=生效中）；保留行以留授权历史
  revokedBy    String?
  revokeReason String?

  team Team @relation(fields: [teamId], references: [id]) // 不设 Cascade（§0 修订 2）：物理删组由服务层禁止，解散走事务化软删

  @@unique([teamId, entityType, entityId]) // 幂等锚点：同实体对同组仅一行
  @@index([entityType, entityId])          // 反查：这条数据共享给了哪些组
  @@index([teamId, revokedAt])             // 正查：这个组生效中的授权
}
```

设计要点：
- **软撤销不物理删**：`revokedAt` 置位即失效；再授权 = 复活原行（revokedAt 置 null），`@@unique` 天然幂等，完整历史保留在行上 + AuditLog 双轨；
- **entityId 不强 FK**：与 `Order.customerRelationId` 快照引用同范式（Relation 可独立归档，不级联）；
- **permission 只有两档**：`read` / `read+followup`。**没有 write 档**——档案本体永远只有跟进人/本人维可写（§6.2），规避多人共改一条档案；
- **Team 关系不设 onDelete: Cascade**：v1 的 Cascade 是审计链断裂隐患（物理删组连带物理删授权行）。解散流程由服务层事务化处理（§9.2），物理删组被服务层禁止。

### 4.2 复用既有（零改动）

- `Team`（schema.prisma:1198）：`leaderId` 即组长；`departmentId` 可空保持跨部门语义；`deletedAt` 软删承载解散；
- `TeamMember`（schema.prisma:1219）：`role`（member/leader）+ `leftAt` 退出标记；
- `DataScopeRule`（rolePermissionMatrix.ts:304）：v2.2 扩展读写分离 `{ kind: 'all', write?: 'self' }`（§4.4）；`{ kind: 'team' }` 分支继续预留（v2.2 的 L2 锚定在服务层解析，不走 scope kind），双文件同步铁律照旧。

### 4.3 AuditLog 动作扩展

| action | payload | 触发 |
|--------|---------|------|
| `team_create` / `team_dissolve` | teamId, name, operatorId | 建组/解散 |
| `team_member_join` / `team_member_leave` | teamId, userId, operatorId | 拉人/退组（含被移除） |
| `team_grant_create` | teamId, entityType, entityId, permission, grantedBy | 授权/复活授权/改档 |
| `team_grant_revoke` | teamId, entityType, entityId, revokedBy, reason | 撤销授权 |

AuditLog 是审计**最终真源**（grant 行仅承载当前状态 + 可变历史），即使 grant 行异常丢失，审计链完整。

### 4.4 v2.2 新增：Relation 敏感标记 + DataScopeRule 读写分离

**Relation.sensitivity（schema.prisma 新增字段）**：

```prisma
/// v2.2 L1 档案层敏感标记：normal=图书馆全公司可查；confidential=仅本人维+管理角色可见。
sensitivity String @default("normal") // 'normal' | 'confidential'
```

- 由档案写权限者（负责人 / 管理角色）设置，变更写 AuditLog（action: `relation_sensitivity_change`，含新旧值）；
- confidential 档案**禁止组共享**：授权资格校验新增一条——`sensitivity='confidential'` 的实体返回 409 `SENSITIVE_ENTITY_NOT_SHAREABLE`（防用组共享绕过敏感标记）。确需协作的场景走 `salesRepIds` 加跟进人（跟进人对档案有写侧身份，语义更强、审计更完整）；
- 敏感**字段**不靠 sensitivity 承载，走既有 `stripSensitive` + `SensitiveFieldScope` 机制（permissionService.ts:318，按 scope 遮罩信用额度/成本/利润类字段，无 scope 者看 `****`）——记录级与字段级两级独立生效，覆盖不同粒度的敏感诉求。

**DataScopeRule 读写分离（rolePermissionMatrix.ts）**：

```typescript
export type DataScopeRule =
  | { kind: 'all'; write?: 'all' | 'department' | 'self' }  // v2.2：读全量时写可独立收窄（缺省 write = 'all'）
  | { kind: 'department'; own: boolean; includeDescendantDepartments?: boolean }
  | { kind: 'team' }
  | { kind: 'self' };
```

- sales 条目改：`relations: { kind: 'all', write: 'self' }`——读图书馆、写本人维；
- 消费端：读侧 `buildScopeWhere` 照旧取 `rule.kind`；写侧 `buildWriteScopeWhere` 改为解析 `rule.write ?? rule.kind` 再走原 where 构造（all→不过滤 / self→本人维）；
- **双文件逐字同步铁律照旧**（`server/src/_shared/rolePermissionMatrix.ts` 与根 `lib/rolePermissionMatrix.ts`）；
- v2.1 的 department→self 收敛逻辑（permissionService.ts:281）不受影响——只作用于 department kind，relations 已改为 all + write 覆盖。

---

## §5 可见性合成规则

### 5.1 合成公式（v2.2 修订：三层视野——档案图书馆化 + 业务按跟进客户锚定）

**锚点概念（v2.2 升格）**：

- `ownerId` = **负责人**（档案主归属，建档时服务端自动填充当前登录人，可转让）；
- `salesRepIds` = **跟进人集合**（含负责人 + 协作跟单；组共享的组员**不**在内——跟进人是持有档案写侧身份的人）；
- `followedBy(user)` = `{ relation | ownerId = user ∨ salesRepIds ∋ user }`——「我跟进的客户」。

**L1 档案层（图书馆）——Relation 档案本体：**

```
visibleArchive(relation, user) =
    relation.deletedAt ≠ null                                    → false     // 软删一票否决
  ∨ relation.sensitivity = 'confidential'
      ∧ ¬(followedBy(user) ∨ scopeKind = 'all')                  → false     // 敏感档案收窄
  ∨ true                                                                    // normal 档案全公司可查
```

**L2 业务层（工作台）——订单/报价/跟进等业务记录：**

```
visibleBiz(bizRecord, user) =
    scopeKind = 'all'                                            → true      // 财务/QC/后勤/管理员/超管
  ∨ followedBy(user) ∋ bizRecord.customerRelation                // 我跟进的客户
  ∨ TeamDataGrant ∃ (teamId ∈ activeTeams(user)                              // 团队共享客户
                     ∧ entityType = 'relation'
                     ∧ entityId = bizRecord.customerRelation
                     ∧ revokedAt IS NULL
                     ∧ team.deletedAt IS NULL)
```

**v2.2 口径变更说明**：

- **v2.1（旧）**：档案读 = 本人维 ∪ 小组维——档案私有，靠共享打开
- **v2.2（新）**：档案读 = 全公司（normal）+ 敏感例外；业务读 = 跟进客户锚定 ∪ 团队——**私有性从档案层移到业务层**
- **变更后果**：
  1. 业务员建档前可查全公司档案（防重复建档、知道客户找谁对接）；
  2. 订单视野跟着「跟进的客户」走：客户转让（ownerId/salesRepIds 变更）后，新跟进人自动继承历史订单视野，退出跟进者视野收回——解决「订单挂死在创建者身上」的锚点错位；
  3. TeamDataGrant 的价值从「档案可见」升格为「业务子树互见 + 协作跟进」（§5.3）——relationServiceV2 读路径的组授权 OR 分支移除（L1 不再消费组授权），组授权消费点迁至 L2 业务服务（订单/报价/跟进）；
  4. 写侧恒为本人维（档案，`write: 'self'`）/ 各业务域自身规则（订单：创建者 ∪ 宿主客户跟进人）——读放开不传染写；
  5. 部门继续退出数据视野计算（v2.1 结论不变）：目录展示 + DR-007 审批链。
- **迁移注意**：
  1. `relationServiceV2.buildScopeWhere` 读侧改为「normal 全量 + confidential 例外」；`buildWriteScopeWhere` 独立解析 `rule.write ?? rule.kind`（§4.4）；
  2. `orderServiceV2.buildScopeWhere` 从 `order.ownerId = me` 换锚为宿主客户归属解析；**L2 消费端（订单/报价/CRM 跟进）不读 relations 模块 scope**（v2.2 后该 scope = L1 图书馆口径，语义不同）——统一实现 `followedBy(user) ∪ teamGranted(user)` 解析（建议抽 shared helper `resolveBizScopeRelationIds(user)`）；
  3. 前端读路径（`listRelations`/`listOrders` 仍走 V1 无行级过滤）统一切 V2——把「未设防的全可见」换成「设计过的图书馆可见」；
  4. 既有测试需回归修订（§11.7 转译说明）。

**并集不是交集**：本人维与小组维取并——用户看到自己跟进的业务 + 各组共享客户的业务，多组之间也是并集融合（§8.1 心理模型不变）。

### 5.2 归属解析时机（v2 关键修订）

**每请求解析，不进 JWT claims**（JWT 是 7 天 cookie，缓进去退组后最长 7 天仍可见，违反 T-03 即时失效）：

- `activeTeams(user)` = `TeamMember WHERE userId = me AND leftAt IS NULL`（`@@index([userId])` 已存在，毫秒级单查）；
- 进程内可选 60s TTL 缓存（key = userId），失效窗口 ≤ 60s，验收线 T-03 以 60s 为上界；
- 授权 ID 集合（grantedEntityIds）同策略：按 (userTeams × activeGrants) 单查取回，进 `IN` 子句。

### 5.3 子实体派生可见性（v2.2 修订：跟进客户锚定）

业务记录（订单/报价/跟进）不直接挂行级归属，其可见性由**宿主客户**派生（§5.1 L2 公式）：

| 子实体 | 派生规则 | v2.2 状态 |
|--------|---------|----------|
| 订单 Order | `customerRelation ∈ followedBy(user) ∪ teamGranted(user) ∪ all` → 可见；写 = 创建者 ∪ 宿主客户跟进人 | Phase 2.6（V2 服务换锚 + 前端 listOrders 切 V2；V1 路径无行级过滤为待收编遗留） |
| 报价 Quotation | 同上（按 relation 关联） | Phase 2.6 |
| 跟进记录 FollowUpRecord | 随宿主客户按 L2 公式可见；**写入**按档位门禁（跟进人自由写；组员 read+followup 可写；read 只读） | Phase 2 已接（crmRouteV2），Phase 2.6 对齐换锚口径 |
| 附件/文档 | 随宿主实体所在层级（档案附件随 L1、业务单据随 L2） | Phase 2 |

派生规则的语义：**跟进客户 = 拥有其全部业务上下文**；档案查阅（L1）≠ 业务可见（L2）——全公司都查得到 H&M 的名片，但只有跟进人和协作团队看得见 H&M 的订单流水。不存在「共享档案但藏起订单」的割裂态，也不存在「看得到订单看不到客户」的逆向割裂。

### 5.4 失效优先级（多重状态叠加时的裁决序）

```
实体软删（deletedAt ≠ null）        → 不可见（一票否决，最高优先级）
敏感升级（sensitivity → confidential）→ 非本人维/管理角色立即不可见（下次查询生效）
用户退组（leftAt ≠ null）            → 该组授权对其失效
授权撤销（revokedAt ≠ null）         → 该授权失效
组解散（team.deletedAt ≠ null）      → 该组全部授权失效
账号停用                             → 一切失效
```

任何一层失效立即生效（下次查询即不可见），不做宽限期。

---

## §6 权限矩阵

### 6.1 角色 × 小组能力矩阵

**核心不变量（v2 修订 4）**：**对实体有行级写权限者，才可共享/撤销该实体的授权**。授权是写相邻动作——你不能决定别人看到一个你无权修改的东西。该不变量复用既有写权限校验（`buildScopeWhere` 的写侧），无需新口径。

| 能力 | 业务员 | 销售主管 | 组长 | 财务 | QC/后勤 | 管理员/超管 |
|------|--------|---------|------|------|---------|------------|
| 创建/解散小组 | ❌ | ✅ | ❌ | ❌ | ❌ | ✅ |
| 拉人/移除组员 | ❌ | ✅ | ✅（本组） | ❌ | ❌ | ✅ |
| 设置组长 | ❌ | ✅ | ❌ | ❌ | ❌ | ✅ |
| 授权数据给组 | ❌ | ✅（对有写权限的实体） | ✅（对有写权限的实体） | ❌ | ❌ | ✅ |
| 撤销授权 | ❌ | ✅（同上） | ✅（本组） | ❌ | ❌ | ✅ |
| 见组内共享数据 | ✅（所在组） | ✅（所在组） | ✅（所在组） | ✅（all 全量） | ✅（all 全量） | ✅（all 全量） |

注：组长授权仍受双重门禁——「是本组组长」∧「对该实体有写权限」。组长不是角色，是身份叠加（不新增系统角色，DR-041 容器不动）。

### 6.2 共享数据的读写分级（v2.2 修订：图书馆口径）

| 数据操作 | 全公司任意成员 | permission='read' 组员 | permission='read+followup' 组员 | 跟进人（owner/salesRep） | all-scope 角色 |
|---------|--------------|----------------------|-------------------------------|------------------------|---------------|
| 查看档案基础资料（L1，敏感字段已遮罩） | ✅ | ✅ | ✅ | ✅ | ✅ |
| 查看 confidential 档案 | ❌ 404 | ❌ 404 | ❌ 404 | ✅ | ✅ |
| 查看该客户订单/报价/跟进历史（L2 派生） | ❌（非跟进即不可见） | ✅ | ✅ | ✅ | ✅ |
| 添加跟进记录（FollowUpRecord） | ❌ 403 | ❌ 403 | ✅ | ✅ | ✅ |
| 修改档案本体字段（信用等级/付款条款等） | ❌ 403 | ❌ 403 | ❌ 403 | ✅ | ✅ |
| 以该客户名义新建订单/报价 | ❌ 403 | ❌ 403 | ❌ 403 | ✅ | 按各自业务域 scope |
| 导出/下载附件 | 随宿主层级（档案附件 ✅ / 业务单据随 L2） | ✅ | ✅ | ✅ | ✅ |

**分级理由**：档案是图书馆（查名片全公司都行），业务是工作台（订单流水只有跟进人和团队看得见）；「欧洲组同事帮着跟进 H&M 询价」的协作由 read+followup 档覆盖；档案本体（信用/条款/额度）多手共改是审计灾难，收敛跟进人单写。

### 6.3 权限守卫顺序

```
模块 scope（DR-041 既有）→ 行级数据范围（v2.2 三层视野：L1 图书馆/L2 跟进锚定/L3 小组）→ 档位门禁（read / read+followup / 本体写）→ 敏感标记（confidential 收窄 + 字段遮罩）
```

四层守卫逐层收窄，任何一层拒绝即 403/404，错误码区分：`MODULE_FORBIDDEN` / `SCOPE_BLOCKED` / `GRANT_PERMISSION_BLOCKED` / `SENSITIVE_ENTITY_NOT_SHAREABLE`。

---

## §7 API 契约

复用既有 `/api/hr/teams` CRUD（已存在），新增授权子资源与查询端点：

| 方法 | 路径 | 说明 | 权限守卫 |
|------|------|------|---------|
| GET | `/api/hr/teams/mine` | 当前用户所在组列表（含 leaderId/成员数/授权数） | 任何登录用户 |
| GET | `/api/hr/teams/:id` | 组详情（成员/授权/审计摘要） | 本组成员 ∨ 主管 ∨ admin |
| GET | `/api/hr/teams/:id/grants` | 组内授权列表（含已撤销，审计视图，分页） | 组长/主管/admin |
| POST | `/api/hr/teams/:id/grants` | 批量授权 `{ items: [{entityType, entityId, permission}] }` | §6.1（双重门禁） |
| PATCH | `/api/hr/teams/:id/grants/:grantId` | 改档 `{ permission }` | §6.1 |
| PATCH | `/api/hr/teams/:id/grants/:grantId/revoke` | 软撤销 `{ reason }` | §6.1 |
| GET | `/api/v1/relations/:id/team-shares` | 反查该客户共享给了哪些组（详情页 chips） | 该数据可见者 |
| POST | `/api/v1/relations/:id/team-shares` | 详情页就地共享（等价 grants 批量接口的便捷封装） | 对该 relation 有写权限者 |

契约细则：
- **幂等**：`@@unique([teamId, entityType, entityId])` 下，重复授权 = 更新 permission；撤销后再授权 = 复活原行；
- **错误码**：`TEAM_NOT_FOUND`(404) / `TEAM_DISSOLVED`(409，向已解散组建授权) / `ENTITY_NOT_FOUND`(404，被共享实体不存在) / `GRANT_SCOPE_BLOCKED`(403，无该实体写权限) / `GRANT_PERMISSION_BLOCKED`(403，档位不足)；
- **事务性**：解散组为单事务（§9.2）；批量授权部分失败整批回滚（不做部分成功，避免半授权态）；
- 所有变更写 AuditLog（§4.3）。

---

## §8 UI 呈现设计（v2 细化）

### 8.1 呈现总原则：不按组建视图，按业务对象统一列表

N 个组的内容**不分块**，全部汇入既有业务列表，只带来源标记。防混乱三支柱：

1. 主工作流永远是业务对象（今天处理哪些客户/订单）——组的数量不改变主轴，列表只变长一点；
2. 来源徽章是**弱信息**（小尺寸雾化 tint，BDS 范式），不关心的人可忽略，关心的人用筛选器精确定位；
3. 进组数量上限靠制度不靠界面：建组需主管/管理员权限（§6.1），组织泛滥在源头被控。

### 8.2 列表页（以客户列表为例）

```
客户列表（图书馆全量；统一排序：最近互动/状态/金额——排序键与组无关）
┌────────────────────────────────────────────┐
│ H&M        [我的]           负责人·我    2h前│
│ ZARA       [波兰成衣组·共享] 负责人·李雷  1d前│
│ 优衣库     [日本面料组·共享] 负责人·韩梅  3d前│
│ 国富纺织   [我的]           负责人·我    5d前│
│ Walmart    （无徽章·查阅）   负责人·王五  7d前│
└────────────────────────────────────────────┘
     筛选器：[全部] [我跟进的] [波兰成衣组] [日本面料组]
```

- 徽章规则（v2.2）：「我的」（本人维命中，即 L1+L2 全量视野）/「组名·共享」（团队命中，L2 协作视野）/ 无徽章（图书馆查阅态——L1 可查、L2 不可见）；同时命中只显示「我的」（主归属优先，不叠双徽章）；
- **负责人列常显**：图书馆的卡片目录——查阅档案时知道该客户找谁对接（负责人列点击可发起协作申请/加跟进人，由写权限者审批）；
- 筛选器默认值 = 用户个人偏好（记住上次选择，符合「系统设置管个人偏好」分层）；「全部」= 图书馆全量（normal 档案，公司总量百~千级，量级可控）；「我跟进的」= 本人维（v2.1「我的」改称，与 §5.1 锚点术语对齐）；
- confidential 档案仅对本人维 + 管理角色出现在列表中（非授权者不可见，非遮罩）；
- all-scope 用户（财务/QC/后勤/admin）：无徽章、无组筛选器（他们看全量，组维度对其透明）。

### 8.3 详情页（v2.2 修订：Tab 分层）

```
H&M 客户详情
┌────────────────────────────────────────────┐
│ H&M  [我的]   负责人·李雷   共享至: 欧洲组 ✕ │  ← chips；✕ 对有权限者可就地移除
│────────────────────────────────────────────│
│ 资料（L1 全公司可查） | 订单 | 报价 | 跟进（L2 仅跟进人/团队可见） │
│ 跟进记录输入框（跟进人 + read+followup 档渲染）│
```

- **Tab 分层渲染**：资料 Tab 随 L1（全公司可查，敏感字段按 scope 遮罩显示 ****）；订单/报价/跟进 Tab 随 L2——非跟进且非团队成员打开时显示空态「仅跟进人与协作组可见」（不裸抛 403，也不隐藏 Tab——让查阅者知道业务数据存在但需授权）；
- chips hover 显示授权人/时间/档位；
- 有写权限者（跟进人/主管/组长）可就地添加共享，不跳 AdminPanel；confidential 档案共享入口禁用（§4.4）；
- **优雅失效态**：会话中授权被撤销 → 下次操作返回 404/403 → 前端转译为「该数据已取消共享」提示页，不裸抛错误。

### 8.4 组主页（补充视图，非主入口）

侧边栏「我的小组」→ 组详情页：
- 成员区（组长标识/移除/退组按钮）+ 授权区（按 entityType 分栏、批量勾选客户）；
- 组长用此页管理；普通组员日常**完全不经过**这里（数据自己流入主列表）；
- 解散组二次确认（BdsDialog：明确提示「全部授权将失效，组员将立即失去共享数据可见性」）。

### 8.5 小组管理入口（v2.1：归位 HRManager 人事管理）

**权责划分原则**：AdminPanel 管"平台怎么运转"（软件层治理：账号/角色/工具权限/平台规则/公司主体/知识库 ACL——换家公司要重配的东西）；HRManager 管"本公司怎么组织"（组织人事：部门/小组/项目/任务/员工档案/考勤薪资绩效培训——描述本公司组织形态）。

- **HRManager·小组管理**（从 AdminPanel 迁入，v2.1 唯一管理真源）：组列表（组名/组长/成员数/生效授权数/状态）+ 建组表单（bds-select 选组长、关联部门可选、多选成员）+ 组编辑（改名/换组长/换关联部门，调既有 PATCH）+ 组详情（成员管理 + 授权管理 + 审计视图 + 解散，复用 8.4 组件）；
- AdminPanel 移除小组管理 Tab（后端 `/api/hr/teams*` 零改动，本就挂 hr 域——UI 迁移即前后端语义对齐）；
- HRManager·组织架构树：部门树 + 人员编制 + 该部门成员参与的小组（只读目录展示，建组/管组统一走小组管理入口；跨部门组在树顶部"跨部门小组"区展示，不再叫"孤儿团队"）；
- **项目/任务挂靠**（v2.1）：项目/任务从"编制团队"迁移挂靠到小组——一个小组 = 数据共享 + 项目任务 + 协作跟进的完整业务单元（如「波兰成衣组」既有共享客户又有项目任务）；"编制团队"概念消除。

### 8.6 Dashboard 视角聚焦器（v2.1 新增）

Dashboard 顶部的组选择器定位为**经营视角过滤器**，不是权限切换：

- **权限层（数据获取）永远融合**：用户视野 = 本人 ∪ 所在全部组（并集），无任何"切换"概念；
- **展示层可选聚焦**：默认「全部」显示融合全景概况；点选某组 → 看板聚焦该组的经营切片（该组关联客户/订单/跟单/业绩统计）；
- **角色语义**：老板/超管可逐组巡检（组选择器含全公司所有组）；销售主管看自己所加入各组的健康度；all-scope 角色（财务/QC）无聚焦器或可选全量；
- **实现要点**：聚焦 = Dashboard 聚合查询的 `teamId` 过滤参数（组关联实体集 = TeamDataGrant 生效授权实体），不动任何权限接口。

---

## §9 边界、竞态与失效语义

### 9.1 边界场景裁决表

| # | 场景 | 裁决 | 理由 |
|---|------|------|------|
| B-01 | 实体软删但授权生效中 | 不可见（deletedAt 一票否决） | §5.4 失效优先级 |
| B-02 | 实体归属部门迁移（departmentId 变更）后原授权 | **继续生效**（授权挂在实体上，非部门上） | 快照语义；换部门不该静默撕毁既有协作约定 |
| B-03 | 组长退组（leader 本人 leftAt） | 组继续运作（授权与成员不受影响）；UI 提示「组长空缺」 | 组长是管理入口非运作前提 |
| B-04 | 组内零成员但授权生效 | 无害空转（可见性要求 user ∈ team，无人满足） | 无需清理任务 |
| B-05 | 授权人自身权限被收（后来无该实体写权限） | 既有授权**不自动回收**（避免权限变更引发大面积静默失效）；由主管在审计视图人工清理 | 显式优于隐式；AuditLog 提供清理线索 |
| B-06 | 同一用户被同一实体经多组可见 | 徽章只显示一条（取第一个命中的组，字典序） | 去重呈现 |

### 9.2 解散组事务（竞态核心防护）

```
POST /api/hr/teams/:id/dissolve 事务体：
1. 校验 team 存在 ∧ deletedAt IS NULL（防重复解散）
2. UPDATE TeamDataGrant SET revokedAt=now, revokedBy=operator,
        revokeReason='team dissolved'
   WHERE teamId=:id AND revokedAt IS NULL
3. UPDATE Team SET deletedAt=now
4. AuditLog(team_dissolve)
```

竞态裁决：
- 解散进行中收到新授权请求 → 步骤 1 校验失败或步骤 2/3 与授权事务的行锁互斥，后到者见 `TEAM_DISSOLVED` 409；
- 解散 vs 撤销并发 → 同一行 revokedAt，后写覆盖先写（结果等价：已撤销），AuditLog 各留一条；
- 进程崩溃于事务中 → 整体回滚，组回到未解散态，可重试（幂等）。

### 9.3 读侧竞态（授权撤销 vs 用户正在浏览）

- 撤销后用户已打开的详情页：下次任何 API 调用（含轮询）返回 403/404 → §8.3 优雅失效态；
- 不做服务端推送（SSE 通知撤销）——成本不成比例，拉取失效足够（60s 缓存上界内）。

---

## §10 性能与规模

| 关注点 | 量级评估 | 对策 |
|--------|---------|------|
| activeTeams 查询 | 用户 ∈ 组数（预期 ≤5），单查 `TeamMember(userId)` 有索引 | 每请求直查，毫秒级 |
| grantedEntityIds 集合 | 组数 × 组均授权数（中型外贸公司客户总量百~千级） | `IN` 子句承载；预期 ≤2000 ID，Postgres 无压力 |
| 列表查询 | scopeWhere 增加一个 OR 分支 `{ id: { in: grantedIds } }` | 与既有 PL-2B OR 组合同构，复用索引 |
| 缓存 | 归属/授权 ID 集 60s TTL 进程内缓存 | 失效上界 60s，验收线 T-03 |
| 规模升级路径 | 授权总量 >10k 或成员结构复杂化 | 预研 EXISTS 子查询/raw SQL（本期不做，记录演进方向） |

验收性能线：授权 2000 条规模下，客户列表 P95 < 500ms（与改造前基线对比劣化 < 50ms）。

---

## §11 验收用例（26 条）

### 11.1 组生命周期

| 编号 | 场景 | 前置 | 步骤 | 期望 | 失败路径 |
|------|------|------|------|------|---------|
| T-01 | 建组 | 销售主管登录 | HRManager→人事管理·小组管理→建「波兰成衣组」，拉 2 名跨部门业务员，设组长 | 组建成功，组员 `teams/mine` 即见 | 业务员建组 → 403 |
| T-02 | 一人多组 | 用户已在「H&M 品牌组」 | 加入「欧洲市场组」 | 同属 2 组，两组合并可见（并集） | — |
| T-03 | 退组即时生效 | 组员在组且组内有共享数据 | 组长移除该组员 | **≤60s 内**列表不再出现组共享数据 | >60s 仍可见 = 缺陷（缓存/JWT 泄漏） |
| T-04 | 解散组 | 组存在且有生效授权 | 管理员解散 | 事务内：全部授权 revokedAt 置位 ∧ 组 deletedAt 置位 ∧ AuditLog 1 条；组员数据回归纯本人范围 | 数据仍可见 = 缺陷 |
| T-05 | 重复解散 | 组已解散 | 再次调 dissolve | 409 TEAM_DISSOLVED（幂等保护） | 二次副作用 = 缺陷 |
| T-06 | 组长空缺 | 组长退组 | 组长 leftAt 置位 | 组/授权继续运作；UI 显示「组长空缺」 | 授权失效 = 缺陷 |

### 11.2 授权与可见性

| 编号 | 场景 | 前置 | 步骤 | 期望 | 失败路径 |
|------|------|------|------|------|---------|
| T-07 | 单组授权 | H&M 归属销售部；采购部业务员在「欧洲组」 | 主管将 H&M 授权给欧洲组 | 采购组员列表出现 H&M（共享徽章），销售部成员不受影响 | 采购组员看不到 = 缺陷 |
| T-08 | 多组交叉 | H&M 已授权「欧洲组」 | 再授权「H&M 品牌组」 | 两组组员均可见；授权行 2 条 | 唯一冲突未幂等升级 = 缺陷 |
| T-09 | 本人维 ∪ 组维 | 组员自己有客户 A；组内共享客户 B | 查看列表 | A、B 均可见 | 交集逻辑滤掉 A = 缺陷 |
| T-10 | 撤销授权 | T-07 生效中 | 主管撤销（填 reason） | ≤60s 内组员不可见；AuditLog team_grant_revoke 含 reason | 仍可见 = 缺陷 |
| T-11 | all 角色零影响 | 财务登录 | 查看客户列表 | 全量可见，与改造前逐字节一致（回归） | 财务可见性变化 = 回归缺陷 |
| T-12 | 重复授权幂等 | H&M 已授权欧洲组(read) | 再授权(read+followup) | 原行 permission 更新，无新行，无报错 | 409/重复行 = 缺陷 |
| T-13 | 撤销后复活 | 授权已撤销 | 重新授权 | 原行 revokedAt 置 null，历史保留 | 新行导致唯一冲突 = 缺陷 |
| T-14 | 实体软删优先 | 授权生效中 | 客户被软删 | 组员不可见（deletedAt 一票否决） | 仍可见 = 缺陷 |
| T-15 | 实体迁移部门 | H&M 授权生效 | H&M 的 departmentId 改到别部 | 授权继续生效（快照语义） | 静默失效 = 缺陷 |

### 11.3 子实体派生

| 编号 | 场景 | 前置 | 步骤 | 期望 | 失败路径 |
|------|------|------|------|------|---------|
| T-16 | 共享客户 → 订单派生可见 | H&M 有 3 张订单；欧洲组被授权（read+followup） | 采购组员打开 H&M 详情 | 3 张订单全部可见 | 订单被藏 = 缺陷 |
| T-17 | 跟进历史派生可见 | 同上 | 查看跟进记录 Tab | 历史记录全部可见 | — |

### 11.4 读写分级

| 编号 | 场景 | 前置 | 步骤 | 期望 | 失败路径 |
|------|------|------|------|------|---------|
| T-18 | read+followup 可跟进 | 授权档位 read+followup | 采购组员在 H&M 添加跟进记录 | 创建成功，作者=采购组员 | 403 = 档位未生效 |
| T-19 | 档案本体不可写 | 同上 | 采购组员改 H&M 信用等级 | 403 GRANT_PERMISSION_BLOCKED | 写成功 = 越权（严重） |
| T-20 | read 档跟进不可写 | 授权档位 read | 添加跟进记录 | 403 | — |
| T-21 | 共享客户不可下单 | 同 T-18 | 采购组员以 H&M 名义新建订单 | 403 | 下单成功 = 越权（严重） |

### 11.5 权限矩阵与竞态回归

| 编号 | 场景 | 步骤 | 期望 |
|------|------|------|------|
| T-22 | 业务员不能建组 | 业务员调 POST /api/hr/teams | 403 |
| T-23 | 组长只能管本组 | 组长 A 对组 B 拉人/授权 | 403 |
| T-24 | 无写权限不可授权 | 业务员（对实体无写权限）尝试共享 | 403 GRANT_SCOPE_BLOCKED |
| T-25 | 解散竞态 | 解散事务进行中并发提交新授权 | 后者 409 TEAM_DISSOLVED，无半授权态 |
| T-26 | 审计完整性 | T-07+T-10 后查 AuditLog | create/revoke 各 1 条，含 actor/entity/team/permission/reason |

性能验收线（T-27，附加）：2000 授权规模下客户列表 P95 < 500ms，劣化 < 50ms。

### 11.6 v2.1 视野口径切换（组为主，部门退出）

| 编号 | 场景 | 前置 | 步骤 | 期望 | 失败路径 |
|------|------|------|------|------|---------|
| T-28 | 同部门同事不可见 | 业务员 A、B 同属业务部，各自有客户（未共享进组） | B 查看客户列表 | B 只见自己的客户，不见 A 的 | B 看到 A 客户 = v2.1 切换未生效（部门维残留） |
| T-29 | 主管视野 = 组并集 | 销售主管加入「波兰成衣组」「日本面料组」，两组各有共享客户 | 主管查看列表 | 两组共享客户并集可见（融合无切换） | 只见一组 = 缺陷 |
| T-30 | 组聚焦视角 | Dashboard 视角聚焦器就绪 | 主管点选「波兰成衣组」 | 看板仅显示该组关联客户/订单/业绩切片；切回「全部」恢复融合全景 | 聚焦影响权限接口调用 = 架构缺陷（聚焦只能过滤聚合查询） |
| T-31 | HR 删除组 = dissolve | 组有生效授权 | 经人事管理删除组 | 走 dissolve 事务：授权全回收 + 审计 1 条 + 软删（无僵尸授权行） | 仅软删无授权回收 = P0 未修复 |
| T-32 | 部门退出 scope 回归 | v2.1 切换后 | 财务/超管查全量列表 | 全量可见不变（all-scope 回归）；DR-007 审批人解析仍按部门 headId 正常出单 | 审批链断 = 部门退出殃及审批 = 缺陷 |

> **v2.2 转译说明**：T-07~15 的「组员可见性」期望在 v2.2 下由图书馆口径天然满足（normal 档案全公司可查），用例语义转译为**徽章/筛选器正确性**（组员看到的应是「组名·共享」徽章而非「我的」）；T-28（同部门同事不可见）已被 v2.2 **取代**——档案层同部门/全公司均可查，隔离移至 L2 业务层（非跟进同事的订单/跟进不可见，见 T-37）；T-29（主管视野 = 组并集）的语义同样移至业务层验证。

### 11.7 v2.2 三层视野（档案图书馆化 / 业务换锚 / 敏感标记）

| 编号 | 场景 | 前置 | 步骤 | 期望 | 失败路径 |
|------|------|------|------|------|---------|
| T-33 | 档案图书馆化 | 业务员 A、B 各有客户，互不跟进不共组 | B 查看客户列表（V2 端点） | B 见到自己 + A 的 + 全公司所有 normal 档案（A 的显示无徽章·查阅态） | B 看不到非跟进档案 = 图书馆化未生效（v2.1 本人维残留） |
| T-34 | confidential 收窄 | H&M 标记 confidential，负责人 A | B（非跟进非管理）查列表/直连详情 | 列表不出现 H&M；详情 API 404 | 可见 = 敏感标记未生效（严重） |
| T-35 | confidential 授权者可见 | 同上 | A 查看列表/详情；财务（all-scope）同查 | 两者均正常可见 | — |
| T-36 | 敏感字段遮罩 | H&M（normal），B 非跟进 | B 打开 H&M 详情资料 Tab | 基础资料可见；信用额度/成本类字段 **** | 明文 = stripSensitive 未接 relations 域 |
| T-37 | 业务层锚定（负向） | B 不跟进 H&M、不共组 | B 打开 H&M 详情订单 Tab；直连 H&M 订单列表 API | Tab 空态「仅跟进人与协作组可见」；API 返回空集/404 | 订单流水可见 = L2 锚定未生效（严重越权） |
| T-38 | 订单换锚（转让继承） | H&M 名下 3 张订单由 A 创建；ownerId 改为 B | B 查看订单列表 | 3 张历史订单进入 B 视野；A 退出 salesRepIds 后视野收回 | 换锚后仍按创建者锚定 = 缺陷 |
| T-39 | 团队业务互见 | H&M 授权欧洲组（read+followup），组员 C | C 打开 H&M 详情 | 订单/报价/跟进全部可见（派生）；C 可写跟进记录 | 只见档案不见订单 = 畸形可见性（v2.1 已知缺口） |
| T-40 | 读写分离 | sales 登录 | 读全公司档案列表；PUT 修改非跟进档案 | 读 200；写 404/403 | 写成功 = 读写分离未生效（严重越权） |
| T-41 | confidential 禁止组共享 | H&M（confidential），负责人尝试授权给组 | POST /team-shares | 409 SENSITIVE_ENTITY_NOT_SHAREABLE | 授权成功 = 绕过敏感标记（严重） |

---

## §12 实施排期

| 阶段 | 内容 | 依赖 | 对应用例 |
|------|------|------|---------|
| Phase 1 | TeamDataGrant 迁移 + hr teams CRUD 权限守卫接线（§6.1）+ AdminPanel 小组管理 Tab + `teams/mine` | 无 | T-01~06, T-22~23 |
| Phase 2 | Relations 域接入小组维（buildScopeWhere 扩展 + 详情 403 门禁）+ 跟进档位门禁 + 详情页 chips + 列表徽章/筛选器 + AuditLog | Phase 1 | T-07~15, T-18~21, T-24~26 |
| Phase 2.5（v2.1） | **P0**：HR DELETE /teams/:id 改走 dissolve；**视野切换**：buildScopeWhere/buildWriteScopeWhere 部门维→本人维 + 其他域口径对齐 + 回归修订；**入口归位**：AdminPanel 小组 Tab 迁 HRManager 人事管理（含组编辑补齐）+ 组织架构树改目录只读 + 项目/任务挂靠小组；**Dashboard 聚焦器** | Phase 2 | T-28~32 + T-01~26 回归 |
| Phase 2.6（v2.2） | **三层视野落地**：① DataScopeRule 读写分离（`{ kind: 'all', write: 'self' }` + 双文件同步）② Relation.sensitivity + confidential 校验 + 组共享门禁 ③ relationServiceV2 读侧图书馆化（组授权 OR 分支移除）④ L2 换锚 shared helper（`resolveBizScopeRelationIds`）+ orderServiceV2/crmRouteV2 接线 ⑤ 前端读路径切 V2（listRelations/listOrders）⑥ 列表徽章/负责人列/详情 Tab 分层/空态 UI | Phase 2.5 | T-33~41 + T-01~26 回归 |
| Phase 3 | （v2.2 已并入 Phase 2.6：订单行级过滤随换锚一并落地）报价派生细化 / 其余子实体（发票/凭证等）锚定扩展 | Phase 2.6 | T-16~17（并入 T-38/39） |
| Phase 4（演进预留） | rule-based 自动归集（按市场标签自动授权）/ 组内活跃度报表 | 产品决策 | — |

---

## §13 关联文档

| 文档 | 关系 |
|------|------|
| [底座域模型组.md](../02-数据模型/底座域模型组.md) §12 §13 | Team/TeamMember 模型真源（本设计零改动复用） |
| [管理后台AdminPanel.md](../04-模块设计/08-设置与后台/Settings-设置/管理后台AdminPanel.md) | 小组管理 Tab 落位（Phase 1 更新） |
| rolePermissionMatrix.ts（`_shared` 与根 `lib` 双文件） | DataScopeRule team 分支启用；**双文件逐字同步铁律** |
| relationServiceV2.ts:112-132（buildScopeWhere） | 小组维 OR 分支的实际接线点（Phase 2） |
| [2026-08-16-设计评审决策记录.md](../10-评审与决策/2026-08-16-设计评审决策记录.md) | DR-041 / DR-007 上下文 |
