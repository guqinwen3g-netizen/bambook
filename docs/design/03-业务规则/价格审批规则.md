# 价格审批规则

> **PRD 关联**：§9.2 价格审批 5 条件（①-⑤）
> **落地状态**：⚠️ 仅 ⑤ 双轨偏差 >15%/>30% 已落地；① 折扣>10% / ② 新客首单 / ③ 改价±5% / ④ 低于成本价 4 条未接入
> **DR-006 关联**：Track A、Track B 和任何定价模型均为参考/试算，不具有自动定价权；所有正式价格必须人工审批
> **DR-007 关联**：审批路由按组织归属解析——负责团队的销售主管或总领导审批；每次只需一位有效审批人；总领导拥有跨团队兜底权

---

## §1 元信息与实现状态

| 维度 | 内容 |
|------|------|
| 定位 | 5 维度价格风险识别 → 分级审批链 → 未通过审批 fail-closed 阻断发送/下单。Track A/B 均为参考/试算（DR-006），不具有自动定价权；草稿自由试算不触发审批，正式承诺必须冻结版本并审批 |
| 触发入口 | Quotation 创建/发送 / Quotation→Order 转换 / Order 二次编辑 / OrderLine unitPrice 变更 |
| 角色 | 销售（发起）/ 销售主管（审批）/ 总领导（跨团队兜底审批）（DR-007：不新增硬编码角色，按组织归属解析） |
| 范式 | 规则型：§6 触发矩阵 |
| 优先级 | P0（⑤双轨偏差已上线属核心防亏损，其余 4 条 P1 紧接接入） |
| **落地状态明细** | ⑤ ✅ / ①❌ / ②❌ / ③❌ / ④❌ |
| ⑤ 已实现代码 | `server/src/quotations/quotationService.ts:124-227`（calculatePriceDeviation + deviation.level≠ok → ApprovalRequest 自动创建）/ `quotationService.ts:204-227`（同事务写入 ApprovalRequest actionType=quotation:price-deviation）/ `quotationService.ts:432-445`（sendQuotation 门禁：level=block 且未 approved → 抛出错误禁止发送）|
| ①②③④ 缺口位置 | `quotationService.ts` createQuotation（未判断折扣/新客/低于成本价）/ convertToOrder（无前后价 diff）/ Order update 路由（无改价 ±5% 校验）/ CostEstimate 模型未与定价联动 |
| ApprovalRequest 模型真源 | `schema.prisma:1807-1828`（id/requesterId/reviewerId/actionType/targetType/targetId/status/risk/payload） |
| PRD 关联 | §9.2 ①-⑤ / §8.6 双轨定价偏差 |

---

## §2A DR-006 价格冻结与版本纪律

> **核心决策**（DR-006）：Track A、Track B 和任何定价模型均为参考/试算，不具有自动定价权。

### 2A.1 三态价格生命周期

| 状态 | 说明 | 审批要求 | 可编辑性 |
|------|------|---------|---------|
| **草稿** | 业务员自由试算与改价 | 不触发审批 | 完全可编辑 |
| **正式承诺** | 报价对外发送、创建订单、生成合同、成为内部面料结算价 | 必须冻结该报价版本并提交领导审批 | 未批准版本不得对外发送或被正式业务单据引用 |
| **已批准** | 审批通过的正式价格 | 变更必须创建新版本并重新审批 | 不可直接修改，只能新建版本 |

### 2A.2 冻结时机

| 动作 | 冻结内容 |
|------|---------|
| 报价对外发送 | 冻结报价版本 + 价格快照 + 双轨数据 |
| 创建订单 | 冻结订单价格 + 关联报价版本引用 |
| 生成合同 | 冻结合同金额 + 价格条款快照 |
| 内部面料结算价 | 冻结内部结算价 + 关联服装订单引用 |

### 2A.3 变更规则

| 场景 | 规则 |
|------|------|
| 草稿修改 | 直接修改，不触发审批，不产生新版本 |
| 已批准价格变更 | 必须创建新版本 → 重新提交审批 → 审批通过后替换旧版本 |
| 版本追溯 | 每次变更保留前后值、变更人、时间、原因与审批证据 |

---

## §2B DR-007 审批路由按组织归属

> **核心决策**（DR-007）：保持"销售主管"通用角色，不新增写死的"服装主管"或"面料主管"。业务员归属的团队/部门决定其主管负责的业务线与客户。

### 2B.1 审批人解析规则

| 项 | 说明 |
|---|---|
| 审批人 | 负责团队的销售主管或总领导 |
| 单次审批 | 每次只需一位有效审批人 |
| 兜底权 | 总领导拥有跨团队兜底审批权 |
| 技术边界 | 总领导的业务审批权不得依赖技术超级管理员身份；通过业务审批 scope 和组织关系授权 |

### 2B.2 审批范围

| 价格类型 | 审批人 |
|---------|--------|
| 服装价格 | 负责团队的销售主管或总领导 |
| 面料价格 | 负责团队的销售主管或总领导 |
| 内部面料结算价 | 负责团队的销售主管或总领导 |

---

## §2 五条件说明（DR-007 彻底去阈值化 · 决策点 3-A）

> **§9.2 DR-007 去阈值声明（决策点 3-A）**：折扣/改价/低于成本价/双轨>15%/双轨>30%/新客首单 等所有价格偏差触发的审批，**任何偏差幅度统一走 DR-007 组织归属路由**（申请人所属部门主管，无主管或部门为财务时总领导兜底，单次一位审批人即可）。不再看阈值比例（折扣>10%/改价±5% 触发条件保留为命中判断依据，但审批路由与阈值幅度无关）；多条触发条件合并为**单个 ApprovalRequest**（不重复发单），payload.hitConditions 中标注所有命中的条件编号。

| 编号 | 条件 | 判断公式 / 触发阈值（命中判断用，不影响审批路由） | 风险等级（UI 展示参考） | 审批人（统一 DR-007） | 落地 |
|------|------|------------------------------------------|----------------------|---------------------|------|
| ① | 折扣 >10% | `discountPercent = 1 - (finalUnitPrice / catalogPrice)`；discountPercent > 0.10 → 命中 | medium 黄标 warn | 申请人所属部门主管；无主管/财务部门→总领导兜底（单次一位即可） | ❌ |
| ② | 新客首单 | `Relation.stage ∈ {Lead, Opportunity, Quotation, TrialOrder}`（非 Customer/Key/Churned）且该 Relation 下 Order 数量 = 0 → 命中 | medium 黄标 warn | 同上（统一 DR-007） | ❌ |
| ③ | 改价 ±5% | `|newUnitPrice - oldUnitPrice| / oldUnitPrice > 0.05` → 命中（Quotation→Order 转换 diff / Order 二次编辑 diff） | medium / high（参考） | 同上（统一 DR-007） | ❌ |
| ④ | 低于成本价 | `finalUnitPrice < rolledUpCost`（BOM roll-up 成本或 CostEstimate.cost）→ 命中（**即时亏损风险**） | high 红标 block（UI 最严标注） | 同上（统一 DR-007，必须审批，无豁免自批） | ❌ |
| ⑤ | 双轨偏差 >15% />30% | `deviationPercent = |trackBFinalUsd - trackAMedianUsd| / trackAMedianUsd`；>15% → warn（medium）；>30% → block（high）→ 命中 | medium / high（参考） | 同上（统一 DR-007） | ✅ |

**审批多条件叠加规则（决策点 3-A）**：同时命中 2+ 条件时 → 生成 **单个** ApprovalRequest（payload 带 `hitConditions: ["①","③","⑤"]` 数组，备注中标注所有命中的条件编号）→ reviewerId 统一在服务端 `resolveReviewerByDepartment(申请人userId)` 解析（DR-007，每次只需一位有效审批人）→ 不重复创建多条。

**兼容 Scope 命名保留（与阈值无关）**：
- `lt10_`、`gt5_` 等 scope 前缀标记为兼容保留名，实质逻辑与偏差幅度无关，全部走 DR-007 统一路由

---

## §3 审批人链分级定义（DR-007 组织归属版）

> **DR-007 核心**：审批路由以负责团队销售主管或总领导为准，替换旧版固定 L1/L2/L3 金额阈值层级。每次只需一位有效审批人；总领导拥有跨团队兜底权。

| 审批人 | 解析逻辑 | 可审批范围 | 超时处理 |
|------|---------|-----------|---------|
| 销售主管 | 业务员归属的团队/部门主管（通过 UserAccount.departmentId → Department.head 解析）；负责该业务线与客户 | 所有价格审批（服装/面料/内部结算价）；风险 medium/high 均可审批 | 24h 未审批 → 自动抄送总领导 + App 推送加急 |
| 总领导 | 跨团队兜底审批人（Role=总领导 或硬编码 ownerId）；不依赖技术角色身份，通过业务审批 scope 授权 | 跨团队兜底 + 被销售主管驳回的申诉 + 战略特批 | 48h 未审批 → 系统提醒；72h 未审批 → auto-reject（默认保护亏损单，需总领导手工 override） |

**审批链解析实现**（待接入 approvalPolicy）：当前 `approvalPolicy` 全局为 `manual always`（即所有审批手工指定 reviewerId）；需升级为规则驱动：`resolveReviewerFor(actionType, hitConditions, requesterDept, requesterTeam)` → 返回负责团队销售主管或总领导的 userId。

---

## §4 ⑤ 双轨偏差已实现细节剖析（✅）

### 4.1 实现链路

1. **A/B 双轨价完备性判断**：`quotationService.ts:125-126` `trackAOk && trackBOk`（两者都有限且 >0）才计算偏差
2. **calculatePriceDeviation 计算**：`server/src/pricing/trackAEstimator.ts` 导入（quotationService.ts:21）→ 返回 `{ deviationPercent, level }`，level ∈ {ok, warn, block}
3. **审批请求生成**：`quotationService.ts:136-152` level≠ok → `resolveActorUserAccountId` 解析 requester → 生成 approvalId
4. **同事务写入 ApprovalRequest**：`quotationService.ts:204-227` `tx.approvalRequest.create`，actionType=`quotation:price-deviation`，risk=deviation.level==='block' ? 'high' : 'medium'，payload 含双轨快照 + 偏差值
5. **发送门禁（fail-closed）**：`quotationService.ts:432-445` 若 `priceDeviationLevel === 'block'` → 查询 ApprovalRequest.status === 'approved' → 未通过则抛出 `需审批通过后方可发送` 错误

### 4.2 关键代码位置锚定

| 功能 | 文件:行号 |
|------|----------|
| 偏差计算触发 | quotationService.ts:124-130 |
| 审批 requester 解析 | quotationService.ts:136-152 |
| ApprovalRequest 创建（事务内） | quotationService.ts:204-227 |
| 红标 block 门禁（Draft→Sent） | quotationService.ts:432-445 |
| ApprovalRequest 模型 | schema.prisma:1807-1828 |

---

## §6 触发矩阵（5 条件 × 动作 × 门禁 × 异常分支）

| # | 触发条件 | 动作 | 门禁（Gate） | 异常分支 |
|---|---------|------|-------------|---------|
| 0 | 草稿阶段价格编辑（Quotation/OrderLine 草稿态） | 自由试算与改价，不触发审批，不产生 ApprovalRequest | 无门禁（DR-006：草稿不触发审批） | — |
| 0.1 | 正式承诺动作（报价发送/创建订单/生成合同/内部结算价） | 冻结报价版本 + 价格快照 → 提交审批（DR-006） | 未批准版本不得对外发送或被正式业务单据引用 | 冻结失败（DB 事务异常）→ 回滚操作 + Toast「价格冻结失败，请重试」 |
| 1 | Quotation 创建且命中 ⑤（双轨偏差） | ✅ 自动计算偏差 → level≠ok 写 ApprovalRequest（quotationService 已实现） | ✅ level=block 且 ApprovalRequest.status≠approved → sendQuotation 阻断（已实现） | Actor 解析失败 → 审批请求跳过（quotationService.ts:148 logger.warn，不阻塞创建，但门禁仍 fail-closed 阻止发送）|
| 2 | Quotation 创建且命中 ①（折扣>10%） | ❌ 待实现：计算 catalogPrice → discountPercent → 追加 hitConditions | ❌ 待接入：折扣 >20% 升级为 block + 禁发送 | 目录价缺失（CatalogPrice 未维护）→ 跳过条件 ①，仅走 ④⑤ + Toast 提示「目录价未配置」 |
| 3 | Quotation 创建且命中 ②（新客首单） | ❌ 待实现：查 Relation.stage + 该 Relation 历史 Order 数 → 追加 hitConditions | ❌ 待接入：新客首单必走 L1 审批后才允许 convertToOrder | Relation 未绑定（customerRelationId=null）→ 视为新客 + 标记，等绑定后复核 |
| 4 | convertToOrder：源 Quotation.unitPrice vs 新 OrderLine.unitPrice 差异 ±5% | ❌ 待实现：diff 百分比 → 命中 ③ → 生成 ApprovalRequest（actionType=`order:price-adjustment`） | ❌ 待接入：diff>15% 未审批 → convertToOrder 中止 | Quotation 有多行，部分行命中 → 按行粒度审批，未命中行可正常转换（混态） |
| 5 | Order 编辑：OrderLine.unitPrice 变更 ±5% | ❌ 待实现：before/after 快照 diff（复用 AuditLog.beforeValue/afterValue） | ❌ 待接入：diff>30% 未审批 → Order update 路由 403 | concurrent 编辑（两人同时改同一行）→ 乐观锁 + 后提交者需重审 |
| 6 | Quotation 创建 / 行编辑且命中 ④（低于成本价） | ❌ 待实现：CostEstimate 查对应 product 的 rolled-up cost → 比对 unitPrice | ❌ 必 block：无论其他条件，低于成本价 → 必须 L2 运营总监审批通过 | CostEstimate 缺失 → 触发告警 + 走 ⑤ 兜底（双轨偏差），但在 ApprovalRequest payload 标记 `costEstimateMissing: true` 供审批人参考 |
| 7 | 审批多条件叠加 → 最高级审批链 | ❌ 待实现：hitConditions 去重合并 → max(审批层级) → 单个 ApprovalRequest | ❌ 单个审批通过即解锁全部条件（不拆多条，避免审批疲劳） | 叠加条件冲突（如下于成本价 ④ + 双轨 ok ⑤）→ 仍按 ④ 高风险走 L2 |
| 8 | ApprovalRequest rejected → 回滚报价/订单为 Draft | ✅ 基础：ApprovalRequest.status=rejected 字段存在（schema.prisma:1814） | ❌ 待接入：被 reject 的 Quotation 禁止 send，禁止 convertToOrder | 审批人 reject 后销售改价 → 原 ApprovalRequest 取消 + 新建一条（版本化） |

---

## §11 业务规则关联（22 条交集）

| 规则 | 关联点 |
|------|--------|
| §9.1 MOQ | MOQ 豁免导致工厂加价 → 加价后单价仍低于成本价 → 叠加触发 ④ + 加价 diff 触发 ③ |
| §9.4 信用控制-Tier 权益 | CustomerTier.discountRate（schema.prisma:547）→ 合法折扣上限，Tier=Platinum 可享 15% 折扣而不触发 ①（条件 ① 阈值应减去 Tier 权益折扣额） |
| §9.7 订单变更 | Order 变更触发 ③ 改价审批，与订单变更审批链（L1/L2/L3）**合并为一个审批请求**（不重复走两遍） |

---

## §14 待补设计缺口

| P | 缺口 | 细化位置 |
|---|------|---------|
| P0 | DR-006 价格冻结机制（报价版本冻结 + 价格快照 + 正式承诺触发审批） | server/src/quotations/quotationService.ts sendQuotation/convertToOrder + server/src/orders/ 创建订单路由 |
| P0 | DR-007 审批路由按组织归属解析（requesterDept → Department.head → 销售主管 / 总领导兜底） | server/src/approvals/approvalPolicy.ts 新增 resolveReviewerByOrg() |
| P1 | approvalPolicy 升级为规则驱动（当前 manual always） | server/src/approvals/approvalPolicy.ts 新增 resolveReviewerFor() |
| P1 | ① 折扣>10% 校验 + CatalogPrice 档案 | 新增 server/src/pricing/catalogPriceService.ts |
| P1 | ② 新客首单 Relation.stage + 历史 Order 数校验 | quotationService.ts createQuotation + convertToOrder 接入 |
| P1 | ③ 改价 ±5% diff（convertToOrder + Order update 两条路径） | quotationService.ts convertToOrder + orders route update handler |
| P0 | ④ 低于成本价 CostEstimate ↔ Pricing 联动（高优先级防亏） | server/src/pricing/costEstimateService.ts 与 quotation/order 定价接入 |
| P2 | 审批合并（多条件 × 订单变更 × MOQ 叠加 → 单请求） | ApprovalRequest.payload.hitConditions 数组 + 合并策略 |

---

## §15 相关文档索引（交叉链接）

| 文档 | 链接 | 关联要点 |
|------|------|---------|
| 评审决策记录 | [10-评审与决策/2026-08-16-设计评审决策记录.md](../10-评审与决策/2026-08-16-设计评审决策记录.md) | DR-006：价格只可人工确认；DR-007：审批权限按组织归属 |
| 业务规则总览 | [业务规则总览.md](./业务规则总览.md) | §6 条款 §9.2 ①-⑤ 5 条与本文件映射 |
| MOQ 最小起订量 | [MOQ最小起订量.md](./MOQ最小起订量.md) | MOQ 豁免加价 → 叠加 ③ 改价 / ④ 低于成本价场景 |
| 信用控制规则 | [信用控制规则.md](./信用控制规则.md) | CustomerTier.discountRate → 条件 ① 折扣阈值的 Tier 权益豁免 |
| 订单变更规则 | [订单变更规则.md](./订单变更规则.md) | ③ 改价审批与 5 类变更审批链合并（避免两遍审批） |
| 10 条事件联动与事件总线 | [10条事件联动（L1-L10）与事件总线.md](./10条事件联动（L1-L10）与事件总线.md) | ApprovalRequest status=approved → 触发 L2 OrderConfirmed 事件 |
| 数据模型总览 | [02-数据模型/实体关系总览.md](../02-数据模型/实体关系总览.md) | ApprovalRequest / Quotation（双轨 6 字段）/ CostEstimate / CustomerTier 关联 |
| 订单详情页 | [04-模块设计/订单管理/订单详情页.md](../04-模块设计/订单管理/订单详情页.md) | 双轨偏差 badge（红/黄）/ 审批挂起态 / 低于成本价高风险告警条 |
| 模块设计-定价与成本 | [04-模块设计/03-订单与生产/Pricing-定价与成本/偏差校验与审批链.md](../04-模块设计/03-订单与生产/Pricing-定价与成本/偏差校验与审批链.md) | 轨道 A/B 定义、calculatePriceDeviation 算法、审批 UI 交互细节 |
