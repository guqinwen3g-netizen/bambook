# 实体关系总览

---

## §1 元信息

| 项目 | 内容 |
|------|------|
| 定位 | 设计真源之数据层入口；120+ Prisma 模型 → 10 组模型组文档的顶层聚合索引与跨组联动真源（v4 核对：实际 120 模型已落地） |
| 入口 | docs/design/02-数据模型/实体关系总览.md（本文件） |
| 代码关联 | server/prisma/schema.prisma（数据模型真源，所有模型定义与 FK/唯一约束/索引以该文件为准） |
| PRD 关联 | PRD §8 数据模型 / §业务十条联动规则 |
| 范式 | Postgres 第三范式 + JSON 扩展字段（字段级 provenance / 明细行 JSON blob）；金额统一 Decimal(18,4)，时间戳 BigInt epoch ms，业务日期 String YYYY-MM-DD |
| 实现状态 | ✅ 模型 Schema 已完整（120 模型，v4 核对落地）；10 份模型组文档已完成分域撰写 |

---

## §2 10 组模型分组索引

| 组号 | 模型组文档 | 覆盖模型数 | 覆盖域 | 文档链接 |
|------|-----------|-----------|--------|---------|
| 1 | 订单与生产模型组.md | 8+ | Order / OrderLine / OrderStatusTransition / DevelopmentCase / SampleNode / ProductionStage / PreCutChecklist / InspectionReport + OutsourcingOrder | [订单与生产模型组.md](./订单与生产模型组.md) |
| 2 | 产品与档案模型组.md | 18+ | ProductAsset / ProductSubCategory / FabricProfile / GarmentProfile / TrimmingProfile / ProductClassification / FabricCompositionLine / MaterialCompositionTerm / FabricCustomerCode / FabricPriceHistory / FabricCertification / PdmlRawFabric / Insight / ProductImage + LookbookCatalog / FabricRecommendation | [产品与档案模型组.md](./产品与档案模型组.md) |
| 3 | 报价与成本模型组.md | 7 | Quotation / QuotationLine / TaxRefundRate / PricingCalculation / CostEstimate / MaterialPriceHistory / OrderProfitSheet / CommissionRule | [报价与成本模型组.md](./报价与成本模型组.md) |
| 4 | 采购与库存模型组.md | 11 | PurchaseOrder / PurchaseLine / MaterialReceipt + Warehouse / InventoryItem / StockMovement + WorkStation / ProductionPlan / WorkHour / PieceRateRule / PieceRateRecord | [采购与库存模型组.md](./采购与库存模型组.md) |
| 5 | 财务域模型组.md | 9 | Invoice / PaymentVoucher / InvoiceAllocation + FxSettlement / OutwardRemittance / VatInvoice + CommissionRule（复用）/ Statement / Payout | [财务域模型组.md](./财务域模型组.md) |
| 6 | 出运与报关模型组.md | 9+ | Shipment / ShipmentLine / ShipmentEvent / ShipmentCarton / ShipmentCartonItem + LetterOfCredit / CustomsDeclaration / TaxRefundDecl + DocumentTemplate / DocumentVersion | [出运与报关模型组.md](./出运与报关模型组.md) |
| 7 | 客户与关系模型组.md | 7+ | Relation / Contact / CreditLimit / FollowUpRecord / Opportunity / CustomerTier + EntityReference / EntityAlias / EntityLink + KnowledgeDocument / KnowledgeChunk / KnowledgeRelation / KnowledgeAcl + SopTemplate | [客户与关系模型组.md](./客户与关系模型组.md) |
| 8 | 底座域模型组.md | 16+ | AuditLog / ProjectMemory / BusinessProfile / KnowledgeItem / SystemAsset + UserAccount / Department / Role / UserRole / Permission / RolePermission / AgentPolicy + JobPosition / Team / TeamMember / Project / ProjectMember / WorkAssignment + EmployeeProfile / EmploymentEvent / AttendanceRecord / LeaveRequest + SalaryStructure / PayrollRun / PayrollItem + PerformanceCycle / PerformanceReview + TrainingCourse / TrainingEnrollment + BusinessSequence + ApprovalRequest + WorkflowDefinition / WorkflowInstance / WorkflowStep | [底座域模型组.md](./底座域模型组.md) |
| 9 | 通知邮件季节模型组.md | 10+ | Notification / NotificationTemplate / NotificationPreference + Email / EmailAttachment / EmailTemplate + Season / TrendTag / TrendTagFabric / TradeShow / TradeShowLead + EmailSignature + ExchangeRate / FxRateLock / CreditRating / ComplianceCheck / RiskAlert + BusinessLine / QCLocation / QCAssignment | [通知邮件季节模型组.md](./通知邮件季节模型组.md) |
| 10 | MES 深化模型组.md | 6 | WorkStation / ProductionPlan / WorkHour / PieceRateRule / PieceRateRecord + OutsourcingOrder / OutsourcingLine + FactoryProfile / FactoryEvaluation / FactoryCertification / FactoryCapacity + BOM / BOMLine / CostEstimate | [MES 深化模型组.md](./MES 深化模型组.md) |
| **合计** | | **~120**（v4 核对：schema 实际 120 模型已落地，含 v4 新增的 CustomsDeclarationLine / HsCode / LcEvent / TradeDocument / LetterOfCredit / TaxRefund / BusinessSequence / SequenceRegister / VoidedNumber / ReportDefinition / ReportRun / DirtyCacheMarker / OutsourcingLine） | | |

---

## §3 区块逐块说明（10 组核心模型字段）

### 3.1 订单与生产模型组（组 1）

#### 核心模型 1：Order

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| code | String? | | | ✅ | 订单编号（SO-202608-001，由 SequenceRegister 分配） |
| poNumber | String? | | | ✅ | 客户 PO 号（导入去重键） |
| customer | String | | | | 客户名（冗余快照，自 customerRelationId 派生） |
| customerRelationId | String? | | 🔗 Relation | | 客户 FK（snapshot，不强制级联） |
| millRelationId | String? | | 🔗 Relation | | 面料工厂 FK（snapshot） |
| billToRelationId | String? | | 🔗 Relation | | 结款方 FK（snapshot） |
| ownerId | String? | | 🔗 UserAccount | | 归属人（行级权限 dataScope） |
| departmentId | String? | | 🔗 Department | | 归属部门 |
| type | String | | | | Order.type ∈ { Fabric, Garment, Other }（3值主分类） |
| status | String | | | | 订单状态（见 §5 状态机） |
| businessLine | String? | | | | businessLine ∈ { garment, capsule, fabric, other }（4值业务线标签）；capsule 仅对 type=Garment 生效，作为子类型透镜 |
| quantity | Int | | | | 订单总数量 |
| dueDate | String | | | | 交期（YYYY-MM-DD） |
| quoteAmount | Decimal(18,4) | | | | 报价金额 |
| salesPrice / purchasePrice | Decimal? | | | | 销售/采购单价（Decimal-first） |
| createdAt | DateTime | | | | 创建时间（补回，便于排序审计） |
| **约束** | | | | | @@index([status, code, ownerId, departmentId, customerCode, millName, businessLine, createdAt]) |

#### 核心模型 2：OrderLine

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| orderId | String | | 🔗 Order(→Cascade) | ✅(复合) | 关联订单头 |
| lineNumber | Int | | | | 行号 |
| itemNo | String? | | | ✅(与 orderId) | 客户行号（复合唯一） |
| materialCode / styleNo | String? | | | | 面料编码 / 成衣款式号 |
| quantity | Decimal(18,4) | | | | 行数量（面料含小数） |
| unitPrice / netValue | Decimal? | | | | 单价 / 行金额 |
| status | String? | | | | Pending / Production / Shipped 等行级状态 |
| sizeBreakdown / productionSteps / bomItems | Json? | | | | 成衣专属扩展（尺码/工序/BOM） |

#### 核心模型 3：ProductionStage（10 阶段门禁）

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | PST__${orderId}__${stageKey} |
| orderId | String | | 🔗 Order | ✅(复合) | 关联订单 |
| stageKey | String | | | ✅(与 orderId) | order_placed → qc_shipped（共 10 键） |
| stageSeq | Int | | | | 1-10 序号 |
| status | String | | | | pending / in_progress / done / blocked |
| signedByProduction / signedByBusiness | String? | | | | ⑦ 产前样双签（PP）门禁 |
| **约束** | | | | | @@unique([orderId, stageKey]) |

#### 核心模型 4：InspectionReport

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| orderId | String | | 🔗 Order | ✅(复合) | 关联订单 |
| inspectionType | String | | | ✅(与 orderId) | midline（中期）/ final（终期） |
| criticalDefects / majorDefects / minorDefects | Int | | | | 三级疵点计数（critical 零容忍） |
| passRate / defectRate | 派生 | | | | ⑩ qc_shipped 门禁：≥90% + ≤3% + 业务批准 |
| result | String? | | | | pass / conditional / fail |
| approvedByBusiness | Boolean | | | | 业务部批准开关 |

#### 核心模型 5：PreCutChecklist

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | PCL__${orderId} |
| orderId | String | | 🔗 Order | ✅ | 1:1 订单 |
| gradingConfirmed / consumptionConfirmed / patternConfirmed / preProductionMeeting | Boolean | | | | ⑥ pre_cut_checked 门禁 4 项全真 |

---

### 3.2 产品与档案模型组（组 2）

#### 核心模型 1：ProductAsset（三档档案统一头）

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| sku | String | | | ✅ | 档案 SKU（面料/成衣/辅料统一编码） |
| mainCategory | String | | | | fabric / garment / trimming |
| subCategoryId | String | | 🔗 ProductSubCategory | | 细分类 |
| season | String | | | | 季节（SS26 / AW26 等值匹配 Season.code） |
| cost | Decimal(18,4) | | | | 档案成本 |
| status | String | | | | active / archived |

#### 核心模型 2：FabricProfile

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| productAssetId | String | | 🔗 ProductAsset(Cascade) | ✅ | 1:1 档案头 |
| millQuality / articleNo / construction / yarnCount | String? | | | | 面料品质 / 款号 / 组织 / 纱支 |
| weightValue / widthValue | Float? | | | | 克重 / 门幅 |
| moqValue / factoryMoqValue / sampleMoqValue | Float? | | | | MOQ 三档（§9.1 校验基准） |

#### 核心模型 3：GarmentProfile

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| productAssetId | String | | 🔗 ProductAsset(Cascade) | ✅ | 1:1 档案头 |
| styleNo | String? | | | | 成衣款号（索引） |
| customerRelationId / factoryRelationId | String? | | 🔗 Relation | | 客户 / 工厂（snapshot） |
| mainFabric / liningFabric / zipper / sizeSpec | String? | | | | BOM 相关字段（待迁移至独立 BOM 模型） |

---

### 3.3 报价与成本模型组（组 3）

#### 核心模型 1：Quotation

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| quotationNumber | String | | | ✅ | QT-YYYYMMDD-001 |
| status | String | | | | Draft→Sent→Accepted/Rejected/Expired（见 §5） |
| currency / totalAmount | + Decimal(18,4) | | | | 币种 / 总价 |
| trackAMedianUsd / trackBFinalUsd | Decimal? | | | | 双轨定价快照（§9.2 偏差校验） |
| priceDeviationPercent / priceDeviationLevel | + String? | | | | 偏差 % / ok/warn/block（>15% 审批 >30% 阻断） |
| priceApprovalId | String? | | 🔗 ApprovalRequest | | 偏差审批关联 |
| customerRelationId | String? | | 🔗 Relation | | 客户（snapshot） |
| convertedOrderId | String? | | 🔗 Order | | Accepted→转订单 |
| ownerId / departmentId | String? | | | | 行级权限 |

#### 核心模型 2：PricingCalculation（轨道 B 退税定价）

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| purchaseCostCny / refundRate / exchangeRate | Decimal | | | | 输入：采购成本 / 退税率 / 汇率 |
| netUsdCost / profitAmount / finalUnitPrice | Decimal | | | | 派生（服务端重算，禁客户端改） |
| orderId / quotationId / productAssetId | String? | | | | 关联（snapshot FK） |

#### 核心模型 3：OrderProfitSheet

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| orderId | String | | 🔗 Order | ✅ | 一单一张（幂等真源） |
| salesRevenue / purchaseCost / freightCost / miscCost | Decimal(18,4) | | | | 聚合口径（CNY 本位币） |
| grossProfit / grossMargin | Decimal(?) | | | | 毛利额 / 毛利率 % |
| details | Json | | | | 明细数组 + unconverted 披露 |

---

### 3.4 采购与库存模型组（组 4）

#### 核心模型 1：PurchaseOrder

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| poNumber | String | | | ✅ | PO-YYYYMMDD-001 |
| status | String | | | | Draft→Sent→Confirmed→PartiallyReceived→Received→Closed |
| supplierRelationId | String? | | 🔗 Relation | | 供应商（snapshot） |
| orderId | String? | | 🔗 Order | | 关联销售订单（BOM 触发采购） |
| totalAmount / currency | Decimal / String | | | | 采购金额 |

#### 核心模型 2：MaterialReceipt

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| purchaseOrderId | String | | 🔗 PurchaseOrder(Cascade) | | 关联 PO |
| status | String | | | | Pending→Inspected→Accepted→Rejected→PartiallyAccepted |
| totalReceived / totalAccepted / totalRejected | Decimal(18,4) | | | | 收/合格/不合格数量 |
| warehouseId | String? | | 🔗 Warehouse | | 入库仓库 |

#### 核心模型 3：InventoryItem

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| warehouseId | String | | 🔗 Warehouse | | 仓库 |
| productAssetId / materialCode | String? | | | | 关联档案 / 独立物料编码 |
| quantity / lockedQuantity | Decimal(18,4) | | | | 当前库存 / 锁定预留 |
| minStock / maxStock | Decimal? | | | | 低库存/高库存预警线 |

#### 核心模型 4：StockMovement

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| movementNumber | String | | | | 流水单号 |
| type | String | | | | Inbound/Outbound/Transfer/Adjustment/Lock/Unlock |
| itemId | String | | 🔗 InventoryItem | | 关联库存项 |
| referenceType / referenceId | String? | | | | 来源追溯（PO/Order/Shipment 等） |
| balanceBefore / balanceAfter | Decimal(18,4) | | | | 变动前后余额（审计链） |

---

### 3.5 财务域模型组（组 5）

#### 核心模型 1：Invoice

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| invoiceNumber | String | | | ✅ | INV-YYYYMMDD-001 |
| type | String | | | | Receivable（应收）/ Payable（应付） |
| status | String | | | | Draft→Issued→PartiallyPaid→Paid/Cancelled（§5） |
| amount / currency / issueDate / dueDate | + | | | | 金额 / 币种 / 开票日 / 到期日 |
| exchangeRate | Decimal? | | | | 开票当日汇率快照 |
| orderId / customerRelationId | String? | | | | 关联订单 / 客户（snapshot） |

#### 核心模型 2：PaymentVoucher

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| voucherNumber | String | | | ✅ | PAY-YYYYMMDD-001 |
| type | String | | | | Receipt（收款）/ Disbursement（付款） |
| amount / paymentDate / paymentMethod | + | | | | 金额 / 收付日 / 方式 |
| status | String | | | | unreconciled / partially_reconciled / reconciled |
| invoiceId / appliedAmount | + Decimal? | | | | 主核销 1:1（向后兼容快照） |
| orderId / customerRelationId | String? | | | | 关联订单 / 客户 |

#### 核心模型 3：InvoiceAllocation

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| invoiceId | String | | 🔗 Invoice | ✅(复合) | 核销发票 |
| voucherId | String | | 🔗 PaymentVoucher | ✅(与 invoiceId) | 核销凭证 |
| appliedAmount | Decimal(18,4) | | | | 本笔核销金额（部分核销支持） |

---

### 3.6 出运与报关模型组（组 6）

#### 核心模型 1：Shipment

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| shipmentNumber | String | | | ✅ | SHP-YYYYMMDD-001 |
| type / status | String | | | | Export/Import/Domestic + 8 状态（§5） |
| shippingMethod | String | | | | Sea/Air/Land/Rail/Courier |
| etd / atd / eta / ata | String? | | | | 预计/实际离港到港日 |
| orderId / customerRelationId / carrierRelationId | String? | | | | 关联订单/客户/货代 |
| grossWeight / netWeight / volume | Decimal(12,3)? | | | | 重量体积（kg / CBM，精度到克/cm³） |

#### 核心模型 2：ShipmentLine

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| shipmentId | String | | 🔗 Shipment | | 所属运单 |
| orderLineId | String? | | 🔗 OrderLine | | loadedWith 关联订单明细行 |
| quantity / unit | Decimal? / String? | | | | 发运数量（Decimal，含小数） |

#### 核心模型 3：ShipmentEvent

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| shipmentId | String | | 🔗 Shipment | | 运单 |
| fromNode / toNode | String? / String | | | | 物流节点（与 Shipment.status 一致） |
| eventDate | String | | | | 节点业务日期（append-only 时间轴） |

#### v4 核对补充模型（schema 已落地，本节扩展清单）

> **v4 审查补充（2026-08-15）**：以下 9 个模型在 `server/prisma/schema.prisma` 中已完整定义，本节原仅列 3 个核心代表，现补充清单指引，详细字段见 [出运与报关模型组.md](./出运与报关模型组.md)。

| 模型 | schema 行号 | 主键 | 关键 FK | 业务定位 |
|------|------------|------|---------|---------|
| ShipmentCarton | 2376 | SHPC__${shortId} | shipmentId | 逐箱装箱明细（C4 发货深化），cartonNo + 三尺寸 + 重量体积 |
| ShipmentCartonItem | 2401 | SHPCI__${shortId} | cartonId(@relation Cascade) + shipmentLineId(snapshot) | 箱内货物分配（混装支持），@@unique([cartonId, shipmentLineId]) |
| CustomsDeclaration | 4288 | CD__${shortId} | shipmentId / orderId / relationId（snapshot） | 报关单主表，declarationNumber @unique，7 状态机 Draft→Submitted→Declared→Inspecting→Released→Exception→Cancelled |
| CustomsDeclarationLine | 4338 | CDL__${shortId} | declarationId(@relation Cascade) | 报关商品行，hsCode / 品牌 / 规格型号 / 数量价格 / 重量 / 产地 |
| HsCode | 4375 | HS__${shortId} | — | HS 编码字典（code @unique），含出口退税率/进口关税率/增值税率/监管条件 |
| LetterOfCredit | 4404 | LC__${shortId} | relationId / orderId（snapshot） | 信用证主表，lcNumber @unique，7 状态机 Issued→Presented→Accepted→Discrepant→Settled→Expired→Cancelled |
| LcEvent | 4463 | LCE__${shortId} | lcId | 信用证节点事件（append-only 时间轴），与 LetterOfCredit.status 一致 |
| TaxRefund | 4481 | TR__${shortId} | declarationId / orderId / relationId（snapshot） | 退税申报主表（**注**：schema 实际命名为 `TaxRefund`，原文档 §14 GAP-DM-003 误称 `TaxRefundDecl`），refundNumber @unique，7 状态机 Draft→Submitted→Reviewing→Approved→Rejected→Refunded→Cancelled |
| TradeDocument | 4522 | TD__${shortId} | shipmentId / declarationId / orderId / relationId（snapshot） | 单据主模型（GAP-DM-001 已修复），documentNumber @unique，9 类型 CI/PL/CO/BL/AWB/InsuranceCert/InspectionCert/PhytosanitaryCert/Other |

---

### 3.7 客户与关系模型组（组 7）

#### 核心模型 1：Relation（组织+联系人统一身份）

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| code | String? | | | ✅ | 业务编码（CUS-0001 / SUP-0001） |
| category | String | | | | Customer / Supplier / Carrier / ... |
| type | String | | | | Organization / Person |
| isOrganization | Boolean | | | | 组织/个人标记 |
| stage | String? | | | | Lead→Opportunity→Quotation→TrialOrder→Customer→Key→Churned |
| tier / ownerId / departmentId | + | | | | 客户等级 / 归属（行级权限） |

#### 核心模型 2：Contact（多联系人）

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键（CTC_xxx） |
| relationId | String | | 🔗 Relation(Cascade) | | 所属组织 |
| name / title / department | String / String? | | | | 姓名/职位/部门 |
| isPrimary / isDecisionMaker | Boolean | | | | 主联系人 / 决策人标记 |

#### 核心模型 3：Opportunity（销售管线）

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键（OPP_xxx） |
| relationId | String | | 🔗 Relation | | 客户 |
| stage | String | | | | Prospecting→Qualification→Proposal→Negotiation→ClosedWon/ClosedLost |
| amount / probability | Decimal / Int | | | | 预计金额 / 成交概率 % |
| orderId | String? | | 🔗 Order | | ClosedWon→关联订单 |

---

### 3.8 底座域模型组（组 8）

#### 核心模型 1：UserAccount

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| displayName | String | | | | 显示名 |
| email | String? | | | ✅ | 登录邮箱（唯一） |
| status | String | | | | active / disabled |
| primaryDeptId | String? | | 🔗 Department | | 主部门 |

#### 核心模型 2：Role + UserRole + Permission + RolePermission

| 模型 | 主键 | 关键 FK | 唯一约束 |
|------|------|--------|---------|
| Role | id | — | — |
| Permission | id | — | scope(✅ 唯一) |
| UserRole | id | userId(🔗User) + roleId(🔗Role) + departmentId | @@unique([userId, roleId, departmentId]) |
| RolePermission | id | roleId(🔗Role) + permissionId(🔗Permission) | @@unique([roleId, permissionId]) |

#### 核心模型 3：ApprovalRequest

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| requesterId / reviewerId | String | | 🔗 UserAccount | | 请求人 / 审核人 |
| actionType / targetType / targetId | String? | | | | 动作类型 + 目标实体 |
| status | String | | | | pending / approved / rejected |
| risk | String | | | | high / medium / low（默认 high，fail closed） |

#### v4 核对补充模型（编号注册 + 报表引擎 + 缓存脏标记，schema 已落地）

> **v4 审查补充（2026-08-15）**：以下 6 个底座域模型在 `server/prisma/schema.prisma` 中已完整定义，但原 §3.8 未列出，现补充清单指引。

| 模型 | schema 行号 | 主键 | 关键约束 | 业务定位 |
|------|------------|------|---------|---------|
| BusinessSequence | 4636 | SEQ_${prefix}_${year} | @@unique([prefix, year]) | 业务编号序列（GAP-DM-006 已修复）：prefix + year + seq，事务内原子取号 |
| SequenceRegister | 4652 | SEQREG_${seqType}_${periodKey} | @@unique([seqType, periodKey]) | Phase 0-03 升级版发号器：11 类业务单据（order/pi/quotation/purchase/invoice/voucher/shipment/customs/customer/supplier/material）独立序列，支持 year/month/day/none 重置周期 + formatTemplate + padding |
| VoidedNumber | 4676 | VOID_${seqType}_${number} | @@unique([seqType, number]) | 作废单号占坑表：显式追踪不复用，含 voidedBy / voidedAt / reason / sourceDocId / sourceDocType |
| ReportDefinition | 4581 | RPD__${shortId} | datasetKey + enabled | 报表定义：datasetKey（白名单）+ dimensions + metrics + filters + schedule（daily/weekly/monthly/null）+ idempotencyKey |
| ReportRun | 4606 | RPR__${shortId} | definitionId + idempotencyKey @unique | 报表运行快照：status(Running/Success/Failed) + trigger(manual/schedule) + rows（上限 5000 行）+ columnLabels（CSV 导出表头） |
| DirtyCacheMarker | ~4697 | — | — | 缓存脏标记表：Order/Invoice/Payment 变更时标记衍生指标（Dashboard 汇总/ReportCenter 报表/客户账龄/库存毛利预估）待重算 |

---

### 3.9 通知邮件季节模型组（组 9）

#### 核心模型 1：Notification

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| userId | String | | 🔗 UserAccount | | 接收人 |
| type | String | | | | order_confirmed / shipment_completed 等 |
| level | String | | | | info / warning / critical |
| dismissedAt / dismissReason | + String? | | | | PRD 7.1「忽略需填原因」 |

#### 核心模型 2：Email

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| messageId | String? | | | ✅ | RFC 2822 Message-ID |
| direction / status | String | | | | inbound / outbound / draft + 6 状态 |
| relationId / orderId / invoiceId | String? | | | | 关联联系人/订单/发票（snapshot） |
| aiExtractedJson | Json? | | | | Phase 4b AI 结构化抽取 |

#### 核心模型 3：Season

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| code | String | | | ✅ | SS26 / AW26（与 Order.season 等值匹配） |
| calendar | Json? | | | | 开发日历里程碑（6 节点） |
| reviewJson | Json? | | | | 季度回顾快照（接单/出货/毛利/客户分析） |

---

### 3.10 MES 深化模型组（组 10）

#### 核心模型 1：BOM（物料清单）

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| bomNumber | String | | | ✅ | BOM-YYYYMMDD-001 |
| status | String | | | | Draft→Confirmed→Archived |
| productAssetId / orderId / quotationId | String? | | | | 关联档案/订单/报价（snapshot） |
| totalMaterialCost / totalLaborCost / totalOverheadCost | Decimal(18,4) | | | | 料工费汇总 |

#### 核心模型 2：OutsourcingOrder（外协加工）

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| orderNumber | String | | | ✅ | OSO-YYYYMMDD-001 |
| supplierId / orderId / bomId | String? | | | | 供应商/订单/BOM（snapshot） |
| processType | String | | | | Sewing / Washing / Printing / ... |
| status | String | | | | Draft→Sent→Confirmed→InProduction→Received→Cancelled |

#### 核心模型 3：FactoryProfile（工厂档案）

| 字段 | 类型 | PK | FK | 唯一 | 说明 |
|------|------|----|----|------|------|
| id | String | ✅ | | | 主键 |
| relationId | String | | 🔗 Relation(Cascade) | ✅ | 1:1 挂 Relation（供应商身份） |
| qualityScore / deliveryScore | Float | | | | 质量/交期评分缓存（真源 FactoryEvaluation） |
| blacklistedAt / blacklistReason | + | | | | 拉黑标记（新建采购单禁选） |

---

## §4 模式切换

N/A（数据模型层无查阅/编辑等前端模式切换，模式切换归各模块交互文档 §4）

---

## §5 状态机（核心业务模型 draft→final 生命周期）

### 5.1 Quotation 报价单

| 从状态 \ 到状态 | Draft | Sent | Accepted | Rejected | Expired |
|----------------|-------|------|----------|----------|---------|
| Draft | — | ✅ 手动发送 | ❌ | ❌ | ❌ |
| Sent | ❌ | — | ✅ 客户接受 | ✅ 客户拒绝 | ✅ 超 validUntil 自动 |
| Accepted | ❌ | ❌ | —（终态，可转 Order） | ❌ | ❌ |
| Rejected / Expired | ❌ | ❌ | ❌ | 终态 | 终态 |

**门禁**：Draft→Sent 前校验 priceDeviationLevel=block 时必须 priceApprovalId 已 Approved（§9.2 ⑤）。

### 5.2 Order 订单

| 从状态 \ 到状态 | Draft | Confirmed | Production | Shipping | Delivered | Closed | Cancelled |
|----------------|-------|-----------|------------|----------|-----------|--------|-----------|
| Draft | — | ✅ 确认 | ❌ | ❌ | ❌ | ❌ | ✅ |
| Confirmed | ❌ | — | ✅ ProductionStage ①→③ 推进 | ❌ | ❌ | ❌ | ✅ 允许 |
| Production | ❌ | ❌ | — | ✅ ⑩ qc_shipped 通过 | ❌ | ❌ | ❌ |
| Shipping | ❌ | ❌ | ❌ | — | ✅ Shipment Delivered | ❌ | ❌ |
| Delivered | ❌ | ❌ | ❌ | ❌ | — | ✅ Invoice Paid | ❌ |
| Closed / Cancelled | 终态 | | | | | 终态 | |

**门禁**：
- Production→Shipping 需 InspectionReport final=pass + approvedByBusiness=true
- 交期锁死：Shipping/Delivered 后 dueDate 只读（§9.7 ⑤ 待接）

### 5.3 Shipment 运单

| 从→到 | Draft | Booked | Loading | Shipped | Arrived | Cleared | Delivered | Cancelled |
|-------|-------|--------|---------|---------|---------|---------|-----------|-----------|
| Draft | — | ✅ 订舱 | ✅ | ❌ | ❌ | ❌ | ❌ | ✅ |
| Booked | ❌ | — | ✅ 装货中 | ✅ 发运 | ❌ | ❌ | ❌ | ✅ |
| Loading | ❌ | ❌ | — | ✅ | ❌ | ❌ | ❌ | ❌ |
| Shipped | ❌ | ❌ | ❌ | — | ✅ 到港 | ❌ | ❌ | ❌ |
| Arrived | ❌ | ❌ | ❌ | ❌ | — | ✅ 清关 | ❌ | ❌ |
| Cleared | ❌ | ❌ | ❌ | ❌ | ❌ | — | ✅ 交付 | ❌ |
| Delivered/Cancelled | 终态 | | | | | | 终态 | |

### 5.4 Invoice 发票

| 从→到 | Draft | Issued | PartiallyPaid | Paid | Cancelled |
|-------|-------|--------|---------------|------|-----------|
| Draft | — | ✅ 开票 | ❌ | ❌ | ✅ |
| Issued | ❌ | — | ✅ 部分核销 | ✅ 全核销 | ❌ |
| PartiallyPaid | ❌ | ❌ | — | ✅ 全核销 | ❌ |
| Paid / Cancelled | 终态 | | | 终态 | 终态 |

### 5.5 PurchaseOrder 采购单

| 从→到 | Draft | Sent | Confirmed | PartiallyReceived | Received | Closed | Cancelled |
|-------|-------|------|-----------|-------------------|----------|--------|-----------|
| Draft | — | ✅ | ❌ | ❌ | ❌ | ❌ | ✅ |
| Sent | ❌ | — | ✅ 供应商确认 | ❌ | ❌ | ❌ | ✅ |
| Confirmed | ❌ | ❌ | — | ✅ 部分到货 | ✅ 全到货 | ❌ | ❌ |
| PartiallyReceived | ❌ | ❌ | ❌ | — | ✅ 全到货 | ❌ | ❌ |
| Received | ❌ | ❌ | ❌ | ❌ | — | ✅ 结算关单 | ❌ |
| Closed/Cancelled | 终态 | | | | | 终态 | |

---

## §6 ER 说明（数据模型类替代 §6 字段清单）

### 6.1 ASCII ER 概览（10 组关系）

> **v4 更新（2026-08-15）**：补充 schema 已落地但原图遗漏的模型（TaxRefund / CustomsDeclaration / LetterOfCredit / TradeDocument / CustomsDeclarationLine / HsCode / LcEvent / SequenceRegister / VoidedNumber / ReportDefinition / ReportRun / DirtyCacheMarker）。

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              底座域 (User/Role/Dept/Audit)                           │
│  UserAccount ── UserRole ── Role ── RolePermission ── Permission                   │
│       │            │                                                               │
│       │            └─ Department (自关联 parentId)                                 │
│       └─ ApprovalRequest ─ WorkflowDefinition/Instance/Step                        │
│           └─ AuditLog + BusinessSequence / SequenceRegister / VoidedNumber         │
│           └─ ReportDefinition ─ ReportRun + DirtyCacheMarker                       │
└──────────────────────────────────────┬──────────────────────────────────────────────┘
                                       │ 归属 (ownerId / departmentId)
          ┌────────────────────────────┴────────────────────────────┐
          ▼                                                         ▼
┌──────────────────────┐            客户与关系域            ┌───────────────────────┐
│   报价与成本域        │    Relation (customer/supplier)    │   通知邮件季节域       │
│  Quotation ── Line   │      │   │   │                     │  Notification / Email  │
│       │              │      │   │   └── Contact           │  Season / TradeShow    │
│       └── PricingCalc│      │   └── Opportunity           │  RiskAlert / ExchRate  │
│       OrderProfitSheet     │   └── FollowUpRecord         │  CreditRating / ComplianceCheck │
└──────┬───────────────┘      └── FactoryProfile(供应商)               │
       │ convertedOrderId          └─ Evaluation / Cert / Capacity     │
       ▼                                                              │
┌──────────────────────────────────────────────────────────────────────┴───────────┐
│                           订单与生产域 (核心业务轴)                                 │
│  Order ─── OrderLine ─── ProductionStage(10) ─── PreCutChecklist                  │
│    │          │              │                                                     │
│    │          │              └── InspectionReport (midline/final)                  │
│    │          │                                                                    │
│    │          └── loadedWith ── ShipmentLine ── Shipment ── ShipmentEvent          │
│    │                                        │                                      │
│    │                                        ├── ShipmentCarton ─ ShipmentCartonItem│
│    │                                        ├── CustomsDeclaration ─ Line          │
│    │                                        │     └── HsCode                       │
│    │                                        └── TradeDocument (CI/PL/CO/BL...)    │
│    │ aboutOrder                                  └── DocumentTemplate / Version   │
│    └──────────────────────── Invoice ── PaymentVoucher ── InvoiceAllocation        │
│                                     │                                               │
│                                     ├── FxSettlement (Receipt 结汇)                │
│                                     ├── OutwardRemittance (Disbursement 付汇)      │
│                                     └── VatInvoice ── TaxRefund                    │
│                                                       ↑                            │
│                                            LetterOfCredit ─ LcEvent                │
└──────┬───────────────────────┬──────────────────────────────────────┬──────────────┘
       │                       │                                      │
       ▼                       ▼                                      ▼
┌──────────────────┐   ┌──────────────────┐                 ┌──────────────────────┐
│  采购与库存域     │   │  产品与档案域      │                 │    MES 深化域         │
│ PurchaseOrder    │   │ ProductAsset(头)  │                 │ BOM ─ BOMLine        │
│   └─ PurchaseLine│   │ FabricProfile     │                 │   └─ CostEstimate    │
│   └─ MaterialRcpt│   │ GarmentProfile    │                 │ WorkStation          │
│       │          │   │ TrimmingProfile   │                 │   └─ ProductionPlan  │
│       ▼          │   │ PriceHistory/Cert │                 │      └─ WorkHour     │
│  Warehouse       │   │ PdmlRawFabric     │                 │ PieceRateRule/Record │
│    └─ InventoryItem ── StockMovement     │                 │ OutsourcingOrder ─ Line │
└──────────────────┘   └───────────────────┘                 └──────────────────────┘
          出运与报关域 (Shipment→Customs→TaxRefund + LetterOfCredit + TradeDocument)
          财务域 (Invoice→Voucher→Allocation + FxSettlement + OutwardRemittance + VatInvoice)
```

> **新增模型落点说明**：
> - **底座域**：BusinessSequence / SequenceRegister / VoidedNumber（编号注册三件套）+ ReportDefinition / ReportRun（报表引擎）+ DirtyCacheMarker（缓存脏标记）已落地但原 ASCII 图遗漏
> - **出运与报关域**：CustomsDeclaration + CustomsDeclarationLine + HsCode + TradeDocument + DocumentTemplate + DocumentVersion + LetterOfCredit + LcEvent + TaxRefund 全部已落地
> - **客户与关系域**：FactoryEvaluation + FactoryCertification + FactoryCapacity（工厂档案附属三模型）显式化

### 6.2 14 组联动关系表

| # | 联动名称 | 主模型 → 从模型 | 触发条件 / 业务语义 | 关键 FK 字段 | 门禁条件 |
|---|---------|---------------|--------------------|-------------|---------|
| L-01 | 订单头→明细行 | Order → OrderLine | 创建订单时录入明细行（1:N） | orderId | onDelete: Cascade |
| L-02 | 订单→10 阶段生产管线 | Order → ProductionStage×10 | Order.Confirmed 时自动插入 10 行（status=pending） | orderId | 按 stageSeq 顺序推进 |
| L-03 | 阶段⑥→开裁前检查清单 | ProductionStage(pre_cut_checked) → PreCutChecklist | 进入阶段⑥时校验 checklist 4 项全 true | orderId(@unique) | 全 true 才允许 stage 进入 done |
| L-04 | 阶段⑨⑩→验货报告 | ProductionStage(final_review / qc_shipped) → InspectionReport | 阶段⑨=中期验货 / ⑩=终期验货，各一份（midline/final 复合唯一） | orderId + inspectionType | ⑩需 passRate≥90% + defectRate≤3% + approvedByBusiness |
| L-05 | 验货通过→运单装载 | InspectionReport(final=pass) → Shipment → ShipmentLine | 终期验货通过后可建运单；ShipmentLine.loadedWith=OrderLine | orderLineId | ShipmentLine.quantity ≤ OrderLine.quantity - Σ已装运 |
| L-06 | 运单→应收/应付发票 | Shipment(Shipped) → Invoice(type by businessSide) | 发运后开票：出给客户=Receivable，出给工厂=Payable | orderId + customerRelationId | Invoice.amount = Σ ShipmentLine × OrderLine.unitPrice |
| L-07 | 发票→收/付款凭证→核销 | Invoice → PaymentVoucher → InvoiceAllocation | 收到款/付款后建 Voucher；1 Voucher 可核销 N Invoice，1 Invoice 可被 N Voucher 核销（M:N via Allocation） | invoiceId + voucherId(复合唯一) | Σ appliedAmount ≤ 双方各自金额 |
| L-08 | 外币凭证→结汇/付汇水单 | PaymentVoucher(Receipt外币) → FxSettlement；(Disbursement外币) → OutwardRemittance | 银行结/购汇后水单录入；1 Voucher : N Settlement | voucherId | Σ foreignAmount ≤ voucher.amount |
| L-09 | 报价单→订单（接单转化） | Quotation(status=Accepted) → Order + OrderLine | 客户接受报价后一键转单；复制行项 + customer + currency + terms | convertedOrderId | 仅 Accepted 状态允许；转单后 Quotation 终态 |
| L-10 | 开发案→订单（打样转大货） | DevelopmentCase(stage=approved) + SampleNode(PP=approved) → Order | 产前样批准后转大货订单；linkedOrderId 回填 | linkedOrderId | SampleNode.level=pp.status=approved 是前置 |
| L-11 | 订单→采购单→收货（采购链路） | Order → PurchaseOrder → MaterialReceipt | BOM 分解或手动触发 PO；PO.Confirmed 后收货；收货分 Pending→Inspected→Accepted | orderId / purchaseOrderId | MaterialReceipt.totalAccepted ≤ Σ PurchaseLine.quantity |
| L-12 | 收货→库存（入库链路） | MaterialReceipt(Accepted) → InventoryItem + StockMovement(type=Inbound) | 检验合格后入库；StockMovement 记录变动前/后余额（审计链） | warehouseId / itemId / referenceType="MaterialReceipt" | StockMovement.balanceAfter = before + quantity |
| L-13 | 产品档案→订单行 / BOM（成衣→面料追溯） | ProductAsset(GarmentProfile) ← BOM → ProductAsset(FabricProfile)；OrderLine.styleNo → GarmentProfile | BOM 行项关联辅料/面料档案；OrderLine.styleNo 反查 GarmentProfile | productAssetId / materialCode | 成衣→BOM→面料的多对多穿透查询 |
| L-14 | 增值税票→退税申报（合规闭环） | VatInvoice(direction=Input, status=Verified) → TaxRefund（schema 实际命名，原文档误称 TaxRefundDecl） | 专票勾选认证后可申报退税；退税率按 HS Code 最长前缀命中 | taxRefundId | direction=Input + invoiceType=Special + status=Verified |

### 6.3 6 追溯场景

| # | 追溯场景 | 查询起点 | 链路 → 终点 | 涉及模型 | 业务价值 |
|---|---------|---------|-------------|---------|---------|
| T-01 | SO# → 订单利润分析 | Order.code（SO-xxx） | → Order → Σ Invoice(Receivable) 收入 − Σ PO 成本 − Σ Shipment 运费 − Σ 杂费 = 毛利 → OrderProfitSheet | Order, Invoice, PaymentVoucher, PurchaseOrder, Shipment, OrderProfitSheet | 订单级利润 360°，§6.2 P1 报表 |
| T-02 | 客户 → 历史报价单 | Relation.id（客户） | → Quotation.customerRelationId + Quotation.status∈{Sent/Accepted/Rejected/Expired} → 按 issueDate 倒序 | Relation, Quotation, QuotationLine | 客户报价历史分析，复购参考 |
| T-03 | PO# → 收货库存去向 | PurchaseOrder.poNumber | → MaterialReceipt → InventoryItem（入库批次） → StockMovement（出/入库流水，含 referenceId） | PurchaseOrder, MaterialReceipt, InventoryItem, StockMovement | PO 收货全量追溯（批次+库位+出入库时间轴） |
| T-04 | 面料编码 → 成衣订单 | FabricProfile / ProductAsset.sku | → BOMLine.materialCode → BOM.productAssetId(Garment) → OrderLine.styleNo → Order | FabricProfile, BOMLine, BOM, GarmentProfile, OrderLine, Order | 面料→BOM→成衣→订单的多跳关联，面料需求反推 |
| T-05 | 发票号 → 到哪张付款 | Invoice.invoiceNumber | → InvoiceAllocation（多笔）→ PaymentVoucher.voucherNumber / paymentDate / amount； 未核销部分 = Invoice.amount − Σ appliedAmount | Invoice, InvoiceAllocation, PaymentVoucher, FxSettlement | 财务对账：哪张发票被哪几笔水单核销，余额多少 |
| T-06 | 疵点类型 → 供应商（质量归因） | InspectionReport.defectSummary 关键词 | → InspectionReport.orderId → Order.millRelationId → Relation（供应商） → FactoryEvaluation（评分历史）； 按 supplier 聚合 defect 频次 | InspectionReport, Order, Relation, FactoryEvaluation, FactoryProfile | 供应商质量趋势分析，§5.3 疵点趋势 |

### 6.4 DR-005 内部面料交易跨组联动

> **决策来源**：[DR-005 FOB 的内部面料交易与合并利润](../10-评审与决策/2026-08-16-设计评审决策记录.md#dr-005-fob-的内部面料交易与合并利润)
> **设计真源**：[订单与生产模型组.md §2B](./订单与生产模型组.md#2b-dr-005-内部面料交易模型) / [Prisma 缺口清单 P1-4/P1-5](./Prisma缺口清单与迁移方案.md)

#### 6.4.1 交易关系拓扑

```
┌──────────────────────────┐         ┌──────────────────────────┐
│  服装部 (FOB 服装订单)     │         │  面料部 (内部面料订单)     │
│  Order.type=Garment       │         │  Order.type=Fabric        │
│  Order.businessLine=garment│        │  Order.isInternalFabricTrade=true │
│                           │  内部采购 │                           │
│  OrderLine.internalTransfer│◀────────│  Order.internalFabricOrderId │
│  Price (服装部面料成本)    │  双向关联  │  OrderLine.internalSettlementPrice │
│                           │         │  (面料部内部收入)           │
│  OrderInternalTransfer     │◀───────▶│  OrderInternalTransfer     │
│  (direction=outgoing)     │         │  (direction=incoming)      │
└───────────┬──────────────┘         └───────────┬──────────────┘
            │                                     │
            ▼                                     ▼
┌──────────────────────────────────────────────────────────────┐
│  公司合并报表 (抵销内部交易)                                   │
│  合并收入 = 外部客户收入（剔除内部面料销售收入）                 │
│  合并成本 = 真实面料成本（剔除内部采购加价）+ 服装生产成本        │
│  合并利润 = 合并收入 − 合并成本                                │
│  服装部利润 = 服装订单收入 − 内部面料采购价 − 服装生产成本       │
│  面料部利润 = 内部面料销售收入 − 面料生产成本                   │
│  抵销额 = 内部面料采购价（=内部面料销售收入）                   │
└──────────────────────────────────────────────────────────────┘
```

#### 6.4.2 跨组联动关系表

| # | 联动名称 | 主模型 → 从模型 | 触发条件 / 业务语义 | 关键 FK 字段 | 门禁条件 |
|---|---------|---------------|--------------------|-------------|---------|
| L-15 | 服装订单→内部面料订单（双向关联） | Order(Garment) ↔ Order(Fabric) | 服装部 FOB 订单向面料部采购面料时，创建内部面料订单并双向关联 | Order.internalFabricOrderId（snapshot FK，自引用 Order） | 面料订单 isInternalFabricTrade=true；内部结算价经 DR-006 价格审批冻结 |
| L-16 | 内部面料订单→内部供料单（DR-033） | Order(Fabric, isInternalFabricTrade) → OrderInternalTransfer | 面料部确认数量/交期/内部结算价后，内部供料单生效 | OrderInternalTransfer.orderId + transferDirection | @@unique([orderId, transferDirection]) 每方向仅 1 条 |
| L-17 | 内部供料→面料出运回写 | Shipment(面料订单) → OrderInternalTransfer → Order(服装订单) | 面料部出运关联内部供料分配后，回写服装订单的面料到货数量/日期/状态 | ShipmentLine.loadedWith=OrderLine(面料) + OrderInternalTransfer.orderId | 支持分批出运/分批到货/差异追溯 |
| L-18 | 内部交易→部门利润核算 | OrderInternalTransfer → OrderProfitSheet(服装部) / OrderProfitSheet(面料部) | 内部结算价进入服装部面料成本 + 面料部内部收入；部门利润独立核算 | OrderProfitSheet.orderId @unique | 服装部利润扣除内部面料采购价；面料部保留内部面料利润 |
| L-19 | 部门利润→公司合并抵销 | OrderProfitSheet(服装部) + OrderProfitSheet(面料部) → 合并报表 | 合并报表抵销内部采购/内部销售，仅计外部客户收入/真实面料成本/服装生产成本 | OrderInternalTransfer.transferAmount（抵销额） | 抵销额 = 内部面料采购价 = 内部面料销售收入 |

#### 6.4.3 待新增字段（❌ 未实现，见 Prisma 缺口清单 P1-4/P1-5）

| 字段 | 模型 | 类型 | 说明 |
|------|------|------|------|
| isInternalFabricTrade | Order | Boolean @default(false) | 内部面料交易标记（DR-005） |
| internalCounterpartyId | Order | String? | 内部关联方 ID（isInternalFabricTrade=true 时必填） |
| internalFabricOrderId | Order | String? | 关联的内部面料订单 ID（双向关联） |
| internalSupplyOrderId | Order | String? | 关联的内部供料单 ID（DR-033） |
| internalTransferPrice | OrderLine | Decimal? @db.Decimal(18,4) | 内部交易结算价（替代 unitPrice 做内部核算） |
| departmentProfit | Order | Decimal? @db.Decimal(18,4) | 部门利润（独立核算） |

> **新增模型**：`OrderInternalTransfer`（内部面料交易核算，见 [Prisma 缺口清单 P1-4](./Prisma缺口清单与迁移方案.md#p1-4--p1-5orderinternaltransfer-模型dr-005)）

#### 6.4.4 追溯场景

| # | 追溯场景 | 查询起点 | 链路 → 终点 | 涉及模型 | 业务价值 |
|---|---------|---------|-------------|---------|---------|
| T-07 | 服装订单 → 内部面料采购全链 | Order.code（服装 SO） | → Order.internalFabricOrderId → Order(面料) → OrderInternalTransfer → Shipment(面料出运) → 服装订单面料到货回写 | Order, OrderInternalTransfer, Shipment, ShipmentLine | 内部面料采购到货全链追溯 |
| T-08 | 部门利润 → 公司合并抵销 | OrderInternalTransfer | → 服装部 OrderProfitSheet（扣除内部采购价）+ 面料部 OrderProfitSheet（含内部销售收入）→ 合并报表抵销 | OrderInternalTransfer, OrderProfitSheet | 部门独立利润 + 公司合并利润双视角 |

---

## §7 API 端点总览（/api/v1/*）

> 详细请求/响应与鉴权见各模块设计文档 §7 与对应 *Route.ts

| 域组 | 模型 | C（POST） | R（GET） | U（PUT/PATCH） | D（DELETE） |
|------|------|----------|---------|---------------|------------|
| **订单与生产** | /orders | /api/v1/orders | /api/v1/orders, /:id, /:id/lines | /api/v1/orders/:id | /api/v1/orders/:id (软删) |
| | /order-lines | /api/v1/order-lines (批量) | /:id | /:id | /:id |
| | /production-stages | /api/v1/production-stages (upsert 批量) | /by-order/:orderId | /:id 推进 | N/A |
| | /inspection-reports | /api/v1/inspection-reports | /:id, /by-order/:orderId | /:id | /:id |
| | /development-cases | /api/v1/development-cases | /:id, /list | /:id | /:id |
| **产品与档案** | /products | /api/v1/products | /:id, /list, /sku/:sku | /:id | /:id |
| | /fabrics | /api/v1/products/fabrics | /:id, /list | /:id | /:id |
| | /garments | /api/v1/products/garments | /:id, /list | /:id | /:id |
| **报价与成本** | /quotations | /api/v1/quotations | /:id, /list, /:id/lines | /:id, /:id/convert-to-order | /:id |
| | /pricing-calculations | /api/v1/pricing | /:id, /recalc/:orderId | /:id (confirm) | /:id |
| | /order-profit-sheets | N/A (生成式) | /by-order/:orderId, /regenerate/:orderId | N/A | N/A |
| **采购与库存** | /purchase-orders | /api/v1/purchase-orders | /:id, /list, /:id/lines | /:id | /:id |
| | /material-receipts | /api/v1/material-receipts | /:id, /by-po/:poId | /:id (inspect/accept) | /:id |
| | /warehouses | /api/v1/warehouses | /:id, /list | /:id | /:id |
| | /inventory-items | N/A (由入库派生) | /:id, /by-warehouse/:whId | /:id (调整) | N/A |
| | /stock-movements | /api/v1/stock-movements (手动) | /:id, /by-item/:itemId | N/A | N/A |
| **财务域** | /invoices | /api/v1/invoices | /:id, /list, /:id/allocations | /:id (issue/cancel) | /:id |
| | /payment-vouchers | /api/v1/payment-vouchers | /:id, /list | /:id (reconcile) | /:id |
| | /fx-settlements | /api/v1/fx-settlements | /:id, /by-voucher/:vId | /:id | /:id |
| | /vat-invoices | /api/v1/vat-invoices | /:id, /list | /:id (verify/declare/red-flush) | /:id |
| **出运与报关** | /shipments | /api/v1/shipments | /:id, /list, /:id/lines, /:id/events | /:id | /:id |
| | /customs-declarations | /api/v1/customs | /:id, /by-shipment/:sId | /:id (submit/release) | /:id |
| **客户与关系** | /relations | /api/v1/relations | /:id, /list, /by-category/:cat | /:id | /:id |
| | /contacts | /api/v1/contacts | /:id, /by-relation/:rId | /:id | /:id |
| | /opportunities | /api/v1/opportunities | /:id, /pipeline | /:id, /:id/close | /:id |
| **底座域** | /users | /api/v1/users | /:id, /list | /:id | /:id (禁删=deactivate) |
| | /roles | /api/v1/roles | /:id, /list, /:id/permissions | /:id | /:id |
| | /approval-requests | /api/v1/approvals | /:id, /mine | /:id (approve/reject) | N/A |
| **通知邮件季节** | /notifications | N/A (事件触发) | /mine, /unread-count | /:id (read/dismiss) | /:id |
| | /emails | /api/v1/emails (后台同步) | /:id, /list, /:id/attachments | /:id (status) | /:id (归档) |
| | /seasons | /api/v1/seasons | /:id, /list, /:id/review | /:id, /:id/generate-review | /:id |
| **MES 深化** | /boms | /api/v1/boms | /:id, /list, /:id/lines, /:id/costs | /:id (版本+) | /:id |
| | /outsourcing-orders | /api/v1/outsourcing | /:id, /list | /:id | /:id |
| | /factories | /api/v1/factories | /:id, /:id/evaluations, /:id/capacity | /:id (blacklist) | /:id |

---

## §8 权限

各域权限（行级 dataScope / 字段级敏感权限 / 7 角色 × 6 能力矩阵）见对应模块设计文档 §8。数据模型层仅通过 `ownerId / departmentId / Relation.ownerId` 等字段为 Service 层行级过滤提供数据基础，不内置数据库层 RLS（行级安全由 service 统一执行）。

---

## §9 四态

N/A（四态规范（空/加载/错误/无权限）归前端页面/组件文档 §9）

---

## §10 移动端

N/A（数据模型层无移动端专属字段；移动端紧凑字段布局归对应模块 §10）

---

## §11 业务规则关联（14 联动 × L1-L10 事件对应矩阵）

| 14 联动 \ 事件 | L1 订单创建 | L2 下单 7d 出生产计划 | L3 产前样 PP 双签 | L4 终期验货 AQL | L5 出运订舱+装货 | L6 出运后开票 | L7 收款核销更新账龄 | L8 交期前 15d 延期通知 | L9 到期后 3d/7d/15d 催款 | L10 月结关单利润重算 |
|----------------|-----------|---------------------|----------------|----------------|----------------|---------------|--------------------|------------------------|-------------------------|---------------------|
| L-01 Order→Line | ✅ 创建时写入行 | | | | | | | | | |
| L-02 Order→10 Stage | ✅ ① order_placed 自动 done | ✅ ③ production_planned 推进门禁 | | | | | | ✅ 阶段进度触发延期 | | |
| L-03 Stage⑥→PreCutChecklist | | | | | | | | ✅ 阻塞超期预警 | | |
| L-04 Stage⑨⑩→InspectionReport | | | | ✅ 终期验货驱动 L4 | | | | ✅ 延期风险 | | |
| L-05 验货→Shipment→Line | | | | ✅ 通过后才能建 Shipment | ✅ L5 状态转换 | | | | | |
| L-06 Shipment→Invoice | | | | | ✅ Shipped 时触发开票 | ✅ L6 Issued | | | | |
| L-07 Invoice→Voucher→Alloc | | | | | | | ✅ L7 核销更新账龄桶 | | | ✅ 月结重算利润 |
| L-08 Voucher→Fx水单 | | | | | | | ✅ 结汇后收款确认 | | | ✅ 汇兑损益入账 |
| L-09 Quotation→Order | ✅ 转单走 L1 创建 | | | | | | | | | |
| L-10 DevCase→Order | ✅ 转单走 L1 创建 | | ✅ PP approved 前置 | | | | | | | |
| L-11 Order→PO→Receipt | | ✅ 计划触发 MRP 下单 | | | | | | ✅ 收货延期 | | |
| L-12 Receipt→Inventory | | | | | | | | ✅ 缺货预警 | | |
| L-13 档案→订单行/BOM | | ✅ 用料反推生产 | | | | | | | | |
| L-14 Vat→退税申报 | | | | | | | ✅ 退税资金回笼 | | ✅ 逾期未申报告警 | ✅ 退税利润修正 |

**矩阵说明**：
- ✅ = 联动直接触发对应 L 事件（事件总线 topic）
- 空格 = 无直接关联
- 审批策略：所有 Lx 事件挂接 WorkflowDefinition.triggerEvent，当前 approvalPolicy=manual always（审计报告 §规则落地矩阵）

---

## §12 可访问性

N/A（数据模型层无可访问性约束，可访问性归前端模块文档 §12）

---

## §13 设计系统约束

N/A（设计系统约束（圆角/阴影/雾化/字重/toast 纪律）归前端模块文档 §13）

---

## §14 缺口（显性化审计报告指出的数据模型缺口）

> **v4 审查状态更新（2026-08-15）**：经与 `server/prisma/schema.prisma` 逐项核对，GAP-DM-001 / 003 / 006 已在 schema 中落地，本表状态已同步修正。GAP-DM-002 / 004 / 005 仍有效。

| 缺口 ID | 等级 | 模型/域 | 问题描述 | 影响 | 修复建议 | v4 状态 |
|---------|------|---------|---------|------|---------|---------|
| GAP-DM-001 | P0 | 单据主模型 TradeDocument | DocumentVersion.documentId 引用的主实体 TradeDocument **未定义主模型**（当前仅有 DocumentTemplate + DocumentVersion），导致"单据版本留痕"缺一个统一的单据头抽象（商业发票/装箱单/产地证等无法共享同一主键体系） | PRD 11.3 P2 无法按 documentId 查询版本历史；导出 PDF 的幂等去重缺真源 | 建 TradeDocument（id/type/number/orderId/status + 1:1 关联 Invoice/Shipment 等），DocumentVersion.documentId 改为 FK → TradeDocument | ✅ **已修复**：schema line 4522 `model TradeDocument` 已定义（documentNumber @unique / type / status / shipmentId / declarationId / orderId / relationId snapshot FK），DocumentVersion 仍保留独立模板版本控制 |
| GAP-DM-002 | P1 | OutsourcingOrder / OutsourcingLine | Schema 已完整（外协单号/工序类型/数量/价格/状态/验收），但 **UI 端 ProductionBoard-外协加工视图未接入**（无 CRUD 路由 + 无 Manager 封装调用） | 成衣洗水/印花等外协工序完全无法在线管理，与 §04-模块设计/03-生产看板/外协加工视图.md 不一致 | 批次 1 补齐：/api/v1/outsourcing CRUD 路由 + MesManager 封装 + 外协泳道看板渲染 | 🟡 仍有效（schema OK，UI/路由待补） |
| GAP-DM-003 | P1 | LetterOfCredit / TaxRefund（原文档误称 TaxRefundDecl） | ~~Schema 中未定义 L/C 信用证主模型 + 退税申报 TaxRefundDecl 主模型~~ | ~~财务闭环缺 2 个关键节点~~ | ~~批次 3 补齐 LetterOfCredit + TaxRefundDecl~~ | ✅ **已修复**：schema line 4404 `model LetterOfCredit`（lcNumber / type / status / 金额 / 期限 / 银行 / 单据条款）+ line 4481 `model TaxRefund`（实际命名为 `TaxRefund` 而非 `TaxRefundDecl`，含 refundNumber / declarationId / orderId / 退税金额 / 状态机）+ 附属 LcEvent（line 4463）+ CustomsDeclarationLine（line 4338）+ HsCode（line 4375）均已落地 |
| GAP-DM-004 | P1 | Order / Shipment / Invoice 关联表未显式化 | ShipmentLine.orderLineId 为 snapshot FK（无 @@relation），InvoiceAllocation 通过中间表但 orderId 维度冗余 snapshot，导致"订单已出运多少 / 已开票多少 / 已回款多少"聚合口径在 Service 层各算各的，缺乏真源视图 | Cockpit 订单 360° KPI 三栏（已出运/已开票/已回款）与财务对账口径可能偏差 >5% | 建 DB VIEW（或 Materialized View）`order_fulfillment_status`，统一按 OrderLine 为粒度聚合 shipped_qty / invoiced_amount / paid_amount，Service 只读视图不修 | 🟡 仍有效（snapshot FK 风格为有意决策，是否建物化视图待评估） |
| GAP-DM-005 | P2 | Statement / Payout | 财务域模型组原声称包含 Statement（对账单）与 Payout（付款），但 Schema 中 **未定义此两模型** | 客户/供应商月结对账单生成无真源；批量付款审批（Payout 批次）无法落地 | 批次 4 评估 Statement（关联 customer + 期间 + 汇总行 + 明细行快照 Json） + Payout（批付 N PaymentVoucher，审批后批量出帐）是否需要，不需要则从 10 组模型文档移除计数 | 🟡 仍有效（schema 仍未定义；§3.5 已同步修正描述） |
| GAP-DM-006 | P2 | BusinessSequence 编号注册 | Order.code / Quotation.quotationNumber / Shipment.shipmentNumber 等业务编号均为自由文本 String?，**无统一 BusinessSequence 注册表模型**保证跨环境/跨会话单调递增 + 日/月维度防重复 | 多用户并发创建同一前缀编号时可能冲突号；审计追溯编号断号/跳号无解 | 建 BusinessSequence(prefix, date_part, current_value, step)，Service 在事务内 `SELECT FOR UPDATE` 取号 + 写回 | ✅ **已修复**：schema line 4636 `model BusinessSequence`（prefix / year / seq + @@unique([prefix, year])）已落地；另增 Phase 0-03 升级版 `SequenceRegister`（line 4652，11 类业务单据独立序列，支持 year/month/day/none 重置周期 + formatTemplate + padding）+ `VoidedNumber`（line 4676，作废单号占坑表，显式追踪不复用） |

---

## §15 相关文档索引

| # | 文档 | 链接 | 关联章节 |
|---|------|------|---------|
| 1 | 设计文档真源索引 | [00-索引.md](../00-索引.md) | §七 75+ Model → 10 份模型组映射；§八 撰写标准（16 节结构） |
| 2 | 订单与生产模型组 | [02-数据模型/订单与生产模型组.md](./订单与生产模型组.md) | 组 1（8+ 模型详细字段 + 关系 + 约束） |
| 3 | 产品与档案模型组 | [02-数据模型/产品与档案模型组.md](./产品与档案模型组.md) | 组 2（18+ 三档档案 + 分类/认证/价格） |
| 4 | 报价与成本模型组 | [02-数据模型/报价与成本模型组.md](./报价与成本模型组.md) | 组 3（双轨定价 + 利润 + 退税） |
| 5 | 采购与库存模型组 | [02-数据模型/采购与库存模型组.md](./采购与库存模型组.md) | 组 4（PO + 入库 + 库存流水） |
| 6 | 财务域模型组 | [02-数据模型/财务域模型组.md](./财务域模型组.md) | 组 5（发票/凭证/核销/外汇/增值税） |
| 7 | 出运与报关模型组 | [02-数据模型/出运与报关模型组.md](./出运与报关模型组.md) | 组 6（运单/装箱/报关/退税/单据模板） |
| 8 | 客户与关系模型组 | [02-数据模型/客户与关系模型组.md](./客户与关系模型组.md) | 组 7（CRM + 知识 + 实体图谱） |
| 9 | 底座域模型组 | [02-数据模型/底座域模型组.md](./底座域模型组.md) | 组 8（用户/权限/HR/工作流/审批） |
| 10 | 通知邮件季节模型组 | [02-数据模型/通知邮件季节模型组.md](./通知邮件季节模型组.md) | 组 9（通知/邮件/季节展会/风险） |
| 11 | MES 深化模型组 | [02-数据模型/MES 深化模型组.md](./MES 深化模型组.md) | 组 10（BOM/工位/排产/计件/外协/工厂） |
| 12 | 审计报告（§数据模型章节） | `.trae/documents/audit-report-v*.md §数据模型一致性` | §14 缺口 GAP-DM-001 ~ 006 的原始审计意见来源 |
| 13 | 业务规则总览 | [03-业务规则/业务规则总览.md](../03-业务规则/业务规则总览.md) | §11 矩阵中 22 条规则落地口径详细说明 |

---

## §16 10 份模型组覆盖清单（每组预计模型数 / 主键类型 / 关键 FK）

> 主键类型缩写：S=String CUID / CU=复合唯一 / SN=短号前缀（QT_/INV_/...）

| 组号 | 模型组文档 | 预计模型数 | 主键类型（代表） | 关键 FK 数（≥1 的模型） | 核心代表模型 | 字段规模估算（核心/扩展 Json） |
|------|-----------|-----------|-----------------|----------------------|-------------|--------------------------|
| 1 | 订单与生产模型组.md | 8+ | S(CUID) + CU(orderId, stageKey/inspectionType) | 6 (Order/Line/Stage/PreCut/Inspection/Outsourcing) | Order, OrderLine, ProductionStage, InspectionReport, OutsourcingOrder | 220+ / 5 (fieldSources, sizeBreakdown, productionSteps, bomItems, garmentSampleStages) |
| 2 | 产品与档案模型组.md | 18+ | S(CUID) + SN(TRDT/TRTF/FACC/...) | 10 (Fabric/Garment/Trimming Profile + Links + Lookbook + Recommend) | ProductAsset, FabricProfile, GarmentProfile, FabricPriceHistory, FabricCertification | 380+ / 8 (backupContacts, shipToAddresses, trendTags, classificationLinks, images, reviewJson, catalogItems, recommendationResults) |
| 3 | 报价与成本模型组.md | 7+ | S(CUID) + SN(QT/PRC/OPS/CR/...) + CU(orderId @unique) | 5 (Quotation / Line / PricingCalc / ProfitSheet / CommissionRule) | Quotation, PricingCalculation, OrderProfitSheet, TaxRefundRate, CommissionRule | 110+ / 3 (quotation attachments, profit details, pricing criteria) |
| 4 | 采购与库存模型组.md | 11+ | S(CUID) + SN(PO/MR/WH/INV/SM/...) + CU([factoryId, month]/[userId, date]) | 9 (PO / Line / Receipt / Warehouse / Item / Movement + BOM 复用 + 工厂产能) | PurchaseOrder, MaterialReceipt, InventoryItem, StockMovement, BOM | 260+ / 6 (PO attachments, BOM items Json, BOM version parent, cost estimates, MES production plan criteria) |
| 5 | 财务域模型组.md | 9+ | S(CUID) + SN(INV/PAY/ALLOC/FXS/OWR/VAT/...) + CU([invoiceId, voucherId]) | 7 (Invoice / Voucher / Allocation / FxSettlement / Remittance / Vat / CommRule) | Invoice, PaymentVoucher, InvoiceAllocation, VatInvoice, FxSettlement | 200+ / 4 (invoice attachments, voucher attachments, vat attachments, unconverted amounts) |
| 6 | 出运与报关模型组.md | 9+ | S(CUID) + SN(SHP/SHPL/SHPE/SHPC/CD/...) + CU([cartonId, shipmentLineId]) | 8 (Shipment / Line / Event / Carton / CartonItem + Customs + LC + Template + Version) | Shipment, ShipmentLine, ShipmentEvent, CustomsDeclaration, DocumentTemplate | 220+ / 5 (shipment attachments, document variables, version snapshot Json, LC terms, customs declaration details) |
| 7 | 客户与关系模型组.md | 7+ （含知识+实体 18+ 若展开） | S(CUID) + SN(CTC/CL/FU/OPP/TIER/...) + CU([userId, notificationType] 类) | 12+ (Relation + 5 CRM 深化 + Knowledge 全家桶 4 + SopTemplate + Entity 3) | Relation, Contact, Opportunity, EntityLink, KnowledgeDocument | 300+ / 20 (Relation 多 Json + Email aiExtracted + Opportunity tags + Entity snapshot) |
| 8 | 底座域模型组.md | 16+（含 HR + 工作流 >40 若展开） | S(CUID) + CU([userId, roleId, deptId]/[cycleId, userId]) | 18 (User + Dept + 5 RBAC + HR 全家桶 + Workflow 3 + Approval + Audit) | UserAccount, Role, ApprovalRequest, WorkflowInstance, EmployeeProfile, PayrollRun | 500+ / 10 (User metadata + Dept metadata + AgentPolicy conditions + Workflow steps + HR review kpi + Payroll details) |
| 9 | 通知邮件季节模型组.md | 10+（含风险/QC >15 若展开） | S(CUID) + SN(SEAS/TRDS/TRDL/FXR/RSKA/BL/QCA...) + CU([vatCode, vatNumber], [seasonId, month]) | 12 (Notification + Email + Season 3 + Risk 5 + QC 2 + BL) | Notification, Email, Season, TradeShow, RiskAlert, ExchangeRate | 260+ / 12 (Email labels/ai + Notification metadata + Season calendar/review + TradeShow attendees + Risk dedupKey) |
| 10 | MES 深化模型组.md | 6+（含 BOM/工厂 13+ 若展开） | S(CUID) + SN(WS/PP/WHR/PRR/PRC/OSO/...) + CU([pieceRateRuleId, ...]) | 10 (WorkStation + PP + WHours + PRRule/Record + Outsourcing 2 + Factory 4 + BOM 3) | BOM, WorkStation, ProductionPlan, OutsourcingOrder, FactoryProfile | 290+ / 8 (ProductionPlan notes + PieceRateRecord details + BOM lines Json + Factory specialties + BOM cost estimates) |
| **合计** | **10 份** | **~120 模型（v4 核对：schema 实际定义 120 模型，含 v4 新增 CustomsDeclarationLine / HsCode / LcEvent / TradeDocument / LetterOfCredit / TaxRefund / BusinessSequence / SequenceRegister / VoidedNumber / ReportDefinition / ReportRun / DirtyCacheMarker / OutsourcingLine 等）** | 全部以 String CUID 为主；复合唯一约束 30+；业务编号 SN 前缀 40+ | **~120 模型含 ≥1 FK；实际 FK 关系字段 280+** | 核心枢纽：Order, Relation, ProductAsset, Shipment, Invoice | **字段总数 3200+ / Json 扩展字段 85+（占比 ~3%，保持 NF3+弹性）** |
