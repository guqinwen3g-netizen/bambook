# 财务域模型组 (Finance Domain Model Group)

> **DR-015 关联**：发货与出运以业务事实为准——货物离开工厂即视为已发货，客户订舱 ≠ 已发货，出运 ≠ 合同履约完成
> **DR-016 关联**：运费与结算分离——费用归属（预付/到付/代垫）与实际结算分离；我方承担运费（预付/代垫）才纳入利润表
> **DR-017 关联**：任何对外付款必须先申请后付款；审批通过前不得创建对外付款凭证
> **DR-018 关联**：收款凭证到账后默认锁定；撤销到账必须审批；同一收款凭证只能核销到一张发票；作废仅隐藏不删除
> **DR-019 关联**：预收款必须先建立收款凭证，后续绑定订单核销；预收款订单核销不改变发票与付款凭证一一对应关系
> **DR-020 关联**：费用按归属对象计入订单、供应商、客户、样品、开发案例或内部运营；报销付款统一走对外付款审批链
> **DR-021 关联**：收付汇必须按真实业务发生记账，不强制收汇与付汇一一对应
> **DR-022 关联**：业务成本、样品快递费、客户报销费用分别核算；快递费默认计入内部运营成本

## §1 元信息

| 项 | 值 |
|---|---|
| **定位** | 财务与结算域的持久化数据模型真源——覆盖**付款申请 PaymentRequest（先申请后付款 DR-017）** / 发票 / 收付款凭证 / 核销明细 / 结汇 / 付汇 / 增值税 / 退税 / 利润表 / 佣金规则 10 个 Prisma 模型。DR-016：费用归属与实际结算分离，我方承担运费才纳入利润表；**DR-017：任何对外付款必须先申请（PaymentRequest）→ 审批通过后才能生成 PaymentVoucher（付款凭证），审批与执行分离；报销/运费/佣金/采购等所有对外付款统一走 PaymentRequest 审批链**；DR-018：收款凭证到账锁定 + 单一发票核销；DR-019：预收款绑定订单核销；DR-020：费用按归属对象核算 + 报销走付款审批链；DR-021：收付汇真实记账非强制一一对应；DR-022：业务成本/样品快递/客户报销分别核算 |
| **范式** | 数据模型型文档（全量字段表 + ER 关系图 + snapshot FK 关系说明） |
| **优先级** | P0 |
| **实现状态** | ✅ 已落地 9 模型（Invoice / PaymentVoucher / InvoiceAllocation / FxSettlement / OutwardRemittance / VatInvoice / TaxRefund / OrderProfitSheet / CommissionRule）；金额字段全量 Decimal(18,4)；汇率 Decimal(18,8)；时间戳 BigInt epoch ms；业务日期 String YYYY-MM-DD。⚠️ CommissionStatement / CommissionPayout 两模型尚未在 schema.prisma 中定义（列为 P2 待补） |
| **关联 PRD 章节** | §5.6（编号规则）、§8（财务域）、§8.3（外汇核销）、§8.4（汇兑损益） |
| **关联代码** | [server/prisma/schema.prisma](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/prisma/schema.prisma) Invoice L1951 / PaymentVoucher L1997 / InvoiceAllocation L2054 / FxSettlement L2077 / OutwardRemittance L2117 / VatInvoice L2168 / OrderProfitSheet L3570 / CommissionRule L3631 / TaxRefund L4481 |

---

## §2 模型组总览

| # | 模型 | 角色 | 主键格式 | 软删 | 关键约束 |
|---|------|------|---------|------|---------|
| 1 | Invoice | 业务发票（应收/应付） | `INV__${shortId}` | ✅ deletedAt | invoiceNumber @unique；9 索引 |
| 2 | PaymentVoucher | 收付款凭证 | `PAY__${shortId}` | ✅ deletedAt | voucherNumber @unique；8 索引 |
| 3 | InvoiceAllocation | 核销明细（N:M 中间表） | `ALLOC__${shortId}` | ❌（硬删除） | `@@unique([invoiceId, voucherId])` |
| 4 | FxSettlement | 结汇水单（外币→CNY） | `FXS__${shortId}` | ✅ deletedAt | settlementNumber @unique；4 索引 |
| 5 | OutwardRemittance | 付汇水单（CNY→外币出境） | `OWR__${shortId}` | ✅ deletedAt | remittanceNumber @unique；4 索引 |
| 6 | VatInvoice | 增值税发票 | `VAT__${shortId}` | ✅ deletedAt | `@@unique([vatCode, vatNumber])`；6 索引 |
| 7 | TaxRefund | 退税申报 | `TR_${shortId}` | ✅ deletedAt | refundNumber @unique；4 索引 |
| 8 | OrderProfitSheet | 订单利润表（聚合投影） | `OPS__${shortId}` | ❌（硬删除） | orderId @unique（幂等） |
| 9 | CommissionRule | 佣金规则 | `CR__${shortId}` | ✅ deletedAt | 2 索引 |

### 2.1 待补模型（P2 缺口）

| 模型 | 规划角色 | 当前状态 |
|------|---------|---------|
| PaymentRequest | 付款申请单（DR-017：任何对外付款必须先申请后付款） | ❌ 未在 schema.prisma 定义 |
| CommissionStatement | 佣金结算单（按周期汇总佣金） | ❌ 未在 schema.prisma 定义 |
| CommissionPayout | 佣金支付记录 | ❌ 未在 schema.prisma 定义 |

> 当前佣金仅落地 CommissionRule（佣金率配置真源），结算与支付流程列为 P2 待补。PricingCalculation.commissionRate 落库为数值快照（规则调整不回溯历史定价），commissionRuleId 记录来源规则以便追溯。
>
> DR-017：PaymentRequest 为付款申请前置实体，审批通过前不得创建 PaymentVoucher（对外付款凭证）。报销付款、运费代垫、佣金支付均统一走 PaymentRequest 审批链。

---

## §3 ER 关系图（ASCII）

```
                          ┌──────────────────────┐
                          │      Relation        │ (跨模块 snapshot FK，无 @relation)
                          │  (customer/supplier/ │
                          │   intermediary)      │
                          └──────────┬───────────┘
                                     │ *RelationId snapshot
                                     │
┌─────────────────────┐              │              ┌─────────────────────┐
│      Invoice        │◄─────────────┼──────────────│   PaymentVoucher    │
│  (id PK,            │  settlesInvoice (snapshot)  │  (id PK,            │
│   invoiceNumber)    │              │              │   voucherNumber)   │
│  type: Receivable/  │              │              │  type: Receipt/     │
│         Payable     │              │              │        Disbursement │
│  status: 5 态       │              │              │  status: 3 态      │
└─────────┬───────────┘              │              └─────────┬───────────┘
          │                          │                        │
          │          ┌───────────────┴────────────┐           │
          │          │   InvoiceAllocation        │           │
          │          │   (invoiceId, voucherId)  │           │
          │          │   @@unique([invoiceId,     │           │
          │          │     voucherId])            │           │
          │          │   appliedAmount            │           │
          │          └────────────────────────────┘           │
          │                                                   │
          │ orderId snapshot                          voucherId │
          │                                                   │
          ▼                                                   ▼
┌─────────────────────┐    voucherId    ┌──────────────────────────────────┐
│   OrderProfitSheet  │                 │  外汇核销闭环（收付双侧镜像）       │
│  (orderId @unique)   │        ┌────────┴────────┐  ┌─────────────────────┴──┐
│  四维聚合 + version  │        │   FxSettlement  │  │  OutwardRemittance    │
│  details Json        │        │  (Receipt 侧)   │  │  (Disbursement 侧)    │
└─────────────────────┘        │  cnyAmount 服务端│  │  cnyAmount 服务端     │
                                │  计算            │  │  计算                 │
                                └─────────────────┘  └───────────────────────┘

┌─────────────────────┐  invoiceId    ┌─────────────────────┐
│     VatInvoice      │◄─────────────│      Invoice        │ (settlesInvoice)
│  (vatCode, vatNumber│              │  (应收/应付)          │
│   @@unique)         │              └─────────────────────┘
│  direction: Input/  │
│         Output      │  taxRefundId
│  status: 5 态       │─────────────▶┌─────────────────────┐
└─────────────────────┘              │     TaxRefund       │
                                     │  (refundNumber)     │
┌─────────────────────┐              │  status: 7 态        │
│  CommissionRule     │              └─────────────────────┘
│  (rate, isActive)   │
│  intermediaryRelationId ──────────▶ Relation (intermediary)
└─────────────────────┘
```

**关系类型说明**：
- **虚线 snapshot FK（无 @relation）**：所有跨模块关联均为 snapshot FK（无 Prisma 强制 FK），通过 EntityLink 图谱同步维护。包括 Invoice.orderId/customerRelationId、PaymentVoucher.invoiceId/orderId/customerRelationId、FxSettlement.voucherId/orderId、OutwardRemittance.voucherId/orderId、VatInvoice.invoiceId/orderId/relationId/taxRefundId、TaxRefund.orderId/relationId/declarationId
- **硬唯一约束**：InvoiceAllocation `@@unique([invoiceId, voucherId])`、VatInvoice `@@unique([vatCode, vatNumber])`、OrderProfitSheet `orderId @unique`

---

## §4 Invoice 模型（业务发票）

### 4.1 核心字段

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | String | @id | `INV__${shortId}` |
| invoiceNumber | String | @unique | 业务发票号（如 INV-20260615-001） |
| type | String | — | Receivable(应收) \| Payable(应付) |
| status | String | — | Draft \| Issued \| PartiallyPaid \| Paid \| Cancelled |
| amount | Decimal | @db.Decimal(18,4) | 发票金额（含税） |
| currency | String | — | USD \| CNY \| EUR \| HKD ... |
| issueDate | String | — | 开票日期 YYYY-MM-DD |
| dueDate | String? | — | 应收/应付到期日 |
| settlementDate | String? | — | 实际结算日（全额完成日，由 allocation 自动写入） |

### 4.2 汇率快照

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| exchangeRate | Decimal? | @db.Decimal(18,4) | 开票当日 currency → baseCurrency 汇率 |
| baseCurrency | String | @default("CNY") | 本位币 |

### 4.3 关联 snapshot FK + 冗余快照

| 字段 | 类型 | 说明 |
|------|------|------|
| orderId | String? | 关联订单（aboutOrder linkKind） |
| customerRelationId | String? | 结算单位（billTo linkKind） |
| customerName | String? | 冗余快照（防 Relation 改名后历史发票显示错乱） |

### 4.4 行级权限 + 时间戳

| 字段 | 类型 | 说明 |
|------|------|------|
| ownerId | String? | 行级权限-所有者 |
| departmentId | String? | 行级权限-部门 |
| notes | String? | 备注 |
| attachments | Json? | 附件 |
| createdAt | BigInt | 创建时间 epoch ms |
| updatedAt | BigInt | 更新时间 |
| deletedAt | BigInt? | 软删时间（null=未删） |

### 4.5 索引

```
@@index([type]) @@index([status]) @@index([orderId])
@@index([customerRelationId]) @@index([invoiceNumber])
@@index([issueDate]) @@index([dueDate])
@@index([ownerId]) @@index([departmentId])
```

---

## §5 PaymentVoucher 模型（收付款凭证）

### 5.1 核心字段

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | String | @id | `PAY__${shortId}` |
| voucherNumber | String | @unique | 业务凭证号（如 PAY-20260615-001） |
| type | String | — | Receipt(收款) \| Disbursement(付款) |
| amount | Decimal | @db.Decimal(18,4) | 凭证金额（实际入/出账，扣手续费前） |
| currency | String | — | 币种 |
| paymentDate | String | — | 实际收/付款日 YYYY-MM-DD |
| paymentMethod | String | — | TT \| LC \| OA \| DP \| DA \| Cash \| Other |
| status | String | @default("unreconciled") | unreconciled \| partially_reconciled \| reconciled |
| bankFee | Decimal | @default(0) @db.Decimal(18,4) | 银行手续费（与 amount 同币种） |
| voucherCategory | String? | — | normal \| advance（预收款，DR-019）\| deposit（保证金） |
| advanceOrderId | String? | — | 预收款绑定订单 ID（DR-019：预收款后续绑定订单核销） |
| lockedAt | BigInt? | — | 到账锁定时间（DR-018：到账后默认锁定） |
| lockedBy | String? | — | 锁定操作人 |
| voidedAt | BigInt? | — | 作废时间（DR-018：作废仅隐藏不删除） |
| voidedBy | String? | — | 作废操作人 |
| voidReason | String? | — | 作废原因 |

### 5.2 汇率快照 + 核销信息

| 字段 | 类型 | 说明 |
|------|------|------|
| exchangeRate | Decimal? @db.Decimal(18,4) | 收/付款日 currency → baseCurrency 汇率 |
| baseCurrency | String @default("CNY") | 本位币 |
| invoiceId | String? | 核销的发票（settlesInvoice linkKind）；空=未核销/暂挂账 |
| appliedAmount | Decimal? @db.Decimal(18,4) | 本笔凭证实际核销发票金额（可能 < amount，余款挂账） |

> `appliedAmount` 是 1:1 主核销快照（向后兼容），InvoiceAllocation 是 N:M 核销明细（替代方案，支持一笔凭证核销多张发票）。

### 5.3 关联 snapshot FK + 权限

| 字段 | 类型 | 说明 |
|------|------|------|
| orderId | String? | 冗余关联订单（aboutOrder linkKind） |
| customerRelationId | String? | 交易对象（billTo linkKind） |
| customerName | String? | 冗余快照 |
| ownerId / departmentId | String? | 行级权限 |

### 5.4 索引

```
@@index([type]) @@index([status]) @@index([invoiceId])
@@index([orderId]) @@index([customerRelationId]) @@index([voucherNumber])
@@index([paymentDate]) @@index([ownerId]) @@index([departmentId])
```

### 5.5 DR-018 收款凭证锁定与作废规则

| 规则 | 说明 |
|------|------|
| 到账锁定 | 收款凭证到账后默认锁定（lockedAt/lockedBy），撤销到账必须审批 |
| 单一发票核销 | 同一收款凭证只能核销到一张发票（InvoiceAllocation 一对一） |
| 撤销到账 | 到账撤销必须走审批流，审批通过后解除锁定 |
| 作废 | 作废仅隐藏不删除（voidedAt/voidedBy/voidReason），保留审计追溯 |
| 异常提交 | 异常提交的凭证默认不进入核销，可隐藏、可作废，但不删除 |

### 5.6 DR-019 预收款规则

| 规则 | 说明 |
|------|------|
| 预收款建立 | 预收款必须先建立收款凭证（voucherCategory=advance） |
| 绑定订单 | 预收款后续绑定订单核销（advanceOrderId） |
| 核销规则 | 预收款订单核销不改变发票与付款凭证一一对应关系（InvoiceAllocation 仍一对一） |
| 未核销状态 | 预收款未绑定订单前 status=unreconciled，不影响发票核销 |

---

## §6 InvoiceAllocation 模型（核销明细）

### 6.1 全量字段

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | String | @id | `ALLOC__${shortId}` |
| invoiceId | String | — | 核销的发票 |
| voucherId | String | — | 核销的凭证 |
| appliedAmount | Decimal | @db.Decimal(18,4) | 本笔核销金额 |
| appliedDate | String | — | 核销日期 YYYY-MM-DD |
| createdAt | BigInt | — | 创建时间 |
| updatedAt | BigInt | — | 更新时间 |

### 6.2 约束

```
@@unique([invoiceId, voucherId])  // 一对 invoice-voucher 只能有一条明细
@@index([invoiceId])
@@index([voucherId])
```

> **硬删除（无 deletedAt）**：核销调整时先 delete 旧明细再 insert 新明细，避免软删 + unique 冲突。替代 PaymentVoucher.appliedAmount 的单 1:1 表达（后者保留作为主核销快照，向后兼容）。
>
> **DR-019 预收款核销**：预收款（voucherCategory=advance）绑定订单后，仍通过 InvoiceAllocation 一对一核销到订单关联的发票，不改变"同一收款凭证只能核销到一张发票"的规则。预收款未绑定订单前不进入 InvoiceAllocation。

---

## §7 FxSettlement 模型（结汇水单）

### 7.1 全量字段

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | String | @id | `FXS__${shortId}` |
| settlementNumber | String | @unique | 结汇水单号（如 FXS-20260808-001） |
| voucherId | String | — | 关联收款凭证（settlesVoucher linkKind） |
| orderId | String? | — | 冗余关联订单（aboutOrder linkKind） |
| customerRelationId | String? | — | 交易对象（billTo linkKind） |
| settleDate | String | — | 结汇日期 YYYY-MM-DD |
| foreignAmount | Decimal | @db.Decimal(18,4) | 结汇外币金额（≤ 凭证未结汇余额） |
| currency | String | — | 外币币种（与凭证一致；CNY 无结汇语义） |
| fxRate | Decimal | @db.Decimal(18,8) | 结汇汇率（外币 → CNY） |
| cnyAmount | Decimal | @db.Decimal(18,4) | 折合人民币（服务端计算 = foreignAmount × fxRate） |
| bank | String? | — | 结汇银行 |
| slipNumber | String? | — | 银行水单号 |
| notes | String? | — | 备注 |
| createdAt | BigInt | — | 创建时间 |
| updatedAt | BigInt | — | 更新时间 |
| deletedAt | BigInt? | — | 软删时间 |

### 7.2 索引

```
@@index([voucherId]) @@index([orderId])
@@index([customerRelationId]) @@index([settleDate])
```

> **业务语义**：一笔外币收款可分次结汇（1:N），核销余额 = voucher.amount - Σsettlements.foreignAmount。cnyAmount 由服务端计算（防篡改），客户端传入一律拒绝。软删即回滚核销余额（derived 查询，无冗余字段）。
>
> **DR-021 收付汇真实记账**：收汇（FxSettlement）与付汇（OutwardRemittance）必须按真实业务发生记账，不强制一一对应。收汇多付汇少或收汇少付汇多均为正常业务场景，系统不强制平衡校验。

---

## §8 OutwardRemittance 模型（付汇水单）

### 8.1 全量字段

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | String | @id | `OWR__${shortId}` |
| remittanceNumber | String | @unique | 付汇水单号（如 OWR-20260808-001） |
| voucherId | String | — | 关联付款凭证（paysVoucher linkKind） |
| orderId | String? | — | 冗余关联订单（aboutOrder linkKind） |
| customerRelationId | String? | — | 交易对象（billTo linkKind） |
| remitDate | String | — | 付汇日期 YYYY-MM-DD |
| foreignAmount | Decimal | @db.Decimal(18,4) | 付汇外币金额（≤ 凭证未付余额） |
| currency | String | — | 外币币种（与凭证一致；CNY 无付汇语义） |
| fxRate | Decimal | @db.Decimal(18,8) | 购汇汇率（外币 → CNY） |
| cnyAmount | Decimal | @db.Decimal(18,4) | 折合人民币（服务端计算） |
| payeeName | String? | — | 境外收款人名称 |
| payeeBank | String? | — | 收款行 |
| payeeSwift | String? | — | SWIFT 代码 |
| purpose | String? | — | GoodsPayment \| Freight \| Insurance \| Commission \| Other |
| bank | String? | — | 付汇银行 |
| slipNumber | String? | — | 银行水单号 |
| notes | String? | — | 备注 |
| createdAt | BigInt | — | 创建时间 |
| updatedAt | BigInt | — | 更新时间 |
| deletedAt | BigInt? | — | 软删时间 |

### 8.2 索引

```
@@index([voucherId]) @@index([orderId])
@@index([customerRelationId]) @@index([remitDate])
```

> **镜像 FxSettlement 付款侧**：一笔外币付款可分次付汇（1:N），未付余额 = voucher.amount - Σremittances.foreignAmount。cnyAmount 服务端计算，软删回滚余额。
>
> **DR-021 收付汇真实记账**：付汇与收汇按真实业务发生记账，不强制一一对应。付汇多收汇少（如预付采购款）或收汇多付汇少（如客户定金大于采购支出）均为正常业务场景。

---

## §9 VatInvoice 模型（增值税发票）

### 9.1 税务标识 + 类型/状态

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | String | @id | `VAT__${shortId}` |
| vatCode | String? | @@unique([vatCode, vatNumber]) | 发票代码（纸质 10/12 位；全电票 NULL） |
| vatNumber | String | @@unique([vatCode, vatNumber]) | 发票号码（纸质 8 位；全电票 20 位） |
| direction | String | @default("Input") | Input(进项) \| Output(销项) |
| invoiceType | String | @default("Special") | Special(专票) \| Normal(普票) |
| status | String | @default("Received") | Received \| Verified \| Declared \| RedFlushed \| Cancelled |

### 9.2 开票方/受票方 + 金额

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| sellerName | String | — | 销售方名称 |
| sellerTaxNo | String? | — | 销售方纳税人识别号 |
| buyerName | String | — | 购买方名称 |
| buyerTaxNo | String? | — | 购买方纳税人识别号 |
| issueDate | String | — | 开票日期 YYYY-MM-DD |
| netAmount | Decimal | @db.Decimal(18,4) | 不含税金额 |
| taxRate | Decimal | @db.Decimal(6,4) | 税率(%)，如 13.0000 |
| taxAmount | Decimal | @db.Decimal(18,4) | 税额 |
| totalAmount | Decimal | @db.Decimal(18,4) | 价税合计 = netAmount + taxAmount |
| currency | String | @default("CNY") | 币种（增值税以人民币计） |

### 9.3 认证/退税/红冲 + 关联

| 字段 | 类型 | 说明 |
|------|------|------|
| verifiedDate | String? | 勾选认证日期 YYYY-MM-DD |
| deductionPeriod | String? | 勾选所属期 YYYY-MM |
| taxRefundId | String? | 关联退税申报（→Declared 必填） |
| redFlushNumber | String? | 红字发票号码 |
| redFlushDate | String? | 红冲日期 YYYY-MM-DD |
| invoiceId | String? | 关联业务发票（settlesInvoice linkKind） |
| orderId | String? | 关联订单（aboutOrder linkKind） |
| relationId | String? | 供应商/客户（billTo linkKind） |

### 9.4 索引

```
@@unique([vatCode, vatNumber])
@@index([status]) @@index([direction]) @@index([relationId])
@@index([taxRefundId]) @@index([invoiceId]) @@index([issueDate])
```

> **金额三栏校验**：totalAmount == round4(netAmount + taxAmount) 精确一致；taxAmount ≈ round4(netAmount × taxRate/100) 容差 0.02（开票尾差）。**退税联动**：→Declared 强制 Input + Special + taxRefundId 有效。

---

## §10 TaxRefund 模型（退税申报）

### 10.1 全量字段

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | String | @id | `TR_${shortId}` |
| refundNumber | String | @unique | 退税申报编号 |
| declarationId | String? | — | 关联报关单 |
| orderId | String? | — | 关联订单 |
| relationId | String? | — | 关联客户 |
| status | String | @default("Draft") | Draft \| Submitted \| Reviewing \| Approved \| Rejected \| Refunded \| Cancelled |
| exportDate | String? | — | 出口日期 |
| declarationDate | String? | — | 申报日期 |
| fxRate | Decimal? | @db.Decimal(18,8) | 外汇汇率 |
| exportAmountFob | Decimal? | @db.Decimal(18,4) | 出口 FOB 金额（原币） |
| exportAmountFobCurrency | String? | — | 出口金额币种 |
| exportAmountCny | Decimal? | @db.Decimal(18,4) | 折人民币金额 |
| refundableVat | Decimal? | @db.Decimal(18,4) | 可退增值税额 |
| refundableRate | Decimal? | @db.Decimal(6,4) | 退税率(%) |
| refundAmount | Decimal? | @db.Decimal(18,4) | 实际退税额 |
| refundDate | String? | — | 退税到账日期 |
| reviewedBy | String? | — | 审核人 |
| reviewedAt | BigInt? | — | 审核时间 |
| reviewNotes | String? | — | 审核意见 |
| notes | String? | — | 备注 |
| createdAt | BigInt | — | 创建时间 |
| updatedAt | BigInt | — | 更新时间 |
| deletedAt | BigInt? | — | 软删时间 |

### 10.2 索引

```
@@index([declarationId]) @@index([orderId])
@@index([relationId]) @@index([status])
```

> **退税计算口径**：出口退税额 = 进项专票 netAmount × refundableRate。TaxRefund 与 VatInvoice 是 1:N 关系（一张退税申报可关联多张进项专票）。

---

## §11 OrderProfitSheet 模型（订单利润表）

### 11.1 全量字段

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | String | @id | `OPS__${shortId}` |
| orderId | String | @unique | 关联订单（一单一张，幂等真源） |
| baseCurrency | String | @default("CNY") | 本位币 |
| salesRevenue | Decimal | @default(0) @db.Decimal(18,4) | 销售收入（CNY） |
| purchaseCost | Decimal | @default(0) @db.Decimal(18,4) | 采购成本（CNY） |
| freightCost | Decimal | @default(0) @db.Decimal(18,4) | 运费（CNY） |
| miscCost | Decimal | @default(0) @db.Decimal(18,4) | 杂费（CNY） |
| grossProfit | Decimal | @default(0) @db.Decimal(18,4) | 毛利 = 收入 - 成本 - 运费 - 杂费 |
| grossMargin | Decimal? | @db.Decimal(8,4) | 毛利率 %（salesRevenue=0 时 null） |
| details | Json | — | 四维明细 + unconverted 披露 |
| version | Int | @default(1) | 版本号，重生成 +1 |
| generatedAt | BigInt | — | 生成时间戳 |
| createdAt | BigInt | — | 创建时间 |
| updatedAt | BigInt | — | 更新时间 |

### 11.2 索引

```
@@index([generatedAt])
```

> **无软删字段**：利润表是聚合投影，`deleteProfitSheet` 是硬删除，重生成即可恢复。orderId @unique 保证幂等（重生成=覆盖更新，version+1）。details JSON 含 sales/purchases/freight/misc/unconverted 五个数组。
>
> **DR-016 运费计算规则**：仅我方承担运费（freightResponsibility = prepaid 预付 / collect 代垫）才纳入 freightCost；到付（freightCollect）不纳入。
>
> **DR-020 费用归属**：miscCost 按归属对象计入——订单相关费用计入订单利润表；供应商/客户/样品/开发案例相关费用分别归集；内部运营费用不进入订单利润表。
>
> **DR-022 成本分开核算**：业务成本（采购+运费+杂费）与样品快递费、客户报销费用分别核算。样品快递费默认计入内部运营成本（不进入订单利润表）；客户报销费用单独归集，不混入订单成本。

---

## §12 CommissionRule 模型（佣金规则）

### 12.1 全量字段

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | String | @id | `CR__${shortId}` |
| name | String | — | 规则名（如 "E5-某代理"） |
| rate | Decimal | @db.Decimal(8,4) | 佣金率百分比（5 = E5 / 10 = E10） |
| intermediaryRelationId | String? | — | 中间人 Relation snapshot FK（空=默认规则） |
| intermediaryName | String? | — | 中间人名称快照 |
| isActive | Boolean | @default(true) | 是否启用 |
| notes | String? | — | 备注 |
| createdAt | BigInt | — | 创建时间 |
| updatedAt | BigInt | — | 更新时间 |
| deletedAt | BigInt? | — | 软删时间 |

### 12.2 索引

```
@@index([intermediaryRelationId, isActive])
@@index([isActive])
```

> **匹配口径**：intermediaryRelationId 精确命中优先；为空的记录为默认规则兜底。PricingCalculation.commissionRate 落库为数值快照（规则调整不回溯历史定价），commissionRuleId 记录来源规则。

---

## §13 待补模型规格（P2 缺口）

### 13.1 CommissionStatement（佣金结算单 · 规划）

| 字段（规划） | 类型 | 说明 |
|-------------|------|------|
| id | String @id | `CS__${shortId}` |
| statementNumber | String @unique | 结算单号 |
| period | String | 结算周期 YYYY-MM |
| intermediaryRelationId | String | 中间人 |
| commissionRuleId | String | 关联佣金规则 |
| orderIds | Json | 涉及订单列表 |
| totalCommissionBase | Decimal(18,4) | 佣金基数（订单销售额合计） |
| rate | Decimal(8,4) | 适用佣金率快照 |
| commissionAmount | Decimal(18,4) | 应付佣金 = 基数 × 率 |
| status | String | Draft \| Confirmed \| Paid |
| paidAt | BigInt? | 支付时间 |

### 13.2 CommissionPayout（佣金支付记录 · 规划）

| 字段（规划） | 类型 | 说明 |
|-------------|------|------|
| id | String @id | `CP__${shortId}` |
| payoutNumber | String @unique | 支付单号 |
| statementId | String | 关联结算单 |
| voucherId | String | 关联付款凭证（PaymentVoucher） |
| amount | Decimal(18,4) | 实付金额 |
| paymentDate | String | 支付日期 |
| currency | String | 币种 |

> 以上两模型为 P2 规划规格，尚未在 schema.prisma 中定义。当前佣金流程通过 CommissionRule（率配置）+ PricingCalculation（定价快照）覆盖，结算与支付流程待业务需求明确后补建。

---

## §14 通用约定

### 14.1 金额精度

| 用途 | 类型 | 精度 |
|------|------|------|
| 金额（发票/凭证/水单/利润） | Decimal(18,4) | 4 位小数 |
| 汇率 | Decimal(18,8) | 8 位小数（高精度避免累计误差） |
| 税率/退税率 | Decimal(6,4) | 4 位小数（如 13.0000） |
| 毛利率 | Decimal(8,4) | 4 位小数 |

### 14.2 时间戳

| 类型 | 存储 | 说明 |
|------|------|------|
| epoch ms | BigInt | createdAt / updatedAt / deletedAt |
| 业务日期 | String | YYYY-MM-DD（issueDate / paymentDate / settleDate 等） |
| 业务月份 | String | YYYY-MM（deductionPeriod） |

### 14.3 snapshot FK 模式

财务域所有跨模块关联均为 **snapshot FK**（无 Prisma @relation 强制 FK），通过 EntityLink 图谱同步维护。原因：
- 跨模块解耦（财务域不直接依赖订单/关系域的物理 FK）
- 历史快照不可变（Relation 改名不影响历史发票显示）
- EntityLink 提供反向追溯能力

### 14.4 软删策略

| 模型 | 软删 | 原因 |
|------|------|------|
| Invoice / PaymentVoucher / FxSettlement / OutwardRemittance / VatInvoice / TaxRefund / CommissionRule | ✅ deletedAt | 业务实体，需历史留痕 |
| InvoiceAllocation | ❌（硬删除） | 核销调整=delete+insert，避免软删+unique 冲突 |
| OrderProfitSheet | ❌（硬删除） | 聚合投影，重生成即可恢复 |

### 14.5 DR-016 费用归属与结算分离

| 项 | 规则 |
|---|------|
| 费用归属 | 费用按归属对象计入：订单 / 供应商 / 客户 / 样品 / 开发案例 / 内部运营 |
| 结算分离 | 费用归属 ≠ 实际结算：归属是会计分录，结算是资金流水 |
| 运费归属 | prepaid（预付）= 我方承担 → 纳入利润表；collect（到付）= 客户承担 → 不纳入；代垫 = 我方先付后收 → 纳入利润表 |
| 实际结算 | 以 PaymentVoucher 实际收付为准，不受归属影响 |

### 14.6 DR-020 报销付款统一审批链

| 项 | 规则 |
|---|------|
| 报销类型 | 差旅费 / 招待费 / 办公用品 / 其他运营费用 |
| 付款方式 | 报销付款统一走 PaymentRequest（付款申请）审批链（DR-017） |
| 归属规则 | 报销费用按归属对象计入对应科目（订单/供应商/客户/内部运营） |

### 14.7 DR-021 收付汇真实记账

| 项 | 规则 |
|---|------|
| 记账原则 | 收汇（FxSettlement）与付汇（OutwardRemittance）按真实业务发生记账 |
| 非强制对应 | 不强制收汇与付汇一一对应，不强制平衡校验 |
| 正常场景 | 收汇多付汇少（客户定金大于采购支出）、收汇少付汇多（预付采购款）均为正常业务场景 |

### 14.8 DR-022 成本分开核算

| 成本类型 | 归属 | 是否进入订单利润表 |
|---------|------|------------------|
| 业务成本（采购+运费+杂费） | 订单 | ✅ 进入 |
| 样品快递费 | 内部运营 | ❌ 默认不进入（可配置） |
| 客户报销费用 | 客户 | ❌ 单独归集，不混入订单成本 |

---

## §15 交叉链接

- [评审决策记录](../10-评审与决策/2026-08-16-设计评审决策记录.md) — DR-015~DR-022：发货与出运/运费与结算分离/付款申请/收款凭证锁定/预收款/费用归属/收付汇真实记账/成本分开核算
- [订单与生产模型组](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/docs/design/02-数据模型/订单与生产模型组.md) — Invoice.orderId / PaymentVoucher.orderId snapshot FK 指向 Order
- [客户与关系模型组](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/docs/design/02-数据模型/客户与关系模型组.md) — customerRelationId / relationId / intermediaryRelationId snapshot FK 指向 Relation
- [出运与报关模型组](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/docs/design/02-数据模型/出运与报关模型组.md) — TaxRefund.declarationId 关联报关单；运单运费纳入利润表
- [报价与成本模型组](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/docs/design/02-数据模型/报价与成本模型组.md) — PricingCalculation.commissionRuleId 关联 CommissionRule
- [FinanceManager 总览](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/docs/design/04-模块设计/05-财务与结算/FinanceManager总览.md) — 财务域前端入口
- [发票管理](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/docs/design/04-模块设计/05-财务与结算/发票管理.md) — Invoice 模型的业务流程文档
- [外汇结汇与差异损益](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/docs/design/04-模块设计/05-财务与结算/外汇结汇与差异损益.md) — FxSettlement 模型的业务流程文档
- [schema.prisma](file:///Users/qinwengu/WorkBuddy/Claw/apps/Bambook/server/prisma/schema.prisma) — 数据模型真源

---

## §16 相关文档索引

- [实体关系总览](实体关系总览.md) — 跨域 ER 总图
- [付款凭证管理](../04-模块设计/05-财务与结算/付款凭证管理.md) — PaymentVoucher 业务流程
- [应收账龄与核销](../04-模块设计/05-财务与结算/应收账龄与核销.md) — InvoiceAllocation 核销机制
- [汇出汇款](../04-模块设计/05-财务与结算/汇出汇款.md) — OutwardRemittance 业务流程
- [增值税发票台账](../04-模块设计/05-财务与结算/增值税发票台账.md) — VatInvoice 业务流程
- [订单利润表生成](../04-模块设计/05-财务与结算/订单利润表生成.md) — OrderProfitSheet 业务流程
