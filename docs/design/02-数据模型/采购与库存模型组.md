# 采购与库存模型组 — 全量字段表 + ER 图

> 模块代号：`Data Model / Purchase & Inventory`
> 关联代码：`server/prisma/schema.prisma` 2965–3915 行、`types.ts` 1985–2204 行
> 覆盖模型：`PurchaseOrder / PurchaseLine / MaterialReceipt / Warehouse（WarehouseLocation） / InventoryItem / StockMovement`
> 文档版本：v1.0 · 最后更新：2026-08-15

---

## §1 实现状态

| 维度 | 状态 | 说明 |
| --- | --- | --- |
| PurchaseOrder 模型 | ✅ 已落地 | schema.prisma 2965–3013 行，含供应商关联、金额、日期、条款、订单关联。 |
| PurchaseLine 模型 | ✅ 已落地 | schema.prisma 3015–3044 行，含物料信息、数量价格、收货跟踪。 |
| MaterialReceipt 模型 | ✅ 已落地 | schema.prisma 3046–3077 行，含收货信息、仓库、检验结果。 |
| Warehouse 模型 | ✅ 已落地 | schema.prisma 3815–3835 行，含编码、名称、类型、管理员。 |
| InventoryItem 模型 | ✅ 已落地 | schema.prisma 3837–3877 行，含仓库、物料、数量、成本、预警。 |
| StockMovement 模型 | ✅ 已落地 | schema.prisma 3879–3915 行，含变动类型、数量、余额、来源追溯。 |
| 服务层 | ✅ 已落地 | `server/src/inventory/inventoryService.ts / inventoryRoute.ts`、`server/src/orders/*`（采购单由 orders 模块承载）。 |
| TypeScript 类型 | ✅ 已落地 | `types.ts` 1985–2204 行对应 interface 定义。 |
| 软删除 | ✅ 已落地 | 所有模型含 `deletedAt BigInt?` / `String?`。 |
| 级联删除 | ✅ 已落地 | PurchaseLine / MaterialReceipt 外键 `onDelete: Cascade` 随 PurchaseOrder 删除。 |
| 索引 | ✅ 已落地 | 关键查询字段均建立索引（详见各模型字段表）。 |
| 测试 | ✅ 已落地 | `server/src/inventory/__tests__/inventoryService.test.ts`、`server/src/orders/__tests__/*`。 |

> **命名说明**：任务中提及的 `WarehouseLocation` 在实际 schema 中命名为 `Warehouse`（schema.prisma 3815 行），承载仓库主数据。`InventoryItem.locationCode` 字段承载库位编码（如 `A-01-03`），未独立建模为 `WarehouseLocation` 表。本文档以实际 schema 为准。

---

## §2 业务定位与目标

### 2.1 业务定位

采购与库存模型组是 Bambook 系统的「物料流转中枢」，承载从采购下单、收货检验、入库出库、库存预警到调拨盘点的全链路物料流转数据。

- **采购链**：`PurchaseOrder`（采购单）→ `PurchaseLine`（行项）→ `MaterialReceipt`（收料单）。
- **库存链**：`Warehouse`（仓库）→ `InventoryItem`（库存项）→ `StockMovement`（变动流水）。

两条链通过 `MaterialReceipt.warehouseId` 关联——收料检验合格后自动入库到指定仓库的 `InventoryItem`，并写入 `StockMovement` 流水。

### 2.2 核心目标

1. **采购单全生命周期**：Draft → Sent → Confirmed → PartiallyReceived → Received → Closed / Cancelled 七态状态机。
2. **收货检验闭环**：MaterialReceipt 承载检验结果（合格/不合格/部分合格），合格量自动入库。
3. **多仓库支持**：Warehouse 支持主仓/辅仓/临时仓/虚拟仓四类。
4. **库存项精细管理**：InventoryItem 按 仓库+物料 维度管理，含批次号、库位、锁定数量、成本、预警线。
5. **变动流水全程追溯**：StockMovement 记录入库/出库/调拨/盘点/锁定/解锁六类变动，含变动前后余额。
6. **来源追溯**：StockMovement.referenceType + referenceId 关联采购单/订单/货运/生产/调整/手动。
7. **库存预警**：minStock / maxStock 双线预警，触发 `StockLowAlarm` / `StockOverstockAlarm` 事件。

---

## §3 核心概念与术语

| 术语 | 定义 |
| --- | --- |
| 采购单 `PurchaseOrder` | 采购业务单据，承载供应商/金额/日期/条款 |
| 采购单号 `poNumber` | 业务采购单号（如 PO-20260806-001），全局唯一 |
| 采购行 `PurchaseLine` | 采购单行项，承载物料/数量/价格/收货跟踪 |
| 物料编码 `materialCode` | 行项关联产品档案的编码（面料/辅料） |
| 收料单 `MaterialReceipt` | 收货检验单据 |
| 收料单号 `receiptNumber` | 业务收料单号 |
| 检验状态 `status` | Pending/Inspected/Accepted/Rejected/PartiallyAccepted |
| 仓库 `Warehouse` | 仓库主数据 |
| 仓库类型 `type` | Main(主仓)/Auxiliary(辅仓)/Temporary(临时仓)/Virtual(虚拟仓) |
| 库存项 `InventoryItem` | 按 仓库+物料 维度的库存记录 |
| 库位 `locationCode` | 库位编码（如 A-01-03） |
| 批次号 `batchNumber` | 物料批次（可追溯） |
| 锁定数量 `lockedQuantity` | 已分配未出库数量 |
| 可用数量 `availableQuantity` | `quantity - lockedQuantity`（前端计算） |
| 变动流水 `StockMovement` | 库存变动记录 |
| 变动类型 `type` | Inbound/Outbound/Transfer/Adjustment/Lock/Unlock |
| 变动前后余额 `balanceBefore / balanceAfter` | 变动前后库存数量 |
| 来源追溯 `referenceType + referenceId` | 关联单据类型 + ID |
| 预警线 `minStock / maxStock` | 最低/最高库存预警线 |

---

## §4 数据模型概览

### 4.1 模型清单

| 模型 | schema 行号 | types 行号 | 字段数 | 用途 |
| --- | --- | --- | --- | --- |
| PurchaseOrder | 2965–3013 | 2026–2053 | 25 + 关联 | 采购单主表 |
| PurchaseLine | 3015–3044 | 1988–2004 | 14 + 关联 | 采购单行项 |
| MaterialReceipt | 3046–3077 | 2006–2024 | 17 + 关联 | 收料检验单 |
| Warehouse | 3815–3835 | 2103–2117 | 13 + 关联 | 仓库主数据 |
| InventoryItem | 3837–3877 | 2131–2157 | 22 + 关联 | 库存项 |
| StockMovement | 3879–3915 | 2177–2196 | 18 + 关联 | 变动流水 |

### 4.2 聚合关系图（ER 图）

```
┌─────────────────────────────────────────────────────────────────────┐
│                        PurchaseOrder  (采购单)                      │
│  id, poNumber(@unique), status, supplierRelationId, supplierName,  │
│  currency, totalAmount, exchangeRate, baseCurrency, orderDate,     │
│  expectedDeliveryDate, actualDeliveryDate, deliveryTerms,          │
│  paymentTerms, shipToAddress, orderId, quotationId, bomId,        │
│  buyer, notes, attachments, createdAt, updatedAt, deletedAt       │
└─────┬──────────────────────────────────────────────┬───────────────┘
      │ 1:N                                           │ 1:N
      ▼                                               ▼
┌──────────────────────┐              ┌──────────────────────────┐
│   PurchaseLine        │              │   MaterialReceipt        │
│  id, purchaseOrderId, │              │  id, receiptNumber,       │
│  lineNumber,          │              │  purchaseOrderId, status, │
│  materialCode,        │              │  receivedDate, receivedBy,│
│  description, category,│              │  inspectedBy, inspection,│
│  specification,       │              │  warehouseId, warehouseName,│
│  quantity, unit,      │              │  totalReceived, totalAccepted,│
│  unitPrice, amount,   │              │  totalRejected, rejection,│
│  receivedQuantity,    │              │  qualityNotes, notes,     │
│  rejectedQuantity,    │              │  createdAt                │
│  notes, createdAt    │              └──────────────┬───────────┘
└──────────────────────┘                             │
                                                     │ warehouseId
                                                     ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        Warehouse  (仓库)                            │
│  id, code(@unique), name, type, address, manager, phone,           │
│  isActive, sortOrder, notes, createdAt, updatedAt, deletedAt        │
└─────┬───────────────────────────────────────────────────────────────┘
      │ 1:N
      ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     InventoryItem  (库存项)                         │
│  id, warehouseId, productAssetId, materialCode, description,        │
│  category, specification, batchNumber, locationCode,               │
│  quantity, lockedQuantity, unit, unitCost, currency,               │
│  minStock, maxStock, lastInDate, lastOutDate, notes,                │
│  createdAt, updatedAt, deletedAt                                   │
└─────┬───────────────────────────────────────────────────────────────┘
      │ 1:N
      ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    StockMovement  (变动流水)                        │
│  id, movementNumber, type, itemId, warehouseId, targetWarehouseId, │
│  quantity, unit, unitCost, balanceBefore, balanceAfter,            │
│  reason, referenceType, referenceId, operator, movementDate,       │
│  notes, createdAt                                                  │
└─────────────────────────────────────────────────────────────────────┘

跨模块关联：
- PurchaseOrder.supplierRelationId ──► Relation ──► FactoryProfile
- PurchaseOrder.orderId ──► Order（销售订单）
- PurchaseOrder.quotationId ──► Quotation
- PurchaseOrder.bomId ──► BOM
- PurchaseLine.materialCode ──► ProductAsset.sku（松外键）
- InventoryItem.productAssetId ──► ProductAsset（强外键，可空）
- StockMovement.referenceType=PurchaseOrder + referenceId ──► PurchaseOrder
```

---

## §5 全量字段表

### 5.1 PurchaseOrder（schema 2965–3013 行）

| 字段 | Prisma 类型 | TS 类型 | 必填 | 索引 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `id` | String @id | string | ✅ | - | 主键（格式 `PO_${shortId}`） |
| `poNumber` | String @unique | string | ✅ | unique | 业务采购单号（如 PO-20260806-001） |
| `status` | String | PurchaseOrderStatus | ✅ | @@index | Draft/Sent/Confirmed/PartiallyReceived/Received/Closed/Cancelled |
| **供应商** | | | | | |
| `supplierRelationId` | String? | string? | - | @@index | 供应商 Relation ID |
| `supplierName` | String? | string? | - | - | 供应商名称快照 |
| `supplierCode` | String? | string? | - | - | 供应商编码 |
| **金额** | | | | | |
| `currency` | String | string | ✅ | - | USD/CNY/EUR |
| `totalAmount` | Decimal @db.Decimal(18,4) | number | ✅ | - | 采购总金额 |
| `exchangeRate` | Decimal? @db.Decimal(18,4) | number? | - | - | 汇率 |
| `baseCurrency` | String @default("CNY") | string | - | - | 本位币 |
| **日期** | | | | | |
| `orderDate` | String | string | ✅ | @@index | 下单日期 YYYY-MM-DD |
| `expectedDeliveryDate` | String? | string? | - | - | 预计交货日期 |
| `actualDeliveryDate` | String? | string? | - | - | 实际交货日期 |
| **条款** | | | | | |
| `deliveryTerms` | String? | string? | - | - | 交货条款 |
| `paymentTerms` | String? | string? | - | - | 付款条款 |
| `shipToAddress` | String? | string? | - | - | 收货地址 |
| **关联** | | | | | |
| `orderId` | String? | string? | - | @@index | 关联销售订单 |
| `quotationId` | String? | string? | - | - | 关联报价单 |
| `bomId` | String? | string? | - | - | 关联 BOM |
| **其他** | | | | | |
| `buyer` | String? | string? | - | - | 采购员 |
| `notes` | String? | string? | - | - | 备注 |
| `attachments` | Json? | any? | - | - | 附件 JSON |
| `createdAt` | BigInt | string | ✅ | - | 创建时间戳 |
| `updatedAt` | BigInt | string | ✅ | - | 更新时间戳 |
| `deletedAt` | BigInt? | string? | - | - | 软删除 |
| **关联** | | | | | |
| `lines` | PurchaseLine[] | PurchaseLine[] | - | - | 1:N 行项 |
| `receipts` | MaterialReceipt[] | MaterialReceipt[] | - | - | 1:N 收料单 |

### 5.2 PurchaseLine（schema 3015–3044 行）

| 字段 | Prisma 类型 | TS 类型 | 必填 | 索引 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `id` | String @id | string | ✅ | - | 主键 |
| `purchaseOrderId` | String | string | ✅ | @@index | 关联 PurchaseOrder，onDelete: Cascade |
| `lineNumber` | Int | number | ✅ | - | 行号 |
| **物料信息** | | | | | |
| `materialCode` | String? | string? | - | @@index | 物料编码（面料/辅料编码） |
| `description` | String | string | ✅ | - | 品名描述 |
| `category` | String? | string? | - | - | Fabric/Trimmings/Accessories/Other |
| `specification` | String? | string? | - | - | 规格描述 |
| **数量价格** | | | | | |
| `quantity` | Decimal @db.Decimal(18,4) | number | ✅ | - | 订单数量 |
| `unit` | String | string | ✅ | - | YD/M/KG/PC/SET |
| `unitPrice` | Decimal @db.Decimal(18,4) | number | ✅ | - | 单价 |
| `amount` | Decimal @db.Decimal(18,4) | number | ✅ | - | 行金额 = quantity * unitPrice |
| **收货跟踪** | | | | | |
| `receivedQuantity` | Decimal @default(0) @db.Decimal(18,4) | number | - | - | 已收数量 |
| `rejectedQuantity` | Decimal @default(0) @db.Decimal(18,4) | number | - | - | 不合格数量 |
| `notes` | String? | string? | - | - | 备注 |
| `createdAt` | BigInt | string | ✅ | - | 创建时间戳 |
| **关联** | | | | | |
| `purchaseOrder` | PurchaseOrder | - | ✅ | - | N:1 反向 |

### 5.3 MaterialReceipt（schema 3046–3077 行）

| 字段 | Prisma 类型 | TS 类型 | 必填 | 索引 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `id` | String @id | string | ✅ | - | 主键（格式 `MR_${shortId}`） |
| `receiptNumber` | String | string | ✅ | - | 收料单号 |
| `purchaseOrderId` | String | string | ✅ | @@index | 关联 PurchaseOrder，onDelete: Cascade |
| `status` | String | string | ✅ | @@index | Pending/Inspected/Accepted/Rejected/PartiallyAccepted |
| **收货信息** | | | | | |
| `receivedDate` | String | string | ✅ | @@index | 收货日期 YYYY-MM-DD |
| `receivedBy` | String? | string? | - | - | 收货人 |
| `inspectedBy` | String? | string? | - | - | 检验员 |
| `inspectionDate` | String? | string? | - | - | 检验日期 |
| **仓库** | | | | | |
| `warehouseId` | String? | string? | - | - | 收货仓库 ID |
| `warehouseName` | String? | string? | - | - | 仓库名称快照 |
| **检验结果** | | | | | |
| `totalReceived` | Decimal @db.Decimal(18,4) | number | ✅ | - | 总收货数量 |
| `totalAccepted` | Decimal @db.Decimal(18,4) | number | ✅ | - | 合格数量 |
| `totalRejected` | Decimal @db.Decimal(18,4) | number | ✅ | - | 不合格数量 |
| `rejectionReason` | String? | string? | - | - | 不合格原因 |
| `qualityNotes` | String? | string? | - | - | 质量备注 |
| `notes` | String? | string? | - | - | 备注 |
| `createdAt` | BigInt | string | ✅ | - | 创建时间戳 |
| **关联** | | | | | |
| `purchaseOrder` | PurchaseOrder | - | ✅ | - | N:1 反向 |

### 5.4 Warehouse（schema 3815–3835 行）

> 即任务描述中的 `WarehouseLocation`，实际 schema 命名为 `Warehouse`。

| 字段 | Prisma 类型 | TS 类型 | 必填 | 索引 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `id` | String @id | string | ✅ | - | 主键（格式 `WH_${shortId}`） |
| `code` | String @unique | string | ✅ | unique | 仓库编码（如 WH-001 / SH-MAIN） |
| `name` | String | string | ✅ | - | 仓库名称 |
| `type` | String | WarehouseType | ✅ | @@index | Main/Auxiliary/Temporary/Virtual |
| `address` | String? | string? | - | - | 仓库地址 |
| `manager` | String? | string? | - | - | 仓库管理员 |
| `phone` | String? | string? | - | - | 联系电话 |
| `isActive` | Boolean @default(true) | boolean | - | @@index | 是否启用 |
| `sortOrder` | Int @default(0) | number | - | - | 排序 |
| `notes` | String? | string? | - | - | 备注 |
| `createdAt` | BigInt | string | ✅ | - | 创建时间戳 |
| `updatedAt` | BigInt | string | ✅ | - | 更新时间戳 |
| `deletedAt` | BigInt? | string? | - | - | 软删除 |
| **关联** | | | | | |
| `items` | InventoryItem[] | - | - | - | 1:N 库存项 |

### 5.5 InventoryItem（schema 3837–3877 行）

| 字段 | Prisma 类型 | TS 类型 | 必填 | 索引 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `id` | String @id | string | ✅ | - | 主键（格式 `INV_${shortId}`） |
| `warehouseId` | String | string | ✅ | @@index | 关联 Warehouse |
| `productAssetId` | String? | string? | - | @@index | 关联产品档案（面料/辅料/成衣） |
| `materialCode` | String? | string? | - | @@index | 物料编码（可独立于产品档案） |
| `description` | String | string | ✅ | - | 品名描述 |
| `category` | String? | string? | - | @@index | Fabric/Trimmings/Accessories/Garment/Other |
| `specification` | String? | string? | - | - | 规格描述 |
| `batchNumber` | String? | string? | - | @@index | 批次号（可追溯） |
| `locationCode` | String? | string? | - | - | 库位编码（如 A-01-03） |
| **数量** | | | | | |
| `quantity` | Decimal @db.Decimal(18,4) | number | ✅ | - | 当前库存数量 |
| `lockedQuantity` | Decimal @default(0) @db.Decimal(18,4) | number | - | - | 锁定/预留数量 |
| `unit` | String | string | ✅ | - | YD/M/KG/PC/SET |
| **成本** | | | | | |
| `unitCost` | Decimal? @db.Decimal(18,4) | number? | - | - | 单位成本 |
| `currency` | String @default("CNY") | string | - | - | 币种 |
| **预警** | | | | | |
| `minStock` | Decimal? @db.Decimal(18,4) | number? | - | - | 最低库存预警线 |
| `maxStock` | Decimal? @db.Decimal(18,4) | number? | - | - | 最高库存预警线 |
| **日期** | | | | | |
| `lastInDate` | String? | string? | - | - | 最后入库日期 |
| `lastOutDate` | String? | string? | - | - | 最后出库日期 |
| `notes` | String? | string? | - | - | 备注 |
| `createdAt` | BigInt | string | ✅ | - | 创建时间戳 |
| `updatedAt` | BigInt | string | ✅ | - | 更新时间戳 |
| `deletedAt` | BigInt? | string? | - | - | 软删除 |
| **关联** | | | | | |
| `warehouse` | Warehouse | Warehouse? | ✅ | - | N:1 反向 |
| `movements` | StockMovement[] | StockMovement[] | - | - | 1:N 变动流水 |
| **派生字段** | | | | | |
| `availableQuantity` | - | number | - | - | 前端计算：quantity - lockedQuantity |

### 5.6 StockMovement（schema 3879–3915 行）

| 字段 | Prisma 类型 | TS 类型 | 必填 | 索引 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `id` | String @id | string | ✅ | - | 主键（格式 `SM_${shortId}`） |
| `movementNumber` | String | string | ✅ | - | 流水单号（如 SM-20260806-001） |
| `type` | String | StockMovementType | ✅ | @@index | Inbound/Outbound/Transfer/Adjustment/Lock/Unlock |
| **关联库存项** | | | | | |
| `itemId` | String | string | ✅ | @@index | 关联 InventoryItem，onDelete: Cascade |
| `warehouseId` | String | string | ✅ | @@index | 操作仓库（源仓库） |
| `targetWarehouseId` | String? | string? | - | - | 调拨目标仓库（仅 Transfer） |
| **数量** | | | | | |
| `quantity` | Decimal @db.Decimal(18,4) | number | ✅ | - | 变动数量（Inbound 正/Outbound 负/Transfer 绝对值） |
| `unit` | String | string | ✅ | - | 单位 |
| `unitCost` | Decimal? @db.Decimal(18,4) | number? | - | - | 变动时单位成本 |
| `balanceBefore` | Decimal @db.Decimal(18,4) | number | ✅ | - | 变动前余额 |
| `balanceAfter` | Decimal @db.Decimal(18,4) | number | ✅ | - | 变动后余额 |
| **来源追溯** | | | | | |
| `reason` | String? | string? | - | - | 变动原因 |
| `referenceType` | String? | string? | - | @@index(组合) | PurchaseOrder/Order/Shipment/ProductionStage/Adjustment/Manual |
| `referenceId` | String? | string? | - | @@index(组合) | 关联单据 ID |
| **操作人** | | | | | |
| `operator` | String? | string? | - | - | 操作人 |
| `movementDate` | String | string | ✅ | @@index | 变动日期 YYYY-MM-DD |
| `notes` | String? | string? | - | - | 备注 |
| `createdAt` | BigInt | string | ✅ | - | 创建时间戳 |
| **关联** | | | | | |
| `item` | InventoryItem | - | ✅ | - | N:1 反向 |

---

## §6 关键流程

### 6.1 采购单创建流程

```
POST /api/purchase-orders
  body: PurchaseOrderInput { poNumber, currency, supplierRelationId, ..., lines[] }
  │
  ▼
1. assertNotBlacklisted(supplierRelationId)  // 黑名单校验
2. $transaction {
     a. PurchaseOrder.create（主表）
     b. PurchaseLine.create[]（行项，批量）
     c. totalAmount = sum(line.amount)
     d. writeAuditLog
   }
3. 发布 PurchaseOrderCreated 事件
4. 返回 PurchaseOrder（含 lines）
```

### 6.2 采购单状态流转

```
Draft ──发送──► Sent ──确认──► Confirmed ──部分收货──► PartiallyReceived ──全部收齐──► Received ──关闭──► Closed
                                                                                                      ▲
Draft / Sent / Confirmed / PartiallyReceived ──取消──► Cancelled

任意状态可软删 → Deleted
```

- **Draft**：草稿，可编辑。
- **Sent**：已发送供应商，计入工厂产能占用。
- **Confirmed**：供应商确认。
- **PartiallyReceived**：部分到货，仍计入产能占用。
- **Received**：全部到货，不再计入产能占用，触发交期评分。
- **Closed**：已关闭（如已付款）。
- **Cancelled**：已取消，不再计入产能占用。

### 6.3 收料检验流程

```
POST /api/purchase-orders/:id/receipts
  body: MaterialReceiptInput { receiptNumber, receivedDate, warehouseId, ... }
  │
  ▼
$transaction {
  1. MaterialReceipt.create
  2. 更新 PurchaseLine.receivedQuantity / rejectedQuantity
  3. if status === 'Accepted' or 'PartiallyAccepted':
       - 自动入库：InventoryItem.upsert（warehouseId + productAssetId/materialCode）
       - 写 StockMovement（type=Inbound, referenceType=PurchaseOrder）
  4. 更新 PurchaseOrder.status：
     - 全部行收齐 → Received
     - 部分行收齐 → PartiallyReceived
  5. writeAuditLog
}
  │
  ▼
发布 MaterialReceiptCreated 事件
  │
  ▼
若 status === 'Received'：
  - 触发交期自动评分（recordAutoEvaluation kind=delivery）
```

### 6.4 库存变动流程

```
POST /api/inventory/movements
  body: StockMovementInput { itemId, type, quantity, ... }
  │
  ▼
$transaction {
  1. 查 InventoryItem（itemId）
  2. 校验变动类型：
     - Inbound: quantity 正数，余额增加
     - Outbound: quantity 正数，余额减少（校验 sufficient stock，余额不可负）
     - Transfer: quantity 正数，源仓减 + 目标仓加
     - Adjustment: quantity 任意正负，余额调整
     - Lock: lockedQuantity 增加（quantity 正数）
     - Unlock: lockedQuantity 减少（quantity 正数）
  3. 计算 balanceBefore / balanceAfter
  4. InventoryItem.update（quantity / lockedQuantity / lastInDate / lastOutDate）
  5. StockMovement.create（含 balanceBefore / balanceAfter）
  6. writeAuditLog
}
  │
  ▼
if quantity < minStock → 发布 StockLowAlarm 事件
if quantity > maxStock → 发布 StockOverstockAlarm 事件
```

### 6.5 库存预警流程

```
库存变动后检查：
  │
  ▼
if item.quantity < item.minStock:
  发布 StockLowAlarm 事件
  payload: { itemId, warehouseId, materialCode, currentQuantity, minStock }
  │
  ▼
if item.quantity > item.maxStock:
  发布 StockOverstockAlarm 事件
  payload: { itemId, warehouseId, materialCode, currentQuantity, maxStock }
```

---

## §7 API 与服务契约

### 7.1 采购单端点（orders 模块）

| Method | Path | 用途 |
| --- | --- | --- |
| GET | `/api/purchase-orders` | 列表（支持 status / supplierRelationId / orderDate 筛选） |
| GET | `/api/purchase-orders/:id` | 详情（含 lines + receipts） |
| POST | `/api/purchase-orders` | 创建 |
| PUT | `/api/purchase-orders/:id` | 更新 |
| DELETE | `/api/purchase-orders/:id` | 软删除 |
| POST | `/api/purchase-orders/:id/send` | 发送（Draft → Sent） |
| POST | `/api/purchase-orders/:id/confirm` | 确认（Sent → Confirmed） |
| POST | `/api/purchase-orders/:id/cancel` | 取消 |
| POST | `/api/purchase-orders/:id/close` | 关闭 |
| POST | `/api/purchase-orders/:id/lines` | 追加行项 |
| PUT | `/api/purchase-orders/:id/lines/:lineId` | 更新行项 |
| DELETE | `/api/purchase-orders/:id/lines/:lineId` | 删除行项 |
| POST | `/api/purchase-orders/:id/receipts` | 创建收料单 |
| GET | `/api/purchase-orders/:id/receipts` | 收料单列表 |

### 7.2 库存端点（inventory 模块）

| Method | Path | 用途 |
| --- | --- | --- |
| GET | `/api/warehouses` | 仓库列表 |
| POST | `/api/warehouses` | 创建仓库 |
| PUT | `/api/warehouses/:id` | 更新仓库 |
| DELETE | `/api/warehouses/:id` | 软删除仓库 |
| GET | `/api/inventory-items` | 库存项列表（支持 warehouseId / productAssetId / materialCode 筛选） |
| GET | `/api/inventory-items/:id` | 库存项详情（含 movements） |
| POST | `/api/inventory-items` | 创建库存项 |
| PUT | `/api/inventory-items/:id` | 更新库存项 |
| DELETE | `/api/inventory-items/:id` | 软删除库存项 |
| POST | `/api/inventory/movements` | 创建变动（入库/出库/调拨/盘点/锁定/解锁） |
| GET | `/api/inventory/movements` | 变动流水列表（支持 itemId / type / movementDate 筛选） |

### 7.3 服务层契约

- `inventoryService`（`server/src/inventory/inventoryService.ts`）：
  - `createWarehouse / updateWarehouse / deleteWarehouse / listWarehouses`
  - `createItem / updateItem / deleteItem / listItems / getItem`
  - `createMovement`：事务内更新余额 + 写流水 + 审计 + 预警事件
  - 库存余额不可为负（Outbound 时校验 sufficient stock，fail-closed）

### 7.4 业务事件

- `PurchaseOrderCreated / Sent / Confirmed / PartiallyReceived / Received / Closed / Cancelled`
- `MaterialReceiptCreated`
- `StockMovementCreated`
- `StockLowAlarm / StockOverstockAlarm`

---

## §8 UI / UX 设计

详见 `components/PurchaseOrdersManager.tsx`（如存在）与 `components/InventoryManager.tsx`（如存在）相关文档。

---

## §9 权限与审计

### 9.1 权限

- 采购单 CRUD：登录用户均可。
- 库存变动：登录用户均可（建议后续按角色分级）。
- 库存调整（Adjustment）：建议管理员权限。

### 9.2 审计

- 所有 mutation 通过 `writeRouteAuditLog` 同事务写入 `AuditLog`。
- `targetType` 区分模型（PurchaseOrder / PurchaseLine / MaterialReceipt / Warehouse / InventoryItem / StockMovement）。
- StockMovement 含 `operator` 字段（操作人）。
- before/after 快照完整记录。

---

## §10 性能与扩展性

### 10.1 索引汇总

| 模型 | 索引字段 |
| --- | --- |
| PurchaseOrder | poNumber(unique), status, supplierRelationId, orderDate, orderId |
| PurchaseLine | purchaseOrderId, materialCode |
| MaterialReceipt | purchaseOrderId, status, receivedDate |
| Warehouse | code(unique), type, isActive |
| InventoryItem | warehouseId, productAssetId, materialCode, category, batchNumber |
| StockMovement | itemId, type, warehouseId, movementDate, referenceType+referenceId |

### 10.2 扩展性

- `PurchaseOrderStatus` 为 String，可扩展新状态。
- `StockMovementType` 为 String，可扩展新变动类型。
- `Warehouse.type` 为 String，可扩展新仓库类型。
- `referenceType` 为 String，可扩展新来源类型。
- `category` 为 String，可扩展新物料分类。

### 10.3 已知性能优化点

- 库存变动同事务更新余额 + 写流水，避免一致性误差。
- 变动流水按 `movementDate` 索引，支持按日期范围查询。
- 库存项按 `warehouseId + productAssetId / materialCode` 索引，支持快速定位。

---

## §11 错误处理与边界

### 11.1 错误场景

| 场景 | 错误信息 |
| --- | --- |
| 供应商黑名单 | `工厂已拉黑，禁止新建采购单` |
| poNumber 重复 | DB 唯一约束抛错 |
| 库存余额不足 | `库存余额不足`（Outbound 时） |
| 锁定数量超可用 | `锁定数量超过可用数量`（Lock 时） |
| 调拨缺目标仓 | `调拨必须指定目标仓库`（Transfer 时） |
| 仓库不存在 | `仓库 ${warehouseId} 不存在` |
| 库存项不存在 | `库存项 ${itemId} 不存在` |

### 11.2 边界

- `quantity` 为 Decimal(18,4)，允许小数（如 1.5 卷）。
- `lockedQuantity` 默认 0，不可为负。
- `availableQuantity` 前端计算：`quantity - lockedQuantity`。
- Outbound 时 `balanceAfter` 不可为负（fail-closed）。
- Transfer 时需 `targetWarehouseId`，源仓与目标仓不可相同。
- Adjustment 时 `quantity` 可正可负，余额可增可减。

---

## §12 测试策略

### 12.1 后端测试

- `server/src/inventory/__tests__/inventoryService.test.ts`：库存 CRUD + 变动 + 预警。
- `server/src/orders/__tests__/`：采购单全生命周期 + 行项 + 收料。

### 12.2 测试用例覆盖

- 采购单状态流转全分支。
- 收料检验合格/不合格/部分合格 → 入库/不入库。
- 库存变动六类（Inbound/Outbound/Transfer/Adjustment/Lock/Unlock）。
- 库存余额不足拒绝。
- 锁定数量超可用拒绝。
- 调拨缺目标仓拒绝。
- 预警触发（低于 minStock / 高于 maxStock）。
- 黑名单拦截 PO 创建。
- 软删除后查询过滤。

### 12.3 验证命令

```bash
cd server && npx vitest run src/inventory src/orders
```

---

## §13 部署与运维

- 与主服务同进程，无独立部署。
- Prisma 迁移：`prisma migrate deploy`。
- 库存预警事件订阅（邮件/IM 通知）可挂定时任务或事件总线。

---

## §14 已知限制与技术债务

1. **PurchaseLine.materialCode 松外键**：未强制约束关联 ProductAsset.sku，可能产生孤儿引用。
2. **InventoryItem 双重关联**：`productAssetId` 与 `materialCode` 可独立存在，可能导致同一物料双份库存记录。
3. **MaterialReceipt 无行级检验**：仅记录总收货/合格/不合格数量，未按行项细分检验结果。
4. **StockMovement 无库存快照**：仅记录变动前后余额，无定期库存快照（建议每日快照）。
5. **无批次先进先出**：批次号记录但未强制 FIFO/FEFO 出库。
6. **无多币种成本核算**：`unitCost + currency` 简单记录，未做汇率换算汇总。
7. **预警仅事件发布**：无主动通知（邮件/IM），仅事件订阅方可感知。
8. **Warehouse 无库位管理**：仅 `locationCode` 字段文本承载，无独立 WarehouseLocation 表（库位结构化管理）。

---

## §15 交叉链接

1. [`./产品与档案模型组.md`](./产品与档案模型组.md) — PurchaseLine.materialCode / InventoryItem.productAssetId 关联产品档案。
2. [`../04-模块设计/06-资源与支撑/Suppliers-供应商/模块概述.md`](../04-模块设计/06-资源与支撑/Suppliers-供应商/模块概述.md) — PurchaseOrder.supplierRelationId 关联工厂档案，拉黑后禁新建 PO。
3. [`../04-模块设计/06-资源与支撑/Suppliers-供应商/产能日历.md`](../04-模块设计/06-资源与支撑/Suppliers-供应商/产能日历.md) — 在手 PO（Sent/Confirmed/PartiallyReceived）按 expectedDeliveryDate 落月聚合占用工厂产能。
4. [`../04-模块设计/06-资源与支撑/Suppliers-供应商/自动评分公式.md`](../04-模块设计/06-资源与支撑/Suppliers-供应商/自动评分公式.md) — PurchaseOrder 收齐（Received）触发交期自动评分。
5. [`../04-模块设计/06-资源与支撑/Suppliers-供应商/黑名单与预警.md`](../04-模块设计/06-资源与支撑/Suppliers-供应商/黑名单与预警.md) — assertNotBlacklisted 拦截 PO 创建。
6. [`../04-模块设计/06-资源与支撑/Products-数字档案/成衣档案.md`](../04-模块设计/06-资源与支撑/Products-数字档案/成衣档案.md) — 成衣 BOM 触发采购单（成衣档案 → BOM → 采购）。
7. [`../01-架构总览/系统架构.md`](../01-架构总览/系统架构.md)（如已生成） — 采购与库存作为物料流转中枢的全局定位。

---

## §16 附录

### 16.1 采购单状态机速查

| 状态 | 含义 | 计入产能占用 | 可编辑 | 可取消 |
| --- | --- | --- | --- | --- |
| Draft | 草稿 | ❌ | ✅ | ✅ |
| Sent | 已发送 | ✅ | ❌ | ✅ |
| Confirmed | 已确认 | ✅ | ❌ | ✅ |
| PartiallyReceived | 部分到货 | ✅ | ❌ | ✅ |
| Received | 全部到货 | ❌ | ❌ | ❌ |
| Closed | 已关闭 | ❌ | ❌ | ❌ |
| Cancelled | 已取消 | ❌ | ❌ | ❌ |

### 16.2 库存变动类型速查

| 类型 | 含义 | quantity 符号 | 余额影响 | 必填字段 |
| --- | --- | --- | --- | --- |
| Inbound | 入库 | 正数 | 增加 | quantity, unit |
| Outbound | 出库 | 正数（内部取负） | 减少（不可为负） | quantity, unit |
| Transfer | 调拨 | 正数（绝对值） | 源仓减 + 目标仓加 | quantity, unit, targetWarehouseId |
| Adjustment | 盘点调整 | 任意正负 | 增减 | quantity, unit, reason |
| Lock | 锁定 | 正数 | lockedQuantity 增加 | quantity, unit |
| Unlock | 解锁 | 正数 | lockedQuantity 减少 | quantity, unit |

### 16.3 关键文件清单

| 文件 | 用途 |
| --- | --- |
| `server/src/inventory/inventoryService.ts` | 库存服务（仓库/库存项/变动） |
| `server/src/inventory/inventoryRoute.ts` | 库存 REST 端点 |
| `server/src/orders/orderServiceV2.ts` | 采购单服务 v2 |
| `server/src/orders/orderLifecycleService.ts` | 采购单生命周期 |
| `server/src/orders/routeV2.ts` | 采购单 REST 端点 v2 |
| `server/src/orders/orderLineMutationService.ts` | 行项变更服务 |
| `server/src/inventory/__tests__/inventoryService.test.ts` | 库存测试 |
| `server/src/orders/__tests__/*` | 采购单测试 |
| `server/prisma/schema.prisma` 2965–3915 行 | 6 个模型定义 |
| `types.ts` 1985–2204 行 | TypeScript 类型定义 |
