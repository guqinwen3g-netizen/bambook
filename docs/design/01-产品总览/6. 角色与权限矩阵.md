# 角色与权限矩阵 / Role & Permission Matrix

> **DR-023 关联**：超级管理员（SuperAdmin）系统兜底权限与业务审批权分离——技术超级管理员身份不等于业务审批权限；总领导的业务审批权通过业务审批 scope 和组织关系授权
> **DR-024 关联**：领导可视化与权限最小化——领导（销售主管/总领导）默认可见全公司数据，但业务操作权限按组织归属最小化
> **DR-025 关联**：组织/团队/归属影响范围——数据范围规则按组织归属（departmentId → Department.head）解析，不按固定角色硬编码
> **DR-026 关联**：公共资料与内部资料分离——产品/供应商/客户资料分公共资料（全员可见）与内部资料（按归属部门可见）
> **DR-027 关联**：供应商产品能力直写——供应商档案直接维护产品能力（面料/成衣/辅料），不通过产品档案间接关联
> **DR-028 关联**：其他贸易完整记录——其他贸易保留采购、销售、结算、附件与审计能力，默认仅强制负责人/业务方向/交易对方/记录日期
> **DR-029 关联**：出运/履约/收款分开——出运（Shipment）、履约（Order fulfillment）、收款（PaymentVoucher）为三个独立阶段，不混同
> **DR-030 关联**：经营总览页面区块——经营总览提供统一 KPI 投影（订单数/金额/准交率/利润），按业务线（服装/面料/其他）分区展示
> **DR-031 关联**：完整字段清单与流程图规范——设计文档须包含完整字段清单（字段名/类型/约束/说明）与流程图（ASCII/表格），禁止省略

## §1 元信息

| 项 | 值 |
|---|---|
| 定位 | 系统权限真源：**7 系统内置角色**（宽泛职能容器）× 60+ 权限 scope × 5 敏感字段 × 4 行级数据范围的完整矩阵，是 seed 脚本与运行时守卫的唯一 fallback 真源。**DR-007 审批路由按组织归属**（departmentId → 部门主管 → 总领导兜底，不按固定阈值硬编码）；DR-023：SuperAdmin 技术兜底权限与业务审批权分离；DR-024：领导可视化全公司数据，操作权限最小化；DR-025：数据范围按组织归属解析；DR-026：资料分公共/内部两级；DR-027：供应商产品能力直写；DR-028：其他贸易完整记录；DR-029：出运/履约/收款分开；DR-030：经营总览统一 KPI 投影；DR-031：文档须含完整字段清单与流程图 |
| 代码关联 | `lib/rolePermissionMatrix.ts`（根目录权威真源）/ `server/src/_shared/rolePermissionMatrix.ts`（服务器端副本，绕过 rootDir 限制）/ `server/src/auth/permissionService.ts`（运行时 scope fallback + 行级范围 + 敏感字段遮罩）/ `server/src/auth/permissionGuard.ts`（守卫中间件 fallback 判断）/ `server/src/permissions/resolveReviewerByDepartment.ts`（DR-007 组织归属解析待实现） |
| Prisma 关联 | `Role` / `Permission` / `RolePermission` 表（运行时可 UI 调整，初始化 seed 从本矩阵取默认值） |
| PRD 关联 | PRD §9.6 角色权限矩阵（PL-1A 贸易公司典型矩阵 + PL-2B 同部门互相可见跨部门隔离 + 10 处用户选择对齐，角色数已收敛至 7 宽泛容器） |
| 实现状态 | ✅ 6 系统内置角色 seed 已完成（**需升级至 7 角色：删除 FinanceManager，新增 QC/后勤**）；✅ 60+ scope 常量 + 35 View→scope 映射已定义；✅ `hasPermission()` / `canViewSensitive()` / `getDataScopeRule()` 运行时函数已实现；✅ seed-rbac.ts 使用根目录 lib 真源；⚠️ `server/src/_shared/rolePermissionMatrix.ts` 副本需手工同步（无自动校验）；⚠️ AdminPanel UI 尚未完整接入 RBAC 管理界面；⚠️ DR-007 组织归属解析（`resolveReviewerByDepartment.ts`）待实现；⚠️ ApprovalRequest reviewerId 创建时非空约束待 migration（见 [5. 全局交互规范.md](./5.%20全局交互规范.md) §14 GAP-G7/G8） |
| 文档版本 | v1.1 |
| 最后更新 | 2026-08-16 |

---

## §2 角色清单（7 系统内置角色，宽泛职能容器，不可删除 isSystem=true）

真源：`SYSTEM_ROLE_IDS` + `SYSTEM_ROLE_META`

> **7 角色设计原则**：宽泛职能容器（不细分岗位），按「业务线（服装/面料/小组）」的 departmentId 进一步切分数据范围和审批路由。所有实际岗位都映射到这 7 个容器，避免角色爆炸。

| Role ID 常量 | DB Role ID | 中文名 | rank | 职能定位（宽泛容器） | 审批权规则 |
|---|---|---|---|---|---|
| `SYSTEM_ROLE_IDS.SALES` | `role-sales` | **业务员** | 1 | 销售一线：报价/跟单/开发/采购申请/付款申请/客户维护；宽泛容器实际包含：服装业务员、面料业务员、业务助理、采购员、开发员等各类一线业务执行角色 | **无审批权**（只发起，审批给上级） |
| `SYSTEM_ROLE_IDS.FINANCE` | `role-finance` | **财务** | 2 | 财务执行：应收应付/发票签发核销/收付款凭证录入/VAT登记/退税申报/银行对接；**审批权与执行权分离**——财务是「付款执行者」（已 approved 的付款需求去银行付出，然后点已付款），不是「付款审批者」 | 退税申报审批（`tax:approve` 财务专业工作）；发票签发（`invoices:write`）；核销执行；**无任何业务审批权**（付款审批/坏账核销不归财务） |
| `SYSTEM_ROLE_IDS.QC` | `role-qc` | **QC** | 2 | 质量工艺：面料/成衣检验、疵点录入、质检报告、工艺标准维护、BOM 版本状态管理；宽泛容器包含面料 QC、成衣 QC、跟单 QC、工艺技术员 | QC 工作流审批（内部）；BOM 版本管理（`bom:admin`）；**无业务审批权**（报价/订单/付款不归 QC） |
| `SYSTEM_ROLE_IDS.LOGISTICS` | `role-logistics` | **后勤** | 2 | 物流单证仓储：发货装箱登记、报关单证制作、物流追踪、采购订单录入、仓库管理；宽泛容器包含单证员、物流专员、仓储管理员、采购助理（执行层面） | 单证模板配置（`customs:admin`）；发货登记（`shipments:write`）；**无业务审批权**（发货放行审批归销售主管/总领导） |
| `SYSTEM_ROLE_IDS.SALES_MANAGER` | `role-sales-manager` | **销售主管** | 3 | 小团队负责人：服装一组主管、服装二组主管、面料一组主管、面料二组主管、客户品牌组主管等；审批本团队业务（DR-007 组织归属：department.managerId = 本人） | **本团队审批权**（报价偏差/交期变更/数量变更/发货放行/客诉扣赔/采购PO审批/价格变更双签之一）；DR-024：可见全公司数据，操作限本团队 |
| `SYSTEM_ROLE_IDS.ADMIN` | `role-admin` | **系统管理员（总领导）** | 4 | 总领导/总经理/总监级（业务兜底审批人）；宽泛职能：用户角色配置、字典配置、知识库审批 + **跨团队业务兜底审批**（DR-007：无对应部门主管或跨团队大单 → 路由到总领导）；HR 人事管理归此角色 | **全公司兜底审批权**（付款审批兜底/坏账核销/价格变更双签之二/订单取消兜底/信用额度调整/利润率配置/权限矩阵 UI 管理等）；DR-024：可见全公司数据 + 大部分敏感字段（薪酬除外） |
| `SYSTEM_ROLE_IDS.SUPER_ADMIN` | `role-super-admin` | **超级管理员（老板）** | 10 | 最高权限；技术兜底（所有模块 R/W/D/A，权限配置，审计日志全可见，不可删除此角色账户）。**DR-023 严格分离**：技术兜底权限 ≠ 业务审批权；日常业务审批按 DR-007 路由到对应 reviewer（SM/总领导），业务审批不依赖 SuperAdmin 技术身份直接放行 | 全部技术权限；业务审批权通过组织归属授权（如需老板亲自批某张审批，在 ApprovalRequest 中 reviewerId 指向老板的 UserAccount ID，而不是靠 SuperAdmin 运行时特判绕过审批链） |

---

## §3 权限 scope 全量清单（60+ scope，分 6 域）

真源：`PERMISSION_SCOPES` 对象（`as const satisfies Record<string, string>`）

### §3.1 经营总览域（9 scope）

| scope | 说明 |
|---|---|
| `dashboard:read` | 全景看板查看 |
| `cockpit:read` | 经营驾驶舱查看（含财务敏感KPI） |
| `reports:read` | 报表中心查看与运行 |
| `reports:write` | 报表设计器/保存自定义报表 |
| `reports:admin` | 报表订阅推送/全员模板管理 |
| `ai:chat` | AI助手对话（含只读查询） |
| `ai:write:low` | AI助手执行低风险写入（登记跟进/加标签） |
| `ai:write:medium` | AI助手执行中风险写入（需确认） |
| `ai:write:high` | AI助手执行高风险写入（需二次验证） |

### §3.2 客户与市场域（13 scope）

| scope | 说明 |
|---|---|
| `relations:read` | 业务伙伴档案查看 |
| `relations:write` | 业务伙伴档案新增/编辑 |
| `relations:delete` | 业务伙伴档案删除（高危） |
| `relations:admin` | 客户等级ABC/信用评级人工调整 |
| `crm:read` | CRM跟进记录查看 |
| `crm:write` | CRM跟进记录登记/编辑 |
| `suppliers:read` | 供应商档案查看 |
| `suppliers:write` | 供应商档案新增/编辑 |
| `suppliers:admin` | 供应商评级调整/暂停新单/解除 |
| `seasons:read` | 季节/季度看板查看 |
| `seasons:write` | 季节项目创建/编辑 |
| `marketing:read` | 营销活动与ROI查看 |
| `marketing:write` | 营销活动创建/编辑 |
| `emails:read` | 业务邮件查看 |
| `emails:write` | 邮件发送/草稿编辑 |
| `emails:admin` | 邮件账户绑定/全局分类规则配置 |

### §3.3 订单履约域（24 scope）

| scope | 说明 |
|---|---|
| `orders:read` | 订单管理查看 |
| `orders:write` | 订单创建/编辑（Draft/Confirmed阶段） |
| `orders:delete` | 订单删除（高危） |
| `orders:approve:change_delivery` | 订单变更-交期审批（DR-007 按组织归属：SM 或 总领导兜底，不再按≤7天阈值分档） |
| `orders:approve:change_qty` | 订单变更-数量审批（DR-007 按组织归属：SM 或 总领导兜底，不再按±10%阈值分档） |
| `orders:approve:change_price` | 订单变更-价格 **双签**（SM + 总领导系统管理员，2 个 reviewerId 都批完才 approved；DR-007 解析到两者） |
| `orders:approve:cancel` | 订单取消审批（按 DR-007 上抬至总领导；approved 后进入 DR-010 取消流程：取消申请中→影响汇总→结案处理） |
| `quotations:read` | 报价单查看 |
| `quotations:write` | 报价单创建/编辑 |
| `quotations:approve` | 报价单审批（偏差warn/block级；DR-007 组织归属：SM 或 总领导兜底） |
| `quotations:convert` | 报价单转化为订单 |
| `products:read` | 产品数字档案查看 |
| `products:write` | 产品档案新增/编辑 |
| `bom:read` | BOM成本核算查看 |
| `bom:write` | BOM创建/编辑（Draft态） |
| `bom:admin` | BOM版本状态变更（Active/Obsolete）+ 新增自定义计算模型（归 QC + 销售主管共同拥有） |
| `production:read` | 生产跟踪/阶段看板查看 |
| `production:write` | 生产阶段登记/QC疵点录入 |
| `shipments:read` | 运单与发货查看 |
| `shipments:write` | 运单创建/装箱/发货登记（归后勤执行 + 业务员 + QC 填写） |
| `shipments:approve` | 发货放行审批（大货最终放行；DR-007 组织归属：SM 或 总领导兜底） |
| `customs:read` | 报关单证/外贸单证查看 |
| `customs:write` | 单证签发/修正案创建（归后勤 + 业务员） |
| `customs:admin` | 单证模板/公司抬头签章配置（归后勤） |
| `qc:read` | 质检工作台/疵点记录查看 |
| `qc:write` | QC疵点录入/QC结果登记（主要归 QC 角色，业务员可协助录入） |
| `aftersales:read` | 售后客诉登记查看 |
| `aftersales:write` | 客诉登记/扣赔处理录入 |
| `aftersales:approve` | 客诉扣赔/折让审批（影响应收金额；DR-007 组织归属：SM） |
| `procurement:read` | 采购订单PO查看 |
| `procurement:write` | 采购订单PO创建/编辑（业务员 + 后勤执行） |
| `procurement:approve` | 采购订单PO审批（DR-007：SM 或 总领导兜底；首单供应商+1级审批→自动上抬到总领导） |

### §3.4 财务与成本域（22 scope）

> **关键原则：审批权与执行权分离（DR-017 付款申请与审批）**。付款审批=业务合理性判断（该不该付）→归 SM/总领导；付款执行=去银行实际付钱→归财务。

| scope | 说明 |
|---|---|
| `finance:read` | 财务看板/账龄/报表查看 |
| `invoices:read` | 应收应付发票查看 |
| `invoices:write` | 发票创建/签发（Commercial Invoice；业务员可创建不带章的 PI，发票签发归财务 + 主管审批敲章） |
| `invoices:reconcile` | 发票核销（收付款分配到发票；归财务执行） |
| `invoices:approve:writeoff` | **坏账核销/发票作废审批**（归总领导系统管理员，不归财务；风险高） |
| `vouchers:read` | 收付款凭证查看 |
| `vouchers:write` | 收付款凭证录入（归财务） |
| `vouchers:approve:pay_lt5` | **付款审批（小单档，兼容保留名）**（DR-007 实际按组织归属解析，不再按 5 万阈值；SM + 总领导均持有） |
| `vouchers:approve:pay_gt5` | **付款审批（大单档，兼容保留名）**（DR-007 实际按组织归属解析，不再按 5 万阈值；总领导主要持有） |
| `vouchers:approve:pay_new_supplier` | **首单付款 +1 级审批**（自动上抬：原本 SM 批 → 自动提升到总领导批） |
| `pricing:read` | 定价与利润/退税率表查看 |
| `pricing:write` | 退税率表维护/定价记录创建（财务 + 销售主管） |
| `pricing:admin` | **默认利润率/客户等级调整%/佣金规则配置**（归总领导系统管理员；涉及公司利润策略） |
| `vat:read` | 增值税发票/进项/销项查看 |
| `vat:write` | VAT发票登记/勾选认证（归财务） |
| `tax:read` | 出口退税申报查看 |
| `tax:write` | 退税申报录入（归财务） |
| `tax:approve` | 退税申报提交审批（归财务；税务专业合规工作，不是业务审批） |
| `remit:read` | 付汇水单/外汇业务查看 |
| `remit:write` | 付汇水单登记（归财务） |
| `fx:lock` | 汇率锁定（远期结售汇）登记/分配（财务 + 总领导审批） |
| `risk:read` | 风险管理-预警/信用/合规/质量 总览查看 |
| `risk:write` | 预警确认/解决/信用评级录入（销售主管 + 总领导） |
| `risk:admin` | **信用额度调整/合规豁免**（高危，归总领导系统管理员） |

### §3.5 平台域（17 scope）

| scope | 说明 |
|---|---|
| `knowledge:read` | 知识库/数据中心看板查看 |
| `knowledge:write` | 知识库文档创建/草稿 |
| `knowledge:approve` | 知识库文档发布审核 |
| `knowledge:admin` | 归档/恢复/全库ACL配置 |
| `tools:execute` | 业务工具（8个小工具）使用权限 |
| `settings:account` | 个人账户设置（密码/通知偏好/头像） |
| `settings:system:read` | 系统设置查看（仅Admin可见内容） |
| `users:read` | 后台管理-用户/角色/部门查看 |
| `users:write` | 后台管理-用户创建/编辑/角色分配 |
| `users:admin` | 用户删除/权限矩阵修改/SuperAdmin角色授予（仅SuperAdmin） |
| `audit:read` | 审计日志查看 |
| `audit:export` | 审计日志导出CSV（仅FinanceMan+/Admin+） |
| `dictionary:write` | 数据字典（枚举值真源）增删改 |
| `hr:read` | HR人事-员工档案/考勤/薪酬查看 |
| `hr:write` | HR人事-员工/入职/调动编辑 |
| `hr:salary:read` | 薪酬明细查看（极敏感，仅SuperAdmin + 指定HR） |
| `data:import` | Excel批量导入（订单/产品/客户/供应商） |
| `data:export:full` | 全量数据导出/迁移（高危，仅SuperAdmin） |

### §3.6 敏感字段查看权限（5 scope，细粒度）

真源：`SensitiveFieldScope` 类型 + `sensitive:xxx` scope

| scope | 敏感字段 | 说明 |
|---|---|---|
| `sensitive:cost` | cost | 成本/采购价/BOM成本 查看（S:cost） |
| `sensitive:profit` | profit | 毛利/毛利率/利润率 查看（S:profit） |
| `sensitive:commission` | commission | 佣金率/佣金金额 查看（S:commission） |
| `sensitive:salary` | salary | 员工薪酬明细 查看（S:salary） |
| `sensitive:tax_base` | tax_base | 进项税/退税计税底值 查看（S:tax_base） |

---

## §4 View → 主权限映射表（35 View 全覆盖）

真源：`VIEW_TO_MAIN_SCOPES` 对象

> **注**：Assistant 仅一个（无重复），共 35 View。售后客诉暂挂在 Orders 下（无独立 View），用 scope 直接控；VAT/退税/付汇暂时挂在 FinanceManager/Invoices（无独立 View）。

| View | read (R) | write (W) | delete (D) | approve (A) | sensitive (S) |
|---|---|---|---|---|---|
| Dashboard | `dashboard:read` | — | — | — | — |
| Cockpit | `cockpit:read` | — | — | — | cost / profit |
| Assistant | `ai:chat` | — | — | — | — |
| Reports | `reports:read` | `reports:write` | — | `reports:admin` | — |
| Relations | `relations:read` | `relations:write` | `relations:delete` | `relations:admin` | — |
| CRM | `crm:read` | `crm:write` | — | — | — |
| Suppliers | `suppliers:read` | `suppliers:write` | — | `suppliers:admin` | — |
| Seasons | `seasons:read` | `seasons:write` | — | — | — |
| Marketing | `marketing:read` | `marketing:write` | — | — | — |
| Emails | `emails:read` | `emails:write` | — | `emails:admin` | — |
| Products | `products:read` | `products:write` | — | — | — |
| BOM | `bom:read` | `bom:write` | — | `bom:admin` | — |
| Orders | `orders:read` | `orders:write` | `orders:delete` | — | — |
| Quotations | `quotations:read` | `quotations:write` | — | `quotations:approve` | — |
| Procurement | `procurement:read` | `procurement:write` | — | `procurement:approve` | — |
| ProductionBoard | `production:read` | `production:write` | — | — | — |
| Shipments | `shipments:read` | `shipments:write` | — | `shipments:approve` | — |
| Customs | `customs:read` | `customs:write` | — | `customs:admin` | — |
| QcWorkbench | `qc:read` | `qc:write` | — | — | — |
| Invoices | `invoices:read` | `invoices:write` | — | `invoices:approve:writeoff` | — |
| PaymentVouchers | `vouchers:read` | `vouchers:write` | — | — | — |
| Pricing | `pricing:read` | `pricing:write` | — | `pricing:admin` | cost / profit / commission / tax_base |
| Risks | `risk:read` | `risk:write` | — | `risk:admin` | — |
| DataCenter | `knowledge:read` | `knowledge:write` | — | `knowledge:approve` | — |
| BusinessTools | `tools:execute` | — | — | — | — |
| Development | `products:read` | `products:write` | — | — | — |
| Inventory | `orders:read` | — | — | — | — |
| DocumentCenter | `customs:read` | `customs:write` | — | — | — |
| MES | `production:read` | — | — | — | — |
| Settings | `settings:account` | — | — | — | — |
| AccountSettings | `settings:account` | — | — | — | — |
| SystemSettings | `settings:system:read` | `dictionary:write` | — | `users:admin` | — |
| AdminPanel | `users:read` | `users:write` | `audit:read` | — | — |
| HR | `hr:read` | `hr:write` | — | — | salary |
| UiLab | `dashboard:read` | — | — | — | — |

> **注**：Assistant 仅一个（无重复），共 35 View。售后客诉暂挂在 Orders 下（无独立 View），用 scope 直接控；VAT/退税/付汇暂时挂在 Finance（无独立 View）。

### §4.6 DR-026~DR-031 新增 scope

| 决策 | 新增 scope | 说明 |
|------|-----------|------|
| DR-026 公共资料与内部资料分离 | `profile:public:read` / `profile:internal:read` | 公共资料（全员可见）/ 内部资料（按归属部门可见） |
| DR-027 供应商产品能力直写 | `supplier:capability:write` | 供应商档案直接维护产品能力（面料/成衣/辅料） |
| DR-028 其他贸易完整记录 | `lighttrade:read` / `lighttrade:write` | 其他贸易记录读写 |
| DR-029 出运/履约/收款分开 | `shipment:read` / `shipment:write` / `fulfillment:read` / `payment:read` / `payment:write` | 出运/履约/收款独立 scope |
| DR-030 经营总览页面区块 | `cockpit:overview:read` | 经营总览统一 KPI 投影查看 |
| DR-031 完整字段清单与流程图规范 | `doc:fieldlist:read` / `doc:flowchart:read` | 文档字段清单/流程图查看 |

---

## §5 行级数据范围规则（4 种范围 × 模块特殊规则）

真源：`DataScopeRule` 类型 + `DEFAULT_DATA_SCOPE_BY_ROLE`

### §5.1 4 种数据范围

| kind | 语义 | SQL 过滤 |
|---|---|---|
| `all` | 全公司可见 | 无过滤 |
| `department` | 仅本部门（含 `own` / `includeDescendantDepartments` 选项） | `WHERE departmentId = user.departmentId`（+ 子部门时 `IN (descendants)`） |
| `team` | 仅本 Team（预留更细粒度，目前按 department 够用） | `WHERE teamId = user.teamId` |
| `self` | 仅自己（`ownerId = me`，暂时不启用因为 CM-2 选了无唯一 owner） | `WHERE ownerId = user.id` |

### §5.2 7 角色 × 默认范围 + 模块特殊规则

> **DR-024 领导可视化**：销售主管 / 系统管理员（总领导）默认数据范围 `all`（可见全公司数据用于决策），但**业务操作权限（写/审批/删除）按组织归属 department 最小化**。

| 角色 | 默认 `*` 数据范围 | 操作范围（写/审批） | 模块特殊规则 |
|---|---|---|---|
| **业务员** Sales | `department`（own=true, 不含子部门） | 仅本部门 | relations/crm/invoices/vouchers 同部门可见；hr=self；其他业务同部门可写 |
| **财务** Finance | `all`（全公司财务域 + 业务域只读） | 仅财务域可写；无审批权 | HR 薪酬明细仍需 `sensitive:salary` + `hr:salary:read` 双重 scope；业务域只读不写 |
| **QC** | `all`（全公司业务数据只读） | QC 域全公司可写；无业务审批权 | `qc:write` 全公司 QC 记录可操作；BOM 版本管理（`bom:admin`）全公司可改；业务域只读 |
| **后勤** Logistics | `all`（全公司业务数据只读） | 执行类全公司可写（发货/单证/采购录入）；无审批权 | `shipments:write` / `customs:write` / `procurement:write` / `customs:admin`（单证模板）全公司操作；业务域只读 |
| **销售主管** SalesManager | `all`（DR-024 领导可视化） | **仅本部门（department）**审批和写 | 跨部门数据可见不可改；审批仅限本部门（DR-007 组织归属：本人 department.managerId）；`reports:admin` 可给全公司订阅 |
| **系统管理员（总领导）** Admin | `all`（方便配置排障 + 业务兜底） | 全公司审批兜底；配置域全写；**薪酬不可见** | 不给 `sensitive:salary`（避免看员工工资）；其他敏感字段（cost/profit/commission/tax_base）全部有（总领导决策需要） |
| **超级管理员（老板）** SuperAdmin | `all`（无条件，运行时特判 `role === SUPER_ADMIN` 直接放行） | 全部技术权限；业务审批仍走组织归属 | 所有敏感字段全部可见；DB seed 时自动授予全部 scope |

### §5.3 DR-024 领导可视化与权限最小化

| 规则 | 说明 |
|------|------|
| 领导可视化 | 销售主管 / 总领导默认可见全公司数据（数据范围 all）——用于查看跨团队 KPI、利润、库存等做决策 |
| 权限最小化 | 业务操作权限（写/审批/删除）按组织归属最小化（仅本 department / 本团队）——避免跨团队越权改数据 |
| 审批权分离 | 总领导的业务审批权通过业务审批 scope（`order:approve:*` / `quotations:approve` / `vouchers:approve:pay_*`）+ DR-007 组织归属授权，不依赖 SuperAdmin 技术兜底权限特判 |

### §5.4 DR-025 组织/团队/归属影响范围

| 规则 | 说明 |
|------|------|
| 数据范围解析 | 按组织归属（departmentId → Department.head → Department.managerId）解析，不按固定角色硬编码 |
| 审批路由 | 报价审批/订单变更审批/付款审批全部按 DR-007 组织归属路由到对应部门主管，**不按固定金额阈值**；无对应主管或跨团队时上抬到总领导 |
| 动态调整 | 部门结构调整（managerId 变更、部门拆分合并）时，数据范围/审批路由自动跟随调整，无需手动修改角色配置 |

---

## §6 7 角色 × 60+ scope 默认矩阵（展开 true 项）

真源：`DEFAULT_ROLE_PERMISSION_MATRIX` = `{ SALES: SALES_BASE, FINANCE: FINANCE_BASE, QC: QC_BASE, LOGISTICS: LOGISTICS_BASE, SALES_MANAGER: SALES_MANAGER_BASE, ADMIN: ADMIN_BASE, SUPER_ADMIN: {} }`

> **SuperAdmin 特判**：矩阵为空对象，运行时 `role === SYSTEM_ROLE_IDS.SUPER_ADMIN` 直接 return true，不查表；DB seed 时 `getDefaultScopeListForRole` 返回 `Object.keys(PERMISSION_SCOPES)` 全量。**DR-023**：业务审批链仍走组织归属，不绕过审批链。

### §6.1 SALES_BASE（业务员，~38 项 true）

| 域 | scope 清单 |
|---|---|
| 经营总览 | `dashboard:read` `cockpit:read` `reports:read` `reports:write` `ai:chat` `ai:write:low` `ai:write:medium` |
| 客户市场 | `relations:read` `relations:write` `crm:read` `crm:write` `suppliers:read` `seasons:read` `seasons:write` `marketing:read` `emails:read` `emails:write` |
| 订单履约 | `orders:read` `orders:write` `quotations:read` `quotations:write` `quotations:convert` `products:read` `products:write` `bom:read` `bom:write`（Draft 态编辑） `production:read` `shipments:read` `shipments:write`（本部门发货登记） `customs:read` `customs:write`（本部门单证） `qc:read`（查看 QC） `aftersales:read` `aftersales:write`（客诉登记） `procurement:read` `procurement:write`（发起 PO 草稿） |
| 财务（只读） | `finance:read` `invoices:read` `vouchers:read` `pricing:read`（报价利润可见但敏感字段遮罩） `risk:read` |
| 平台 | `knowledge:read` `knowledge:write` `tools:execute` `settings:account` `data:import` |

**业务员敏感字段**：全部 ❌（cockpit/pricing 中的成本/利润/佣金字段自动遮罩 `****`）

---

### §6.2 SALES_MANAGER_BASE（销售主管，继承业务员 + ~15 项额外）

继承 `SALES_BASE` 全部，**额外追加**：

| 类别 | scope 清单 | 说明 |
|---|---|---|
| **敏感字段（决策必需）** | `sensitive:cost` `sensitive:profit` | 做报价偏差审批、定价决策必须看成本和利润 |
| 审批（本团队 DR-007） | `orders:approve:change_delivery` `orders:approve:change_qty` `orders:approve:change_price`（双签之一） `quotations:approve` `shipments:approve` `aftersales:approve` `procurement:approve` | 本团队业务审批 |
| 付款审批（小单） | `vouchers:approve:pay_lt5` | DR-007 组织归属：本团队小单付款申请；大单自动上抬到总领导 |
| 评级配置 | `relations:admin` `suppliers:admin` | 客户等级/信用、供应商评级调整 |
| 风控 & 报表 | `risk:write` `reports:admin` | 预警确认；报表订阅推送管理 |
| BOM 管理 | `bom:admin` | BOM 版本/计算模型（和 QC 共享管理权） |

---

### §6.3 FINANCE_BASE（财务，~45 项 true；审批权与执行权严格分离）

> **核心原则**：财务是「执行者」不是「审批者」。付款执行、发票签发、核销、退税申报归财务；**付款审批、坏账核销、价格变更、订单取消不归财务**。

| 域 | scope 清单 |
|---|---|
| 经营总览 | `dashboard:read` `cockpit:read` `reports:read` `reports:write` `reports:admin` `ai:chat` `ai:write:low` `ai:write:medium` |
| **敏感字段** | `sensitive:cost` `sensitive:profit` `sensitive:commission` `sensitive:tax_base`（佣金可见，财务做账发工资需要） |
| 客户市场（只读） | `relations:read` `crm:read` `suppliers:read` `seasons:read` `marketing:read` `emails:read` |
| 订单履约（只读） | `orders:read` `quotations:read` `products:read` `bom:read` `production:read` `shipments:read` `customs:read` `qc:read` `aftersales:read` `procurement:read` |
| **财务域（可写 + 专业审批）** | `finance:read` `invoices:read` `invoices:write`（发票签发，PI 无章业务员可做，正式发票财务） `invoices:reconcile`（核销） `vouchers:read` `vouchers:write`（收付款凭证录入） `pricing:read` `pricing:write`（退税率表维护） `vat:read` `vat:write`（VAT 登记认证） `tax:read` `tax:write` `tax:approve`（退税申报审批=财务专业工作） `remit:read` `remit:write`（水单） `fx:lock`（汇率锁定） `risk:read` `risk:write`（预警录入） |
| 平台 | `knowledge:read` `knowledge:write` `tools:execute` `settings:account` `audit:read`（审计查看不可导出） `hr:read` |

**财务禁止拥有的 scope**：全部 `vouchers:approve:pay_*`（付款审批权）、`invoices:approve:writeoff`（坏账核销）、`orders:approve:cancel`（订单取消）、`risk:admin`（信用额度）、`pricing:admin`（利润率/佣金规则）、`audit:export`（审计导出）

---

### §6.4 QC_BASE（QC 角色，新增 ~25 项 true）

| 域 | scope 清单 |
|---|---|
| 经营总览（只读） | `dashboard:read` `cockpit:read` `reports:read` `ai:chat` `ai:write:low` |
| 客户市场（只读） | `relations:read` `suppliers:read`（看供应商产能） `seasons:read` `products:read`（看产品规格） |
| **核心：质量工艺域** | `qc:read` `qc:write`（疵点/质检报告全公司录入） `production:read` `production:write`（阶段登记/疵点录入） `bom:read` `bom:write`（Draft 编辑） `bom:admin`（BOM 版本状态/工艺模型管理；和 SM 共享） |
| 订单履约（只读） | `orders:read` `quotations:read` `shipments:read` `customs:read` `aftersales:read`（客诉中 QC 相关部分） `procurement:read`（看采购物料规格） |
| 财务（只读） | `finance:read`（仅 KPI 层，敏感字段遮罩） |
| 平台 | `knowledge:read` `knowledge:write`（质检知识库） `tools:execute` `settings:account` |

**QC 敏感字段**：全部 ❌

---

### §6.5 LOGISTICS_BASE（后勤角色，新增 ~28 项 true；物流/单证/仓储/采购执行）

| 域 | scope 清单 |
|---|---|
| 经营总览（只读） | `dashboard:read` `cockpit:read` `reports:read` `ai:chat` `ai:write:low` |
| 客户市场（只读） | `relations:read`（查地址联系人） `crm:read` `suppliers:read`（查供应商发货） `seasons:read` `marketing:read` `emails:read`（发货通知邮件） |
| **核心：执行操作域** | `shipments:read` `shipments:write`（全公司装箱/发货登记） `customs:read` `customs:write`（单证制作） `customs:admin`（单证模板/公司签章配置） `procurement:read` `procurement:write`（PO 录入执行） `aftersales:read` `aftersales:write`（客诉物流部分录入） `inventory:read`（库存查看） |
| 订单履约（只读） | `orders:read` `quotations:read` `products:read` `bom:read`（看装箱规格） `production:read`（看生产进度排发货） `qc:read`（发货前看质检放行） |
| 财务（只读） | `finance:read`（KPI） `invoices:read`（商业发票做报关用） `vouchers:read`（水单核对） |
| 平台 | `knowledge:read` `knowledge:write`（物流知识库） `tools:execute` `settings:account` `data:import`（批量导入运单/装箱） |

**后勤敏感字段**：全部 ❌

---

### §6.6 ADMIN_BASE（系统管理员 = 总领导，~55 项 true；业务兜底审批 + 系统配置）

> **DR-023 + DR-007 双核心**：总领导 = 业务兜底审批人（跨团队/大单/高风险审批）+ 系统配置管理员。技术权限与业务审批权在此角色合一（因为总领导本人就是这两个职能）。**`sensitive:salary` 仍然不给**，薪酬仅老板 + 指定 HR 可见。

| 域 | scope 清单 |
|---|---|
| **敏感字段** | `sensitive:cost` ✅ `sensitive:profit` ✅ `sensitive:commission` ✅ `sensitive:tax_base` ✅；`sensitive:salary` ❌（员工薪酬绝密） |
| **审批兜底核心** | `orders:approve:change_delivery` `orders:approve:change_qty` `orders:approve:change_price`（双签之二） `orders:approve:cancel`（取消审批兜底） `quotations:approve` `shipments:approve` `aftersales:approve` `procurement:approve`（跨团队采购审批） `vouchers:approve:pay_lt5` `vouchers:approve:pay_gt5` `vouchers:approve:pay_new_supplier`（付款审批兜底全档） `invoices:approve:writeoff`（坏账核销） `tax:approve`（退税审批可覆盖） `pricing:admin`（利润率/佣金规则） `risk:admin`（信用额度/合规豁免） |
| 评级/报表/BOM | `relations:admin` `suppliers:admin` `reports:admin` `bom:admin` |
| **配置后台** | `settings:system:read` `users:read` `users:write` `users:admin`（角色权限管理） `dictionary:write`（字典） `knowledge:approve` `knowledge:admin`（知识库） `tools:execute` `settings:account` `audit:read` `audit:export`（审计日志导出 CSV） |
| 知识库/AI | `knowledge:read` `knowledge:write` `ai:chat` `ai:write:low` `ai:write:medium` `ai:write:high` |
| 报表 | `reports:read` `reports:write` |
| **HR** | `hr:read` `hr:write`（人事管理归总领导） |
| **业务域全只读**（排障审计） | `dashboard:read` `cockpit:read` `relations:read` `crm:read` `suppliers:read` `seasons:read` `emails:read` `orders:read` `quotations:read` `products:read` `bom:read` `production:read` `shipments:read` `customs:read` `qc:read` `aftersales:read` `procurement:read` `finance:read` `invoices:read` `vouchers:read` `pricing:read` `vat:read` `tax:read` `remit:read` `risk:read` `marketing:read` |
| 数据导入导出 | `data:import` |

---

### §6.7 SUPER_ADMIN_BASE（超级管理员 = 老板）

矩阵为空对象 `{}`，运行时特判：
```typescript
if (r === SYSTEM_ROLE_IDS.SUPER_ADMIN) return true; // 任意 scope 直接放行
```
DB seed 时 `getDefaultScopeListForRole` 返回 `Object.keys(PERMISSION_SCOPES)` 全量 60+ scope。

**DR-023 严格执行**：业务审批链仍按 DR-007 组织归属解析 reviewerId（SM/总领导），**不因为老板是 SuperAdmin 就自动把所有审批路由给老板**（避免老板审批爆炸）。仅当 DB 中某张 ApprovalRequest 的 reviewerId 明确是老板的 UserAccount ID 时，老板才会收到该审批。

---

## §7 敏感字段可见性矩阵（5 字段 × 7 角色）

> **设计原则**：
> - 业务员：全 ❌（一线不用看成本利润佣金，只看销售价；做报价时由系统校验，不显示成本）
> - 销售主管：成本/利润 ✅（审批报价必须知道成本利润，否则没法审批偏差）；佣金/薪酬 ❌（佣金涉及业务员个人收入，销售主管不该看单个人提成明细）
> - QC/后勤：全 ❌（不接触财务机密）
> - 财务：成本/利润/佣金/税基 ✅（做账、发工资、退税计算必须）；薪酬 ❌（薪酬另外走 HR 专门 scope）
> - 系统管理员（总领导）：成本/利润/佣金/税基 ✅（决策必须）；薪酬 ❌（员工薪酬绝密）
> - 超级管理员（老板）：全 ✅；`hr:salary:read` 指定的 HR 也可见薪酬

| 敏感字段（scope） | 业务员 | 销售主管 | 财务 | QC | 后勤 | **系统管理员（总领导）** | **超级管理员（老板）** |
|---|---|---|---|---|---|---|---|
| `cost`（S:cost）成本/采购价/BOM 成本 | ❌ | ✅ | ✅ | ❌ | ❌ | ✅ | ✅ |
| `profit`（S:profit）毛利/毛利率/利润率 | ❌ | ✅ | ✅ | ❌ | ❌ | ✅ | ✅ |
| `commission`（S:commission）佣金率/佣金金额 | ❌ | ❌ | ✅ | ❌ | ❌ | ✅ | ✅ |
| `salary`（S:salary）员工薪酬明细 | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅（+ 指定 HR via `hr:salary:read`） |
| `tax_base`（S:tax_base）进项税/退税计税底值 | ❌ | ❌ | ✅ | ❌ | ❌ | ✅ | ✅ |

> **遮罩规则**：无对应 `sensitive:xxx` scope 时，前端字段渲染为 `****`，后端 API 返回时 `permissionService` 自动遮罩。前后端双层保障。

---

## §8 API 端点

| 端点 | 方法 | 用途 | 权限 scope |
|---|---|---|---|
| `/api/v1/auth/permissions` | GET | 查询当前用户运行时 scope 集合（`runtimeUserScopes`） | 登录即可 |
| `/api/v1/admin/roles` | GET | 查询角色列表（7 系统内置 + 自定义） | `users:read` |
| `/api/v1/admin/roles` | POST | 创建自定义角色（7 系统内置 isSystem=true 不可创建删除） | `users:admin`（仅 SuperAdmin + 系统管理员总领导） |
| `/api/v1/admin/roles/{id}` | PUT | 更新角色权限矩阵 | `users:admin` |
| `/api/v1/admin/roles/{id}` | DELETE | 删除自定义角色（系统角色 isSystem=true 不可删） | `users:admin` |
| `/api/v1/admin/permissions` | GET | 查询全量 scope 清单（60+） | `users:read` |
| `/api/v1/admin/users/{id}/roles` | PUT | 分配用户角色（7 内置或自定义） | `users:write`（SuperAdmin 角色授予需 `users:admin`） |
| `/api/v1/permissions/resolve-reviewer` | POST | **DR-007 新增**：按 requesterId + departmentId + actionType 解析 reviewerId（服务端 `resolveReviewerByDepartment.ts` 暴露给前端调用，前端创建审批前先查 reviewerId 以预览审批人） | 登录即可（结果按 requester 计算，不泄露敏感） |

---

## §9 权限校验流程（运行时）

```
前端/守卫调用 hasPermission(roleId, scope, runtimeUserScopes?)
        │
        ▼
┌─────────────────────────────────────┐
│ 1. runtimeUserScopes 优先            │
│    if (set.has(scope)) return true  │
└─────────────────────────────────────┘
        │ (set 为空或无该 scope)
        ▼
┌─────────────────────────────────────┐
│ 2. 角色数组遍历（支持多角色用户）     │
│    for (r of roles):                │
│      if (r === SUPER_ADMIN) → true  │
│      matrix = DEFAULT_ROLE_PERMISSION_MATRIX[r] │
│      if (matrix[scope] === true) → true │
└─────────────────────────────────────┘
        │ (全部角色都无该 scope)
        ▼
    return false
```

**关键函数**（`lib/rolePermissionMatrix.ts`）：

| 函数 | 签名 | 用途 |
|---|---|---|
| `hasPermission(roleId, scope, runtimeUserScopes?)` | `(roleId: string\|string[]\|null, scope: PermissionScope, set?: Set<string>) => boolean` | 普通 scope 运行时判断 |
| `canViewSensitive(roleId, field, runtimeUserScopes?)` | `(roleId, field: SensitiveFieldScope, set?) => boolean` | 敏感字段判断（内部转 `sensitive:${field}` scope 调 hasPermission） |
| `getDataScopeRule(roleId, moduleOrView?)` | `(roleId, moduleOrView?: string) => DataScopeRule` | 行级范围 fallback（先查模块特殊规则，无则用 `*` 通配） |
| `getDefaultScopeListForRole(roleId)` | `(roleId: SystemRoleId) => string[]` | seed 用，返回角色全部默认 scope 数组 |

**运行时数据流**：
1. 用户登录 → `/api/v1/auth/permissions` 返回 DB 中 `runtimeUserScopes`（DB RolePermission 表 join 结果）
2. 前端/守卫调用 `hasPermission` 时优先查 `runtimeUserScopes`，为空则 fallback 到 `DEFAULT_ROLE_PERMISSION_MATRIX`
3. `permissionService` 在 API 返回前根据 `canViewSensitive` 遮罩敏感字段
4. `permissionGuard` 中间件在路由前根据 `hasPermission` 拦截无权限请求

---

## §10 移动端

**已搁置，不推进**。本项目唯一产品形态为桌面端 Electron 客户端（见 [2. 系统架构.md](./2.%20系统架构.md) §1）。权限矩阵为后端真源，如未来启用移动端 PWA，沿用同一套 scope 与 `hasPermission` 逻辑，无独立设计。

---

## §11 业务规则关联

> **核心纪律**：审批链统一走 DR-007 组织归属解析（departmentId → SM → 总领导兜底），**不再按固定金额阈值分档**。`vouchers:approve:pay_lt5` / `pay_gt5` 仅为兼容保留的 scope 名，实际权限按角色分配，运行时不判断金额阈值。

| 规则 | 关联 scope | 审批路由 | 说明 |
|---|---|---|---|
| **价格审批 ⑤ 双轨偏差** | `quotations:approve` | DR-007：SM（本团队）→ 总领导（兜底/跨团队） | 偏差 > 15% warn 级 / > 30% block 级，自动生成 ApprovalRequest 并按 DR-007 填 reviewerId |
| **付款申请审批（DR-017）** | `vouchers:approve:pay_lt5` `vouchers:approve:pay_gt5` | DR-007：SM（本团队小单）→ 总领导（兜底/大单/跨团队） | **不再按金额阈值分档**；首单供应商 +1 级（SM 自动上抬到总领导）用 `vouchers:approve:pay_new_supplier` scope |
| **付款执行（非审批）** | 无审批 scope，需 `vouchers:write` + `executorStatus` 写权限 | 财务角色专用操作 | 审批 approved 后出现在财务待执行列表 → 财务去银行实际付款 → 财务点「已付款」写入 `executorStatus=executed` |
| **订单变更 交期** | `orders:approve:change_delivery` | DR-007：SM（本部门）→ 总领导（兜底） | 不再按 ≤7 天阈值分档 |
| **订单变更 数量** | `orders:approve:change_qty` | DR-007：SM → 总领导 | 不再按 ±10% 阈值分档 |
| **订单变更 价格（双签）** | `orders:approve:change_price` | **双 reviewerId**：SM + 总领导（2 人都批才 approved） | 价格变更风险最高，强制双签 |
| **订单取消（DR-010）** | `orders:approve:cancel` | DR-007 直接上抬至总领导（不归 SM） | approved 后进入 DR-010 取消流程：取消申请中 → 影响汇总 → 结案处理（流程本身代替多人会签） |
| **订单暂停（DR-010）** | 复用 `orders:approve:cancel` scope 或新增 actionType=`order.pause` | 同取消，上抬至总领导 | approved 后暂停生效 → 到期提醒 |
| **首单供应商采购 +1 级** | `procurement:approve` + `vouchers:approve:pay_new_supplier` | SM → 自动上抬到总领导 | 首单风险高，多一层把关 |
| **坏账核销 / 发票作废** | `invoices:approve:writeoff` | **归总领导（不归财务）** | 直接损失公司利润，风险高；财务仅执行核销动作，无审批权 |
| **退税申报审批** | `tax:approve` | **归财务** | 税务合规专业工作，财务直接审批 |
| **客诉扣赔 / 折让** | `aftersales:approve` | DR-007：SM（本团队）→ 总领导（大金额兜底） | 影响应收金额 |
| **坏账核销 + 首单付款双逻辑叠加** | `invoices:approve:writeoff` + `vouchers:approve:pay_new_supplier` | 审批链上抬至总领导 | 风险叠加 → 更高一层 |

---

## §12 可访问性

N/A — 权限矩阵为后端逻辑，无 UI 可访问性要求。AdminPanel UI 的可访问性见对应模块设计文档。

---

## §13 设计系统约束

N/A — 权限矩阵为后端逻辑，无直接设计系统约束。AdminPanel UI 的设计约束见 `4. 设计系统规范.md`。

---

## §14 待补缺口

| 编号 | 缺口 | 影响 | 优先级 |
|---|---|---|---|
| GAP-R1 | AdminPanel UI 尚未完整接入 RBAC 管理界面（角色 CRUD / 权限矩阵可视化编辑 / 用户角色分配 / QC+后勤角色启用开关） | 管理员无法通过 UI 调整权限，只能改 DB | P1 |
| GAP-R2 | `server/src/_shared/rolePermissionMatrix.ts` 副本需手工同步 `lib/rolePermissionMatrix.ts` 真源，无自动校验 | seed 与守卫可能矩阵不一致（高风险） | P0 |
| GAP-R3 | **DR-007 组织归属解析服务端实现**（`server/src/permissions/resolveReviewerByDepartment.ts` + `/api/v1/permissions/resolve-reviewer` 端点） | 当前 reviewerId 手工分配或留空，按阈值分档逻辑仍散落在 Service | **P0** |
| GAP-R4 | SuperAdmin 角色授予仅靠 `users:admin` scope，无二次确认/审批流 | 单点权限过高风险 | P2 |
| GAP-R5 | **已收口**：Operations/Warehouse/Executive → 合并纳入 7 角色宽泛容器（QC + 后勤），7 角色为唯一 fallback 真源 | — | ✅ 已解决 |
| GAP-R6 | 敏感字段遮罩仅在后端 `permissionService` 实现，前端无统一 `<SensitiveField>` 组件封装 | 前端可能遗漏遮罩逻辑 | P2 |
| GAP-R7 | DR-023 SuperAdmin 业务审批权分离：SuperAdmin 直接放行所有 scope，审批链入口可绕过 DR-007 | 业务审批可越权（技术兜底绕过业务审批链） | P1 |
| GAP-R8 | DR-024 领导可视化实现（SalesManager/Admin 数据范围默认 `all`，写/审批限 department） | 当前 SalesManager 数据范围可能不是 all | P1 |
| GAP-R9 | DR-025 组织归属动态解析数据范围实现 | 部门结构调整时需手动改角色配置 | P2 |
| GAP-R10 | DR-026~DR-031 新增 scope（profile:public:read / supplier:capability:write / lighttrade:read / shipment:read / fulfillment:read / payment:read / cockpit:overview:read / doc:fieldlist:read）未在代码中定义 | 新增业务域无权限控制 | P1 |
| **GAP-R11** | **7 角色 Prisma seed + SYSTEM_ROLE_IDS 常量升级**（删除 FINANCE_MANAGER，新增 QC + LOGISTICS；DB seed migration） | 当前代码仍为 6 角色，FinanceManager 删除后 seed 脚本崩 | **P0** |
| **GAP-R12** | **ApprovalRequest 双签支持**（`orders:approve:change_price` 需要 2 个 reviewerId，2 人均批才 approved；新增 `signOffs[]` 字段或类似机制） | 当前仅支持单 reviewerId | **P1** |
| **GAP-R13** | **财务 Cockpit 待执行付款列表**（Approved 后 executorStatus=pending_execution 的 payment.request 专属视图）+ 财务「已付款」操作按钮权限守卫 | 财务看不到 approved 的付款需求，无法执行 | **P1** |
| GAP-R14 | 订单变更交期/数量 scope 阈值分档代码清理（≤7 天/±10% 数值判断移除，统一走 DR-007） | 遗留硬编码阈值可能误导 reviewer 分配 | P2 |

---

## §15 相关文档索引

- [00-索引.md](../00-索引.md) — 设计文档真源总索引
- [评审决策记录](../10-评审与决策/2026-08-16-设计评审决策记录.md) — DR-007 审批路由组织归属 / DR-010 订单取消暂停 / DR-017 付款审批与执行分离 / DR-023~DR-031：SuperAdmin 审批权分离/领导可视化/组织归属/资料分离/供应商能力直写/其他贸易/出运履约收款分开/经营总览/文档规范
- [5. 全局交互规范.md](./5.%20全局交互规范.md) — **7 角色审批权限可见范围** / 审批通知抽屉 / ApprovalRequest 双签 / executorStatus 财务执行字段
- [2. 系统架构.md](./2.%20系统架构.md) — permissionService / permissionGuard 在后端架构的位置
- [3. 信息架构与导航.md](./3.%20信息架构与导航.md) — **35 View** 枚举（View→scope 映射的 View 侧，36→35 已去重 Assistant）
- [4. 设计系统规范.md](./4.%20设计系统规范.md) — **BDS v2.2** 治理结构 / AdminPanel UI 的 BDS flat 设计约束
- [03-业务规则/价格审批规则.md](../03-业务规则/价格审批规则.md) — 双轨偏差 warn/block 触发审批 + DR-007 组织归属 reviewerId
- [03-业务规则/订单变更规则.md](../03-业务规则/订单变更规则.md) — DR-010 取消/暂停状态流程 + 价格变更双签
- [03-业务规则/财务域模型组.md](../03-业务规则/财务域模型组.md) — DR-017 付款申请审批 + 财务执行分离（审批归业务领导，执行归财务）
- [03-业务规则/业务规则总览.md](../03-业务规则/业务规则总览.md) — §9 22 条规则中涉及权限的条款
- [02-数据模型/实体关系总览.md](../02-数据模型/实体关系总览.md) — Role/Permission/RolePermission 表模型 + ApprovalRequest reviewerId/executorStatus 字段

---

## §16 补充说明

1. **副本同步风险（GAP-R2）**：`server/src/_shared/rolePermissionMatrix.ts` 是为绕过 `server/tsconfig.json` 的 `rootDir=./src` 限制而做的快照副本，文件头明确警告「当修改 `/lib/rolePermissionMatrix.ts` 时必须同步更新此文件内容，否则 seed 脚本与服务器守卫矩阵不一致」。建议未来通过 monorepo workspace 或 path mapping 消除副本。
2. **PL-2B 同部门互相可见**：业务员/销售主管的默认数据范围是 `department`（非 `self`），即同部门同事互相可见客户/订单数据，跨部门隔离。这是贸易公司典型场景（一个销售部共享客户池）。**DR-024**：销售主管数据范围升级为 `all`（可见全公司做决策），但操作仍限本部门。
3. **系统管理员（总领导）敏感字段边界**：总领导 rank=4，**故意不给 `sensitive:salary`（员工薪酬）**，但其他成本/利润/佣金/税基全部有权。避免总领导权限过高看到员工个人工资条（薪酬绝密仅老板 + 指定 HR via `hr:salary:read` 可见）。
4. **SuperAdmin 不可删除**：`SYSTEM_ROLE_META` 中 SuperAdmin rank=10，DB seed 时标记 `isSystem=true`，AdminPanel 不允许删除此角色账户。同样，7 系统内置角色（Sales/Finance/QC/Logistics/SalesManager/Admin/SuperAdmin）全部 isSystem=true，不可删除。
5. **多角色用户支持**：`hasPermission` 接受 `roleId: string | string[]`，支持一个用户绑定多个角色（如某人身兼销售主管 + 临时 QC 主管），权限取并集（任一角色有 scope 即放行）。
6. **7 角色宽泛容器设计原则**：不按实际岗位（服装业务员/面料业务员/业务助理/采购员/单证员/仓库管理员/面料QC/成衣QC）拆角色，而是按职能族（一线业务/财务/质检/物流执行/团队领导/总领导/老板）归纳。实际数据范围和审批路由靠 departmentId（服装一组/面料二组/品牌组…）切分，避免角色爆炸 + 每新增岗位都要重新配置 scope。
7. **审批与执行严格分离（DR-017 付款模型核心）**：付款审批（该不该付）= 销售主管/总领导 scope；付款执行（钱有没有实际从银行出去）= 财务 `vouchers:write` + executorStatus 写权限。两个步骤 scoped 不同、角色不同、时间不同，严禁混同为「财务审批付款」。
8. **双签仅用于价格变更（高风险）**：订单价格变更直接影响利润且不可逆，是唯一强制 2 个 reviewerId（SM + 总领导）的业务场景。其他审批（取消/暂停/交期/数量/付款）都是单人审批但通过 DR-010 状态流程或 DR-007 上抬兜底保证风险可控，不做多人会签（避免审批瓶颈）。
