# Bambook v0.8 交付验收清单

> 版本：v0.8 ｜ 汇总日期：2026-08-25 ｜ 基线：`165ff39`（工作区含在途功能）
> 权威源：`docs/MILESTONE_v0.8.md`（验收范围）、`docs/design/09-路线图与技术债务/2026-08-21-v0.8交付验收剧本.md`（三条铁律）
> 配套证据：`.trae/documents/bambook-comprehensive-project-scan.md`（v31.9，201 章）、`docs/CODE_WIKI.md`、`docs/BUSINESS_CAPABILITY_MATRIX.md`（v2.0）

---

## 1. 交付范围口径（对照剧本 §3）

| 类别 | 板块 |
|---|---|
| **19 板块交付** | 关系智库 / CRM / 供应商 / 数字档案 / 开发管理 / 报价管理 / 订单管理 / 生产跟单 / 采购 / 库存 / QC 工作台 / 货运 / 外贸报关 / 单据中心 / 财务 / 定价利润 / BOM / 人事 / 后台管理 |
| **2 板块占位** | Cockpit（经营驾驶舱）、报表中心 —— 剧本口径：占位暂停，不进入交付范围 |
| **7 板块不在验收** | 营销 / 季节 / 风险 / 智能邮箱 / 数据中心 / 业务工具 / MES（代码均已实现，非占位） |

---

## 2. 三条铁律走查结论（local + local，代码实证）

| 铁律 | 结论 | 证据 |
|---|---|---|
| **S1 主链闭环** | **通过**：面料主链 **11/11**、成衣主链 **12/12**（全环节 API 实测） | 审计报告 196-199 章 |
| **S2 数据可追溯** | **通过**：审计实体查询（/audit/entity）、序列占用幂等（A12 修复）、EntityLink 图谱 | 196/197/199 章 |
| **S3 角色权限** | **通过**：写侧 fail-closed（越权改单 403/404）、confidential 列表+详情双向收窄、未登录 fail-closed | 200 章 |

走查覆盖（示例链路）：客户建档→开发/convert→订单→状态流转→订单行→采购→BOM→库存入库→QC（测试委托+终期放行）→货运（Booked→Shipped 门禁）→财务发票→审计追溯→跨实体搜索。

---

## 3. 走查发现并已处理的问题

| # | 问题 | 处置 | 状态 |
|---|---|---|---|
| 1 | **A12 发票创建 500**：`nextBusinessNumber('INV')` 未传 `occupied`，票号与历史/种子冲突 | 修复 `invoiceMutationService.ts` + 同型 `orders/route.ts`（QA-SEC-5 原子追平） | ✅ 已修复+复验（发票连续两张不冲突；342 个相关测试通过） |
| 2 | B8 QC 测试委托契约（顶层 testItems+agency） | 走查契约，无代码缺陷 | ✅ 复验通过 |
| 3 | B9 货运字段（type/status/shippingMethod）+ QC complete `report` 嵌套契约 | 走查契约 | ✅ 复验通过（含出运放行门禁闭环） |
| 4 | 单据号占用校验同型缺口 | 已修复 INV/ORD；CD/LC/TR 仍为"事后 throw"（并发竞态） | 📌 follow-up（见 §5） |
| 5 | 本地 8081 运行旧代码进程误导走查 | 重启至当前代码（环境类；建议 dev 后端用 watch） | ✅ 已处理 |

---

## 4. 登记项 / follow-up 处置状态（2026-08-25 逐项修复）

| # | 项 | 结论 | 状态 |
|---|---|---|---|
| F1 | B6 工序链 REST 端点 | **澄清关闭**：端点齐全（`mesRoute.ts:692-731`，6 端点，挂 `/api/v1/mes`，curl 200 复验）；走查 404 系脚本 URL 误用 | ✅ 关闭 |
| F2 | V2 relations 列表 search 契约 | **澄清关闭**：V1 全量 + 前端 client 过滤（RelationsManager 本地过滤）；V2 支持服务端 `search`（routeV2:66） | ✅ 关闭 |
| F3 | CD/LC/TR 单号事后防重 → occupied 原子追平 | **已修复**：三处 service 统一为 nextBusinessNumber(tx,prefix,{occupied}) 占用原子 + 显式号唯一校验；176 定向 + 4467 全量回归 + curl 重复单号拦截 均过 | ✅ 已修复 |
| F4 | Dashboard「全景看板」占位 + Velocity 空态 | **保留观察**：41 订单含 22 面料单；Dashboard velocity 空态待因子分解（建议服务端聚合替代前端 prop 计算） | 🔍 观察 |
| F5 | BOM lines 字段契约（unitCost） | **确认无缺陷**：BomManager 全程 unitCost/materialType/currency 正确；走查脚本 payload 误用 unitPrice 系脚本问题 | ✅ 关闭 |
| F6 | 原生 confirm/alert 202 处治理 | **已完成**：全库 `window.alert/confirm` = **0 处**（历史基线 202 处已全部替换清零） | ✅ 关闭 |

> 本轮合计：3 项澄清关闭、2 项已修复/确认、1 项保留观察；全量门禁复跑 4467/4467 绿。

---

## 5. 测试与门禁状态（2026-08-25）

| 门禁 | 状态 |
|---|---|
| 前端 `npm test` | ✅ **2644 通过 + 6 quarantine，117 文件** |
| 后端 `cd server && npx vitest run` | ✅ **4467/4467，316 文件**（终复核实测） |
| 前端/后端/OPS `tsc --noEmit` | ✅ 全零错误（三工程终复核） |
| `electron-vite build` | ✅ 通过 |
| `check:tokens` | ✅ 通过（L6 基线 71，含工作区在途功能准入） |
| 版本口径 | ✅ v0.8（V3.0 已全面更正；npm 0.8.0） |

> **终复核（2026-08-25 全量执行）**：上表 7 项门禁全部实测绿——该状态为交付基线。

---

## 6. 生产 UAT（待凭证与授权，剧本 §2）

1. 生产登录（正式账号）→ 系统可用
2. 只读走查：订单台账 / 关系智库 / 发票 / 报表
3. 浅写验收（明示授权 + UAT- 打标）
4. Dashboard 口径复核

---

## 7. 交付资料产物

| 产物 | 说明 |
|---|---|
| `docs/MILESTONE_v0.8.md` | v0.8 验收范围权威源（既有） |
| `docs/design/09-…/2026-08-21-v0.8交付验收剧本.md` | 三条铁律剧本（既有） |
| `.trae/documents/bambook-comprehensive-project-scan.md` | 审计报告 v31.9（201 章，含走查/fix 全记录） |
| `docs/CODE_WIKI.md` | 代码级知识库（实读核实） |
| `docs/BUSINESS_CAPABILITY_MATRIX.md` | 能力矩阵 v2.0（v0.8 口径） |
| 本次交付清单 | 本文档 |

---

## 8. 结论与签署

- **结论**：v0.8 三条铁律在代码实证层面全部满足；19 交付板块 API/UI 全量可用；唯一阻塞缺陷（A12）已修复并复验；5 项 follow-up 不阻塞交付。
- **待办分流**：① 生产 UAT（需凭证）；② F1-F6 随迭代；③ 全量门禁复核确认后按 Git 流程提交（工作区在途功能与本次修复需 hunk 级拆分，遵守多会话协同纪律）。

| 签署 | 角色 | 日期 | 结论 |
|---|---|---|---|
| （负责人） | 甲方验收 | 2026-08-25 | □ 通过 □ 附条件通过 □ 退回 |
| （负责人） | 项目方 | 2026-08-25 | — |