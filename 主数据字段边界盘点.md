# 主数据字段边界盘点

> 目标：判断现有字段应属于产品/面料主档、组织/人员主档，还是订单/样品业务快照。

## 1. ProductAsset / ProductSubCategory

现有 `ProductAsset` 适合作为产品通用主档的轻量基础，但不足以承载面料完整档案。

应保留为产品通用主档：

- `id`：产品主档 ID。
- `sku`：系统内部唯一代码，后续可用于二维码。
- `name`：档案显示名。
- `mainCategory`：固定大类，如 Fabric、Garment、Accessories。
- `techPackUrl` / `imageUrl`：通用附件或图片。
- `status`：通用状态。
- `updatedAt` / `deletedAt`：同步与软删除字段。

需要改造：

- `subCategoryId`：当前代表单一子分类，后续应保留兼容，但新增多维分类表承载产品类别、供应商、客户、环保等级、价格等级等视图。
- `cost`：不应继续作为唯一成本真源。面料价格会随时间、客户和场景变化，应迁移到价格历史或当前价格视图。
- `season`：对某些产品有意义，但不是面料主档的核心唯一字段。保留为通用可选信息即可。

`ProductSubCategory` 暂时保留兼容旧 UI，但后续应被多维 `ProductClassification` 取代。

## 2. Relation

现有 `Relation` 同时承载组织和人员，已经具备关系智库雏形，但一对多信息还挤在单表字段里。

应保留为组织主档的字段：

- `id`
- `name`
- `category`
- `type`
- `isOrganization`
- `tags`
- `rating`
- `lastInteraction`
- `preferences`
- `website`
- `paymentTerms`
- `paymentPreference`
- `currency`
- `taxId`
- `creditLimit`
- `officialAddress`
- `factoryAddresses`
- `warehouseAddress`
- `billingAddress`
- `shippingAddress`
- `coordinatesLat` / `coordinatesLng`

应保留为人员主档的字段：

- `parentId`：所属组织。
- `reportsToId`：组织结构关系。
- `role`
- `department`
- `phone`
- `mobile`
- `wechat`
- `whatsapp`
- `birthday`
- `language`
- `timezone`
- `personalNote`

需要拆成一对多结构：

- `contactInfo`：现在是单个字符串，后续应拆成多个联系方式。
- `factoryAddresses` / `shippingAddress`：后续应拆成多个地址或 ship-to 记录。
- 财务信息：付款方式、账户、偏好、币种不应长期压在组织单行里。
- 互动历史：目前只有 `lastInteraction`，后续应有邮件、会议、电话、拜访等独立记录。

## 3. Order / OrderLine

订单里已经存在若干关系智库引用字段，应继续保留并扩展。

主档引用候选：

- `customerRelationId`
- `millRelationId`
- `consigneeRelationId`
- `billToRelationId`
- 未来新增 `fabricAssetId` 或行级 `fabricAssetId`

订单快照字段：

- `customer`
- `factoryName`
- `millName`
- `millAddress`
- `millContact`
- `millPhone`
- `consigneeName`
- `consigneeAddress`
- `consigneeContact`
- `billToName`
- `billToAddress`
- `billToContact`
- `poNumber`
- `season`
- `poDate`
- `productionDate`
- `clientDate`
- `salesPrice`
- `contractAmount`
- `purchasePrice`
- `invoiceNumber`
- `invoiceDate`
- `shipmentDate`
- `shipmentQuantity`
- `shipmentAmount`
- `paymentInstrument`
- `paymentTerms`
- `expectedPaymentDate`
- `actualPaymentDate`
- `actualPaymentAmount`

这些字段应保留历史真实性，即使主档后续更新，也不能反向篡改历史订单。

面料匹配候选：

- `Order.fabricCode`
- `Order.fabricContent`
- `Order.width`
- `Order.gsm`
- `Order.clientCode`
- `Order.productColorCode`
- `Order.referenceBatch`
- `Order.productionBatch`
- `OrderLine.materialCode`
- `OrderLine.millQuality`
- `OrderLine.description`
- `OrderLine.width`
- `OrderLine.cloth`
- `OrderLine.weight`
- `OrderLine.category`

这些字段可用于从订单自动匹配或创建面料主档，但原订单值仍应作为业务快照保留。

## 4. SampleManager 相关字段

当前 `SampleManager` 仍是占位页面，尚未形成独立样品数据模型。

第一轮不从样品模块迁移字段，只预留未来方向：

- 样品记录应引用面料主档。
- 样品记录应引用客户组织、供应商组织和联系人。
- 调样历史、寄样记录、客户反馈、样品发票等属于样品业务记录，不属于面料主档本体。
- 面料主档详情页可以汇总展示历史调样，但不直接保存样品流程状态。

## 5. 迁移结论

- 第一轮只为订单/样品增加主档引用，不删除旧字段。
- `ProductAsset.subCategoryId` 暂保留，新增多维分类模型后再逐步迁移。
- `Relation` 暂保留兼容，新增结构化表后再逐步迁移组织、人员、ship-to、财务信息。
- 面料价格、客户编码、成分必须独立建表，不继续塞进 `ProductAsset`。
- 订单与订单行中的面料字段可用于匹配主档，但仍作为历史快照保留。
